---
phase: 01-feature-analysis-enhancement
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/features/horse_history_features.py
  - src/models/two_stage_return_model.py
  - src/pipelines/training_pipeline.py
  - tests/test_horse_history_features.py
  - tests/test_two_stage_return_model.py
autonomous: true
requirements:
  - FEAT-02

must_haves:
  truths:
    - "odds_to_ability_ratioがWinTwoStageModel._prepare_features()で推論時にも計算され、FEATURE_COLSに含まれている"
    - "class_drop_bounce, win_dominance, distance_change, surface_change, freshness_scoreがHorseHistoryFeatures.compute()で計算され、BASE_COLSに含まれている"
    - "5つの履歴ベース新特徴量がすべてWinTwoStageModel.FEATURE_COLSに追加されている"
    - "新特徴量がNaNで溢れず、合理的な値域(0-10等)にクリップされている"
    - "既存テストが全て引き続きパスする(後方互換性)"
  artifacts:
    - path: "src/features/horse_history_features.py"
      provides: "5つの新特徴量計算ロジックをcompute()ループに追加"
      min_lines: 1100
    - path: "src/models/two_stage_return_model.py"
      provides: "WinTwoStageModel.FEATURE_COLSに6新特徴量を追加、_prepare_features()に推論時計算を追加"
      contains: "odds_to_ability_ratio"
    - path: "src/pipelines/training_pipeline.py"
      provides: "odds_to_ability_ratio計算を_train_submodel()に追加"
      contains: "odds_to_ability_ratio"
    - path: "tests/test_horse_history_features.py"
      provides: "新特徴量のテストケース"
    - path: "tests/test_two_stage_return_model.py"
      provides: "FEATURE_COLS整合性テスト"
  key_links:
    - from: "src/features/horse_history_features.py"
      to: "BASE_COLS"
      via: "5 new feature names added to BASE_COLS list"
      pattern: "class_drop_bounce|win_dominance|distance_change|surface_change|freshness_score"
    - from: "src/models/two_stage_return_model.py"
      to: "FEATURE_COLS"
      via: "6 new feature names added (5 history + odds_to_ability_ratio)"
      pattern: "odds_to_ability_ratio|distance_change|surface_change|class_drop_bounce|win_dominance|freshness_score"
    - from: "src/pipelines/training_pipeline.py"
      to: "src/models/two_stage_return_model.py"
      via: "odds_to_ability_ratio computation after AbilityModel.train_oof()"
      pattern: "odds_to_ability_ratio"
    - from: "src/models/two_stage_return_model.py _prepare_features()"
      to: "odds_to_ability_ratio inference computation"
      via: "Compute from p_market_win_adj and p_ability_win when column missing from df"
      pattern: "odds_to_ability_ratio"
---

<objective>
単勝予測に特化した新特徴量を6つ追加し、特徴量エンジンに統合する。FEAT-02を満たす。

Purpose: RESEARCH.mdで特定された高影響特徴量(odds-to-ability比、クラス落リバウンド、距離/芝ダート変更要検知、勝利dominance、フレッシュネス)を実装し、単勝モデルの予測力を向上させる。
Output: 6つの新特徴量がHorseHistoryFeaturesとWinTwoStageModelに統合され、訓練・推論両パスで計算され、テスト済み
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-feature-analysis-enhancement/01-RESEARCH.md
@.planning/phases/01-feature-analysis-enhancement/01-01-SUMMARY.md

<interfaces>
<!-- Executor needs these contracts from the codebase -->

From src/features/horse_history_features.py:
```python
class HorseHistoryFeatures:
    BASE_COLS: list[str] = [
        "norm_finish_logit_avg", "harontimel5_avg", "harontimel5_zscore",
        "harontime_late_trend", "timediff_avg", "jyuni1c_avg", "jyuni4c_avg",
        "closing_index_avg", "kyakusitukubun_cd", "jockey_surprise", "jockey_cond_wr",
        "weight_absolute", "weight_zscore", "days_since_last_race", "rest_category",
        "form_trend", "form_consistency", "form_peak_flag",
        "class_move", "blinker_change", "is_nar_transfer", "nar_recent_ratio",
        "track_condition_delta",
    ]  # 23 current features

    def compute(self, race_df, entry_df, target_race_ids=None) -> pd.DataFrame:
        # Main loop at line 555: for i, row in enumerate(horses.itertuples(...))
        # New features append to results.append() dict at line 966
```

Key variables available in compute() loop scope (line 555-994):
```python
# After line 598: hist_idx (int) -- number of history races before target_date
# After line 579: n_past (int) -- number of valid past races (kakuteijyuni > 0, syussotosu >= 8)
# After line 603: days_since (float) -- days since last race
# After line 577: hp_kakuteijyuni (np.ndarray) -- past finish positions
# After line 578: hp_syussotosu (np.ndarray) -- past field sizes
# After line 898: class_move (float) -- current class - last class level
# Line 408-427: horse_arrs dict with keys: race_date, valid_field, kakuteijyuni,
#   syussotosu, harontimel3, distance_bin, surface, baba_cd, timediff, jyuni1c,
#   jyuni4c, kyakusitukubun, bataijyu, track_condition_code, gradecd, jyokencd1,
#   blinker, jyocd
# Line 586: history_mask -- (kakuteijyuni > 0) boolean mask
# Line 594: hist_start = max(0, hist_idx - self._n_past)
```

From src/models/two_stage_return_model.py:
```python
class WinTwoStageModel:
    FEATURE_COLS: list[str] = [ ... ]  # 27 features (may be fewer after Plan 01 noise removal)
    def _prepare_features(self, df: pd.DataFrame) -> pd.DataFrame:
        available_cols = [c for c in self.FEATURE_COLS if c in df.columns]
        features = df[available_cols].copy()
        # ... type conversions
        return features
    def train_hit_model(self, df: pd.DataFrame, *, num_threads: int = 0) -> None: ...
    def predict_ev(self, df: pd.DataFrame) -> pd.DataFrame: ...
```

From src/pipelines/training_pipeline.py:
```python
# Line 407: df = stage1.train_oof(df, n_folds=3, num_threads=num_threads)
# Line 408: oof_mask = df["p_ability_win"].notna()
# Line 409: df_oof = df[oof_mask].copy()
# Line 421: win_2s = WinTwoStageModel()
# Line 443: win_2s.train_hit_model(df_oof, num_threads=num_threads)
# Both p_market_win_adj (from FeatureEngine -> compute_market_bias) and
# p_ability_win (from AbilityModel.train_oof) are available in df_oof at line 409+
```

From src/backtest/race_predictor.py (inference path):
```python
# Line 89: df = submodel.market.predict_and_calc_error(df)  # produces p_market_win_adj
# Line 95: df = submodel.stage1.add_ability_probs(df)       # produces p_ability_win
# Line 97: df = submodel.win.predict_ev(df)                 # calls _prepare_features(df)
# Therefore: both p_market_win_adj and p_ability_win are available in df at predict_ev time
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HorseHistoryFeaturesに5つの新特徴量を追加</name>
  <files>src/features/horse_history_features.py, tests/test_horse_history_features.py</files>
  <read_first>
    - src/features/horse_history_features.py (entire file, especially compute() loop at lines 555-994 and BASE_COLS at lines 240-266)
    - tests/test_horse_history_features.py (existing test patterns)
    - src/features/form_cycle_features.py (reference for form-based feature pattern)
  </read_first>
  <behavior>
    - Test 1: distance_change returns 1.0 when current distance_bin differs from last race distance_bin
    - Test 2: distance_change returns 0.0 when current distance_bin equals last race distance_bin
    - Test 3: distance_change returns NaN when no history exists
    - Test 4: surface_change returns 1.0 when current surface differs from last race surface
    - Test 5: surface_change returns 0.0 when current surface equals last race surface
    - Test 6: surface_change returns NaN when no history exists
    - Test 7: class_drop_bounce returns positive value when class drops significantly (class_move < -0.5) AND recent performance is poor (normalized position > 0.5)
    - Test 8: class_drop_bounce returns 0.0 when class drops but recent performance is good
    - Test 9: class_drop_bounce returns NaN when fewer than 2 past races
    - Test 10: win_dominance returns average field size of past winning races
    - Test 11: win_dominance returns 0.0 when horse has no past wins (but has history)
    - Test 12: win_dominance returns NaN when no history exists
    - Test 13: freshness_score returns value in [0.0, 1.0] for horse with 3+ past races and valid days_since
    - Test 14: freshness_score returns NaN when fewer than 3 past races
    - Test 15: freshness_score returns optimal rest_score=1.0 for 30-60 day rest
    - Test 16: All 5 new features appear in BASE_COLS class attribute
  </behavior>
  <action>
    1. Add 5 new features to the compute() loop in `src/features/horse_history_features.py`.

    **Insertion point:** After the `track_condition_delta` computation (around line 958) and before `results.append()` (line 966).

    **distance_change** (binary flag: current distance_bin != last race distance_bin):
    ```python
    # distance_change: 距離変更要検知 (current distance_bin != last race distance_bin)
    if hist_idx > 0 and "distance_bin" in horse_arrs and "distance_bin" in dir(row):
        current_db = str(getattr(row, "distance_bin", "unknown"))
        last_db = str(horse_arrs["distance_bin"][history_mask][hist_start:hist_idx][-1])
        distance_change: float = 1.0 if current_db != last_db else 0.0
    else:
        distance_change = float("nan")
    ```

    **surface_change** (binary flag: current surface != last race surface):
    ```python
    # surface_change: 芝ダート変更要検知 (current surface != last race surface)
    if hist_idx > 0 and "surface" in horse_arrs and hasattr(row, "surface"):
        current_surf = str(getattr(row, "surface", ""))
        last_surf = str(horse_arrs["surface"][history_mask][hist_start:hist_idx][-1])
        surface_change: float = 1.0 if current_surf != last_surf else 0.0
    else:
        surface_change = float("nan")
    ```

    **class_drop_bounce** (クラス降級後のリバウンド期待値):
    ```python
    # class_drop_bounce: クラス落リバウンド (降級かつ直近成績悪化時に高い値)
    if hist_idx >= 2 and not np.isnan(class_move) and class_move < -0.5:
        # Get last 2 normalized finish positions
        recent_kj = hp_kakuteijyuni[-2:].astype(float)
        recent_ss = hp_syussotosu[-2:].astype(float)
        valid_recent = recent_ss > 1
        if valid_recent.any():
            norm_recent = (recent_kj[valid_recent] - 1) / (recent_ss[valid_recent] - 1)
            avg_recent = float(np.nanmean(norm_recent))
            # High bounce when dropping class AND performing poorly (high normalized position)
            class_drop_bounce: float = min(float(abs(class_move)) * avg_recent, 10.0) if avg_recent > 0.5 else 0.0
        else:
            class_drop_bounce = float("nan")
    elif not np.isnan(class_move):
        class_drop_bounce = 0.0  # Not a drop -- no bounce signal
    else:
        class_drop_bounce = float("nan")  # Insufficient data
    ```

    **win_dominance** (勝利時のフィールドサイズ平均 = 勝利の質):
    ```python
    # win_dominance: 勝利dominance (勝利時の平均フィールドサイズ -- 大きいほど価値ある勝利)
    if n_past > 0:
        win_mask = hp_kakuteijyuni == 1
        if win_mask.any():
            win_sizes = hp_syussotosu[win_mask].astype(float)
            valid_sizes = win_sizes[~np.isnan(win_sizes) & (win_sizes > 0)]
            win_dominance: float = float(np.mean(valid_sizes)) if len(valid_sizes) > 0 else float("nan")
        else:
            win_dominance = 0.0  # Has history but no wins -- meaningful signal (not NaN)
    else:
        win_dominance = float("nan")
    ```

    **freshness_score** (休息品質 x 直近フォーム品質):
    ```python
    # freshness_score: フレッシュネス (休息品質 x 直近フォーム品質)
    if not np.isnan(days_since) and n_past >= 3:
        # Rest score: optimal around 30-60 days
        if days_since <= 7:
            rest_score = 0.3  # 連戦疲労
        elif days_since <= 30:
            rest_score = 0.7  # 短期休息
        elif days_since <= 60:
            rest_score = 1.0  # 最適休息
        elif days_since <= 90:
            rest_score = 0.8  # やや長い
        else:
            rest_score = 0.4  # 長期休養明け
        # Form score from recent 3 starts
        recent_kj_f = hp_kakuteijyuni[-3:].astype(float)
        recent_ss_f = hp_syussotosu[-3:].astype(float)
        valid_recent_f = recent_ss_f > 1
        if valid_recent_f.any():
            norm_pos = (recent_kj_f[valid_recent_f] - 1) / (recent_ss_f[valid_recent_f] - 1)
            form_score = 1.0 - float(np.nanmean(norm_pos))
            freshness_score: float = max(rest_score * max(form_score, 0.0), 0.0)
        else:
            freshness_score = float("nan")
    else:
        freshness_score = float("nan")
    ```

    2. Add all 5 new feature names to the `results.append()` dict at line 966:
    ```python
    # After track_condition_delta at line 992:
    "distance_change": distance_change,
    "surface_change": surface_change,
    "class_drop_bounce": class_drop_bounce,
    "win_dominance": win_dominance,
    "freshness_score": freshness_score,
    ```

    3. Add all 5 new feature names to `BASE_COLS` list (line 240-266), after "track_condition_delta":
    ```python
    "distance_change",
    "surface_change",
    "class_drop_bounce",
    "win_dominance",
    "freshness_score",
    ```

    4. Add tests to `tests/test_horse_history_features.py`:
    - Create `TestDistanceChange` class with tests for same/different/missing distance_bin
    - Create `TestSurfaceChange` class with tests for same/different/missing surface
    - Create `TestClassDropBounce` class with tests for drop+poor, drop+good, insufficient data
    - Create `TestWinDominance` class with tests for wins with varying field sizes, no wins, no history
    - Create `TestFreshnessScore` class with tests for optimal rest, short rest, long rest, insufficient data
    - Each test uses mock data consistent with existing test patterns (mock ParquetStore, DataFrames with race_id, umaban, kettonum, etc.)
    - Test that all 5 new names appear in HorseHistoryFeatures.BASE_COLS
  </action>
  <verify>
    <automated>cd /c/Users/hirom/develop/keiba-ai && python -m pytest tests/test_horse_history_features.py -v -k "Distance or Surface or Drop or Dominance or Freshness or BASE_COLS"</automated>
  </verify>
  <done>
    - HorseHistoryFeatures.BASE_COLS contains 28 entries (23 existing + 5 new)
    - compute() produces DataFrame with 5 new columns
    - distance_change: 1.0/0.0/NaN depending on distance_bin change
    - surface_change: 1.0/0.0/NaN depending on surface change
    - class_drop_bounce: positive for class drop + poor form, 0.0 for non-drop, NaN for insufficient data
    - win_dominance: float for winners, 0.0 for non-winners with history, NaN for no history
    - freshness_score: [0.0, 1.0] range, NaN for insufficient data
    - All existing tests still pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 6新特徴量のWinTwoStageModel統合と推論パス対応</name>
  <files>src/models/two_stage_return_model.py, src/pipelines/training_pipeline.py, tests/test_two_stage_return_model.py</files>
  <read_first>
    - src/models/two_stage_return_model.py (FEATURE_COLS at lines 44-77, _prepare_features at lines 82-92, predict_ev at lines 164-178)
    - src/pipelines/training_pipeline.py (_train_submodel at lines 380-447, specifically lines 407-443)
    - src/backtest/race_predictor.py (inference path: lines 89-97 showing p_market_win_adj and p_ability_win availability)
    - tests/test_two_stage_return_model.py (existing test patterns)
  </read_first>
  <behavior>
    - Test 1: odds_to_ability_ratio is in WinTwoStageModel.FEATURE_COLS
    - Test 2: distance_change, surface_change, class_drop_bounce, win_dominance, freshness_score are all in WinTwoStageModel.FEATURE_COLS
    - Test 3: odds_to_ability_ratio computation produces ratio in [0.1, 10.0] range when both p_market_win_adj and p_ability_win are present
    - Test 4: odds_to_ability_ratio is NaN when p_ability_win is NaN
    - Test 5: odds_to_ability_ratio is clipped to [0.1, 10.0] for extreme values
    - Test 6: FEATURE_COLS has exactly 33 entries (27 original + 6 new) minus any removed from Plan 01
    - Test 7: _prepare_features computes odds_to_ability_ratio when the column is in FEATURE_COLS but missing from df (inference path)
    - Test 8: _prepare_features does NOT overwrite odds_to_ability_ratio if it already exists in df (training path)
  </behavior>
  <action>
    1. Add ALL 6 new feature names to `WinTwoStageModel.FEATURE_COLS` in `src/models/two_stage_return_model.py`:
    - Insert after `"pace_scenario_fit"` (last item, line 77):
      ```python
      # FEAT-02: 単勝特化新特徴量 (5 from HorseHistoryFeatures + 1 late-stage)
      "distance_change",
      "surface_change",
      "class_drop_bounce",
      "win_dominance",
      "freshness_score",
      # FEAT-02: 市場確率/能力確率比 (値 > 1.0 = 過小評価, < 1.0 = 過大評価)
      "odds_to_ability_ratio",
      ```

    2. Add odds_to_ability_ratio computation to the **inference path** in `WinTwoStageModel._prepare_features()`:
    - Modify `_prepare_features()` to compute `odds_to_ability_ratio` when it is in `FEATURE_COLS` but missing from the input DataFrame:
      ```python
    def _prepare_features(self, df: pd.DataFrame) -> pd.DataFrame:
        # FEAT-02: 推論時にodds_to_ability_ratioが未計算ならここで計算する
        # 訓練時は_train_submodel()で既に計算済みなのでスキップされる
        if "odds_to_ability_ratio" in self.FEATURE_COLS and "odds_to_ability_ratio" not in df.columns:
            if "p_market_win_adj" in df.columns and "p_ability_win" in df.columns:
                p_market = df["p_market_win_adj"].clip(lower=1e-6)
                p_ability = df["p_ability_win"].clip(lower=1e-6)
                df = df.copy()
                df["odds_to_ability_ratio"] = (p_market / p_ability).clip(0.1, 10.0)

        available_cols = [c for c in self.FEATURE_COLS if c in df.columns]
        features = df[available_cols].copy()
        # ... existing type conversion code ...
      ```
    - This handles the inference path (backtest, live trading) where `predict_ev()` is called on a DataFrame that has `p_market_win_adj` (from MarketModel) and `p_ability_win` (from AbilityModel) but not yet `odds_to_ability_ratio`.
    - The `df = df.copy()` avoids mutating the caller's DataFrame.
    - The condition `"odds_to_ability_ratio" not in df.columns` ensures training path (where `_train_submodel()` already computed it) is not affected.

    3. Add odds_to_ability_ratio computation to `_train_submodel()` in `src/pipelines/training_pipeline.py`:
    - After line 409 (`df_oof = df[oof_mask].copy()`), before WinTwoStageModel training (line 421):
      ```python
      # FEAT-02: odds_to_ability_ratio -- 市場確率と能力確率の比
      # p_market_win_adj は FeatureEngine.build_all() -> compute_market_bias() で生成済み
      # p_ability_win は直上の AbilityModel.train_oof() で生成済み
      if "p_market_win_adj" in df_oof.columns and "p_ability_win" in df_oof.columns:
          p_market = df_oof["p_market_win_adj"].clip(lower=1e-6)
          p_ability = df_oof["p_ability_win"].clip(lower=1e-6)
          df_oof["odds_to_ability_ratio"] = (p_market / p_ability).clip(0.1, 10.0)
      ```
    - This follows the RESEARCH.md Pattern 3 exactly, computing after AbilityModel produces p_ability_win and before WinTwoStageModel.train_hit_model() consumes it.

    4. Add tests to `tests/test_two_stage_return_model.py`:
    - Create `TestOddsToAbilityRatio` class:
      - Test that odds_to_ability_ratio is in FEATURE_COLS
      - Test computation with mock DataFrame containing p_market_win_adj and p_ability_win columns
      - Test that ratio = p_market / p_ability clipped to [0.1, 10.0]
      - Test edge cases: very high p_market / very low p_ability -> clipped to 10.0
      - Test edge cases: very low p_market / very high p_ability -> clipped to 0.1
      - Test NaN propagation when either input is NaN
    - Create `TestInferencePathComputation` class:
      - Test that _prepare_features computes odds_to_ability_ratio when column is missing but both p_market_win_adj and p_ability_win exist in df
      - Test that _prepare_features does NOT recompute odds_to_ability_ratio when it already exists in df
      - Test that _prepare_features skips computation when p_market_win_adj or p_ability_win is missing (no KeyError)
    - Create `TestHistoryFeaturesInFeatureCols` class:
      - Test that distance_change, surface_change, class_drop_bounce, win_dominance, freshness_score are all in FEATURE_COLS
      - Test FEATURE_COLS has no duplicates
      - Test FEATURE_COLS total length is >= 27 (may be more with new features, less if Plan 01 removed noise)
  </action>
  <verify>
    <automated>cd /c/Users/hirom/develop/keiba-ai && python -m pytest tests/test_two_stage_return_model.py -v</automated>
  </verify>
  <done>
    - All 6 new features (distance_change, surface_change, class_drop_bounce, win_dominance, freshness_score, odds_to_ability_ratio) appear in WinTwoStageModel.FEATURE_COLS
    - _train_submodel() computes odds_to_ability_ratio from p_market_win_adj and p_ability_win (training path)
    - _prepare_features() computes odds_to_ability_ratio when missing but inputs available (inference path)
    - _prepare_features() does NOT overwrite pre-existing odds_to_ability_ratio (idempotent)
    - Ratio is clipped to [0.1, 10.0] to prevent extreme values in both paths
    - Both columns verified present before computation (not KeyError)
    - All existing tests in test_two_stage_return_model.py still pass
    - New test cases cover edge cases (NaN, extreme ratios, missing columns, inference vs training path)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FeatureEngine output -> odds_to_ability_ratio | Depends on p_market_win_adj being computed correctly by compute_market_bias() |
| AbilityModel output -> odds_to_ability_ratio | Depends on p_ability_win being OOF-predicted (no leakage) |
| HorseHistoryFeatures -> new features | Past data must be strictly before target_date (PIT safety via searchsorted) |
| Inference path computation -> _prepare_features | odds_to_ability_ratio computed at prediction time from available columns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Information disclosure | p_market_win_adj usage | accept | Uses pre-race odds (tanodds), not confirmed_odds. Verified in feature_engine.py lines 139-143. |
| T-01-04 | Tampering | feature value ranges | mitigate | All new features clipped to reasonable ranges: odds_to_ability_ratio [0.1, 10.0], class_drop_bounce capped at 10.0, freshness_score [0.0, 1.0]. |
| T-01-05 | Elevation of privilege | PIT leakage via new features | mitigate | HorseHistoryFeatures.compute() uses searchsorted(target_date, side='left') for all past data access. New features use the same horse_arrs/history_mask that enforce PIT. Verified at lines 572-574 and 586-593. |
| T-01-06 | Elevation of privilege | Data leakage via odds_to_ability_ratio in inference | mitigate | Inference path uses pre-race p_market_win_adj (from tanodds) and p_ability_win (from AbilityModel). Neither uses post-race data. |
</threat_model>

<verification>
1. `python -m pytest tests/test_horse_history_features.py -v` -- all tests pass including new ones
2. `python -m pytest tests/test_two_stage_return_model.py -v` -- all tests pass including new ones
3. `grep -c "class_drop_bounce\|win_dominance\|distance_change\|surface_change\|freshness_score" src/features/horse_history_features.py` >= 5
4. `grep -c "class_drop_bounce\|win_dominance\|distance_change\|surface_change\|freshness_score\|odds_to_ability_ratio" src/models/two_stage_return_model.py` >= 6
5. `grep -c "odds_to_ability_ratio" src/pipelines/training_pipeline.py` >= 1
6. `grep -c "odds_to_ability_ratio" src/models/two_stage_return_model.py` >= 2 (FEATURE_COLS + _prepare_features computation)
7. `python -m pytest tests/ -v --co -q 2>&1 | grep -c "test_" ` -- no test collection errors
</verification>

<success_criteria>
1. 6つの新特徴量が実装され、テスト済み(distance_change, surface_change, class_drop_bounce, win_dominance, freshness_score, odds_to_ability_ratio)
2. HorseHistoryFeatures.BASE_COLSが5特徴量分拡張されている(23 -> 28)
3. WinTwoStageModel.FEATURE_COLSに6特徴量すべてが追加されている(27 -> 33+, Plan 01ノイズ除去分を減算)
4. _train_submodel()でp_ability_win生成後にodds_to_ability_ratioが計算される(訓練パス)
5. _prepare_features()で推論時にodds_to_ability_ratioが未計算なら自動計算される(推論パス)
6. 全新特徴量にNaN処理と値域クリップが適用されている
7. 既存テスト全てパス(後方互換性)
8. RESEARCH.mdのアンチパターンに従っていない(共有計算からの除外なし、pred_contrib追加列の処理)
</success_criteria>

<output>
After completion, create `.planning/phases/01-feature-analysis-enhancement/01-02-SUMMARY.md`
</output>
