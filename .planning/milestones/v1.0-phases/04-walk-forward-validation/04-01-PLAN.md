---
phase: 04-walk-forward-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/walk_forward_cv.py
  - scripts/run_wf_validation.py
  - tests/test_walk_forward_cv.py
autonomous: true
requirements: [VALI-01, VALI-02]

must_haves:
  truths:
    - "2フォールド(2024, 2025テスト)のウォークフォワード検証が実行され、各テスト年度のROIが個別に確認できる"
    - "訓練期間とテスト期間のROIギャップが分析され、過学習の兆候が評価されている"
    - "Feature importanceの年度間Spearman順位相関が計算され、安定性が評価されている"
    - "複数年度のプールROI(総払戻/総投資)が計算されている"
    - "3基準(ROI gap / 一貫性 / 安定性)の自動PASS/FAIL判定が実装されている"
    - "検証結果がJSON + MLflowに記録されている"
  artifacts:
    - path: "src/models/walk_forward_cv.py"
      provides: "FoldResult, WFValidationResult データクラス + extract_feature_ranking / compute_feature_stability / judge_overfitting ユーティリティ"
      contains: "class FoldResult"
    - path: "scripts/run_wf_validation.py"
      provides: "CLIエントリポイント: 2フォールドWF検証実行"
      exports: ["main"]
    - path: "data/backtest/wf_validation_result.json"
      provides: "検証結果JSON (実行後に生成)"
  key_links:
    - from: "scripts/run_wf_validation.py"
      to: "src/models/walk_forward_cv.py"
      via: "WFValidationResult, FoldResult, extract_feature_ranking, compute_feature_stability, judge_overfitting"
      pattern: "from models.walk_forward_cv import"
    - from: "scripts/run_wf_validation.py"
      to: "src/backtest/engine.py"
      via: "BacktestEngine(models=..., store=store).run(test_start, test_end)"
      pattern: "BacktestEngine"
    - from: "scripts/run_wf_validation.py"
      to: "src/pipelines/training_pipeline.py"
      via: "TrainingPipelineV5(store=store, model_dir=...).run(train_start, train_end)"
      pattern: "TrainingPipelineV5"
---

<objective>
Walk-forward検証インフラとCLIスクリプトを実装し、2フォールド(2024/2025テスト)で過学習検出・ROI検証を自動実行する

Purpose: Phase 1-3の改善(特徴量分析・Benterキャリブレーション・選択ゲート)が過学習していないことを証明し、ROI>100%が単年度の偶然でないことを確認する
Output: run_wf_validation.py スクリプト + WFValidationResultデータクラス + テスト
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-walk-forward-validation/04-CONTEXT.md
@.planning/phases/04-walk-forward-validation/04-RESEARCH.md

<interfaces>
<!-- Executor needs these contracts from the codebase -->

From src/models/walk_forward_cv.py:
```python
@dataclass
class Fold:
    fold_idx: int
    train_start: str
    train_end: str
    test_start: str
    test_end: str

@dataclass
class CVResult:
    folds: list[Fold]
    fold_results: list[Any]
    mean_roi: float
    std_roi: float
    max_drawdown: float

class WalkForwardCV:
    def __init__(self, pipeline=None, backtest_engine_factory=None,
                 train_years=4, test_years=1, step_years=1): ...
    def generate_folds(self, start_date, end_date) -> list[Fold]: ...
    def run(self, start_date, end_date) -> CVResult: ...
```

From src/backtest/engine.py:
```python
@dataclass
class BacktestResult:
    total_bets: int
    total_stake: float
    total_return: float
    winning_bets: int
    total_roi: float
    max_drawdown: float
    final_bankroll: float
    monthly_returns: dict[str, float]
    bet_history: list[dict[str, Any]]
    # property: profit -> total_return - total_stake

class BacktestEngine:
    def __init__(self, models: TrainedModelsV5, initial_bankroll=100_000,
                 store=None, betting_mode="flat", diag_prefix="bt"): ...
    def run(self, test_start: str, test_end: str) -> BacktestResult: ...
```

From src/domain/models.py:
```python
@dataclass
class SubmodelSet:
    stage1: AbilityModel
    win: WinTwoStageModel
    # ... other fields

@dataclass
class TrainedModelsV5:
    submodels: dict[str, SubmodelSet]  # keys: "turf", "dirt"
    quality_screener: RaceQualityScreener
    regime_detector: RegimeDetector
    train_period: tuple[str, str]
```

From src/features/win_feature_analysis.py:
```python
def analyze_feature_importance(model: lgb.Booster, features_df: pd.DataFrame, *, top_n=0) -> pd.DataFrame:
    # Returns DataFrame with ['feature', 'gain', 'mean_abs_shap'] sorted by mean_abs_shap desc
```

From scripts/run_backtest.py:
```python
ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, ROOT)
sys.path.insert(0, os.path.join(ROOT, "src"))
# _run_multi_year() pattern: per-year loop with TrainingPipelineV5 + BacktestEngine
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WFValidationResultデータクラスと過学習検出ユーティリティを実装</name>
  <files>src/models/walk_forward_cv.py, tests/test_walk_forward_cv.py</files>
  <read_first>
    - src/models/walk_forward_cv.py (拡張対象: 既存Fold/CVResultの後に追加)
    - tests/test_walk_forward_cv.py (テスト追加先: 既存テストクラスの後に追加)
    - .planning/phases/04-walk-forward-validation/04-RESEARCH.md (Pattern 1/2/3の設計)
    - .planning/phases/04-walk-forward-validation/04-CONTEXT.md (D-04, D-07, D-08, D-09, D-10, D-11, D-13)
  </read_first>
  <acceptance_criteria>
    - src/models/walk_forward_cv.py に `class FoldResult` データクラスが存在し、fields: fold_idx, train_start, train_end, test_start, test_end, train_roi, test_roi, roi_gap, train_bets, test_bets, train_stake, test_stake, train_return, test_return, top_features (list[str]), feature_ranking (dict[str,int]) を持つ
    - src/models/walk_forward_cv.py に `class WFValidationResult` データクラスが存在し、fields: folds (list[FoldResult]), pool_roi, weighted_roi, total_stake, total_return, total_bets, roi_gap_verdict, consistency_verdict, stability_verdict, overall_verdict, spearman_rho, roi_gap_max, git_hash を持つ
    - src/models/walk_forward_cv.py に `extract_feature_ranking(model: lgb.Booster, top_n: int = 10) -> tuple[dict[str, int], list[str]]` が存在する
    - src/models/walk_forward_cv.py に `compute_feature_stability(rankings: list[dict[str, int]], top_n: int = 10) -> float` が存在し、scipy.stats.spearmanr を使用する
    - src/models/walk_forward_cv.py に `judge_overfitting(result: WFValidationResult, warning_gap=0.20, fail_gap=0.30, min_rho=0.5) -> None` が存在する
    - src/models/walk_forward_cv.py の既存クラス(Fold, CVResult, WalkForwardCV)は一切変更しない(追加のみ)
    - python -m pytest tests/test_walk_forward_cv.py -v が通る
  </acceptance_criteria>
  <action>
Per D-04: WalkForwardCVを拡張してWFValidationResultデータクラスを追加する。ただし既存のFold/CVResult/WalkForwardCVクラスは一切変更せず、ファイル末尾に追加のみ行う。

1. src/models/walk_forward_cv.py の既存コード(imports含む)を一切変更せず、ファイル末尾に以下を追加する:

```python
# --- Phase 4: Walk-Forward Validation Infrastructure ---

import lightgbm as lgb
from scipy.stats import spearmanr


@dataclass
class FoldResult:
    """単一フォールドのWF検証結果"""
    fold_idx: int
    train_start: str
    train_end: str
    test_start: str
    test_end: str
    train_roi: float = 0.0
    test_roi: float = 0.0
    roi_gap: float = 0.0
    train_bets: int = 0
    test_bets: int = 0
    train_stake: float = 0.0
    test_stake: float = 0.0
    train_return: float = 0.0
    test_return: float = 0.0
    top_features: list[str] = field(default_factory=list)
    feature_ranking: dict[str, int] = field(default_factory=dict)


@dataclass
class WFValidationResult:
    """Walk-forward検証の全体結果"""
    folds: list[FoldResult] = field(default_factory=list)
    pool_roi: float = 0.0
    weighted_roi: float = 0.0
    total_stake: float = 0.0
    total_return: float = 0.0
    total_bets: int = 0
    roi_gap_verdict: str = "PASS"
    consistency_verdict: str = "PASS"
    stability_verdict: str = "PASS"
    overall_verdict: str = "PASS"
    spearman_rho: float = 0.0
    roi_gap_max: float = 0.0
    git_hash: str = ""
```

2. `extract_feature_ranking` 関数を追加:
```python
def extract_feature_ranking(
    model: lgb.Booster, top_n: int = 10
) -> tuple[dict[str, int], list[str]]:
    """LightGBMモデルからtop-N特徴量の順位を取得

    Args:
        model: 学習済みlgb.Booster
        top_n: 上位特徴量数

    Returns:
        (ranking_dict, top_features_list)
        ranking_dict: {feature_name: rank} (0=top, 1=2nd, ...)
        top_features_list: top-N特徴量名のリスト(順位順)
    """
    feature_names = model.feature_name()
    gain = model.feature_importance(importance_type="gain")
    sorted_features = sorted(zip(feature_names, gain), key=lambda x: -x[1])
    top_features = [f for f, _ in sorted_features[:top_n]]
    ranking = {f: rank for rank, (f, _) in enumerate(sorted_features[:top_n])}
    return ranking, top_features
```

3. `compute_feature_stability` 関数を追加 (Per D-09: Spearman rho < 0.5 で WARNING):
```python
def compute_feature_stability(
    rankings: list[dict[str, int]], top_n: int = 10,
) -> float:
    """複数フォールド間の特徴量順位相関(平均)を計算

    共通特徴量が3未満の場合はNaNを返す。
    """
    if len(rankings) < 2:
        return float("nan")

    all_features: set[str] = set()
    for r in rankings:
        top = sorted(r, key=r.get)[:top_n]  # type: ignore[arg-type]
        all_features.update(top)

    if len(all_features) < 3:
        return float("nan")

    rhos: list[float] = []
    for i in range(len(rankings) - 1):
        r1 = rankings[i]
        r2 = rankings[i + 1]
        common = [f for f in all_features if f in r1 and f in r2]
        if len(common) < 3:
            continue
        ranks1 = [r1[f] for f in common]
        ranks2 = [r2[f] for f in common]
        rho, _ = spearmanr(ranks1, ranks2)
        rhos.append(float(rho))

    return float(np.mean(rhos)) if rhos else float("nan")
```

4. `judge_overfitting` 関数を追加 (Per D-07, D-08, D-13):
```python
def judge_overfitting(
    result: WFValidationResult,
    warning_gap: float = 0.20,  # D-08
    fail_gap: float = 0.30,     # D-08
    min_rho: float = 0.5,       # D-09
) -> None:
    """3基準の自動判定を実行し、結果をresultに反映

    基準1 ROI gap (train - test の最大値):
      < 20% -> PASS, 20-30% -> WARNING, > 30% -> FAIL
    基準2 両年度ROI一貫性:
      全年度test_roi > 100% -> PASS, 一方のみ -> WARNING, 全<100% -> FAIL
    基準3 Feature importance安定性:
      rho >= 0.5 -> PASS, rho < 0.5 -> WARNING
    全PASS -> overall PASS, 一つでもFAIL -> overall FAIL, WARNINGのみ -> overall WARNING
    """
    gaps = [f.roi_gap for f in result.folds]
    max_gap = max(gaps) if gaps else 0.0
    result.roi_gap_max = max_gap
    if max_gap > fail_gap:
        result.roi_gap_verdict = "FAIL"
    elif max_gap > warning_gap:
        result.roi_gap_verdict = "WARNING"
    else:
        result.roi_gap_verdict = "PASS"

    test_rois = [f.test_roi for f in result.folds]
    above_100 = sum(1 for r in test_rois if r > 1.0)
    if len(test_rois) > 0 and above_100 == len(test_rois):
        result.consistency_verdict = "PASS"
    elif above_100 > 0:
        result.consistency_verdict = "WARNING"
    else:
        result.consistency_verdict = "FAIL"

    if not np.isnan(result.spearman_rho):
        if result.spearman_rho >= min_rho:
            result.stability_verdict = "PASS"
        else:
            result.stability_verdict = "WARNING"

    verdicts = [result.roi_gap_verdict, result.consistency_verdict, result.stability_verdict]
    if "FAIL" in verdicts:
        result.overall_verdict = "FAIL"
    elif "WARNING" in verdicts:
        result.overall_verdict = "WARNING"
    else:
        result.overall_verdict = "PASS"
```

5. tests/test_walk_forward_cv.py の末尾に新しいテストクラスを追加する。既存のテストクラス(TestWalkForwardCV等)は一切変更しない:

```python
class TestFoldResult:
    """FoldResult データクラスのテスト"""

    def test_fold_result_defaults(self) -> None:
        from models.walk_forward_cv import FoldResult
        fr = FoldResult(fold_idx=0, train_start="2020-01-01", train_end="2023-12-31",
                        test_start="2024-01-01", test_end="2024-12-31")
        assert fr.train_roi == 0.0
        assert fr.top_features == []
        assert fr.feature_ranking == {}

    def test_fold_result_roi_gap(self) -> None:
        from models.walk_forward_cv import FoldResult
        fr = FoldResult(fold_idx=0, train_start="2020-01-01", train_end="2023-12-31",
                        test_start="2024-01-01", test_end="2024-12-31",
                        train_roi=1.30, test_roi=1.05, roi_gap=0.25)
        assert fr.roi_gap == 0.25


class TestWFValidationResult:
    """WFValidationResult データクラスのテスト"""

    def test_defaults(self) -> None:
        from models.walk_forward_cv import WFValidationResult
        r = WFValidationResult()
        assert r.folds == []
        assert r.pool_roi == 0.0
        assert r.overall_verdict == "PASS"


class TestExtractFeatureRanking:
    """extract_feature_ranking のテスト"""

    def test_returns_top_features(self) -> None:
        from models.walk_forward_cv import extract_feature_ranking
        # Create a minimal mock Booster
        import lightgbm as lgb
        import numpy as np
        data = np.random.rand(100, 5)
        ds = lgb.Dataset(data, label=np.random.randint(0, 2, 100))
        model = lgb.train(
            {"objective": "binary", "verbose": -1, "num_leaves": 4},
            ds, num_boost_round=10,
            feature_names=["f1", "f2", "f3", "f4", "f5"],
        )
        ranking, top = extract_feature_ranking(model, top_n=3)
        assert len(top) == 3
        assert len(ranking) == 3
        assert all(f in ranking for f in top)


class TestComputeFeatureStability:
    """compute_feature_stability のテスト"""

    def test_identical_rankings(self) -> None:
        from models.walk_forward_cv import compute_feature_stability
        r1 = {"a": 0, "b": 1, "c": 2, "d": 3}
        r2 = {"a": 0, "b": 1, "c": 2, "d": 3}
        rho = compute_feature_stability([r1, r2])
        assert rho == 1.0

    def test_reversed_rankings(self) -> None:
        from models.walk_forward_cv import compute_feature_stability
        r1 = {"a": 0, "b": 1, "c": 2, "d": 3}
        r2 = {"a": 3, "b": 2, "c": 1, "d": 0}
        rho = compute_feature_stability([r1, r2])
        assert rho == -1.0

    def test_single_ranking_returns_nan(self) -> None:
        from models.walk_forward_cv import compute_feature_stability
        import math
        rho = compute_feature_stability([{"a": 0, "b": 1}])
        assert math.isnan(rho)


class TestJudgeOverfitting:
    """judge_overfitting のテスト (Per D-08, D-13)"""

    def _make_result(self, train_roi=1.2, test_roi=1.1, spearman_rho=0.8) -> "WFValidationResult":
        from models.walk_forward_cv import WFValidationResult, FoldResult
        return WFValidationResult(
            folds=[
                FoldResult(fold_idx=0, train_start="2020-01-01", train_end="2023-12-31",
                           test_start="2024-01-01", test_end="2024-12-31",
                           train_roi=train_roi, test_roi=test_roi,
                           roi_gap=train_roi - test_roi),
                FoldResult(fold_idx=1, train_start="2021-01-01", train_end="2024-12-31",
                           test_start="2025-01-01", test_end="2025-12-31",
                           train_roi=train_roi, test_roi=test_roi,
                           roi_gap=train_roi - test_roi),
            ],
            spearman_rho=spearman_rho,
        )

    def test_all_pass(self) -> None:
        from models.walk_forward_cv import judge_overfitting
        r = self._make_result(train_roi=1.15, test_roi=1.10, spearman_rho=0.8)
        judge_overfitting(r)
        assert r.overall_verdict == "PASS"

    def test_roi_gap_warning(self) -> None:
        """D-08: ROI gap 20-30% -> WARNING"""
        from models.walk_forward_cv import judge_overfitting
        r = self._make_result(train_roi=1.35, test_roi=1.10, spearman_rho=0.8)
        judge_overfitting(r)
        assert r.roi_gap_verdict == "WARNING"

    def test_roi_gap_fail(self) -> None:
        """D-08: ROI gap > 30% -> FAIL"""
        from models.walk_forward_cv import judge_overfitting
        r = self._make_result(train_roi=1.50, test_roi=1.10, spearman_rho=0.8)
        judge_overfitting(r)
        assert r.roi_gap_verdict == "FAIL"
        assert r.overall_verdict == "FAIL"

    def test_consistency_warning(self) -> None:
        """D-07: 一方のみ>100% -> WARNING"""
        from models.walk_forward_cv import judge_overfitting, FoldResult, WFValidationResult
        r = WFValidationResult(
            folds=[
                FoldResult(fold_idx=0, train_start="", train_end="", test_start="", test_end="",
                           train_roi=1.1, test_roi=1.05, roi_gap=0.05),
                FoldResult(fold_idx=1, train_start="", train_end="", test_start="", test_end="",
                           train_roi=1.1, test_roi=0.95, roi_gap=0.15),
            ],
            spearman_rho=0.8,
        )
        judge_overfitting(r)
        assert r.consistency_verdict == "WARNING"

    def test_stability_warning(self) -> None:
        """D-09: rho < 0.5 -> WARNING"""
        from models.walk_forward_cv import judge_overfitting
        r = self._make_result(train_roi=1.15, test_roi=1.10, spearman_rho=0.3)
        judge_overfitting(r)
        assert r.stability_verdict == "WARNING"
```

IMPORTANT: `import lightgbm as lgb` と `from scipy.stats import spearmanr` はファイル上部の既存importセクションに追加するのではなく、新規追加コードブロック内に配置する。既存の `if TYPE_CHECKING` インポートブロックや他のimport文は一切変更しない。`import numpy as np` は既存コード内の `run()` メソッドで使用されているため、関数レベルではなくモジュールレベルに昇格させる。具体的には、`walk_forward_cv.py` のimportセクションに `import numpy as np` を追加する(既存のrun()メソッド内の `import numpy as np` は削除する)。
  </action>
  <verify>
    <automated>cd C:/Users/hirom/develop/keiba-ai && python -m pytest tests/test_walk_forward_cv.py -v</automated>
  </verify>
  <done>
    - FoldResult, WFValidationResult データクラスが walk_forward_cv.py に追加されている
    - extract_feature_ranking, compute_feature_stability, judge_overfitting 関数が追加されている
    - 既存のFold, CVResult, WalkForwardCVクラスは一切変更されていない
    - 全テスト(test_walk_forward_cv.pyの新規+既存)が通過する
  </done>
</task>

<task type="auto">
  <name>Task 2: run_wf_validation.py CLIスクリプトを実装</name>
  <files>scripts/run_wf_validation.py</files>
  <read_first>
    - scripts/run_backtest.py (CLIパターン、_run_multi_year()パターンの流用元)
    - src/models/walk_forward_cv.py (Task 1で追加したWFValidationResult等を使用)
    - src/backtest/engine.py (BacktestEngineの使用方法)
    - src/domain/models.py (TrainedModelsV5.submodels の構造: dict[str, SubmodelSet])
    - .planning/phases/04-walk-forward-validation/04-CONTEXT.md (D-02, D-05, D-06, D-12, D-14)
    - .planning/phases/04-walk-forward-validation/04-RESEARCH.md (Pitfall 1-5, Code Examples 1-4)
  </read_first>
  <acceptance_criteria>
    - scripts/run_wf_validation.py が存在し、`python scripts/run_wf_validation.py --help` がエラーなく実行できる
    - FOLDS定数が2フォールド(2020-2023->2024, 2021-2024->2025)を含む (Per D-02)
    - 各フォールドで TrainingPipelineV5.run() + BacktestEngine.run() をtrain期間とtest期間の両方に実行する (Per D-05)
    - train期間とtest期間で別々のBacktestEngineインスタンスを作成する (Per Pitfall 2)
    - 芝/ダート両方のstage1 + win.hit_modelからfeature importanceを抽出する (Per Anti-Pattern 3)
    - プールROI(総払戻/総投資)とベット数加重ROIを計算する (Per D-10, D-11)
    - spearmanrでfeature importance安定性を評価する (Per D-09)
    - judge_overfitting()で3基準PASS/FAIL判定する (Per D-13)
    - 結果を data/backtest/wf_validation_result.json に保存する (Per D-14)
    - MLflow experiment "wf_validation" に結果を記録する (Per D-12)
    - フォールド完了ごとに途中結果をJSONに書き出す (Per Pitfall 4)
    - WFValidationResultをdictに変換してJSON出力する(to_dict()メソッドまたはdataclasses.asdict)
    - `python -m pytest tests/ -v --timeout=60 -k "not (backtest_engine or validation_suite or dry_run or etl)"` が通る(※WF検証スクリプト自体は実行しないためテストへの影響なし)
  </acceptance_criteria>
  <action>
Per D-06: 新規スクリプト `scripts/run_wf_validation.py` を作成する。run_backtest.pyとは独立したエントリポイント。

スクリプト構造(全体を1ファイルに実装):

```python
"""Walk-forward検証スクリプト (Phase 4)

2フォールド(2024, 2025テスト)のウォークフォワード検証を実行し、
過学習検出・ROI検証・feature importance安定性評価を自動実行する。

Usage:
    python scripts/run_wf_validation.py
"""
```

1. **ヘッダー・import** (run_backtest.pyと同じパターン):
```python
from __future__ import annotations
import argparse
import json
import logging
import os
import subprocess
import sys
import time
import warnings
from dataclasses import asdict
from pathlib import Path
from typing import Any

import numpy as np
import mlflow

warnings.filterwarnings("ignore")
if sys.platform == "win32":
    sys.stdout.reconfigure(encoding="utf-8", errors="replace")
    sys.stderr.reconfigure(encoding="utf-8", errors="replace")

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
sys.path.insert(0, ROOT)
sys.path.insert(0, os.path.join(ROOT, "src"))

logging.basicConfig(level=logging.INFO, format="%(asctime)s [%(levelname)s] %(message)s", datefmt="%H:%M:%S")
logger = logging.getLogger(__name__)
```

2. **FOLDS定数** (Per D-02, D-03):
```python
FOLDS = [
    {
        "train_start": "2020-01-01",
        "train_end": "2023-12-31",
        "test_start": "2024-01-01",
        "test_end": "2024-12-31",
    },
    {
        "train_start": "2021-01-01",
        "train_end": "2024-12-31",
        "test_start": "2025-01-01",
        "test_end": "2025-12-31",
    },
]
```

3. **_save_intermediate_result** 関数 (Per Pitfall 4: フォールド完了ごとにセーブ):
```python
def _save_intermediate_result(result_dict: dict[str, Any], path: Path) -> None:
    """フォールド完了ごとに途中結果をJSONに書き出す"""
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(json.dumps(result_dict, indent=2, ensure_ascii=False, default=str), encoding="utf-8")
```

4. **_extract_all_feature_rankings** 関数 (Per Anti-Pattern 3: 芝/ダート両方):
```python
def _extract_all_feature_rankings(models: Any) -> tuple[dict[str, int], list[str]]:
    """芝/ダート両方のstage1 + win.hit_modelからfeature importanceを統合して取得

    各サーフェスの各モデルからtop-10を取得し、統合ランキングを作成。
    複数モデルで出現する特徴量は最高順位を採用。
    """
    from models.walk_forward_cv import extract_feature_ranking

    all_rankings: dict[str, int] = {}
    all_top_features: list[str] = []

    for surface, sub in models.submodels.items():
        # Stage1 (AbilityModel)
        if hasattr(sub.stage1, "model") and sub.stage1.model is not None:
            ranking, top = extract_feature_ranking(sub.stage1.model, top_n=10)
            for f, r in ranking.items():
                if f not in all_rankings or r < all_rankings[f]:
                    all_rankings[f] = r
            all_top_features.extend(top)

        # Win hit model
        if hasattr(sub.win, "hit_model") and sub.win.hit_model is not None:
            ranking, top = extract_feature_ranking(sub.win.hit_model, top_n=10)
            for f, r in ranking.items():
                if f not in all_rankings or r < all_rankings[f]:
                    all_rankings[f] = r
            all_top_features.extend(top)

    # top_features: 出現頻度順でソート(複数モデルで出現する特徴量を優先)
    from collections import Counter
    freq = Counter(all_top_features)
    top_features = [f for f, _ in freq.most_common(10)]

    return all_rankings, top_features
```

5. **main()** 関数: WF検証のメインループ

```python
def main() -> None:
    from db.parquet_store import ParquetStore

    store = ParquetStore()
    if not store.exists("raw", "races"):
        logger.error("Parquetデータが見つかりません。先に run_etl.py を実行してください。")
        sys.exit(1)

    # git hash
    try:
        git_hash = subprocess.check_output(["git", "rev-parse", "HEAD"], text=True).strip()[:7]
    except (subprocess.CalledProcessError, FileNotFoundError):
        git_hash = "unknown"

    from models.walk_forward_cv import (
        FoldResult, WFValidationResult,
        compute_feature_stability, judge_overfitting,
    )

    wf_result = WFValidationResult(git_hash=git_hash)
    output_path = Path(ROOT) / "data" / "backtest" / "wf_validation_result.json"

    # 予想時間表示 (Per Pitfall 4)
    est_per_fold_min = 120  # ~2時間/フォールド (train bt + test bt)
    print(f"=== Walk-Forward Validation ===")
    print(f"Folds: {len(FOLDS)}")
    print(f"Estimated time: ~{est_per_fold_min * len(FOLDS)} min")
    print()

    fold_rankings: list[dict[str, int]] = []

    for i, fold_def in enumerate(FOLDS):
        print(f"--- Fold {i}: train {fold_def['train_start']}~{fold_def['train_end']}, "
              f"test {fold_def['test_start']}~{fold_def['test_end']} ---")
        logger.info("Fold %d: train %s~%s, test %s~%s",
                    i, fold_def["train_start"], fold_def["train_end"],
                    fold_def["test_start"], fold_def["test_end"])

        # 1. 学習
        t0 = time.time()
        year_model_dir = Path("data/models-wf-validation") / str(i)
        year_model_dir.mkdir(parents=True, exist_ok=True)

        from pipelines.training_pipeline import TrainingPipelineV5
        pipeline = TrainingPipelineV5(store=store, model_dir=year_model_dir)
        models = pipeline.run(fold_def["train_start"], fold_def["train_end"])
        elapsed_train = time.time() - t0
        logger.info("Fold %d: 学習完了 (%.0f秒)", i, elapsed_train)

        # 2. Feature importance抽出
        ranking, top_features = _extract_all_feature_rankings(models)
        fold_rankings.append(ranking)
        logger.info("Fold %d: top features = %s", i, top_features[:5])

        # 3a. Test期間バックテスト
        t1 = time.time()
        from backtest.engine import BacktestEngine
        test_engine = BacktestEngine(
            models=models, store=store, diag_prefix=f"wf_{i}_test",
        )
        test_result = test_engine.run(fold_def["test_start"], fold_def["test_end"])
        elapsed_test = time.time() - t1
        logger.info("Fold %d: テストBT完了 (%.0f秒) ROI=%.1f%%", i, elapsed_test, test_result.total_roi * 100)

        # 3b. Train期間バックテスト (Per D-05)
        # 別インスタンスで状態汚染を回避 (Per Pitfall 2)
        t2 = time.time()
        train_engine = BacktestEngine(
            models=models, store=store, diag_prefix=f"wf_{i}_train",
        )
        train_result = train_engine.run(fold_def["train_start"], fold_def["train_end"])
        elapsed_train_bt = time.time() - t2
        logger.info("Fold %d: 学習BT完了 (%.0f秒) ROI=%.1f%%", i, elapsed_train_bt, train_result.total_roi * 100)

        # 4. FoldResult作成
        fold_result = FoldResult(
            fold_idx=i,
            train_start=fold_def["train_start"],
            train_end=fold_def["train_end"],
            test_start=fold_def["test_start"],
            test_end=fold_def["test_end"],
            train_roi=train_result.total_roi,
            test_roi=test_result.total_roi,
            roi_gap=train_result.total_roi - test_result.total_roi,
            train_bets=train_result.total_bets,
            test_bets=test_result.total_bets,
            train_stake=train_result.total_stake,
            test_stake=test_result.total_stake,
            train_return=train_result.total_return,
            test_return=test_result.total_return,
            top_features=top_features,
            feature_ranking=ranking,
        )
        wf_result.folds.append(fold_result)

        # 5. 途中結果をセーブ (Per Pitfall 4)
        result_dict = asdict(wf_result)
        _save_intermediate_result(result_dict, output_path)
        logger.info("Fold %d: 途中結果保存 -> %s", i, output_path)

        print(f"  学習: %.0f秒 | テストBT: %.0f秒 | 学習BT: %.0f秒" %
              (elapsed_train, elapsed_test, elapsed_train_bt))
        print(f"  Train ROI: %.1f%% | Test ROI: %.1f%% | Gap: %.1f%%" %
              (train_result.total_roi * 100, test_result.total_roi * 100,
               (train_result.total_roi - test_result.total_roi) * 100))
        print(f"  Test bets: %d | Test stake: %.0f | Test return: %.0f" %
              (test_result.total_bets, test_result.total_stake, test_result.total_return))
        print()

    # 6. 集計 (Per D-10, D-11)
    total_test_stake = sum(f.test_stake for f in wf_result.folds)
    total_test_return = sum(f.test_return for f in wf_result.folds)
    total_test_bets = sum(f.test_bets for f in wf_result.folds)
    wf_result.total_stake = total_test_stake
    wf_result.total_return = total_test_return
    wf_result.total_bets = total_test_bets

    # D-10: プールROI
    wf_result.pool_roi = total_test_return / total_test_stake if total_test_stake > 0 else 0.0

    # D-11: ベット数加重ROI
    if total_test_bets > 0:
        wf_result.weighted_roi = sum(f.test_roi * f.test_bets for f in wf_result.folds) / total_test_bets
    else:
        wf_result.weighted_roi = 0.0

    # 7. Feature importance安定性 (Per D-09)
    rho = compute_feature_stability(fold_rankings)
    wf_result.spearman_rho = rho
    logger.info("Feature stability Spearman rho = %.3f", rho)

    # 8. 過学習判定 (Per D-07, D-08, D-13)
    judge_overfitting(wf_result)

    # 9. MLflow記録 (Per D-12)
    mlflow.set_experiment("wf_validation")
    with mlflow.start_run(run_name=f"wf_{FOLDS[0]['test_start'][:4]}_{FOLDS[-1]['test_end'][:4]}"):
        mlflow.log_params({
            "n_folds": len(FOLDS),
            "train_years": 4,
            "test_years": 1,
            "git_hash": git_hash,
        })
        mlflow.log_metrics({
            "pool_roi": wf_result.pool_roi,
            "weighted_roi": wf_result.weighted_roi,
            "spearman_rho": float(rho) if not np.isnan(rho) else -1.0,
            "roi_gap_max": wf_result.roi_gap_max,
            "total_bets": wf_result.total_bets,
        })
        for i, fold in enumerate(wf_result.folds):
            mlflow.log_metrics({
                f"fold_{i}_train_roi": fold.train_roi,
                f"fold_{i}_test_roi": fold.test_roi,
                f"fold_{i}_roi_gap": fold.roi_gap,
                f"fold_{i}_test_bets": fold.test_bets,
            })
        mlflow.set_tag("verdict", wf_result.overall_verdict)

    # 10. 最終結果保存 (Per D-14)
    result_dict = asdict(wf_result)
    _save_intermediate_result(result_dict, output_path)

    # 11. 結果表示
    print("=" * 60)
    print("  Walk-Forward Validation Result")
    print("=" * 60)
    for fold in wf_result.folds:
        print(f"  Fold {fold.fold_idx}: Train ROI={fold.train_roi:.1%} | "
              f"Test ROI={fold.test_roi:.1%} | Gap={fold.roi_gap:.1%}")
    print(f"  Pool ROI (D-10):         {wf_result.pool_roi:.1%}")
    print(f"  Weighted ROI (D-11):     {wf_result.weighted_roi:.1%}")
    print(f"  Feature Stability rho:   {wf_result.spearman_rho:.3f}")
    print(f"  ROI Gap Max:             {wf_result.roi_gap_max:.1%}")
    print()
    print(f"  ROI Gap Verdict:         {wf_result.roi_gap_verdict}")
    print(f"  Consistency Verdict:     {wf_result.consistency_verdict}")
    print(f"  Stability Verdict:       {wf_result.stability_verdict}")
    print(f"  *** Overall Verdict:     {wf_result.overall_verdict} ***")
    print()
    print(f"  Result saved: {output_path}")
    print(f"  MLflow experiment: wf_validation")


if __name__ == "__main__":
    main()
```

重要な設計ポイント(RESEARCH.md Pitfall対応):
- **Pitfall 1**: WalkForwardCV.generate_folds()は使わず、FOLDS定数でハードコード
- **Pitfall 2**: train/testで別BacktestEngineインスタンス(状態汚染回避)
- **Pitfall 3**: _extract_all_feature_rankingsで共通特徴量のみ比較(compute_feature_stability内で処理)
- **Pitfall 4**: フォールド完了ごとに_save_intermediate_resultで途中セーブ
- **Pitfall 5**: MLflow experiment名を"wf_validation"に設定(既存experimentと混在回避)
  </action>
  <verify>
    <automated>cd C:/Users/hirom/develop/keiba-ai && python scripts/run_wf_validation.py --help 2>&1 || python -c "import ast; ast.parse(open('scripts/run_wf_validation.py').read()); print('Syntax OK')" && python -m pytest tests/test_walk_forward_cv.py -v</automated>
  </verify>
  <done>
    - scripts/run_wf_validation.py が存在し、構文エラーがない
    - FOLDS定数が2フォールド(2020-2023->2024, 2021-2024->2025)を定義している
    - 各フォールドでTrainingPipelineV5 + BacktestEngine(train+test)を実行する構造
    - train/testで別BacktestEngineインスタンスを作成する
    - Feature importance抽出(芝/ダートstage1+win.hit_model)が実装されている
    - プールROI + ベット数加重ROI + Spearman rho + judge_overfitting判定を実行
    - 途中結果セーブ + 最終JSON出力(data/backtest/wf_validation_result.json)
    - MLflow experiment "wf_validation" に記録
    - 既存テストが全て通過する
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script -> Parquet files | 読み取り専用。WF検証はデータを変更しない |
| Script -> MLflow local | ローカルMLflow(tracking URI未設定時は./mlruns)への書き込み |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | wf_validation_result.json | accept | ローカルファイル。検証結果の改ざんリスクは低い(データは再現可能) |
| T-04-02 | Repudiation | MLflow experiment記録 | accept | git_hash記録で再現性担保。認証なしローカルMLflow |
</threat_model>

<verification>
1. python -m pytest tests/test_walk_forward_cv.py -v -- 新規テスト全通過
2. python -c "import ast; ast.parse(open('scripts/run_wf_validation.py').read()); print('OK')" -- 構文チェック
3. grep -c "class FoldResult" src/models/walk_forward_cv.py -- FoldResultデータクラス存在確認
4. grep -c "class WFValidationResult" src/models/walk_forward_cv.py -- WFValidationResultデータクラス存在確認
5. grep -c "def judge_overfitting" src/models/walk_forward_cv.py -- judge_overfitting関数存在確認
6. grep -c "FOLDS" scripts/run_wf_validation.py -- フォールド定義存在確認
</verification>

<success_criteria>
- FoldResult, WFValidationResultデータクラスがwalk_forward_cv.pyに実装されている
- extract_feature_ranking, compute_feature_stability, judge_overfittingがwalk_forward_cv.pyに実装されている
- run_wf_validation.pyが2フォールド定義 + train/test別BT + feature importance + 過学習判定を実装している
- 新規テスト(walk_forward_cv拡張分)が全て通過する
- 既存テストが回帰していない
</success_criteria>

<output>
After completion, create `.planning/phases/04-walk-forward-validation/04-01-SUMMARY.md`
</output>
