---
phase: 05-foundation-features
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/features/odds_dynamics_features.py
  - src/models/stage1_ability_model.py
  - src/models/two_stage_return_model.py
  - tests/test_odds_dynamics_features.py
autonomous: true
requirements: [ODTS-01, ODTS-02]

must_haves:
  truths:
    - "odds_accelerationがオッズ変動の2次微分として計算され、steam moveを検出できる"
    - "odds_direction_consistencyがオッズ変動方向の時間加重一貫性として計算される"
    - "スナップショット不足時はNaNとなり、LightGBMが自然処理する"
  artifacts:
    - path: "src/features/odds_dynamics_features.py"
      provides: "odds_acceleration, odds_direction_consistencyの計算"
      contains: "odds_acceleration"
    - path: "src/models/stage1_ability_model.py"
      provides: "新特徴量を含むFEATURE_COLS (05-01の更新に追加)"
      contains: "odds_acceleration"
    - path: "src/models/two_stage_return_model.py"
      provides: "新特徴量を含むFEATURE_COLS"
      contains: "odds_acceleration"
  key_links:
    - from: "src/features/odds_dynamics_features.py"
      to: "compute_odds_dynamics()"
      via: "既存odds_10/odds_30/odds_60値を利用した3点差分計算"
      pattern: "odds_acceleration"
    - from: "src/features/odds_dynamics_features.py"
      to: "compute_odds_dynamics()"
      via: "スナップショットgroupbyを利用した方向一貫性計算"
      pattern: "odds_direction_consistency"
    - from: "src/models/two_stage_return_model.py"
      to: "WinTwoStageModel.FEATURE_COLS"
      via: "odds系特徴量グループへの新列追加"
      pattern: "odds_acceleration.*odds_direction_consistency"
---

<objective>
オッズ変動特徴量 ODTS-01 (2次微分/加速度) と ODTS-02 (方向一貫性) を odds_dynamics_features.py に追加する。

Purpose: オッズ時系列データからsteam moveの強さと持続的スマートマネー流入を検出する特徴量を追加し、Stage2モデルが市場ダイナミクスをより深く理解できるようにする。
Output: odds_dynamics_features.py の拡張 + モデルFEATURE_COLS更新 + テスト
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-foundation-features/05-CONTEXT.md
@.planning/phases/05-foundation-features/05-RESEARCH.md
@.planning/phases/05-foundation-features/05-01-SUMMARY.md

<interfaces>
<!-- Executor needs these contracts from the codebase -->

From src/features/odds_dynamics_features.py:
```python
def compute_odds_dynamics(df: pd.DataFrame, odds_ts: pd.DataFrame | None) -> pd.DataFrame:
    # line 112 — メイン関数
    # 入力: df (race_id, umaban), odds_ts (race_id, happyotime, umaban, tanodds)
    # 出力: odds_drop_rate_60_10, odds_drop_rate_30_10, odds_velocity,
    #        odds_volatility, popularity_change_30_10 列が追加された df

    # Key intermediate values:
    # odds_10: pd.Series — t-10minのオッズ (line 171, _pick_target_snapshot)
    # odds_30: pd.Series — t-30minのオッズ (line 172)
    # odds_60: pd.Series — t-60minのオッズ (line 173)
    # base_index: pd.MultiIndex — (race_id, umaban) (line 174)
    # ts: DataFrame — ソート済みオッズ時系列 (line 142)
    # ts["_odds_diff"]: groupby差分 (line 216)
    # ts["_mins_before_anchor"]: 発走前分数 (line 167-169)
    # grouped: ts.groupby(["race_id", "umaban"]) (line 158)

    # nan_cols (line 129-135): None/empty時のNaN列定義
```

From src/models/two_stage_return_model.py:
```python
class WinTwoStageModel:
    FEATURE_COLS: list[str] = [...]  # line 47 — odds系特徴量の後に新列を追加
    # 既存odds系: odds_drop_rate_60_10, odds_drop_rate_30_10, odds_velocity, odds_volatility, popularity_change_30_10
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ODTS-01~02 — オッズ加速度と方向一貫性を compute_odds_dynamics に追加</name>
  <files>src/features/odds_dynamics_features.py, tests/test_odds_dynamics_features.py, src/models/stage1_ability_model.py, src/models/two_stage_return_model.py</files>
  <read_first>
    src/features/odds_dynamics_features.py (全体、特にlines 112-243 compute_odds_dynamics, lines 170-177 odds_10/30/60取得, lines 216-221 volatility, lines 234-241 agg_df構築)
    tests/test_odds_dynamics_features.py (全体、特にodds_ts_df fixture)
    src/models/stage1_ability_model.py (FEATURE_COLS — 05-01で既に更新済み)
    src/models/two_stage_return_model.py (FEATURE_COLS — 05-01で既に更新済み)
  </read_first>
  <behavior>
    - Test 1: odds_acceleration が正の値 (steam move) を返すこと — オッズが加速的に低下するケース
    - Test 2: odds_acceleration が負の値を返すこと — オッズが加速的に上昇するケース
    - Test 3: odds_acceleration がスナップショット不足(<3点)でNaNになること
    - Test 4: odds_direction_consistency が1.0 (完全一貫) を返すこと — 全て同方向の変動
    - Test 5: odds_direction_consistency が0.0 (完全不一致) を返すこと — 交互方向の変動
    - Test 6: odds_direction_consistency がスナップショット不足(<5点)でNaNになること (D-21)
  </behavior>
  <action>
    **ODTS-01: odds_acceleration — 2次微分(加速度) (D-16, D-17, D-18)**

    `compute_odds_dynamics()` 内で、既存の odds_10/odds_30/odds_60 Series (lines 171-177) を使用して3点差分を計算:

    1. `nan_cols` (line 129) に `"odds_acceleration"` と `"odds_direction_consistency"` を追加
    2. odds_10/odds_30/odds_60 が reindex 済み (line 175-177) の後に追加:
       ```python
       # ODTS-01: オッズ加速度 (2次微分) — D-16, D-17
       # 3点差分型: velocity_early (t-60→t-30) と velocity_late (t-30→t-10) の差分
       # 正 = オッズ低下が加速 (steam move)
       vel_early = (odds_30 - odds_60) / 30.0
       vel_late = (odds_10 - odds_30) / 20.0
       odds_acceleration = vel_late - vel_early
       odds_acceleration.name = "odds_acceleration"
       ```
    3. 3点のうちいずれかがNaNの場合、自動的にNaNになる (D-18) — numpyの演算で自然伝播
    4. agg_df (line 235) の concat リストに `odds_acceleration` を追加

    **ODTS-02: odds_direction_consistency — 方向一貫性 (D-19, D-20, D-21)**

    `compute_odds_dynamics()` 内で、既存の `ts` DataFrame と `_odds_diff` 列 (line 216) を使用:

    1. `_odds_diff` が既に計算済み (line 216: `ts["_odds_diff"] = ts.groupby(["race_id", "umaban"])["tanodds"].diff()`)
    2. `_odds_diff` 計算の直後に方向計算を追加:
       ```python
       # ODTS-02: オッズ方向一貫性 — D-19, D-20, D-21
       ts["_odds_dir"] = np.sign(ts["_odds_diff"])
       ```
    3. groupby で各 (race_id, umaban) の一貫性を計算:
       ```python
       def _compute_direction_consistency(group: pd.DataFrame) -> float:
           n = len(group)
           if n < 5:
               return np.nan  # D-21: 最小5点
           # halflife = n/4 (Claude's Discretion: 直近ほど高く評価)
           halflife_snaps = max(n / 4, 1)
           decay_rate = np.log(2) / halflife_snaps
           # 各スナップショットの順序: group内で _ts_datetime 降順(直近→古い)
           # 方向: _odds_dir 値 (+1, -1, 0)
           dirs = group.sort_values("_ts_datetime", ascending=False)["_odds_dir"].dropna().values
           if len(dirs) < 5:
               return np.nan
           weights = np.array([(1 - decay_rate) ** i for i in range(len(dirs))])
           weights = weights / weights.sum()
           consistency = float(abs(np.sum(weights * dirs)) / np.sum(weights))
           return consistency

       direction_consistency = (
           ts[ts["_odds_dir"].notna()]
           .groupby(["race_id", "umaban"])
           .apply(_compute_direction_consistency)
       )
       direction_consistency.name = "odds_direction_consistency"
       ```
    4. agg_df (line 235) の concat リストに `direction_consistency` を追加
    5. base_index に reindex してNaN を埋める:
       ```python
       direction_consistency = direction_consistency.reindex(base_index)
       ```

    **モデル FEATURE_COLS 更新:**

    `two_stage_return_model.py` — WinTwoStageModel.FEATURE_COLS の odds系特徴量グループに追加:
    - `"odds_acceleration"` — odds_volatility の後
    - `"odds_direction_consistency"` — odds_acceleration の後

    `stage1_ability_model.py` — AbilityModel にはオッズ特徴量を含めない (Rule 1: Stage1はオッズ不入力)。よって追加不要。

    **テスト追加:**

    `tests/test_odds_dynamics_features.py` に追加:
    - odds_acceleration テスト: fixtureのodds_ts_df (umaban=2: オッズ低下 10.0→5.0) で正の加速度を検証
      - umaban=2: odds_60=10.0, odds_30=7.0, odds_10=5.0 (概算)
      - vel_early = (7-10)/30 = -0.1, vel_late = (5-7)/20 = -0.1
      - acceleration = -0.1 - (-0.1) = 0.0 (概算)
      - より明確なテストケースが必要: oddsが加速的に低下するケース (例: odds_60=10, odds_30=7, odds_10=3)
      - vel_early = (7-10)/30 = -0.1, vel_late = (3-7)/20 = -0.2, accel = -0.1
      - 負 = オッズ低下が加速 = steam move (オッズは下がる方が「人気が上がる」方向)
    - odds_acceleration NaN テスト: odds_ts=None の場合にNaN
    - odds_direction_consistency テスト: 全て同方向 (オッズ低下のみ) の場合に ≈ 1.0
    - odds_direction_consistency テスト: 交互方向の場合に < 1.0
    - odds_direction_consistency NaN テスト: スナップショット<5点でNaN
  </action>
  <verify>
    <automated>cd C:/Users/hirom/develop/keiba-ai && python -m pytest tests/test_odds_dynamics_features.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
    - odds_acceleration が compute_odds_dynamics で計算される
    - odds_direction_consistency が compute_odds_dynamics で計算される
    - WinTwoStageModel.FEATURE_COLS に odds_acceleration, odds_direction_consistency が含まれる
    - AbilityModel.FEATURE_COLS には odds_acceleration, odds_direction_consistency が含まれない (Stage1 Rule 1)
    - 全テスト通過
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Odds time-series → Feature computation | オッズスナップショットの不足や品質が特徴量に直接影響 |
| Feature computation → Model training | NaN率が高い場合、モデルの学習データが減少する可能性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | I (Information) | odds_acceleration 時間窓 | accept | 既存の _pick_target_snapshot (tolerance=15-20min) を使用。snapshot密度が低いレースでは自然にNaNになる |
| T-05-05 | I (Information) | odds_direction_consistency NaN率 | mitigate | 最小5点要件 (D-21) でNaN率が高い可能性あり。実装後にNaN率を計測し、50%超の場合は最小3点に緩和 (Claude's Discretion) |
| T-05-06 | I (Information) | odds_acceleration スケール | accept | オッズレベルに依存する絶対値。odds_velocity等と同スケール。LightGBMは木分割でスケール不変 |
</threat_model>

<verification>
1. `python -m pytest tests/test_odds_dynamics_features.py -v` 全テスト通過
2. `python -c "from features.odds_dynamics_features import compute_odds_dynamics; import pandas as pd; df = pd.DataFrame({'race_id': ['R1'], 'umaban': [1]}); result = compute_odds_dynamics(df, None); assert 'odds_acceleration' in result.columns and 'odds_direction_consistency' in result.columns"`
3. `python -c "from models.two_stage_return_model import WinTwoStageModel; cols = WinTwoStageModel.FEATURE_COLS; assert 'odds_acceleration' in cols and 'odds_direction_consistency' in cols"`
4. `ruff check src/features/odds_dynamics_features.py` lint通過
5. `mypy src/features/odds_dynamics_features.py src/models/two_stage_return_model.py` 型チェック通過
</verification>

<success_criteria>
- odds_acceleration が既存odds_10/30/60値を用いた3点差分で計算される
- odds_direction_consistency が時間加重型で0〜1の値をとる
- スナップショット不足時(<3点 for acceleration, <5点 for consistency)にNaN
- WinTwoStageModel.FEATURE_COLS に新特徴量が追加される
- AbilityModel には追加されない (Stage1はオッズ不入力)
- 全テスト通過、ruff lint通過、mypy型チェック通過
</success_criteria>

<output>
After completion, create `.planning/phases/05-foundation-features/05-02-SUMMARY.md`
</output>
