---
phase: 08-win-backtest-core
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/backtest/race_predictor.py
  - scripts/run_wf_validation.py
autonomous: true
requirements:
  - WIN-03
  - WIN-05

must_haves:
  truths:
    - "get_win_candidates()がwin_selection_edge>0 AND tanodds>=1.0でフィルタリングする"
    - "候補がwin_gate_score降順でソートされ、上位2頭が返される"
    - "select_bets()がbetting_target='win'時にget_win_candidates()を呼び出しBetType.WINのBetを生成する"
    - "conformal_confidence_scoreがsoft ranking signalとして使用される(hard filterではない)"
    - "run_wf_validation.pyが--betting-target引数を受け取りBacktestEngineに渡す"
  artifacts:
    - path: "src/backtest/race_predictor.py"
      provides: "get_win_candidates(), select_bets() win path"
      contains: "def get_win_candidates"
    - path: "scripts/run_wf_validation.py"
      provides: "--betting-target CLI argument"
      contains: "betting-target"
  key_links:
    - from: "src/backtest/engine.py"
      to: "src/backtest/race_predictor.py"
      via: "get_win_candidates() dispatch"
      pattern: "get_win_candidates"
    - from: "src/backtest/race_predictor.py"
      to: "result_df columns"
      via: "win_selection_edge, win_gate_score, tanodds, conformal_confidence_score"
      pattern: "win_selection_edge"
    - from: "scripts/run_wf_validation.py"
      to: "src/backtest/engine.py"
      via: "BacktestEngine(betting_target=...)"
      pattern: "betting_target"
---

<objective>
RacePredictorに単勝候補選択(get_win_candidates)とConformal信頼性連携を追加し、WF検証スクリプトを最小修正で単勝対応する。

Purpose: 単勝ベット候補が正しく選択され、Conformal信頼性スコアがsoft signalとして機能する。WF検証で単勝ROI過学習検出が可能になる。
Output: get_win_candidates(), select_bets() win path, --betting-target in run_wf_validation.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-win-backtest-core/08-CONTEXT.md
@.planning/phases/08-win-backtest-core/08-RESEARCH.md
@.planning/phases/08-win-backtest-core/08-01-SUMMARY.md

<interfaces>
<!-- race_predictor.py: get_place_candidates() signature (lines 408-425) -->
```python
def get_place_candidates(
    self,
    race_df: pd.DataFrame,
    *,
    regime_params: dict[str, Any] | None = None,
) -> pd.DataFrame:
```

<!-- race_predictor.py: get_place_candidates() filter pattern (lines 424-434) -->
```python
edge_col = "place_selection_edge"
ev_col = "place_selection_ev"
prob_col = "place_selection_prob"
if edge_col not in prepared.columns or "fukuoddslow" not in prepared.columns:
    return prepared.iloc[0:0].copy()

edge_threshold = float(regime_params.get("edge_threshold", 0.03))
min_place_prob = float(regime_params.get("min_place_prob", 0.0))
max_place_odds = float(regime_params.get("max_place_odds", float("inf")))

selection_prob = pd.to_numeric(prepared[prob_col], errors="coerce")
odds = pd.to_numeric(prepared["fukuoddslow"], errors="coerce")
selection_edge = pd.to_numeric(prepared[edge_col], errors="coerce")
```

<!-- race_predictor.py: select_bets() signature (lines 532-542) -->
```python
def select_bets(
    self,
    race_df: pd.DataFrame,
    bankroll: float,
    *,
    candidates: pd.DataFrame | None = None,
) -> list[Bet]:
```

<!-- race_predictor.py: select_bets() place bet creation (lines 607-637) -->
```python
for _, row in candidates.iterrows():
    edge_val = float(row["place_selection_edge"])
    odds_val = float(row["fukuoddslow"])
    if self._betting_mode == "kelly" and self.stake_calc is not None:
        stake = self.stake_calc.calc_stake(
            edge=edge_val, odds=odds_val, bankroll=bankroll, bet_type=BetType.PLACE,
        )
        if self.dd_ctrl is not None:
            stake = self.dd_ctrl.adjust_stake(stake, bankroll)
            stake = max(0, math.floor(stake / 100) * 100)
    else:
        stake = 100.0
    if stake < 100:
        continue
    bets.append(
        Bet(
            race_id=row["race_id"],
            umaban=int(row["umaban"]),
            bet_type=BetType.PLACE,
            odds=odds_val,
            ev_lower_corrected=float(row.get(ev_col, row.get("ev_place_corrected", 0))),
            stake=stake,
            edge=edge_val,
        )
    )
```

<!-- WinSelectionGate.score() outputs (confirmed in codebase) -->
<!-- After predict() pipeline, result_df contains:
     - win_selection_ev (from build_win_selection_ev)
     - win_selection_edge (= win_selection_ev - 1.0)
     - win_selection_prob (from p_win_final or p_win_pred)
     - win_gate_score (from WinSelectionGate.score(), NaN if not trained)
     - win_gate_pass (from WinSelectionGate.score(), False if not trained)
     - conformal_confidence_score (from RobustConfidenceEstimator, 0.0 if not calibrated)
     - tanodds (from odds_tanpuku.parquet via FeatureEngine merge)
-->

<!-- run_wf_validation.py: engine construction (lines 174-176, 187-189) -->
```python
test_engine = BacktestEngine(
    models=models, store=store, diag_prefix=f"wf_{i}_test",
)
# ...
train_engine = BacktestEngine(
    models=models, store=store, diag_prefix=f"wf_{i}_train",
)
```

<!-- run_wf_validation.py: no argparse parser, called with no args -->
<!-- main() starts directly at line 106 with store initialization -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add get_win_candidates() + update select_bets() win path in race_predictor.py</name>
  <files>src/backtest/race_predictor.py</files>
  <read_first>
    src/backtest/race_predictor.py (full file — must see get_place_candidates, select_bets, imports, _betting_mode)
    src/backtest/engine.py (betting_target dispatch from Plan 01)
  </read_first>
  <behavior>
    - Test 1: get_win_candidates() with race_df containing win_selection_edge=0.1, tanodds=2.4, win_gate_score=0.8 returns filtered candidate
    - Test 2: get_win_candidates() with win_selection_edge=-0.1 (negative edge) returns empty DataFrame
    - Test 3: get_win_candidates() with tanodds=0.5 (< 1.0) returns empty DataFrame
    - Test 4: get_win_candidates() with 5 candidates returns only top 2 by win_gate_score
    - Test 5: get_win_candidates() with missing win_selection_edge column returns empty DataFrame
    - Test 6: get_win_candidates() with NaN win_gate_score still returns candidates (sorted by win_selection_edge as fallback)
    - Test 7: select_bets() with win candidates produces Bet objects with BetType.WIN and tanodds as odds
    - Test 8: select_bets() with win candidates in kelly mode uses calc_stake with BetType.WIN
  </behavior>
  <action>
    **1. Add get_win_candidates() method** per D-06, D-07, D-08, D-09, D-10:

    Add this method to the `RacePredictor` class. Place it after `get_place_candidates()` (after line 525, before `should_bet()`). It is the symmetrical counterpart to `get_place_candidates()` but simplified (no regime_params dependency, no gate model interaction):

    ```python
    def get_win_candidates(
        self,
        race_df: pd.DataFrame,
    ) -> pd.DataFrame:
        """単勝ベット候補を選択。

        フィルタ: win_selection_edge > 0 AND tanodds >= 1.0 (D-06)
        ソート: win_gate_score DESC (D-07), fallback win_selection_edge DESC
        上限: 2頭 (D-09)
        win_gate_pass はログ表示のみ、フィルタに使用しない (D-08)
        conformal_confidence_score はランキングのタイブレークにのみ使用 (soft signal)
        """
        edge_col = "win_selection_edge"
        odds_col = "tanodds"

        if edge_col not in race_df.columns or odds_col not in race_df.columns:
            return race_df.iloc[0:0].copy()

        selection_edge = pd.to_numeric(race_df[edge_col], errors="coerce")
        odds = pd.to_numeric(race_df[odds_col], errors="coerce")

        # D-06: Basic filter — edge > 0 AND odds >= 1.0
        mask = selection_edge.fillna(0.0) > 0.0
        mask &= odds.fillna(0.0) >= 1.0

        candidates = race_df.loc[mask].copy()

        if candidates.empty:
            return candidates

        # D-08: Log gate pass status for debugging (not used as filter)
        if "win_gate_pass" in candidates.columns:
            n_gate_pass = int(candidates["win_gate_pass"].fillna(False).astype(bool).sum())
            logger.debug(
                "Win candidates: %d total, %d gate pass", len(candidates), n_gate_pass,
            )

        # D-07: Rank by win_gate_score DESC, fallback to win_selection_edge DESC
        if "win_gate_score" in candidates.columns:
            gate_score = pd.to_numeric(candidates["win_gate_score"], errors="coerce")
            candidates["_win_gate_score_num"] = gate_score.fillna(float("-inf"))
            # Soft signal: conformal_confidence_score as tertiary sort
            if "conformal_confidence_score" in candidates.columns:
                conf_score = pd.to_numeric(
                    candidates["conformal_confidence_score"], errors="coerce"
                )
                candidates["_conf_score"] = conf_score.fillna(0.0)
                candidates = candidates.sort_values(
                    ["_win_gate_score_num", edge_col, "_conf_score"],
                    ascending=[False, False, False],
                )
                candidates = candidates.drop(columns=["_conf_score"])
            else:
                candidates = candidates.sort_values(
                    ["_win_gate_score_num", edge_col],
                    ascending=[False, False],
                )
            candidates = candidates.drop(columns=["_win_gate_score_num"])
        else:
            candidates = candidates.sort_values([edge_col], ascending=[False])

        # D-09: Max 2 candidates per race
        return candidates.head(2)
    ```

    **2. Add win path to select_bets()** per D-13:

    In `select_bets()`, the method currently receives `candidates` as an optional parameter. The engine's run() method dispatches to `get_win_candidates()` or `get_place_candidates()` and passes the result as `candidates` to `select_bets()`. So `select_bets()` needs to detect the candidate type and generate the appropriate Bet objects.

    Add a `betting_target` parameter to `select_bets()`:
    ```python
    def select_bets(
        self,
        race_df: pd.DataFrame,
        bankroll: float,
        *,
        candidates: pd.DataFrame | None = None,
        betting_target: str = "place",
    ) -> list[Bet]:
    ```

    Then in the method body, add win branch before the existing place logic (around line 548):

    ```python
    regime = self.models.regime_detector.current_regime
    regime_params = self.models.regime_detector.get_strategy_params(regime)

    bets: list[Bet] = []
    max_bets = regime_params.get("max_bets_per_race", 3)

    # --- Win bets ---
    if betting_target == "win":
        ev_col = "win_selection_ev"
        edge_col = "win_selection_edge"
        if candidates is None:
            candidates = self.get_win_candidates(race_df)
        if candidates.empty:
            return bets
        candidates = candidates.head(max_bets)

        for _, row in candidates.iterrows():
            edge_val = float(row.get(edge_col, 0))
            odds_val = float(row.get("tanodds", 0))

            if self._betting_mode == "kelly" and self.stake_calc is not None:
                stake = self.stake_calc.calc_stake(
                    edge=edge_val,
                    odds=odds_val,
                    bankroll=bankroll,
                    bet_type=BetType.WIN,
                )
                if self.dd_ctrl is not None:
                    stake = self.dd_ctrl.adjust_stake(stake, bankroll)
                    stake = max(0, math.floor(stake / 100) * 100)
            else:
                stake = 100.0

            if stake < 100:
                continue

            bets.append(
                Bet(
                    race_id=row["race_id"],
                    umaban=int(row["umaban"]),
                    bet_type=BetType.WIN,
                    odds=odds_val,
                    ev_lower_corrected=float(row.get(ev_col, 0)),
                    stake=stake,
                    edge=edge_val,
                )
            )
        return bets

    # --- Place bets (existing logic, unchanged) ---
    ev_col = "place_selection_ev"
    # ... rest of existing place logic unchanged ...
    ```

    **3. Update engine.py select_bets() call to pass betting_target:**

    In `src/backtest/engine.py` at the `select_bets()` call site (around line 596), add `betting_target=self.betting_target`:
    ```python
    bets = self._race_predictor.select_bets(
        result_df, bankroll, candidates=candidate_df,
        betting_target=self.betting_target,
    )
    ```

    **CRITICAL:** Use "tanodds" as the column name (not "tanoddslow"). The Parquet column is "tanodds". The WinSelectionGate internally handles its own column aliasing.

    Write tests FIRST, then implement.
  </action>
  <verify>
    <automated>python -m pytest tests/ -v -k "win_candidate or select_bets" 2>&1 | head -60</automated>
  </verify>
  <done>
    - get_win_candidates() method exists in RacePredictor with correct filter/sort/head logic
    - select_bets() has betting_target parameter with win branch that generates BetType.WIN Bets
    - engine.py passes betting_target to select_bets()
    - conformal_confidence_score is tertiary sort key (not hard filter)
    - win_gate_pass is logged but not used for filtering
    - All existing tests pass: python -m pytest tests/ -v
  </done>
</task>

<task type="auto">
  <name>Task 2: Add --betting-target to run_wf_validation.py</name>
  <files>scripts/run_wf_validation.py</files>
  <read_first>
    scripts/run_wf_validation.py (full file — must see main(), engine construction at lines 174 and 187, no argparse parser currently)
    scripts/run_backtest.py (reference for --betting-target pattern from Plan 01)
  </read_first>
  <acceptance_criteria>
    - run_wf_validation.py has argparse parser with --betting-target argument (choices=["win","place","wide"], default="win")
    - test_engine and train_engine construction pass betting_target=args.betting_target
    - main() calls parser.parse_args() at the start
    - grep "betting.target" scripts/run_wf_validation.py returns at least 4 matches (add_argument + 2 engine calls + default)
  </acceptance_criteria>
  <action>
    **Per D-15, D-16, D-17: Minimal modification.**

    The script currently has no argparse parser. Add one at the top of `main()`.

    **1. Add argparse parser at start of main():**

    After `store = ParquetStore()` (line 110), add:
    ```python
    parser = argparse.ArgumentParser(description="Walk-Forward Validation")
    parser.add_argument(
        "--betting-target",
        choices=["win", "place", "wide"],
        default="win",
        help="ベッティング対象 (デフォルト: win)",
    )
    args = parser.parse_args()
    ```

    **2. Pass betting_target to both engine constructions:**

    At line 174 (test_engine):
    ```python
    test_engine = BacktestEngine(
        models=models, store=store, diag_prefix=f"wf_{i}_test",
        betting_target=args.betting_target,
    )
    ```

    At line 187 (train_engine):
    ```python
    train_engine = BacktestEngine(
        models=models, store=store, diag_prefix=f"wf_{i}_train",
        betting_target=args.betting_target,
    )
    ```

    No other changes. D-16: fold definitions unchanged. D-17: overfitting detection logic unchanged.
  </action>
  <verify>
    <automated>grep -c "betting.target" scripts/run_wf_validation.py && grep "paytansyoumaban1\|betting.target\|add_argument" scripts/run_wf_validation.py</automated>
  </verify>
  <done>
    - run_wf_validation.py accepts --betting-target win|place|wide with default "win"
    - Both test_engine and train_engine receive betting_target parameter
    - No changes to fold definitions or overfitting detection logic
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| result_df columns -> candidate filter | Column names must exist (runtime check with early return) |
| conformal_confidence_score -> ranking | Untrusted when not calibrated (0.0 for all) — handled as soft signal |
| CLI args -> WF validation | User-supplied string passed to BacktestEngine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | I | get_win_candidates column check | mitigate | Early return on missing columns — no KeyError, graceful degradation |
| T-08-05 | D | conformal_confidence_score uncalibrated | mitigate | 0.0 default when uncalibrated — used as tertiary sort only, not filter |
| T-08-06 | E | run_wf_validation.py betting_target | mitigate | argparse choices validation — rejects invalid values |
</threat_model>

<verification>
1. `python -m pytest tests/ -v` — all tests pass (existing + new)
2. `grep -c "def get_win_candidates" src/backtest/race_predictor.py` returns 1
3. `grep "tanodds" src/backtest/race_predictor.py | grep -v "^#"` shows correct column name
4. `grep "betting_target" src/backtest/race_predictor.py | grep -v "^#" | wc -l` >= 2
5. `grep "betting.target" scripts/run_wf_validation.py | wc -l` >= 3
6. `python -c "from backtest.race_predictor import RacePredictor; print('import ok')"` succeeds
</verification>

<success_criteria>
- get_win_candidates()がwin_selection_edge>0 AND tanodds>=1.0でフィルタリング
- 候補がwin_gate_score降順でソート(未学習時はwin_selection_edge降順にfallback)
- 上位2頭のみ返却(D-09)
- conformal_confidence_scoreが3次ソートキー(hard filterではない)
- select_bets()がbetting_target="win"でBetType.WINのBetを生成
- run_wf_validation.pyが--betting-targetを受け取りBacktestEngineに渡す
- フォールド定義・過学習検出ロジックは変更なし(D-16, D-17)
- 全テスト通過: python -m pytest tests/ -v
</success_criteria>

<output>
After completion, create `.planning/phases/08-win-backtest-core/08-02-SUMMARY.md`
</output>
