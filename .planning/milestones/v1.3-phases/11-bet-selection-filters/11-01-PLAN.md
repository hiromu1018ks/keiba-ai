---
phase: 11-bet-selection-filters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/betting/odds_band_filter.py
  - src/backtest/race_predictor.py
  - src/models/regime_detector.py
  - tests/test_odds_band_filter.py
  - tests/test_race_predictor.py
  - tests/test_regime_detector.py
autonomous: true
requirements:
  - BSEL-01
  - BSEL-03

must_haves:
  truths:
    - "get_win_candidates() は EV_lower_win_corrected < 1.0 の候補を除外する"
    - "EV_lower_win_corrected が NaN の場合、既存 edge>0 のみで判定 (フォールバック)"
    - "OddsBandFilter はトレーニング期間ROI < 100% のバンドを除外する"
    - "RegimeDetector.get_strategy_params() が COLLAPSED 時に skip=True を返す"
    - "除外件数が INFO レベルでログ出力される"
  artifacts:
    - path: "src/betting/odds_band_filter.py"
      provides: "OddsBandFilter クラス (calibrate + filter + excluded_bands)"
      exports: ["OddsBandFilter"]
    - path: "src/backtest/race_predictor.py"
      provides: "get_win_candidates() に EV_lower >= 1.0 フィルター追加"
      contains: "EV_lower_win_corrected"
    - path: "src/models/regime_detector.py"
      provides: "COLLAPSED 分岐に skip=True 追加"
      contains: '"skip": True'
  key_links:
    - from: "src/backtest/race_predictor.py"
      to: "EV_lower_win_corrected column"
      via: "pd.to_numeric mask check in get_win_candidates()"
      pattern: "EV_lower_win_corrected.*fillna"
    - from: "src/betting/odds_band_filter.py"
      to: "training bet history"
      via: "calibrate() method computes ROI per band"
      pattern: "calibrate.*bet_history"
---

<objective>
フィルター基盤コンポーネントを実装する: EV下限フィルター (BSEL-01), OddsBandFilter クラス (BSEL-03), および RegimeDetector skip=True 拡張。

Purpose: バックテストパイプラインで使用される3つのフィルターコンポーネント (race_predictor.py内のEVフィルター, 新規OddsBandFilterクラス, regime_detector.pyのskip=True) を構築する。これらは独立したコンポーネントであり、engine.pyへの統合はPlan 02で行う。

Output: odds_band_filter.py (新規), race_predictor.py (EV filter追加), regime_detector.py (skip=True追加), 各テストファイル
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-bet-selection-filters/11-CONTEXT.md
@.planning/phases/11-bet-selection-filters/11-RESEARCH.md

<interfaces>
<!-- Executor needs these types/signatures from existing codebase -->

From src/domain/types.py:
```python
class RegimeState(str, Enum):
    AGGRESSIVE = "aggressive"
    CONSERVATIVE = "conservative"
    COLLAPSED = "collapsed"
```

From src/backtest/race_predictor.py (lines 408-470):
```python
def get_win_candidates(self, race_df: pd.DataFrame) -> pd.DataFrame:
    # Current filter: win_selection_edge > 0 AND tanodds >= 1.0
    # Sort: win_gate_score DESC, fallback win_selection_edge DESC
    # Max: 2 candidates per race
    mask = selection_edge.fillna(0.0) > 0.0
    mask &= odds.fillna(0.0) >= 1.0
```

From src/models/regime_detector.py (lines 178-232):
```python
def get_strategy_params(self, regime: RegimeState) -> dict[str, object]:
    # COLLAPSED branch (line 221):
    else:  # COLLAPSED
        return {
            "ev_threshold": 1.50,
            "edge_threshold": 0.09,
            ...
            "description": "崩壊 -> ほぼ停止",
        }
```

From src/backtest/report.py (lines 398-406) -- odds band pattern to replicate:
```python
lambda b: (
    "1.0-3.0" if b.get("tanoddslow", 0) < 3.0
    else "3.0-10.0" if b.get("tanoddslow", 0) < 10.0
    else "10.0-30.0" if b.get("tanoddslow", 0) < 30.0
    else "30.0+"
),
["1.0-3.0", "3.0-10.0", "10.0-30.0", "30.0+"],
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OddsBandFilter クラス作成 + 単体テスト</name>
  <files>src/betting/odds_band_filter.py, tests/test_odds_band_filter.py</files>
  <read_first>
    - src/betting/odds_band_filter.py (存在しない場合は新規作成)
    - src/backtest/report.py (lines 339-406 -- _band_stats pattern and odds band boundaries)
    - src/betting/stake_calculator.py (existing betting module for import pattern reference)
  </read_first>
  <behavior>
    - Test 1: calibrate() に空リストを渡すと excluded_bands が空であること
    - Test 2: calibrate() にROI < 100% のバンドのみのbet_historyを渡すと、そのバンドが excluded になること
    - Test 3: calibrate() にROI >= 100% のバンドを含むbet_historyを渡すと、そのバンドは excluded にならないこと
    - Test 4: filter() は除外バンドに該当する候補を除外し、それ以外は保持すること
    - Test 5: calibrate() 未呼び出しで filter() を呼んでもエラーにならず全候補を返すこと
    - Test 6: excluded_bands プロパティが除外バンド名・ROI・件数を含むdictを返すこと
  </behavior>
  <action>
Create `src/betting/odds_band_filter.py` with OddsBandFilter class per D-06.

Class structure:
```python
import logging
from typing import Any
import pandas as pd

logger = logging.getLogger(__name__)

class OddsBandFilter:
    """D-06: 動的オッズバンドフィルター。トレーニング期間ROI < 100%のバンドを除外 (D-07)。"""

    BANDS = [(1.0, 3.0), (3.0, 10.0), (10.0, 30.0), (30.0, float("inf"))]
    BAND_NAMES = ["1.0-3.0", "3.0-10.0", "10.0-30.0", "30.0+"]

    def __init__(self) -> None:
        self._excluded_bands: set[str] = set()
        self._band_roi: dict[str, float] = {}
        self._band_counts: dict[str, int] = {}

    @staticmethod
    def _get_band_name(odds: float) -> str:
        """オッズ値からバンド名を返す (report.py _band_stats と同じ境界)"""
        for (lo, hi), name in zip(OddsBandFilter.BANDS, OddsBandFilter.BAND_NAMES):
            if lo <= odds < hi:
                return name
        return "30.0+"  # fallback for inf/edge cases

    def calibrate(self, bet_history: list[dict[str, Any]]) -> None:
        """D-05: トレーニング期間ベットデータから各バンドROIを計算。D-07: ROI < 100% を除外。

        bet_history items must have keys: "odds" (decision-time odds), "result" (payout), "stake".
        """
        if not bet_history:
            return

        # Group by band and compute ROI
        band_data: dict[str, dict[str, float]] = {}
        for name in self.BAND_NAMES:
            band_data[name] = {"total_stake": 0.0, "total_return": 0.0, "count": 0}

        for bet in bet_history:
            odds = float(bet.get("odds", 0))
            band = self._get_band_name(odds)
            band_data[band]["total_stake"] += float(bet.get("stake", 0))
            band_data[band]["total_return"] += float(bet.get("result", 0))
            band_data[band]["count"] += 1

        self._excluded_bands = set()
        self._band_roi = {}
        self._band_counts = {}
        for name, data in band_data.items():
            count = int(data["count"])
            self._band_counts[name] = count
            if count == 0:
                continue
            roi = data["total_return"] / data["total_stake"] if data["total_stake"] > 0 else 0.0
            self._band_roi[name] = roi
            if roi < 1.0:  # D-07: ROI < 100% → exclude
                self._excluded_bands.add(name)

        logger.info(
            "OddsBandFilter calibration: excluded_bands=%s, band_roi=%s, band_counts=%s",
            self._excluded_bands, self._band_roi, self._band_counts,
        )

    def filter(self, candidate_df: pd.DataFrame, odds_col: str = "tanodds") -> pd.DataFrame:
        """除外バンドに該当する候補を除外してDataFrameを返す。"""
        if candidate_df.empty or not self._excluded_bands:
            return candidate_df
        if odds_col not in candidate_df.columns:
            return candidate_df

        odds = pd.to_numeric(candidate_df[odds_col], errors="coerce")
        mask = pd.Series([True] * len(candidate_df), index=candidate_df.index)
        for band_name in self._excluded_bands:
            band_idx = self.BAND_NAMES.index(band_name)
            lo, hi = self.BANDS[band_idx]
            band_mask = (odds >= lo) & (odds < hi)
            mask &= ~band_mask

        excluded = candidate_df.loc[~mask]
        if not excluded.empty:
            logger.info(
                "OddsBandFilter excluded %d candidates in bands: %s",
                len(excluded), self._excluded_bands,
            )

        return candidate_df.loc[mask].copy()

    @property
    def excluded_bands(self) -> dict[str, dict[str, Any]]:
        """D-08: 除外バンド情報を返す (band_name → {roi, count})"""
        return {
            name: {"roi": self._band_roi.get(name, 0.0), "count": self._band_counts.get(name, 0)}
            for name in self._excluded_bands
        }
```

Then create `tests/test_odds_band_filter.py` with tests covering all 6 behaviors above.
Use pytest patterns from existing tests (MagicMock, pd.DataFrame fixtures).
All type annotations required (mypy strict).
  </action>
  <acceptance_criteria>
    - `src/betting/odds_band_filter.py` が存在し `class OddsBandFilter` を含む
    - `OddsBandFilter` に `calibrate(self, bet_history: list[dict[str, Any]]) -> None` メソッドが存在する
    - `OddsBandFilter` に `filter(self, candidate_df: pd.DataFrame, odds_col: str = "tanodds") -> pd.DataFrame` メソッドが存在する
    - `OddsBandFilter` に `excluded_bands` property が存在する
    - `BAND_NAMES = ["1.0-3.0", "3.0-10.0", "10.0-30.0", "30.0+"]` が定義されている
    - `python -m pytest tests/test_odds_band_filter.py -v` が全テストPASSする
    - `ruff check src/betting/odds_band_filter.py` がエラーなし
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_odds_band_filter.py -v && ruff check src/betting/odds_band_filter.py</automated>
  </verify>
  <done>OddsBandFilter クラスが calibrate/filter/excluded_bands を持ち、全6テストが通る</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EV_lower フィルター追加 + RegimeDetector skip=True 拡張</name>
  <files>src/backtest/race_predictor.py, src/models/regime_detector.py, tests/test_race_predictor.py, tests/test_regime_detector.py</files>
  <read_first>
    - src/backtest/race_predictor.py (lines 408-470 -- get_win_candidates() full method)
    - src/models/regime_detector.py (lines 178-232 -- get_strategy_params() full method)
    - tests/test_race_predictor.py (lines 1340-1420 -- TestGetWinCandidates class, _make_win_race_df helper)
    - tests/test_regime_detector.py (existing test patterns)
  </read_first>
  <behavior>
    EV filter (race_predictor.py):
    - Test 7: EV_lower_win_corrected >= 1.0 の候補は保持される
    - Test 8: EV_lower_win_corrected < 1.0 の候補は除外される
    - Test 9: EV_lower_win_corrected が NaN の候補は除外されない (D-03 fallback)
    - Test 10: EV_lower_win_corrected 列が存在しない場合、既存 edge>0 のみで動作

    RegimeDetector (regime_detector.py):
    - Test 11: get_strategy_params(RegimeState.COLLAPSED) が "skip": True を含む
    - Test 12: get_strategy_params(RegimeState.AGGRESSIVE) に "skip" キーが含まれない (または False)
  </behavior>
  <action>
**Part A: EV_lower filter in get_win_candidates() (D-01, D-02, D-03, D-04)**

In `src/backtest/race_predictor.py`, modify `get_win_candidates()` method:

After line 431 (`mask &= odds.fillna(0.0) >= 1.0`), add the EV_lower filter:

```python
        # D-01: EV lower bound filter (dual with edge > 0)
        ev_lower_col = "EV_lower_win_corrected"
        n_before_ev = int(mask.sum())
        if ev_lower_col in race_df.columns:
            ev_lower = pd.to_numeric(race_df[ev_lower_col], errors="coerce")
            # D-03: NaN -> True (fallback to edge-only, fillna(1.0) passes >= 1.0)
            ev_mask = ev_lower.fillna(1.0) >= 1.0
            mask &= ev_mask
            n_ev_excluded = n_before_ev - int(mask.sum())
            if n_ev_excluded > 0:
                ev_total = len(race_df)
                logger.info(
                    "EV filter: excluded %d candidates (%.1f%%), "
                    "EV_lower < 1.0 in race %s",
                    n_ev_excluded,
                    100.0 * n_ev_excluded / max(ev_total, 1),
                    race_df["race_id"].iloc[0] if "race_id" in race_df.columns else "?",
                )
```

This implements:
- D-01: Dual filter (edge > 0 + EV_lower >= 1.0)
- D-02: Filter applied in get_win_candidates() early
- D-03: NaN fallback via fillna(1.0) which passes the >= 1.0 check
- D-04: INFO level log with exclusion count and percentage

**Part B: RegimeDetector skip=True (D-11)**

In `src/models/regime_detector.py`, modify the COLLAPSED branch of `get_strategy_params()`:

In the `else:  # COLLAPSED` block (line 221-232), add `"skip": True` to the returned dict:

```python
        else:  # COLLAPSED
            return {
                "ev_threshold": 1.50,
                "edge_threshold": 0.09,
                "min_place_prob": 0.10,
                "max_place_odds": 16.0,
                "wide_enabled": False,
                "score_threshold": 0.050,
                "max_bets_per_race": 1,
                "weak_prob_prune_threshold": 0.35,
                "skip": True,  # D-11: COLLAPSED regime skip flag
                "description": "崩壊 -> ほぼ停止",
            }
```

**Tests:**

In `tests/test_race_predictor.py`, add to `TestGetWinCandidates` class (after line 1419):

```python
    def test_ev_lower_filter_excludes_below_threshold(self, mock_models: MagicMock) -> None:
        """Test 8: EV_lower_win_corrected < 1.0 の候補は除外される (D-01)"""
        from backtest.race_predictor import RacePredictor

        predictor = RacePredictor(models=mock_models)
        race_df = self._make_win_race_df(
            n=3,
            EV_lower_win_corrected=[0.8, 1.2, 0.5],  # horses 2 passes, 1 & 3 fail
        )
        result = predictor.get_win_candidates(race_df)
        assert len(result) == 1
        assert result.iloc[0]["umaban"] == 2

    def test_ev_lower_nan_fallback_to_edge_only(self, mock_models: MagicMock) -> None:
        """Test 9: EV_lower_win_corrected が NaN なら edge>0 のみで判定 (D-03)"""
        from backtest.race_predictor import RacePredictor

        predictor = RacePredictor(models=mock_models)
        race_df = self._make_win_race_df(
            n=2,
            EV_lower_win_corrected=[float("nan"), float("nan")],  # NaN -> fallback
        )
        result = predictor.get_win_candidates(race_df)
        # Both have edge > 0, both should pass via fallback
        assert len(result) == 2

    def test_ev_lower_column_missing_keeps_existing_behavior(self, mock_models: MagicMock) -> None:
        """Test 10: EV_lower_win_corrected 列なし → 既存動作と同じ"""
        from backtest.race_predictor import RacePredictor

        predictor = RacePredictor(models=mock_models)
        race_df = self._make_win_race_df(n=3)
        # Remove EV_lower column if it exists
        race_df = race_df.drop(columns=["EV_lower_win_corrected"], errors="ignore")
        result = predictor.get_win_candidates(race_df)
        assert len(result) == 2  # max 2 candidates, same as existing

    def test_ev_lower_above_threshold_kept(self, mock_models: MagicMock) -> None:
        """Test 7: EV_lower_win_corrected >= 1.0 の候補は保持される"""
        from backtest.race_predictor import RacePredictor

        predictor = RacePredictor(models=mock_models)
        race_df = self._make_win_race_df(
            n=3,
            EV_lower_win_corrected=[1.5, 1.2, 2.0],  # all >= 1.0
        )
        result = predictor.get_win_candidates(race_df)
        assert len(result) == 2  # max 2
```

Note: `_make_win_race_df` helper already accepts `**overrides` so `EV_lower_win_corrected` will be passed through as a column.

In `tests/test_regime_detector.py`, add tests:

```python
    def test_collapsed_strategy_has_skip_true(self) -> None:
        """Test 11: COLLAPSED params に skip=True が含まれる (D-11)"""
        from models.regime_detector import RegimeDetector, RegimeState

        detector = RegimeDetector()
        params = detector.get_strategy_params(RegimeState.COLLAPSED)
        assert params.get("skip") is True

    def test_aggressive_strategy_no_skip(self) -> None:
        """Test 12: AGGRESSIVE params に skip キーがない (または False)"""
        from models.regime_detector import RegimeDetector, RegimeState

        detector = RegimeDetector()
        params = detector.get_strategy_params(RegimeState.AGGRESSIVE)
        assert params.get("skip") is not True
```
  </action>
  <acceptance_criteria>
    - race_predictor.py get_win_candidates() に `EV_lower_win_corrected` を含む
    - race_predictor.py に `ev_mask = ev_lower.fillna(1.0) >= 1.0` パターンが含まれる
    - regime_detector.py get_strategy_params() COLLAPSED 分岐に `"skip": True` が含まれる
    - `python -m pytest tests/test_race_predictor.py::TestGetWinCandidates -v` が全テストPASS
    - `python -m pytest tests/test_regime_detector.py -v -k "skip"` が全テストPASS
    - `ruff check src/backtest/race_predictor.py src/models/regime_detector.py` がエラーなし
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_race_predictor.py::TestGetWinCandidates tests/test_regime_detector.py -v -k "ev_lower or skip" && ruff check src/backtest/race_predictor.py src/models/regime_detector.py</automated>
  </verify>
  <done>EV_lower フィルターが get_win_candidates() で動作し、NaN フォールバックが確認済み。COLLAPSED skip=True が追加済み。全テスト通過。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal data pipeline | All filtering operates on internally computed DataFrames (no external input) |
| Training data vs test data | OddsBandFilter calibration must only use training-period data (look-ahead bias prevention) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure (look-ahead) | OddsBandFilter.calibrate() | mitigate | calibrate() receives explicit bet_history list; caller (engine) controls what data is passed -- test period data must NOT be passed |
| T-11-02 | Tampering | EV_lower_win_corrected column | accept | Column computed by RobustConfidenceEstimator.predict_interval() -- internal trusted computation |
</threat_model>

<verification>
1. `python -m pytest tests/test_odds_band_filter.py tests/test_race_predictor.py::TestGetWinCandidates tests/test_regime_detector.py -v` 全通過
2. `ruff check src/betting/odds_band_filter.py src/backtest/race_predictor.py src/models/regime_detector.py` エラーなし
3. `python -m pytest tests/ -v --co -q` でテスト収集エラーなし
</verification>

<success_criteria>
- OddsBandFilter クラスが calibrate/filter/excluded_bands を持ち全テスト通過
- get_win_candidates() が EV_lower < 1.0 を除外し NaN はフォールバック
- RegimeDetector COLLAPSED 分岐が skip=True を返す
- ruff/mypy エラーなし
</success_criteria>

<output>
After completion, create `.planning/phases/11-bet-selection-filters/11-01-SUMMARY.md`
</output>
