---
phase: 12-stake-sizing-enhancement
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/models/regime_detector.py
  - src/betting/meta_switcher.py
  - src/backtest/race_predictor.py
  - src/backtest/engine.py
  - config/settings.yaml
  - tests/test_stake_calculator.py
  - tests/test_backtest_engine.py
autonomous: true
requirements: [SIZE-01, SIZE-02]

must_haves:
  truths:
    - "RegimeDetector.get_strategy_params() が fractional_kelly を返す (AGGRESSIVE=0.50, CONSERVATIVE=0.25, COLLAPSED=0.00)"
    - "MetaSwitcher._default_params() が fractional_kelly を返す"
    - "engine.py が regime_params から fractional_kelly を取得し StakeCalculator に注入する"
    - "RacePredictor.select_bets() のwinパスで Kelly→EV乗算→DD の順序でパイプラインが実行される"
    - "COLLAPSED レジームで stake=0 が返され、ベットが生成されない"
  artifacts:
    - path: "src/models/regime_detector.py"
      provides: "get_strategy_params() に fractional_kelly 追加"
      contains: "fractional_kelly"
    - path: "src/betting/meta_switcher.py"
      provides: "_default_params() に fractional_kelly 追加"
      contains: "fractional_kelly"
    - path: "src/backtest/race_predictor.py"
      provides: "Kelly→EV乗算→DD パイプライン"
      contains: "apply_ev_scaling"
    - path: "src/backtest/engine.py"
      provides: "fractional_kelly 注入"
      contains: "fractional_kelly"
    - path: "config/settings.yaml"
      provides: "betting_strategy section"
      contains: "betting_strategy:"
  key_links:
    - from: "src/backtest/engine.py"
      to: "src/betting/stake_calculator.py"
      via: "regime_params['fractional_kelly'] → stake_calc.fractional_kelly"
      pattern: "fractional_kelly"
    - from: "src/backtest/race_predictor.py"
      to: "src/betting/stake_calculator.py"
      via: "apply_ev_scaling(stake, ev=ev_val)"
      pattern: "apply_ev_scaling"
    - from: "src/models/regime_detector.py"
      to: "src/backtest/engine.py"
      via: "get_strategy_params(regime)"
      pattern: "get_strategy_params"
---

<objective>
RegimeDetector/MetaSwitcher に fractional_kelly パラメータを追加し、engine.py から RacePredictor まで Kelly→EV乗算→DD パイプラインを統合する

Purpose: SIZE-01(レジーム別Kelly)とSIZE-02(EV比例乗算器)をバックテストパスに統合し、レジーム状態に応じた動的サイジングを実現する。
Output: バックテスト実行時にレジーム別Kelly + EV乗算が動作する完全パイプライン
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-stake-sizing-enhancement/12-CONTEXT.md
@.planning/phases/12-stake-sizing-enhancement/12-RESEARCH.md
@.planning/phases/12-stake-sizing-enhancement/12-01-SUMMARY.md
@src/models/regime_detector.py
@src/betting/meta_switcher.py
@src/backtest/race_predictor.py
@src/backtest/engine.py
@config/settings.yaml

<interfaces>
<!-- Plan 12-01 で作成された StakeCalculator の新しいインターフェース -->

From src/betting/stake_calculator.py (Plan 12-01 成果物):
```python
class StakeCalculator:
    MIN_EDGE_THRESHOLD: float = 0.005
    MIN_STAKE: int = 100
    MAX_STAKE: int = 10000
    RACE_EXPOSURE_CAP: float = 0.02

    def __init__(
        self,
        fractional_kelly: float = 0.5,
        kelly_fraction_cap: float = 0.25,
        target_ev: float = 1.10,
        max_scale: float = 2.0,
    ) -> None: ...

    def calc_stake(self, edge: float, odds: float, bankroll: float, bet_type: BetType) -> float: ...
    def apply_ev_scaling(self, stake: float, ev: float) -> float: ...
    def check_race_exposure(self, bets: list[Bet], bankroll: float) -> list[Bet]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RegimeDetector/MetaSwitcher に fractional_kelly 追加 + engine.py 注入 + settings.yaml</name>
  <files>src/models/regime_detector.py, src/betting/meta_switcher.py, src/backtest/engine.py, config/settings.yaml</files>
  <read_first>
    src/models/regime_detector.py (lines 185-240: get_strategy_params メソッド)
    src/betting/meta_switcher.py (全行: _default_params メソッド)
    src/backtest/engine.py (lines 340-370: __init__ の StakeCalculator 構築)
    src/backtest/engine.py (lines 700-760: レースループ内の regime_params 取得 + select_bets 呼び出し)
    config/settings.yaml (全行)
  </read_first>
  <action>
    1. **src/models/regime_detector.py** の get_strategy_params() に fractional_kelly を追加:

       AGGRESSIVE dict に追加: `"fractional_kelly": 0.50,`
       CONSERVATIVE dict に追加: `"fractional_kelly": 0.25,`
       COLLAPSED dict に追加: `"fractional_kelly": 0.00,`

       各dictの "description" 行の直前に挿入する。例:
       ```python
       if regime == RegimeState.AGGRESSIVE:
           return {
               "ev_threshold": 1.10,
               "edge_threshold": 0.05,
               "fractional_kelly": 0.50,   # D-01: half-Kelly
               # ... 既存パラメータ ...
               "description": "歪み強い -> 攻める",
           }
       ```

    2. **src/betting/meta_switcher.py** の _default_params() に fractional_kelly を追加:

       AGGRESSIVE dict に追加: `"fractional_kelly": 0.50,`
       CONSERVATIVE dict に追加: `"fractional_kelly": 0.25,`
       COLLAPSED dict に追加: `"fractional_kelly": 0.00,`

       各dictの "description" 行の直前に挿入する。

    3. **config/settings.yaml** に betting_strategy section を追加 (ファイル末尾に追加):

       ```yaml
       betting_strategy:
         default_fractional_kelly: 0.5
         kelly_fraction_cap: 0.25
         target_ev: 1.10
         max_scale: 2.0
         regime_fractions:
           aggressive: 0.50
           conservative: 0.25
           collapsed: 0.00
       ```

    4. **src/backtest/engine.py** の __init__ (line 369) とレースループ (line 710) を変更:

       __init__ の StakeCalculator 構築 (line 369) は変更なし。デフォルト StakeCalculator() で作成。

       レースループ内の regime_params 取得後 (line 710 の直後) に fractional_kelly 注入を追加:
       ```python
       regime_params = self.models.regime_detector.get_strategy_params(regime)
       # D-11: レジーム別 fractional_kelly を StakeCalculator に注入
       if self._race_predictor.stake_calc is not None:
           fk = float(regime_params.get("fractional_kelly", 0.5))
           self._race_predictor.stake_calc.fractional_kelly = fk
       ```

       この注入は edge_threshold 取得 (line 711) の直後に追加する。
  </action>
  <verify>
    <automated>python -m pytest tests/ -v -k "stake_calculator or regime_detector or meta_switcher" 2>&1 | head -80</automated>
  </verify>
  <done>
    - regime_detector.py get_strategy_params() の3レジームdict全てに "fractional_kelly" キーが存在する
    - meta_switcher.py _default_params() の3レジームdict全てに "fractional_kelly" キーが存在する
    - config/settings.yaml に "betting_strategy:" section が存在する
    - engine.py のレースループ内で fractional_kelly を StakeCalculator に注入している
    - ruff check が全変更ファイルで通過する
  </done>
</task>

<task type="auto">
  <name>Task 2: RacePredictor.select_bets() に Kelly→EV乗算→DD パイプラインを統合 + テスト</name>
  <files>src/backtest/race_predictor.py, tests/test_backtest_engine.py</files>
  <read_first>
    src/backtest/race_predictor.py (lines 618-680: select_bets メソッドのwinパス)
    src/backtest/race_predictor.py (lines 740-770: select_bets メソッドのplaceパス)
    src/backtest/engine.py (lines 854-860: select_bets 呼び出し箇所)
    tests/test_backtest_engine.py (既存テストパターン確認)
  </read_first>
  <action>
    1. **src/backtest/race_predictor.py** の select_bets() winパス (lines 649-662) を変更:

       現在のコード:
       ```python
       if self._betting_mode == "kelly" and self.stake_calc is not None:
           stake = self.stake_calc.calc_stake(
               edge=edge_val,
               odds=odds_val,
               bankroll=bankroll,
               bet_type=BetType.WIN,
           )
           if self.dd_ctrl is not None:
               stake = self.dd_ctrl.adjust_stake(stake, bankroll)
               stake = max(0, math.floor(stake / 100) * 100)
       ```

       変更後:
       ```python
       if self._betting_mode == "kelly" and self.stake_calc is not None:
           stake = self.stake_calc.calc_stake(
               edge=edge_val,
               odds=odds_val,
               bankroll=bankroll,
               bet_type=BetType.WIN,
           )
           # D-07: EV比例乗算 (Kelly→EV→DD パイプライン)
           ev_val = float(row.get(ev_col, 0))
           stake = self.stake_calc.apply_ev_scaling(stake, ev=ev_val)
           if self.dd_ctrl is not None:
               stake = self.dd_ctrl.adjust_stake(stake, bankroll)
               stake = max(0, math.floor(stake / 100) * 100)
       ```

       注意: ev_col は winパスの上部 (line 641) で `"win_selection_ev"` に設定済み。

    2. **tests/test_backtest_engine.py** に統合テストを追加:

       TestStakeSizingIntegration クラスを作成し、以下のテストを追加:

       - test_regime_injects_fractional_kelly:
         BacktestEngine を kelly モード + win target で初期化。
         regime_params に fractional_kelly=0.25 が含まれることを確認。
         StakeCalculator に fractional_kelly が注入されることを確認。
         モックで minimal なパイプラインを構成:
         - engine.models.regime_detector.get_strategy_params() が {"fractional_kelly": 0.25, "ev_threshold": 1.10, ...} を返す
         - extract_pre_post_odds, FeatureEngine, SubModelManager をモック化
         - レースループが実行され、stake_calc.fractional_kelly == 0.25 になることを確認

       - test_ev_scaling_in_select_bets:
         RacePredictor を kelly モードで初期化 (StakeCalculator + DrawdownController 付き)。
         select_bets() を呼び出し、EV乗算が適用されていることを確認。
         具体的には:
         - StakeCalculator(fractional_kelly=0.50, target_ev=1.10, max_scale=2.0)
         - edge=0.06, odds=5.0, ev=1.50 → kelly_stake=700, ev_scaled=700*1.3636=954.5 → floor=900
         - 結果の bet.stake が apply_ev_scaling 適用後の値であることを確認

       - test_collapsed_regime_zero_stake:
         fractional_kelly=0.00 の StakeCalculator で calc_stake が 0 を返すことを確認し、
         select_bets() が空リストを返すことを確認

       テストのモックパターンは Phase 11 の test_backtest_engine.py の既存パターン (TestBetSelectionFilters) に従う。
  </action>
  <verify>
    <automated>python -m pytest tests/test_backtest_engine.py tests/test_stake_calculator.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    - race_predictor.py select_bets() winパスで calc_stake → apply_ev_scaling → dd_ctrl.adjust_stake の順序で呼び出される
    - ev_val が row.get(ev_col, 0) から取得される (ev_col = "win_selection_ev")
    - テスト3つが全て通過する (regime注入、EV乗算、COLLAPSED zero stake)
    - ruff check src/backtest/race_predictor.py が通過する
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine→StakeCalculator | fractional_kelly が regime_params から注入される。不正値のリスク |
| RacePredictor→StakeCalculator | EV値が DataFrame から取得される。NaN/0のリスク |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | I | engine.py fractional_kelly注入 | mitigate | regime_params.get("fractional_kelly", 0.5) でデフォルト値あり |
| T-12-04 | D | RacePredictor EV値 | mitigate | apply_ev_scaling 内で NaN/0/負数ガード済み (Plan 12-01) |
| T-12-05 | E | DD制御バイパス | accept | Kelly→EV→DD順序でDDが最終ゲート (D-08) |

</threat_model>

<verification>
1. `python -m pytest tests/test_stake_calculator.py tests/test_backtest_engine.py -v` — 全テスト通過
2. `ruff check src/models/regime_detector.py src/betting/meta_switcher.py src/backtest/race_predictor.py src/backtest/engine.py` — リント通過
3. `python -m pytest tests/ -v --co -q 2>&1 | tail -5` — コレクションエラーなし
4. grep検証: `grep -n "fractional_kelly" src/models/regime_detector.py src/betting/meta_switcher.py src/backtest/engine.py` — 各ファイルに fractional_kelly が含まれる
5. grep検証: `grep -n "apply_ev_scaling" src/backtest/race_predictor.py` — EV乗算が統合されている
6. grep検証: `grep -n "betting_strategy:" config/settings.yaml` — 設定sectionが存在する
</verification>

<success_criteria>
- RegimeDetector.get_strategy_params() が AGGRESSIVE=0.50, CONSERVATIVE=0.25, COLLAPSED=0.00 の fractional_kelly を返す
- MetaSwitcher._default_params() が同様の fractional_kelly を返す
- engine.py のレースループで fractional_kelly が StakeCalculator に注入される
- RacePredictor.select_bets() のwinパスで Kelly→EV乗算→DD パイプラインが動作する
- 全テスト通過、リント通過
</success_criteria>

<output>
After completion, create `.planning/phases/12-stake-sizing-enhancement/12-02-SUMMARY.md`
</output>
