---
phase: 13-risk-calibration-parameter-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/regime_detector.py
  - src/betting/meta_switcher.py
  - src/backtest/parameter_freeze_protocol.py
  - tests/test_parameter_freeze.py
autonomous: true
requirements: [VAL-01]

must_haves:
  truths:
    - "RegimeDetector.get_strategy_params()のfractional_kelly, ev_threshold, edge_thresholdがコンストラクタ注入で外部から上書き可能"
    - "MetaSwitcher._default_params()の値がRegimeDetector.get_strategy_params()と一致する"
    - "ParameterFreezeProtocolが戦略パラメータをJSON manifestで保存し、SHA256ハッシュで改ざん検知できる"
    - "verify_strategy_manifest()がSHA256不一致時にValueErrorを送出する"
  artifacts:
    - path: "src/models/regime_detector.py"
      provides: "コンストラクタ注入可能なRegimeDetector主要3パラメータ"
      contains: "self._override_params"
    - path: "src/betting/meta_switcher.py"
      provides: "RegimeDetectorに揃えた_default_params()"
      contains: "ev_threshold"
    - path: "src/backtest/parameter_freeze_protocol.py"
      provides: "戦略パラメータJSON manifest + SHA256"
      contains: "save_strategy_manifest"
    - path: "tests/test_parameter_freeze.py"
      provides: "JSON manifest保存・検証テスト"
      min_lines: 30
  key_links:
    - from: "src/backtest/parameter_freeze_protocol.py"
      to: "src/tuning/strategy_optimizer.py (Plan 03)"
      via: "save_strategy_manifest() 呼び出し"
      pattern: "save_strategy_manifest"
    - from: "src/models/regime_detector.py"
      to: "src/tuning/strategy_optimizer.py (Plan 03)"
      via: "RegimeDetector(params=...) コンストラクタ注入"
      pattern: "override_params"
---

<objective>
RegimeDetectorの主要パラメータをコンストラクタ注入可能に外部化し、MetaSwitcherの値乖離を解消し、ParameterFreezeProtocolに戦略パラメータJSON manifest機能を追加する。

Purpose: Optuna最適化(Plan 03)の前提として、RegimeDetector/StakeCalculator/DDControllerの全パラメータがプログラマティックに制御可能である必要がある。また最適化後のパラメータがテスト期間中に変更されないことをSHA256で保証する。

Output: RegimeDetector外部化済み、MetaSwitcher値揃え済み、ParameterFreezeProtocol JSON manifest + SHA256、テスト追加
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-risk-calibration-parameter-optimization/13-CONTEXT.md
@.planning/phases/13-risk-calibration-parameter-optimization/13-RESEARCH.md
</context>

<interfaces>

From src/models/regime_detector.py (current — will be modified):
```python
class RegimeDetector:
    def __init__(self, cfg: RegimeConfig | None = None) -> None:
        # lines 133-176: detect() with hysteresis
    def get_strategy_params(self, regime: RegimeState) -> dict[str, object]:
        # lines 185-243: 25+ hardcoded params per regime
```

From src/betting/meta_switcher.py (current — will be modified):
```python
class MetaSwitcher:
    @staticmethod
    def _default_params(regime: RegimeState) -> dict[str, object]:
        # lines 42-70: values DIVERGE from RegimeDetector
        # AGGRESSIVE: ev_threshold=1.15 (RegimeDetector: 1.10)
        # CONSERVATIVE: ev_threshold=1.35 (RegimeDetector: 1.30), edge_threshold=0.07 (RegimeDetector: 0.06)
        # COLLAPSED: ev_threshold=1.55 (RegimeDetector: 1.50), edge_threshold=0.10 (RegimeDetector: 0.09)
```

From src/backtest/parameter_freeze_protocol.py (current — will be extended):
```python
class ParameterFreezeProtocol:
    def __init__(self, models: TrainedModelsV5) -> None:
    def freeze(self) -> None:
    def verify(self) -> dict[str, Any]:
    @contextmanager
    def frozen_period(self) -> Iterator[None]:
    @staticmethod
    def _serialize(obj: Any) -> bytes:
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: RegimeDetector外部化 + MetaSwitcher値揃え</name>
  <files>src/models/regime_detector.py, src/betting/meta_switcher.py</files>
  <read_first>
    src/models/regime_detector.py (全体)
    src/betting/meta_switcher.py (全体)
  </read_first>
  <action>
Per D-15, D-16, D-17:

1. **src/models/regime_detector.py** — get_strategy_params()の主要3パラメータ(fractional_kelly, ev_threshold, edge_threshold)をコンストラクタ注入可能に外部化:

   a. RegimeDetector.__init__() に override_params 引数を追加:
      ```python
      def __init__(
          self,
          cfg: RegimeConfig | None = None,
          override_params: dict[str, dict[str, float]] | None = None,
      ) -> None:
          self.cfg = cfg or RegimeConfig()
          # override_params: {"aggressive": {"fractional_kelly": 0.6, "ev_threshold": 1.2, ...}, ...}
          self._override_params: dict[str, dict[str, float]] = override_params or {}
      ```

   b. get_strategy_params() の戻り値で、主要3パラメータをoverride_paramsで上書き:
      ```python
      def get_strategy_params(self, regime: RegimeState) -> dict[str, object]:
          # 既存のハードコードdictを取得
          params = self._get_base_params(regime)
          # override_params があれば主要3パラメータを上書き
          regime_key = regime.value  # "aggressive", "conservative", "collapsed"
          if regime_key in self._override_params:
              for key in ("fractional_kelly", "ev_threshold", "edge_threshold"):
                  if key in self._override_params[regime_key]:
                      params[key] = self._override_params[regime_key][key]
          return params
      ```

   c. 既存のget_strategy_params()の本体を_get_base_params()にリネーム(ハードコード値をそのまま保持)し、get_strategy_params()をオーバーライド ロジックのシムにする。

2. **src/betting/meta_switcher.py** — _default_params() の値をRegimeDetectorに揃える(D-16):
   現在の乖離:
   | レジーム | パラメータ | MetaSwitcher(現在) | RegimeDetector(現在) | 変更後MetaSwitcher |
   |----------|-----------|-------------------|---------------------|------------------|
   | AGGRESSIVE | ev_threshold | 1.15 | 1.10 | **1.10** |
   | CONSERVATIVE | ev_threshold | 1.35 | 1.30 | **1.30** |
   | CONSERVATIVE | edge_threshold | 0.07 | 0.06 | **0.06** |
   | COLLAPSED | ev_threshold | 1.55 | 1.50 | **1.50** |
   | COLLAPSED | edge_threshold | 0.10 | 0.09 | **0.09** |

   変更:
   - AGGRESSIVE: `"ev_threshold": 1.15` → `"ev_threshold": 1.10`
   - CONSERVATIVE: `"ev_threshold": 1.35` → `"ev_threshold": 1.30`, `"edge_threshold": 0.07` → `"edge_threshold": 0.06`
   - COLLAPSED: `"ev_threshold": 1.55` → `"ev_threshold": 1.50`, `"edge_threshold": 0.10` → `"edge_threshold": 0.09`
   - コメント内の "raised from X.Y" は削除(統一済みのため不要)
  </action>
  <acceptance_criteria>
    1. `grep "override_params" src/models/regime_detector.py` にヒットすること(コンストラクタ引数)
    2. `grep "_override_params" src/models/regime_detector.py` にヒットすること(インスタンス変数)
    3. `grep "_get_base_params" src/models/regime_detector.py` にヒットすること
    4. MetaSwitcher AGGRESSIVE ev_threshold == 1.10 (grep `"ev_threshold": 1.10` in meta_switcher.py)
    5. MetaSwitcher CONSERVATIVE ev_threshold == 1.30, edge_threshold == 0.06
    6. MetaSwitcher COLLAPSED ev_threshold == 1.50, edge_threshold == 0.09
    7. `python -c "from models.regime_detector import RegimeDetector; d = RegimeDetector(override_params={'aggressive': {'fractional_kelly': 0.8}}); print(d.get_strategy_params(__import__('domain.types', fromlist=['RegimeState']).RegimeState.AGGRESSIVE)['fractional_kelly'])"` → 0.8
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/hirom/develop/keiba-ai && python -m pytest tests/test_regime_detector.py tests/test_meta_switcher.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>RegimeDetector.get_strategy_params()の主要3パラメータがコンストラクタ注入で上書き可能。MetaSwitcher._default_params()の値がRegimeDetectorと一致</done>
</task>

<task type="auto">
  <name>Task 2: ParameterFreezeProtocol JSON manifest + SHA256 + テスト</name>
  <files>src/backtest/parameter_freeze_protocol.py, tests/test_parameter_freeze.py</files>
  <read_first>
    src/backtest/parameter_freeze_protocol.py (全体)
    tests/test_parameter_freeze.py (全体)
  </read_first>
  <action>
Per D-13, D-14:

1. **src/backtest/parameter_freeze_protocol.py** — 戦略パラメータJSON manifest機能を追加:

   a. 既存クラスに変更を加えず、モジュールレベル関数として追加:
      ```python
      import json
      from pathlib import Path

      def save_strategy_manifest(
          params: dict[str, Any],
          path: Path,
      ) -> str:
          """戦略パラメータをJSON manifestとして保存 + SHA256ハッシュ返却

          D-13: Optuna最適化完了後のパラメータ保存用。
          JSON形式で人間可読 + SHA256で改ざん検知。

          Args:
              params: 戦略パラメータdict (Optuna best_params等)
              path: manifest保存先パス

          Returns:
              SHA256ハッシュ文字列
          """
          data = json.dumps(params, sort_keys=True, indent=2)
          sha256 = hashlib.sha256(data.encode()).hexdigest()
          manifest = {"params": params, "sha256": sha256}
          path.write_text(json.dumps(manifest, indent=2), encoding="utf-8")
          logger.info(f"Strategy manifest saved: {path} (sha256={sha256[:8]}...)")
          return sha256

      def verify_strategy_manifest(path: Path) -> dict[str, Any]:
          """JSON manifestのSHA256照合

          D-14: テスト期間バックテスト実行前にmanifestを読み込みハッシュ照合。
          不一致時はValueError送出。

          Args:
              path: manifestファイルパス

          Returns:
              検証済みパラメータdict

          Raises:
              ValueError: SHA256ハッシュ不一致
              FileNotFoundError: manifestファイル不在
          """
          if not path.exists():
              raise FileNotFoundError(f"Strategy manifest not found: {path}")
          manifest = json.loads(path.read_text(encoding="utf-8"))
          expected = manifest["sha256"]
          actual = hashlib.sha256(
              json.dumps(manifest["params"], sort_keys=True).encode()
          ).hexdigest()
          if actual != expected:
              raise ValueError(
                  f"Strategy manifest hash mismatch: expected={expected[:8]}... actual={actual[:8]}... "
                  f"Parameters may have been tampered with."
              )
          logger.info(f"Strategy manifest verified: {path} (sha256={actual[:8]}...)")
          return manifest["params"]

      def load_and_freeze_strategy(
          params: dict[str, Any],
          manifest_path: Path,
      ) -> None:
          """パラメータを保存し、manifestが既に存在する場合は検証

          D-14: Optuna完了後の自動生成用。
          manifestが存在しない場合は新規保存。
          manifestが存在する場合はSHA256照合を実行。

          Args:
              params: 保存するパラメータ
              manifest_path: manifestパス
          """
          if manifest_path.exists():
              saved = verify_strategy_manifest(manifest_path)
              if saved != params:
                  logger.warning(
                      "Strategy params differ from manifest. "
                      "Optuna may have produced different results."
                  )
          else:
              save_strategy_manifest(params, manifest_path)
      ```

   b. `from pathlib import Path` をファイル先頭のimportに追加(Pathが未importの場合)

2. **tests/test_parameter_freeze.py** — JSON manifest テストを追加:

   a. 以下のテストを追加(tmp_path fixtureを使用):
      ```python
      from pathlib import Path
      from backtest.parameter_freeze_protocol import (
          save_strategy_manifest,
          verify_strategy_manifest,
          load_and_freeze_strategy,
      )

      class TestStrategyManifest:
          def test_save_creates_json_file(self, tmp_path):
              """save_strategy_manifest がJSON manifestを作成"""
              params = {"fk_aggressive": 0.5, "dd_threshold_1": 0.10}
              path = tmp_path / "strategy_manifest.json"
              sha = save_strategy_manifest(params, path)
              assert path.exists()
              assert len(sha) == 64  # SHA256 hex

          def test_manifest_contains_params_and_sha256(self, tmp_path):
              """manifestがparams + sha256を含む"""
              params = {"fk_aggressive": 0.5}
              path = tmp_path / "strategy_manifest.json"
              save_strategy_manifest(params, path)
              import json
              manifest = json.loads(path.read_text())
              assert "params" in manifest
              assert "sha256" in manifest
              assert manifest["params"] == params

          def test_verify_returns_params_on_match(self, tmp_path):
              """SHA256一致時にparamsを返す"""
              params = {"fk_aggressive": 0.5, "ev_threshold": 1.10}
              path = tmp_path / "strategy_manifest.json"
              save_strategy_manifest(params, path)
              result = verify_strategy_manifest(path)
              assert result == params

          def test_verify_raises_on_tampered_manifest(self, tmp_path):
              """SHA256不一致時にValueError"""
              params = {"fk_aggressive": 0.5}
              path = tmp_path / "strategy_manifest.json"
              save_strategy_manifest(params, path)
              # manifest改ざん
              import json
              manifest = json.loads(path.read_text())
              manifest["params"]["fk_aggressive"] = 0.9  # 値変更
              path.write_text(json.dumps(manifest, indent=2))
              with pytest.raises(ValueError, match="hash mismatch"):
                  verify_strategy_manifest(path)

          def test_verify_raises_on_missing_file(self, tmp_path):
              """manifest不在時にFileNotFoundError"""
              path = tmp_path / "nonexistent.json"
              with pytest.raises(FileNotFoundError):
                  verify_strategy_manifest(path)

          def test_sha256_deterministic(self, tmp_path):
              """同じparamsで同じSHA256"""
              params = {"a": 1, "b": 2.0}
              path1 = tmp_path / "m1.json"
              path2 = tmp_path / "m2.json"
              sha1 = save_strategy_manifest(params, path1)
              sha2 = save_strategy_manifest(params, path2)
              assert sha1 == sha2

          def test_load_and_freeze_saves_new(self, tmp_path):
              """manifest不在時に新規保存"""
              params = {"fk_aggressive": 0.5}
              path = tmp_path / "strategy_manifest.json"
              load_and_freeze_strategy(params, path)
              assert path.exists()
              result = verify_strategy_manifest(path)
              assert result == params

          def test_load_and_freeze_verifies_existing(self, tmp_path):
              """manifest存在時に検証"""
              params = {"fk_aggressive": 0.5}
              path = tmp_path / "strategy_manifest.json"
              save_strategy_manifest(params, path)
              load_and_freeze_strategy(params, path)  # 同じparams → エラーなし
      ```
  </action>
  <acceptance_criteria>
    1. `grep "def save_strategy_manifest" src/backtest/parameter_freeze_protocol.py` にヒット
    2. `grep "def verify_strategy_manifest" src/backtest/parameter_freeze_protocol.py` にヒット
    3. `grep "def load_and_freeze_strategy" src/backtest/parameter_freeze_protocol.py` にヒット
    4. `grep "sha256" src/backtest/parameter_freeze_protocol.py | grep -v "^#" | grep -c ""` >= 3
    5. `python -m pytest tests/test_parameter_freeze.py -v` 全テストPASS
    6. テストに TestStrategyManifest クラスが8つ以上のテストメソッドを含む
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/hirom/develop/keiba-ai && python -m pytest tests/test_parameter_freeze.py -v --tb=short</automated>
  </verify>
  <done>ParameterFreezeProtocolに戦略パラメータJSON manifest保存・SHA256検証機能が追加され、全テストがPASSする</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Optuna → JSON manifest | 最適化結果がmanifestに書き込まれる。manifestの改ざんが検出可能である必要 |
| JSON manifest → BacktestEngine | テスト期間中のパラメータ不変性をSHA256で保証 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-13-03 | T (Tampering) | strategy manifest JSON | mitigate | SHA256ハッシュで改ざん検知。verify_strategy_manifest()が不一致時にValueError |
| T-13-04 | I (Info Disclosure) | strategy manifest JSON | accept | パスワード・APIキー等の機密情報は含まれない。パブリックな戦略パラメータのみ |
</threat_model>

<verification>
1. `python -m pytest tests/test_parameter_freeze.py -v` 全テストPASS
2. `python -c "from backtest.parameter_freeze_protocol import save_strategy_manifest, verify_strategy_manifest, load_and_freeze_strategy; print('OK')"` → OK
3. `python -c "from models.regime_detector import RegimeDetector; d = RegimeDetector(override_params={'aggressive': {'ev_threshold': 1.5}}); p = d.get_strategy_params(__import__('domain.types', fromlist=['RegimeState']).RegimeState.AGGRESSIVE); assert p['ev_threshold'] == 1.5; print('OK')"` → OK
</verification>

<success_criteria>
1. RegimeDetectorがoverride_paramsで主要3パラメータを上書き可能
2. MetaSwitcher._default_params()がRegimeDetector.get_strategy_params()と完全一致
3. save_strategy_manifest()がJSON + SHA256でmanifestを生成
4. verify_strategy_manifest()がSHA256不一致を検出
5. 既存のParameterFreezeProtocol機能(model pickle凍結)が無傷
6. テストが全てPASS
</success_criteria>

<output>
After completion, create `.planning/phases/13-risk-calibration-parameter-optimization/13-02-SUMMARY.md`
</output>
