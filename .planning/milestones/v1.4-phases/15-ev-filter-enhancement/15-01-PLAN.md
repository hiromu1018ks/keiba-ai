---
phase: 15-ev-filter-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/models.py
  - src/pipelines/training_pipeline.py
  - src/backtest/race_predictor.py
  - tests/test_race_predictor.py
autonomous: true
requirements: [EVF-01]

must_haves:
  truths:
    - "SubmodelSetにev_lower_threshold_turf/ev_lower_threshold_dirtフィールドが存在し、アンサンブルOOF分布から計算された値が格納される"
    - "get_win_candidates()が固定1.0ではなくSubmodelSetから取得した動的閾値でEV_lowerフィルタリングを実行する"
    - "EV_lowerがNaNの場合、サーフェス別デフォルト閾値(芝0.8/ダート0.7)にフォールバックする"
    - "TrainingPipelineがOOF positive-edge winnersから25th percentileを計算しSubmodelSetに格納する"
  artifacts:
    - path: "src/domain/models.py"
      provides: "SubmodelSet.ev_lower_threshold_turf, ev_lower_threshold_dirt フィールド"
      contains: "ev_lower_threshold_turf"
    - path: "src/pipelines/training_pipeline.py"
      provides: "動的閾値計算ロジック(_compute_ev_threshold) + SubmodelSet格納"
      contains: "_compute_ev_threshold"
    - path: "src/backtest/race_predictor.py"
      provides: "動的閾値を用いたEV_lowerフィルター"
      contains: "ev_lower_threshold_turf"
  key_links:
    - from: "src/pipelines/training_pipeline.py"
      to: "src/domain/models.py"
      via: "SubmodelSet() constructor引数に閾値を渡す"
      pattern: "ev_lower_threshold_turf="
    - from: "src/backtest/race_predictor.py"
      to: "src/domain/models.py"
      via: "submodel.ev_lower_threshold_turf/dirt から閾値を取得"
      pattern: "ev_lower_threshold"
---

<objective>
EV_lower固定閾値1.0をアンサンブルOOF分布ベースの動的閾値に置き換え、過剰除外(3,594件)を根本的に解決する

Purpose: D-06に基づくConformal再キャリブレーションは不要(OOF残差は既にアンサンブル由来。RESEARCH A1検証済み)。D-01/D-02/D-03に従い、OOF positive-edge winnersの25th percentileからサーフェス別閾値を計算し、race_predictor.pyのフィルターに適用する。
Output: SubmodelSet新フィールド + 動的閾値計算 + フィルター適用 + テスト更新
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-ev-filter-enhancement/15-CONTEXT.md
@.planning/phases/15-ev-filter-enhancement/15-RESEARCH.md
@src/domain/models.py
@src/pipelines/training_pipeline.py
@src/backtest/race_predictor.py
@tests/test_race_predictor.py

<interfaces>
<!-- SubmodelSet dataclass (src/domain/models.py:228-255) -->
```python
@dataclass
class SubmodelSet:
    market: MarketModel
    stage1: AbilityModel
    place_ability: PlaceAbilityModel
    win: WinTwoStageModel
    ev_corrector: EVCorrectionModel
    place: PlaceTwoStageModel
    place_ev_corrector: PlaceEVCorrectionModel
    wide: WideTwoStageModel
    confidence: RobustConfidenceEstimator
    place_selection_gate: PlaceSelectionGateModel | None = None
    use_ensemble: bool = False
    benter_combo: BenterCombination | None = None
    isotonic_calibrator: IsotonicRegression | None = None
    temperature_scaler: TemperatureScaling | None = None
    win_benter: BenterCombination | None = None
    win_isotonic_calibrator: IsotonicRegression | None = None
    win_temperature_scaler: TemperatureScaling | None = None
    win_selection_gate: WinSelectionGateModel | None = None
    # NEW FIELDS TO ADD:
    # ev_lower_threshold_turf: float = 1.0
    # ev_lower_threshold_dirt: float = 1.0
```

<!-- Current EV_lower filter (src/backtest/race_predictor.py:437-451) -->
```python
if ev_lower_col in race_df.columns:
    ev_lower = pd.to_numeric(race_df[ev_lower_col], errors="coerce")
    ev_mask = ev_lower.fillna(1.0) >= 1.0   # <-- REPLACE with dynamic threshold
    mask &= ev_mask
```

<!-- Pipeline SubmodelSet construction (src/pipelines/training_pipeline.py:817-836) -->
```python
return SubmodelSet(
    market=market, stage1=stage1, place_ability=place_ability,
    win=win_2s, ev_corrector=ev_corrector, place=place_2s,
    place_ev_corrector=place_ev_corrector, wide=wide_2s,
    confidence=conf, place_selection_gate=place_selection_gate,
    use_ensemble=use_ensemble, benter_combo=benter_combo,
    isotonic_calibrator=isotonic_cal, temperature_scaler=temp_scaler,
    win_benter=win_benter, win_isotonic_calibrator=win_isotonic_cal,
    win_temperature_scaler=win_temp_scaler,
    win_selection_gate=win_selection_gate,
    # ADD: ev_lower_threshold_turf=..., ev_lower_threshold_dirt=...
)
```

<!-- Test mock pattern (tests/test_race_predictor.py:13-28, 34-47) -->
```python
def _make_submodel_mock() -> MagicMock:
    sm = MagicMock()
    # ... existing fields ...
    # ADD: sm.ev_lower_threshold_turf = 1.0
    # ADD: sm.ev_lower_threshold_dirt = 1.0

@pytest.fixture
def mock_models() -> MagicMock:
    models = MagicMock(spec=TrainedModelsV5)
    models.submodels = {"turf": _make_submodel_mock()}
    # ... rest of fixture ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dynamic threshold fields to SubmodelSet and compute in pipeline</name>
  <files>src/domain/models.py, src/pipelines/training_pipeline.py</files>
  <read_first>
    - src/domain/models.py (SubmodelSet dataclass, lines 228-255)
    - src/pipelines/training_pipeline.py (_train_submodel method, lines 750-836)
    - .planning/phases/15-ev-filter-enhancement/15-CONTEXT.md (D-01, D-02, D-03)
    - .planning/phases/15-ev-filter-enhancement/15-RESEARCH.md (Pattern 3: Dynamic Threshold)
  </read_first>
  <action>
**1. src/domain/models.py** -- SubmodelSetに2つの新フィールドを追加:

`win_selection_gate` フィールドの後に以下を追加:
```python
    # EV_lower dynamic threshold (D-01/D-02, EVF-01)
    ev_lower_threshold_turf: float = 1.0
    ev_lower_threshold_dirt: float = 1.0
```

**2. src/pipelines/training_pipeline.py** -- 閾値計算関数とSubmodelSet格納:

(a) クラスメソッドまたはモジュール関数として `_compute_ev_threshold()` を追加(TrainingPipelineV5クラス内の `_train_submodel` の前にプライベートスタティックメソッドとして定義):

```python
@staticmethod
def _compute_ev_threshold(
    df_oof: pd.DataFrame,
    surface: str,
    fallback: float = 1.0,
) -> float:
    """D-01/D-02: アンサンブルOOF positive-edge winnersの25th percentileから動的閾値を計算.

    positive-edge winners = kakuteijyuni==1 AND win_selection_edge > 0
    """
    surf_df = df_oof[df_oof["surface"] == surface]
    if "EV_lower_win_corrected" not in surf_df.columns:
        return fallback
    winners = surf_df[
        (surf_df["kakuteijyuni"] == 1) &
        (surf_df["win_selection_edge"] > 0)
    ]
    ev_lower_values = pd.to_numeric(
        winners["EV_lower_win_corrected"], errors="coerce"
    ).dropna()
    if len(ev_lower_values) < 30:
        logger.info(
            "EV threshold for %s: too few positive-edge winners (%d), using fallback %.2f",
            surface, len(ev_lower_values), fallback,
        )
        return fallback
    threshold = float(ev_lower_values.quantile(0.25))
    logger.info(
        "EV threshold for %s: %.4f (from %d positive-edge winners, "
        "q25=%.4f q50=%.4f q75=%.4f)",
        surface, threshold, len(ev_lower_values),
        float(ev_lower_values.quantile(0.25)),
        float(ev_lower_values.quantile(0.50)),
        float(ev_lower_values.quantile(0.75)),
    )
    return threshold
```

(b) `_train_submodel()` 内の `return SubmodelSet(...)` の直前(WinSelectionGate構築の後、drift diagnosticsの前)、閾値計算を追加。drift diagnosticsブロック(792-803行目)の直後に挿入:

```python
        # --- D-01/D-02: Dynamic EV_lower threshold from OOF winners ---
        # D-03 fallback values: conservative defaults below 1.0
        ev_threshold_turf = self._compute_ev_threshold(
            df_oof, surface="turf", fallback=0.8,
        )
        ev_threshold_dirt = self._compute_ev_threshold(
            df_oof, surface="dirt", fallback=0.7,
        )
```

(c) `return SubmodelSet(...)` に2引数を追加:
```python
            ev_lower_threshold_turf=ev_threshold_turf,
            ev_lower_threshold_dirt=ev_threshold_dirt,
```

注意: この閾値計算は `wsg_train_df` が構築された後に実行する。`wsg_train_df` には `conf.predict_lower_bound()` で計算された `EV_lower_win_corrected` が含まれているため、`df_oof` ではなく `wsg_train_df` を閾値計算の入力に使用する。ただし `wsg_train_df = df_oof.copy()` であり、EV_lower列は `wsg_win_df` 経由でコピーされる。確認結果: `df_oof` 自体にはEV_lower列がまだないため、`wsg_train_df` を使用する:

```python
        ev_threshold_turf = self._compute_ev_threshold(
            wsg_train_df, surface="turf", fallback=0.8,
        )
        ev_threshold_dirt = self._compute_ev_threshold(
            wsg_train_df, surface="dirt", fallback=0.7,
        )
```

この計算は drift diagnostics ブロックの後に配置する。
  </action>
  <verify>
    <automated>cd C:\Users\hirom\develop\keiba-ai && python -c "from domain.models import SubmodelSet; s = SubmodelSet.__dataclass_fields__; assert 'ev_lower_threshold_turf' in s; assert 'ev_lower_threshold_dirt' in s; print('OK: fields exist')" && python -c "from pipelines.training_pipeline import TrainingPipelineV5; assert hasattr(TrainingPipelineV5, '_compute_ev_threshold'); print('OK: method exists')"</automated>
  </verify>
  <done>
    - SubmodelSetにev_lower_threshold_turf(float, default=1.0)とev_lower_threshold_dirt(float, default=1.0)が追加されている
    - TrainingPipelineV5._compute_ev_threshold()が定義され、positive-edge winnersの25th percentileを計算する
    - _train_submodel()の戻り値SubmodelSetに計算済み閾値が格納される
    - logger.infoで閾値と分布統計(n, q25, q50, q75)が出力される
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace fixed EV_lower filter with dynamic threshold in get_win_candidates()</name>
  <files>src/backtest/race_predictor.py, tests/test_race_predictor.py</files>
  <read_first>
    - src/backtest/race_predictor.py (get_win_candidates method, lines 408-490)
    - src/backtest/race_predictor.py (predict_race method surface_key usage, lines 72-76)
    - tests/test_race_predictor.py (TestGetWinCandidates class, lines 1340-1486)
    - tests/test_race_predictor.py (_make_submodel_mock, lines 13-28)
    - .planning/phases/15-ev-filter-enhancement/15-CONTEXT.md (D-03)
  </read_first>
  <action>
**1. src/backtest/race_predictor.py** -- get_win_candidates()のEV_lowerフィルターを動的閾値に変更:

race_predictor.py line 437-451の `if ev_lower_col in race_df.columns:` ブロックを以下に置換:

```python
        # D-01/D-02: Dynamic EV_lower threshold from SubmodelSet
        if ev_lower_col in race_df.columns:
            ev_lower = pd.to_numeric(race_df[ev_lower_col], errors="coerce")
            # Resolve threshold from submodel (surface-specific, D-02)
            threshold = 1.0  # default for unknown surface
            if "surface" in race_df.columns and not race_df.empty:
                surf_key = str(race_df["surface"].iloc[0])
                _sm = self.models.submodels.get(surf_key)
                if _sm is not None:
                    if surf_key == "turf":
                        threshold = float(getattr(_sm, "ev_lower_threshold_turf", 1.0))
                    elif surf_key == "dirt":
                        threshold = float(getattr(_sm, "ev_lower_threshold_dirt", 1.0))
            # D-03: NaN fallback to surface-specific threshold (not fillna(1.0))
            ev_mask = ev_lower.fillna(threshold) >= threshold
            mask &= ev_mask
            _n_ev_excluded = n_before_ev - int(mask.sum())
            if _n_ev_excluded > 0:
                ev_total = len(race_df)
                logger.info(
                    "EV filter: excluded %d candidates (%.1f%%), "
                    "EV_lower < %.4f (threshold) in race %s",
                    _n_ev_excluded,
                    100.0 * _n_ev_excluded / max(ev_total, 1),
                    threshold,
                    race_df["race_id"].iloc[0] if "race_id" in race_df.columns else "?",
                )
```

**2. tests/test_race_predictor.py** -- テスト更新:

(a) `_make_submodel_mock()` (line 13-28)に新フィールド追加:
```python
    sm.ev_lower_threshold_turf = 1.0
    sm.ev_lower_threshold_dirt = 1.0
```

(b) `test_ev_lower_filter_excludes_below_threshold` (line 1436-1447):
動的閾値のデフォルト1.0が使われるため、テストロジックはそのまま有効(値1.2が1.0以上なので合格、0.8と0.5は1.0未満で除外)。ただし明示的にSubmodelSetの閾値を設定するテストも追加する。

(c) 新しいテストケースを TestGetWinCandidates に追加:

```python
    def test_ev_lower_dynamic_threshold_turf(self, mock_models: MagicMock) -> None:
        """D-01/D-02: turf SubmodelSet閾値0.85の時、EV_lower 0.90は合格"""
        from backtest.race_predictor import RacePredictor

        # Set dynamic threshold for turf
        mock_models.submodels["turf"].ev_lower_threshold_turf = 0.85
        predictor = RacePredictor(models=mock_models)
        race_df = self._make_win_race_df(
            n=3,
            EV_lower_win_corrected=[0.90, 0.80, 1.5],  # 0.90 >= 0.85 pass, 0.80 < 0.85 fail
        )
        result = predictor.get_win_candidates(race_df)
        # 0.90 passes (>= 0.85), 0.80 fails (< 0.85), 1.5 passes
        assert len(result) == 2
        assert set(result["umaban"].tolist()) == {1, 3}

    def test_ev_lower_dynamic_threshold_dirt(self, mock_models: MagicMock) -> None:
        """D-02: dirt SubmodelSet閾値0.70の時、EV_lower 0.65は除外"""
        from backtest.race_predictor import RacePredictor

        sm = _make_submodel_mock()
        sm.ev_lower_threshold_dirt = 0.70
        mock_models.submodels["dirt"] = sm
        predictor = RacePredictor(models=mock_models)
        race_df = self._make_win_race_df(
            n=3,
            surface=["dirt"] * 3,
            EV_lower_win_corrected=[0.75, 0.65, 1.2],  # 0.75 >= 0.70 pass
        )
        result = predictor.get_win_candidates(race_df)
        assert len(result) == 2
        assert set(result["umaban"].tolist()) == {1, 3}

    def test_ev_lower_nan_uses_surface_fallback(self, mock_models: MagicMock) -> None:
        """D-03: EV_lower NaNの場合、サーフェス別デフォルト閾値でフォールバック"""
        from backtest.race_predictor import RacePredictor

        mock_models.submodels["turf"].ev_lower_threshold_turf = 0.80
        predictor = RacePredictor(models=mock_models)
        race_df = self._make_win_race_df(
            n=2,
            EV_lower_win_corrected=[float("nan"), float("nan")],
        )
        result = predictor.get_win_candidates(race_df)
        # NaN -> fillna(0.80) -> 0.80 >= 0.80 -> both pass
        assert len(result) == 2
```

注意: `_make_win_race_df` に `surface` キーワードを渡せるように、メソッドの `overrides` 処理で `surface` 列が追加されることを確認する。既存の実装(line 1344-1359)では `**overrides` が `data` dictを上書きするため `surface=["dirt"]*3` を渡せば列が追加される。

(d) `test_ev_lower_nan_fallback_to_edge_only` (line 1449-1460) を更新:
このテストは NaN の場合に全馬が合格することを期待しているが、新しいコードでは `fillna(threshold)` で `threshold >= threshold` が成り立つので合格する。テストのassertionはそのまま有効。
  </action>
  <verify>
    <automated>cd C:\Users\hirom\develop\keiba-ai && python -m pytest tests/test_race_predictor.py::TestGetWinCandidates -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
    - race_predictor.py get_win_candidates()が固定1.0ではなくSubmodelSetから動的閾値を取得する
    - サーフェス別(thurf/dirt)に独立した閾値を使用する (D-02)
    - NaNの場合、サーフェス別デフォルト閾値にフォールバックする (D-03)
    - 除外ログに閾値の実際の値が含まれる
    - 全TestGetWinCandidatesテスト(既存+新規3件)が通過する
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pipeline -> SubmodelSet | 閾値計算結果がデータ型制約なしでfloatフィールドに格納される |
| SubmodelSet -> RacePredictor | 推論時の閾値取得。getattrのデフォルト値がセーフティネット |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | _compute_ev_threshold | mitigate | 最小サンプル数30件で閾値計算を保護。サンプル不足時はフォールバック値を使用 |
| T-15-02 | Denial of Service | get_win_candidates | accept | 閾値が極端に低い場合、除外件数が減るだけでシステム破綻はない。Phase 17 Optunaで最適化 |
| T-15-03 | Tampering | SubmodelSet fields | mitigate | getattr()のデフォルト1.0で未設定フィールドを保護。dataclass default=1.0で後方互換 |
</threat_model>

<verification>
1. SubmodelSetにev_lower_threshold_turf/dirtフィールドが存在: `python -c "from domain.models import SubmodelSet; assert 'ev_lower_threshold_turf' in SubmodelSet.__dataclass_fields__"`
2. _compute_ev_thresholdが定義されている: `python -c "from pipelines.training_pipeline import TrainingPipelineV5; assert hasattr(TrainingPipelineV5, '_compute_ev_threshold')"`
3. race_predictor.pyが固定1.0を含まない: `grep -n "fillna(1\.0)" src/backtest/race_predictor.py | grep -v "# " || echo "OK: no hardcoded fillna(1.0)"`
4. 全テスト通過: `python -m pytest tests/test_race_predictor.py::TestGetWinCandidates -v`
</verification>

<success_criteria>
- SubmodelSetにev_lower_threshold_turf/dirtフィールドが追加されている
- _train_submodel()がOOF positive-edge winnersから25th percentileを計算して格納する
- get_win_candidates()がサーフェス別動的閾値でEV_lowerをフィルタリングする
- NaNの場合サーフェス別フォールバック(芝0.8/ダート0.7)を使用する
- 全TestGetWinCandidatesテストが通過する
</success_criteria>

<output>
After completion, create `.planning/phases/15-ev-filter-enhancement/15-01-SUMMARY.md`
</output>
