---
phase: 19-ev-calibration
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - tests/test_ev_correction.py
  - tests/test_ev_isotonic.py
autonomous: true
requirements:
  - EVC-03

must_haves:
  truths:
    - "correct_ev()がIsotonic + オッズバンドスケーリングを適用してev_win_calibratedを生成する"
    - "Isotonic未設定時はev_win_calibrated = ev_win_correctedでフォールバックする"
    - "OOF EV生成がK-fold(shuffle=False)でlook-ahead biasなく動作する"
    - "fit_ev_calibration()がIsotonicとオッズバンドスケーリングを正しく返す"
    - "全テストがDB不要(mockベース)で実行される"
  artifacts:
    - path: "tests/test_ev_isotonic.py"
      provides: "Isotonic EVキャリブレーションとオッズバンド補正のテストスイート"
      exports: ["TestEVIsotonicCalibration", "TestOddsBandScaling", "TestOOFEVGeneration", "TestEVCorrectionIntegration"]
    - path: "tests/test_ev_correction.py"
      provides: "拡張EVCorrectionModelテスト(ev_win_calibrated列の検証)"
      contains: "ev_win_calibrated"
  key_links:
    - from: "tests/test_ev_isotonic.py"
      to: "src/models/ev_correction_model.py"
      via: "EVCorrectionModel correct_ev() with Isotonic + band scales"
      pattern: "ev_win_calibrated"
    - from: "tests/test_ev_isotonic.py"
      to: "src/pipelines/training_pipeline.py"
      via: "generate_ev_oof_predictions + fit_ev_calibration"
      pattern: "generate_ev_oof_predictions"
---

<objective>
Phase 19-01の実装に対する包括的テストスイートを作成し、Isotonic EVキャリブレーションとパイプライン統合の品質を保証する。

Purpose: EVC-03要件(EVCorrectionModel統合 + パイプライン適用)の品質を自動テストで検証する。correct_ev()のIsotonic適用、オッズバンドスケーリング、OOF生成、フォールバック動作を全てカバーする。
Output: test_ev_isotonic.py (新規) + test_ev_correction.py (拡張)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-ev-calibration/19-CONTEXT.md
@.planning/phases/19-ev-calibration/19-RESEARCH.md

<!-- After 19-01 completes, read these SUMMARY for implementation details -->
@.planning/phases/19-ev-calibration/19-01-SUMMARY.md

<interfaces>
<!-- Key interfaces from 19-01 implementation -->

From src/models/ev_correction_model.py — EVCorrectionModel (after 19-01):
```python
class EVCorrectionModel:
    def __init__(
        self,
        *,
        ev_isotonic_calibrator: IsotonicRegression | None = None,
        ev_odds_band_scales: dict[str, float] | None = None,
    ) -> None: ...

    def correct_ev(self, df: pd.DataFrame) -> pd.DataFrame:
        # Returns df with: ev_win_corrected, ev_win_calibrated
        ...
```

From src/pipelines/training_pipeline.py — new methods (after 19-01):
```python
class TrainingPipelineV5:
    @staticmethod
    def generate_ev_oof_predictions(
        df: pd.DataFrame, *, n_splits: int = 5, num_threads: int = 0,
    ) -> tuple[np.ndarray, np.ndarray, np.ndarray]:
        # Returns: (oof_ev_corrected, oof_actual_return, oof_odds)

    @staticmethod
    def fit_ev_calibration(
        oof_ev: np.ndarray,
        oof_actual: np.ndarray,
        oof_odds: np.ndarray,
    ) -> tuple[IsotonicRegression, dict[str, float]]:
        # Returns: (isotonic_model, odds_band_scales)
```

From src/betting/odds_band_filter.py — OddsBandFilter:
```python
BANDS = [(1.0, 3.0), (3.0, 10.0), (10.0, 30.0), (30.0, float("inf"))]
BAND_NAMES = ["1.0-3.0", "3.0-10.0", "10.0-30.0", "30.0+"]
```

From src/domain/models.py — SubmodelSet (after 19-01):
```python
    ev_isotonic_calibrator: IsotonicRegression | None = None
    ev_odds_band_scales: dict[str, float] | None = None
```

<!-- Existing test fixture pattern from tests/test_ev_correction.py -->
```python
@pytest.fixture
def pre_ev_df() -> pd.DataFrame:
    return pd.DataFrame({
        "race_id": ["R1"] * 8,
        "kakuteijyuni": [1, 2, 3, 4, 5, 6, 7, 8],
        "odds": [4.0, 6.0, 9.0, 16.0, 28.0, 45.0, 90.0, 160.0],
        "p_win_pred": [...],
        "e_return_win_pred": [...],
        "ev_win": [...],
        # ... 全EVCorrectionModel.FEATURE_COLSの列
    })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Isotonic + オッズバンド別補正 + OOF生成のテストスイート作成</name>
  <files>
    tests/test_ev_isotonic.py
  </files>
  <read_first>
    tests/test_ev_correction.py (全文 — fixtureパターン参照),
    src/models/ev_correction_model.py (correct_evメソッド全文),
    src/pipelines/training_pipeline.py (generate_ev_oof_predictions + fit_ev_calibration),
    src/betting/odds_band_filter.py (BANDS/BAND_NAMES),
    src/domain/models.py (SubmodelSet新フィールド)
  </read_first>
  <action>
新規ファイル `tests/test_ev_isotonic.py` を作成する。既存の `tests/test_ev_correction.py` のmockパターン(fixture + MagicMock)に従う。全テストDB不要。

**共通fixture**:

```python
import numpy as np
import pandas as pd
import pytest
from unittest.mock import MagicMock, patch
from sklearn.isotonic import IsotonicRegression


@pytest.fixture
def ev_corrected_df() -> pd.DataFrame:
    """correct_ev()のPxE補正後の出力をシミュレートするDataFrame。
    ev_win_correctedが含まれている状態。"""
    return pd.DataFrame({
        "race_id": ["R1"] * 8,
        "kakuteijyuni": [1, 2, 3, 4, 5, 6, 7, 8],
        "confirmed_odds": [4.0, 6.0, 9.0, 16.0, 28.0, 45.0, 90.0, 160.0],
        "odds": [4.0, 6.0, 9.0, 16.0, 28.0, 45.0, 90.0, 160.0],
        "p_win_pred": [0.28, 0.22, 0.18, 0.12, 0.08, 0.06, 0.04, 0.02],
        "e_return_win_pred": [4.0, 6.0, 9.0, 16.0, 28.0, 45.0, 90.0, 160.0],
        "ev_win": [1.12, 1.32, 1.62, 1.92, 2.24, 2.70, 3.60, 3.20],
        "ev_win_corrected": [1.05, 1.20, 1.40, 1.60, 1.80, 2.20, 2.80, 2.50],
        "p_win_corrected": [0.26, 0.20, 0.16, 0.10, 0.07, 0.05, 0.03, 0.02],
        "e_return_win_corrected": [4.04, 6.0, 8.75, 16.0, 25.7, 44.0, 93.3, 125.0],
        "signed_log_error_win": [0.1, -0.1, 0.2, -0.3, 0.0, 0.5, -0.2, 0.3],
        "abs_log_error_win": [0.1, 0.1, 0.2, 0.3, 0.0, 0.5, 0.2, 0.3],
        "market_entropy": [2.5] * 8,
        "popularity_rank": [1, 2, 3, 4, 5, 6, 7, 8],
        "implied_prob_hhi": [0.08] * 8,
        "surface": ["turf"] * 8,
        "distance_bin": ["mile"] * 8,
        "track_condition_code": [1] * 8,
        "field_size": [8] * 8,
        "jockey_wr_overall": [0.12] * 8,
        "jockey_wr_distance": [0.10] * 8,
        "jockey_wr_venue": [0.11] * 8,
        "jockey_prize_log": [11.0] * 8,
        "trainer_wr_overall": [0.14] * 8,
        "trainer_wr_distance": [0.12] * 8,
        "trainer_wr_venue": [0.13] * 8,
        "trainer_prize_log": [10.5] * 8,
        "jt_combo_wr": [0.15] * 8,
        "jt_combo_place_rate": [0.25] * 8,
        "jt_combo_starts": [5.0] * 8,
        "jt_combo_prize_log": [4.0] * 8,
    })


@pytest.fixture
def mock_isotonic() -> IsotonicRegression:
    """IsotonicRegression mock: 高EVを押し下げるキャリブレーションをシミュレート"""
    iso = IsotonicRegression(y_min=0, out_of_bounds="clip")
    # 学習: EV過大評価パターン (高EVほどactualが低い)
    ev_train = np.array([0.5, 0.8, 1.0, 1.2, 1.5, 1.8, 2.0, 2.5, 3.0, 4.0])
    actual_train = np.array([0.3, 0.5, 0.7, 0.8, 0.9, 0.95, 1.0, 1.05, 1.1, 1.15])
    iso.fit(ev_train, actual_train)
    return iso


@pytest.fixture
def band_scales() -> dict[str, float]:
    """オッズバンド別スケーリング係数 (高オッズ帯を押し下げるパターン)"""
    return {
        "1.0-3.0": 1.0,
        "3.0-10.0": 0.95,
        "10.0-30.0": 0.85,
        "30.0+": 0.70,
    }
```

**TestEVIsotonicCalibration クラス** (Isotonic適用のテスト):

1. `test_correct_ev_produces_calibrated_column`: Isotonic設定時、ev_win_calibrated列が生成される
2. `test_calibrated_reduces_overestimation`: Isotonic適用後の高EV値が補正前より低いことを確認
3. `test_calibrated_non_negative`: ev_win_calibrated >= 0 (y_min=0制約)
4. `test_calibrated_preserves_order`: ev_win_correctedの順序がev_win_calibratedでも維持される (単調増加)
5. `test_correct_ev_no_isotonic_fallback`: Isotonic未設定時、ev_win_calibrated == ev_win_corrected

**TestOddsBandScaling クラス** (オッズバンド別スケーリングのテスト):

1. `test_band_scaling_applied_to_calibrated`: band_scales設定時、高オッズ帯のev_win_calibratedが縮小される
2. `test_band_scaling_no_scales`: band_scales未設定時、ev_win_calibratedがIsotonic結果と同じ
3. `test_band_scaling_per_band`: 各オッズバンドのスケーリングが正しく適用される
4. `test_band_scaling_missing_odds_column`: odds列がない場合、スケーリングがスキップされる

**TestOOFEVGeneration クラス** (OOF EV生成のテスト — mockベース):

1. `test_generate_ev_oof_returns_three_arrays`: generate_ev_oof_predictions()が3つのndarrayを返す
2. `test_generate_ev_oof_no_shuffle`: KFold(shuffle=False)が使用されること
3. `test_generate_ev_oof_sorts_by_race_date`: 入力dfがrace_dateでソートされること
4. `test_generate_ev_oof_all_indices_covered`: 全データポイントがOOF予測でカバーされる (NaNなし)

**TestEVCorrectionIntegration クラス** (統合テスト):

1. `test_full_pipeline_isotonic_and_band_scaling`: Isotonic + band scaling のフルパイプラインが正しく動作
2. `test_ev_corrected_column_unchanged`: ev_win_corrected列はIsotonic適用前後で不変 (下位互換性)
3. `test_model_init_accepts_isotonic`: EVCorrectionModel(ev_isotonic_calibrator=iso)が正常に初期化
4. `test_model_init_accepts_band_scales`: EVCorrectionModel(ev_odds_band_scales=scales)が正常に初期化
5. `test_submodelset_new_fields_default_none`: SubmodelSetの新フィールドのデフォルト値がNone

**注意点**:
- `generate_ev_oof_predictions`のテストではWinTwoStageModelとEVCorrectionModelをmockする必要がある。`patch`でWinTwoStageModelとEVCorrectionModelをmock化し、fold内学習チェーンをシミュレートする。
- fit_ev_calibrationのテストでは実際のIsotonicRegressionを使用する(軽量)。
- 全テストでDBアクセスなし。unittest.mock使用。
  </action>
  <verify>
    <automated>python -m pytest tests/test_ev_isotonic.py -v -x 2>&1 | tail -30</automated>
  </verify>
  <done>
    - tests/test_ev_isotonic.pyが存在し、4つのテストクラスが含まれる
    - TestEVIsotonicCalibration: 5テスト (Isotonic適用、フォールバック、非負、順序維持)
    - TestOddsBandScaling: 4テスト (スケーリング適用、未設定時、バンド別、odds列なし)
    - TestOOFEVGeneration: 4テスト (戻り値形式、shuffle=False、ソート、全カバー)
    - TestEVCorrectionIntegration: 5テスト (フルパイプライン、下位互換、初期化)
    - 全テストDB不要(mock使用)
    - python -m pytest tests/test_ev_isotonic.py -v が全通過
  </done>
</task>

<task type="auto">
  <name>Task 2: 既存テスト拡張 + 全テスト通過確認</name>
  <files>
    tests/test_ev_correction.py
  </files>
  <read_first>
    tests/test_ev_correction.py (全文),
    src/models/ev_correction_model.py (correct_evメソッド全文)
  </read_first>
  <action>
既存のtest_ev_correction.pyにev_win_calibrated列の検証テストを追加する。

**追加するテスト (TestEVCorrectionModel内)**:

1. `test_correct_ev_produces_ev_win_calibrated`:
   trained_ev_model(pre_ev_df)でcorrect_ev()を呼び出し、resultに"ev_win_calibrated"列が存在することを確認。Isotonic未設定のためev_win_calibrated == ev_win_correctedであることを検証。

2. `test_correct_ev_calibrated_equals_corrected_without_isotonic`:
   Isotonicなしのcorrect_ev()でev_win_calibratedがev_win_correctedと等しいことをnp.allcloseで検証。

3. `test_correct_ev_with_isotonic_produces_different_calibrated`:
   Isotonic付きのEVCorrectionModelでcorrect_ev()を呼び出し、ev_win_calibratedがev_win_correctedと異なることを検証。
   ```python
   iso = IsotonicRegression(y_min=0, out_of_bounds="clip")
   iso.fit(np.array([0.5, 1.0, 1.5, 2.0, 3.0]), np.array([0.3, 0.7, 0.9, 1.0, 1.1]))
   model = EVCorrectionModel(ev_isotonic_calibrator=iso)
   model.p_correction_model = trained_ev_model.p_correction_model
   model.e_correction_model = trained_ev_model.e_correction_model
   result = model.correct_ev(pre_ev_df)
   assert "ev_win_calibrated" in result.columns
   assert not np.allclose(result["ev_win_calibrated"].values, result["ev_win_corrected"].values)
   ```

4. `test_correct_ev_calibrated_non_negative`:
   Isotonic付きのcorrect_ev()で全ev_win_calibrated >= 0であることを確認。

**テスト実行して全通過確認**:
```bash
python -m pytest tests/ -v -x
```
  </action>
  <verify>
    <automated>python -m pytest tests/ -v -x 2>&1 | tail -30</automated>
  </verify>
  <done>
    - test_ev_correction.pyに4つの新テストが追加されている
    - ev_win_calibrated列のフォールバック(Isotonicなし)が検証されている
    - ev_win_calibrated列のIsotonic適用が検証されている
    - python -m pytest tests/ -v が全通過(既存 + 新規テスト)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test mock → Production code | テストはmockで生産コードを検証。mockの振る舞いが実際のsklearn IsotonicRegressionと整合している必要がある |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Tampering | テストのmock一貫性 | mitigate | IsotonicRegressionのテストでは実際のsklearnインスタンスを使用。mockはEVCorrectionModel.predictのみに使用 |
| T-19-06 | Denial | テストカバレッジ | accept | 全エッジケースをカバー不可能だが、主要パス(適用/フォールバック/統合)は確実にテスト |
</threat_model>

<verification>
1. `python -m pytest tests/ -v -x` — 全テスト通過
2. `python -m pytest tests/test_ev_isotonic.py -v` — 新規テストファイルが18テスト以上を含む
3. `grep -c "ev_win_calibrated" tests/test_ev_correction.py` → 4以上
4. `grep -c "def test_" tests/test_ev_isotonic.py` → 15以上
5. `python -m pytest tests/ --co -q 2>&1 | wc -l` — テスト数が既存より18以上増加
</verification>

<success_criteria>
- test_ev_isotonic.pyが4テストクラス18テスト以上を含む
- test_ev_correction.pyに4つの新テストが追加されている
- Isotonic適用テスト: ev_win_calibratedが生成され、ev_win_correctedと異なることを確認
- フォールバックテスト: Isotonic未設定時、ev_win_calibrated == ev_win_corrected
- オッズバンドスケーリングテスト: 高オッズ帯のEVが縮小されることを確認
- OOF生成テスト: KFold(shuffle=False)、全インデックスカバーを確認
- python -m pytest tests/ -v が全通過 (回帰なし)
</success_criteria>

<output>
After completion, create `.planning/phases/19-ev-calibration/19-02-SUMMARY.md`
</output>
