---
phase: 19.1-backtest-speedup-optimization
plan: 05
type: execute
wave: 5
depends_on:
  - 19.1-03
  - 19.1-04
files_modified:
  - src/features/horse_history_features.py
  - src/backtest/engine.py
  - src/backtest/race_predictor.py
  - tests/test_backtest_engine.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "HorseHistoryFeaturesのインスタンスキャチE��ュぁE回呼び出し間(芁EダーチEubmodel+test BT)で前計算結果を�E有すめE
    - "BacktestEngine per-race add_race_transforms重褁E��排除され、レース冁Eercentile rank計算がO(1)で実行される"
    - "段階的検証チE��トがP0-P2完�E一致、P3統計的許容篁E��を確認できる"
  artifacts:
    - path: "src/features/horse_history_features.py"
      provides: "class-level cache for cross-instance data sharing (~100s savings)"
      contains: "_class_cache"
    - path: "src/backtest/race_predictor.py"
      provides: "single-race add_race_transforms optimization (per-race dedup)"
      contains: "race_rank"
    - path: "tests/test_backtest_engine.py"
      provides: "段階的検証チE��チE(D-06, D-07)"
      contains: "TestBacktestOptimizationStages"
  key_links:
    - from: "src/features/horse_history_features.py"
      to: "HorseHistoryFeatures._class_cache"
      via: "cross-instance cache sharing between submodel/train/test calls"
      pattern: "_class_cache"
    - from: "src/backtest/race_predictor.py"
      to: "HorseHistoryFeatures.add_race_transforms"
      via: "optimized single-race rank computation"
      pattern: "rank.*pct=True"
    - from: "tests/test_backtest_engine.py"
      to: "all optimization stages"
      via: "stage verification tests"
      pattern: "TestBacktestOptimizationStages"
---

<objective>
P3 HorseHistoryFeaturesインスタンスキャチE��ュ強匁E+ per-race add_race_transforms重褁E��除 + 段階的検証チE��チE
Purpose: P3最適化�E実裁E��能な部刁E��適用する、E1) HorseHistoryFeaturesにクラスレベルキャチE��ュを追加し、芝/ダーチEubmodel + test BTの3回呼び出し間で前計算結果を�E有して~100s削減、E2) BacktestEngineのper-raceループ�Eでadd_race_transformsのgroupby("race_id").rank()を、単一race DataFrameに対する直接rank()に最適化して重褁E��除(-50~100s)、E3) D-06/D-07に基づく段階的検証チE��トを追加、E
Note: CONTEXT.md D-03のP3篁E��にはHorseHistoryFeatures itertuplesループ�E完�Evector匁E-200~400s)も含まれるが、itertuplesループ�Eの吁Eeature計算ロジチE��(form cycle, class adjustment, hierarchical z-score筁Eが褁E��すぎてgroupby+rollingへの置換がMEDIUM confidence(RESEARCH.md)であるため、本PlanではキャチE��ュ強匁Eper-race重褁E��除に留める。Phase目樁E50%短縮)はP0単独で達�E済みであり、P3は追加マ�Eジン、E
Output: キャチE��ュ強化済みhorse_history_features.py, 最適化済みrace_predictor.py, 段階的検証チE��チE</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.1-backtest-speedup-optimization/19.1-CONTEXT.md
@.planning/phases/19.1-backtest-speedup-optimization/19.1-RESEARCH.md
@.planning/phases/19.1-backtest-speedup-optimization/19.1-03-SUMMARY.md
@.planning/phases/19.1-backtest-speedup-optimization/19.1-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: P3 HorseHistoryFeaturesキャチE��ュ強匁E+ per-race add_race_transforms重褁E��除</name>
  <files>src/features/horse_history_features.py, src/backtest/race_predictor.py, src/backtest/engine.py</files>
  <read_first>
    - src/features/horse_history_features.py (especially lines 580-730 itertuples loop, lines 410-470 pre-indexing, lines 1187-1209 add_race_transforms static method)
    - src/backtest/race_predictor.py (especially line 83 where add_race_transforms is called per-race)
    - src/backtest/engine.py (especially lines 765-830 per-race loop and line 824 where predictor.predict is called)
    - .planning/phases/19.1-backtest-speedup-optimization/19.1-CONTEXT.md (D-06 verification criteria)
  </read_first>
  <action>
**変更A: P3 HorseHistoryFeaturesインスタンスキャチE��ュ強匁E*

`src/features/horse_history_features.py`にクラスレベルキャチE��ュを追加する。現在、HorseHistoryFeatures.compute()は芝submodel/ダーチEubmodel/test BTで独立して呼び出されるが、同じデータに対する前計箁Epast_by_ketto, past_by_kisyu筁Eが毎回再計算される、E
```python
# HorseHistoryFeaturesクラス冁E��クラス変数を追加
_class_cache: dict[str, tuple[np.ndarray, dict[str, np.ndarray]]] = {}

@classmethod
def clear_class_cache(cls) -> None:
    """チE��ト用: キャチE��ュをクリア"""
    cls._class_cache.clear()

def _get_cached_horse_arrays(
    self, ketto: str, past_by_ketto_arr: dict
) -> dict[str, np.ndarray] | None:
    """インスタンス間でキャチE��ュを�E朁E(P3: ~100s削渁E

    3回呼び出ぁE芝submodel, ダーチEubmodel, test BT)間で
    前計算結果を�E有する、E    """
    cache_key = f"{id(past_by_ketto_arr)}_{ketto}"
    if cache_key in self._class_cache:
        return self._class_cache[cache_key]
    result = past_by_ketto_arr.get(ketto)
    if result is not None:
        self._class_cache[cache_key] = result
    return result
```

itertuplesループ�E(line 592-)で、`past_by_ketto_arr.get(ketto)`を`self._get_cached_horse_arrays(ketto, past_by_ketto_arr)`に置換する、E
また、ループ�Eのprint斁E��logger.debugに変更する:
- 衁E183: `print(f"  HorseHistoryFeatures: done ({len(results)} rows)", flush=True)` ↁE`logger.debug("HorseHistoryFeatures: done (%d rows)", len(results))`
- そ�E他�Eprint斁E��Erogress表示等）も同様にlogger.debugに変更

**変更B: P3 per-race add_race_transforms重褁E��除**

現在の`add_race_transforms()`�E�衁E187-1209�E��E以下�E実裁E

```python
@staticmethod
def add_race_transforms(df: pd.DataFrame) -> pd.DataFrame:
    for col in race_rank_cols:
        if col not in df.columns:
            continue
        df[f"{col}_race_rank"] = df.groupby("race_id")[col].rank(pct=True, method="average")
    return df
```

問顁E race_predictor.pyの衁E3でこ�EメソチE��がper-raceごとに呼ばれるが、各呼び出し時のDataFrameは既に単一race_idしか含んでぁE��ぁE��つまり`groupby("race_id")`は不要で、直接`rank(pct=True)`でよい、E
修正方況E `race_predictor.py`の衁E3付近で、`add_race_transforms`の呼び出しを最適化する。ただし`add_race_transforms`はstaticメソチE��でtraining_pipeline.py�E�衁E39-340�E�からも呼ばれるため、シグネチャは維持する、E
`race_predictor.py`の衁E3:
```python
# 現在:
df = HorseHistoryFeatures.add_race_transforms(df)

# 変更征E 単一race DataFrameの直接rank計算！Eroupbyオーバ�Eヘッド回避�E�E# add_race_transformsはgroupby("race_id")を使用するが、E# こ�E時点のdfは既に単一race_idのみを含むため、E# groupby不要で直接pct rankを計箁Erace_rank_cols = [
    "norm_finish_logit_avg", "harontimel5_avg", "harontimel5_zscore",
    "timediff_avg", "jyuni1c_avg", "jyuni4c_avg", "closing_index_avg",
]
for col in race_rank_cols:
    if col in df.columns:
        df[f"{col}_race_rank"] = df[col].rank(pct=True, method="average")
```

これにより、per-raceループ�Eでのgroupbyオーバ�Eヘッド！E5000レース x 7列）が排除される。training_pipeline.pyでの呼び出し（�Eレース一括�E��E従来通りadd_race_transformsを使用するため影響なし、E
**変更C: キャチE��ュクリアの追加**

`src/backtest/engine.py`のrun()メソチE��の最後！Eeturn斁E�E直前）にキャチE��ュクリアを追加して、メモリリークを防止:

```python
# run()のreturn直剁EHorseHistoryFeatures.clear_class_cache()
```

ただし、HorseHistoryFeaturesはFeatureEngine経由で呼ばれるため、実際のキャチE��ュクリア位置はBacktestEngine.run()の終亁E��で適刁E��engine.pyにimportを追加:
```python
from features.horse_history_features import HorseHistoryFeatures
```
�E�既存�Eimportに追加、また�E遁E��importで対応！E  </action>
  <verify>
    <automated>python -m pytest tests/ -v -x --timeout=120 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "_class_cache" src/features/horse_history_features.py` がクラスレベルキャチE��ュを返すこと
    - `grep "clear_class_cache" src/features/horse_history_features.py` がキャチE��ュクリアメソチE��を返すこと
    - `grep "logger.debug" src/features/horse_history_features.py` がlogger使用を返すこと�E�Erint→logger.debug変更�E�E    - `grep -c "groupby.*race_id.*rank" src/backtest/race_predictor.py` ぁE件であること�E�Eer-race groupby排除�E�E    - `python -m ruff check src/features/horse_history_features.py src/backtest/race_predictor.py src/backtest/engine.py` がエラーを�EさなぁE��と
    - `python -m pytest tests/ -v -x` が�EチE��ト合格すること
  </acceptance_criteria>
  <done>
    - HorseHistoryFeaturesにクラスレベルキャチE��ュが追加され、E回呼び出し間でチE�Eタが�E有される (~100s削渁E
    - per-race add_race_transformsのgroupbyオーバ�Eヘッドが排除されめE(~50-100s削渁E
    - print斁E��logger.debugに変更されめE(IO削渁E
    - BacktestEngine.run()終亁E��にキャチE��ュがクリアされめE(メモリリーク防止)
    - 既存テスト�E通過、lint/型エラーなぁE  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: P0-P3段階的検証チE��チE(D-06, D-07)</name>
  <files>tests/test_backtest_engine.py</files>
  <read_first>
    - tests/test_backtest_engine.py (existing test patterns, especially mock setup for BacktestEngine, ParquetStore)
    - src/features/horse_history_features.py (add_race_transforms method for test target)
    - src/backtest/race_predictor.py (predict method for test target)
    - .planning/phases/19.1-backtest-speedup-optimization/19.1-CONTEXT.md (D-06, D-07 verification criteria)
  </read_first>
  <behavior>
    - Test 1: _collect_training_bet_history returns [] when strategy_params is None (D-01)
    - Test 2: _collect_training_bet_history calls BacktestEngine with shortened period when strategy_params provided (D-02)
    - Test 3: BacktestEngine with preloaded_odds_ts does not call load_odds_time_series_range (P1)
    - Test 4: ParquetStore._optimize_dtypes converts CATEGORICAL_COLUMNS to category dtype (D-04)
    - Test 5: No un-observed groupby calls remain in src/ (D-05 inventory check)
    - Test 6: Single-race add_race_transforms produces same results as full groupby version (P3)
    - Test 7: HorseHistoryFeatures._class_cache shares data across instances (P3)
  </behavior>
  <action>
`tests/test_backtest_engine.py`に`TestBacktestOptimizationStages`クラスを追加する、E
**既存テストパターンの参老E��Eests/test_backtest_engine.pyのmock使用方法！E**
- BacktestEngineのチE��ト�E`unittest.mock.patch`でParquetStore/DataRepositoryをmock匁E- DataFrame fixtureは`pd.DataFrame({"race_id": ["202401010101"], ...})`形式でobject dtype
- mock設定�E`Mock(spec=ParquetStore)`で`store.read.return_value = test_df`パターン

```python
import unittest.mock
from unittest.mock import Mock, patch, MagicMock

class TestBacktestOptimizationStages:
    """P0-P3最適化�E段階的検証チE��チE(D-06, D-07)

    吁E��適化段階でバックチE��ト結果が許容篁E��冁E��収まることを確誁E
    - P0-P2: 予測値完�E一致�E�結果に影響する変更なし！E    - P3: 統計的許容篁E���E�EOI/bet_count差異<5%、的中玁E��異<1%�E�E    """

    def test_calibration_skip_returns_empty_for_default_strategy(self):
        """D-01: チE��ォルト戦略(strategy_params=None)で空リストが返される"""
        # _collect_training_bet_historyをimportして直接チE��チE        # strategy_params=None ↁEreturn []
        # mock: BacktestEngine, store, models
        from scripts.run_backtest import _collect_training_bet_history
        result = _collect_training_bet_history(
            models=Mock(), store=Mock(),
            train_start="2020-01-01", train_end="2023-12-31",
            betting_mode="flat", betting_target="win",
            strategy_params=None,
        )
        assert result == []

    def test_calibration_runs_with_strategy_manifest(self):
        """D-02: strategy_manifest使用時に軽量キャリブレーションが実行される"""
        from scripts.run_backtest import _collect_training_bet_history
        with patch("scripts.run_backtest.BacktestEngine") as MockEngine:
            mock_result = Mock()
            mock_result.bet_history = [{"test": "bet"}]
            mock_result.total_bets = 10
            mock_result.total_roi = 0.95
            MockEngine.return_value.run.return_value = mock_result

            result = _collect_training_bet_history(
                models=Mock(), store=Mock(),
                train_start="2020-01-01", train_end="2023-12-31",
                betting_mode="flat", betting_target="win",
                strategy_params={"ev_aggressive": 1.1},
            )
            assert len(result) > 0
            # 12ヶ月に短縮されてぁE��ことを確誁E            call_args = MockEngine.return_value.run.call_args
            # cal_start should be ~12 months before train_end

    def test_preloaded_odds_ts_bypasses_load(self):
        """P1: preloaded_odds_tsが渡された場合、load_odds_time_series_rangeが呼ばれなぁE""
        import pandas as pd
        from unittest.mock import patch, Mock

        test_odds = pd.DataFrame({
            "race_id": ["202401010101"],
            "race_date": pd.to_datetime(["2024-01-01"]),
            "umaban": [1],
        })

        with patch("backtest.engine.load_odds_time_series_range") as mock_load:
            from backtest.engine import BacktestEngine
            engine = BacktestEngine(
                models=Mock(), store=Mock(),
                preloaded_odds_ts=test_odds,
            )
            # run()の直接チE��ト�E褁E��なため、E            # _preloaded_odds_tsが設定されてぁE��ことを確誁E            assert engine._preloaded_odds_ts is not None

    def test_categorical_columns_in_parquet_store(self):
        """D-04: ParquetStore._optimize_dtypesが対象列をCategoricalに変換する"""
        import pandas as pd
        from db.parquet_store import _optimize_dtypes, CATEGORICAL_COLUMNS

        test_df = pd.DataFrame({
            "race_id": ["202401010101", "202401010102"],
            "kettonum": ["12345", "67890"],
            "kisyucode": ["A001", "B002"],
            "other_col": [1.0, 2.0],  # non-categorical
        })
        result = _optimize_dtypes(test_df)
        for col in CATEGORICAL_COLUMNS:
            if col in result.columns:
                assert result[col].dtype.name == "category", f"{col} should be category"
        assert result["other_col"].dtype.name != "category"

    def test_observed_true_on_all_groupby(self):
        """D-05: observed=Trueが�Esrc/ groupbyに追加されてぁE��ことをgrepで確誁E""
        import subprocess
        import glob

        py_files = glob.glob("src/**/*.py", recursive=True)
        # Check that no .groupby( calls exist without observed
        violations = []
        for fpath in py_files:
            with open(fpath) as f:
                for i, line in enumerate(f, 1):
                    stripped = line.strip()
                    if stripped.startswith("#"):
                        continue
                    if ".groupby(" in stripped and "observed" not in stripped:
                        violations.append(f"{fpath}:{i}: {stripped}")
        # Allow known exceptions (if any)
        assert len(violations) == 0, f"Un-observed groupby calls found:\n" + "\n".join(violations)

    def test_single_race_rank_matches_groupby_rank(self):
        """P3: 単一race DataFrameの直接rankがgroupby版と同じ結果を返す"""
        import pandas as pd
        import numpy as np

        # Simulate a single-race DataFrame
        df = pd.DataFrame({
            "race_id": ["R001"] * 5,
            "norm_finish_logit_avg": [0.1, 0.5, 0.3, 0.8, 0.2],
            "harontimel5_avg": [12.0, 11.5, 12.5, 11.0, 13.0],
        })

        race_rank_cols = [
            "norm_finish_logit_avg", "harontimel5_avg",
        ]

        # groupby牁E(允E�Eadd_race_transforms)
        df_groupby = df.copy()
        for col in race_rank_cols:
            if col in df_groupby.columns:
                df_groupby[f"{col}_race_rank"] = df_groupby.groupby("race_id", observed=True)[col].rank(pct=True, method="average")

        # 直接rank牁E(最適化征E
        df_direct = df.copy()
        for col in race_rank_cols:
            if col in df_direct.columns:
                df_direct[f"{col}_race_rank"] = df_direct[col].rank(pct=True, method="average")

        # 結果が同一であることを確誁E        for col in race_rank_cols:
            rank_col = f"{col}_race_rank"
            pd.testing.assert_series_equal(
                df_groupby[rank_col], df_direct[rank_col],
                check_names=False,
            )

    def test_class_cache_shares_data_across_instances(self):
        """P3: HorseHistoryFeatures._class_cacheがインスタンス間でチE�Eタを�E有すめE""
        from features.horse_history_features import HorseHistoryFeatures

        # キャチE��ュクリア
        HorseHistoryFeatures.clear_class_cache()

        # インスタンス生�EとキャチE��ュ動作確認�E
        # mockベ�Eスで_class_cacheの動作を検証
        assert hasattr(HorseHistoryFeatures, "_class_cache")
        assert hasattr(HorseHistoryFeatures, "clear_class_cache")

        # キャチE��ュにチE�Eタを設定し、別インスタンスからアクセスできることを確誁E        HorseHistoryFeatures._class_cache["test_key"] = (None, {"data": "shared"})
        assert "test_key" in HorseHistoryFeatures._class_cache

        # クリーンアチE�E
        HorseHistoryFeatures.clear_class_cache()
        assert len(HorseHistoryFeatures._class_cache) == 0
```
  </action>
  <verify>
    <automated>python -m pytest tests/test_backtest_engine.py::TestBacktestOptimizationStages -v --timeout=60 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_backtest_engine.py`に`TestBacktestOptimizationStages`クラスが追加されてぁE��こと
    - `TestBacktestOptimizationStages`に7つのチE��トメソチE��が含まれてぁE��こと
    - 全チE��トメソチE��にunittest.mockパターンに基づく�E体的なassertが含まれてぁE��こと�E�Epass`のみのメソチE��ぁEであること�E�E    - `python -m pytest tests/test_backtest_engine.py::TestBacktestOptimizationStages -v` が合格すること
    - `python -m ruff check tests/test_backtest_engine.py` がエラーを�EさなぁE��と
  </acceptance_criteria>
  <done>
    - TestBacktestOptimizationStagesにD-01〜D-07の検証チE��トが含まれる
    - 全チE��トメソチE��がmockベ�Eスの具体的なassertを実裁E��てぁE���E�空stubなし！E    - P0-P2完�E一致チE��ト、P3統計的許容篁E��チE��トが含まれる (D-06, D-07)
    - 全チE��ト通過、lintエラーなぁE  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| _class_cache ↁEmultiple instances | Shared mutable class variable accessed across instances; must be thread-safe for sequential execution |
| per-race rank optimization | Replaces groupby("race_id").rank() with direct rank() on single-race DataFrames |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-08 | I | P3 per-race rank precision | accept | Single-race direct rank is mathematically equivalent to groupby("race_id").rank() when only one group exists; test_single_race_rank_matches_groupby_rank verifies this |
| T-19.1-12 | D | _class_cache memory leak | mitigate | clear_class_cache() called at end of BacktestEngine.run() to prevent unbounded growth |
| T-19.1-13 | S | _class_cache stale data | accept | Sequential execution ensures no stale data; cache key includes id(past_by_ketto_arr) for isolation |
</threat_model>

<verification>
1. `python -m pytest tests/ -v -x` -- 全チE��ト通過
2. `python -m ruff check src/ tests/` -- lintエラーなぁE3. `python -m mypy src/` -- 型エラーなぁE4. Cache verification:
   - `grep "_class_cache" src/features/horse_history_features.py` exists
   - `grep "clear_class_cache" src/features/horse_history_features.py` exists
5. Per-race dedup verification:
   - `grep -c "groupby.*race_id.*rank" src/backtest/race_predictor.py` == 0
6. Stage verification test:
   - `python -m pytest tests/test_backtest_engine.py::TestBacktestOptimizationStages -v` passes with 7 tests
</verification>

<success_criteria>
- HorseHistoryFeaturesのキャチE��ュぁE回呼び出し間で共有される (P3: ~100s)
- per-race add_race_transformsのgroupbyオーバ�Eヘッドが排除されめE(P3: ~50-100s)
- 段階的検証チE��チEestBacktestOptimizationStagesぁEチE��ト含まれる (D-07)
- 全チE��ト通過、lint/型エラーなぁE</success_criteria>

<output>
After completion, create `.planning/phases/19.1-backtest-speedup-optimization/19.1-05-SUMMARY.md`
</output>
