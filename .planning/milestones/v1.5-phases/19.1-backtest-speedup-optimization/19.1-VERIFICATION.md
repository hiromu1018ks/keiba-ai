---
phase: 19.1-backtest-speedup-optimization
verified: 2026-05-08T12:00:00Z
status: passed
score: 17/17 must-haves verified
overrides_applied: 0
re_verification: false
---

# Phase 19.1: バックテスト高速化検証レポート

**Phase Goal:** バックテスト実行時間を50%以上短縮する最適化を実装する。P0キャリブレーションBT条件付きスキップ(-60%), P1 MLflow/odds高速化, P2 Categorical+observed=True, P3キャッシュ強化。
**Verified:** 2026-05-08
**Status:** passed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | strategy_params is None時、D-01キャリブレーションBTがスキップされ空リストが返される | VERIFIED | `scripts/run_backtest.py:241` -- `if strategy_params is None: return []` |
| 2 | --strategy-manifest使用時、D-02軽量12ヶ月キャリブレーションBTが実行される | VERIFIED | `scripts/run_backtest.py:251-254` -- `timedelta(days=365)`で直近12ヶ月に短縮、BacktestEngineでcal_start~train_end実行 |
| 3 | 全mlflow.lightgbm.log_model呼び出しにpip_requirementsが指定されている | VERIFIED | `src/pipelines/training_pipeline.py` -- 14/14呼び出しにpip_requirements=_MLFLOW_PIP_REQS設定、5行コンテキスト検索で0件の欠落確認 |
| 4 | _coerce_typesが新ETL形式データで早期returnする | VERIFIED | `src/db/readers.py:155-161` -- race_dateがdatetime + object型数値列なし時に`return df` |
| 5 | BacktestEngine.run()が外部からodds_ts DataFrameを受け取れる | VERIFIED | `src/backtest/engine.py:373` -- `preloaded_odds_ts: pd.DataFrame \| None = None`コンストラクタ引数、L388 `self._preloaded_odds_ts` |
| 6 | preloaded_odds_ts渡し時、load_odds_time_series_rangeの重複呼び出しが回避される | VERIFIED | `src/backtest/engine.py:557-558` -- `if self._preloaded_odds_ts is not None: odds_ts_df = self._preloaded_odds_ts` + 日付フィルタ |
| 7 | preloaded_odds_ts未指定時、従来通り内部ロードされる（後方互換） | VERIFIED | `src/backtest/engine.py:563` -- else分岐で`load_odds_time_series_range(self.store, start, end)`呼び出し |
| 8 | ParquetStore.read()が対象列をpd.Categoricalに変換する (D-04) | VERIFIED | `src/db/parquet_store.py:13-16` -- CATEGORICAL_COLUMNS定数(race_id, kettonum等7列)、L19-24 _optimize_dtypes()、read()の全4return文で適用(L61,66,68,71) |
| 9 | src/features/内の全groupby呼び出しにobserved=Trueが指定されている (D-05) | VERIFIED | Python検証スクリプト(5行コンテキストチェック)で0件違反 |
| 10 | src/pipelines/training_pipeline.pyのgroupby呼び出しにobserved=Trueが指定されている | VERIFIED | 同上検証に含む、0件違反 |
| 11 | oddsデータがpreloaded_odds_tsでTest BTに渡される | VERIFIED | `scripts/run_backtest.py:483-495` -- `preloaded_odds = load_odds_time_series_range(store, test_start, test_end)` -> `preloaded_odds_ts=preloaded_odds` |
| 12 | src/models/内の全groupby呼び出しにobserved=Trueが指定されている | VERIFIED | 同上検証に含む、0件違反 |
| 13 | src/backtest/内の全groupby呼び出しにobserved=Trueが指定されている | VERIFIED | 同上検証に含む、0件違反 |
| 14 | src/db/odds_extractor.pyのgroupby呼び出しにobserved=Trueが指定されている | VERIFIED | 同上検証に含む、0件違反 |
| 15 | HorseHistoryFeatures._class_cacheがインスタンス間でデータを共有する | VERIFIED | `src/features/horse_history_features.py:270` -- `_class_cache`クラス変数、L334-349 `_get_cached_horse_arrays()`、L1304 `clear_class_cache()` at end of BacktestEngine.run() |
| 16 | per-race add_race_transformsがgroupby不要で直接rank()を実行する | VERIFIED | `src/backtest/race_predictor.py:83-96` -- 7列の直接`df[_col].rank(pct=True, method="average")`、groupby("race_id").rank()なし |
| 17 | TestBacktestOptimizationStagesが7つの段階的検証テストを含む (D-06, D-07) | VERIFIED | `tests/test_backtest_engine.py:2055` -- 7テストメソッド: D-01~P3段階検証、全7テスト通過確認 |

**Score:** 17/17 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/run_backtest.py` | P0 D-01/D-02 skip + P1 odds preloading | VERIFIED | strategy_params is None (L241), timedelta(days=365) (L253), preloaded_odds (L483-495) |
| `src/pipelines/training_pipeline.py` | P1 MLflow pip_requirements + P2 observed=True | VERIFIED | _MLFLOW_PIP_REQS (L54), 14 pip_requirements, 6 observed=True |
| `src/db/readers.py` | P1 _coerce_types early return | VERIFIED | object_needing_coercion check (L155-161) |
| `src/backtest/engine.py` | P1 preloaded_odds_ts + P2 observed=True + P3 cache clear | VERIFIED | preloaded_odds_ts (L373,557-558), 3 observed=True, clear_class_cache (L1304) |
| `src/db/parquet_store.py` | P2 D-04 Categorical conversion | VERIFIED | CATEGORICAL_COLUMNS (L13), _optimize_dtypes (L19), 全4return文で適用 |
| `src/features/horse_history_features.py` | P3 _class_cache + P2 observed=True | VERIFIED | _class_cache (L270), clear_class_cache (L273), _get_cached_horse_arrays (L334), 5 observed=True |
| `src/backtest/race_predictor.py` | P3 per-race direct rank + P2 observed=True | VERIFIED | 直接rank() (L84-96), 4 observed=True |
| `tests/test_backtest_engine.py` | D-06/D-07 stage verification tests | VERIFIED | TestBacktestOptimizationStages (L2055), 7テスト全通過 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `scripts/run_backtest.py` | `_collect_training_bet_history()` | strategy_params is None check | WIRED | L241: `if strategy_params is None: return []` |
| `scripts/run_backtest.py` | `BacktestEngine` | preloaded_odds_ts pass-through | WIRED | L483-495: load_odds_time_series_range -> preloaded_odds_ts=preloaded_odds |
| `src/db/parquet_store.py` | all feature generators | _optimize_dtypes() in read() | WIRED | 全4return文で_optimize_dtypes適用 |
| `src/features/horse_history_features.py` | _class_cache | cross-instance cache sharing | WIRED | _get_cached_horse_arrays()が_class_cache参照/更新 |
| `src/backtest/engine.py` | HorseHistoryFeatures.clear_class_cache() | run() return前クリア | WIRED | L1304: run()終了時にクリア呼び出し |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `scripts/run_backtest.py` preloaded_odds | preloaded_odds | load_odds_time_series_range(store, test_start, test_end) | Yes -- 実DB/Parquet参照 | FLOWING |
| `src/backtest/engine.py` odds_ts_df | self._preloaded_odds_ts | Constructor注入 (preloaded_odds_ts) | Yes -- preloaded_odds経由 | FLOWING |
| `src/db/parquet_store.py` _optimize_dtypes | df (Categorical columns) | ParquetStore.read()の戻り値 | Yes -- object列をcategoryに変換 | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| 全テストスイート通過 | `python -m pytest tests/ -q 2>&1 \| tail -5` | 1356 passed, 1 skipped, 5 warnings in 239.69s | PASS |
| 段階的検証テスト通過 | `python -m pytest tests/test_backtest_engine.py::TestBacktestOptimizationStages -v` | 7 passed in 1.34s | PASS |
| mlflow呼び出し全pip_requirements | Python検証(5行コンテキスト) | 14/14 calls covered, 0 missing | PASS |
| observed=True全groupby適用 | Python検証(src/**/*.py, 5行コンテキスト) | 0 violations | PASS |
| D-01 skip ロジック存在 | `grep "strategy_params is None" scripts/run_backtest.py` | L241に実装確認 | PASS |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (なし) | - | - | - | - |

既存のruff警告(N814 Camelcase import, F841 unused variables)はPhase 19.1以前から存在するものであり、今回の変更に起因しない。

### Human Verification Required

1. **バックテスト実行時間の実際の短縮率確認**
   - **Test:** `python scripts/run_backtest.py --train-start 20200101 --train-end 20231231 --test-start 20240101 --test-end 20241231`を実行し実行時間を計測
   - **Expected:** 最適化前(約8200s)に対して50%以上の短縮(約4100s以下)
   - **Why human:** 実際のDB接続とParquetファイルアクセスが必要であり、CI環境では実行不可。P0単独で-60%削減見込みだが実測が必要

2. **Categorical型変換後のバックテスト結果完全一致確認 (P0-P2)**
   - **Test:** 最適化前後のbacktest_result.jsonを比較
   - **Expected:** P0-P2は予測値完全一致、ROI/bet_count/的中率が同一
   - **Why human:** 実際のバックテスト実行結果の比較が必要

3. **メモリ使用量改善確認**
   - **Test:** バックテスト実行中のメモリプロファイリング
   - **Expected:** Categorical変換による92%メモリ削減が実データで確認できる
   - **Why human:** 実行時メモリ計測が必要

### Gaps Summary

ギャップなし。全17のmust-have truthsがVERIFIED。全1356テスト通過。段階的検証テスト7/7通過。

ただし、Phase目標の「50%以上短縮」は実際のバックテスト実行で検証が必要（上記Human Verification #1）。P0キャリブレーションBTスキップ単体で推定-60%削減であり、目標達成の見込みは高いが、実測値の確認が推奨される。

---

_Verified: 2026-05-08T12:00:00Z_
_Verifier: Claude (gsd-verifier)_
