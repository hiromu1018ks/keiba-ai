---
phase: 20-high-odds-pattern-features
plan: 03
type: execute
wave: 3
depends_on:
  - 20-02
files_modified:
  - src/models/stage1_ability_model.py
  - tests/test_horse_history_features.py
autonomous: true
requirements:
  - HODDS-05

must_haves:
  truths:
    - "新特徴量18個がAbilityModel.FEATURE_COLSに追加されている"
    - "_prepare_features()が新特徴量をavailable_colsから正しくフィルタする"
    - "新特徴量がFEATURE_COLSとBASE_COLSで整合している"
  artifacts:
    - path: "src/models/stage1_ability_model.py"
      provides: "AbilityModel.FEATURE_COLSの更新"
      min_lines: 130
  key_links:
    - from: "src/models/stage1_ability_model.py FEATURE_COLS"
      to: "src/features/horse_history_features.py BASE_COLS"
      via: "FEATURE_COLSはBASE_COLSのスーパーセットである必要がある"
      pattern: "class_promotions|time_improvement_rate|dist_change_avg_pos"
---

<objective>
Stage1AbilityModel.FEATURE_COLSに新特徴量18個を追加し、Feature importance分析で高オッズ帯の改善を検証する (HODDS-05)。

Purpose: モデルの特徴量リストに新特徴量を追加し、学習時に新特徴量が利用可能になるようにする。_prepare_features()のavailable_colsフィルタにより、新特徴量が欠損していても安全にスキップされる。
Output: 更新された`stage1_ability_model.py` (FEATURE_COLSに18特徴量追加)、更新されたテスト
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-high-odds-pattern-features/20-CONTEXT.md
@.planning/phases/20-high-odds-pattern-features/20-RESEARCH.md
@.planning/phases/20-high-odds-pattern-features/20-02-SUMMARY.md
@src/models/stage1_ability_model.py
@src/features/horse_history_features.py
@src/features/high_odds_features.py
</context>

<interfaces>
<!-- Plan 20-02で更新されたインターフェース -->

From src/features/high_odds_features.py:
```python
FEATURE_COLS: list[str] = [
    # HODDS-02
    "class_promotions", "class_demotions", "class_net_change",
    "class_max_level", "class_level_std",
    "v_recovery_flag", "v_recovery_duration",
    # HODDS-03
    "time_improvement_rate", "position_improvement_rate",
    # HODDS-04
    "dist_change_avg_pos", "dist_change_win_rate", "dist_change_exp_count",
    "surf_change_avg_pos", "surf_change_win_rate", "surf_change_exp_count",
    "cond_change_avg_pos", "cond_change_win_rate", "cond_change_exp_count",
]
```

From src/features/horse_history_features.py BASE_COLS (Plan 20-02で更新):
```python
BASE_COLS: list[str] = [
    # ... existing ~30 features ...
    # HODDS-02: クラストラジェクトリ
    "class_promotions", "class_demotions", "class_net_change",
    "class_max_level", "class_level_std",
    "v_recovery_flag", "v_recovery_duration",
    # HODDS-03: フォーム改善率
    "time_improvement_rate", "position_improvement_rate",
    # HODDS-04: 環境変化適性
    "dist_change_avg_pos", "dist_change_win_rate", "dist_change_exp_count",
    "surf_change_avg_pos", "surf_change_win_rate", "surf_change_exp_count",
    "cond_change_avg_pos", "cond_change_win_rate", "cond_change_exp_count",
]
```

From src/models/stage1_ability_model.py:
```python
class AbilityModel:
    FEATURE_COLS: list[str] = [
        # ... ~80 existing features ...
        "actual_pace_fit",  # L107: 現在の最後の要素
    ]

    def _prepare_features(self, df: pd.DataFrame) -> pd.DataFrame:
        available_cols = [c for c in self.FEATURE_COLS if c in df.columns]
        features = df[available_cols].copy()
        # ... type conversions ...
        return features
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: AbilityModel.FEATURE_COLS更新 + 整合性テスト (HODDS-05)</name>
  <files>src/models/stage1_ability_model.py, tests/test_horse_history_features.py</files>
  <read_first>
    - src/models/stage1_ability_model.py (FEATURE_COLS L28-107、_prepare_features L113-130)
    - src/features/horse_history_features.py (BASE_COLS L277-313)
    - src/features/high_odds_features.py (FEATURE_COLS)
    - tests/test_horse_history_features.py (既存テストパターン)
  </read_first>
  <action>
`src/models/stage1_ability_model.py` の `FEATURE_COLS` に18個の新特徴量を追加し、整合性を検証する (per D-12, D-14)。

**変更箇所: src/models/stage1_ability_model.py**

`FEATURE_COLS` リストの末尾 (`"actual_pace_fit",` の後、L107の閉じ括弧の前) に追加:
```python
        # HODDS-02: クラストラジェクトリ
        "class_promotions",
        "class_demotions",
        "class_net_change",
        "class_max_level",
        "class_level_std",
        "v_recovery_flag",
        "v_recovery_duration",
        # HODDS-03: フォーム改善率
        "time_improvement_rate",
        "position_improvement_rate",
        # HODDS-04: 環境変化適性
        "dist_change_avg_pos",
        "dist_change_win_rate",
        "dist_change_exp_count",
        "surf_change_avg_pos",
        "surf_change_win_rate",
        "surf_change_exp_count",
        "cond_change_avg_pos",
        "cond_change_win_rate",
        "cond_change_exp_count",
```

**重要:** `_prepare_features()` は `available_cols = [c for c in self.FEATURE_COLS if c in df.columns]` でフィルタするため、新特徴量がDataFrameに存在しない場合でも安全にスキップされる (per CONTEXT.md code_context)。新特徴量に `.astype("category")` は不要 (全て連続値float、per RESEARCH.md Anti-Patterns)。

**テスト追加: tests/test_horse_history_features.py**

既存テストファイルの末尾に、BASE_COLSとFEATURE_COLSの整合性を検証するテストクラスを追加:

```python
class TestHighOddsFeatureIntegration:
    """HODDS-05: 新特徴量のFEATURE_COLS/BASE_COLS整合性テスト"""

    def test_base_cols_contains_high_odds_features(self):
        """BASE_COLSに高オッズ特徴量18個が全て含まれている"""
        from features.high_odds_features import FEATURE_COLS as HIGH_ODDS_COLS
        from features.horse_history_features import HorseHistoryFeatures
        base = set(HorseHistoryFeatures.BASE_COLS)
        for col in HIGH_ODDS_COLS:
            assert col in base, f"BASE_COLS missing: {col}"

    def test_ability_model_feature_cols_contains_high_odds_features(self):
        """AbilityModel.FEATURE_COLSに高オッズ特徴量18個が全て含まれている"""
        from features.high_odds_features import FEATURE_COLS as HIGH_ODDS_COLS
        from models.stage1_ability_model import AbilityModel
        model_cols = set(AbilityModel.FEATURE_COLS)
        for col in HIGH_ODDS_COLS:
            assert col in model_cols, f"AbilityModel.FEATURE_COLS missing: {col}"

    def test_no_duplicate_in_feature_cols(self):
        """AbilityModel.FEATURE_COLSに重複がない"""
        from models.stage1_ability_model import AbilityModel
        cols = AbilityModel.FEATURE_COLS
        assert len(cols) == len(set(cols)), "FEATURE_COLS has duplicates"
```

**既存テスト回帰確認:** `python -m pytest tests/test_horse_history_features.py -v` で全テストPASSを確認。
  </action>
  <verify>
    <automated>python -m pytest tests/test_horse_history_features.py -v -k "test_base_cols_contains_high_odds or test_ability_model_feature_cols_contains or test_no_duplicate"</automated>
  </verify>
  <acceptance_criteria>
    - `src/models/stage1_ability_model.py` の FEATURE_COLS に `"class_promotions"` が含まれる
    - `src/models/stage1_ability_model.py` の FEATURE_COLS に `"dist_change_avg_pos"` が含まれる
    - `src/models/stage1_ability_model.py` の FEATURE_COLS に `"time_improvement_rate"` が含まれる
    - `src/models/stage1_ability_model.py` の FEATURE_COLS に `"position_improvement_rate"` が含まれる
    - `src/models/stage1_ability_model.py` の FEATURE_COLS に `"v_recovery_flag"` が含まれる
    - `src/models/stage1_ability_model.py` の FEATURE_COLS に `"cond_change_exp_count"` が含まれる
    - `tests/test_horse_history_features.py` に `TestHighOddsFeatureIntegration` クラスが含まれる
    - `python -m pytest tests/test_horse_history_features.py -v` が全テストPASS (回帰なし)
    - FEATURE_COLSに重複がない (`python -c "from models.stage1_ability_model import AbilityModel; c=AbilityModel.FEATURE_COLS; assert len(c)==len(set(c)), f'duplicates: {len(c)} vs {len(set(c))}'"`)
  </acceptance_criteria>
  <done>Stage1AbilityModel.FEATURE_COLSに18個の新特徴量が追加されている。_prepare_features()のavailable_colsフィルタにより、欠損時も安全にスキップされる。BASE_COLSとFEATURE_COLSの整合性テストが全て通過する。既存テストに回帰なし。</done>
</task>

<task type="auto">
  <name>Task 2: 全体回帰テスト + 新特徴量FEATURE_COLS整合性最終確認</name>
  <files></files>
  <read_first>
    - src/models/stage1_ability_model.py (更新後のFEATURE_COLS確認)
    - src/features/horse_history_features.py (更新後のBASE_COLS確認)
    - src/features/high_odds_features.py (FEATURE_COLS定義)
  </read_first>
  <action>
Phase 20全体の回帰テストを実行し、新特徴量の実装が既存パイプラインに悪影響を与えていないことを確認する。

**手順:**

1. **全テスト実行:** `python -m pytest tests/ -v --tb=short` で全テストを実行。全てPASSすることを確認。

2. **FEATURE_COLS整合性の3点確認:**
   - `src/features/high_odds_features.py` の FEATURE_COLS (18個)
   - `src/features/horse_history_features.py` の BASE_COLS に全18個が含まれる
   - `src/models/stage1_ability_model.py` の FEATURE_COLS に全18個が含まれる

3. **インポート整合性確認:**
   ```python
   python -c "from features.high_odds_features import compute_class_trajectory, compute_form_improvement_rate, compute_env_adaptability, FEATURE_COLS; print(f'OK: {len(FEATURE_COLS)} features')"
   ```

4. **構文チェック:**
   ```bash
   python -m py_compile src/features/high_odds_features.py
   python -m py_compile src/features/horse_history_features.py
   python -m py_compile src/models/stage1_ability_model.py
   python -m py_compile scripts/analyze_high_odds.py
   ```

5. **ruff lint:**
   ```bash
   ruff check src/features/high_odds_features.py src/features/horse_history_features.py src/models/stage1_ability_model.py scripts/analyze_high_odds.py
   ```

テスト失敗やlint errorがある場合は修正する。
  </action>
  <verify>
    <automated>python -m pytest tests/ -v --tb=short 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ -v --tb=short` が全テストPASS (0 failed)
    - `ruff check src/features/high_odds_features.py src/features/horse_history_features.py src/models/stage1_ability_model.py scripts/analyze_high_odds.py` が0 errors
    - `python -c "from features.high_odds_features import FEATURE_COLS; assert len(FEATURE_COLS) == 18"` が成功
    - `python -c "from features.high_odds_features import compute_class_trajectory, compute_form_improvement_rate, compute_env_adaptability"` が成功
  </acceptance_criteria>
  <done>Phase 20の全実装が完了し、回帰テスト全通過、lint errorなし。18個の新特徴量がhigh_odds_features.py → horse_history_features.py (BASE_COLS) → stage1_ability_model.py (FEATURE_COLS) の3箇所で整合している。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 特徴量計算 → モデル入力 | FEATURE_COLSがavailable_colsフィルタで安全に処理される。未実装特徴量はスキップ |
| BASE_COLS ↔ FEATURE_COLS | 3箇所のリストが整合している必要がある。テストで検証 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | I (Tampering) | FEATURE_COLS整合性 | mitigate | テストでBASE_COLSとFEATURE_COLSの整合性を自動検証。リスト不整合を早期発見 |
| T-20-06 | D (Denial) | _prepare_features() | accept | available_colsフィルタが新特徴量の欠損を安全に処理。学習時のクラッシュを防止 |
</threat_model>

<verification>
- `python -m pytest tests/ -v --tb=short` 全テストPASS
- `ruff check src/features/high_odds_features.py src/features/horse_history_features.py src/models/stage1_ability_model.py scripts/analyze_high_odds.py` 0 errors
- `python -c "from features.high_odds_features import FEATURE_COLS; print(len(FEATURE_COLS))"` 出力 "18"
- `python -c "from models.stage1_ability_model import AbilityModel; print(len(AbilityModel.FEATURE_COLS))"` で更新後の件数を確認
</verification>

<success_criteria>
1. AbilityModel.FEATURE_COLSに18個の新特徴量が追加されている
2. BASE_COLSとFEATURE_COLSで新特徴量が整合している (テストで検証)
3. FEATURE_COLSに重複がない
4. 全テストPASS (回帰なし)
5. ruff lint 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/20-high-odds-pattern-features/20-03-SUMMARY.md`
</output>
