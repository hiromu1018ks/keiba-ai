---
phase: 21-conformal-ev
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - src/pipelines/training_pipeline.py
  - src/db/model_loader.py
  - src/backtest/race_predictor.py
  - src/models/ev_diagnostics.py
  - tests/test_conformal_ev_model.py
  - tests/test_race_predictor.py
  - tests/test_backtest_engine.py
autonomous: true
requirements: [CONF-03]

must_haves:
  truths:
    - "_train_submodel()の学習チェーン末尾(Isotonic後)にConformalEV学習が追加される"
    - "ModelLoaderがcqr_quantile_low/high_{surface}.lgbとcqr_params_{surface}.jsonを自動読み込みする"
    - "RacePredictor.predict()がconformal_ev_model.predict_interval()を呼び出す"
    - "ev_diagnostics.pyにCQRカバレッジ率・区間幅指標が追加される"
    - "CQRモデルのSHA256ハッシュがstrategy manifestに含まれ、PFP改ざん検知対象となる (per D-10)"
    - "既存テスト全通過（回帰なし）"
  artifacts:
    - path: "src/pipelines/training_pipeline.py"
      provides: "_train_submodel()にCQR学習追加, _save_models_local()にCQR保存追加, PFP SHA256ハッシュ計算追加"
      contains: "ConformalEVModel"
    - path: "src/db/model_loader.py"
      provides: "ConformalEVモデルのローカル/MLflow読み込み"
      contains: "ConformalEVModel"
    - path: "src/backtest/race_predictor.py"
      provides: "predict_interval()呼び出しのConformalEVModel差し替え"
      contains: "conformal_ev_model"
    - path: "src/models/ev_diagnostics.py"
      provides: "CQRカバレッジ率・区間幅指標"
      contains: "_compute_cqr_coverage"
  key_links:
    - from: "training_pipeline._train_submodel()"
      to: "ConformalEVModel.train()"
      via: "Isotonic学習後のバリデーション分割(後方20%)"
      pattern: "ConformalEVModel"
    - from: "race_predictor.predict()"
      to: "submodel.conformal_ev_model.predict_interval()"
      via: "confidence.predict_interval()の差し替え"
      pattern: "conformal_ev_model.predict_interval"
    - from: "model_loader.load_from_dir()"
      to: "ConformalEVModel.load()"
      via: "cqr_quantile_{low/high}_{surface}.lgb + cqr_params_{surface}.json"
      pattern: "cqr_quantile"
    - from: "training_pipeline._save_models_local()"
      to: "strategy manifest SHA256"
      via: "cqr_params_{surface}.jsonのSHA256ハッシュをmanifestに含める"
      pattern: "cqr.*sha256|sha256.*cqr"
---

<objective>
ConformalEVModelをTrainingPipelineV5、ModelLoader、RacePredictorに統合し、ev_diagnosticsにCQRカバレッジ指標を追加する。あわせてPFP改ざん検知のためCQRモデルパラメータのSHA256ハッシュをstrategy manifestに含める (per D-10)。

Purpose: Plan 01で作成したConformalEVModelを実際の学習・推論パイプラインに組み込み、エンドツーエンドで動作するようにする。CQRモデルの完全性をPFPで保証する。
Output: パイプライン統合、モデル保存/読み込み、推論差し替え、診断拡張、PFP改ざん検知統合
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-conformal-ev/21-CONTEXT.md
@.planning/phases/21-conformal-ev/21-RESEARCH.md
@.planning/phases/21-conformal-ev/21-01-SUMMARY.md
@src/pipelines/training_pipeline.py
@src/db/model_loader.py
@src/backtest/race_predictor.py
@src/models/ev_diagnostics.py

<interfaces>
<!-- Plan 01で作成されるConformalEVModelの公開API -->

From src/models/conformal_ev_model.py (Plan 01で作成):
```python
class ConformalEVModel:
    def __init__(self, alpha: float = 0.1, feature_cols: list[str] | None = None) -> None: ...
    def train(self, df_calib: pd.DataFrame, *, num_threads: int = 0, lgb_params: dict | None = None) -> None: ...
    def predict_interval(self, win_df: pd.DataFrame, place_df: pd.DataFrame, alphas: tuple[float, ...] = (0.1, 0.2)) -> tuple[pd.DataFrame, pd.DataFrame]: ...
    def save(self, models_dir: Path, surface: str) -> None: ...
    @classmethod
    def load(cls, models_dir: Path, surface: str) -> "ConformalEVModel | None": ...
```

From src/domain/models.py (Plan 01で変更済み):
```python
@dataclass
class SubmodelSet:
    conformal_ev_model: ConformalEVModel | None = None  # Phase 21
    # confidence: RobustConfidenceEstimator は削除済み
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TrainingPipelineV5統合 + ModelLoader + RacePredictor差し替え + PFP改ざん検知</name>
  <files>
    src/pipelines/training_pipeline.py,
    src/db/model_loader.py,
    src/backtest/race_predictor.py
  </files>
  <read_first>
    src/pipelines/training_pipeline.py
    src/db/model_loader.py
    src/backtest/race_predictor.py
    src/domain/models.py
    src/models/conformal_ev_model.py
    .planning/phases/21-conformal-ev/21-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: _train_submodel()がIsotonic学習後にConformalEV学習を呼び出す
    - Test 2: _save_models_local()がcqr_quantile_{low/high}_{surface}.lgbとcqr_params_{surface}.jsonを保存する
    - Test 3: ModelLoader.load_from_dir()がConformalEVModelをロードする
    - Test 4: ModelLoaderのMLflow load()がConformalEVModelをロードする
    - Test 5: RacePredictor.predict()がsubmodel.conformal_ev_model.predict_interval()を呼ぶ
    - Test 6: conformal_ev_modelがNoneの場合にフォールバック動作
  </behavior>
  <action>
    **1. src/pipelines/training_pipeline.py を変更:**

    **1a. import変更:**
    - `from models.robust_confidence_estimator import RobustConfidenceEstimator` を `from models.conformal_ev_model import ConformalEVModel` に変更

    **1b. _train_submodel() 内の変更 (per D-07/D-08/D-09):**

    **既存の信頼区間キャリブレーションブロック(line ~851-869)を置き換え:**

    古いコード(line ~851-869)を削除:
    ```python
    # 7. 信頼区間キャリブレーション
    with TimingContext(f"{surface}/confidence"):
        conf = RobustConfidenceEstimator()
        ... (全て削除)
        conf.calibrate(win_calib_df, place_calib_df)
    ```

    新しいCQR学習ブロックを追加(line ~851、Isotonic学習チェーンの直後):
    ```python
    # 7. Conformal EV Prediction Interval (CQR) per D-07/D-08/D-09
    conformal_ev: ConformalEVModel | None = None
    if len(df_oof) >= 500 and "ev_win_calibrated" in df_oof.columns:
        with TimingContext(f"{surface}/conformal_ev"):
            # バリデーション分割: 後方20%をCQRキャリブレーションに使用 (per D-07)
            n_total = len(df_oof)
            split_idx = int(n_total * 0.8)
            df_cqr_train = df_oof.iloc[:split_idx].copy()
            df_cqr_calib = df_oof.iloc[split_idx:].copy()

            # actual_ev_win を計算 (Phase 19パターン)
            df_cqr_train["actual_ev_win"] = (
                df_cqr_train["confirmed_odds"]
                * (df_cqr_train["kakuteijyuni"] == 1).astype(float)
            )
            df_cqr_calib["actual_ev_win"] = (
                df_cqr_calib["confirmed_odds"]
                * (df_cqr_calib["kakuteijyuni"] == 1).astype(float)
            )

            # 特徴量列を既存モデルから抽出 (WinTwoStageの特徴量列をベース)
            # ev_win_calibrated, actual_ev_win, non-feature列を除外
            _non_feature_cols = {
                "race_id", "umaban", "surface", "race_date",
                "ev_win_corrected", "ev_win_calibrated", "actual_ev_win",
                "confirmed_odds", "kakuteijyuni", "kettonum",
                "EV_lower_win_corrected", "EV_upper_win_corrected",
                "conformal_confidence_score",
                "ev_place_corrected", "actual_ev_place",
                "win_selection_edge", "p_hit", "e_return",
                "p_corrected", "e_corrected",
            }
            feature_cols = [c for c in df_cqr_train.columns if c not in _non_feature_cols]

            # train + calibを結合してtrain()に渡す (内部で80/20分割)
            df_cqr_all = pd.concat([df_cqr_train, df_cqr_calib], ignore_index=True)

            conformal_ev = ConformalEVModel(
                alpha=0.1,
                feature_cols=feature_cols,
            )
            conformal_ev.train(df_cqr_all, num_threads=num_threads)
            logger.info(
                "Conformal EV fitted for %s: Q_90=%.4f, Q_80=%.4f",
                surface,
                conformal_ev._calibration_quantile_90,
                conformal_ev._calibration_quantile_80,
            )
    else:
        logger.info(
            "Skipping Conformal EV for %s: len=%d, has_ev_calibrated=%s",
            surface, len(df_oof), "ev_win_calibrated" in df_oof.columns,
        )
    ```

    **WinSelectionGate/PlaceSelectionGateの学習データ生成を更新:**

    古いコード(line ~875-890)で`conf.predict_lower_bound()`を呼んでいる部分を更新:
    ```python
    # PlaceSelectionGate学習データ生成
    if conformal_ev is not None:
        _, gate_place_df = conformal_ev.predict_interval(df_oof.copy(), df_oof.copy())
    else:
        gate_place_df = df_oof.copy()

    # WinSelectionGate学習データ生成
    if conformal_ev is not None:
        wsg_win_df, _ = conformal_ev.predict_interval(df_oof.copy(), df_oof.copy())
        if "EV_lower_win_corrected" in wsg_win_df.columns:
            wsg_train_df["EV_lower_win_corrected"] = wsg_win_df["EV_lower_win_corrected"].values
    ```

    **SubmodelSet返却を更新:**
    `confidence=conf` を `conformal_ev_model=conformal_ev` に変更。
    古い`conf`変数は削除。

    **1c. _save_models_local() 内の変更 (per D-10):**

    RobustConfidenceEstimatorパラメータ保存ブロック(line ~1553-1568)を削除し、ConformalEV保存に置き換え:
    ```python
    # Phase 21: ConformalEVModel保存 (per D-10)
    if sub.conformal_ev_model is not None:
        sub.conformal_ev_model.save(models_dir, surface)
    ```

    **1d. _log_to_mlflow() 内の変更:**

    RobustConfidenceEstimatorパラメータのMLflow記録(line ~1408-1421)を削除し、ConformalEVの保存に置き換え:
    ```python
    # Phase 21: ConformalEVModel MLflow保存
    if sub.conformal_ev_model is not None:
        sub.conformal_ev_model.save(models_dir, surface)
        # CQR LightGBMモデルをMLflowに記録
        if sub.conformal_ev_model.q_low_model is not None:
            mlflow.lightgbm.log_model(
                sub.conformal_ev_model.q_low_model,
                name=f"cqr_quantile_low_{surface}",
                pip_requirements=_MLFLOW_PIP_REQS,
            )
        if sub.conformal_ev_model.q_high_model is not None:
            mlflow.lightgbm.log_model(
                sub.conformal_ev_model.q_high_model,
                name=f"cqr_quantile_high_{surface}",
                pip_requirements=_MLFLOW_PIP_REQS,
            )
        # CQR paramsをアーティファクトとして記録
        import json
        cqr_params = {
            "alpha": sub.conformal_ev_model.alpha,
            "calibration_quantile_90": sub.conformal_ev_model._calibration_quantile_90,
            "calibration_quantile_80": sub.conformal_ev_model._calibration_quantile_80,
            "_calibrated": sub.conformal_ev_model._calibrated,
        }
        if sub.conformal_ev_model.feature_cols is not None:
            cqr_params["feature_cols"] = sub.conformal_ev_model.feature_cols
        mlflow.log_dict(cqr_params, f"cqr_params_{surface}.json")
    ```

    **1e. PFP改ざん検知統合 (per D-10):**

    _save_models_local()内でCQRモデル保存後に、`cqr_params_{surface}.json`のSHA256ハッシュを計算し、strategy manifest (ParameterFreezeProtocol) に含める:

    ```python
    # Phase 21: CQR model parameter SHA256 for PFP tamper detection (per D-10)
    if sub.conformal_ev_model is not None:
        cqr_params_path = models_dir / f"cqr_params_{surface}.json"
        if cqr_params_path.is_file():
            import hashlib
            sha256 = hashlib.sha256(cqr_params_path.read_bytes()).hexdigest()
            # strategy_manifestにCQRハッシュを追加
            # 既存のmanifest構造にcqr_checksumsエントリを追加
            manifest_key = f"cqr_params_{surface}_sha256"
            # 注: ModelLoaderが.lgbファイルの完全性をBooster(model_file=)で
            # 検証するため、.lgbファイル自体のハッシュは不要。
            # cqr_params_{surface}.jsonは軽量なJSONパラメータファイルのため
            # SHA256で改ざん検知が容易。.lgbファイルはModelLoaderの
            # 既存ロード機構で十分に保護される。
            logger.info("CQR params SHA256 for %s: %s", surface, sha256[:16])
    ```

    実装方法:
    - _save_models_local()のCQR保存ブロック直後でSHA256を計算
    - 既存のstrategy_manifest.jsonがdata/strategy_manifest.jsonに存在する場合、
      `cqr_checksums`キーを追加: `{"cqr_checksums": {"turf": "sha256hex...", "dirt": "sha256hex..."}}`
    - strategy_manifest.jsonが存在しない場合はwarningログのみ出力（manifestは別フェーズで生成されるため）
    - `hashlib.sha256(path.read_bytes()).hexdigest()`で計算
    - .lgbファイルの完全性はModelLoaderの既存Booster(model_file=)ロード機構で担保されるため、
      SHA256チェックはcqr_params_{surface}.jsonのみで十分

    **2. src/db/model_loader.py を変更:**

    **2a. import変更:**
    - `from models.robust_confidence_estimator import RobustConfidenceEstimator` (line 81, 515) を `from models.conformal_ev_model import ConformalEVModel` に変更

    **2b. _load_from_mlflow() 内 (line ~185-222):**
    RobustConfidenceEstimatorブロックをConformalEVModel読み込みに置き換え:
    ```python
    # ConformalEVModel (Phase 21)
    conformal_ev = None
    try:
        q_low_uri = f"{artifact_uri}/cqr_quantile_low_{surface}"
        q_high_uri = f"{artifact_uri}/cqr_quantile_high_{surface}"
        cqr_params_uri = f"runs:/{run_id}/cqr_params_{surface}.json"
        # ファイル存在チェック後にロード
        conformal_ev = ConformalEVModel.load_from_mlflow(
            q_low_uri, q_high_uri, cqr_params_uri,
        )
    except Exception:
        logger.info("ConformalEVModel not found for surface=%s, skipping", surface)
    ```

    ConformalEVModelに`load_from_mlflow`クラスメソッドを追加する必要があるが、シンプルにするためload_from_dir()パターンと同じ構造で実装:
    ```python
    @classmethod
    def load_from_mlflow(cls, q_low_uri: str, q_high_uri: str, params_uri: str) -> "ConformalEVModel":
        """MLflowからCQRモデルをロード"""
        import lightgbm as lgb
        obj = cls()
        obj.q_low_model = lgb.Booster(model_file=q_low_uri) if not q_low_uri.startswith("runs:/") else mlflow.lightgbm.load_model(q_low_uri)
        obj.q_high_model = lgb.Booster(model_file=q_high_uri) if not q_high_uri.startswith("runs:/") else mlflow.lightgbm.load_model(q_high_uri)
        # params.jsonのダウンロードとパース
        ...
        obj._calibrated = True
        return obj
    ```

    ただし、MLflowロードの複雑性を避けるため、load_from_dir()と同じパターンでローカルディレクトリからのロードを主とする。MLflow側はsave()でローカルに保存したファイルを_log_to_mlflow()が記録するため、MLflowからの読み込みは`_load_from_mlflow()`内で`mlflow.artifacts.download_artifacts()`でダウンロード後に`ConformalEVModel.load()`を呼ぶ方針。

    **2c. load_from_dir() 内 (line ~637-650):**
    RobustConfidenceEstimatorブロックをConformalEVModel読み込みに置き換え:
    ```python
    # ConformalEVModel (Phase 21)
    conformal_ev = ConformalEVModel.load(models_dir, surface)
    ```

    **2d. SubmodelSet構築の更新:**
    両方のロード関数で`confidence=confidence`を`conformal_ev_model=conformal_ev`に変更。

    **3. src/backtest/race_predictor.py を変更:**

    **3a. import に TYPE_CHECKING で ConformalEVModel を追加:**
    (変更不要 — TYPE_CHECKING内でdomain.modelsから参照されるため、直接importは不要)

    **3b. predict() 内の差し替え (line ~165-169):**

    古いコード:
    ```python
    if "ev_place_corrected" not in df.columns:
        df["ev_place_corrected"] = 0.0
    win_df, place_df = submodel.confidence.predict_interval(df, df)
    ```

    新しいコード:
    ```python
    if "ev_place_corrected" not in df.columns:
        df["ev_place_corrected"] = 0.0
    if submodel.conformal_ev_model is not None:
        win_df, place_df = submodel.conformal_ev_model.predict_interval(df, df)
    else:
        # フォールバック: ConformalEVModelがない場合はEVをそのまま使用
        win_df = df.copy()
        ev_col = "ev_win_calibrated" if "ev_win_calibrated" in df.columns else "ev_win_corrected"
        win_df["EV_lower_win_corrected"] = pd.to_numeric(df[ev_col], errors="coerce").fillna(0.0)
        win_df["EV_upper_win_corrected"] = win_df["EV_lower_win_corrected"]
        win_df["conformal_confidence_score"] = 0.0
        place_df = df.copy()
        place_df["EV_lower_place"] = pd.to_numeric(df.get("ev_place_corrected", pd.Series(0.0, index=df.index)), errors="coerce").fillna(0.0)
        place_df["EV_upper_place"] = place_df["EV_lower_place"]
    ```

    **3c. その他のconfidence参照箇所:**
    - `predict()`内で`submodel.confidence`を参照する箇所が他にないかgrepで確認し、全て`conformal_ev_model`に更新
  </action>
  <verify>
    <automated>cd C:/Users/hirom/develop/keiba-ai && python -m pytest tests/ -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - training_pipeline.py に ConformalEVModel の import がある: `grep "from models.conformal_ev_model import ConformalEVModel" src/pipelines/training_pipeline.py`
    - training_pipeline.py に RobustConfidenceEstimator の import がない: `grep -c "RobustConfidenceEstimator" src/pipelines/training_pipeline.py` == 0
    - training_pipeline.py に conformal_ev のCQR学習ブロックがある: `grep "conformal_ev" src/pipelines/training_pipeline.py`
    - _train_submodel()内でConformalEVModel.train()が呼ばれる: `grep "conformal_ev.train\|ConformalEVModel" src/pipelines/training_pipeline.py`
    - _save_models_local()でCQR保存がある: `grep "conformal_ev_model.save" src/pipelines/training_pipeline.py`
    - _save_models_local()にSHA256ハッシュ計算がある: `grep "sha256" src/pipelines/training_pipeline.py`
    - model_loader.py に ConformalEVModel の import がある: `grep "from models.conformal_ev_model import ConformalEVModel" src/db/model_loader.py`
    - model_loader.py に RobustConfidenceEstimator の import がない: `grep -c "RobustConfidenceEstimator" src/db/model_loader.py` == 0
    - model_loader.py で ConformalEVModel.load() が呼ばれる: `grep "ConformalEVModel.load" src/db/model_loader.py`
    - model_loader.py に confidence_params.json の参照がない: `grep -c "confidence_params.json" src/db/model_loader.py` == 0
    - race_predictor.py で conformal_ev_model.predict_interval() が呼ばれる: `grep "conformal_ev_model.predict_interval" src/backtest/race_predictor.py`
    - race_predictor.py に confidence.predict_interval の参照がない: `grep -c "confidence.predict_interval" src/backtest/race_predictor.py` == 0
    - 全テスト通過: `python -m pytest tests/ -v`
  </acceptance_criteria>
  <done>
    - TrainingPipelineV5._train_submodel()でIsotonic後にConformalEV学習が追加されている
    - ModelLoaderがConformalEVモデルをローカル/MLflowから読み込む
    - RacePredictor.predict()がconformal_ev_model.predict_interval()を呼ぶ
    - CQRモデルパラメータのSHA256ハッシュがstrategy manifestに含まれている (per D-10)
    - RobustConfidenceEstimatorの全参照が削除されている
    - 全テスト通過
  </done>
</task>

<task type="auto">
  <name>Task 2: ev_diagnostics拡張 + テスト更新</name>
  <files>
    src/models/ev_diagnostics.py,
    tests/test_conformal_ev_model.py,
    tests/test_race_predictor.py,
    tests/test_backtest_engine.py
  </files>
  <read_first>
    src/models/ev_diagnostics.py
    tests/test_race_predictor.py
    tests/test_backtest_engine.py
    tests/test_conformal_ev_model.py
  </read_first>
  <behavior>
    - Test 1: _compute_cqr_coverage()がカバレッジ率を正しく計算する
    - Test 2: _compute_cqr_coverage()が区間幅の統計量(mean/median)を計算する
    - Test 3: _compute_cqr_coverage()がサンプル不足時にwarning辞書を返す
    - Test 4: compute_ev_diagnostics()の結果にcqr_coverageセクションが含まれる
    - Test 5: 既存テスト(RacePredictor/BacktestEngine)が全通過する
  </behavior>
  <action>
    **1. src/models/ev_diagnostics.py を変更 (per D-11):**

    compute_ev_diagnostics()の戻り値dictにCQRカバレッジ指標を追加。

    **1a. 新規関数 _compute_cqr_coverage() を追加:**

    ```python
    def _compute_cqr_coverage(
        df: pd.DataFrame,
        pred_col: str = "ev_win_calibrated",
        lower_col: str = "EV_lower_win_corrected",
        upper_col: str = "EV_upper_win_corrected",
        actual_col: str = "actual_ev_win",
    ) -> dict:
        """CQR区間のカバレッジ率と区間幅を計算.

        90%信頼区間のカバレッジ率: actual が [lower, upper] に含まれる割合。
        区間幅の統計量: mean, median, min, max。
        """
        lower = pd.to_numeric(df[lower_col], errors="coerce")
        upper = pd.to_numeric(df[upper_col], errors="coerce")
        actual = pd.to_numeric(df[actual_col], errors="coerce")
        valid = lower.notna() & upper.notna() & actual.notna()

        if valid.sum() < 30:
            return {"warning": "insufficient_samples", "n_samples": int(valid.sum())}

        coverage = float(((actual[valid] >= lower[valid]) & (actual[valid] <= upper[valid])).mean())
        width = upper[valid] - lower[valid]
        return {
            "coverage_rate": coverage,
            "target_coverage": 0.90,
            "coverage_met": coverage >= 0.90,
            "mean_interval_width": float(width.mean()),
            "median_interval_width": float(width.median()),
            "min_interval_width": float(width.min()),
            "max_interval_width": float(width.max()),
            "n_samples": int(valid.sum()),
        }
    ```

    **1b. compute_ev_diagnostics() 内で _compute_cqr_coverage() を呼び出し:**

    compute_ev_diagnostics()関数の戻り値dictにCQRカバレッジセクションを追加:
    ```python
    # Phase 21: CQR coverage metrics (per D-11)
    cqr_result = _compute_cqr_coverage(df)
    result["cqr_coverage"] = cqr_result

    # サーフェス別CQRカバレッジ
    if "surface" in df.columns:
        cqr_by_surface = {}
        for surf, group in df.groupby("surface", observed=True):
            cqr_surf = _compute_cqr_coverage(group)
            cqr_by_surface[surf] = cqr_surf
        result["cqr_coverage_by_surface"] = cqr_by_surface
    ```

    **2. テスト更新:**

    **2a. tests/test_conformal_ev_model.py に統合テストを追加:**

    - `test_pipeline_integration_train_call`: mockベースでTraininPipeline._train_submodel()内のConformalEV学習が呼ばれることを確認（TrainingPipelineのmock）

    **2b. 既存テストの修正:**

    Plan 01で一時スキップしたテストの修正:
    - tests/test_race_predictor.py: `submodel.confidence` を `submodel.conformal_ev_model` に変更。`RobustConfidenceEstimator`のmockを`ConformalEVModel`のmockに置き換え
    - tests/test_backtest_engine.py: 同様に`confidence`参照を`conformal_ev_model`に変更

    具体的な修正箇所の特定方法:
    ```bash
    grep -n "confidence" tests/test_race_predictor.py
    grep -n "confidence" tests/test_backtest_engine.py
    grep -n "RobustConfidenceEstimator" tests/test_race_predictor.py
    grep -n "RobustConfidenceEstimator" tests/test_backtest_engine.py
    ```

    これらの箇所で:
    - `RobustConfidenceEstimator()` -> `ConformalEVModel()`
    - `sub.confidence` -> `sub.conformal_ev_model`
    - `confidence.predict_interval` -> `conformal_ev_model.predict_interval`
    - `confidence._calibrated = True` -> `conformal_ev_model._calibrated = True`
    - `confidence.calibrate(...)` -> 不要（train()に統合）

    **2c. ev_diagnosticsテスト:**

    test_conformal_ev_model.py または新規test_ev_diagnostics.py（既存があれば追記）に:
    - `test_cqr_coverage_calculation`: 正常データでcoverage_rateが計算される
    - `test_cqr_coverage_insufficient_samples`: サンプル30未満でwarning返却
    - `test_cqr_coverage_in_compute_ev_diagnostics`: compute_ev_diagnostics()の結果にcqr_coverageキーが含まれる
  </action>
  <verify>
    <automated>cd C:/Users/hirom/develop/keiba-ai && python -m pytest tests/ -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - ev_diagnostics.py に _compute_cqr_coverage 関数が存在: `grep "_compute_cqr_coverage" src/models/ev_diagnostics.py`
    - ev_diagnostics.py で coverage_rate が計算される: `grep "coverage_rate" src/models/ev_diagnostics.py`
    - ev_diagnostics.py で coverage_met フラグが存在: `grep "coverage_met" src/models/ev_diagnostics.py`
    - compute_ev_diagnostics()にcqr_coverageが含まれる: `grep "cqr_coverage" src/models/ev_diagnostics.py`
    - 全RobustConfidenceEstimator参照がコードベースから削除: `grep -r "RobustConfidenceEstimator" src/ tests/` == 0
    - 全confidence参照がconformal_ev_modelに更新: `grep -c "submodel.confidence\b" src/` == 0
    - 全テスト通過: `python -m pytest tests/ -v`
    - テスト数が既存(1327+)から減少していない
  </acceptance_criteria>
  <done>
    - ev_diagnostics.pyにCQRカバレッジ率・区間幅指標が追加されている
    - compute_ev_diagnostics()の結果にcqr_coverageセクションが含まれる
    - RobustConfidenceEstimatorのコードベースからの完全除去が確認される
    - 既存テスト全通過（回帰なし）
  </done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/ -v` 全テスト通過
2. `grep -r "RobustConfidenceEstimator" src/ tests/` が0件（完全除去確認）
3. `grep "conformal_ev_model" src/pipelines/training_pipeline.py` がヒット
4. `grep "conformal_ev_model" src/db/model_loader.py` がヒット
5. `grep "conformal_ev_model" src/backtest/race_predictor.py` がヒット
6. `grep "_compute_cqr_coverage" src/models/ev_diagnostics.py` がヒット
7. `grep "sha256" src/pipelines/training_pipeline.py` がヒット（PFP改ざん検知）
8. テスト数が1327以上を維持
</verification>

<success_criteria>
- TrainingPipelineV5にCQR学習がIsotonic後(学習チェーン末尾)に追加されている
- ModelLoaderがConformalEVモデルを自動読み込みする
- RacePredictorがconformal_ev_model.predict_interval()を呼び出す
- ev_diagnosticsにCQRカバレッジ指標が追加されている
- CQRモデルパラメータのSHA256ハッシュがPFP改ざん検知に統合されている (per D-10)
- RobustConfidenceEstimatorがコードベースから完全に削除されている
- 全テスト通過（回帰なし）
</success_criteria>

<output>
After completion, create `.planning/phases/21-conformal-ev/21-02-SUMMARY.md`
</output>
