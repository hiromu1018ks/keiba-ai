---
phase: 23-safety-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/feature_engine.py
  - src/models/conformal_ev_model.py
  - src/models/ev_correction_model.py
  - src/pipelines/training_pipeline.py
  - tests/test_post_race_leakage.py
autonomous: true
requirements: [SAFE-01]

must_haves:
  truths:
    - "build_all()の出力DataFrameにPOST_RACE_COLSが含まれない"
    - "build_all()のキャッシュ書き込み前にPOST_RACE_COLSが除外される"
    - "CQRモデルがwhitelist FEATURE_COLSを使用する (ブラックリスト方式を廃止)"
    - "EV correctionのodds-band scalingが常に発走前oddsを使用する"
    - "popularity_rankのフォールバックがtanodds→tanninkiの2段のみ (ninkiを含まない)"
    - "3層CIテストがbuild_all出力/FEATURE_COLS/predict入力の漏洩を検出する"
  artifacts:
    - path: "src/features/feature_engine.py"
      provides: "POST_RACE_COLS drop + ninki fallback removal"
      contains: "post_race_present"
    - path: "src/models/conformal_ev_model.py"
      provides: "CQR whitelist FEATURE_COLS"
      contains: "FEATURE_COLS: list[str]"
    - path: "src/models/ev_correction_model.py"
      provides: "EV correction odds fix (line 370)"
      contains: 'odds_col = "odds"'
    - path: "tests/test_post_race_leakage.py"
      provides: "3-layer POST_RACE leakage CI tests"
      contains: "class TestPostRaceLeakage"
  key_links:
    - from: "src/features/feature_engine.py"
      to: "src/domain/types.py"
      via: "POST_RACE_COLS import"
      pattern: "from domain.types import POST_RACE_COLS"
    - from: "tests/test_post_race_leakage.py"
      to: "src/features/feature_engine.py"
      via: "build_all() mock-based verification"
      pattern: "build_all"
    - from: "tests/test_post_race_leakage.py"
      to: "src/models/ev_correction_model.py"
      via: "FEATURE_COLS POST_RACE overlap check"
      pattern: "FEATURE_COLS"
---

<objective>
POST_RACE情報漏洩を特徴量パイプラインから完全に排除する

Purpose: Spike調査で特定された5件の構造的リスク(M1-M6)を一括修正し、Phase 24以降の特徴量改善作業を安全に進められる基盤を確立する。
Output: 修正済みfeature_engine.py, conformal_ev_model.py, ev_correction_model.py, training_pipeline.py + 新規テストtest_post_race_leakage.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-safety-gate/23-CONTEXT.md
@.planning/phases/23-safety-gate/23-RESEARCH.md
@.planning/spikes/data-leak-phase-20-22.md

<interfaces>
<!-- Executor needs these contracts -->

From src/domain/types.py (line 38-55):
```python
POST_RACE_COLS: list[str] = [
    "kakuteijyuni", "confirmed_odds", "ninki", "kyakusitukubun",
    "time", "timediff", "harontimel3", "harontimel4",
    "jyuni1c", "jyuni2c", "jyuni3c", "jyuni4c",
    "honsyokin", "chakusacd", "dmjyuni", "dmtime",
]
```

From src/features/feature_engine.py (line 295-303):
```python
# --- Feature Cache Write (PERF-03) --- single write point, guaranteed ---
if self._use_cache and _cache_name is not None and not result_df.empty:
    try:
        if store is not None:
            store.write(self._cache_dir, _cache_name, result_df)
```

From src/models/conformal_ev_model.py (line 57-66):
```python
_NON_FEATURE_COLS: set[str] = {
    "race_id", "umaban", "race_date", "surface", "kettonum",
    "distance_bin", "grade_code", "track_condition_code",
} | set(POST_RACE_COLS) | _MODEL_OUTPUT_COLS
```

From src/models/ev_correction_model.py (line 370):
```python
odds_col = "confirmed_odds" if "confirmed_odds" in df.columns else "odds"
```

From src/features/feature_engine.py (line 440-456):
```python
if "ninki" in df.columns:
    fallback_values = pd.to_numeric(df["ninki"], errors="coerce")
    ...  # ninki fallback block to remove
```
</interfaces>
</context>

<tasks>

<task id="1" type="execute">
  <name>Task 1: build_all() POST_RACE drop + popularity_rank fallback fix</name>
  <files>
    <file>src/features/feature_engine.py</file>
  </files>
  <read_first>
    <file>src/features/feature_engine.py</file>
    <file>src/domain/types.py</file>
  </read_first>
  <action>
M1修正 (per D-01, D-02): feature_engine.pyのbuild_all()メソッド内、キャッシュ書き込みブロック(行295 "`# --- Feature Cache Write`")の直前にPOST_RACE_COLS dropを挿入。手順:
1. ファイル先頭のimportセクションに `from domain.types import POST_RACE_COLS` を追加する(未追加の場合)。
2. 行295(キャッシュ書き込み)の直前に以下のブロックを挿入:
   - `post_race_present` に、`result_df.columns` と `POST_RACE_COLS` の積集合のリスト内包表記を代入。
   - `post_race_present` が空でなければ、`logger.info` でドロップ数と列名をログ出力し、`result_df = result_df.drop(columns=post_race_present)` を実行。
   - `# ★ SAFE-01: POST_RACE列を確実に除外 (leakage prevention)` というコメントを付与。
3. 既存のバックテスト/ペーパートレード側のdrop (engine.py:819, run_paper_trading.py:80) は安全ネットとして残す (D-02)。

M6修正 (per D-01, D-05): feature_engine.pyの_map_basic_features()メソッド内のpopularity_rankフォールバックチェーンからninkiブロックを除去。手順:
1. 行440-456の `if "ninki" in df.columns:` ブロック全体を削除。
2. フォールバックの警告メッセージを `"tanodds/tanninki/ninki fallback"` から `"tanodds/tanninki"` に更新 (行459-461)。
3. 削除後も `fallback_mask.any()` のチェックは残し、tanodds/tanninki両方で解決できない馬にはNaNを設定する。
  </action>
  <acceptance_criteria>
    - feature_engine.py に `from domain.types import POST_RACE_COLS` importが存在する
    - `# --- Feature Cache Write` の直前にPOST_RACE_COLS dropブロックがある
    - dropブロックに `logger.info` でドロップ列数をログ出力するコードがある
    - ninki フォールバックブロック (`if "ninki" in df.columns:`) が_map_basic_features()内に存在しない
    - 警告メッセージが `"tanodds/tanninki"` を含み `"ninki"` を含まない
    - `python -m pytest tests/test_feature_engine.py -v` が全テストパスする
  </acceptance_criteria>
</task>

<task id="2" type="execute">
  <name>Task 2: CQR whitelist化 + EV correction odds fix</name>
  <files>
    <file>src/models/conformal_ev_model.py</file>
    <file>src/models/ev_correction_model.py</file>
    <file>src/pipelines/training_pipeline.py</file>
  </files>
  <read_first>
    <file>src/models/conformal_ev_model.py</file>
    <file>src/models/ev_correction_model.py</file>
    <file>src/pipelines/training_pipeline.py</file>
    <file>src/pipelines/training_pipeline.py</file>
    <file>src/domain/types.py</file>
  </read_first>
  <action>
M2修正 (per D-01, D-03): conformal_ev_model.pyのCQR特徴量抽出をblacklist→whitelistに変更。手順:
1. conformal_ev_model.py の ConformalEVModel クラスにクラス変数 `FEATURE_COLS: list[str]` を定義する。training_pipeline.py の行903-907で計算されるfeature_colsリストをベースにする。具体的には:
   - training_pipeline.py:897-902 の `_non_feature_cols` と行903-907 の `feature_cols` リストを確認。
   - ConformalEVModel.FEATURE_COLS に、training_pipeline.py:903-907と同じロジックで抽出される列をハードコードする。`_non_feature_cols` セット({ race_id, umaban, surface, race_date, kettonum, distance_bin, grade_code, track_condition_code } | POST_RACE_COLS | _MODEL_OUTPUT_COLS)に含まれない numeric列が対象。
   - `_MODEL_OUTPUT_COLS` は既にconformal_ev_model.py:23-56に定義されている。
2. ConformalEVModel.train() メソッドの行141-146のfeature_cols決定ロジックを変更:
   - `if self.feature_cols is None:` の中を、`self.feature_cols = [c for c in df_calib.columns if c in self.FEATURE_COLS and pd.api.types.is_numeric_dtype(df_calib[c])]` に変更。
   - blacklist方式の `_NON_FEATURE_COLS` 参照を廃止。
3. training_pipeline.py:909-912 の ConformalEVModel() コンストラクタ呼び出しから `feature_cols=feature_cols` 引数を削除し、FEATURE_COLSによる自動選択に任せる (またはFEATURE_COLSを明示的に渡す)。
4. conformal_ev_model.py の `_NON_FEATURE_COLS` は非推奨コメント付きで残すか、train()で使用されなくなったことをコメントで明記。

M3修正 (per D-01, D-04): ev_correction_model.py:370 のodds_col決定を修正。手順:
1. ev_correction_model.py:370 の `odds_col = "confirmed_odds" if "confirmed_odds" in df.columns else "odds"` を `odds_col = "odds"` に変更。
2. コメントに `# ★ M3 fix: 常に発走前oddsを使用 (学習/推論一貫性)` を付与。
3. 行265-267 のtrain()内E補正ターゲットの `winners["confirmed_odds"]` は変更しない (RESEARCH Pitfall 3参照: ターゲット計算は実際の払戻を反映すべき)。
4. PlaceEVCorrectionModel.correct_ev() に同等のodds_colロジックがある場合も同様に修正する (ある場合)。
  </action>
  <acceptance_criteria>
    - ConformalEVModel クラスに `FEATURE_COLS: list[str]` クラス変数が定義されている
    - ConformalEVModel.train() のfeature_cols決定がFEATURE_COLSベース (blacklist参照なし)
    - ev_correction_model.py のcorrect_ev() 内 odds_col が `"odds"` 固定 (confirmed_oddsフォールバックなし)
    - ev_correction_model.py のtrain() 内 `winners["confirmed_odds"]` (行266付近) は変更されていない
    - training_pipeline.py で ConformalEVModel に明示的 feature_cols を渡している場合、FEATURE_COLSに置き換わっている
    - `python -m pytest tests/ -v` が全テストパスする
  </acceptance_criteria>
</task>

<task id="3" type="execute">
  <name>Task 3: 3層CI漏洩検証テスト (test_post_race_leakage.py)</name>
  <files>
    <file>tests/test_post_race_leakage.py</file>
  </files>
  <read_first>
    <file>src/domain/types.py</file>
    <file>src/features/feature_engine.py</file>
    <file>src/models/stage1_ability_model.py</file>
    <file>src/models/two_stage_return_model.py</file>
    <file>src/models/ev_correction_model.py</file>
    <file>src/models/conformal_ev_model.py</file>
    <file>tests/test_leakage.py</file>
  </read_first>
  <action>
新規テストファイル tests/test_post_race_leakage.py を作成し、3層検証を実装 (per D-09, D-10)。

Layer 1 — build_all()出力検証:
- test_build_all_output_no_post_race_cols クラスメソッドを作成。
- FeatureEngine インスタンスを構築し、build_all()をモック化されたデータで呼び出す。
- race_df, entry_df, odds_df を最小限のfixtureで構築 (race_id, race_date, trackcd, umaban 等の必須列のみ)。
- entry_df に POST_RACE_COLS の一部 (例: kakuteijyuni, ninki) を含めておく。
- build_all() の戻り値の列に POST_RACE_COLS が含まれないことを `assert not (set(result.columns) & set(POST_RACE_COLS))` で検証。
- feature_engine.pyの各サブモジュール呼び出し (compute_intra_race_features等) をモック化して、build_all()単体のdrop動作を検証。

Layer 2 — 全モデルFEATURE_COLS検証:
- test_model_feature_cols_no_post_race クラスメソッドを作成。
- Stage1AbilityModel, WinTwoStageModel, PlaceTwoStageModel, EVCorrectionModel, PlaceEVCorrectionModel, ConformalEVModel のFEATURE_COLSをインポート。
- 各モデルクラスのFEATURE_COLSとPOST_RACE_COLSの積集合が空であることを検証: `assert not (set(cls.FEATURE_COLS) & set(POST_RACE_COLS))`
- モデルクラス名をテスト出力に含め、どのモデルが漏洩列を持つか特定可能にする。

Layer 3 — predict()入力検証:
- test_predict_input_no_post_race クラスメソッドを作成。
- RacePredictor をモックTrainedModelsV5で構築し、predict()にPOST_RACE_COLSを含むDataFrameを渡す。
- predict()内で各モデルに渡されるデータにPOST_RACE_COLSが含まれないことを検証。
- または、EVCorrectionModel.correct_ev() が POST_RACE_COLSを含むDataFrameを受け取った場合にodds-band scalingでconfirmed_oddsを使用しないことを検証。

テストパターンは tests/test_leakage.py の構造を踏襲し、クラスベースで mock使用、DB不要とする。
  </action>
  <acceptance_criteria>
    - tests/test_post_race_leakage.py が存在する
    - TestPostRaceLeakage クラスが定義されている
    - test_build_all_output_no_post_race_cols メソッドが存在する
    - test_model_feature_cols_no_post_race メソッドが存在し、5+モデルクラスのFEATURE_COLSを検証する
    - test_predict_input_no_post_race メソッドが存在する
    - `python -m pytest tests/test_post_race_leakage.py -v` が全テストパスする
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_post_race_leakage.py -v` — 全3テストがパス
2. `python -m pytest tests/test_feature_engine.py -v` — 既存テスト回帰なし
3. `python -m pytest tests/ -v` — 全テストスイートパス
4. `ruff check src/features/ src/models/ src/pipelines/` — リントエラーなし
5. `mypy src/features/ src/models/ src/pipelines/` — 型エラーなし
6. feature_engine.py にPOST_RACE_COLS dropがキャッシュ書き込み前に存在
7. conformal_ev_model.py にFEATURE_COLS whitelistが定義されている
8. ev_correction_model.py:370 が `"odds"` 固定 (confirmed_oddsフォールバックなし)
</verification>

<success_criteria>
- build_all()出力のPOST_RACE_COLSが0列であることがCIテストで自動検証される
- CQRがblacklist方式を廃止しwhitelist FEATURE_COLSを使用する
- EV correctionのodds-band scalingが学習/推論で一貫して発走前oddsを使用する
- popularity_rankフォールバックがninkiを含まない
- 全既存テストがパスする (回帰なし)
</success_criteria>

<output>
After completion, create `.planning/phases/23-safety-gate/23-01-SUMMARY.md`
</output>
