---
phase: 24-feature-audit-pruning
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - src/features/win_feature_analysis.py
  - scripts/prune_noise_features.py
  - tests/test_prune_noise_features.py
autonomous: true
requirements: [AUDIT-01, AUDIT-02]

must_haves:
  truths:
    - "Tier 1ノイズ特徴量が各モデルのFEATURE_COLSから除外される (モデル別個別)"
    - "除外前後のOOF logloss/AUCが比較され、logloss悪化0.5%超で除外が取り消される"
    - "回帰モデル(return/E-correction)ではOOF安全性確認がMAEベースまたはスキップされる"
    - "フルバックテストROIがv1.5ベースライン84.4%以上である、またはロールバック+原因分析レポートが出力される"
    - "ROI悪化時は即座にFEATURE_COLSが復元され、原因分析レポートがdata/audit/に保存される"
  artifacts:
    - path: "scripts/prune_noise_features.py"
      provides: "Tier 1除外 + OOF安全性確認 + フルBT ROI検証 + ロールバックの統合スクリプト"
      contains: "def main"
    - path: "data/audit/tier_report.json"
      provides: "Tier分類結果"
      contains: "tier1"
    - path: "data/audit/pruning_validation.json"
      provides: "プルーニング前後のOOF品質比較結果"
      contains: "original_logloss"
    - path: "data/audit/roi_comparison.json"
      provides: "フルBT前後のROI比較結果 (D-04 Step 2)"
      contains: "baseline_roi"
    - path: "data/audit/cause_analysis.json"
      provides: "ROI悪化時の原因分析レポート (D-05)。generate_cause_analysis()で生成"
      contains: "odds_band_roi"
  key_links:
    - from: "scripts/prune_noise_features.py"
      to: "src/features/win_feature_analysis.py"
      via: "classify_feature_tiers() + validate_noise_removal()"
      pattern: "classify_feature_tiers"
    - from: "scripts/prune_noise_features.py"
      to: "src/backtest/validation_report.py"
      via: "generate_cause_analysis() for D-05 cause analysis"
      pattern: "generate_cause_analysis"
    - from: "scripts/prune_noise_features.py"
      to: "src/models/stage1_ability_model.py"
      via: "FEATURE_COLS list editing"
      pattern: "FEATURE_COLS"
---

<objective>
Tier 1ノイズ特徴量を各モデルのFEATURE_COLSからモデル別に除外し、OOF品質確認→フルBT検証の段階的ROI検証を実行する。

Purpose: AUDIT-01/AUDIT-02の実践フェーズ。Plan 01で構築したTier分類ロジックを実行し、各モデルのTier 1ノイズをFEATURE_COLSから除外する。除外前後のOOF logloss/AUC比較で安全性確認後、フルバックテストでROIを検証する。ROI悪化時はロールバック+原因分析レポートを出力。
Output: プルーニング済みFEATURE_COLS (7モデル)、検証レポート、フルBT ROI比較結果、原因分析レポート(条件付き)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-feature-audit-pruning/24-CONTEXT.md
@.planning/phases/24-feature-audit-pruning/24-RESEARCH.md
@.planning/phases/24-feature-audit-pruning/24-01-SUMMARY.md

<interfaces>
<!-- Created by Plan 01, consumed here -->

From src/features/win_feature_analysis.py (Plan 01 additions):
```python
def classify_feature_tiers(
    pivot_df: pd.DataFrame,
    metadata: dict[str, Any],
    *,
    tier2_percentile: float = 10.0,
) -> dict[str, dict[str, Any]]:
    # Returns: {model_name: {"tier1": [...], "tier2": [...], "tier1_count": int, "tier2_count": int}}
```

From src/features/win_feature_analysis.py (Phase 23):
```python
def validate_noise_removal(
    original_model: lgb.Booster,
    df: pd.DataFrame,
    noise_features: list[str],
    target_col: str = "kakuteijyuni",
    num_threads: int = 0,
) -> dict[str, float]:
    # Returns: {original_logloss, new_logloss, original_auc, new_auc}
    # Uses binary target: (df[target_col] == 1).astype(int)
    # Fixed: objective="binary", metric="binary_logloss"
```

From src/backtest/validation_report.py:
```python
def generate_cause_analysis(bet_history: list[dict[str, Any]]) -> dict[str, Any]:
    # Returns: {odds_band_roi, regime_roi, ev_diagnosis, bet_count_sufficiency, surface_roi}

def generate_validation_report(result, test_start, test_end, train_start, train_end, ...) -> dict[str, Any]:
    # Returns full validation report with ROI, yearly_breakdown, cause_analysis

def evaluate_validation(roi: float, total_bets: int) -> str:
    # Returns "PASS" or "FAIL"
```

<!-- Model FEATURE_COLS locations (pruning targets) -->
AbilityModel.FEATURE_COLS (80 features):       src/models/stage1_ability_model.py:28
WinTwoStageModel.FEATURE_COLS (38 features):   src/models/two_stage_return_model.py:48
PlaceTwoStageModel.HIT_FEATURE_COLS (45):      src/models/two_stage_return_model.py:289
PlaceTwoStageModel.RETURN_FEATURE_COLS (43):   src/models/two_stage_return_model.py:345
EVCorrectionModel.FEATURE_COLS (24):           src/models/ev_correction_model.py:151
PlaceEVCorrectionModel.FEATURE_COLS (29):      src/models/ev_correction_model.py:405
ConformalEVModel.FEATURE_COLS (131):           src/models/conformal_ev_model.py:81

<!-- Binary vs Regression model classification -->
Binary models (validate_noise_removal OK): stage1, win_hit, place_hit
Regression models (skip or MAE): win_return, place_return, ev_correction, place_ev_correction, conformal_ev
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: プルーニングロジック + OOF安全性確認スクリプト (D-02, D-03, D-04 Step1)</name>
  <files>scripts/prune_noise_features.py, tests/test_prune_noise_features.py</files>
  <read_first>
    - src/features/win_feature_analysis.py (classify_feature_tiers(), validate_noise_removal() のシグネチャと戻り値構造)
    - scripts/analyze_feature_importance.py (モデルロード・ターゲット構築パターン、_find_model_file(), _load_features_for_analysis())
    - src/models/stage1_ability_model.py (AbilityModel.FEATURE_COLS の定義パターン)
    - src/models/two_stage_return_model.py (WinTwoStageModel, PlaceTwoStageModel のFEATURE_COLS/HIT_FEATURE_COLS/RETURN_FEATURE_COLS)
    - src/models/ev_correction_model.py (EVCorrectionModel.FEATURE_COLS, PlaceEVCorrectionModel.FEATURE_COLS)
    - src/models/conformal_ev_model.py (ConformalEVModel.FEATURE_COLS)
    - src/backtest/validation_report.py (generate_cause_analysis() のシグネチャと戻り値構造)
  </read_first>
  <action>
    scripts/prune_noise_features.py を新規作成する。Tier 1除外→OOF安全性確認→フルBT ROI検証→ロールバック/確定の統合スクリプト。

    CLI引数:
    - --model-dir: モデルファイルディレクトリ (default: data/models)
    - --features-path: OOF特徴量parquetパス (default: data/features/horse_features.parquet)
    - --apply: 実際にFEATURE_COLSを編集する (dry-runモードがデフォルト)
    - --full-bt: フルバックテストでROI検証を実行する (D-04 Step 2)
    - --output: 検証結果JSONの出力先 (default: data/audit/pruning_validation.json)
    - --safety-threshold: logloss悪化許容閾値 (default: 0.005 = 0.5%)
    - --baseline-roi: ROI比較ベースライン (default: 0.844 = 84.4%)
    - --rollback: ROI悪化時に自動ロールバックする (--full-btと併用)

    処理フロー (D-04 Step 1: OOF安全性確認):
    Step 1: モデルファイルをロード (--model-dir から *.lgb をglob)
    Step 2: 特徴量データをロード (--features-path)
    Step 3: compute_all_model_importance() でgain+perm重要度を計算
    Step 4: classify_feature_tiers() でTier 1/2を分類
    Step 5: 各モデルのTier 1特徴量に対して安全性確認を実行:
      - Binaryモデル (stage1, win_hit, place_hit): validate_noise_removal() でOOF logloss/AUC比較 (D-03)。target_col="kakuteijyuni"。
      - Regressionモデル (win_return, place_return, ev_correction, place_ev_correction, conformal_ev):
        OOF安全性確認をスキップし、safety_passed=True とする。理由: validate_noise_removal() はbinary objective固定でlogloss/AUCを計算するため、regressionモデルには不適切 (RESEARCH Pitfall 6)。安全性はD-04 Step 2のフルBTで包括的に検証する。
    Step 6: logloss悪化がsafety_threshold超の場合は当該モデルのTier 1除外をスキップ (binaryモデルのみ)
    Step 7: 安全性確認通過したTier 1特徴量のリストをJSON出力
    Step 8: --apply 指定時のみ、各モデルファイルのFEATURE_COLS定義行を直接編集

    モデル名→FEATURE_COLS定義ファイル+変数名のマッピング (D-02):
    - stage1 → stage1_ability_model.py: AbilityModel.FEATURE_COLS
    - win_hit → two_stage_return_model.py: WinTwoStageModel.FEATURE_COLS
    - win_return → two_stage_return_model.py: WinTwoStageModel.FEATURE_COLS (同じモデルの別フェーズ)
    - place_hit → two_stage_return_model.py: PlaceTwoStageModel.HIT_FEATURE_COLS
    - place_return → two_stage_return_model.py: PlaceTwoStageModel.RETURN_FEATURE_COLS
    - ev_correction → ev_correction_model.py: EVCorrectionModel.FEATURE_COLS
    - place_ev_correction → ev_correction_model.py: PlaceEVCorrectionModel.FEATURE_COLS
    - conformal_ev → conformal_ev_model.py: ConformalEVModel.FEATURE_COLS

    モデル分類定数をスクリプト内に定義:
    BINARY_MODELS = {"stage1", "win_hit", "place_hit"}
    REGRESSION_MODELS = {"win_return", "place_return", "ev_correction", "place_ev_correction", "conformal_ev"}

    --apply の編集方法:
    各モデルファイルのFEATURE_COLS定義からTier 1特徴量の文字列を削除。Python ASTではなく、正規表現または行ベースのテキスト編集で該当行を削除。編集前のバックアップを.backupとして保存。

    --full-bt のフルBT ROI検証フロー (D-04 Step 2, BLOCKER 1/2 fix):
    Step B1: subprocessで run_backtest.py を実行:
      python scripts/run_backtest.py --years 2024 --train-window 4 --ensemble --calibration-bt --report
    Step B2: バックテスト結果JSON (backtest_result.json) から total_roi を読み取り
    Step B3: ROI比較結果を data/audit/roi_comparison.json に保存:
      {
        "timestamp": "ISO8601",
        "baseline_roi": 0.844,
        "baseline_source": "v1.5 (Phase 22 result)",
        "pruned_roi": {pruned_roi},
        "roi_delta": {delta},
        "roi_improved": true/false,
        "bt_command": "run_backtest.py --years 2024 --train-window 4 --ensemble --calibration-bt --report"
      }
    Step B4: pruned_roi >= baseline_roi の場合は確定 (コミット可能状態)
    Step B5: pruned_roi < baseline_roi の場合、D-05ロールバックフローへ

    --rollback のロールバック+原因分析フロー (D-05, BLOCKER 1/3 fix):
    Step R1: .backupファイルから元のFEATURE_COLSに復元
    Step R2: バックテストのbet_history (data/backtest/bt_2024_*.csv) を読み込み
    Step R3: generate_cause_analysis() を呼び出して原因分析レポートを生成
    Step R4: 原因分析レポートを data/audit/cause_analysis.json に保存:
      {
        "timestamp": "ISO8601",
        "baseline_roi": 0.844,
        "pruned_roi": {value},
        "rollback_performed": true,
        "cause_analysis": {generate_cause_analysis()の戻り値},
        "tier1_removed_per_model": {model: [features]},
        "recommendation": "Tier 1 features may be providing regularization benefit. Consider keeping features with gain>0 in some models."
      }
    Step R5: logger.warning でロールバック完了と原因分析レポートのパスを出力

    出力JSON (data/audit/pruning_validation.json):
    {
      "timestamp": "ISO8601",
      "models": {
        "win_hit": {
          "tier1_features": [...],
          "tier2_features": [...],
          "model_type": "binary",
          "oof_safety": {"original_logloss": X, "new_logloss": Y, "original_auc": Z, "new_auc": W},
          "safety_passed": true,
          "applied": false
        },
        "win_return": {
          "tier1_features": [...],
          "tier2_features": [...],
          "model_type": "regression",
          "oof_safety": null,
          "safety_skipped_reason": "regression model - safety validated via full BT",
          "safety_passed": true,
          "applied": false
        },
        ...
      },
      "total_tier1_removed": N,
      "total_tier2_flagged": M
    }

    テスト追加 (tests/test_prune_noise_features.py):
    - TestPruneNoiseFeatures クラス
    - test_dry_run_does_not_modify_files: --applyなしではFEATURE_COLSが変更されない
    - test_oof_safety_check_blocks_removal: logloss悪化0.5%超で除外がブロックされる
    - test_regression_model_skips_oof_check: regressionモデルではoof_safetyがnull、safety_passedがtrue
    - test_per_model_independent_pruning: モデルAのTier 1がモデルBに影響しない
    - test_backup_created_on_apply: --apply時に.backupが作成される
    - test_rollback_restores_original: ロールバックでFEATURE_COLSが復元される
    - test_roi_comparison_json_structure: --full-btの出力JSONがbaseline_roi, pruned_roi, roi_improvedを含む
    - test_rollback_with_cause_analysis: ROI悪化時にロールバック+原因分析JSONが生成される
    テストではlgb.Boosterをmockし、validate_noise_removalもmonkeypatchで制御する。subprocess呼び出しもmockする。
  </action>
  <verify>
    <automated>python -m pytest tests/test_prune_noise_features.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - prune_noise_features.py が --model-dir, --features-path, --apply, --full-bt, --rollback, --output, --safety-threshold, --baseline-roi を受け付ける
    - dry-runモード (--applyなし) でFEATURE_COLSが変更されない
    - OOF logloss悪化0.5%超のbinaryモデルはTier 1除外がスキップされる
    - regressionモデルではOOF安全性確認がスキップされ、safety_passed=Trueになる
    - モデル別に独立してTier 1が判定される (D-02)
    - --apply時に.backupファイルが作成される
    - --full-bt がフルBTを実行し、ROI比較結果をdata/audit/roi_comparison.jsonに保存する (D-04 Step 2)
    - --rollback が.backupから復元し、原因分析レポートをdata/audit/cause_analysis.jsonに保存する (D-05)
    - JSON出力に各モデルのmodel_type (binary/regression)、oof_safety結果とsafety_passedが含まれる
    - 8つのテストが全てPASSする
    - ruff check scripts/prune_noise_features.py が通過する
  </acceptance_criteria>
  <done>プルーニング統合スクリプトが作成され、Tier 1除外→OOF安全性確認(binaryのみ)→フルBT ROI検証→ロールバック+原因分析の全フローがテストで検証される</done>
</task>

<task type="auto">
  <name>Task 2: Tier 2レポート出力の拡張</name>
  <files>src/features/win_feature_analysis.py</files>
  <read_first>
    - src/features/win_feature_analysis.py (Task 1で追加したclassify_feature_tiers()の戻り値構造)
    - scripts/analyze_feature_importance.py (--tier-reportの出力パターン)
  </read_first>
  <action>
    win_feature_analysis.py に generate_tier_report() 関数を追加し、Tier 1/2の包括的レポートを生成する。

    関数シグネチャ: generate_tier_report(tier_result: dict[str, dict[str, Any]], pivot_df: pd.DataFrame) -> dict[str, Any]

    レポート内容:
    - 各モデルのTier 1/2特徴量リスト (名前 + gain値 + perm値付き)
    - 全モデル横断のTier 1出現頻度 (どの特徴量が何モデルでTier 1か)
    - サマリー: 全体のTier 1件数、Tier 2件数、除外される総特徴量数
    - 推奨アクション: Tier 1 = "auto-remove", Tier 2 = "review manually"

    この関数をscripts/analyze_feature_importance.pyの--tier-reportモードからも呼び出せるようにする。

    また、既存テストの回帰確認として tests/test_post_race_leakage.py が通過することを確認。
  </action>
  <verify>
    <automated>python -m pytest tests/test_win_feature_analysis.py tests/test_post_race_leakage.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - generate_tier_report() が win_feature_analysis.py に追加される
    - レポートに各特徴量のgain値とperm値が含まれる
    - 全モデル横断のTier 1出現頻度が計算される
    - test_post_race_leakage.py の全テストが引き続き通過する
  </acceptance_criteria>
  <done>包括的Tier レポートが生成され、POST_RACE漏洩テストが引き続き通過する</done>
</task>

</tasks>

<verification>
python -m pytest tests/ -v --tb=short
ruff check scripts/prune_noise_features.py src/features/win_feature_analysis.py
python -m pytest tests/test_post_race_leakage.py -v
python -m pytest tests/test_prune_noise_features.py -v
</verification>

<success_criteria>
1. prune_noise_features.pyがdry-run + --applyモードで正しく動作する
2. Tier 1除外がモデル別に独立して適用される (D-02)
3. OOF logloss/AUC比較で安全性が確認される (binaryモデルのみ) (D-03)
4. 回帰モデルではOOF安全性確認がスキップされ、フルBTで検証される (RESEARCH Pitfall 6対応)
5. logloss悪化0.5%超で除外がブロックされる
6. --apply時に.backupが作成され、ロールバックが可能
7. --full-bt がフルBT ROI検証を実行し、data/audit/roi_comparison.jsonを出力する (D-04 Step 2)
8. --rollback がロールバック+data/audit/cause_analysis.jsonを出力する (D-05)
9. v1.5ベースライン ROI 84.4%と比較される (AUDIT-02)
10. 包括的Tier レポートがJSON出力される
11. POST_RACE漏洩テストが引き続き通過する
12. 全テスト通過
</success_criteria>

<output>
After completion, create `.planning/phases/24-feature-audit-pruning/24-02-SUMMARY.md`
</output>
