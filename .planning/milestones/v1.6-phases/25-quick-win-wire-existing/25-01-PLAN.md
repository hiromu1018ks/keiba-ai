---
phase: 25-quick-win-wire-existing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/two_stage_return_model.py
  - src/paper_trading/predictor.py
  - tests/test_two_stage_return_model.py
autonomous: true
requirements:
  - WIRE-01
  - WIRE-02
  - WIRE-03

must_haves:
  truths:
    - "WinTwoStageModel.FEATURE_COLSに12特徴量(jockey_wr_overall, jockey_wr_distance, jockey_wr_venue, jockey_prize_log, trainer_wr_overall, trainer_wr_distance, trainer_wr_venue, trainer_prize_log, jt_combo_wr, jt_combo_place_rate, jt_combo_starts, jt_combo_prize_log)が含まれる"
    - "PlaceTwoStageModel.HIT_FEATURE_COLSに残り9特徴量(jockey_wr_distance, jockey_wr_venue, jockey_prize_log, trainer_wr_distance, trainer_wr_venue, trainer_prize_log, jt_combo_wr, jt_combo_starts, jt_combo_prize_log)が含まれる"
    - "PlaceTwoStageModel.RETURN_FEATURE_COLSに12特徴量すべてが含まれる"
    - "AbilityModel.FEATURE_COLSに12特徴量が含まれていない"
    - "paper_trading/predictor.pyがJockeyTrainerComboFeaturesを計算・マージする"
    - "全テストが通過する"
  artifacts:
    - path: "src/models/two_stage_return_model.py"
      provides: "WinTwoStageModel.FEATURE_COLS (50特徴量), PlaceTwoStageModel.HIT_FEATURE_COLS (54特徴量), PlaceTwoStageModel.RETURN_FEATURE_COLS (55特徴量)"
    - path: "src/paper_trading/predictor.py"
      provides: "JockeyTrainerComboFeatures計算・マージ"
    - path: "tests/test_two_stage_return_model.py"
      provides: "更新されたfeature_dfフィクスチャ + 新特徴量のFEATURE_COLS存在テスト"
  key_links:
    - from: "src/models/two_stage_return_model.py"
      to: "src/features/jockey_context_features.py, src/features/trainer_context_features.py, src/features/jockey_trainer_combo.py"
      via: "FEATURE_COLSの文字列名参照"
      pattern: "jockey_wr_|trainer_wr_|jt_combo_"
    - from: "src/paper_trading/predictor.py"
      to: "src/features/jockey_trainer_combo.py"
      via: "import + compute() + merge"
      pattern: "JockeyTrainerComboFeatures"
---

<objective>
既に実装済みのJockey/Trainer/Combo合計12特徴量をFEATURE_COLSに追加し、paper_tradingパスにJockeyTrainerComboFeatures計算を追加する。

Purpose: 計算ロジックはPhase 22-24で実装済みだが、モデルのFEATURE_COLSリストに追加されていないためモデルが特徴量を認識できない。文字列リストへの追加で即座にモデルが新特徴量を利用可能になる。
Output: 更新されたtwo_stage_return_model.py, predictor.py, test_two_stage_return_model.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-quick-win-wire-existing/25-CONTEXT.md

<interfaces>
<!-- 12特徴量の定義元 -->

From src/features/jockey_context_features.py:
```python
FEATURE_COLS: list[str] = [
    "jockey_wr_overall",
    "jockey_wr_distance",
    "jockey_wr_venue",
    "jockey_prize_log",
]
```

From src/features/trainer_context_features.py:
```python
FEATURE_COLS: list[str] = [
    "trainer_wr_overall",
    "trainer_wr_distance",
    "trainer_wr_venue",
    "trainer_prize_log",
]
```

From src/features/jockey_trainer_combo.py:
```python
FEATURE_COLS: list[str] = [
    "jt_combo_wr",
    "jt_combo_place_rate",
    "jt_combo_starts",
    "jt_combo_prize_log",
]
```

<!-- PlaceTwoStageModel.HIT_FEATURE_COLS に既に含まれる3特徴量 (追加不要) -->
- jockey_wr_overall (L307)
- trainer_wr_overall (L308)
- jt_combo_place_rate (L309)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WinTwoStageModel + PlaceTwoStageModel FEATURE_COLSに12特徴量を追加</name>
  <files>src/models/two_stage_return_model.py</files>
  <read_first>
    - src/models/two_stage_return_model.py (L48-102: WinTwoStageModel.FEATURE_COLS, L289-340: PlaceTwoStageModel.HIT_FEATURE_COLS, L345-400: PlaceTwoStageModel.RETURN_FEATURE_COLS)
    - src/features/jockey_context_features.py (FEATURE_COLS定義)
    - src/features/trainer_context_features.py (FEATURE_COLS定義)
    - src/features/jockey_trainer_combo.py (FEATURE_COLS定義)
    - src/models/stage1_ability_model.py (L28-128: 変更対象外の確認用)
  </read_first>
  <action>
    per D-01, D-03:

    **WinTwoStageModel.FEATURE_COLS (L48-102):**
    現在38特徴量のリストの末尾（"deviation_zscore"の後、L101-102の間）に以下12特徴量をグループコメント付きで追加:
    - コメント: "# 騎手コンテキスト (Group C, Stage2)"
    - "jockey_wr_overall", "jockey_wr_distance", "jockey_wr_venue", "jockey_prize_log"
    - コメント: "# 調教師コンテキスト (Group D, Stage2)"
    - "trainer_wr_overall", "trainer_wr_distance", "trainer_wr_venue", "trainer_prize_log"
    - コメント: "# 騎手-調教師コンビ (Stage2)"
    - "jt_combo_wr", "jt_combo_place_rate", "jt_combo_starts", "jt_combo_prize_log"
    これで38 -> 50特徴量になる。

    **PlaceTwoStageModel.HIT_FEATURE_COLS (L289-340):**
    既に含まれている3つ: jockey_wr_overall (L307), trainer_wr_overall (L308), jt_combo_place_rate (L309)
    残り9特徴量を追加。L309の"jt_combo_place_rate"の直後にグループコメント付きで追加:
    - "jockey_wr_distance", "jockey_wr_venue", "jockey_prize_log"
    - "trainer_wr_distance", "trainer_wr_venue", "trainer_prize_log"
    - "jt_combo_wr", "jt_combo_starts", "jt_combo_prize_log"
    これで45 -> 54特徴量になる。

    **PlaceTwoStageModel.RETURN_FEATURE_COLS (L345-400):**
    現在0/12特徴量。L399の"deviation_zscore"の後にグループコメント付きで12特徴量すべてを追加:
    - コメント: "# 騎手コンテキスト (Group C, Stage2)"
    - "jockey_wr_overall", "jockey_wr_distance", "jockey_wr_venue", "jockey_prize_log"
    - コメント: "# 調教師コンテキスト (Group D, Stage2)"
    - "trainer_wr_overall", "trainer_wr_distance", "trainer_wr_venue", "trainer_prize_log"
    - コメント: "# 騎手-調教師コンビ (Stage2)"
    - "jt_combo_wr", "jt_combo_place_rate", "jt_combo_starts", "jt_combo_prize_log"
    これで43 -> 55特徴量になる。

    per D-02: AbilityModel.FEATURE_COLS (stage1_ability_model.py L28-128) は一切変更しない。
  </action>
  <verify>
    <automated>python -m pytest tests/test_post_race_leakage.py tests/test_two_stage_return_model.py -v -x 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - WinTwoStageModel.FEATURE_COLS に "jockey_wr_overall", "jockey_wr_distance", "jockey_wr_venue", "jockey_prize_log", "trainer_wr_overall", "trainer_wr_distance", "trainer_wr_venue", "trainer_prize_log", "jt_combo_wr", "jt_combo_place_rate", "jt_combo_starts", "jt_combo_prize_log" が含まれる
    - len(WinTwoStageModel.FEATURE_COLS) == 50
    - PlaceTwoStageModel.HIT_FEATURE_COLS に上記12特徴量がすべて含まれる
    - len(PlaceTwoStageModel.HIT_FEATURE_COLS) == 54
    - PlaceTwoStageModel.RETURN_FEATURE_COLS に上記12特徴量がすべて含まれる
    - len(PlaceTwoStageModel.RETURN_FEATURE_COLS) == 55
    - AbilityModel.FEATURE_COLS に "jockey_wr_overall" 等が含まれていない
    - FEATURE_COLSのいずれのリストにも重複がない
  </acceptance_criteria>
  <done>Win/Place両方の2段階モデルのFEATURE_COLSに12特徴量が追加され、POST_RACE漏洩テストと既存テストが通過する</done>
</task>

<task type="auto">
  <name>Task 2: paper_trading/predictor.pyにJockeyTrainerComboFeatures計算を追加 + テスト更新</name>
  <files>src/paper_trading/predictor.py, tests/test_two_stage_return_model.py</files>
  <read_first>
    - src/paper_trading/predictor.py (L1-120: setup()メソッド全体)
    - tests/test_two_stage_return_model.py (L1-70: feature_dfフィクスチャ, L549-577: 既存FEATURE_COLSテスト)
    - src/features/jockey_trainer_combo.py (JockeyTrainerComboFeaturesクラスとcompute()シグネチャ)
  </read_first>
  <action>
    per D-05:

    **src/paper_trading/predictor.py:**
    1. setup()メソッド内のimportブロック（L53-57）に以下を追加:
       `from features.jockey_trainer_combo import JockeyTrainerComboFeatures`
    2. trainer_df計算後（L104の後）に以下を追加:
       ```
       jt_combo = JockeyTrainerComboFeatures(self.store)
       jt_combo_df = jt_combo.compute(entry_df)
       ```
    3. L107のマージループ `for col_df in [hist_df, jockey_df, trainer_df]:` を `for col_df in [hist_df, jockey_df, trainer_df, jt_combo_df]:` に変更。

    **tests/test_two_stage_return_model.py:**
    1. feature_dfフィクスチャ（L19-66）に以下の12カラムを追加:
       - "jockey_wr_distance": [0.12, 0.11, 0.10, 0.09, 0.08, 0.07, 0.05, 0.03]
       - "jockey_wr_venue": [0.13, 0.12, 0.11, 0.10, 0.09, 0.08, 0.06, 0.04]
       - "jockey_prize_log": [14.0, 13.5, 13.0, 12.5, 12.0, 11.5, 10.5, 9.5]
       - "trainer_wr_distance": [0.11, 0.10, 0.09, 0.08, 0.07, 0.06, 0.04, 0.02]
       - "trainer_wr_venue": [0.10, 0.09, 0.08, 0.07, 0.06, 0.05, 0.04, 0.02]
       - "trainer_prize_log": [13.5, 13.0, 12.5, 12.0, 11.5, 11.0, 10.0, 9.0]
       - "jt_combo_wr": [0.15, 0.13, 0.11, 0.09, 0.07, 0.05, 0.03, 0.01]
       - "jt_combo_starts": [50, 45, 40, 35, 30, 25, 20, 15]
       - "jt_combo_prize_log": [14.0, 13.5, 13.0, 12.5, 12.0, 11.5, 11.0, 10.0]
       注: "jockey_wr_overall", "trainer_wr_overall", "jt_combo_place_rate" は既にL57-59に存在する。
    2. test_train_return_model_early_stopping_guardフィクスチャ（L154-182のdf）にも同じ12カラムの値を追加（既存3つが無い場合は12個すべて、既存3つがある場合は残り9個）。
    3. 既存のTestFEATURESIntegrationクラス（L549付近）を更新または新しいテストメソッドを追加して、12特徴量のFEATURE_COLS存在確認と重複なし確認を行う:
       - test_jockey_trainer_combo_in_feature_cols: 12特徴量がWinTwoStageModel.FEATURE_COLSに含まれる
       - test_no_duplicates: FEATURE_COLSに重複がない
       - test_minimum_count: len(WinTwoStageModel.FEATURE_COLS) >= 50
  </action>
  <verify>
    <automated>python -m pytest tests/test_two_stage_return_model.py tests/test_post_race_leakage.py -v -x 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - predictor.py に `from features.jockey_trainer_combo import JockeyTrainerComboFeatures` import文が存在する
    - predictor.py に `JockeyTrainerComboFeatures(self.store)` のインスタンス化が存在する
    - predictor.py のマージループに `jt_combo_df` が含まれる
    - feature_dfフィクスチャに "jockey_wr_distance", "jockey_wr_venue", "jockey_prize_log", "trainer_wr_distance", "trainer_wr_venue", "trainer_prize_log", "jt_combo_wr", "jt_combo_starts", "jt_combo_prize_log" の9カラムが追加されている
    - tests/test_two_stage_return_model.py のテストが全て通過する
    - test_post_race_leakage.py のテストが全て通過する
  </acceptance_criteria>
  <done>paper_tradingパスでJockeyTrainerComboFeaturesが計算・マージされ、全テストが通過する</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FEATURE_COLS設定 | モデルが使用する特徴量名のリスト。誤った特徴量名を追加すると実行時エラー |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | I (Information Disclosure) | FEATURE_COLS | accept | 特徴量名の追加のみ。POST_RACE漏洩テスト(test_post_race_leakage.py)が既に存在し、追加特徴量もPRE_RACEであることを確認 |
| T-25-02 | S (Spoofing) | paper_trading/predictor.py | mitigate | JockeyTrainerComboFeatures.compute()が既にrace_dateによるリーク防止を実装済み。import path確認のみ |
</threat_model>

<verification>
1. `python -m pytest tests/test_two_stage_return_model.py -v` が全て通過
2. `python -m pytest tests/test_post_race_leakage.py -v` が全て通過
3. `python -m pytest tests/ -v --tb=short` 全テスト通過
4. `ruff check src/models/two_stage_return_model.py src/paper_trading/predictor.py` エラーなし
</verification>

<success_criteria>
- WinTwoStageModel.FEATURE_COLSが50特徴量（38+12）を含む
- PlaceTwoStageModel.HIT_FEATURE_COLSが54特徴量（45+9）を含む
- PlaceTwoStageModel.RETURN_FEATURE_COLSが55特徴量（43+12）を含む
- AbilityModel.FEATURE_COLSが変更されていない
- paper_trading/predictor.pyがJockeyTrainerComboFeaturesを計算・マージする
- POST_RACE漏洩テストが通過する
- 全テストが通過する
</success_criteria>

<output>
After completion, create `.planning/phases/25-quick-win-wire-existing/25-01-SUMMARY.md`
</output>
