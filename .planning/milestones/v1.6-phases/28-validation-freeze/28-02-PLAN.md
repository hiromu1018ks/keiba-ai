---
phase: 28-validation-freeze
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - data/backtest/bt_2023_predictions.parquet
  - data/backtest/bt_2024_predictions.parquet
  - data/backtest/bt_2025_predictions.parquet
  - data/backtest/multi_year_result.json
  - data/validation/multi_year_validation_report.json
  - feature_importance_report.json
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - integrated-backtest-roi
  - feature-importance-recalc
  - roadmap-documentation

must_haves:
  truths:
    - "マルチ年度BT (2023/2024/2025) のROIが算出されている"
    - "Feature importanceが全モデルで再計算されている"
    - "ROADMAP.mdにPhase 28結果が記録されている"
  artifacts:
    - path: "data/backtest/multi_year_result.json"
      provides: "3年統合ROI結果"
      contains: "overall"
    - path: "feature_importance_report.json"
      provides: "全モデルのpermutation+gain重要度"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 28完了記録"
      contains: "Phase 28"
  key_links:
    - from: "data/backtest/multi_year_result.json"
      to: ".planning/ROADMAP.md"
      via: "ROI結果をROADMAPに転記"
      pattern: "roi"
---

<objective>
Phase 28の本丸: マルチ年度バックテスト実行 + Feature importance再計算 + 結果ドキュメント更新。

Purpose: Phase 23-27の全特徴量追加・修正の統合ROI検証を行い、v1.6マイルストーンの最終成果を記録する。
Output: 3年マルチ年度BT ROI結果 + Feature importanceレポート + ROADMAP.md更新
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-validation-freeze/28-CONTEXT.md
@.planning/phases/28-validation-freeze/28-RESEARCH.md
@.planning/phases/28-validation-freeze/28-01-SUMMARY.md

<interfaces>
<!-- BT output schema -->
data/backtest/multi_year_result.json schema:
{
  "overall": {
    "total_bets": int,
    "total_stake": float,
    "total_return": float,
    "profit": float,
    "roi": float,  // 1.0 = 100%
    "best_year": int,
    "worst_year": int
  },
  "years": {
    "YYYY": { "roi": float, "total_bets": int, ... }
  }
}

<!-- _load_strategy_params fallback -->
scripts/run_backtest.py line 191-193:
  if not path.exists():
    logger.warning("Strategy manifest not found: %s -- using default params", path)
    return None
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: マルチ年度バックテスト実行 (2023/2024/2025)</name>
  <files>data/backtest/multi_year_result.json, data/validation/multi_year_validation_report.json</files>
  <read_first>
    - scripts/run_backtest.py (--years, --train-window, --ensemble, --calibration-bt, --report, --strategy-manifest 引数定義)
  </read_first>
  <action>
    マルチ年度バックテストを実行する。per D-01 (3年テスト), D-03 (BTフラグ), D-04 (完全コマンド)。

    重要事項: `data/strategy_manifest.json` が存在しない (Phase 13以降消失)。_load_strategy_paramsはwarning付きでNoneを返し、デフォルト戦略パラメータでBTが実行される。per D-02 (既存manifestをそのまま使用 = 存在しない場合はデフォルト)。blockerではない。

    実行コマンド (D-04 per user decision, strategy_manifest不存在でもwarningのみで実行継続):
    python scripts/run_backtest.py --years 2023 2024 2025 --train-window 4 --ensemble --calibration-bt --report --strategy-manifest data/strategy_manifest.json

    所要時間: ~3時間 (各年~57分 x 3年 + キャリブレーションBT)

    実行完了後、以下を確認:
    1. `data/backtest/multi_year_result.json` が存在し、overall キーを含む
    2. overall.roi 値を抽出 (1.0 = 100%)
    3. 年度別ROI (2023/2024/2025) を確認
    4. D-05フォーマットでROI結果を記録: "v1.5: 84.4% -> v1.6: XX% (+Y.Ypp)"

    BT出力ファイル:
    - data/backtest/bt_2023_*.{csv,parquet}
    - data/backtest/bt_2024_*.{csv,parquet}
    - data/backtest/bt_2025_*.{csv,parquet}
    - data/backtest/multi_year_result.json
    - data/validation/multi_year_validation_report.json

    ROI結果はTask 3 (ROADMAP更新) で使用する。
  </action>
  <verify>
    <automated>python -c "import json; r=json.load(open('data/backtest/multi_year_result.json')); o=r['overall']; print(f'ROI: {o[\"roi\"]*100:.1f}%, Bets: {o[\"total_bets\"]}, Profit: {o[\"profit\"]:.0f}円'); [print(f'  {y}: ROI={v[\"roi\"]*100:.1f}%') for y,v in r['years'].items()]"</automated>
  </verify>
  <done>
    - マルチ年度BT (2023/2024/2025) が完了
    - `data/backtest/multi_year_result.json` にoverall + yearly ROIが記録されている
    - ROI結果が確認可能 (D-05フォーマットで記録準備完了)
  </done>
</task>

<task type="auto">
  <name>Task 2: Feature importance再計算 (--all-models)</name>
  <files>feature_importance_report.json</files>
  <read_first>
    - scripts/analyze_feature_importance.py (--all-models, --model-dir, --format 引数定義)
  </read_first>
  <action>
    Feature importanceを全モデルで再計算する。per D-09 (3本柱の1つ) と D-10 (analyze_feature_importance.py --all-models)。

    BT実行後のモデルディレクトリ (data/models-backtest/) に保存された最新モデルを使用。

    実行コマンド:
    python scripts/analyze_feature_importance.py --all-models --model-dir data/models-backtest --format both

    このスクリプトは以下を生成:
    - feature_importance_report.json (全モデルのpermutation+gain重要度)
    - feature_importance_report.csv (CSV形式)

    確認事項:
    1. Phase 26-27で追加した新特徴量 (血統、タイム指数、相対比較、mining、交互作用、TE) がimportance reportに含まれていること
    2. Tier 1 (高重要度) 特徴量に新特徴量が含まれているか確認
    3. Tier 2 (低/ゼロ重要度) 特徴量の数を確認

    所要時間: ~10-15分
  </action>
  <verify>
    <automated>python -c "import json; r=json.load(open('feature_importance_report.json')); print(f'Models in report: {len(r)}'); [print(f'  {k}: {len(v.get(\"features\",[]))} features') for k,v in r.items() if isinstance(v, dict)]" 2>/dev/null || echo "Report format differs - check manually"</automated>
  </verify>
  <done>
    - `feature_importance_report.json` が生成されている
    - 全モデルのpermutation+gain重要度が計算されている
    - 新特徴量 (Phase 26-27) がレポートに含まれていることを目視確認
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: BT結果 + Feature importance確認</name>
  <files></files>
  <action>
    ユーザーにBT結果とFeature importanceの確認を依頼する。

    ROI結果を表示:
    python -c "import json; r=json.load(open('data/backtest/multi_year_result.json')); o=r['overall']; print(f'Overall ROI: {o[\"roi\"]*100:.1f}%'); [print(f'  {y}: {v[\"roi\"]*100:.1f}%') for y,v in r['years'].items()]"

    Feature importanceレポートの新特徴量を確認:
    - feature_importance_report.json 内の te_*, rel_*, dam_*, sire_*, record_*, mining_* の有無

    ROI改善幅の確認:
    - v1.5ベースライン: 84.4%
    - D-05フォーマット: "v1.5: 84.4% -> v1.6: XX% (+Y.Ypp)"
  </action>
  <verify>
    <automated>echo "Waiting for human approval"</automated>
  </verify>
  <done>ユーザーが "approved" を入力し、Task 3 (ROADMAP更新) に進む準備ができている</done>
  <what-built>マルチ年度バックテスト (2023/2024/2025) + Feature importance再計算</what-built>
  <how-to-verify>
    1. ROI結果確認:
       python -c "import json; r=json.load(open('data/backtest/multi_year_result.json')); o=r['overall']; print(f'Overall ROI: {o[\"roi\"]*100:.1f}%'); [print(f'  {y}: {v[\"roi\"]*100:.1f}%') for y,v in r['years'].items()]"
    2. Feature importanceレポート確認:
       - feature_importance_report.json を開き、新特徴量 (te_*, rel_*, dam_*, sire_*, record_*, mining_*) が含まれているか
    3. ROI改善幅の確認:
       - v1.5ベースライン: 84.4%
       - v1.6結果: 上記コマンド出力
       - D-05フォーマット: "v1.5: 84.4% -> v1.6: XX% (+Y.Ypp)"
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with ROADMAP.md update, or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 3: ROADMAP.md + ドキュメント更新</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (現在のPhase 28セクション)
    - data/backtest/multi_year_result.json (ROI結果)
  </read_first>
  <action>
    ROADMAP.mdのPhase 28セクションを完了状態に更新する。

    更新内容:
    1. Phase 28ステータスを "Not started" -> "Complete" に変更
    2. Phase 28のPlans行を更新:
       Plans:
       - [x] 28-01-PLAN.md -- pytest回帰確認 + 特徴量凍結manifest生成
       - [x] 28-02-PLAN.md -- マルチ年度BT + Feature importance + ドキュメント更新
    3. Plans数を "2 plans" に更新
    4. v1.6マイルストーン全体の完了記録を追加:
       - "**Result:** ROI v1.5: 84.4% -> v1.6: XX% (+Y.Ypp)" (D-05フォーマット、実際のROI値を埋める)
       - 100%未達の場合は D-05/D-06 により改善幅ベースで記録
    5. Progress テーブルの Phase 28 行を更新: completed日付を今日の日付に
    6. v1.6 milestone status を "Shipped 2026-05-15" (または実際完了日) に更新

    ROI値は data/backtest/multi_year_result.json の overall.roi から取得。例: roi=0.923 の場合 "92.3%"。
  </action>
  <verify>
    <automated>grep -c "Phase 28" .planning/ROADMAP.md</automated>
  </verify>
  <done>
    - ROADMAP.mdのPhase 28が完了状態で記録されている
    - v1.6マイルストーンのROI結果がD-05フォーマットで記録されている
    - Phase 28の2プランがチェック済み
    - Progress テーブルが更新されている
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BT実行 -> PostgreSQL | BacktestEngineがParquetStore経由でデータ読み取り。信頼境界内 (ローカルDB) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03 | Tampering | data/backtest/multi_year_result.json | accept | BT出力の自動生成ファイル。git管理下で改ざん検知可能 |
| T-28-04 | Elevation | run_backtest.py (長時間実行) | accept | ~3時間の計算処理。リソース消費は想定内 |
| T-28-05 | Information | strategy_manifest不存在 | accept | D-02で既存manifest使用を決定。不存在時はデフォルトパラメータでBT実行 (blockerではない) |
</threat_model>

<verification>
1. `data/backtest/multi_year_result.json` が存在し、overall + years キーを含む
2. `feature_importance_report.json` が存在する
3. ROADMAP.mdにPhase 28完了が記録されている
4. ROI結果がD-05フォーマット ("v1.5: 84.4% -> v1.6: XX%") で記録されている
</verification>

<success_criteria>
- マルチ年度BT ROIが算出されている (年度別 + 全体)
- Feature importanceレポートが生成されている
- ROADMAP.mdが更新され、v1.6マイルストーン完了が記録されている
- ROI改善幅が明記されている (D-05フォーマット)
</success_criteria>

<output>
Create `.planning/phases/28-validation-freeze/28-02-SUMMARY.md` when done
