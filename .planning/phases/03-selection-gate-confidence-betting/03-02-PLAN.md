---
phase: 03-selection-gate-confidence-betting
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/models/regime_detector.py
  - src/betting/meta_switcher.py
  - src/betting/gate_keeper.py
  - src/betting/win_strategy.py
autonomous: true
requirements: [BETT-01]

must_haves:
  truths:
    - "RegimeDetectorのedge_thresholdがJRA控除率25%を考慮して引き上げられている"
    - "MetaSwitcherのedge_thresholdがRegimeDetectorと同期して引き上げられている"
    - "GateKeeperのデフォルトedge閾値が0.03から0.04に引き上げられている"
    - "RegimeDetectorとMetaSwitcherの閾値差(+0.01~+0.02)が維持されている"
  artifacts:
    - path: "src/models/regime_detector.py"
      provides: "レジーム別edge_threshold"
      contains: "edge_threshold"
    - path: "src/betting/meta_switcher.py"
      provides: "ベッティング戦略パラメータ"
      contains: "edge_threshold"
    - path: "src/betting/gate_keeper.py"
      provides: "最終ベット足切り"
      contains: "edge_threshold"
  key_links:
    - from: "src/models/regime_detector.py"
      to: "edge_threshold values"
      via: "get_strategy_params()"
      pattern: "edge_threshold.*0\\.0[5-9]"
    - from: "src/betting/meta_switcher.py"
      to: "edge_threshold values"
      via: "_default_params()"
      pattern: "edge_threshold.*0\\.0[5-9]|0\\.1"
    - from: "src/betting/gate_keeper.py"
      to: "default parameter"
      via: "filter_bets()"
      pattern: "edge_threshold.*0\\.04"
---

<objective>
JRA控除率25%を考慮してedge_thresholdを引き上げ、ベットフィルタリングの安全性マージンを確保する。

Purpose: 控除率を上回るエッジのみベット対象とすることでROI陽性を担保する (BETT-01)
Output: RegimeDetector/MetaSwitcher/GateKeeperのedge_threshold更新
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-selection-gate-confidence-betting/03-CONTEXT.md
@.planning/phases/03-selection-gate-confidence-betting/03-RESEARCH.md
@.planning/phases/03-selection-gate-confidence-betting/03-01-SUMMARY.md

<interfaces>
<!-- Current threshold values that executor MUST update -->

From src/models/regime_detector.py (lines 183, 211, 224):
```python
# AGGRESSIVE:
"edge_threshold": 0.04,  # v5: 0.05->0.04 ベット数確保
# CONSERVATIVE:
"edge_threshold": 0.05,  # 5% edge — more selective
# COLLAPSED:
"edge_threshold": 0.08,  # 8% edge — near halt
```

From src/betting/meta_switcher.py (lines 47, 55, 63):
```python
# AGGRESSIVE:
"edge_threshold": 0.04,  # 4% edge (raised from 3%)
# CONSERVATIVE:
"edge_threshold": 0.06,  # 6% edge (raised from 5%)
# COLLAPSED:
"edge_threshold": 0.09,  # 9% edge (raised from 8%)
```

From src/betting/gate_keeper.py (lines 28, 30):
```python
return bet.edge >= 0.03  # デフォルト閾値
def filter_bets(self, bets: list[Bet], edge_threshold: float = 0.03) -> list[Bet]:
```

From src/betting/win_strategy.py (lines 70-77):
```python
def _calc_stake(self, ev_lower: float, odds: float, bankroll: float) -> float:
    if bankroll <= 0 or odds <= 1.0:
        return 0.0
    edge = ev_lower - 1.0
    kelly = min(edge / (odds - 1.0), self.KELLY_FRACTION_CAP)
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RegimeDetector + MetaSwitcher edge_threshold更新</name>
  <files>src/models/regime_detector.py, src/betting/meta_switcher.py</files>
  <read_first>
    src/models/regime_detector.py (lines 178-232)
    src/betting/meta_switcher.py (lines 35-72)
  </read_first>
  <action>
**src/models/regime_detector.py — edge_threshold更新 (D-10)**

`get_strategy_params()` メソッド内の3つのレジーム定義を更新する。

AGGRESSIVE (line 183): `"edge_threshold": 0.04` -> `"edge_threshold": 0.05`
コメントも更新: `# v5: 0.05->0.04 ベット数確保` -> `# Phase 3: JRA控除率25%考慮 +0.01`

CONSERVATIVE (line 211): `"edge_threshold": 0.05` -> `"edge_threshold": 0.06`
コメントも更新: `# 5% edge — more selective` -> `# 6% edge — JRA控除率考慮 (Phase 3)`

COLLAPSED (line 224): `"edge_threshold": 0.08` -> `"edge_threshold": 0.09`
コメントも更新: `# 8% edge — near halt` -> `# 9% edge — JRA控除率考慮 (Phase 3)`

**src/betting/meta_switcher.py — edge_threshold更新 (D-10)**

`_default_params()` static method内の3つのレジーム定義を更新する。

AGGRESSIVE (line 47): `"edge_threshold": 0.04` -> `"edge_threshold": 0.05`
コメントも更新: `# 4% edge (raised from 3%)` -> `# 5% edge — JRA控除率25%考慮 (Phase 3)`

CONSERVATIVE (line 55): `"edge_threshold": 0.06` -> `"edge_threshold": 0.07`
コメントも更新: `# 6% edge (raised from 5%)` -> `# 7% edge — JRA控除率25%考慮 (Phase 3)`

COLLAPSED (line 63): `"edge_threshold": 0.09` -> `"edge_threshold": 0.10`
コメントも更新: `# 9% edge (raised from 8%)` -> `# 10% edge — JRA控除率25%考慮 (Phase 3)`

更新後の閾値対応表:
- RegimeDetector: AGGRESSIVE=0.05, CONSERVATIVE=0.06, COLLAPSED=0.09
- MetaSwitcher:   AGGRESSIVE=0.05, CONSERVATIVE=0.07, COLLAPSED=0.10
- MetaSwitcherのCONSERVATIVEとCOLLAPSEDが+0.01高い関係を維持
  </action>
  <verify>
    <automated>cd C:/Users/hirom/develop/keiba-ai && python -m pytest tests/ -v -k "regime or meta_switcher or betting" 2>&1 | head -40; echo "---"; grep -n "edge_threshold" src/models/regime_detector.py; echo "---"; grep -n "edge_threshold" src/betting/meta_switcher.py</automated>
  </verify>
  <done>
    - RegimeDetector AGGRESSIVE edge_threshold == 0.05
    - RegimeDetector CONSERVATIVE edge_threshold == 0.06
    - RegimeDetector COLLAPSED edge_threshold == 0.09
    - MetaSwitcher AGGRESSIVE edge_threshold == 0.05
    - MetaSwitcher CONSERVATIVE edge_threshold == 0.07
    - MetaSwitcher COLLAPSED edge_threshold == 0.10
    - `grep "edge_threshold.*0\\.04" src/models/regime_detector.py` が空 (旧値なし)
    - `grep "edge_threshold.*0\\.04" src/betting/meta_switcher.py` が空 (旧値なし)
    - `grep "edge_threshold.*0\\.09" src/betting/meta_switcher.py` が空 (旧値なし)
  </done>
</task>

<task type="auto">
  <name>Task 2: GateKeeper デフォルト閾値更新 + WinStrategy確認</name>
  <files>src/betting/gate_keeper.py</files>
  <read_first>
    src/betting/gate_keeper.py (全文42行)
    src/betting/win_strategy.py (全文, Kelly計算部分)
  </read_first>
  <action>
**src/betting/gate_keeper.py — デフォルト閾値更新**

1. `should_bet()` メソッド (line 28): `return bet.edge >= 0.03` -> `return bet.edge >= 0.04`
   コメント更新: `# edge >= 0.03 デフォルト閾値で判定` -> `# edge >= 0.04 デフォルト閾値 (Phase 3: JRA控除率考慮)`

2. `filter_bets()` メソッド (line 30): `edge_threshold: float = 0.03` -> `edge_threshold: float = 0.04`

**src/betting/win_strategy.py — 変更不要 (D-11)**

既存の簡易Kelly計算は維持する:
- `kelly = min(edge / (odds - 1.0), self.KELLY_FRACTION_CAP)` (cap=25%)
- WinSelectionGateがベット可否を判定し、Kellyは賭け金計算のみ担当
- 変更不要だが、read_firstで現在の実装がD-11要件を満たしていることを確認する
  </action>
  <verify>
    <automated>cd C:/Users/hirom/develop/keiba-ai && python -m pytest tests/ -v -k "gate_keeper" 2>&1 | head -20; echo "---"; grep -n "0.03\|0.04" src/betting/gate_keeper.py; echo "---"; grep -c "KELLY_FRACTION_CAP" src/betting/win_strategy.py</automated>
  </verify>
  <done>
    - `grep "0\\.03" src/betting/gate_keeper.py` が空 (旧値なし)
    - `grep "0\\.04" src/betting/gate_keeper.py` で should_bet と filter_bets の2箇所にヒット
    - `grep -c "KELLY_FRACTION_CAP" src/betting/win_strategy.py` >= 1 (Kelly計算が維持されている)
    - 全既存テストPASS (閾値変更によるテスト調整が必要な場合はテスト側のデフォルト値も更新)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edge_threshold config -> Bet decision | 閾値がベット可否に直結。低すぎると赤字ベット、高すぎるとベット数激減 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | D (Denial of Service) | edge_threshold引き上げすぎ | mitigate | +0.01の微小引き上げに留める。バックテストでベット数>0を確認(Phase 4) |
| T-03-07 | S (Spoofing) | GateKeeperデフォルト値 | accept | デフォルト値はセーフティネット。実行時はRegimeDetector/MetaSwitcherから動的値を取得 |
</threat_model>

<verification>
1. `grep "edge_threshold" src/models/regime_detector.py | grep -v "^#"` で3つのレジーム閾値が0.05/0.06/0.09になっている
2. `grep "edge_threshold" src/betting/meta_switcher.py | grep -v "^#"` で3つのレジーム閾値が0.05/0.07/0.10になっている
3. `grep "0\\.04" src/betting/gate_keeper.py` でshould_betとfilter_betsの2箇所にヒット
4. `grep "0\\.03" src/betting/gate_keeper.py` が空
5. `python -m pytest tests/ -v` で全テストPASS
</verification>

<success_criteria>
- RegimeDetectorの3レジームedge_thresholdが全て+0.01引き上げられている
- MetaSwitcherの3レジームedge_thresholdが全て+0.01引き上げられている
- RegimeDetectorとMetaSwitcherの閾値差(AGGRESSIVE=同値、CONSERVATIVE=+0.01、COLLAPSED=+0.01)が維持されている
- GateKeeperのデフォルト閾値が0.04に更新されている
- 既存Kelly計算(edge/(odds-1), cap=25%)が変更されていない(D-11)
- 全テストPASS
</success_criteria>

<output>
After completion, create `.planning/phases/03-selection-gate-confidence-betting/03-02-SUMMARY.md`
</output>
