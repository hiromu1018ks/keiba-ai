---
phase: 05-foundation-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/horse_history_features.py
  - src/features/pace_aptitude_features.py
  - src/features/interaction_features.py
  - src/models/stage1_ability_model.py
  - src/models/two_stage_return_model.py
  - tests/test_horse_history_features.py
  - tests/test_pace_aptitude_features.py
  - tests/test_interaction_features.py
autonomous: true
requirements: [TSER-01, TSER-02, TSER-03, PACE-01, PACE-02]

must_haves:
  truths:
    - "harontimel5_avgが全過去走のEMA重み付けで計算され、直近の成績に高い重みが付与されている"
    - "class_adj_formetricが新特徴量として生成され、NaN率50%未満である"
    - "haron_zscore_trendが過去走z-scoreの線形回帰傾きとして計算される"
    - "pace_corner_stability, pace_closing_power, pace_position_consistencyの3サブ特徴量がPaceAptitudeFeaturesに追加される"
    - "actual_pace_fitが実績ベースのペース適性としてinteraction_featuresに追加される"
  artifacts:
    - path: "src/features/horse_history_features.py"
      provides: "EMA版harontimel5_avg, class_adj_formetric, haron_zscore_trend"
      contains: "class_adj_formetric"
    - path: "src/features/pace_aptitude_features.py"
      provides: "3つのペースフィグアサブ特徴量"
      contains: "pace_corner_stability"
    - path: "src/features/interaction_features.py"
      provides: "actual_pace_fit"
      contains: "actual_pace_fit"
    - path: "src/models/stage1_ability_model.py"
      provides: "新特徴量を含むFEATURE_COLS"
      contains: "class_adj_formetric"
    - path: "src/models/two_stage_return_model.py"
      provides: "新特徴量を含むFEATURE_COLS"
      contains: "class_adj_formetric"
  key_links:
    - from: "src/features/horse_history_features.py"
      to: "HorseHistoryFeatures.BASE_COLS"
      via: "class_adj_formetric, haron_zscore_trendのBASE_COLS追加"
      pattern: "class_adj_formetric.*haron_zscore_trend"
    - from: "src/features/pace_aptitude_features.py"
      to: "PaceAptitudeFeatures.compute_batch()"
      via: "result_colsリストへの新列名追加"
      pattern: "pace_corner_stability.*pace_closing_power.*pace_position_consistency"
    - from: "src/features/interaction_features.py"
      to: "_add_pace_projection_features()"
      via: "actual_pace_fit生成の追加"
      pattern: "actual_pace_fit"
    - from: "src/models/stage1_ability_model.py"
      to: "AbilityModel.FEATURE_COLS"
      via: "新特徴量のFEATURE_COLSへの追加"
      pattern: "class_adj_formetric|actual_pace_fit|pace_corner_stability"
---

<objective>
時系列特徴量(TSER-01~03)とペース特徴量(PACE-01~02)を実装する。

Purpose: 過去走のEMA重み付け・クラス調整フォーメトリック・z-score改善トラジェクトリ・ペースフィグア・実績ペース適性を追加し、後続Phase 6-7のモデル改善がより豊かな入力から恩恵を受けられるようにする。
Output: horse_history_features.py, pace_aptitude_features.py, interaction_features.py の拡張 + モデルFEATURE_COLS更新 + テスト
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-foundation-features/05-CONTEXT.md
@.planning/phases/05-foundation-features/05-RESEARCH.md

<interfaces>
<!-- Executor needs these contracts from the codebase -->

From src/features/horse_history_features.py:
```python
class HorseHistoryFeatures:
    BASE_COLS: list[str] = [...]  # line 265 — 新列名を追加する
    def compute(self, race_df, entry_df, target_race_ids) -> pd.DataFrame  # line ~310
    @staticmethod
    def add_race_transforms(df) -> pd.DataFrame  # line 1133

# Key arrays in compute_batch loop (horse_arrs dict):
# horse_arrs["harontimel3"] — 過去走ハロンタイム
# horse_arrs["gradecd"] — グレードコード (A/B/C/D/E or empty)
# horse_arrs["jyokencd1"] — 条件コード (数値)
# horse_arrs["kakuteijyuni"] — 確定着順
# horse_arrs["syussotosu"] — 出走頭数
# horse_arrs["distance_bin"] — 距離ビン
# horse_arrs["valid_mask"] — 有効レースマスク

def _class_level_from_values(grade_code, jyoken_code) -> float  # line 56
_CLASS_LEVEL_MAP: dict[str, float]  # line 42: {"A": 8.0, "B": 7.0, ...}
```

From src/features/pace_aptitude_features.py:
```python
class PaceAptitudeFeatures:
    def compute_batch(self, df) -> pd.DataFrame  # line 68
    # Returns DataFrame with columns: kettonum, race_id, pace_aptitude, front_pace_wr, closing_pace_wr
    # hist has: norm_finish, norm_1c, jyuni1c, jyuni4c columns available
```

From src/features/interaction_features.py:
```python
def _add_pace_projection_features(df: pd.DataFrame) -> None:  # line 87
    # Adds pace_pressure, closer_share, pace_scenario_fit to df
    # df already has: kyakusitukubun_cd, front_pace_wr, closing_pace_wr (from pace_df merge)
```

From src/models/stage1_ability_model.py:
```python
class AbilityModel:
    FEATURE_COLS: list[str] = [...]  # line 28 — 新列を追加する
```

From src/models/two_stage_return_model.py:
```python
class WinTwoStageModel:
    FEATURE_COLS: list[str] = [...]  # line 47 — 新列を追加する
```

From src/features/form_cycle_features.py:
```python
def compute_form_features(kakuteijyuni, syussotosu) -> tuple[float, float, float]:
    # Uses np.polyfit(x, norm, 1)[0] for linear regression slope
    # TSER-03と同じパターン
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TSER-01~03 — horse_history_features.py にEMA・クラス調整フォーメトリック・z-score改善トラジェクトリを追加</name>
  <files>src/features/horse_history_features.py, tests/test_horse_history_features.py, src/models/stage1_ability_model.py, src/models/two_stage_return_model.py</files>
  <read_first>
    src/features/horse_history_features.py (全体、特にlines 265-297 BASE_COLS, lines 660-748 harontimel5_avg/zscore計算, lines 1094-1128 results.append, lines 1133-1157 add_race_transforms)
    src/features/form_cycle_features.py (np.polyfitパターン参照)
    src/models/stage1_ability_model.py (FEATURE_COLS)
    src/models/two_stage_return_model.py (FEATURE_COLS)
    tests/test_horse_history_features.py (既存テストパターン)
  </read_first>
  <behavior>
    - Test 1: harontimel5_avgがEMA重み付けで計算されること (halflife=3, 直近値に高い重み)
    - Test 2: class_adj_formetricが高クラス好走を高く評価すること
    - Test 3: haron_zscore_trendが有効z-score 3走以上の場合のみ計算されること
    - Test 4: class_adj_formetricがNaNのclass_levelを適切にフィルタすること
  </behavior>
  <action>
    **TSER-01: harontimel5_avg を EMA 重み付けに置き換え (D-01, D-02, D-03)**

    `horse_history_features.py` lines 673-683 の `harontimel5_avg` 計算を以下のように変更:
    1. `ht_valid[-self._n_past:]` の単純平均を、全過去走のEMA重み付けに置き換える
    2. halflife=3, decay = np.log(2)/3 ≈ 0.231
    3. 重み配列: `weights = (1 - decay) ** np.arange(n)` で n=len(ht_valid)
    4. `ht_valid` は古い→新しい順でソート済み。重みインデックス0=最新の重み(1.0)にするため `weights = weights[::-1]` してから正規化
    5. `harontimel5_avg = float(np.sum(ht_valid * weights) / weights.sum())`
    6. ルックバックウィンドウを5走から全過去走に拡張: `ht_valid` 全体を使用 (D-03)
    7. 列名 `harontimel5_avg` はそのまま変更しない (D-01)

    **TSER-02: class_adj_formetric を新規追加 (D-04, D-05, D-06)**

    `compute_batch()` ループ内 (harontimel5_zscore 計算の直後あたり) に追加:
    1. BASE_COLS (line 265) に `"class_adj_formetric"` を追加
    2. 計算式: `class_adj_formetric = Σ(norm_finish_i × class_level_i) / Σ(class_level_i)` (D-05)
    3. 各過去走の class_level を `_class_level_from_values(gradecd_i, jyokencd1_i)` で取得 (D-06)
    4. `gradecd` と `jyokencd1` は既に `horse_arrs` 内に存在 (cols_horse lines 456-457)
    5. norm_finish_i = (kakuteijyuni - 1) / (syussotosu - 1), syussotosu > 1でクリップ
    6. NaNフィルタ: `valid = ~np.isnan(class_levels) & ~np.isnan(norm_finish)` で除外
    7. 有効データ0件時は NaN (LightGBMネイティブ処理、D-23)
    8. results.append (line 1094) に `"class_adj_formetric": class_adj_formetric` を追加

    **TSER-03: haron_zscore_trend を新規追加 (D-07, D-08, D-09)**

    `compute_batch()` ループ内 (harontimel5_zscore 計算の直後) に追加:
    1. BASE_COLS に `"haron_zscore_trend"` を追加
    2. 既存の `harontimel5_zscore` 計算 (lines 686-735) で既に `zscores` リストを構築済み
    3. その `zscores` リストから `z_arr = np.array(zscores)` を作成
    4. 有効z-scoreを抽出: `valid_z = z_arr[~np.isnan(z_arr)]`
    5. 最低3走以上の有効z-scoreが必要 (D-09): `if len(valid_z) >= 3`
    6. `haron_zscore_trend = float(np.polyfit(np.arange(len(valid_z), dtype=float), valid_z, 1)[0])`
    7. 不足時は NaN
    8. results.append に `"haron_zscore_trend": haron_zscore_trend` を追加
    9. 注意: zscores リストが既にスコープ内にあるので、harontimel5_zscore 計算ブロック内の `else: harontimel5_zscore = float("nan")` の後に `haron_zscore_trend = float("nan")` を設定する必要がある。zscores が計算されなかった全てのパスで NaN を設定する。

    **モデル FEATURE_COLS 更新:**

    `stage1_ability_model.py` の `AbilityModel.FEATURE_COLS` に以下を追加:
    - `"class_adj_formetric"` — ペース適性グループの後あたり
    - `"haron_zscore_trend"` — harontimel5_zscore の後あたり

    `two_stage_return_model.py` の `WinTwoStageModel.FEATURE_COLS` に以下を追加:
    - `"class_adj_formetric"` — pace_scenario_fit の後あたり
    - `"haron_zscore_trend"` — freshness_score の後あたり

    **テスト更新:**

    `tests/test_horse_history_features.py` に追加:
    - EMAテスト: 5走のハロンタイム [35.0, 34.5, 34.0, 33.5, 33.0] に対し、EMA平均が直近値に近いことを検証 (単純平均34.0より小さい=速いタイム重視)
    - class_adj_formetricテスト: 高クラス(A=8.0)での1着と低クラス(数値)での1着を比較し、高クラス好走が低い(良い)値になることを検証
    - haron_zscore_trendテスト: 改善傾向 (z-scoreが減少) の場合に負の値、悪化傾向で正の値を検証
    - データ不足テスト: z-score 2走以下の場合に NaN になることを検証

    既存テストで `harontimel5_avg` の数値をアサートしている場合は、EMA版の期待値に更新する。
  </action>
  <verify>
    <automated>cd C:/Users/hirom/develop/keiba-ai && python -m pytest tests/test_horse_history_features.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
    - harontimel5_avg が EMA重み付け (halflife=3) で計算される
    - class_adj_formetric が BASE_COLS に含まれ、results.append に出力される
    - haron_zscore_trend が BASE_COLS に含まれ、results.append に出力される
    - AbilityModel.FEATURE_COLS に class_adj_formetric, haron_zscore_trend が含まれる
    - WinTwoStageModel.FEATURE_COLS に class_adj_formetric, haron_zscore_trend が含まれる
    - 全テストが通過する
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PACE-01~02 — ペースフィグアサブ特徴量と実績ペース適性を追加</name>
  <files>src/features/pace_aptitude_features.py, src/features/interaction_features.py, tests/test_pace_aptitude_features.py, tests/test_interaction_features.py</files>
  <read_first>
    src/features/pace_aptitude_features.py (全体、特にlines 68-250 compute_batch)
    src/features/interaction_features.py (全体、特にlines 87-108 _add_pace_projection_features)
    tests/test_pace_aptitude_features.py (既存テストパターン)
    tests/test_interaction_features.py (既存テストパターン)
  </read_first>
  <behavior>
    - Test 1: pace_corner_stability が1C→4C位置変位の一貫性を計算すること
    - Test 2: pace_closing_power がharontimel3の相対値を計算すること
    - Test 3: pace_position_consistency が正規化着順のばらつきを計算すること
    - Test 4: actual_pace_fit がfront_runnerにはfront_pace_wr、closerにはclosing_pace_wrを割り当てること
    - Test 5: actual_pace_fit が脚質不明時にNaNになること
  </behavior>
  <action>
    **PACE-01: ペースフィグアサブ特徴量3つを PaceAptitudeFeatures に追加 (D-10, D-11, D-12)**

    `pace_aptitude_features.py` の `compute_batch()` メソッドを拡張:

    1. `result_cols` (line 76) に `"pace_corner_stability"`, `"pace_closing_power"`, `"pace_position_consistency"` を追加
    2. hist の正規化列 (line 138-143) に以下を追加:
       - `hist["norm_4c"] = hist["jyuni4c"].astype(float) / hist["syussotosu"].astype(float)`
    3. 新しいnumpy配列を追加 (lines 155-168 の配列定義エリア):
       - `h_norm_4c = hist["norm_4c"].values`
       - `h_jyuni1c = hist["jyuni1c"].astype(float).values` — 既に存在する場合はそれを使用
    4. `results` dict (line 197-201) に3つの新キーを追加: `np.full(len(targets), np.nan)`
    5. 各馬ループ内 (lines 204-245) で、cutoff位置c >= 2の場合に追加計算:
       ```python
       # pace_corner_stability: 1C→4Cの位置変位の標準偏差 (低い=一貫性高い)
       if c >= 2:
           corner_1c = h_norm_1c[base + start : base + c]
           corner_4c = h_norm_4c[base + start : base + c]
           disp = corner_4c - corner_1c
           results["pace_corner_stability"][ti] = float(np.std(disp))

       # pace_closing_power: harontimel3の相対的位置
       # 注意: histにharontimel3列が含まれている場合のみ計算
       # harontimel3は entries_hist に含まれる可能性があるが、
       # pace_aptitudeのmerge対象には含まれていない。
       # よって、past_dfにharontimel3を含める必要がある。
       # entries_for_mergeにharontimel3が含まれるか確認し、
       # 含まれる場合はh_harontimel3配列を使用。
       # NaN率が高い場合はNaNのまま (D-23)
       if "harontimel3" in hist.columns and c >= 2:
           h_harontimel3 = hist["harontimel3"].astype(float).values
           ht_past = h_harontimel3[base + start : base + c]
           ht_valid_pace = ht_past[~np.isnan(ht_past)]
           if len(ht_valid_pace) >= 2:
               # harontimel3の平均を計算し、低い=速い上がり
               # pace_closing_power = -mean(harontimel3) (低いほど良いので負符号)
               results["pace_closing_power"][ti] = float(-np.mean(ht_valid_pace))

       # pace_position_consistency: 正規化着順の標準偏差
       if c >= 2:
           pos_finish = h_norm_finish[base + start : base + c]
           results["pace_position_consistency"][ti] = float(np.std(pos_finish))
       ```
    6. 注意: harontimel3をpast_dfに含めるため、entries_for_mergeでharontimel3をdropしないようにする。現在の `entries_for_merge = entries_hist.drop(columns=["syussotosu"], errors="ignore")` (line 96) はharontimel3を残しているので、既に含まれているはず。確認が必要。
    7. `_add_pace_projection_features` が pace_aptitude_features の結果を使って actual_pace_fit を計算するため、pace_df のマージ結果に3つの新列が含まれる。training_pipeline.py の pace_df マージ部 (lines 308-323) を更新して、新列もマージに含める必要がある。ただし、D-24で「新ステップ追加は不要」とあり、compute_batchがkettonum + race_idキーでDataFrameを返すため、既存のマージパターンで自動的に含まれる。training_pipeline側のマージ列リスト更新は次のTaskで対応。

    **PACE-02: actual_pace_fit を interaction_features.py に追加 (D-13, D-14, D-15)**

    `interaction_features.py` の `_add_pace_projection_features()` (line 87) の末尾に追加:

    1. `df["pace_scenario_fit"]` の計算 (line 107) の後に追加
    2. actual_pace_fit は実績ベースの front_pace_wr/closing_pace_wr を使用 (D-14)
    3. 脚質に基づいて適性値を選択:
       ```python
       # actual_pace_fit: 実績ベースのペース適性 (D-13)
       # front_runner (脚質1=逃げ, 2=先行) → front_pace_wr
       # closer (脚質3=差し, 4=追込) → closing_pace_wr
       if "front_pace_wr" in df.columns and "closing_pace_wr" in df.columns:
           is_front_runner = style.isin([1, 2])
           is_closer = style.isin([3, 4])
           df["actual_pace_fit"] = np.where(
               is_front_runner, df["front_pace_wr"],
               np.where(is_closer, df["closing_pace_wr"], np.nan)
           )
       ```
    4. front_pace_wr/closing_pace_wr は training_pipeline で pace_df マージにより df に既に追加済み。interaction_features は HorseHistoryFeatures の後に呼ばれるため利用可能。

    **training_pipeline.py の pace_df マージ列更新:**

    `training_pipeline.py` lines 308-318 の pace_df マージで、新列もマージに含める:
    ```python
    pace_merge_cols = ["kettonum", "race_id", "pace_aptitude", "front_pace_wr", "closing_pace_wr",
                       "pace_corner_stability", "pace_closing_power", "pace_position_consistency"]
    pace_merge_cols = [c for c in pace_merge_cols if c in pace_df.columns]
    df = df.merge(pace_df[pace_merge_cols], on=["kettonum", "race_id"], how="left")
    ```

    **モデル FEATURE_COLS 更新 (Task 1で追加済みの場合はスキップ、そうでなければ追加):**

    `stage1_ability_model.py`:
    - `"pace_corner_statility"` をペース適性グループに追加
    - `"pace_closing_power"` をペース適性グループに追加
    - `"pace_position_consistency"` をペース適性グループに追加
    - `"actual_pace_fit"` をペース適性グループに追加

    `two_stage_return_model.py`:
    - `"actual_pace_fit"` を pace_scenario_fit の後に追加

    **テスト追加:**

    `tests/test_pace_aptitude_features.py` に追加:
    - compute_batchのモックテストで新列が出力されることを検証
    - 一貫した位置取りの馬 (1C=2, 4C=2 で毎回同じ) で pace_corner_stability ≈ 0
    - バラついた位置取りで pace_corner_stability > 0
    - pace_position_consistency が常に着順1着の馬で ≈ 0

    `tests/test_interaction_features.py` に追加:
    - front_pace_wr と closing_pace_wr があるDataFrameで、脚質1の馬に actual_pace_fit == front_pace_wr
    - 脚質4の馬に actual_pace_fit == closing_pace_wr
    - 脚質NaNの馬に actual_pace_fit が NaN
  </action>
  <verify>
    <automated>cd C:/Users/hirom/develop/keiba-ai && python -m pytest tests/test_pace_aptitude_features.py tests/test_interaction_features.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
    - pace_corner_stability, pace_closing_power, pace_position_consistency が compute_batch の result_cols に含まれる
    - actual_pace_fit が interaction_features.py に追加される
    - AbilityModel.FEATURE_COLS に新ペース特徴量が含まれる
    - WinTwoStageModel.FEATURE_COLS に actual_pace_fit が含まれる
    - 全テストが通過する
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Feature computation → Model training | 特徴量の計算値がモデル入力として使用される。NaN処理の不整合がモデル学習に影響 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | I (Information) | horse_history_features EMA | mitigate | EMA計算時のnumpy配列インデックス方向に注意。古い→新しい順のht_validに対し重みインデックス0が最新(最大重み)になるよう weights[::-1] で反転。反転忘れは情報劣化(古いデータを過大評価)を引き起こす |
| T-05-02 | D (Denial) | class_level NaN | mitigate | _class_level_from_values がNaNを返す場合、class_adj_formetricの分母Σ(class_level)が0になる。valid = ~np.isnan(class_levels) フィルタで除外。フィルタ後の有効データ0件時はNaN (D-23) |
| T-05-03 | I (Information) | zscore trend PIT | accept | zscoresはexpanding_statsベースでPIT safeだが、haron_zscore_trendは過去走内の相対傾向であり、look-aheadリスクは低い |
</threat_model>

<verification>
1. `python -m pytest tests/test_horse_history_features.py tests/test_pace_aptitude_features.py tests/test_interaction_features.py -v` 全テスト通過
2. `python -c "from features.horse_history_features import HorseHistoryFeatures; print('class_adj_formetric' in HorseHistoryFeatures.BASE_COLS)"` → True
3. `python -c "from models.stage1_ability_model import AbilityModel; cols = AbilityModel.FEATURE_COLS; assert 'class_adj_formetric' in cols and 'haron_zscore_trend' in cols and 'pace_corner_stability' in cols and 'actual_pace_fit' in cols"`
4. `ruff check src/features/horse_history_features.py src/features/pace_aptitude_features.py src/features/interaction_features.py src/models/stage1_ability_model.py src/models/two_stage_return_model.py` lint通過
</verification>

<success_criteria>
- harontimel5_avgがEMA重み付け (halflife=3, 全過去走) で計算される
- class_adj_formetricとharon_zscore_trendがBASE_COLSに追加され、NaN時にNaNを出力
- pace_corner_stability, pace_closing_power, pace_position_consistencyがPaceAptitudeFeatures.compute_batch()で計算される
- actual_pace_fitが脚質ベースでfront_pace_wr/closing_pace_wrを選択して生成される
- AbilityModel.FEATURE_COLSとWinTwoStageModel.FEATURE_COLSに全新特徴量が追加される
- 全テスト通過、ruff lint通過
</success_criteria>

<output>
After completion, create `.planning/phases/05-foundation-features/05-01-SUMMARY.md`
</output>
