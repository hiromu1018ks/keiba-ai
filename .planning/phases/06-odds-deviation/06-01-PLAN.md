---
phase: 06-odds-deviation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/odds_deviation_features.py
  - src/models/two_stage_return_model.py
  - src/models/robust_confidence_estimator.py
  - src/models/win_selection_gate.py
  - src/pipelines/training_pipeline.py
  - src/backtest/race_predictor.py
  - tests/test_odds_deviation.py
  - tests/test_robust_confidence_estimator.py
  - tests/test_race_predictor.py
autonomous: true
requirements: [ODDS-01, ODDS-02, ODDS-03]

must_haves:
  truths:
    - "odds_to_ability_ratioに加えてdeviation_rankとdeviation_zscoreがレース内相対特徴量として計算される"
    - "WinTwoStageModelのFEATURE_COLSにdeviation_rankとdeviation_zscoreが含まれる"
    - "TrainingPipelineとRacePredictorの両方でAbilityModel後にcompute_odds_deviation_features()が呼ばれる"
    - "RobustConfidenceEstimatorがpredict_interval()でEV上下区間を80%/90%の2段階信頼水準で計算する"
    - "conformal_confidence_scoreがWinSelectionGateのpair_scoresに統合される"
    - "predict_lower_bound()がpredict_interval()のラッパーとして動作し既存呼び出し元が壊れない"
  artifacts:
    - path: "src/features/odds_deviation_features.py"
      provides: "compute_odds_deviation_features() standalone関数"
      exports: ["compute_odds_deviation_features"]
    - path: "src/models/two_stage_return_model.py"
      provides: "FEATURE_COLSにdeviation_rank, deviation_zscore追加"
      contains: "deviation_rank"
    - path: "src/models/robust_confidence_estimator.py"
      provides: "predict_interval()メソッドとconformal_confidence_score計算"
      exports: ["predict_interval"]
    - path: "tests/test_odds_deviation.py"
      provides: "乖離特徴量とEV区間の単体テスト・数値一貫性テスト"
  key_links:
    - from: "src/pipelines/training_pipeline.py"
      to: "src/features/odds_deviation_features.py"
      via: "import + call after odds_to_ability_ratio計算"
      pattern: "compute_odds_deviation_features"
    - from: "src/backtest/race_predictor.py"
      to: "src/features/odds_deviation_features.py"
      via: "import + call after AbilityModel.add_ability_probs()"
      pattern: "compute_odds_deviation_features"
    - from: "src/models/robust_confidence_estimator.py"
      to: "src/models/win_selection_gate.py"
      via: "conformal_confidence_score列"
      pattern: "conformal_confidence_score"
    - from: "src/models/win_selection_gate.py"
      to: "conformal_confidence_score"
      via: "pair_scoresにconfidence_*ペアを追加"
      pattern: "confidence"
---

<objective>
モデル予測確率と市場オッズの乖離をEV信号としてモデルに組み込み、Conformal予測区間でベット選択の信頼性を最適化する。

Purpose: レース内でのオッズ乖離の相対的位置(順位と標準化値)をStage2特徴量として提供し、同時にEV区間の信頼性を定量化してWinSelectionGateのスコアリングに統合する。これによりROIに直結するエッジ信号の品質が向上する。
Output: deviation特徴量計算モジュール、EV区間拡張、Gate統合、三層テストスイート
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-odds-deviation/06-CONTEXT.md
@.planning/phases/06-odds-deviation/06-RESEARCH.md

<interfaces>
<!-- Executor must use these directly - no codebase exploration needed -->

From src/models/two_stage_return_model.py:
```python
class WinTwoStageModel:
    FEATURE_COLS: list[str] = [
        # ... (47 features) ...
        "odds_to_ability_ratio",  # line 91 - existing ratio
        # ADD AFTER line 97 ("actual_pace_fit"):
        # "deviation_rank",
        # "deviation_zscore",
    ]
```

From src/models/robust_confidence_estimator.py:
```python
class RobustConfidenceEstimator:
    def __init__(self, alpha: float = 0.1, rolling_window: int = 200)
    def calibrate(self, win_df: pd.DataFrame, place_df: pd.DataFrame) -> None
    def predict_lower_bound(self, win_df, place_df) -> tuple[pd.DataFrame, pd.DataFrame]
    # Internal state:
    self._win_cp_quantile: float  # CP quantile from calibration
    self._win_rolling_quantile: float  # Rolling quantile from calibration
    self._win_cp_quantile_by_condition: dict[str, float]  # Per surface/distance_bin
    self._calibrated: bool
```

From src/models/win_selection_gate.py:
```python
class WinSelectionGateModel:
    def __init__(self, *, n_bins=6, prior_weight=24.0, ...)
    # Scoring dimensions (existing 3):
    # prob_edges, edge_edges, odds_edges -> combo_scores: dict[tuple[int,int,int], float]
    # pair_scores: dict[tuple[str, int, int], float]
    # pair keys: ("prob_edge", p, e), ("prob_odds", p, o), ("edge_odds", e, o)
    def _build_score_tables(self, df) -> dict[str, Any]
    def _score_frame_from_tables(self, df, tables) -> pd.Series
    def score(self, df) -> pd.DataFrame
```

From src/pipelines/training_pipeline.py (line ~430-433):
```python
# Existing pattern (must follow exactly):
if "p_market_win_adj" in df_oof.columns and "p_ability_win" in df_oof.columns:
    p_market = df_oof["p_market_win_adj"].clip(lower=1e-6)
    p_ability = df_oof["p_ability_win"].clip(lower=1e-6)
    df_oof["odds_to_ability_ratio"] = (p_market / p_ability).clip(0.1, 10.0)
```

From src/backtest/race_predictor.py (line ~89-98):
```python
# Existing inference chain:
df = submodel.market.predict_and_calc_error(df)    # line 90
df = submodel.stage1.add_ability_probs(df)          # line 96
df = submodel.place_ability.predict(df)             # line 97
df = submodel.win.predict_ev(df)                    # line 98
# INSERT compute_odds_deviation_features() BETWEEN line 97 and 98
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Odds deviation features module + pipeline integration (ODDS-01)</name>
  <files>
    src/features/odds_deviation_features.py,
    src/models/two_stage_return_model.py,
    src/pipelines/training_pipeline.py,
    src/backtest/race_predictor.py,
    tests/test_odds_deviation.py
  </files>
  <read_first>
    src/features/odds_deviation_features.py (new file - does not exist yet),
    src/models/two_stage_return_model.py (lines 47-98 for FEATURE_COLS, lines 146-168 for _prepare_features),
    src/models/market_model.py (lines 82-131 for groupby rank pattern reference),
    src/pipelines/training_pipeline.py (lines 422-438 for odds_to_ability_ratio calculation and integration point),
    src/backtest/race_predictor.py (lines 89-98 for inference chain order),
    tests/test_odds_dynamics_features.py (for test pattern reference)
  </read_first>
  <behavior>
    - Test 1: compute_odds_deviation_features() receives DataFrame with odds_to_ability_ratio and race_id -> returns df with deviation_rank and deviation_zscore columns
    - Test 2: Single-horse race (groupby edge case) -> deviation_rank=1.0, deviation_zscore=NaN (std=0)
    - Test 3: Missing odds_to_ability_ratio column -> both deviation columns are NaN, no exception
    - Test 4: deviation_zscore is clipped to [-5.0, 5.0]
    - Test 5: deviation_rank ascending=False (highest ratio = highest rank = "undervalued")
    - Test 6: Numerical consistency: race with 3 horses, known ratios -> verify exact rank and zscore values
  </behavior>
  <action>
    **Step 1: Create `src/features/odds_deviation_features.py`** per D-02 (standalone function).

    Create the file with a single function `compute_odds_deviation_features(df: pd.DataFrame) -> pd.DataFrame`:

    ```python
    """ODDS-01: モデル予測確率と市場オッズの乖離を特徴量化"""
    from __future__ import annotations

    import numpy as np
    import pandas as pd


    def compute_odds_deviation_features(df: pd.DataFrame) -> pd.DataFrame:
        """odds_to_ability_ratioからレース内相対特徴量を計算。

        入力前提: odds_to_ability_ratio列が既に計算済み(training_pipeline)または
        WinTwoStageModel._prepare_features()で計算される(race_predictor)。

        Args:
            df: race_id, odds_to_ability_ratio列を含むDataFrame

        Returns:
            deviation_rank, deviation_zscore列が追加されたDataFrame
        """
        df = df.copy()

        ratio = df.get("odds_to_ability_ratio")
        if ratio is None:
            df["deviation_rank"] = pd.Series(np.nan, index=df.index, dtype=float)
            df["deviation_zscore"] = pd.Series(np.nan, index=df.index, dtype=float)
            return df

        ratio = pd.to_numeric(ratio, errors="coerce")

        # レース内ランク (descending: ratio大=過小評価=高いrank)
        df["deviation_rank"] = (
            ratio.groupby(df["race_id"])
            .rank(method="first", ascending=False)
            .astype("Float64")
        )

        # レース内z-score標準化
        race_mean = ratio.groupby(df["race_id"]).transform("mean")
        race_std = ratio.groupby(df["race_id"]).transform("std").replace(0, np.nan)
        df["deviation_zscore"] = ((ratio - race_mean) / race_std).clip(-5.0, 5.0)

        return df
    ```

    This follows the MarketModel groupby rank pattern (market_model.py lines 126-131) exactly per D-03.

    **Step 2: Add deviation features to `WinTwoStageModel.FEATURE_COLS`** per D-04.

    In `src/models/two_stage_return_model.py`, add two new entries to `FEATURE_COLS` list after line 97 ("actual_pace_fit"):
    ```python
        # ODDS-01: オッズ乖離レース内相対特徴量
        "deviation_rank",
        "deviation_zscore",
    ```

    **Step 3: Integrate into `TrainingPipeline._train_submodel()`** per D-02.

    In `src/pipelines/training_pipeline.py`, after line 433 (the `df_oof["odds_to_ability_ratio"] = ...` assignment), add:
    ```python
        # ODDS-01: deviation features (after odds_to_ability_ratio computed)
        from features.odds_deviation_features import compute_odds_deviation_features
        df_oof = compute_odds_deviation_features(df_oof)
    ```

    **Step 4: Integrate into `RacePredictor.predict()`** per D-02.

    In `src/backtest/race_predictor.py`, between line 97 (`df = submodel.place_ability.predict(df)`) and line 98 (`df = submodel.win.predict_ev(df)`), add:
    ```python
        # ODDS-01: deviation features (after AbilityModel, before WinTwoStageModel)
        from features.odds_deviation_features import compute_odds_deviation_features
        df = compute_odds_deviation_features(df)
    ```

    Also update `_prepare_features()` in `two_stage_return_model.py` to handle the new features at inference time. In the `_prepare_features()` method (lines 146-168), after the existing `odds_to_ability_ratio` lazy computation block (lines 149-157), add a similar block for deviation features:
    ```python
        # ODDS-01: 推論時にdeviation特徴量が未計算なら計算する
        if (
            "deviation_rank" in self.FEATURE_COLS
            and "deviation_rank" not in df.columns
        ):
            from features.odds_deviation_features import compute_odds_deviation_features
            df = compute_odds_deviation_features(df)
    ```

    This ensures that if `compute_odds_deviation_features()` was called in RacePredictor before `_prepare_features()`, the columns already exist and are used. If not (edge case), the lazy computation kicks in.

    **Step 5: Create test file `tests/test_odds_deviation.py`** per D-10.

    Create tests covering the 6 behavior cases listed above. Use `unittest.mock` (no DB required). Include numerical consistency checks per D-13:
    - Probabilities normalization check (sum=1.0 in race)
    - EV interval ordering (lower < point < upper) -- placeholder for Task 2 integration
    - NaN rate verification

    The test file should have a test class `TestOddsDeviationFeatures` with all 6 tests.
  </action>
  <verify>
    <automated>cd C:/Users/hirom/develop/keiba-ai && python -m pytest tests/test_odds_deviation.py -v</automated>
  </verify>
  <done>
    - src/features/odds_deviation_features.py exists with compute_odds_deviation_features() function
    - WinTwoStageModel.FEATURE_COLS contains "deviation_rank" and "deviation_zscore"
    - TrainingPipeline calls compute_odds_deviation_features() after odds_to_ability_ratio
    - RacePredictor calls compute_odds_deviation_features() after AbilityModel before WinTwoStageModel
    - _prepare_features() has lazy computation fallback for deviation features
    - All 6 tests in test_odds_deviation.py pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Conformal EV interval expansion + WinSelectionGate integration (ODDS-03, ODDS-02)</name>
  <files>
    src/models/robust_confidence_estimator.py,
    src/models/win_selection_gate.py,
    tests/test_robust_confidence_estimator.py,
    tests/test_race_predictor.py
  </files>
  <read_first>
    src/models/robust_confidence_estimator.py (entire file, 149 lines),
    src/models/win_selection_gate.py (lines 239-330 for _build_score_tables, lines 330-370 for _lookup_score, lines 915-940 for score()),
    tests/test_robust_confidence_estimator.py (entire file, existing tests),
    tests/test_race_predictor.py (existing test patterns),
    src/backtest/race_predictor.py (line 147 for predict_lower_bound call site)
  </read_first>
  <behavior>
    - Test 1: predict_interval() returns win_df with EV_lower_win_corrected, EV_upper_win_corrected, and conformal_confidence_score columns
    - Test 2: 80% interval is narrower than 90% interval (lower_80 > lower_90, upper_80 < upper_90)
    - Test 3: lower <= ev_corrected <= upper for all rows
    - Test 4: predict_lower_bound() still works after refactor (backward compat)
    - Test 5: conformal_confidence_score is finite and non-negative
    - Test 6: WinSelectionGate _build_score_tables includes confidence-based pair_scores
    - Test 7: Pipeline integration test: RacePredictor.predict() produces conformal_confidence_score when confidence estimator supports it
  </behavior>
  <action>
    **Step 1: Add `predict_interval()` to RobustConfidenceEstimator** per D-05, D-06, D-07.

    In `src/models/robust_confidence_estimator.py`, add a new method `predict_interval()` after the existing `predict_lower_bound()` method:

    ```python
    def predict_interval(
        self,
        win_df: pd.DataFrame,
        place_df: pd.DataFrame,
        alphas: tuple[float, ...] = (0.1, 0.2),
    ) -> tuple[pd.DataFrame, pd.DataFrame]:
        """EVの信頼区間(上下)を複数水準で推定。min(CP, Rolling_Quantile)を採用 (Rule 4)。

        Args:
            win_df: ev_win_corrected を含むDataFrame
            place_df: ev_place_corrected を含むDataFrame
            alphas: 信頼水準のタプル。0.1=90%区間、0.2=80%区間

        Returns:
            win_df, place_df with EV_lower/upper columns and conformal_confidence_score
        """
        if not self._calibrated:
            logger.warning("RobustConfidenceEstimator not calibrated, using EV as bounds")
            win_df = win_df.copy()
            place_df = place_df.copy()
            win_df["EV_lower_win_corrected"] = win_df.get("ev_win_corrected", 0.0)
            win_df["EV_upper_win_corrected"] = win_df.get("ev_win_corrected", 0.0)
            win_df["conformal_confidence_score"] = 0.0
            place_df["EV_lower_place"] = place_df.get("ev_place_corrected", 0.0)
            place_df["EV_upper_place"] = place_df.get("ev_place_corrected", 0.0)
            return win_df, place_df

        win_df = win_df.copy()
        place_df = place_df.copy()
        win_ev = pd.to_numeric(win_df["ev_win_corrected"], errors="coerce").fillna(0.0)

        # Race-condition-dependent CP quantile (same as predict_lower_bound)
        cp_quantile = self._win_cp_quantile
        if self._win_cp_quantile_by_condition:
            cond_keys = (
                win_df["surface"].astype(str) + "_" + win_df["distance_bin"].astype(str)
                if "surface" in win_df.columns and "distance_bin" in win_df.columns
                else pd.Series("", index=win_df.index)
            )
            cond_quantiles = cond_keys.map(self._win_cp_quantile_by_condition)
            cp_quantile_per_row = cond_quantiles.fillna(self._win_cp_quantile)
        else:
            cp_quantile_per_row = pd.Series(self._win_cp_quantile, index=win_df.index)

        # Use first alpha for primary lower/upper bounds
        primary_alpha = alphas[0]

        # Recalculate CP quantile for primary alpha if different from self.alpha
        if abs(primary_alpha - self.alpha) < 1e-9:
            primary_cp_quantile = cp_quantile_per_row
            primary_rolling_quantile = self._win_rolling_quantile
        else:
            # Reuse calibration data: recompute quantile at different alpha
            # For simplicity, scale proportionally (conservative approximation)
            scale = np.sqrt(primary_alpha / self.alpha)
            primary_cp_quantile = cp_quantile_per_row * scale
            primary_rolling_quantile = self._win_rolling_quantile * scale

        # Primary interval (widest, e.g. 90%)
        cp_lower = win_ev - primary_cp_quantile
        rolling_lower = win_ev - primary_rolling_quantile
        lower = np.maximum(np.minimum(cp_lower, rolling_lower), 0.0)

        cp_upper = win_ev + primary_cp_quantile
        rolling_upper = win_ev + primary_rolling_quantile
        upper = np.maximum(cp_upper, rolling_upper)

        win_df["EV_lower_win_corrected"] = lower
        win_df["EV_upper_win_corrected"] = upper

        # Use second alpha for confidence scoring (narrower, e.g. 80%)
        if len(alphas) > 1:
            secondary_alpha = alphas[1]
            if abs(secondary_alpha - self.alpha) < 1e-9:
                secondary_cp_quantile = cp_quantile_per_row
            else:
                scale = np.sqrt(secondary_alpha / self.alpha)
                secondary_cp_quantile = cp_quantile_per_row * scale
            secondary_lower = np.maximum(win_ev - secondary_cp_quantile, 0.0)
            win_df["_ev_lower_secondary"] = secondary_lower

        # conformal_confidence_score per D-06, D-08:
        # Higher score = more confident bet
        # Components: EV lower bound (80%) and interval width (90%)
        # Score = EV_lower_80 * (1 - normalized_width)
        # normalized_width = width / max(width) in race
        interval_width = (upper - lower).clip(lower=1e-6)
        if "race_id" in win_df.columns:
            max_width = interval_width.groupby(win_df["race_id"]).transform("max").clip(lower=1e-6)
            normalized_width = (interval_width / max_width).clip(0.0, 1.0)
        else:
            max_width = interval_width.max()
            normalized_width = (interval_width / max(max_width, 1e-6)).clip(0.0, 1.0)

        ev_lower_for_score = win_df.get("_ev_lower_secondary", lower)
        win_df["conformal_confidence_score"] = (
            ev_lower_for_score * (1.0 - normalized_width)
        ).fillna(0.0)

        # Clean up temporary column
        win_df.drop(columns=["_ev_lower_secondary"], inplace=True, errors="ignore")

        # Place bounds (simpler: no confidence score needed for place)
        place_ev = pd.to_numeric(place_df["ev_place_corrected"], errors="coerce").fillna(0.0)
        cp_lower_place = place_ev - self._place_cp_quantile
        rolling_lower_place = place_ev - self._place_rolling_quantile
        place_df["EV_lower_place"] = np.maximum(
            np.minimum(cp_lower_place, rolling_lower_place), 0.0
        )
        cp_upper_place = place_ev + self._place_cp_quantile
        rolling_upper_place = place_ev + self._place_rolling_quantile
        place_df["EV_upper_place"] = np.maximum(cp_upper_place, rolling_upper_place)

        return win_df, place_df
    ```

    **Step 2: Refactor `predict_lower_bound()` as wrapper** per Pitfall 2 in RESEARCH.md.

    Change the existing `predict_lower_bound()` method body to call `predict_interval()` and return only the lower bound columns:

    ```python
    def predict_lower_bound(
        self,
        win_df: pd.DataFrame,
        place_df: pd.DataFrame,
    ) -> tuple[pd.DataFrame, pd.DataFrame]:
        """EVの信頼区間下限を推定。predict_interval()のラッパー (後方互換)。"""
        win_result, place_result = self.predict_interval(win_df, place_df)
        # Remove upper bound columns for backward compatibility
        win_result = win_result.drop(
            columns=["EV_upper_win_corrected", "conformal_confidence_score"],
            errors="ignore",
        )
        place_result = place_result.drop(
            columns=["EV_upper_place"],
            errors="ignore",
        )
        return win_result, place_result
    ```

    This ensures the 3 existing call sites (training_pipeline.py lines ~773, ~783 and race_predictor.py line 147) continue working without modification.

    **Step 3: Update RacePredictor.predict() to call predict_interval()** per D-05.

    In `src/backtest/race_predictor.py`, change line 147:
    ```python
    # BEFORE:
    win_df, place_df = submodel.confidence.predict_lower_bound(df, df)
    # AFTER:
    win_df, place_df = submodel.confidence.predict_interval(df, df)
    ```
    This replaces predict_lower_bound with predict_interval so the full EV interval and conformal_confidence_score are available downstream.

    **Step 4: Integrate conformal_confidence_score into WinSelectionGate** per D-08.

    In `src/models/win_selection_gate.py`, modify `_build_score_tables()` to add confidence-based pair scores. Following the Pitfall 3 guidance (avoid combination explosion), add confidence as pair_scores only, NOT as a 4th combo dimension.

    In `_build_score_tables()`, after the existing `pair_scores` block (around line 294), add:
    ```python
        # ODDS-03: conformal confidence pair scores (D-08)
        confidence_edges = _quantile_edges(
            work.get("conformal_confidence_score", pd.Series(0.0, index=work.index)),
            max(3, self.n_bins // 2),  # Fewer bins to avoid sparsity
        )
        work["_confidence_bin"] = _bucketize(
            work.get("conformal_confidence_score", pd.Series(0.0, index=work.index)),
            confidence_edges,
        )

        confidence_pair_specs = {
            "confidence_prob": ["_confidence_bin", "_prob_bin"],
            "confidence_edge": ["_confidence_bin", "_edge_bin"],
        }
        for prefix, cols in confidence_pair_specs.items():
            grouped = (
                work.groupby(cols, observed=True)["realized_win_roi"]
                .agg(["mean", "count"])
                .reset_index()
            )
            for _, data in grouped.iterrows():
                if pd.isna(data[cols[0]]) or pd.isna(data[cols[1]]):
                    continue
                if data["count"] < 5:
                    continue
                key = (prefix, int(data[cols[0]]), int(data[cols[1]]))
                pair_scores[key] = _smoothed_score(
                    float(data["mean"]),
                    float(data["count"]),
                    global_score,
                    self.prior_weight,
                )
    ```

    Update `_lookup_score()` to include the new confidence pairs in the fallback. After the existing pair_values list (around line 356), extend:
    ```python
        # ODDS-03: confidence pair fallback
        if "_confidence_bin" in prepared.columns:
            confidence_bin = int(prepared["_confidence_bin"].iloc[0]) if "_confidence_bin" in prepared.columns else None
            if confidence_bin is not None:
                for key in [
                    ("confidence_prob", confidence_bin, prob_key),
                    ("confidence_edge", confidence_bin, edge_key),
                ]:
                    if key in pair_scores:
                        pair_values.append(pair_scores[key])
    ```

    Note: The `_lookup_score` method receives individual row data. The confidence_bin must be computed in `score()` and passed through. Update `score()` to compute _confidence_bin before calling _score_frame:
    ```python
    # In score() method, before calling _score_frame or _surface_score:
    if "conformal_confidence_score" in prepared.columns:
        confidence_edges = getattr(self, '_confidence_edges', [])
        if confidence_edges:
            prepared["_confidence_bin"] = _bucketize(
                prepared["conformal_confidence_score"], confidence_edges
            )
    ```

    Add `self._confidence_edges: list[float] = []` to `__init__`.
    Store confidence_edges in `_build_score_tables` return dict and assign in the train method.

    Update `save()` and `load()` to persist `_confidence_edges` and the new pair_scores.

    **Step 5: Add tests to `tests/test_robust_confidence_estimator.py`** per D-11.

    Add these tests to the existing `TestRobustConfidenceEstimator` class:
    - `test_predict_interval_returns_all_columns`: Verify EV_lower_win_corrected, EV_upper_win_corrected, conformal_confidence_score exist
    - `test_interval_ordering`: lower <= ev <= upper for all rows
    - `test_conformal_confidence_score_non_negative`: score >= 0
    - `test_predict_lower_bound_backward_compat`: predict_lower_bound still returns only lower columns (no upper, no confidence_score)
    - `test_narrower_alpha_narrower_interval`: 80% interval is inside 90% interval

    **Step 6: Add pipeline integration test to `tests/test_race_predictor.py`** per D-12.

    Add a test that verifies when RacePredictor.predict() runs with a mock confidence estimator that supports predict_interval(), the output contains conformal_confidence_score and EV_upper_win_corrected. Mock all submodel components and verify the full chain.

    **Step 7: Add numerical consistency tests to `tests/test_odds_deviation.py`** per D-13.

    Add tests verifying:
    - EV interval ordering: lower < point < upper (using mock data)
    - conformal_confidence_score range: [0, inf)
    - NaN rate: deviation features NaN rate is bounded when odds_to_ability_ratio exists
  </action>
  <verify>
    <automated>cd C:/Users/hirom/develop/keiba-ai && python -m pytest tests/test_robust_confidence_estimator.py tests/test_odds_deviation.py tests/test_race_predictor.py -v</automated>
  </verify>
  <done>
    - RobustConfidenceEstimator has predict_interval() method with alphas parameter
    - predict_interval() returns EV_lower_win_corrected, EV_upper_win_corrected, conformal_confidence_score
    - predict_lower_bound() is a backward-compatible wrapper that drops upper and confidence columns
    - RacePredictor.predict() calls predict_interval() instead of predict_lower_bound()
    - WinSelectionGate._build_score_tables() includes confidence_prob and confidence_edge pair_scores
    - conformal_confidence_score = EV_lower_80 * (1 - normalized_width_90)
    - All existing tests continue to pass (no regressions)
    - All new tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pipeline input → Feature computation | race_id groupby correctness depends on valid race_id values |
| Calibration data → EV interval | Nonconformity scores from calibration must represent prediction errors accurately |
| conformal_confidence_score → WinSelectionGate | Score used for gate decisions must not be NaN or infinite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | D (Denial of Service) | compute_odds_deviation_features | accept | Single race inference: groupby on ~18 rows max. No performance risk |
| T-06-02 | I (Information Disclosure) | conformal_confidence_score | mitigate | Clip zscore to [-5, 5] and score to non-negative. Validate no inf/nan in output |
| T-06-03 | T (Tampering) | _prepare_features lazy computation | mitigate | Feature exists check before lazy computation. Training path always pre-computes |
| T-06-04 | S (Spoofing) | predict_lower_bound wrapper | accept | Backward compat wrapper is internal API. No external consumers |
</threat_model>

<verification>
## Phase 6 Verification

1. **Unit tests pass:** `python -m pytest tests/test_odds_deviation.py tests/test_robust_confidence_estimator.py -v`
2. **Integration tests pass:** `python -m pytest tests/test_race_predictor.py -v`
3. **No regressions:** `python -m pytest tests/ -v --tb=short`
4. **Feature columns verified:**
   ```bash
   python -c "from models.two_stage_return_model import WinTwoStageModel; cols = WinTwoStageModel.FEATURE_COLS; assert 'deviation_rank' in cols; assert 'deviation_zscore' in cols; print('OK')"
   ```
5. **New function importable:**
   ```bash
   python -c "from features.odds_deviation_features import compute_odds_deviation_features; print('OK')"
   ```
6. **predict_interval importable:**
   ```bash
   python -c "from models.robust_confidence_estimator import RobustConfidenceEstimator; e = RobustConfidenceEstimator(); assert hasattr(e, 'predict_interval'); print('OK')"
   ```
</verification>

<success_criteria>
- deviation_rank と deviation_zscore が compute_odds_deviation_features() で計算される
- WinTwoStageModel.FEATURE_COLS に deviation_rank, deviation_zscore が含まれる
- TrainingPipeline と RacePredictor の両方で compute_odds_deviation_features() が呼ばれる
- RobustConfidenceEstimator.predict_interval() が EV上下区間と conformal_confidence_score を返す
- predict_lower_bound() が後方互換ラッパーとして動作する
- conformal_confidence_score が WinSelectionGate の pair_scores に統合される
- 三層テスト(単体/統合/数値一貫性)が全て通過する
- 既存テストにリグレッションがない
</success_criteria>

<output>
After completion, create `.planning/phases/06-odds-deviation/06-01-SUMMARY.md`
</output>
