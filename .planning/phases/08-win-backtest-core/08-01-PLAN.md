---
phase: 08-win-backtest-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backtest/engine.py
  - src/db/everydb2_queries.py
  - scripts/run_backtest.py
autonomous: true
requirements:
  - WIN-01
  - WIN-02
  - WIN-04

must_haves:
  truths:
    - "build_win_payout_map()がpaytansyoumaban1/paytansyopay1から(race_id,umaban)->multiplier辞書を構築する"
    - "final_win_odds_mapがtanodds列から(race_id,umaban)->odds辞書を構築する"
    - "_settle_bet()がBetType.WINの場合にwin_payout_mapを参照して正しい単勝払戻金額を返す"
    - "BacktestEngineがbetting_target='win'で初期化でき、run()内でwin payout/odds mapを構築する"
    - "--betting-target win|placeフラグでrun_backtest.pyがモードを切り替えられる"
  artifacts:
    - path: "src/backtest/engine.py"
      provides: "build_win_payout_map(), win_payout_map, final_win_odds_map, _settle_bet() WIN fix"
      contains: "def build_win_payout_map"
    - path: "scripts/run_backtest.py"
      provides: "--betting-target CLI argument"
      contains: "betting-target"
    - path: "src/db/everydb2_queries.py"
      provides: "get_payouts() SQL with paytansyoumaban1/paytansyopay1"
      contains: "paytansyoumaban1"
  key_links:
    - from: "scripts/run_backtest.py"
      to: "src/backtest/engine.py"
      via: "BacktestEngine(betting_target=...)"
      pattern: "betting_target"
    - from: "src/backtest/engine.py"
      to: "payouts.parquet"
      via: "build_win_payout_map(payouts_df)"
      pattern: "paytansyopay1"
    - from: "src/backtest/engine.py"
      to: "feat_df"
      via: "final_win_odds_map from tanodds column"
      pattern: "tanodds"
---

<objective>
BacktestEngineに単勝決済インフラを追加し、_settle_bet()のWIN bugを修正する。

Purpose: 単勝ベットが正しい払戻金額で決済されるようにする。現在_settle_bet()はWINベットを複勝payout_mapで誤決済しており、これがPhase 8の前提条件。
Output: build_win_payout_map(), final_win_odds_map, _settle_bet() WIN fix, --betting-target CLI, ETL SQL update
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-win-backtest-core/08-CONTEXT.md
@.planning/phases/08-win-backtest-core/08-RESEARCH.md

<interfaces>
<!-- engine.py: build_payout_map pattern (lines 102-125) -->
```python
def build_payout_map(payouts_df: pd.DataFrame) -> dict[tuple[str, int], float]:
    payout_map: dict[tuple[str, int], float] = {}
    if payouts_df.empty:
        return payout_map
    for _, row in payouts_df.iterrows():
        race_id = str(row.get("race_id", ""))
        for i in range(1, 6):
            umaban = row.get(f"payfukusyoumaban{i}")
            pay = row.get(f"payfukusyopay{i}")
            if pd.notna(umaban) and pd.notna(pay):
                try:
                    key = (race_id, int(umaban))
                    val = float(pay) / 100.0
                    if key not in payout_map or val > payout_map[key]:
                        payout_map[key] = val
                except (ValueError, TypeError):
                    continue
    return payout_map
```

<!-- engine.py: BacktestEngine.__init__ (lines 182-208) -->
```python
def __init__(
    self,
    models: TrainedModelsV5,
    initial_bankroll: float = 100_000,
    store: ParquetStore | None = None,
    betting_mode: str = "flat",
    diag_prefix: str = "bt",
) -> None:
    if betting_mode not in ("flat", "kelly"):
        raise ValueError(f"betting_mode must be 'flat' or 'kelly', got '{betting_mode}'")
    self.models = models
    self.initial_bankroll = initial_bankroll
    self.store = store or ParquetStore()
    self.betting_mode = betting_mode
    self.diag_prefix = diag_prefix
```

<!-- engine.py: final_odds_map construction (lines 276-281) -->
```python
final_odds_map: dict[tuple[str, int], float] = {}
if not final_odds_df.empty:
    for _, r in final_odds_df.iterrows():
        key = (str(r["race_id"]), int(r["umaban"]))
        if pd.notna(r.get("fukuoddslow")):
            final_odds_map[key] = float(r["fukuoddslow"])
```

<!-- engine.py: _settle_bet() (lines 909-950) -->
```python
def _settle_bet(self, bet: Bet, race_df: pd.DataFrame) -> float:
    if bet.bet_type == BetType.WIDE:
        # ... WIDE logic unchanged ...
        return 0.0

    # BUG: WIN bets fall through here, using place payout_map
    payout_key = (bet.race_id, bet.umaban)
    if hasattr(self, "payout_map") and payout_key in self.payout_map:
        return float(bet.stake * self.payout_map[payout_key])  # WRONG for WIN

    horse = race_df[race_df["umaban"] == bet.umaban]
    if horse.empty:
        return 0.0
    finish_pos = int(horse.iloc[0]["kakuteijyuni"])
    settle_odds = bet.final_odds if bet.final_odds > 0 else bet.odds
    if bet.bet_type == BetType.PLACE:
        if 1 <= finish_pos <= 3:
            return float(bet.stake * settle_odds)
    elif bet.bet_type == BetType.WIN:
        if finish_pos == 1:
            return float(bet.stake * settle_odds)
    return 0.0
```

<!-- engine.py: Bet final_odds assignment (lines 601-609) -->
```python
updated_bets = []
for bet in bets:
    if bet.bet_type == BetType.WIDE:
        updated_bets.append(bet)
    else:
        fo = final_odds_map.get((bet.race_id, bet.umaban), bet.odds)
        updated_bets.append(replace(bet, final_odds=fo))
bets = updated_bets
```

<!-- engine.py: payout_map construction in run() (lines 284-290) -->
```python
payouts_df = load_payouts(self.store, start, end)
self.payout_map = build_payout_map(payouts_df)
logger.info("Loaded payout map: %d entries", len(self.payout_map))
self.wide_payout_map = build_wide_payout_map(payouts_df)
logger.info("Loaded wide payout map: %d entries", len(self.wide_payout_map))
```

<!-- engine.py: candidate_df dispatch (line 504) -->
```python
candidate_df = self._race_predictor.get_place_candidates(
    result_df, regime_params=regime_params,
)
```

<!-- everydb2_queries.py: get_payouts() SQL (lines 267-274) -->
```python
sql = f"""
    SELECT
        CAST(year || monthday || jyocd || kaiji || nichiji || racenum AS varchar) AS race_id,
        payfukusyoumaban1, payfukusyopay1,
        payfukusyoumaban2, payfukusyopay2,
        payfukusyoumaban3, payfukusyopay3,
        payfukusyoumaban4, payfukusyopay4,
        payfukusyoumaban5, payfukusyopay5
    FROM s_harai
    WHERE year || monthday = %s
      AND datakubun IN ('1', '2')
"""
```

<!-- run_backtest.py: build_parser() (lines 61-88) -->
```python
def build_parser() -> argparse.ArgumentParser:
    parser = argparse.ArgumentParser(description="バックテスト")
    parser.add_argument("--train-start", ...)
    parser.add_argument("--train-end", ...)
    parser.add_argument("--test-start", ...)
    parser.add_argument("--test-end", ...)
    parser.add_argument("--years", nargs="+", type=int, ...)
    parser.add_argument("--train-window", type=int, default=4, ...)
    parser.add_argument("--report", action="store_true", ...)
    parser.add_argument("--betting-mode", choices=["flat", "kelly"], default="flat", ...)
    parser.add_argument("--ensemble", action="store_true", ...)
    parser.add_argument("--skip-train", action="store_true", ...)
    return parser
```

<!-- run_backtest.py: BacktestEngine construction (lines 314-319) -->
```python
engine = BacktestEngine(
    models=models,
    store=store,
    betting_mode=args.betting_mode,
    diag_prefix=f"bt_{test_year}",
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add build_win_payout_map() + fix _settle_bet() + add final_win_odds_map + betting_target dispatch in engine.py</name>
  <files>src/backtest/engine.py, tests/test_backtest_engine.py</files>
  <read_first>
    src/backtest/engine.py (full file — must see __init__, build_payout_map, run, _settle_bet)
    tests/test_backtest_engine.py (existing test patterns)
  </read_first>
  <behavior>
    - Test 1: build_win_payout_map() with payouts DataFrame containing paytansyoumaban1=5, paytansyopay1=240.0 returns {("202401010101", 5): 2.4}
    - Test 2: build_win_payout_map() with empty DataFrame returns empty dict
    - Test 3: build_win_payout_map() with NaN paytansyoumaban1 skips that row
    - Test 4: BacktestEngine.__init__(betting_target="win") sets self.betting_target to "win"
    - Test 5: BacktestEngine.__init__(betting_target="invalid") raises ValueError
    - Test 6: _settle_bet() with BetType.WIN and win_payout_map hit returns stake * multiplier (not place payout)
    - Test 7: _settle_bet() with BetType.WIN and win_payout_map miss logs WARNING and falls back to finish_pos==1 check
    - Test 8: _settle_bet() with BetType.PLACE still uses payout_map (unchanged behavior)
  </behavior>
  <action>
    **1. Add build_win_payout_map() function** (after build_wide_payout_map, before BacktestEngine class, around line 169):

    ```python
    def build_win_payout_map(
        payouts_df: pd.DataFrame,
    ) -> dict[tuple[str, int], float]:
        """payouts DataFrame から (race_id, umaban) -> odds_multiplier のマップを構築 (単勝用)。

        paytansyopay1 は「100円あたりの円」なので、100で割って倍率に変換する。
        単勝は1着のみ払戻しがあるため、ループは1回のみ。
        """
        win_payout_map: dict[tuple[str, int], float] = {}
        if payouts_df.empty:
            return win_payout_map
        for _, row in payouts_df.iterrows():
            race_id = str(row.get("race_id", ""))
            umaban = row.get("paytansyoumaban1")
            pay = row.get("paytansyopay1")
            if pd.notna(umaban) and pd.notna(pay):
                try:
                    key = (race_id, int(umaban))
                    val = float(pay) / 100.0
                    win_payout_map[key] = val
                except (ValueError, TypeError):
                    continue
        return win_payout_map
    ```

    **2. Add betting_target parameter to BacktestEngine.__init__()** per D-14, D-12:

    Add `betting_target: str = "win"` parameter after `diag_prefix`. Validate it is one of "win", "place", "wide" (raise ValueError if not). Store as `self.betting_target`.

    ```python
    def __init__(
        self,
        models: TrainedModelsV5,
        initial_bankroll: float = 100_000,
        store: ParquetStore | None = None,
        betting_mode: str = "flat",
        diag_prefix: str = "bt",
        betting_target: str = "win",
    ) -> None:
        if betting_mode not in ("flat", "kelly"):
            raise ValueError(f"betting_mode must be 'flat' or 'kelly', got '{betting_mode}'")
        if betting_target not in ("win", "place", "wide"):
            raise ValueError(f"betting_target must be 'win', 'place', or 'wide', got '{betting_target}'")
        self.models = models
        self.initial_bankroll = initial_bankroll
        self.store = store or ParquetStore()
        self.betting_mode = betting_mode
        self.diag_prefix = diag_prefix
        self.betting_target = betting_target
        # ... rest unchanged ...
    ```

    **3. Construct win_payout_map and final_win_odds_map in run()** per D-02, D-03:

    After `self.wide_payout_map = build_wide_payout_map(payouts_df)` (around line 290), add:
    ```python
    # 単勝払戻マップを構築（精算用）
    self.win_payout_map = build_win_payout_map(payouts_df)
    logger.info("Loaded win payout map: %d entries", len(self.win_payout_map))
    ```

    After `final_odds_map` construction block (after line 281), add:
    ```python
    # 単勝確定オッズマップを構築（精算用。tanodds列を使用）
    final_win_odds_map: dict[tuple[str, int], float] = {}
    if not feat_df.empty and "tanodds" in feat_df.columns:
        for _, r in feat_df.iterrows():
            key = (str(r["race_id"]), int(r["umaban"]))
            if pd.notna(r.get("tanodds")):
                final_win_odds_map[key] = float(r["tanodds"])
    ```

    NOTE: feat_df is constructed at line 292. The final_win_odds_map must be built AFTER feat_df is available. Place it after `feat_df = submodel_mgr.add_distance_band_features(feat_df)` (around line 295).

    **4. Add betting_target dispatch in run() candidate selection** per D-13, D-14:

    At line 504, replace the hardcoded `get_place_candidates` call with a dispatch:
    ```python
    if self.betting_target == "win":
        candidate_df = self._race_predictor.get_win_candidates(result_df)
    else:
        candidate_df = self._race_predictor.get_place_candidates(
            result_df, regime_params=regime_params,
        )
    ```
    NOTE: `get_win_candidates()` will be implemented in Plan 08-02. For now, if `self.betting_target == "win"` and the method does not exist, fall back to `get_place_candidates`. Use `hasattr` or try/except to handle the case gracefully:
    ```python
    if self.betting_target == "win":
        get_win = getattr(self._race_predictor, "get_win_candidates", None)
        if callable(get_win):
            candidate_df = get_win(result_df)
        else:
            candidate_df = self._race_predictor.get_place_candidates(
                result_df, regime_params=regime_params,
            )
    else:
        candidate_df = self._race_predictor.get_place_candidates(
            result_df, regime_params=regime_params,
        )
    ```

    **5. Fix _settle_bet() WIN bug** per research Pitfall 1:

    Insert WIN branch BEFORE the shared payout_map block (line 931). The new structure:
    ```python
    def _settle_bet(self, bet: Bet, race_df: pd.DataFrame) -> float:
        # ワイド: wide_payout_map（確定配当）から精算
        if bet.bet_type == BetType.WIDE:
            # ... existing WIDE logic unchanged ...
            return 0.0

        # 単勝: win_payout_map（確定配当）から精算
        if bet.bet_type == BetType.WIN:
            win_key = (bet.race_id, bet.umaban)
            if hasattr(self, "win_payout_map") and win_key in self.win_payout_map:
                return float(bet.stake * self.win_payout_map[win_key])
            # D-04: フォールバック — 着順ベース
            logger.warning(
                "Win payout missing for %s umaban=%d, using odds fallback",
                bet.race_id, bet.umaban,
            )
            horse = race_df[race_df["umaban"] == bet.umaban]
            if horse.empty:
                return 0.0
            finish_pos = int(horse.iloc[0]["kakuteijyuni"])
            if finish_pos == 1:
                settle_odds = bet.final_odds if bet.final_odds > 0 else bet.odds
                return float(bet.stake * settle_odds)
            return 0.0

        # 複勝: payout_map（確定配当）から精算
        payout_key = (bet.race_id, bet.umaban)
        if hasattr(self, "payout_map") and payout_key in self.payout_map:
            return float(bet.stake * self.payout_map[payout_key])

        horse = race_df[race_df["umaban"] == bet.umaban]
        if horse.empty:
            return 0.0
        finish_pos = int(horse.iloc[0]["kakuteijyuni"])
        settle_odds = bet.final_odds if bet.final_odds > 0 else bet.odds
        if bet.bet_type == BetType.PLACE:
            if 1 <= finish_pos <= 3:
                return float(bet.stake * settle_odds)
        return 0.0
    ```

    **6. Fix Bet final_odds assignment for WIN** per research Pitfall 3:

    At lines 601-609, update the final_odds assignment loop to use the correct map based on bet_type:
    ```python
    updated_bets = []
    for bet in bets:
        if bet.bet_type == BetType.WIDE:
            updated_bets.append(bet)
        elif bet.bet_type == BetType.WIN:
            fo = final_win_odds_map.get((bet.race_id, bet.umaban), bet.odds)
            updated_bets.append(replace(bet, final_odds=fo))
        else:
            fo = final_odds_map.get((bet.race_id, bet.umaban), bet.odds)
            updated_bets.append(replace(bet, final_odds=fo))
    bets = updated_bets
    ```

    **CRITICAL:** Use "tanodds" (not "tanoddslow") as the column name. The actual Parquet column name is "tanodds". The researcher confirmed CONTEXT.md references "tanoddslow" but this is incorrect — the column in the feature DataFrame is "tanodds".

    Write tests FIRST, then implement. Follow the existing test patterns in tests/test_backtest_engine.py (uses mock_models fixture, unittest.mock).
  </action>
  <verify>
    <automated>python -m pytest tests/test_backtest_engine.py -v -k "win_payout or betting_target or settle_bet" 2>&1 | head -60</automated>
  </verify>
  <done>
    - build_win_payout_map() function exists and passes unit tests
    - BacktestEngine.__init__() accepts betting_target parameter (default="win", validates win|place|wide)
    - win_payout_map and final_win_odds_map are constructed in run()
    - _settle_bet() has explicit WIN branch using win_payout_map (no longer falls through to place payout_map)
    - Bet final_odds assignment uses final_win_odds_map for BetType.WIN
    - candidate_df dispatch uses betting_target (with graceful fallback if get_win_candidates not yet available)
    - All existing tests still pass: python -m pytest tests/test_backtest_engine.py -v
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add --betting-target to run_backtest.py + update get_payouts() SQL in everydb2_queries.py</name>
  <files>scripts/run_backtest.py, src/db/everydb2_queries.py</files>
  <read_first>
    scripts/run_backtest.py (full file — must see build_parser, validate_args, BacktestEngine construction at lines 314 and 427)
    src/db/everydb2_queries.py (get_payouts method around line 260)
    src/db/etl.py (_TABLE_TYPE_RULES around line 111 to confirm paytansyoumaban1/paytansyopay1 already in type rules)
  </read_first>
  <behavior>
    - Test 1: run_backtest.py build_parser() returns parser with --betting-target argument accepting "win", "place", "wide", default "win"
    - Test 2: run_backtest.py build_parser() --betting-target with invalid value is rejected by choices
    - Test 3: get_payouts() SQL string contains "paytansyoumaban1" and "paytansyopay1"
  </behavior>
  <action>
    **1. Add --betting-target argument to build_parser()** per D-11, D-12:

    In `scripts/run_backtest.py`, add after the `--betting-mode` argument (around line 81):
    ```python
    parser.add_argument(
        "--betting-target",
        choices=["win", "place", "wide"],
        default="win",
        help="ベッティング対象 (win=単勝, place=複勝, wide=ワイド, デフォルト: win)",
    )
    ```

    **2. Pass betting_target to BacktestEngine construction** per D-14:

    At line 314 (single-year mode) and line 427 (multi-year mode), add `betting_target=args.betting_target`:
    ```python
    engine = BacktestEngine(
        models=models,
        store=store,
        betting_mode=args.betting_mode,
        diag_prefix=f"bt_{test_year}",
        betting_target=args.betting_target,
    )
    ```

    **3. Update get_payouts() SQL in everydb2_queries.py** per D-05:

    Add `paytansyoumaban1` and `paytansyopay1` to the SELECT clause. The updated SQL:
    ```python
    sql = f"""
        SELECT
            CAST(year || monthday || jyocd || kaiji || nichiji || racenum AS varchar) AS race_id,
            paytansyoumaban1, paytansyopay1,
            payfukusyoumaban1, payfukusyopay1,
            payfukusyoumaban2, payfukusyopay2,
            payfukusyoumaban3, payfukusyopay3,
            payfukusyoumaban4, payfukusyopay4,
            payfukusyoumaban5, payfukusyopay5
        FROM s_harai
        WHERE year || monthday = %s
          AND datakubun IN ('1', '2')
    """
    ```

    NOTE: The ETL type rules in `src/db/etl.py` already include `paytansyoumaban1` in the "int" list and `paytansyopay1` in the "float" list for the "payouts" table (confirmed at etl.py lines 112-113). No changes needed there.

    Write tests FIRST for the parser argument, then implement.
  </action>
  <verify>
    <automated>python -m pytest tests/test_backtest_engine.py tests/ -v -k "betting_target or parser" 2>&1 | head -40 && grep -n "paytansyoumaban1" src/db/everydb2_queries.py && grep -n "betting-target" scripts/run_backtest.py</automated>
  </verify>
  <done>
    - run_backtest.py has --betting-target argument with choices=["win", "place", "wide"], default="win"
    - BacktestEngine construction in both single-year and multi-year modes passes betting_target
    - get_payouts() SQL includes paytansyoumaban1 and paytansyopay1 columns
    - All existing tests pass: python -m pytest tests/ -v
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> BacktestEngine | User-supplied string validated against allowed values |
| payouts_df -> win_payout_map | External data (Parquet) parsed into internal structure |
| feat_df -> final_win_odds_map | Feature data read for odds lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | S | betting_target parameter | mitigate | Validate against ("win","place","wide") in __init__ — rejects typos/invalid |
| T-08-02 | T | _settle_bet payout lookup | mitigate | WIN branch is separate from PLACE — no cross-contamination possible |
| T-08-03 | D | payout data integrity | accept | Using actual JRA payout data — authoritative source, no tampering risk in backtest |
</threat_model>

<verification>
1. `python -m pytest tests/test_backtest_engine.py -v` — all tests pass (existing + new)
2. `grep -c "build_win_payout_map" src/backtest/engine.py` returns >= 2 (definition + call)
3. `grep "win_payout_map" src/backtest/engine.py | grep -v "^#" | grep -c "win_payout_map"` >= 3
4. `grep "betting_target" src/backtest/engine.py | grep -v "^#" | wc -l` >= 3 (param + validate + dispatch)
5. `grep "tanodds" src/backtest/engine.py | grep -v "^#"` shows correct column name (NOT tanoddslow)
6. `grep "paytansyoumaban1" src/db/everydb2_queries.py` returns match
7. `grep "betting-target" scripts/run_backtest.py` returns match
</verification>

<success_criteria>
- build_win_payout_map()構築: payouts_dfから(race_id,umaban)->multiplier辞書を返す
- final_win_odds_map構築: feat_dfのtanodds列から(race_id,umaban)->odds辞書を返す
- _settle_bet()修正: WIN branchがwin_payout_mapを参照し、PLACE payout_mapを汚染しない
- betting_target: BacktestEngineがデフォルト"win"で初期化、バリデーション付き
- CLI: --betting-target win|place|wide がrun_backtest.pyで動作
- ETL SQL: paytansyoumaban1/paytansyopay1がget_payouts()に含まれる
- 全テスト通過: python -m pytest tests/ -v
</success_criteria>

<output>
After completion, create `.planning/phases/08-win-backtest-core/08-01-SUMMARY.md`
</output>
