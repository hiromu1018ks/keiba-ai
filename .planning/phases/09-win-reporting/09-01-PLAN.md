---
phase: 09-win-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backtest/engine.py
  - src/backtest/report.py
  - scripts/run_backtest.py
  - src/backtest/templates/report.html
  - tests/test_backtest_report.py
autonomous: true
requirements: [RPT-01, RPT-02, RPT-03]

must_haves:
  truths:
    - "バックテスト結果のJSON/bet_historyに各単勝ベットのumaban/odds/EV/edge/gate_score/confidence/結果が記録されている"
    - "バックテスト終了時に単勝ROI/回収率/的中率/ベット数の集計がCLI標準出力される"
    - "レポートにオッズバンド別(人気帯1-3/4-6/7+ と オッズ倍率帯)のROI内訳が表示される"
    - "HTMLレポートにregime別/月別/表面×距離別/EVバンド別の診断セクションが表示される"
    - "AI分析用JSON(改善点自動特定付き)が保存される"
    - "place/wideモードでのバックテストが既存の動作を維持する"
  artifacts:
    - path: "src/backtest/engine.py"
      provides: "bet_history dictにwin_selection_ev/edge/prob, win_gate_score, conformal_confidence_score, tanoddslow, regime フィールド追加"
      contains: "win_selection_ev"
    - path: "src/backtest/report.py"
      provides: "BacktestReportGenerator拡張: betting_target条件分岐, _compute_regime_stats(), save_ai_diagnostics(), odds_multiplier_bands"
      contains: "odds_multiplier_bands"
    - path: "scripts/run_backtest.py"
      provides: "display_single_year_result()のwin指標出力, AI diagnostic JSON保存"
      contains: "win_rate_display"
    - path: "src/backtest/templates/report.html"
      provides: "regime/odds_multiplier_bandsセクション, win専用KPI cards"
      contains: "regime"
    - path: "tests/test_backtest_report.py"
      provides: "新規メソッド・フィールドのテスト"
      contains: "test_regime_stats"
  key_links:
    - from: "src/backtest/engine.py"
      to: "bet_history dict"
      via: "win_selection_* fields extraction from horse_rows DataFrame"
      pattern: "win_selection_ev"
    - from: "src/backtest/report.py"
      to: "src/backtest/templates/report.html"
      via: "Jinja2 template.render() with condition_stats, regime_stats, odds_multiplier_bands"
      pattern: "regime_bands|odds_multiplier_bands"
    - from: "scripts/run_backtest.py"
      to: "src/backtest/report.py"
      via: "BacktestReportGenerator.generate() + save_ai_diagnostics()"
      pattern: "save_ai_diagnostics"
---

<objective>
Win bet historyフィールド拡張 + ROI診断レポート + オッズバンド別分析を実装する。

Purpose: ユーザーが単勝バックテスト結果を多角的に分析し、どのスコア成分・オッズ帯・regimeがROIに寄与しているか特定できるようにする (RPT-01, RPT-02, RPT-03)。
Output: 拡張bet_history (JSON), HTML診断レポート, AI分析用JSON, CLI標準出力
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-win-reporting/09-CONTEXT.md
@.planning/phases/09-win-reporting/09-RESEARCH.md

<interfaces>
<!-- Executor が参照すべき既存インターフェース -->

From src/backtest/engine.py (BacktestResult dataclass):
```python
@dataclass
class BacktestResult:
    total_bets: int = 0
    total_stake: float = 0.0
    total_return: float = 0.0
    winning_bets: int = 0
    total_roi: float = 0.0
    max_drawdown: float = 0.0
    final_bankroll: float = 0.0
    monthly_returns: dict[str, float] = field(default_factory=dict)
    bet_history: list[dict[str, Any]] = field(default_factory=list)
    n_pre_post_odds_bets: int = 0
    n_fallback_odds_bets: int = 0
    avg_edge: float = 0.0
    min_edge: float = 0.0
    max_edge: float = 0.0
```

From src/backtest/engine.py (bet_history construction pattern, line 799-808):
```python
"p_place_pred": (
    float(horse_rows.iloc[0].get("p_place_pred", 0))
    if not horse_rows.empty
    else 0.0
),
```

From src/backtest/report.py (_band_stats helper, lines 189-223):
```python
def _band_stats(
    bets_list: list[dict[str, Any]],
    key_fn: Any,
    band_order: list[str],
) -> list[dict[str, Any]]:
    # groups by key_fn(bet), computes bets/wins/win_rate/avg_payout/roi per band
```

From src/backtest/report.py (_compute_condition_stats return type):
```python
return {
    "surface_distance": surface_distance,
    "popularity_bands": popularity_bands,
    "ev_bands": ev_bands,
}
```

From src/backtest/report.py (BacktestReportGenerator.generate signature):
```python
def generate(self, result: BacktestResult, bet_history: list[dict[str, Any]],
             train_period: str = "", test_period: str = "") -> Path:
```

From scripts/run_backtest.py (display_single_year_result signature):
```python
def display_single_year_result(result, elapsed_train, elapsed_test,
                               train_start, train_end, test_start, test_end) -> dict[str, Any]:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bet_history に win-specific フィールド追加 + regime 保存 + テスト拡張</name>
  <files>src/backtest/engine.py, tests/test_backtest_report.py</files>
  <read_first>
    src/backtest/engine.py (lines 750-812 — bet_history dict構築部)
    src/backtest/engine.py (lines 535-545 — regime 変数定義部)
    tests/test_backtest_report.py (既存テストパターン)
  </read_first>
  <behavior>
    - Test 1: _compute_regime_stats() が regime="aggressive"/"conservative"/"collapsed" 別に bets/wins/roi を正しく集計する
    - Test 2: _compute_condition_stats() の戻り値に "odds_multiplier_bands" と "regime_bands" が含まれる (win モード時)
    - Test 3: _compute_condition_stats() の戻り値に "odds_multiplier_bands" と "regime_bands" が含まれない (place モード時)
    - Test 4: bet_history に win_selection_ev フィールドが含まれることを確認するモックテスト
  </behavior>
  <action>
    **Step 1: engine.py に win-specific フィールド追加 (D-09, D-10)**

    `src/backtest/engine.py` lines 758-812 の bet_history dict 構築部に、`"top3_finishers": _top3` の直後 (line 809 の直前) に以下のフィールドを追加する。

    ```python
    # --- Win-specific fields (D-09, RPT-01) ---
    "win_selection_ev": (
        float(horse_rows.iloc[0].get("win_selection_ev", 0.0))
        if not horse_rows.empty
        else 0.0
    ),
    "win_selection_edge": (
        float(horse_rows.iloc[0].get("win_selection_edge", 0.0))
        if not horse_rows.empty
        else 0.0
    ),
    "win_selection_prob": (
        float(horse_rows.iloc[0].get("win_selection_prob", 0.0))
        if not horse_rows.empty
        else 0.0
    ),
    "win_gate_score": (
        float(horse_rows.iloc[0].get("win_gate_score", float("nan")))
        if not horse_rows.empty
        else float("nan")
    ),
    "conformal_confidence_score": (
        float(horse_rows.iloc[0].get("conformal_confidence_score", 0.0))
        if not horse_rows.empty
        else 0.0
    ),
    "tanoddslow": (
        float(horse_rows.iloc[0].get("tanoddslow", 0.0))
        if not horse_rows.empty
        else 0.0
    ),
    "regime": str(regime),
    ```

    注意点 (RESEARCH.md Pitfall 5 より):
    - `"popularity"` は既に line 773 で追加済み。再追加しない。
    - `"kakuteijyuni"` は既に lines 792-797 で追加済み。再追加しない。
    - `"odds"` (line 764) は bet.odds = 決定時オッズ。`"tanoddslow"` は確定オッズ。別フィールド。
    - `regime` 変数は line 539-542 でスコープ内に既に存在する (RESEARCH.md Pitfall 1 で確認済み)。

    **Step 2: tests/test_backtest_report.py にテスト追加**

    以下のテストクラスを追加する。

    ```python
    class TestComputeRegimeStats:
        """_compute_regime_stats のテスト"""

        def _make_bets(self) -> list[dict[str, Any]]:
            return [
                {"regime": "aggressive", "stake": 100.0, "result": 250.0},
                {"regime": "aggressive", "stake": 100.0, "result": 0.0},
                {"regime": "conservative", "stake": 100.0, "result": 180.0},
                {"regime": "conservative", "stake": 100.0, "result": 0.0},
                {"regime": "collapsed", "stake": 100.0, "result": 0.0},
            ]

        def test_regime_aggregation(self) -> None:
            from backtest.report import BacktestReportGenerator
            gen = BacktestReportGenerator(output_dir=Path("/tmp"))
            result = gen._compute_regime_stats(self._make_bets())
            assert len(result) == 3
            agg = [r for r in result if r["regime"] == "aggressive"][0]
            assert agg["bets"] == 2
            assert agg["wins"] == 1
            assert agg["roi"] == pytest.approx(250.0 / 200.0)

        def test_empty_input(self) -> None:
            from backtest.report import BacktestReportGenerator
            gen = BacktestReportGenerator(output_dir=Path("/tmp"))
            assert gen._compute_regime_stats([]) == []

    class TestComputeConditionStatsWin:
        """_compute_condition_stats の win 拡張テスト"""

        def _make_win_bets(self) -> list[dict[str, Any]]:
            return [
                {"surface": "turf", "kyori": 1200, "popularity": 1,
                 "ev": 1.8, "stake": 100.0, "result": 250.0, "is_win": True,
                 "tanoddslow": 2.5, "regime": "aggressive"},
                {"surface": "turf", "kyori": 1600, "popularity": 5,
                 "ev": 1.3, "stake": 100.0, "result": 300.0, "is_win": True,
                 "tanoddslow": 5.0, "regime": "conservative"},
                {"surface": "dirt", "kyori": 1200, "popularity": 8,
                 "ev": 0.9, "stake": 100.0, "result": 0.0, "is_win": False,
                 "tanoddslow": 15.0, "regime": "aggressive"},
                {"surface": "dirt", "kyori": 1800, "popularity": 3,
                 "ev": 1.5, "stake": 100.0, "result": 400.0, "is_win": True,
                 "tanoddslow": 8.0, "regime": "collapsed"},
            ]

        def test_odds_multiplier_bands_present_in_win_mode(self) -> None:
            from backtest.report import BacktestReportGenerator
            gen = BacktestReportGenerator(output_dir=Path("/tmp"))
            result = gen._compute_condition_stats(
                self._make_win_bets(), betting_target="win"
            )
            assert "odds_multiplier_bands" in result
            assert "regime_bands" in result

        def test_odds_multiplier_bands_absent_in_place_mode(self) -> None:
            from backtest.report import BacktestReportGenerator
            gen = BacktestReportGenerator(output_dir=Path("/tmp"))
            result = gen._compute_condition_stats(
                self._make_win_bets(), betting_target="place"
            )
            assert "odds_multiplier_bands" not in result
            assert "regime_bands" not in result

        def test_odds_multiplier_bands_values(self) -> None:
            from backtest.report import BacktestReportGenerator
            gen = BacktestReportGenerator(output_dir=Path("/tmp"))
            result = gen._compute_condition_stats(
                self._make_win_bets(), betting_target="win"
            )
            bands = result["odds_multiplier_bands"]
            # tanoddslow: 2.5, 5.0, 15.0, 8.0
            # bands: 1.0-3.0 (1), 3.0-10.0 (2), 10.0-30.0 (1)
            band_names = [b["band"] for b in bands]
            assert "1.0-3.0" in band_names
            assert "3.0-10.0" in band_names
            assert "10.0-30.0" in band_names
    ```

    注意: 上記テストは Task 2 で `_compute_condition_stats` のシグネチャ変更 (`betting_target` 引数追加) および `_compute_regime_stats` メソッド追加が完了した後にパスする。Task 1 ではテストファイルを作成し、RED 状態 (fail) を確認する。
  </action>
  <verify>
    <automated>cd C:/Users/hirom/develop/keiba-ai && python -m pytest tests/test_backtest_report.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
    - engine.py の bet_history dict に win_selection_ev, win_selection_edge, win_selection_prob, win_gate_score, conformal_confidence_score, tanoddslow, regime フィールドが追加されている
    - テストファイルに TestComputeRegimeStats と TestComputeConditionStatsWin クラスが存在する
  </done>
</task>

<task type="auto">
  <name>Task 2: BacktestReportGenerator に betting_target 条件分岐 + regime/odds band 分析 + AI diagnostic JSON + HTML拡張 + CLI出力拡張</name>
  <files>src/backtest/report.py, src/backtest/templates/report.html, scripts/run_backtest.py, tests/test_backtest_report.py</files>
  <read_first>
    src/backtest/report.py (全文 — メソッド追加箇所の把握)
    src/backtest/templates/report.html (全文 — セクション追加箇所の把握)
    scripts/run_backtest.py (lines 215-263 — display_single_year_result)
    scripts/run_backtest.py (lines 340-365 — レポート出力部)
    tests/test_backtest_report.py (Task 1 で追加したテストを含む最新版)
  </read_first>
  <acceptance_criteria>
    - `grep -c "odds_multiplier_bands" src/backtest/report.py` が 0 より大きい
    - `grep -c "_compute_regime_stats" src/backtest/report.py` が 0 より大きい
    - `grep -c "save_ai_diagnostics" src/backtest/report.py` が 0 より大きい
    - `grep -c "betting_target" src/backtest/report.py` が 0 より大きい
    - `grep -c "regime" src/backtest/templates/report.html` が 0 より大きい
    - `grep -c "odds_multiplier" src/backtest/templates/report.html` が 0 より大きい
    - `python -m pytest tests/test_backtest_report.py -v` が全テストパスする
    - `python -m pytest tests/test_backtest_report.py -v --tb=short 2>&1 | tail -5` に "failed" が含まれない
  </acceptance_criteria>
  <action>
    **Step 1: report.py の _compute_condition_stats に betting_target 引数を追加 (D-04, D-05)**

    `_compute_condition_stats` のシグネチャを変更:

    ```python
    def _compute_condition_stats(
        self,
        bets: list[dict[str, Any]],
        betting_target: str = "place",
    ) -> dict[str, Any]:
    ```

    既存の `return` 文の直前 (line 243) に、betting_target="win" の場合のみ odds_multiplier_bands と regime_bands を追加:

    ```python
        # --- Win-specific bands (D-01, D-03, RPT-03) ---
        odds_multiplier_bands: list[dict[str, Any]] = []
        regime_bands: list[dict[str, Any]] = []
        if betting_target == "win":
            odds_multiplier_bands = _band_stats(
                bets,
                lambda b: (
                    "1.0-3.0" if b.get("tanoddslow", 0) < 3.0
                    else "3.0-10.0" if b.get("tanoddslow", 0) < 10.0
                    else "10.0-30.0" if b.get("tanoddslow", 0) < 30.0
                    else "30.0+"
                ),
                ["1.0-3.0", "3.0-10.0", "10.0-30.0", "30.0+"],
            )
            regime_bands = self._compute_regime_stats(bets)

        return {
            "surface_distance": surface_distance,
            "popularity_bands": popularity_bands,
            "ev_bands": ev_bands,
            "odds_multiplier_bands": odds_multiplier_bands,
            "regime_bands": regime_bands,
        }
    ```

    **Step 2: _compute_regime_stats メソッドを追加 (D-07)**

    `_compute_monthly_stats` の後に追加:

    ```python
    def _compute_regime_stats(self, bets: list[dict[str, Any]]) -> list[dict[str, Any]]:
        """Regime別集計: ROI, 的中率, ベット数"""
        if not bets:
            return []
        regime_data: dict[str, dict[str, float]] = defaultdict(
            lambda: {"bets": 0, "wins": 0, "stake": 0.0, "total_return": 0.0}
        )
        for b in bets:
            regime = b.get("regime", "unknown")
            regime_data[regime]["bets"] += 1
            regime_data[regime]["stake"] += b["stake"]
            if b["result"] > 0:
                regime_data[regime]["wins"] += 1
                regime_data[regime]["total_return"] += b["result"]
        result: list[dict[str, Any]] = []
        for regime in ["aggressive", "conservative", "collapsed"]:
            if regime not in regime_data:
                continue
            s = regime_data[regime]
            n = s["bets"]
            result.append({
                "regime": regime,
                "bets": n,
                "wins": int(s["wins"]),
                "win_rate": s["wins"] / n if n > 0 else 0.0,
                "roi": s["total_return"] / s["stake"] if s["stake"] > 0 else 0.0,
            })
        return result
    ```

    **Step 3: generate() に betting_target 引数を追加 (D-04, D-05)**

    `generate()` のシグネチャを変更:
    ```python
    def generate(
        self,
        result: BacktestResult,
        bet_history: list[dict[str, Any]],
        train_period: str = "",
        test_period: str = "",
        betting_target: str = "place",
    ) -> Path:
    ```

    `generate()` 内の `_compute_condition_stats(bets)` 呼び出しを `_compute_condition_stats(bets, betting_target=betting_target)` に変更。

    `template.render()` 呼び出しに `betting_target=betting_target` を追加:
    ```python
    html = template.render(
        summary=summary,
        bankroll_series=bankroll,
        monthly_stats=monthly,
        condition_stats=conditions,
        bet_details=bets,
        footer_info=footer_info,
        betting_target=betting_target,
    )
    ```

    **Step 4: save_ai_diagnostics() メソッドを追加 (D-06, D-08)**

    `save_bet_history()` の後に追加:

    ```python
    def save_ai_diagnostics(
        self,
        bets: list[dict[str, Any]],
        result: BacktestResult,
        betting_target: str = "place",
    ) -> Path | None:
        """AI分析用診断JSONを保存 (D-06, D-08)"""
        if betting_target != "win" or not bets:
            return None

        try:
            commit_hash = (
                subprocess.check_output(
                    ["git", "rev-parse", "--short", "HEAD"], stderr=subprocess.DEVNULL
                )
                .decode()
                .strip()
            )
        except (subprocess.CalledProcessError, FileNotFoundError):
            commit_hash = "unknown"

        monthly = self._compute_monthly_stats(bets)
        regime = self._compute_regime_stats(bets)
        conditions = self._compute_condition_stats(bets, betting_target="win")

        # Highlights: best/worst band 自動特定 (D-08)
        all_bands: list[dict[str, Any]] = []
        for band in conditions.get("popularity_bands", []):
            all_bands.append({"name": f"人気{band['band']}", "roi": band["roi"], "bets": band["bets"]})
        for band in conditions.get("odds_multiplier_bands", []):
            all_bands.append({"name": f"オッズ{band['band']}", "roi": band["roi"], "bets": band["bets"]})
        for band in conditions.get("ev_bands", []):
            all_bands.append({"name": f"EV{band['band']}", "roi": band["roi"], "bets": band["bets"]})
        for r in regime:
            all_bands.append({"name": f"Regime:{r['regime']}", "roi": r["roi"], "bets": r["bets"]})

        significant_bands = [b for b in all_bands if b["bets"] >= 5]
        best_band = max(significant_bands, key=lambda x: x["roi"]) if significant_bands else None
        worst_band = min(significant_bands, key=lambda x: x["roi"]) if significant_bands else None

        # 月別トレンド (D-08)
        monthly_trend = "stable"
        if len(monthly) >= 3:
            first_half = sum(m["roi"] for m in monthly[: len(monthly) // 2]) / (len(monthly) // 2)
            second_half = sum(m["roi"] for m in monthly[len(monthly) // 2 :]) / (
                len(monthly) - len(monthly) // 2
            )
            if second_half > first_half * 1.1:
                monthly_trend = "improving"
            elif second_half < first_half * 0.9:
                monthly_trend = "declining"

        edges = [b.get("edge", 0) for b in bets if "edge" in b]
        avg_edge = sum(edges) / len(edges) if edges else 0.0

        diagnostic = {
            "meta": {
                "betting_target": "win",
                "generated_at": datetime.now(timezone.utc).isoformat(),
                "commit": commit_hash,
            },
            "summary": {
                "roi": result.total_roi,
                "win_rate": result.winning_bets / result.total_bets if result.total_bets > 0 else 0.0,
                "total_bets": result.total_bets,
                "total_stake": result.total_stake,
                "total_return": result.total_return,
                "avg_edge": avg_edge,
                "profit": result.profit,
            },
            "monthly_trend": monthly,
            "regime_breakdown": regime,
            "odds_multiplier_bands": conditions.get("odds_multiplier_bands", []),
            "popularity_bands": conditions.get("popularity_bands", []),
            "ev_bands": conditions.get("ev_bands", []),
            "surface_distance": conditions.get("surface_distance", []),
            "highlights": {
                "best_band": best_band,
                "worst_band": worst_band,
                "monthly_trend": monthly_trend,
                "overperforming_conditions": [
                    b for b in significant_bands
                    if b["roi"] > result.total_roi and b["bets"] >= 5
                ],
                "underperforming_conditions": [
                    b for b in significant_bands
                    if b["roi"] < result.total_roi and b["bets"] >= 5
                ],
            },
        }

        path = self.output_dir / "ai_diagnostics.json"
        path.write_text(json.dumps(diagnostic, ensure_ascii=False, indent=2), encoding="utf-8")
        return path
    ```

    **Step 5: report.html に win-specific セクション追加**

    `<template>` 内の「条件分析」セクション (line 230 の `</div>` の直前) に、regime 別とオッズ倍率帯のテーブルを追加。全て `{% if betting_target == "win" and condition_stats.odds_multiplier_bands %}` でガードする (Pitfall 4 対策):

    「EV帯」テーブルの直後 (line 225 `</div>` の後) に追加:

    ```html
    {% if betting_target == "win" %}
    {% if condition_stats.regime_bands %}
    <div>
        <h3>Regime別</h3>
        <table class="display" style="width:100%;">
            <thead>
                <tr><th>Regime</th><th>ベット数</th><th>的中率</th><th>ROI</th></tr>
            </thead>
            <tbody>
                {% for row in condition_stats.regime_bands %}
                <tr class="{{ 'heatmap-positive' if row.roi >= 1 else 'heatmap-negative' }}">
                    <td>{{ row.regime }}</td><td>{{ row.bets }}</td>
                    <td>{{ "%.1f%%"|format(row.win_rate * 100) }}</td>
                    <td>{{ "%.1f%%"|format(row.roi * 100) }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
    {% endif %}
    {% if condition_stats.odds_multiplier_bands %}
    <div>
        <h3>オッズ倍率帯</h3>
        <table class="display" style="width:100%;">
            <thead>
                <tr><th>オッズ帯</th><th>ベット数</th><th>的中率</th><th>平均払戻</th><th>ROI</th></tr>
            </thead>
            <tbody>
                {% for row in condition_stats.odds_multiplier_bands %}
                <tr class="{{ 'heatmap-positive' if row.roi >= 1 else 'heatmap-negative' }}">
                    <td>{{ row.band }}</td><td>{{ row.bets }}</td>
                    <td>{{ "%.1f%%"|format(row.win_rate * 100) }}</td>
                    <td>&yen;{{ row.avg_payout|format_number }}</td>
                    <td>{{ "%.1f%%"|format(row.roi * 100) }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
    {% endif %}
    {% endif %}
    ```

    また、サマリー KPI Cards (line 85-110) の直後に、win モード時の追加 KPI cards を挿入:

    ```html
    {% if betting_target == "win" %}
    <div class="kpi-card">
        <div class="kpi-label">ベット数</div>
        <div class="kpi-value">{{ summary.total_bets }}</div>
    </div>
    <div class="kpi-card">
        <div class="kpi-label">投資額</div>
        <div class="kpi-value">&yen;{{ summary.total_stake|format_number }}</div>
    </div>
    <div class="kpi-card">
        <div class="kpi-label">払戻額</div>
        <div class="kpi-value">&yen;{{ summary.total_return|format_number }}</div>
    </div>
    {% endif %}
    ```

    **Step 6: run_backtest.py の display_single_year_result() に win 指標を追加 (D-06, RPT-02)**

    `display_single_year_result()` の関数シグネチャに `betting_target` 引数を追加:

    ```python
    def display_single_year_result(
        result: BacktestResult,
        elapsed_train: float,
        elapsed_test: float,
        train_start: str,
        train_end: str,
        test_start: str,
        test_end: str,
        betting_target: str = "win",
    ) -> dict[str, Any]:
    ```

    関数内の "Before vs After" セクションの直前に、win モード時の追加出力を追加:

    ```python
    if betting_target == "win":
        print()
        print("=" * 50)
        print("  単勝ベット詳細")
        print("=" * 50)
        wr = result.winning_bets / result.total_bets if result.total_bets > 0 else 0
        print(f"  的中率:         {wr:>9.1%}")
        print(f"  的中数:         {result.winning_bets:>8,} / {result.total_bets:,}")
        avg_odds = (
            sum(b.get("tanoddslow", 0) for b in result.bet_history) / len(result.bet_history)
            if result.bet_history
            else 0.0
        )
        print(f"  平均オッズ:     {avg_odds:>9.1f}")
        if result.avg_edge > 0:
            print(f"  平均Edge:       {result.avg_edge:>9.3f}")
            print(f"  Edge範囲:       {result.min_edge:.3f} ~ {result.max_edge:.3f}")
    ```

    `_run_single_year` 内の呼び出しを更新:

    ```python
    out = display_single_year_result(
        result,
        elapsed_train,
        elapsed_test,
        train_start,
        train_end,
        test_start,
        test_end,
        betting_target=args.betting_target,
    )
    ```

    レポート出力部 (line 357) の `gen.generate()` 呼び出しに `betting_target` を追加:

    ```python
    report_path = gen.generate(
        result,
        result.bet_history,
        train_period=f"{train_start} ~ {train_end}",
        test_period=f"{test_start} ~ {test_end}",
        betting_target=args.betting_target,
    )
    ```

    `save_bet_history()` の直後に `save_ai_diagnostics()` を追加:

    ```python
    if args.betting_target == "win":
        diag_path = gen.save_ai_diagnostics(
            gen._derive_fields(result.bet_history),
            result,
            betting_target=args.betting_target,
        )
        if diag_path:
            print(f"AI診断JSON: {diag_path}")
    ```

    **Step 7: MultiYearReportGenerator.generate() にも betting_target を伝播**

    `MultiYearReportGenerator.generate()` 内の `self._single_gen._compute_condition_stats(enriched)` 呼び出しに `betting_target` を渡せるように、generate() のシグネチャにも `betting_target: str = "place"` を追加し、`_compute_condition_stats(enriched, betting_target=betting_target)` に変更する。

    **Step 8: テストを修正して GREEN にする**

    Task 1 で追加したテストが全てパスするように、テスト内のメソッド呼び出しが実際のシグネチャと一致していることを確認する。必要に応じてテストの import 文や引数を修正する。

    既存テスト (TestComputeConditionStats 等) が壊れないよう、`_compute_condition_stats` の `betting_target` 引数はデフォルト値 `"place"` とし、既存の呼び出しは変更不要とする。
  </action>
  <verify>
    <automated>cd C:/Users/hirom/develop/keiba-ai && python -m pytest tests/test_backtest_report.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    - _compute_condition_stats() に betting_target 引数が追加され、win 時に odds_multiplier_bands + regime_bands が返される
    - _compute_regime_stats() メソッドが追加され、regime 別の bets/wins/roi を集計する
    - save_ai_diagnostics() メソッドが追加され、highlights (best/worst band, monthly trend, over/underperforming) を含む JSON を保存する
    - report.html に regime 別テーブル、オッズ倍率帯テーブル、win KPI cards が追加され、全て {% if betting_target == "win" %} でガードされている
    - display_single_year_result() に betting_target 引数が追加され、win 時に的中率/平均オッズ/Edge を表示する
    - run_backtest.py から gen.generate() と gen.save_ai_diagnostics() に betting_target が渡される
    - 全テストがパスする (既存テスト含む)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bet_history → JSON file | Python dict → ディスク上の JSON。JSON serialization は型安全性なし |
| report.py → HTML template | ユーザー提供データの HTML 表示。XSS 対策は Jinja2 autoescape が既に有効 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | report.html | accept | Jinja2 autoescape=True が既に有効 (report.py line 52)。bet_history はローカルファイル、リモートアクセスなし |
| T-09-02 | Tampering | ai_diagnostics.json | accept | ローカル出力ファイル。改竄リスクは他のフェーズと同様に低い |
</threat_model>

<verification>
1. `python -m pytest tests/test_backtest_report.py -v` — 全テストパス
2. `grep -c "win_selection_ev" src/backtest/engine.py` — 1以上 (フィールド追加確認)
3. `grep -c "_compute_regime_stats" src/backtest/report.py` — 2以上 (定義+呼び出し)
4. `grep -c "odds_multiplier_bands" src/backtest/report.py` — 3以上 (定義+戻り値+テンプレート)
5. `grep -c "save_ai_diagnostics" src/backtest/report.py` — 2以上 (定義+呼び出し)
6. `grep -c "betting_target" src/backtest/templates/report.html` — 3以上 (ガード条件)
7. `python -c "from backtest.report import BacktestReportGenerator; g = BacktestReportGenerator.__new__(BacktestReportGenerator); print(hasattr(g, '_compute_regime_stats'))"` — True
</verification>

<success_criteria>
1. バックテスト結果の bet_history JSON に win_selection_ev/edge/prob, win_gate_score, conformal_confidence_score, tanoddslow, regime フィールドが含まれる
2. HTML レポートに Regime 別、オッズ倍率帯別の ROI テーブルが表示される (win モード時)
3. AI 診断 JSON (ai_diagnostics.json) が保存され、highlights セクションに改善点自動特定データが含まれる
4. CLI 標準出力に単勝的中率/平均オッズ/Edge 統計が表示される (win モード時)
5. place/wide モードのバックテストで既存の出力が変更されない (既存テスト全パス)
6. 全テストがパスする
</success_criteria>

<output>
After completion, create `.planning/phases/09-win-reporting/09-01-SUMMARY.md`
</output>
