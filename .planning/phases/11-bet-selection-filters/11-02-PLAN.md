---
phase: 11-bet-selection-filters
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/backtest/engine.py
  - src/backtest/report.py
  - tests/test_backtest_engine.py
  - tests/test_backtest_report.py
autonomous: true
requirements:
  - BSEL-01
  - BSEL-02
  - BSEL-03

must_haves:
  truths:
    - "COLLAPSED レジームのレースはベットが完全スキップされ、スキップ件数がカウントされる"
    - "OddsBandFilter が候補選択後に呼び出され、赤字バンドの候補が除外される"
    - "フィルター適用順序: COLLAPSED skip (race-level) -> EV filter (candidate-level) -> OddsBandFilter (candidate-level)"
    - "残存ベット数が年間1,000件未満の場合 WARNING が出力される (D-10)"
    - "除外統計 (EV除外/COLLAPSEDスキップ/オッズバンド除外) がログ+レポートに出力される"
  artifacts:
    - path: "src/backtest/engine.py"
      provides: "COLLAPSED skip + OddsBandFilter統合 + 除外カウンター + bet count guard + BacktestResult除外フィールド"
      contains: "n_collapsed_skipped"
    - path: "src/backtest/report.py"
      provides: "除外統計のレポート表示 + AI診断セクション拡張"
      contains: "exclusion_stats"
  key_links:
    - from: "src/backtest/engine.py"
      to: "src/betting/odds_band_filter.py"
      via: "OddsBandFilter.calibrate() + filter() in race loop"
      pattern: "OddsBandFilter"
    - from: "src/backtest/engine.py"
      to: "src/models/regime_detector.py"
      via: "regime_params.get('skip', False) check"
      pattern: "regime_params.*skip"
    - from: "src/backtest/report.py"
      to: "src/backtest/engine.py"
      via: "BacktestResult exclusion fields"
      pattern: "exclusion_stats|n_collapsed"
---

<objective>
BacktestEngine に全フィルターを統合し、除外統計のレポート出力を実装する。

Purpose: Plan 01で作成したフィルターコンポーネント (OddsBandFilter, EV filter, regime skip=True) をBacktestEngine.run()のレースループに統合する。フィルター連鎖 (D-09)、除外カウンター、ベット数ガード (D-10)、レポート除外統計表示を実装。

Output: engine.py (COLLAPSED skip + OddsBandFilter + counters + BacktestResult), report.py (exclusion stats display)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-bet-selection-filters/11-CONTEXT.md
@.planning/phases/11-bet-selection-filters/11-RESEARCH.md
@.planning/phases/11-bet-selection-filters/11-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs — executor MUST read these files first -->

From src/betting/odds_band_filter.py (created in Plan 01):
```python
class OddsBandFilter:
    BANDS = [(1.0, 3.0), (3.0, 10.0), (10.0, 30.0), (30.0, float("inf"))]
    BAND_NAMES = ["1.0-3.0", "3.0-10.0", "10.0-30.0", "30.0+"]

    def calibrate(self, bet_history: list[dict[str, Any]]) -> None: ...
    def filter(self, candidate_df: pd.DataFrame, odds_col: str = "tanodds") -> pd.DataFrame: ...
    @property
    def excluded_bands(self) -> dict[str, dict[str, Any]]: ...
```

From src/backtest/race_predictor.py (modified in Plan 01):
```python
# get_win_candidates() now includes EV_lower_win_corrected >= 1.0 filter
# NaN fallback via fillna(1.0) >= 1.0
```

From src/models/regime_detector.py (modified in Plan 01):
```python
# get_strategy_params(RegimeState.COLLAPSED) returns dict with "skip": True
```

From src/backtest/engine.py (current, to be modified):
```python
# Line 56: class BacktestResult -- dataclass with total_bets, total_stake, etc.
# Line 600: recent_stats_list: list[dict[str, float]] = []
# Line 603: for race_id in race_ids:  (race loop start)
# Line 682-687: regime detection
# Line 687: regime_params = self.models.regime_detector.get_strategy_params(regime)
# Line 689-702: candidate selection (get_win_candidates call)
# Line 965-991: recent_stats_list.append() -- MUST happen before COLLAPSED skip
# Line 996-1026: ROI calculation and BacktestResult construction
```

From src/backtest/report.py (current, to be modified):
```python
# Line 24-50: generate() method
# Line 90-193: save_ai_diagnostics() method
# Line 295-416: _compute_condition_stats() with odds band pattern
# BacktestReportGenerator.__init__(output_dir)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Engine統合 — COLLAPSED skip + OddsBandFilter + counters + BacktestResult</name>
  <files>src/backtest/engine.py, tests/test_backtest_engine.py</files>
  <read_first>
    - src/backtest/engine.py (全文 — レースループ、BacktestResult dataclass、__init__)
    - src/betting/odds_band_filter.py (Plan 01で作成 — インターフェース確認)
    - tests/test_backtest_engine.py (既存テストパターン — mock_setup fixture)
  </read_first>
  <behavior>
    - Test 1: COLLAPSED regime のレースでベットが0件になり、n_collapsed_skipped がインクリメントされる
    - Test 2: regime_params["skip"] == True のレースが recent_stats_list 蓄積後にスキップされる (Pitfall 3対策)
    - Test 3: OddsBandFilter.filter() が候補選択後に呼び出される
    - Test 4: 残存ベット数 < 1000/年で WARNING ログが出力される
    - Test 5: BacktestResult に exclusion_stats フィールドが含まれる
    - Test 6: OddsBandFilter が __init__ で初期化され、run() で使用される
  </behavior>
  <action>
**Step 1: Extend BacktestResult dataclass**

In `src/backtest/engine.py`, add exclusion fields to `BacktestResult` dataclass (after line 72):

```python
    # Phase 11: Bet selection filter exclusion stats
    n_collapsed_skipped: int = 0          # D-11: COLLAPSED regime skip count
    n_ev_excluded: int = 0                # D-01: EV filter exclusion count
    n_odds_band_excluded: int = 0         # D-06: OddsBandFilter exclusion count
    exclusion_stats: dict[str, Any] = field(default_factory=dict)  # Full exclusion breakdown
```

**Step 2: Add OddsBandFilter import and initialization**

In engine.py imports section, add:
```python
from betting.odds_band_filter import OddsBandFilter
```

In `BacktestEngine.__init__()` (around line 340), add after betting_target assignment:
```python
        # Phase 11: Bet selection filters
        self._odds_band_filter: OddsBandFilter | None = None
        if betting_target == "win":
            self._odds_band_filter = OddsBandFilter()
```

**Step 3: Add filter counters before race loop**

In `run()` method, before the `for race_id in race_ids:` loop (around line 602), add:
```python
        # Phase 11: Bet selection filter counters
        n_collapsed_skipped = 0
        n_ev_excluded = 0
        n_odds_band_excluded = 0
        n_total_candidates = 0
        training_bet_buffer: list[dict[str, Any]] = []  # D-05: training period bets for OddsBandFilter
```

**Step 4: COLLAPSED skip after stats accumulation (D-11, D-09)**

CRITICAL: The COLLAPSED skip must happen AFTER `recent_stats_list.append()` (line 965-991) to prevent regime detector from getting stuck (Pitfall 3 in RESEARCH.md).

The insertion point is AFTER line 991 (recent_stats_list.append) and BEFORE line 993 (diagnostic log save). However, since the current loop continues naturally, we need to restructure slightly.

Actually, the better approach: add the COLLAPSED skip check AFTER `recent_stats_list.append()` but BEFORE the bet settlement code. Looking at the current flow:

```
Line 687: regime_params = get_strategy_params(regime)
Line 689-702: candidate selection
... bet processing and settlement ...
Line 965-991: recent_stats_list.append()
... continue to next race
```

The problem: if we skip at line 687 (right after regime detection), stats won't accumulate. If we skip at line 991 (after stats), bets are already settled.

The solution: Check `regime_params.get("skip", False)` AFTER regime detection (line 687) but move `recent_stats_list.append()` to BEFORE the skip check. Specifically:

After line 687 (`regime_params = ...`), insert stats accumulation:
```python
            # D-11: 統計を蓄積 (COLLAPSEDスキップ前に行う — レジーム遷移に必要)
            row_data = result_df.iloc[0] if not result_df.empty else {}
            recent_stats_list.append({
                "market_error_std": (
                    float(result_df["signed_log_error_win"].std())
                    if "signed_log_error_win" in result_df.columns and len(result_df) > 1
                    else 0.2
                ),
                "market_error_mean": (
                    float(result_df["signed_log_error_win"].mean())
                    if "signed_log_error_win" in result_df.columns
                    else 0.0
                ),
                "overround_rolling": float(row_data.get("overround", 0.20))
                    if not result_df.empty else 0.20,
                "entropy_rolling": float(row_data.get("market_entropy", 2.0))
                    if not result_df.empty else 2.0,
                "odds_skewness_rolling": calc_odds_skewness(result_df),
                "favorite_implied_prob_rolling": calc_favorite_implied_prob(result_df),
                "odds_volatility_mean": (
                    float(result_df["odds_volatility"].mean())
                    if "odds_volatility" in result_df.columns and not result_df.empty
                    else 0.1
                ),
                "field_size_mean": float(row_data.get("field_size", 14.0))
                    if not result_df.empty else 14.0,
            })

            # D-11: COLLAPSED regime skip (race-level, D-09: filter order #1)
            if regime_params.get("skip", False):
                n_collapsed_skipped += 1
                continue
```

Then REMOVE the duplicate `recent_stats_list.append()` block at lines 965-991 (it's now moved earlier).

**Step 5: OddsBandFilter call after candidate selection (D-09: filter order #3)**

After candidate selection (after line 702 `candidate_df = ...`), add OddsBandFilter call:

```python
            # D-09: OddsBandFilter (candidate-level, filter order #3)
            n_candidates_before_band = len(candidate_df)
            if (
                self._odds_band_filter is not None
                and not candidate_df.empty
            ):
                candidate_df = self._odds_band_filter.filter(candidate_df)
                n_odds_band_excluded += n_candidates_before_band - len(candidate_df)
            n_total_candidates += n_candidates_before_band
```

**Step 6: Accumulate training bets for OddsBandFilter calibration (D-05)**

After bet settlement (inside the bet settlement loop), accumulate training bets. The calibration happens on the first batch. Add after `training_bet_buffer` is populated sufficiently.

Actually, looking at the architecture more carefully: `BacktestEngine.run()` only receives test dates. For D-05 (training period data), the caller (`run_backtest.py`) needs to provide training bet data. But changing the engine signature is complex.

Simpler approach (per RESEARCH.md Option A with warm-up adaptation): calibrate the OddsBandFilter from the first N races of the test period. This naturally prevents look-ahead since only past data is used.

Add a calibration trigger after accumulating enough races. After the bet settlement loop, add:

```python
            # D-05: OddsBandFilter キャリブレーション用バッファ蓄積
            if self._odds_band_filter is not None:
                training_bet_buffer.extend(bet_history_for_race)  # settled bets for this race
                if (
                    not self._odds_band_filter._excluded_bands
                    and len(training_bet_buffer) >= 200
                ):
                    self._odds_band_filter.calibrate(training_bet_buffer)
```

Wait -- this uses test period data for calibration which is look-ahead for the first 200 races. But D-05 says "training period data". The engine.run() doesn't have training data access.

Best approach: Accept training bet history as an optional parameter to `run()`. This is cleaner and aligns with D-05. Modify the `run()` signature:

```python
def run(
    self,
    test_start: str,
    test_end: str,
    training_bet_history: list[dict[str, Any]] | None = None,  # D-05
) -> BacktestResult:
```

In the race loop, calibrate before the first candidate selection:

```python
        # D-05: OddsBandFilter キャリブレーション (トレーニング期間データ)
        if self._odds_band_filter is not None and training_bet_history:
            self._odds_band_filter.calibrate(training_bet_history)
```

This is the cleanest approach. The caller (`run_backtest.py`) can optionally provide training bet data. If not provided, OddsBandFilter stays inactive (no excluded bands).

**Step 7: Log exclusion stats at end of race loop (D-04, D-08, D-10)**

Before the `return BacktestResult(...)` statement (line 1011), add:

```python
        # Phase 11: Bet selection filter summary logging
        logger.info(
            "Bet Selection Filters: EV_excluded=%d, COLLAPSED_skipped=%d, "
            "OddsBand_excluded=%d",
            n_ev_excluded,
            n_collapsed_skipped,
            n_odds_band_excluded,
        )
        if self._odds_band_filter and self._odds_band_filter.excluded_bands:
            logger.info(
                "OddsBandFilter excluded bands: %s",
                self._odds_band_filter.excluded_bands,
            )

        # D-10: Bet count guard
        if total_bets > 0:
            # Estimate test years from race dates
            n_test_years = max(1, int(round(total_bets / max(len(bet_history), 1) * 1)))  # rough
            # Simpler: parse from test_start/test_end
            try:
                from datetime import datetime as dt
                t_start = dt.strptime(test_start, "%Y-%m-%d")
                t_end = dt.strptime(test_end, "%Y-%m-%d")
                n_years = max(1, (t_end - t_start).days / 365.25)
            except (ValueError, TypeError):
                n_years = 1.0
            bets_per_year = total_bets / n_years
            if bets_per_year < 1000:
                logger.warning(
                    "Bet count guard: %.0f bets/year (below 1000 threshold). "
                    "Consider parameter adjustment in Phase 13.",
                    bets_per_year,
                )
```

**Step 8: Update BacktestResult construction**

Update the `return BacktestResult(...)` to include exclusion fields:

```python
        return BacktestResult(
            total_bets=total_bets,
            total_stake=total_stake,
            total_return=total_return,
            winning_bets=winning_bets,
            total_roi=total_return / total_stake if total_stake > 0 else 0.0,
            max_drawdown=max_dd,
            final_bankroll=bankroll,
            monthly_returns=monthly_returns,
            bet_history=bet_history,
            n_pre_post_odds_bets=n_pre_post_odds_bets,
            n_fallback_odds_bets=n_fallback_odds_bets,
            avg_edge=result_data.get("avg_edge", 0.0),
            min_edge=result_data.get("min_edge", 0.0),
            max_edge=result_data.get("max_edge", 0.0),
            n_collapsed_skipped=n_collapsed_skipped,
            n_ev_excluded=n_ev_excluded,
            n_odds_band_excluded=n_odds_band_excluded,
            exclusion_stats={
                "collapsed_skipped": n_collapsed_skipped,
                "ev_excluded": n_ev_excluded,
                "odds_band_excluded": n_odds_band_excluded,
                "total_candidates_evaluated": n_total_candidates,
                "odds_band_filter_excluded": (
                    self._odds_band_filter.excluded_bands
                    if self._odds_band_filter
                    else {}
                ),
            },
        )
```

**Tests:**

In `tests/test_backtest_engine.py`, add a new test class `TestBetSelectionFilters`:

Use the existing `mock_trained_models` fixture pattern. Mock the engine dependencies and verify:

1. COLLAPSED skip test: Set `models.regime_detector.get_strategy_params.return_value` to include `"skip": True`. Verify `n_collapsed_skipped` in result.

2. OddsBandFilter integration: Verify `self._odds_band_filter` is initialized when `betting_target="win"`.

3. Bet count guard: Test with mock bet history producing < 1000 bets/year and verify WARNING log.

4. BacktestResult exclusion fields: Verify new fields are populated.

Note: Full engine integration test may require significant mocking. Focus on the key behaviors:
- OddsBandFilter is created for win target
- COLLAPSED skip counter increments
- BacktestResult has exclusion fields
  </action>
  <acceptance_criteria>
    - engine.py BacktestResult dataclass に `n_collapsed_skipped: int = 0` が含まれる
    - engine.py BacktestResult dataclass に `exclusion_stats: dict[str, Any]` が含まれる
    - engine.py に `from betting.odds_band_filter import OddsBandFilter` import が含まれる
    - engine.py run() に `regime_params.get("skip", False)` チェックが含まれる
    - engine.py run() に `self._odds_band_filter.filter(candidate_df)` 呼び出しが含まれる
    - engine.py run() に bet count guard WARNING が含まれる
    - recent_stats_list.append() が COLLAPSED skip の前にある (Pitfall 3 対策)
    - `python -m pytest tests/test_backtest_engine.py -v` が全テストPASS
    - `ruff check src/backtest/engine.py` エラーなし
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_backtest_engine.py -v && ruff check src/backtest/engine.py</automated>
  </verify>
  <done>COLLAPSED skip + OddsBandFilter統合 + 除外カウンター + bet count guard が engine.py に実装済み。BacktestResult に除外フィールド追加済み。全テスト通過。</done>
</task>

<task type="auto">
  <name>Task 2: レポート除外統計表示 + AI診断拡張</name>
  <files>src/backtest/report.py, tests/test_backtest_report.py</files>
  <read_first>
    - src/backtest/report.py (全文 — generate(), save_ai_diagnostics(), _compute_condition_stats())
    - src/backtest/engine.py (lines 56-77 — BacktestResult dataclass with new exclusion fields)
    - tests/test_backtest_report.py (既存テストパターン)
  </read_first>
  <action>
**Part A: Extend BacktestReportGenerator.generate()**

In `src/backtest/report.py`, modify the `generate()` method to include exclusion stats in the summary dict passed to the template.

After line 48 (`"generated_at": ...`), add:
```python
            "exclusion_stats": {
                "collapsed_skipped": result.n_collapsed_skipped,
                "ev_excluded": result.n_ev_excluded,
                "odds_band_excluded": result.n_odds_band_excluded,
                "odds_band_filter_excluded": result.exclusion_stats.get(
                    "odds_band_filter_excluded", {}
                ),
            },
```

**Part B: Extend save_ai_diagnostics()**

In `save_ai_diagnostics()`, add an "exclusion" section to the diagnostic JSON. After the "highlights" section (around line 188), add:

```python
            "exclusion": {
                "collapsed_skipped": result.n_collapsed_skipped,
                "ev_excluded": result.n_ev_excluded,
                "odds_band_excluded": result.n_odds_band_excluded,
                "excluded_odds_bands": result.exclusion_stats.get(
                    "odds_band_filter_excluded", {}
                ),
                "bet_count_guard": (
                    "WARNING: below 1000 bets/year"
                    if result.total_bets > 0
                    and result.total_bets / max(
                        (
                            lambda: 1.0
                        )(),  # simplified -- use actual test period if available
                        1,
                    )
                    < 1000
                    else "OK"
                ),
            },
```

Actually, the bet count guard was already logged as a WARNING in engine.py. For the diagnostics JSON, just include the raw counts and let the consumer decide.

Simplify:
```python
            "exclusion": {
                "collapsed_skipped": result.n_collapsed_skipped,
                "ev_excluded": result.n_ev_excluded,
                "odds_band_excluded": result.n_odds_band_excluded,
                "excluded_odds_bands": result.exclusion_stats.get(
                    "odds_band_filter_excluded", {}
                ),
                "total_candidates_evaluated": result.exclusion_stats.get(
                    "total_candidates_evaluated", 0
                ),
            },
```

**Part C: Tests**

In `tests/test_backtest_report.py`, add tests that verify:

1. `generate()` output includes exclusion_stats when BacktestResult has exclusion data
2. `save_ai_diagnostics()` output includes "exclusion" section with collapsed_skipped, ev_excluded, odds_band_excluded
3. Missing exclusion data (default BacktestResult) doesn't break report generation

Create a helper to build a BacktestResult with exclusion data:
```python
def _make_result_with_exclusions() -> BacktestResult:
    return BacktestResult(
        total_bets=10,
        total_stake=1000.0,
        total_return=1100.0,
        winning_bets=3,
        n_collapsed_skipped=5,
        n_ev_excluded=20,
        n_odds_band_excluded=8,
        exclusion_stats={
            "collapsed_skipped": 5,
            "ev_excluded": 20,
            "odds_band_excluded": 8,
            "total_candidates_evaluated": 100,
            "odds_band_filter_excluded": {"10.0-30.0": {"roi": 0.65, "count": 45}},
        },
    )
```
  </action>
  <acceptance_criteria>
    - report.py generate() に `exclusion_stats` キーを含む summary dict が構築される
    - report.py save_ai_diagnostics() 出力に `"exclusion"` キーが含まれる
    - report.py save_ai_diagnostics() 出力に `collapsed_skipped`, `ev_excluded`, `odds_band_excluded` が含まれる
    - `python -m pytest tests/test_backtest_report.py -v` 全テストPASS
    - `ruff check src/backtest/report.py` エラーなし
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_backtest_report.py -v && ruff check src/backtest/report.py</automated>
  </verify>
  <done>レポートに除外統計 (EV除外/COLLAPSEDスキップ/オッズバンド除外) が表示される。AI診断JSON に exclusion セクションが含まれる。全テスト通過。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Engine → OddsBandFilter | Training bet history passed from caller; look-ahead bias prevention relies on caller providing correct data |
| Engine → Report | BacktestResult fields are internal; no untrusted external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Information Disclosure (look-ahead) | engine.run(training_bet_history) | mitigate | training_bet_history parameter explicitly documents "must be training period only"; caller (run_backtest.py) controls data source |
| T-11-04 | Denial of Service | Bet count guard | accept | WARNING only, no auto-relaxation per D-10 |
</threat_model>

<verification>
1. `python -m pytest tests/test_backtest_engine.py tests/test_backtest_report.py -v` 全通過
2. `ruff check src/backtest/engine.py src/backtest/report.py` エラーなし
3. `python -m pytest tests/ -v --co -q` でテスト収集エラーなし (全テスト互換性確認)
</verification>

<success_criteria>
- COLLAPSED skip が recent_stats_list 蓄積後に実行される (Pitfall 3 対策)
- フィルター適用順序が D-09 通り (COLLAPSED → EV → OddsBand)
- OddsBandFilter が calibrate → filter の流れで engine.run() に統合
- Bet count guard WARNING が 1000件/年未満で出力
- BacktestResult に除外フィールド (n_collapsed_skipped, n_ev_excluded, n_odds_band_excluded, exclusion_stats) 追加
- レポート/AI診断に除外統計表示
- ruff/mypy エラーなし
</success_criteria>

<output>
After completion, create `.planning/phases/11-bet-selection-filters/11-02-SUMMARY.md`
</output>
