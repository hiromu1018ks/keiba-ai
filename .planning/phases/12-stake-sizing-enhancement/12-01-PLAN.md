---
phase: 12-stake-sizing-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/betting/stake_calculator.py
  - tests/test_stake_calculator.py
autonomous: true
requirements: [SIZE-01, SIZE-02]

must_haves:
  truths:
    - "StakeCalculator はコンストラクタで fractional_kelly, kelly_fraction_cap, target_ev, max_scale を受け取る"
    - "AGGRESSIVE(fk=0.50) の stake > CONSERVATIVE(fk=0.25) の stake > COLLAPSED(fk=0.00) の stake (=0)"
    - "apply_ev_scaling(stake, ev) は ev > target_ev で stake を拡大し、ev < target_ev で縮小する"
    - "max_scale=2.0 で拡大が2倍にキャップされる"
  artifacts:
    - path: "src/betting/stake_calculator.py"
      provides: "コンストラクタ注入 + apply_ev_scaling()"
      contains: "def apply_ev_scaling"
    - path: "tests/test_stake_calculator.py"
      provides: "レジーム別Kellyテスト + EV乗算テスト"
      contains: "test_apply_ev_scaling"
  key_links:
    - from: "calc_stake()"
      to: "self.fractional_kelly"
      via: "インスタンス変数参照"
      pattern: "self\\.fractional_kelly"
    - from: "apply_ev_scaling()"
      to: "self.target_ev, self.max_scale"
      via: "EV比例計算"
      pattern: "min\\(ev / self\\.target_ev"
---

<objective>
StakeCalculator のハードコード定数をコンストラクタ注入にリファクタリングし、EV比例乗算器 apply_ev_scaling() を追加する

Purpose: レジーム別Kelly分数(SIZE-01)とEV比例乗算器(SIZE-02)の計算基盤を構築する。Phase 13 Optuna最適化に向けたパラメータ注入の準備でもある。
Output: リファクタリング済み StakeCalculator + 包括的テストスイート
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-stake-sizing-enhancement/12-CONTEXT.md
@.planning/phases/12-stake-sizing-enhancement/12-RESEARCH.md
@src/betting/stake_calculator.py
@tests/test_stake_calculator.py
@src/domain/types.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StakeCalculator コンストラクタ注入 + apply_ev_scaling() 実装</name>
  <files>src/betting/stake_calculator.py, tests/test_stake_calculator.py</files>
  <read_first>
    src/betting/stake_calculator.py (全行 — 現在のハードコード定数とcalc_stake()ロジック)
    tests/test_stake_calculator.py (全行 — 既存テストのアサーション、特にline 94の calc.FRACTIONAL_KELLY)
    src/domain/types.py (BetType enum)
  </read_first>
  <behavior>
    - Test 1: デフォルトコンストラクタ StakeCalculator() で fractional_kelly=0.5, kelly_fraction_cap=0.25, target_ev=1.10, max_scale=2.0 が設定される
    - Test 2: StakeCalculator(fractional_kelly=0.50) の calc_stake が既存テストと同じ結果を返す (後方互換性)
    - Test 3: StakeCalculator(fractional_kelly=0.25) の stake が fractional_kelly=0.50 の stake の約半分になる
    - Test 4: StakeCalculator(fractional_kelly=0.00) の calc_stake が 0.0 を返す (COLLAPSED)
    - Test 5: effective_cap = kelly_fraction_cap * fractional_kelly が正しく計算される (fk=0.50: cap=0.125, fk=0.25: cap=0.0625)
    - Test 6: apply_ev_scaling(1000, ev=1.10) → scale=1.0 → 1000.0 (target_ev境界)
    - Test 7: apply_ev_scaling(1000, ev=2.20) → scale=2.0 → 2000.0 (max_scale境界)
    - Test 8: apply_ev_scaling(1000, ev=1.50) → scale=1.3636... → 1363.6... (中間値)
    - Test 9: apply_ev_scaling(1000, ev=0.80) → scale=0.7272... → 727.2... (低EV縮小)
    - Test 10: apply_ev_scaling(0, ev=1.50) → 0.0 (stake=0ガード)
    - Test 11: apply_ev_scaling(1000, ev=nan) → 1000.0 (NaNガード)
    - Test 12: apply_ev_scaling(1000, ev=0) → 1000.0 (ev<=0ガード)
    - Test 13: apply_ev_scaling(1000, ev=-1.0) → 1000.0 (負のevガード)
  </behavior>
  <action>
    【red】まずテストを書く:

    tests/test_stake_calculator.py に以下のテストクラス・メソッドを追加する (既存テストは残す):

    1. TestConstructorInjection クラス:
       - test_default_constructor: StakeCalculator() の fractional_kelly==0.5, kelly_fraction_cap==0.25, target_ev==1.10, max_scale==2.0 を確認
       - test_custom_fractional_kelly: StakeCalculator(fractional_kelly=0.25) の fractional_kelly==0.25 を確認
       - test_backward_compatible: StakeCalculator() の calc_stake(edge=0.06, odds=5.0, bankroll=100000, bet_type=BetType.PLACE) == 700.0 (既存テストと同じ)

    2. TestRegimeBasedKelly クラス:
       - test_aggressive_half_kelly: StakeCalculator(fractional_kelly=0.50) の calc_stake(edge=0.06, odds=5.0, bankroll=100000, BetType.PLACE) == 700.0
       - test_conservative_quarter_kelly: StakeCalculator(fractional_kelly=0.25) の calc_stake(edge=0.06, odds=5.0, bankroll=100000, BetType.PLACE) == 300.0 (kelly=0.015*0.25=0.00375, cap=0.0625, raw=375, floor=300)
       - test_collapsed_zero_kelly: StakeCalculator(fractional_kelly=0.00) の calc_stake(edge=0.06, odds=5.0, bankroll=100000, BetType.PLACE) == 0.0
       - test_aggressive_larger_than_conservative: 上記の aggressive stake > conservative stake を確認

    3. TestEvScaling クラス:
       - test_target_ev_boundary: calc=StakeCalculator(target_ev=1.10); calc.apply_ev_scaling(1000.0, ev=1.10) == 1000.0
       - test_max_scale_boundary: calc=StakeCalculator(target_ev=1.10, max_scale=2.0); calc.apply_ev_scaling(1000.0, ev=2.20) == 2000.0
       - test_mid_scale: calc.apply_ev_scaling(1000.0, ev=1.50) → scale=1.50/1.10=1.3636...; abs(result - 1363.636...) < 0.01
       - test_low_ev_shrink: calc.apply_ev_scaling(1000.0, ev=0.80) → scale=0.80/1.10=0.7272...; abs(result - 727.272...) < 0.01
       - test_zero_stake: calc.apply_ev_scaling(0.0, ev=1.50) == 0.0
       - test_nan_ev: calc.apply_ev_scaling(1000.0, ev=float('nan')) == 1000.0
       - test_zero_ev: calc.apply_ev_scaling(1000.0, ev=0.0) == 1000.0
       - test_negative_ev: calc.apply_ev_scaling(1000.0, ev=-1.0) == 1000.0

    【green】次に src/betting/stake_calculator.py を実装:

    1. クラス属性の変更:
       - `FRACTIONAL_KELLY: float = 0.5` を削除し、`__init__` の引数にする
       - `KELLY_FRACTION_CAP: float = 0.25` を削除し、`__init__` の引数にする
       - `MIN_EDGE_THRESHOLD`, `RACE_EXPOSURE_CAP`, `MIN_STAKE`, `MAX_STAKE` はクラス属性のまま維持 (D-03, D-04)
       - `__init__` に `target_ev: float = 1.10` と `max_scale: float = 2.0` も追加 (D-05)

    2. __init__ メソッド:
       ```python
       def __init__(
           self,
           fractional_kelly: float = 0.5,
           kelly_fraction_cap: float = 0.25,
           target_ev: float = 1.10,
           max_scale: float = 2.0,
       ) -> None:
           self.fractional_kelly = fractional_kelly
           self.kelly_fraction_cap = kelly_fraction_cap
           self.target_ev = target_ev
           self.max_scale = max_scale
       ```

    3. calc_stake() の修正:
       - `self.FRACTIONAL_KELLY` → `self.fractional_kelly` に変更 (line 64)
       - `self.KELLY_FRACTION_CAP` → `self.kelly_fraction_cap` に変更 (line 67)
       - `effective_cap = self.kelly_fraction_cap * self.fractional_kelly` に変更 (line 67)
       - それ以外のロジックは変更なし

    4. apply_ev_scaling() メソッドの追加 (D-06, D-07):
       ```python
       def apply_ev_scaling(self, stake: float, ev: float) -> float:
           """EV比例乗算器 (D-06/D-07).

           scale = min(ev / target_ev, max_scale)
           EV < target_ev → scale < 1.0 → 縮小
           EV >= target_ev → scale >= 1.0 → 拡大 (max_scale まで)
           """
           if stake <= 0 or math.isnan(ev) or ev <= 0:
               return stake
           scale = min(ev / self.target_ev, self.max_scale)
           return stake * scale
       ```

    5. 既存テスト test_calc_stake_fractional_kelly_constants を修正:
       - `calc.FRACTIONAL_KELLY == 0.5` → `calc.fractional_kelly == 0.5` に変更
       - `calc.KELLY_FRACTION_CAP == 0.25` → `calc.kelly_fraction_cap == 0.25` に変更

    【refactor】必要に応じてリファクタリング:
    - 型アノテーションが mypy strict に適合していることを確認
    - ruff check/src/betting/stake_calculator.py が通ることを確認
  </action>
  <verify>
    <automated>python -m pytest tests/test_stake_calculator.py -v</automated>
  </verify>
  <done>
    - StakeCalculator に __init__ が追加され、fractional_kelly, kelly_fraction_cap, target_ev, max_scale を受け取る
    - calc_stake() 内の self.FRACTIONAL_KELLY 参照が self.fractional_kelly に置換されている
    - calc_stake() 内の self.KELLY_FRACTION_CAP 参照が self.kelly_fraction_cap に置換されている
    - effective_cap = self.kelly_fraction_cap * self.fractional_kelly が正しく計算される
    - apply_ev_scaling() が追加され、EV比例乗算が動作する
    - fractional_kelly=0.00 で calc_stake() が 0.0 を返す
    - 全テストが通過する (既存 + 新規)
    - ruff check src/betting/stake_calculator.py が通過する
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 内部計算のみ | 本プランは純粋な計算ロジックのリファクタリング。外部入力なし |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | I | apply_ev_scaling | accept | 純粋関数。外部入力なし。引数は型チェック済み |
| T-12-02 | D | calc_stake | mitigate | fractional_kelly=0.00 で stake=0 を返すことをテストで確認 |
</threat_model>

<verification>
1. `python -m pytest tests/test_stake_calculator.py -v` — 全テスト通過
2. `ruff check src/betting/stake_calculator.py` — リント通過
3. `python -c "from betting.stake_calculator import StakeCalculator; c=StakeCalculator(fractional_kelly=0.25); print(c.calc_stake(0.06, 5.0, 100000, __import__('domain.types',fromlist=['BetType']).BetType.PLACE))"` → 300.0 が出力されることを確認
</verification>

<success_criteria>
- StakeCalculator がコンストラクタ注入可能で、デフォルト値で後方互換性がある
- AGGRESSIVE(fk=0.50) > CONSERVATIVE(fk=0.25) > COLLAPSED(fk=0.00) のstake順序が確認される
- apply_ev_scaling() が EV比例乗算を正しく実装する (scale = min(ev/target_ev, max_scale))
- 全テスト通過、リント通過
</success_criteria>

<output>
After completion, create `.planning/phases/12-stake-sizing-enhancement/12-01-SUMMARY.md`
</output>
