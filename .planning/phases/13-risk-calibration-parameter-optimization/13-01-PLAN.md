---
phase: 13-risk-calibration-parameter-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/types.py
  - src/domain/models.py
  - src/betting/drawdown_controller.py
  - src/backtest/engine.py
  - tests/test_drawdown_controller.py
autonomous: true
requirements: [RISK-01]

must_haves:
  truths:
    - "DrawdownControllerはDD%のみで3段階(NORMAL/REDUCED/STOP)を判定し、ROI計算を一切行わない"
    - "コンストラクタ注入でrolling_window, dd_thresholds, multipliers, min_stay_races等の全パラメータを外部から設定可能"
    - "DDConfigの__post_init__がdd_threshold_2 > dd_threshold_1を検証し不正な閾値を拒否する"
    - "ヒステリシスバンドにより状態遷移に最低滞在レース数が課され、発振を防止する"
    - "リカバリはSTOP→REDUCED→NORMALと段階的で即時復帰しない"
    - "BacktestEngine.__init__がstrategy_paramsを受け取り、DDConfig/StakeCalculatorを注入できる"
  artifacts:
    - path: "src/betting/drawdown_controller.py"
      provides: "DDConfig dataclass + ROI除去済みDrawdownController"
      contains: "class DDConfig"
    - path: "src/domain/types.py"
      provides: "RecoveryState enum (NORMAL, REDUCED, STOP)"
      contains: "STOP = "
    - path: "src/domain/models.py"
      provides: "DDState dataclass (rolling_roiフィールド除去)"
      contains: "class DDState"
    - path: "src/backtest/engine.py"
      provides: "strategy_params対応 + WIN/PLACE別DDControllerインスタンス生成"
      contains: "strategy_params"
    - path: "tests/test_drawdown_controller.py"
      provides: "DD%ベース3段階制御の全テスト"
      min_lines: 100
  key_links:
    - from: "src/backtest/engine.py"
      to: "src/betting/drawdown_controller.py"
      via: "import DDConfig, DrawdownController"
      pattern: "from betting.drawdown_controller import"
    - from: "src/backtest/engine.py"
      to: "src/betting/stake_calculator.py"
      via: "strategy_params注入でStakeCalculator構築"
      pattern: "StakeCalculator"
    - from: "src/betting/drawdown_controller.py"
      to: "src/domain/types.py"
      via: "RecoveryState enum参照"
      pattern: "from domain.types import RecoveryState"
---

<objective>
DrawdownControllerをROI依存から完全に解放し、DD%のみの3段階制御(NORMAL/REDUCED/STOP)に再設計する。同時にBacktestEngineにstrategy_params注入を追加し、Plan 03のOptuna最適化がengineパラメータを直接制御できるようにする。

Purpose: WIN的中率10%環境ではROIがノイジーすぎてDD制御信号として不適切。DD%は銀行ロール健全性の直接的指標であり信頼性が高い。全パラメータをコンストラクタ注入に移行し、Phase 13-03のOptuna最適化を可能にする。

Output: DDConfig dataclass、ROI除去済みDrawdownController、RecoveryState enum更新、DDState rolling_roi除去、BacktestEngine strategy_params注入、WIN/PLACE別インスタンス管理、全テスト書き直し
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-risk-calibration-parameter-optimization/13-CONTEXT.md
@.planning/phases/13-risk-calibration-parameter-optimization/13-RESEARCH.md
@.planning/phases/12-stake-sizing-enhancement/12-CONTEXT.md
</context>

<interfaces>

From src/domain/types.py (current -- will be modified):
```python
class RecoveryState(str, Enum):
    NORMAL = "normal"
    REDUCED = "reduced"
    RECOVERING = "recovering"  # -> STOP に変更
```

From src/domain/models.py (current -- will be modified):
```python
@dataclass
class DDState:
    current_dd: float
    rolling_roi: float      # -> 除去
    n_bets_eval: int
    recovery_state: RecoveryState = RecoveryState.NORMAL
```

From src/betting/stake_calculator.py (Phase 12成果 -- 参照のみ、変更なし):
```python
class StakeCalculator:
    def __init__(self, fractional_kelly: float = 0.50, target_ev: float = 1.10, max_scale: float = 2.0) -> None:
```

From src/backtest/engine.py lines 338-371 (current -- will be modified):
```python
class BacktestEngine:
    def __init__(
        self,
        models: TrainedModelsV5,
        initial_bankroll: float = 100_000,
        store: ParquetStore | None = None,
        betting_mode: str = "flat",
        diag_prefix: str = "bt",
        betting_target: str = "win",
    ) -> None:
        ...
        if betting_mode == "kelly":
            from betting.drawdown_controller import DrawdownController
            from betting.stake_calculator import StakeCalculator

            self._race_predictor = RacePredictor(
                models,
                stake_calculator=StakeCalculator(),
                dd_controller=DrawdownController(peak_bankroll=initial_bankroll),
            )
```

From src/backtest/race_predictor.py (参照のみ、変更なし):
```python
class RacePredictor:
    def __init__(
        self,
        models: TrainedModelsV5,
        *,
        stake_calculator: StakeCalculator | None = None,
        dd_controller: DrawdownController | None = None,
        alpha: float = 0.4,
    ) -> None:
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: DrawdownController再設計 -- DDConfig dataclass + __post_init__検証 + ROI除去 + 3段階制御 + ヒステリシス</name>
  <files>src/betting/drawdown_controller.py, src/domain/types.py, src/domain/models.py</files>
  <read_first>
    src/betting/drawdown_controller.py
    src/domain/types.py
    src/domain/models.py (lines 190-197 -- DDState dataclass)
    src/monitoring/model_monitor.py (lines 20-30, 80-113 -- rolling_roi参照箇所)
  </read_first>
  <action>
Per D-01, D-02, D-03, D-04, D-06:

1. **src/domain/types.py** -- RecoveryState enum を3段階に更新:
   - `RECOVERING = "recovering"` を `STOP = "stop"` に置換
   - NORMAL, REDUCED, STOP の3値のみにする

2. **src/domain/models.py** -- DDState dataclass から rolling_roi フィールドを除去:
   - `rolling_roi: float` 行を削除
   - フィールド: `current_dd: float`, `n_bets_eval: int`, `recovery_state: RecoveryState` のみ

3. **src/betting/drawdown_controller.py** -- 完全再設計:
   a. DDConfig dataclass をファイル先頭(from dataclasses import dataclass)に追加:
      ```python
      @dataclass
      class DDConfig:
          rolling_window: int = 400
          dd_threshold_1: float = 0.10    # NORMAL->REDUCED境界
          dd_threshold_2: float = 0.20    # REDUCED->STOP境界
          multiplier_normal: float = 1.0
          multiplier_reduced: float = 0.50
          multiplier_stop: float = 0.0
          min_stay_races: int = 10        # ヒステリシス最低滞在レース数
          max_adjustment_per_n_bets: int = 20
          max_adjustment_amount: float = 0.15

          def __post_init__(self) -> None:
              """T-13-01: 閾値整合性検証"""
              if self.dd_threshold_2 <= self.dd_threshold_1:
                  raise ValueError(
                      f"dd_threshold_2 ({self.dd_threshold_2}) must be > "
                      f"dd_threshold_1 ({self.dd_threshold_1})"
                  )
              if self.multiplier_normal <= 0:
                  raise ValueError(f"multiplier_normal must be > 0, got {self.multiplier_normal}")
              if not (0 <= self.multiplier_reduced < self.multiplier_normal):
                  raise ValueError(
                      f"multiplier_reduced must be in [0, multiplier_normal), "
                      f"got {self.multiplier_reduced}"
                  )
              if self.multiplier_stop != 0.0:
                  # STOP はベット停止を意味する。0.0以外の値は意図確認
                  logger.warning(
                      f"multiplier_stop={self.multiplier_stop} (expected 0.0 for bet stop)"
                  )
              if self.rolling_window < 1:
                  raise ValueError(f"rolling_window must be >= 1, got {self.rolling_window}")
              if self.min_stay_races < 1:
                  raise ValueError(f"min_stay_races must be >= 1, got {self.min_stay_races}")
      ```
   b. DrawdownController.__init__() を変更:
      - シグネチャ: `def __init__(self, peak_bankroll: float, cfg: DDConfig | None = None) -> None:`
      - `self.cfg = cfg or DDConfig()`
      - `self._state = RecoveryState.NORMAL`
      - `self._races_in_state = 0`
      - `self._current_multiplier = 1.0`
      - `self._multiplier_at_window_start = 1.0`
      - `self._bets_in_window = 0`
      - `self.peak_bankroll = peak_bankroll`
      - `self.bet_history` は不要(ROI計算用だったため除去)。`n_bets` カウンタを追加: `self.n_bets = 0`
   c. `update()` メソッド -- D-02 ROI完全除去:
      - シグネチャ変更: `def update(self, bankroll: float) -> None:` (bet_return引数を削除)
      - `if bankroll > self.peak_bankroll: self.peak_bankroll = bankroll`
      - `dd = (self.peak_bankroll - bankroll) / self.peak_bankroll`
      - `self.n_bets += 1`
      - `self._transition(dd)` を呼び出し
   d. `_transition()` 新規メソッド -- D-03 ヒステリシス + D-04 段階的リカバリ:
      ```python
      def _transition(self, dd: float) -> None:
          target = self._determine_target_state(dd)
          if target != self._state:
              # ヒステリシス: 最低滞在レース数未満なら遷移しない
              if self._races_in_state < self.cfg.min_stay_races:
                  return
              # D-04: 段階的リカバリ -- STOP->NORMAL への即時遷移を禁止
              if self._state == RecoveryState.STOP and target == RecoveryState.NORMAL:
                  target = RecoveryState.REDUCED  # 強制的にREDUCED経由
              old = self._state
              self._state = target
              self._races_in_state = 0
              self._update_multiplier()
              logger.info(f"DD: {old.value} -> {target.value}")
          else:
              self._races_in_state += 1
      ```
   e. `_determine_target_state()` 新規メソッド:
      ```python
      def _determine_target_state(self, dd: float) -> RecoveryState:
          if dd >= self.cfg.dd_threshold_2:
              return RecoveryState.STOP
          elif dd >= self.cfg.dd_threshold_1:
              return RecoveryState.REDUCED
          return RecoveryState.NORMAL
      ```
   f. `_update_multiplier()` 新規メソッド:
      ```python
      def _update_multiplier(self) -> None:
          if self._state == RecoveryState.NORMAL:
              self._current_multiplier = self.cfg.multiplier_normal
          elif self._state == RecoveryState.REDUCED:
              self._current_multiplier = self.cfg.multiplier_reduced
          else:
              self._current_multiplier = self.cfg.multiplier_stop
          self._multiplier_at_window_start = self._current_multiplier
          self._bets_in_window = 0
      ```
   g. 以下のメソッド/属性を完全に削除:
      - `_compute_rolling_roi()` / `_calc_rolling_roi()`
      - `_get_table_multiplier()`
      - `MULTIPLIER_TABLE` クラス定数
      - `ROLLING_WINDOW`, `EWMA_ALPHA`, `RECOVERY_INCREMENT`, `RECOVERY_ROI_THRESHOLD`, `RECOVERY_DD_THRESHOLD`, `RECOVERY_MAX_MULTIPLIER` クラス定数
      - `self.bet_history`
      - `import numpy as np` (ROI計算でのみ使用)
   h. `get_state()` -- rolling_roiフィールド除去に対応:
      ```python
      def get_state(self, bankroll: float) -> DDState:
          dd = (self.peak_bankroll - bankroll) / self.peak_bankroll
          return DDState(
              current_dd=dd,
              n_bets_eval=self.n_bets,
              recovery_state=self._state,
          )
      ```
   i. `get_multiplier()` は既存ロジック(MAX_ADJUSTMENT_PER_N_BETS)をcfg参照に変更:
      - `self.cfg.max_adjustment_per_n_bets` と `self.cfg.max_adjustment_amount` を使用
   j. `adjust_stake()` は変更なし(既存ロジックを維持)
   k. `log_state()` は rolling_roi 出力をDD%のみに変更:
      - `"DD: {dd:.1%} | Multiplier: {mult:.2f} | State: {state.value} | Peak: ... | Current: ..."` の形式

**注意:** `src/monitoring/model_monitor.py` の `rolling_roi` 参照は別モジュールのDDStateとは独立して独自計算しているため、このタスクでは変更不要。monitoring側はmodel_monitor内で独自にROI計算している。
  </action>
  <acceptance_criteria>
    1. `grep -c "rolling_roi" src/betting/drawdown_controller.py` returns 0
    2. `grep -c "_calc_rolling_roi\|_compute_rolling_roi" src/betting/drawdown_controller.py` returns 0
    3. `grep -c "MULTIPLIER_TABLE" src/betting/drawdown_controller.py` returns 0
    4. `grep -c "class DDConfig" src/betting/drawdown_controller.py` returns 1
    5. `grep "__post_init__" src/betting/drawdown_controller.py` にヒット(閾値検証)
    6. `grep "STOP" src/domain/types.py` contains `STOP = "stop"`
    7. `grep -c "rolling_roi" src/domain/models.py` returns 0 (DDState内)
    8. DrawdownController.__init__ が `cfg: DDConfig | None = None` 引数を持つ
    9. `update()` が `bankroll` のみの引数(bet_returnなし)
    10. `import numpy` が drawdown_controller.py に存在しない
    11. `python -c "from betting.drawdown_controller import DDConfig; DDConfig(dd_threshold_1=0.20, dd_threshold_2=0.10)"` が ValueError を送出すること
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/hirom/develop/keiba-ai && python -m pytest tests/test_drawdown_controller.py -x -v 2>&1 | head -5 || echo "Tests expected to fail until Task 2"</automated>
  </verify>
  <done>DrawdownControllerがDD%のみの3段階制御に再設計され、ROI依存が完全に除去されている。DDConfig dataclassが__post_init__検証付きで全パラメータを保持し、RecoveryState enumがNORMAL/REDUCED/STOPに更新されている</done>
</task>

<task type="auto">
  <name>Task 2: BacktestEngine strategy_params注入 + WIN/PLACE別DDController + 全テスト書き直し</name>
  <files>src/backtest/engine.py, tests/test_drawdown_controller.py</files>
  <read_first>
    src/backtest/engine.py (lines 327-375, lines 695-720)
    src/betting/drawdown_controller.py (Task 1完了後の新実装)
    tests/test_drawdown_controller.py
    src/domain/types.py (Task 1完了後)
    src/domain/models.py (Task 1完了後)
  </read_first>
  <action>
Per D-05, and to support Plan 03 StrategyOptimizer:

1. **src/backtest/engine.py** -- BacktestEngine.__init__ に strategy_params 引数を追加 + kelly mode セクション(lines 338-371)を変更:
   a. __init__ シグネチャに `strategy_params: dict[str, Any] | None = None` を追加(betting_target引数の後):
      ```python
      def __init__(
          self,
          models: TrainedModelsV5,
          initial_bankroll: float = 100_000,
          store: ParquetStore | None = None,
          betting_mode: str = "flat",
          diag_prefix: str = "bt",
          betting_target: str = "win",
          strategy_params: dict[str, Any] | None = None,
      ) -> None:
      ```
   b. `self.strategy_params = strategy_params` を保存
   c. kelly mode セクションを変更:
      ```python
      if betting_mode == "kelly":
          from betting.drawdown_controller import DDConfig, DrawdownController
          from betting.stake_calculator import StakeCalculator

          if strategy_params is not None:
              # Optuna最適化済みパラメータで注入
              dd_cfg = strategy_params.get("dd_config", DDConfig())
              stake_calc = StakeCalculator(
                  fractional_kelly=strategy_params.get("fractional_kelly", 0.5),
                  target_ev=strategy_params.get("target_ev", 1.10),
                  max_scale=strategy_params.get("max_scale", 2.0),
              )
              dd_ctrl = DrawdownController(
                  peak_bankroll=initial_bankroll,
                  cfg=dd_cfg,
              )
          else:
              stake_calc = StakeCalculator()
              dd_ctrl = DrawdownController(peak_bankroll=initial_bankroll)

          self._race_predictor = RacePredictor(
              models,
              stake_calculator=stake_calc,
              dd_controller=dd_ctrl,
          )
      ```
   d. 注: D-05 WIN/PLACE別インスタンスは現在の実装では betting_target="win" または "place" のいずれかで同時ベットはない。Plan 03のOptuna最適化時に dd_config を WIN用にカスタマイズして注入する。現時点ではDDConfig()デフォルト + strategy_params上書き。

2. **tests/test_drawdown_controller.py** -- 全15テストをROI非依存の新テストに全面書き直し。以下のテスト群を新設:

   a. **TestDDConfig** (新規):
      - `test_default_config_values`: DDConfig() の各フィールドデフォルト値検証
      - `test_custom_config`: DDConfig(rolling_window=500, dd_threshold_1=0.15, ...) でカスタム値設定検証
      - `test_post_init_rejects_threshold_2_leq_threshold_1`: DDConfig(dd_threshold_1=0.20, dd_threshold_2=0.10) で ValueError
      - `test_post_init_rejects_negative_multiplier`: DDConfig(multiplier_normal=-1) で ValueError
      - `test_post_init_rejects_reduced_geq_normal`: DDConfig(multiplier_reduced=1.5) で ValueError
      - `test_post_init_rejects_zero_rolling_window`: DDConfig(rolling_window=0) で ValueError
      - `test_post_init_rejects_zero_min_stay`: DDConfig(min_stay_races=0) で ValueError

   b. **TestDrawdownControllerCore**:
      - `test_initial_state_is_normal`: 初期 _state == NORMAL, multiplier == 1.0
      - `test_initial_multiplier_is_1`: get_multiplier() 初期値 == 1.0
      - `test_update_tracks_peak_bankroll`: update(110000) -> peak_bankroll == 110000
      - `test_dd_calculation`: peak=100000, update(90000) -> DD == 10%
      - `test_normal_to_reduced_transition`: updateでDD >= dd_threshold_1 -> state REDUCED (min_stay_races後)
      - `test_reduced_to_stop_transition`: DD >= dd_threshold_2 -> state STOP (min_stay_races後)
      - `test_stop_to_reduced_gradual_recovery`: STOP状態からDD改善 -> min_stay_races後にREDUCED(段階的)
      - `test_reduced_to_normal_recovery`: REDUCED状態からDD改善 -> min_stay_races後にNORMAL
      - `test_hysteresis_prevents_oscillation`: min_stay_races未満では遷移しない

   c. **TestDrawdownControllerMultiplier**:
      - `test_multiplier_normal`: NORMAL -> cfg.multiplier_normal (1.0)
      - `test_multiplier_reduced`: REDUCED -> cfg.multiplier_reduced
      - `test_multiplier_stop`: STOP -> cfg.multiplier_stop (0.0)
      - `test_adjust_stake_applies_multiplier`: adjust_stake(1000, 90000) -> 円単位丸め
      - `test_adjust_stake_zero_when_stop`: STOP -> adjust_stake == 0
      - `test_max_adjustment_rate_limits_change`: max_adjustment_per_n_bets / max_adjustment_amount 制限

   d. **TestDrawdownControllerGetState**:
      - `test_get_state_returns_ddstate`: get_state() が DDState(current_dd, n_bets_eval, recovery_state) を返す
      - `test_get_state_dd_accuracy`: DD%精度検証
      - `test_log_state_no_error`: log_state() が例外を送出しない

   **テストのキーポイント:** `update()` の引数は `bankroll` のみ(bet_returnなし)。ROI依存テストは全てDD%閾値ベースに置き換える。ヒステリシス(min_stay_races)のテストでは、遷移が発生するまで十分な回数のupdate()を呼ぶ。
  </action>
  <acceptance_criteria>
    1. `python -m pytest tests/test_drawdown_controller.py -v` 全テストPASS
    2. `grep -c "bet_return" tests/test_drawdown_controller.py` returns 0
    3. `grep -c "rolling_roi" tests/test_drawdown_controller.py` returns 0
    4. `grep -c "RECOVERING" tests/test_drawdown_controller.py` returns 0
    5. engine.py で `strategy_params` が __init__ 引数に含まれる
    6. engine.py で `DDConfig` が import されている
    7. テスト数が20以上(DDConfig __post_init__ テスト追加で増加)
    8. テストに `from betting.drawdown_controller import DDConfig` が含まれる
    9. engine.pyのkelly modeセクションで strategy_params is not None 分岐が存在する
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/hirom/develop/keiba-ai && python -m pytest tests/test_drawdown_controller.py -v --tb=short</automated>
  </verify>
  <done>全テストがPASSし、DrawdownControllerのROI依存が完全に除去されている。DDConfig __post_init__検証がテスト済み。engine.pyがstrategy_params注入に対応し、DDConfig import付きでWIN/PLACE別インスタンス生成の準備ができている</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DDConfig -> DrawdownController | 設定値が信頼境界を越えて注入される。不正な閾値(dd_threshold_2 < dd_threshold_1等)で状態遷移が破綻する可能性 |
| strategy_params -> BacktestEngine | Optunaが生成したパラメータがengineに注入される。極端な値で異常動作の可能性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-13-01 | E (Elevation) | DDConfig | mitigate | __post_init__ で dd_threshold_2 > dd_threshold_1, all values > 0, multiplier_normal > multiplier_reduced を検証 |
| T-13-02 | D (Denial) | DrawdownController.update() | accept | bankroll=0, 負値のbankroll等は呼び出し側(BacktestEngine)で保護済み |
</threat_model>

<verification>
1. `python -m pytest tests/test_drawdown_controller.py -v` 全テストPASS
2. `python -c "from betting.drawdown_controller import DDConfig, DrawdownController; c = DrawdownController(100000, DDConfig()); print(c.cfg.rolling_window)"` -> 400
3. `python -c "from domain.types import RecoveryState; print([s.value for s in RecoveryState])"` -> ['normal', 'reduced', 'stop']
4. `python -c "from betting.drawdown_controller import DDConfig; DDConfig(dd_threshold_1=0.20, dd_threshold_2=0.10)"` -> ValueError
5. `python -m pytest tests/ -v --tb=short` 全体テスト(他モジュールへの影響確認)
</verification>

<success_criteria>
1. DrawdownControllerにROI計算コードが1行も残っていない(grep -c rolling_roi == 0)
2. RecoveryState enumがNORMAL, REDUCED, STOPの3値のみ
3. DDConfig dataclassが全パラメータを保持し、__post_init__で閾値検証を実行
4. DDConfig(dd_threshold_1=0.20, dd_threshold_2=0.10)がValueErrorを送出
5. ヒステリシス(min_stay_races)により状態遷移に最低滞在条件が課されている
6. 段階的リカバリ(STOP->REDUCED->NORMAL)が実装されている
7. BacktestEngine.__init__がstrategy_params引数を受け取る
8. engine.py kelly modeでstrategy_params is not None分岐が存在する
9. 全20以上のテストがPASS
10. 他モジュール(test_stake_calculator等)のテストもPASS(回帰なし)
</success_criteria>

<output>
After completion, create `.planning/phases/13-risk-calibration-parameter-optimization/13-01-SUMMARY.md`
</output>
