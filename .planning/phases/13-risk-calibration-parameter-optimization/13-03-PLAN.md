---
phase: 13-risk-calibration-parameter-optimization
plan: 03
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - src/tuning/strategy_optimizer.py
  - scripts/run_strategy_optimization.py
  - src/models/walk_forward_cv.py
  - tests/test_strategy_optimizer.py
autonomous: true
requirements: [VAL-02]

must_haves:
  truths:
    - "StrategyOptimizerがOptuna TPEで~16次元戦略パラメータを同時最適化する"
    - "各トライアルがWalk-forward 2fold評価でルックアヘッドバイアスを防止する"
    - "目的関数がROI主 + ベット数制約(年間1000件以上)で統計的有意性を担保する"
    - "最適パラメータがJSON manifestとしてSHA256付きで保存される"
    - "CLIスクリプトで最適化を実行できる"
  artifacts:
    - path: "src/tuning/strategy_optimizer.py"
      provides: "StrategyOptimizerクラス (Optuna TPE最適化)"
      exports: ["StrategyOptimizer"]
    - path: "scripts/run_strategy_optimization.py"
      provides: "戦略最適化CLIスクリプト"
      min_lines: 40
    - path: "src/models/walk_forward_cv.py"
      provides: "戦略パラメータ注入対応run()"
      contains: "strategy_params"
    - path: "tests/test_strategy_optimizer.py"
      provides: "StrategyOptimizer単体テスト"
      min_lines: 80
  key_links:
    - from: "src/tuning/strategy_optimizer.py"
      to: "src/backtest/parameter_freeze_protocol.py"
      via: "save_strategy_manifest() 呼び出し"
      pattern: "save_strategy_manifest"
    - from: "src/tuning/strategy_optimizer.py"
      to: "src/models/walk_forward_cv.py"
      via: "WalkForwardCV.run() strategy_params注入"
      pattern: "strategy_params"
    - from: "src/tuning/strategy_optimizer.py"
      to: "src/betting/drawdown_controller.py"
      via: "DDConfig params構築"
      pattern: "DDConfig"
    - from: "scripts/run_strategy_optimization.py"
      to: "src/tuning/strategy_optimizer.py"
      via: "StrategyOptimizerインポート"
      pattern: "StrategyOptimizer"
---

<objective>
Optuna TPEサンプラーで全戦略パラメータ(~16次元)を同時最適化するStrategyOptimizerを実装し、Walk-forward枠組みでルックアヘッドバイアスを防止して最適設定を発見する。

Purpose: レジーム別Kelly分数、EV閾値、DD閾値等の相互作用を最適化するには一括最適化が必須。Walk-forward評価でルックアヘッドバイアスを構造的に防止し、信頼できる最適設定を得る。

Output: StrategyOptimizerクラス、軽量WF評価ループ、CLIスクリプト、JSON manifest自動生成、テスト群
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-risk-calibration-parameter-optimization/13-CONTEXT.md
@.planning/phases/13-risk-calibration-parameter-optimization/13-RESEARCH.md
@.planning/phases/13-risk-calibration-parameter-optimization/13-01-SUMMARY.md
@.planning/phases/13-risk-calibration-parameter-optimization/13-02-SUMMARY.md
</context>

<interfaces>

From src/betting/drawdown_controller.py (Plan 01成果):
```python
@dataclass
class DDConfig:
    rolling_window: int = 400
    dd_threshold_1: float = 0.10
    dd_threshold_2: float = 0.20
    multiplier_normal: float = 1.0
    multiplier_reduced: float = 0.50
    multiplier_stop: float = 0.0
    min_stay_races: int = 10
    max_adjustment_per_n_bets: int = 20
    max_adjustment_amount: float = 0.15

class DrawdownController:
    def __init__(self, peak_bankroll: float, cfg: DDConfig | None = None) -> None:
```

From src/models/regime_detector.py (Plan 02成果):
```python
class RegimeDetector:
    def __init__(self, cfg: RegimeConfig | None = None, override_params: dict[str, dict[str, float]] | None = None) -> None:
```

From src/backtest/parameter_freeze_protocol.py (Plan 02成果):
```python
def save_strategy_manifest(params: dict[str, Any], path: Path) -> str:
def verify_strategy_manifest(path: Path) -> dict[str, Any]:
def load_and_freeze_strategy(params: dict[str, Any], manifest_path: Path) -> None:
```

From src/betting/stake_calculator.py (Phase 12成果):
```python
class StakeCalculator:
    def __init__(self, fractional_kelly: float = 0.50, target_ev: float = 1.10, max_scale: float = 2.0) -> None:
```

From src/tuning/optuna_tuner.py (参照実装):
```python
SEARCH_SPACES: dict[str, dict[str, tuple]] = { ... }
# OptunaTuner は ML HP のみ。StrategyOptimizer は戦略パラメータ用
```

From src/models/walk_forward_cv.py (current — will be modified):
```python
class WalkForwardCV:
    def __init__(self, pipeline, backtest_engine_factory, train_years=4, test_years=1, step_years=1) -> None:
    def run(self, start_date: str, end_date: str) -> CVResult:
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: WalkForwardCV戦略パラメータ注入 + StrategyOptimizer実装</name>
  <files>src/models/walk_forward_cv.py, src/tuning/strategy_optimizer.py</files>
  <read_first>
    src/models/walk_forward_cv.py (全体)
    src/tuning/optuna_tuner.py (全体 — 参照実装)
    src/backtest/engine.py (lines 327-375 — engine.__init__シグネチャ)
    src/betting/drawdown_controller.py (DDConfig dataclass — Plan 01成果)
    src/models/regime_detector.py (override_params — Plan 02成果)
    src/backtest/parameter_freeze_protocol.py (save_strategy_manifest — Plan 02成果)
  </read_first>
  <action>
Per D-07, D-08, D-09, D-10, D-11, D-12:

1. **src/models/walk_forward_cv.py** — run() に strategy_params 引数を追加:

   a. run() シグネチャ変更:
      ```python
      def run(
          self,
          start_date: str,
          end_date: str,
          strategy_params: dict[str, Any] | None = None,
      ) -> CVResult:
      ```

   b. fold ループ内で strategy_params を factory に渡す:
      ```python
      # 2. Backtest (OOS — strategy_params注入)
      if self.backtest_engine_factory is not None:
          engine = self.backtest_engine_factory(models, strategy_params)
          fold_result = engine.run(fold.test_start, fold.test_end)
      ```
      注: backtest_engine_factory の型は `Callable[[TrainedModelsV5, dict | None], BacktestEngine]` に拡張される。StrategyOptimizer内でラップするためWalkForwardCVの型アノテーションのみ更新:
      ```python
      backtest_engine_factory: Callable[[TrainedModelsV5, dict[str, Any] | None], BacktestEngine] | None = None
      ```

   c. pipeline なしでバックテストのみ実行するパターンを追加(StrategyOptimizer用):
      ```python
      # 1. 学習 (パラメータを凍結)
      if self.pipeline is not None:
          models = self.pipeline.run(fold.train_start, fold.train_end)
      else:
          raise RuntimeError("pipeline is required for run()")
      ```
      この部分は変更なし。StrategyOptimizerは WalkForwardCV を使わず独自軽量WFループを実装する(RESEARCH Pitfall 3: pipeline.run()で毎回学習するのを回避)。

2. **src/tuning/strategy_optimizer.py** (新規) — StrategyOptimizer クラス:

   ```python
   """Optuna戦略パラメータ最適化 (D-07, D-12)

   Walk-forward枠組みで~16次元戦略パラメータをOptuna TPE最適化。
   学習済みモデルをロードしてバックテストのみ実行(pipeline.run回避)。
   """

   from __future__ import annotations

   import json
   import logging
   from pathlib import Path
   from typing import Any

   import numpy as np
   import optuna
   from optuna.pruners import MedianPruner
   from optuna.samplers import TPESampler

   logger = logging.getLogger(__name__)

   class StrategyOptimizer:
       """全戦略パラメータのOptuna TPE最適化

       D-07: 全パラメータ一括最適化
       D-08: ~16次元探索空間
       D-09: ROI主 + ベット数制約
       D-10: Walk-forward枠組み
       D-11: 100トライアル + MedianPruner
       """

       def __init__(
           self,
           models_dir: Path | str = "data/models",
           data_dir: Path | str = "data",
           initial_bankroll: float = 100_000,
           n_folds: int = 2,
           train_years: int = 4,
           test_years: int = 1,
           min_bets_per_fold: int = 1000,
       ) -> None:
           self.models_dir = Path(models_dir)
           self.data_dir = Path(data_dir)
           self.initial_bankroll = initial_bankroll
           self.n_folds = n_folds
           self.train_years = train_years
           self.test_years = test_years
           self.min_bets_per_fold = min_bets_per_fold

       def _suggest_params(self, trial: optuna.Trial) -> dict[str, Any]:
           """D-08: ~16次元探索空間"""
           params: dict[str, Any] = {}

           # レジーム別パラメータ (AGGRESSIVE, CONSERVATIVEのみ。COLLAPSEDはfk=0固定)
           for regime in ("aggressive", "conservative"):
               params[f"fk_{regime}"] = trial.suggest_float(
                   f"fk_{regime}", 0.10, 0.80,
               )
               params[f"ev_{regime}"] = trial.suggest_float(
                   f"ev_{regime}", 1.05, 2.00,
               )
               params[f"edge_{regime}"] = trial.suggest_float(
                   f"edge_{regime}", 0.03, 0.15,
               )

           # DD制御パラメータ
           params["dd_threshold_1"] = trial.suggest_float("dd_threshold_1", 0.05, 0.20)
           params["dd_threshold_2"] = trial.suggest_float("dd_threshold_2", 0.15, 0.35)
           params["multiplier_reduced"] = trial.suggest_float("multiplier_reduced", 0.1, 0.8)
           params["rolling_window"] = trial.suggest_int("rolling_window", 200, 800)
           params["min_stay_races"] = trial.suggest_int("min_stay_races", 5, 30)

           # EVスケーリング
           params["target_ev"] = trial.suggest_float("target_ev", 1.05, 1.50)
           params["max_scale"] = trial.suggest_float("max_scale", 1.0, 3.0)

           # OddsBandFilter ROI閾値
           params["roi_threshold"] = trial.suggest_float("roi_threshold", 0.8, 1.2)

           return params

       def _build_strategy_config(self, params: dict[str, Any]) -> dict[str, Any]:
           """Optuna params → コンポーネント注入用dictに変換"""
           from betting.drawdown_controller import DDConfig

           dd_config = DDConfig(
               rolling_window=params["rolling_window"],
               dd_threshold_1=params["dd_threshold_1"],
               dd_threshold_2=params["dd_threshold_2"],
               multiplier_reduced=params["multiplier_reduced"],
               min_stay_races=params["min_stay_races"],
           )

           regime_overrides = {}
           for regime in ("aggressive", "conservative"):
               regime_overrides[regime] = {
                   "fractional_kelly": params[f"fk_{regime}"],
                   "ev_threshold": params[f"ev_{regime}"],
                   "edge_threshold": params[f"edge_{regime}"],
               }

           return {
               "dd_config": dd_config,
               "regime_overrides": regime_overrides,
               "target_ev": params["target_ev"],
               "max_scale": params["max_scale"],
               "roi_threshold": params["roi_threshold"],
           }

       def _run_single_backtest(
           self,
           strategy_config: dict[str, Any],
           test_start: str,
           test_end: str,
           trial: optuna.Trial | None = None,
           fold_idx: int = 0,
       ) -> dict[str, Any]:
           """単一foldバックテスト実行

           注: 学習済みモデルをロードしてバックテストのみ実行。
           pipeline.run()は呼ばない(RESEARCH Pitfall 3)。
           """
           # 実際のBacktestEngine構築はサブクラス/monkey-patchでテスト可能
           # ここではインターフェースのみ定義
           raise NotImplementedError("Subclass or inject backtest_runner")

       def _objective(self, trial: optuna.Trial) -> float:
           """D-09: ROI主 + ベット数制約"""
           params = self._suggest_params(trial)
           strategy_config = self._build_strategy_config(params)

           # Walk-forward 2fold評価
           folds = self._generate_folds()
           rois = []
           total_bets = 0

           for fold_idx, (test_start, test_end) in enumerate(folds):
               result = self._run_single_backtest(
                   strategy_config, test_start, test_end, trial, fold_idx
               )
               rois.append(result.get("roi", 0.0))
               total_bets += result.get("n_bets", 0)

               # D-11: MedianPruner用中間報告
               if trial is not None:
                   trial.report(result.get("roi", 0.0), step=fold_idx)
                   if trial.should_prune():
                       raise optuna.TrialPruned()

           mean_roi = float(np.mean(rois)) if rois else 0.0

           # D-09: ベット数制約 (foldあたりmin_bets_per_fold)
           avg_bets_per_fold = total_bets / max(len(folds), 1)
           if avg_bets_per_fold < self.min_bets_per_fold:
               return -1.0  # ペナルティ

           return mean_roi

       def _generate_folds(self) -> list[tuple[str, str]]:
           """WF fold定義。run_wf_validation.py のパターンを踏襲"""
           # デフォルト: 2020-2024 train / 2024 test, 2021-2025 train / 2025 test
           # 注: 実際のfold定義はCLI引数で上書き可能
           return [
               ("2024-01-01", "2024-12-31"),
               ("2025-01-01", "2025-12-31"),
           ]

       def optimize(
           self,
           n_trials: int = 100,
           seed: int = 42,
           output_path: Path | str | None = None,
       ) -> dict[str, Any]:
           """D-11: Optuna最適化実行 + D-14: manifest自動生成"""
           sampler = TPESampler(seed=seed)
           pruner = MedianPruner()
           study = optuna.create_study(
               direction="maximize",
               sampler=sampler,
               pruner=pruner,
           )
           study.optimize(self._objective, n_trials=n_trials)

           result = {
               "best_params": study.best_params,
               "best_value": study.best_value,
               "n_trials": len(study.trials),
               "n_pruned": sum(1 for t in study.trials if t.state == optuna.TrialState.PRUNED),
           }

           # D-14: JSON manifest自動生成
           if output_path is not None:
               from backtest.parameter_freeze_protocol import save_strategy_manifest
               save_strategy_manifest(result["best_params"], Path(output_path))

           logger.info(
               f"Optimization complete: best_roi={result['best_value']:.4f}, "
               f"trials={result['n_trials']}, pruned={result['n_pruned']}"
           )
           return result
   ```

   **注意:** _run_single_backtest()はNotImplementedErrorをraiseする設計。実際のバックテスト実行はPostgreSQLが必要なため、本番実行時はサブクラスまたは関数インジェクションでoverrideする。テストではmockで検証する。この設計により:
   - テスト環境(DBなし)で_optimize()のロジックを検証可能
   - 本番実行時にBacktestEngineを注入可能
   - RESEARCH Pitfall 3回避(pipeline.run()で毎回学習しない)
  </action>
  <acceptance_criteria>
    1. `grep "class StrategyOptimizer" src/tuning/strategy_optimizer.py` にヒット
    2. `grep "_suggest_params" src/tuning/strategy_optimizer.py` にヒット
    3. `grep "_objective" src/tuning/strategy_optimizer.py` にヒット
    4. `grep "TPESampler" src/tuning/strategy_optimizer.py` にヒット
    5. `grep "MedianPruner" src/tuning/strategy_optimizer.py` にヒット
    6. `grep "strategy_params" src/models/walk_forward_cv.py` にヒット(run()引数)
    7. `grep "save_strategy_manifest" src/tuning/strategy_optimizer.py` にヒット
    8. `grep "fk_aggressive" src/tuning/strategy_optimizer.py` にヒット(16次元パラメータの一部)
    9. WalkForwardCV.run()に `strategy_params: dict[str, Any] | None = None` 引数が追加されている
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/hirom/develop/keiba-ai && python -c "from tuning.strategy_optimizer import StrategyOptimizer; print('import OK')"</automated>
  </verify>
  <done>StrategyOptimizerが~16次元のOptuna TPE最適化ロジックを持つ。WalkForwardCV.run()がstrategy_paramsを受け取れる。_run_single_backtest()がmock可能なインターフェース</done>
</task>

<task type="auto">
  <name>Task 2: CLIスクリプト + テスト</name>
  <files>scripts/run_strategy_optimization.py, tests/test_strategy_optimizer.py</files>
  <read_first>
    src/tuning/strategy_optimizer.py (Task 1完了後)
    scripts/run_tuning.py (参照パターン)
    scripts/run_wf_validation.py (参照パターン)
    src/tuning/optuna_tuner.py (参照実装)
    src/models/walk_forward_cv.py (Task 1完了後)
  </read_first>
  <action>
Per D-12, D-14:

1. **scripts/run_strategy_optimization.py** (新規) — CLIスクリプト:

   ```python
   """戦略パラメータOptuna最適化CLI

   Usage:
       python scripts/run_strategy_optimization.py \
           --n-trials 100 \
           --models-dir data/models \
           --output data/strategy_manifest.json
   """
   import argparse
   import logging
   from pathlib import Path

   from tuning.strategy_optimizer import StrategyOptimizer

   logging.basicConfig(level=logging.INFO, format="%(levelname)s %(name)s: %(message)s")
   logger = logging.getLogger(__name__)


   def main() -> None:
       parser = argparse.ArgumentParser(description="Optuna戦略パラメータ最適化")
       parser.add_argument(
           "--n-trials", type=int, default=100,
           help="Optuna試行回数 (default: 100)",
       )
       parser.add_argument(
           "--seed", type=int, default=42,
           help="TPESampler乱数シード (default: 42)",
       )
       parser.add_argument(
           "--models-dir", type=str, default="data/models",
           help="学習済みモデルディレクトリ",
       )
       parser.add_argument(
           "--output", type=str, default="data/strategy_manifest.json",
           help="出力manifestファイルパス",
       )
       parser.add_argument(
           "--min-bets", type=int, default=1000,
           help="1foldあたりの最低ベット数 (default: 1000)",
       )
       args = parser.parse_args()

       optimizer = StrategyOptimizer(
           models_dir=args.models_dir,
           min_bets_per_fold=args.min_bets,
       )

       logger.info(f"Starting strategy optimization: n_trials={args.n_trials}")
       result = optimizer.optimize(
           n_trials=args.n_trials,
           seed=args.seed,
           output_path=Path(args.output),
       )

       logger.info(
           f"Best ROI: {result['best_value']:.4f}, "
           f"Best params: {result['best_params']}, "
           f"Trials: {result['n_trials']}, "
           f"Pruned: {result['n_pruned']}"
       )


   if __name__ == "__main__":
       main()
   ```

2. **tests/test_strategy_optimizer.py** (新規) — mockベースのStrategyOptimizerテスト:

   テストは_run_single_backtest()をmockしてOptunaループを検証。DB不要。

   ```python
   """src/tuning/strategy_optimizer.py のテスト"""
   from __future__ import annotations

   from pathlib import Path
   from unittest.mock import MagicMock, patch

   import optuna
   import pytest

   from tuning.strategy_optimizer import StrategyOptimizer


   @pytest.fixture
   def optimizer() -> StrategyOptimizer:
       return StrategyOptimizer(
           models_dir="data/models",
           min_bets_per_fold=100,  # テスト用に小さく
       )


   class TestSuggestParams:
       def test_suggest_returns_all_dimensions(self, optimizer: StrategyOptimizer) -> None:
           """_suggest_params が16+次元を返す"""
           study = optuna.create_study(direction="maximize")
           # Trialを手動作成
           trial = study.ask()

           with patch.object(optimizer, '_run_single_backtest'):
               params = optimizer._suggest_params(trial)

           # レジーム別 (x2): fk, ev, edge = 6
           assert "fk_aggressive" in params
           assert "fk_conservative" in params
           assert "ev_aggressive" in params
           assert "ev_conservative" in params
           assert "edge_aggressive" in params
           assert "edge_conservative" in params
           # DD制御: 5
           assert "dd_threshold_1" in params
           assert "dd_threshold_2" in params
           assert "multiplier_reduced" in params
           assert "rolling_window" in params
           assert "min_stay_races" in params
           # EVスケーリング: 2
           assert "target_ev" in params
           assert "max_scale" in params
           # OddsBandFilter: 1
           assert "roi_threshold" in params
           # Total: 14+
           assert len(params) >= 14

       def test_param_ranges_valid(self, optimizer: StrategyOptimizer) -> None:
           """パラメータが妥当な範囲内"""
           study = optuna.create_study(direction="maximize")
           trial = study.ask()
           params = optimizer._suggest_params(trial)

           assert 0.10 <= params["fk_aggressive"] <= 0.80
           assert 0.05 <= params["dd_threshold_1"] <= 0.20
           assert 0.15 <= params["dd_threshold_2"] <= 0.35
           assert params["dd_threshold_2"] >= params["dd_threshold_1"]  # 意味 constraint
           assert 200 <= params["rolling_window"] <= 800
           assert 1.05 <= params["target_ev"] <= 1.50


   class TestBuildStrategyConfig:
       def test_builds_dd_config(self, optimizer: StrategyOptimizer) -> None:
           """_build_strategy_config がDDConfigを構築"""
           params = {
               "rolling_window": 500,
               "dd_threshold_1": 0.12,
               "dd_threshold_2": 0.25,
               "multiplier_reduced": 0.4,
               "min_stay_races": 15,
               "fk_aggressive": 0.6, "ev_aggressive": 1.2, "edge_aggressive": 0.08,
               "fk_conservative": 0.3, "ev_conservative": 1.4, "edge_conservative": 0.07,
               "target_ev": 1.15, "max_scale": 2.5, "roi_threshold": 0.95,
           }
           config = optimizer._build_strategy_config(params)

           assert config["dd_config"].rolling_window == 500
           assert config["dd_config"].dd_threshold_1 == 0.12
           assert config["dd_config"].dd_threshold_2 == 0.25
           assert config["regime_overrides"]["aggressive"]["fractional_kelly"] == 0.6
           assert config["target_ev"] == 1.15

       def test_regime_overrides_structure(self, optimizer: StrategyOptimizer) -> None:
           """regime_overrides が aggressive/conservative の2レジームを持つ"""
           params = {
               "rolling_window": 400, "dd_threshold_1": 0.10, "dd_threshold_2": 0.20,
               "multiplier_reduced": 0.5, "min_stay_races": 10,
               "fk_aggressive": 0.5, "ev_aggressive": 1.1, "edge_aggressive": 0.05,
               "fk_conservative": 0.25, "ev_conservative": 1.3, "edge_conservative": 0.06,
               "target_ev": 1.1, "max_scale": 2.0, "roi_threshold": 1.0,
           }
           config = optimizer._build_strategy_config(params)
           assert "aggressive" in config["regime_overrides"]
           assert "conservative" in config["regime_overrides"]
           for regime_key in ("aggressive", "conservative"):
               ro = config["regime_overrides"][regime_key]
               assert "fractional_kelly" in ro
               assert "ev_threshold" in ro
               assert "edge_threshold" in ro


   class TestObjective:
       def test_penalty_when_bets_below_minimum(self, optimizer: StrategyOptimizer) -> None:
           """ベット数不足時は-1.0ペナルティ"""
           def mock_backtest(cfg, test_start, test_end, trial=None, fold_idx=0):
               return {"roi": 1.5, "n_bets": 50}  # 50 < 100 (min_bets)
           optimizer._run_single_backtest = mock_backtest

           study = optuna.create_study(direction="maximize")
           trial = study.ask()
           result = optimizer._objective(trial)
           assert result == -1.0

       def test_returns_roi_when_sufficient_bets(self, optimizer: StrategyOptimizer) -> None:
           """ベット数十分時はROIを返す"""
           def mock_backtest(cfg, test_start, test_end, trial=None, fold_idx=0):
               return {"roi": 1.15, "n_bets": 2000}
           optimizer._run_single_backtest = mock_backtest

           study = optuna.create_study(direction="maximize")
           trial = study.ask()
           result = optimizer._objective(trial)
           assert result == pytest.approx(1.15, abs=0.01)


   class TestOptimize:
       def test_optimize_returns_best_params(self, optimizer: StrategyOptimizer) -> None:
           """optimize() がbest_params, best_value, n_trialsを返す"""
           def mock_backtest(cfg, test_start, test_end, trial=None, fold_idx=0):
               return {"roi": 1.05, "n_bets": 2000}
           optimizer._run_single_backtest = mock_backtest

           result = optimizer.optimize(n_trials=5, seed=42)
           assert "best_params" in result
           assert "best_value" in result
           assert "n_trials" in result
           assert result["n_trials"] == 5

       def test_optimize_saves_manifest(self, optimizer: StrategyOptimizer, tmp_path: Path) -> None:
           """optimize() がJSON manifestを保存"""
           def mock_backtest(cfg, test_start, test_end, trial=None, fold_idx=0):
               return {"roi": 1.05, "n_bets": 2000}
           optimizer._run_single_backtest = mock_backtest

           manifest_path = tmp_path / "strategy_manifest.json"
           result = optimizer.optimize(n_trials=3, output_path=manifest_path)
           assert manifest_path.exists()

           import json
           manifest = json.loads(manifest_path.read_text())
           assert "params" in manifest
           assert "sha256" in manifest

       def test_pruning_works(self, optimizer: StrategyOptimizer) -> None:
           """MedianPrunerが動作する(一部トライアルがpruned)"""
           call_count = 0
           def mock_backtest(cfg, test_start, test_end, trial=None, fold_idx=0):
               nonlocal call_count
               call_count += 1
               # 悪いROI → prunerがpruneする可能性
               return {"roi": 0.5, "n_bets": 2000}
           optimizer._run_single_backtest = mock_backtest

           result = optimizer.optimize(n_trials=10, seed=42)
           assert "n_pruned" in result
   ```

   **テストのキーポイント:** `_run_single_backtest()`をmock lambdaで差し替え、Optunaループの正しさのみを検証。DB不要。実際のバックテスト結果の正しさはPostgreSQL環境でのみ検証可能。
  </action>
  <acceptance_criteria>
    1. `python -m pytest tests/test_strategy_optimizer.py -v` 全テストPASS
    2. `grep "class TestSuggestParams" tests/test_strategy_optimizer.py` にヒット
    3. `grep "class TestBuildStrategyConfig" tests/test_strategy_optimizer.py` にヒット
    4. `grep "class TestObjective" tests/test_strategy_optimizer.py` にヒット
    5. `grep "class TestOptimize" tests/test_strategy_optimizer.py` にヒット
    6. テスト数が10以上
    7. scripts/run_strategy_optimization.py が存在し `--n-trials`, `--output` 引数を持つ
    8. `python scripts/run_strategy_optimization.py --help` が正常終了(exit code 0)
  </acceptance_criteria>
  <verify>
    <automated>cd C:/Users/hirom/develop/keiba-ai && python -m pytest tests/test_strategy_optimizer.py -v --tb=short</automated>
  </verify>
  <done>StrategyOptimizerの全テストがPASS。CLIスクリプトが--helpで正常動作。Optuna TPE最適化のロジックがmockで検証済み</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Optuna Trial → BacktestEngine | サンプラーが生成したパラメータがDDConfig/RegimeDetectorに注入される。極端な値で異常動作の可能性 |
| CLI args → StrategyOptimizer | ユーザー入力が--n-trials等の数値として渡される。不正値でリソース枯渇の可能性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-13-05 | D (Denial) | StrategyOptimizer | mitigate | argparseが型チェック。min_bets_per_fold > 0検証 |
| T-13-06 | E (Elevation) | _suggest_params ranges | mitigate | suggest_float/intのboundsでパラメータ範囲を制約。dd_threshold_2 > dd_threshold_1はobjective内でソフト制約 |
</threat_model>

<verification>
1. `python -m pytest tests/test_strategy_optimizer.py -v` 全テストPASS
2. `python -m pytest tests/test_drawdown_controller.py -v` Plan 01テストもPASS(回帰なし)
3. `python -m pytest tests/test_parameter_freeze.py -v` Plan 02テストもPASS(回帰なし)
4. `python scripts/run_strategy_optimization.py --help` 正常終了
5. `python -c "from tuning.strategy_optimizer import StrategyOptimizer; print('OK')"` → OK
</verification>

<success_criteria>
1. StrategyOptimizerが~16次元のOptuna TPE最適化ロジックを持つ
2. _suggest_paramsがレジーム別(6) + DD制御(5) + EVスケーリング(2) + OddsBandFilter(1) = 14+パラメータを定義
3. 目的関数がROI主 + ベット数制約を実装
4. Walk-forward評価(foldごとに中間report → MedianPrunerがprune可能)
5. JSON manifestが最適化完了後に自動生成される
6. テストがmockベースでDB不要、10テスト以上
7. CLIスクリプトが--helpで正常動作
8. 既存テストに回帰なし
</success_criteria>

<output>
After completion, create `.planning/phases/13-risk-calibration-parameter-optimization/13-03-SUMMARY.md`
</output>
