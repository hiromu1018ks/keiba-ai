---
phase: 16-odds-band-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/betting/default_strategy.py
  - src/tuning/strategy_optimizer.py
  - tests/test_strategy_optimizer.py
  - tests/test_default_strategy.py
autonomous: true
requirements:
  - ODDS-02

must_haves:
  truths:
    - "build_default_strategy_config()がRegimeDetector._get_base_params()のハードコード既定値からstrategy_config dictを構築する"
    - "strategy_optimizer.pyの_build_default_config()がdefault_strategy.pyにデリゲートする (重複実装排除)"
    - "_run_single_backtest()のステップ3がdefault_config、ステップ4-5がOptuna提案のstrategy_configを使用する"
    - "テストでtraining backtestとtest backtestに異なるstrategy_configが使用されることを検証できる"
  artifacts:
    - path: "src/betting/default_strategy.py"
      provides: "build_default_strategy_config() 共通ユーティリティ (Plan 02からもimport)"
      contains: "build_default_strategy_config"
    - path: "src/tuning/strategy_optimizer.py"
      provides: "_build_default_config()デリゲート + _run_single_backtest()ステップ3修正"
      contains: "_build_default_config"
    - path: "tests/test_default_strategy.py"
      provides: "build_default_strategy_config()の独立テスト"
      exports: ["TestBuildDefaultStrategyConfig"]
    - path: "tests/test_strategy_optimizer.py"
      provides: "ルックアヘッドバイアス修正のテスト（デフォルトvs Optuna configの差異検証）"
      exports: ["TestBuildDefaultConfig", "TestRunSingleBacktest.test_uses_default_config_for_training"]
  key_links:
    - from: "src/tuning/strategy_optimizer.py._build_default_config"
      to: "src/betting/default_strategy.py"
      via: "from betting.default_strategy import build_default_strategy_config"
      pattern: "build_default_strategy_config"
    - from: "src/tuning/strategy_optimizer.py._run_single_backtest"
      to: "src/tuning/strategy_optimizer.py._build_default_config"
      via: "ステップ3のdefault_config = self._build_default_config()"
      pattern: "default_config"
---

<objective>
共通ユーティリティdefault_strategy.pyを作成し、strategy_optimizer.pyのルックアヘッドバイアスを修正する（ODDS-02）。

Purpose: 現在_run_single_backtest()のステップ3がOptuna最適化対象と同じstrategy_configでtraining_bet_historyを生成しているため、テスト期間の最適化パラメータが学習期間のベット生成に漏洩している。RegimeDetectorのハードコード既定値から構築したデフォルト設定でtraining_bet_historyを生成するように修正する。また、Plan 02（engine.py/run_backtest.py）も同じデフォルト設定を使用するため、共通ユーティリティとしてdefault_strategy.pyに抽出する。

Output: default_strategy.py共通ユーティリティ + _build_default_config()デリゲート + _run_single_backtest()ステップ3修正 + テスト
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-odds-band-rebuild/16-CONTEXT.md
@.planning/phases/16-odds-band-rebuild/16-RESEARCH.md
@src/tuning/strategy_optimizer.py
@src/models/regime_detector.py
@src/betting/drawdown_controller.py
@src/betting/stake_calculator.py
@tests/test_strategy_optimizer.py
</context>

<interfaces>
<!-- Executor needs these contracts from the codebase -->

From src/models/regime_detector.py:
```python
class RegimeDetector:
    def _get_base_params(self, regime: RegimeState) -> dict[str, object]:
        """レジームに応じたハードコード戦略パラメータ (ベースライン)"""
        # Returns dict with keys: ev_threshold, edge_threshold, fractional_kelly, ...
        # AGGRESSIVE: ev_threshold=1.10, edge_threshold=0.05, fractional_kelly=0.50
        # CONSERVATIVE: ev_threshold=1.30, edge_threshold=0.06, fractional_kelly=0.25
        # COLLAPSED: ev_threshold=1.50, edge_threshold=0.09, fractional_kelly=0.00
```

From src/betting/drawdown_controller.py:
```python
@dataclass
class DDConfig:
    rolling_window: int = 400
    dd_threshold_1: float = 0.10
    dd_threshold_2: float = 0.20
    multiplier_normal: float = 1.0
    multiplier_reduced: float = 0.50
    multiplier_stop: float = 0.0
    min_stay_races: int = 10
```

From src/betting/stake_calculator.py:
```python
class StakeCalculator:
    def __init__(self, fractional_kelly=0.5, kelly_fraction_cap=0.25,
                 target_ev=1.10, max_scale=2.0) -> None: ...
```

From src/domain/types.py:
```python
class RegimeState(str, Enum):
    AGGRESSIVE = "aggressive"
    CONSERVATIVE = "conservative"
    COLLAPSED = "collapsed"
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: default_strategy.py作成 + _build_default_config()デリゲート + _run_single_backtest()ステップ3修正</name>
  <files>src/betting/default_strategy.py, src/tuning/strategy_optimizer.py, tests/test_default_strategy.py, tests/test_strategy_optimizer.py</files>
  <read_first>
    - src/tuning/strategy_optimizer.py (全文 — 現在の_build_strategy_config, _run_single_backtestを理解)
    - src/models/regime_detector.py:201-259 (_get_base_paramsの全パラメータ確認)
    - src/betting/drawdown_controller.py:14-30 (DDConfig dataclassのデフォルト値)
    - src/betting/stake_calculator.py:30-40 (StakeCalculator.__init__デフォルト値)
    - tests/test_strategy_optimizer.py (既存テストパターン、特にTestRunSingleBacktest)
  </read_first>
  <behavior>
    - Test 1: build_default_strategy_config()がdictを返し、必須キー(dd_config, regime_overrides, fractional_kelly, target_ev, max_scale, roi_threshold)を全て含む
    - Test 2: build_default_strategy_config()のdd_configがDDConfigデフォルト値(rolling_window=400, dd_threshold_1=0.10, dd_threshold_2=0.20)を持つ
    - Test 3: build_default_strategy_config()のregime_overrides["aggressive"]["fractional_kelly"]が0.50である
    - Test 4: build_default_strategy_config()のregime_overrides["conservative"]["fractional_kelly"]が0.25である
    - Test 5: build_default_strategy_config()のトップレベルfractional_kellyが0.25である（CONSERVATIVE値 per RESEARCH Pitfall 1）
    - Test 6: build_default_strategy_config()のtarget_evが1.10、max_scaleが2.0、roi_thresholdが1.0である
    - Test 7: _run_single_backtest()のMockEngine.call_args_list[0]（ステップ3=train_engine）がdefault_configを使用し、call_args_list[1]（ステップ4=test_engine）がoptunaのstrategy_configを使用する
    - Test 8: _run_single_backtest()がデフォルトregime_overridesでRegimeDetector._override_paramsを上書きした後、Optuna regime_overridesで再度上書きすることを確認
    - Test 9: _build_default_config()がbuild_default_strategy_config()にデリゲートしている（同じ戻り値を返す）
  </behavior>
  <action>
    **1. src/betting/default_strategy.py を新規作成 (共通ユーティリティ):**

    RegimeDetector._get_base_params()のハードコード既定値からstrategy_config dictを構築する関数。
    strategy_optimizer.py._build_default_config()、BacktestEngine._generate_training_bet_history()、
    run_backtest.py._collect_training_bet_history()の全てからimportされるSingle Source of Truth。

    ```python
    """デフォルト戦略パラメータ構築ユーティリティ (ルックアヘッド防止)

    RegimeDetector._get_base_params()のハードコード既定値から
    BacktestEngine strategy_config dictを構築する。
    strategy_optimizer.py._build_default_config()、
    BacktestEngine._generate_training_bet_history()、
    run_backtest.py._collect_training_bet_history()の全てで使用。
    """
    from __future__ import annotations

    from typing import Any

    from betting.drawdown_controller import DDConfig
    from domain.types import RegimeState
    from models.regime_detector import RegimeDetector


    def build_default_strategy_config() -> dict[str, Any]:
        """RegimeDetector既定値からデフォルトstrategy_configを構築 (ルックアヘッド防止)。

        Returns:
            dict: BacktestEngineのstrategy_paramsとして使用可能な設定辞書。
                キー: dd_config, regime_overrides, fractional_kelly, target_ev, max_scale, roi_threshold
        """
        # _get_base_params()はインスタンス状態に依存しないハードコード値を返す
        detector = RegimeDetector()
        conservative_params = detector._get_base_params(RegimeState.CONSERVATIVE)

        # DDConfigデフォルト値 (Optuna探索範囲内にあることを確認済み: Pitfall 2)
        dd_config = DDConfig()

        # 各レジームのハードコード既定値を取得
        regime_overrides: dict[str, dict[str, float]] = {}
        for regime_key in ("aggressive", "conservative"):
            state = RegimeState(regime_key)
            base = detector._get_base_params(state)
            regime_overrides[regime_key] = {
                "fractional_kelly": base["fractional_kelly"],
                "ev_threshold": base["ev_threshold"],
                "edge_threshold": base["edge_threshold"],
            }

        return {
            "dd_config": dd_config,
            "regime_overrides": regime_overrides,
            # トップレベルfractional_kellyはCONSERVATIVE値 (最も中立な状態 per Pitfall 1)
            "fractional_kelly": conservative_params["fractional_kelly"],
            # StakeCalculatorデフォルト
            "target_ev": 1.10,
            "max_scale": 2.0,
            # OddsBandFilterデフォルト
            "roi_threshold": 1.0,
        }
    ```

    **2. tests/test_default_strategy.py を新規作成:**

    共通ユーティリティの独立テスト:
    ```python
    """src/betting/default_strategy.py 共通ユーティリティテスト (D-01/D-02)"""
    from __future__ import annotations

    import pytest


    class TestBuildDefaultStrategyConfig:
        """build_default_strategy_config()のテスト"""

        def test_returns_all_required_keys(self):
            from betting.default_strategy import build_default_strategy_config
            config = build_default_strategy_config()
            for key in ("dd_config", "regime_overrides", "fractional_kelly",
                        "target_ev", "max_scale", "roi_threshold"):
                assert key in config, f"Missing key: {key}"

        def test_dd_config_defaults(self):
            from betting.default_strategy import build_default_strategy_config
            config = build_default_strategy_config()
            dd = config["dd_config"]
            assert dd.rolling_window == 400
            assert dd.dd_threshold_1 == 0.10
            assert dd.dd_threshold_2 == 0.20
            assert dd.multiplier_reduced == 0.50
            assert dd.min_stay_races == 10

        def test_aggressive_fractional_kelly(self):
            from betting.default_strategy import build_default_strategy_config
            config = build_default_strategy_config()
            agg = config["regime_overrides"]["aggressive"]
            assert agg["fractional_kelly"] == 0.50
            assert agg["ev_threshold"] == 1.10
            assert agg["edge_threshold"] == 0.05

        def test_conservative_fractional_kelly(self):
            from betting.default_strategy import build_default_strategy_config
            config = build_default_strategy_config()
            con = config["regime_overrides"]["conservative"]
            assert con["fractional_kelly"] == 0.25
            assert con["ev_threshold"] == 1.30
            assert con["edge_threshold"] == 0.06

        def test_top_level_fractional_kelly_uses_conservative(self):
            from betting.default_strategy import build_default_strategy_config
            config = build_default_strategy_config()
            assert config["fractional_kelly"] == 0.25

        def test_stake_calculator_defaults(self):
            from betting.default_strategy import build_default_strategy_config
            config = build_default_strategy_config()
            assert config["target_ev"] == 1.10
            assert config["max_scale"] == 2.0
            assert config["roi_threshold"] == 1.0
    ```

    **3. src/tuning/strategy_optimizer.py の _build_default_config() をデリゲートに変更:**

    `_build_strategy_config()`メソッドの直後に_build_default_config()を追加または修正する。
    内部ロジックを削除し、default_strategy.pyにデリゲートする:

    ```python
    def _build_default_config(self) -> dict[str, Any]:
        """RegimeDetector既定値からデフォルトstrategy_configを構築 (ルックアヘッド防止)

        D-01: デフォルトパラメータソースはRegimeDetector._get_base_params()のハードコード既定値
        D-02: 16次元全てを適用
        D-03: _build_strategy_config()と並存
        Warning 1修正: default_strategy.pyにデリゲート (重複実装排除)
        """
        from betting.default_strategy import build_default_strategy_config
        return build_default_strategy_config()
    ```

    **4. src/tuning/strategy_optimizer.py の _run_single_backtest() を修正:**

    line 142-168（ステップ2-3）を以下のように変更する:

    - line 143-148のregime_overrides注入をステップ3の後に移動する
    - ステップ3で`default_config = self._build_default_config()`を呼び出し、`strategy_params=default_config`でtrain_engineを構築する
    - デフォルトregime_overridesを一時的に注入してからtrain_engineを実行
    - その後Optuna regime_overridesで上書きする

    具体的な変更（line 142-171）:
    ```python
        # 2. デフォルトconfigでtraining_bet_history生成 (D-04: ルックアヘッド防止)
        default_config = self._build_default_config()

        # 2a. デフォルトregime_overridesを一時的に注入
        default_regime_overrides = default_config.get("regime_overrides")
        if default_regime_overrides:
            models.regime_detector._override_params = default_regime_overrides

        # 3. トレーニング期間バックテスト (デフォルトパラメータで実行)
        train_start = info.train_start
        train_end = info.train_end
        training_bet_history: list[dict[str, Any]] | None = None
        try:
            train_engine = BacktestEngine(
                models=models,
                initial_bankroll=self.initial_bankroll,
                betting_mode="kelly",
                diag_prefix=f"opt_train_fold{fold_idx}",
                betting_target="win",
                strategy_params=default_config,  # [CHANGED] strategy_config -> default_config
            )
            train_result = train_engine.run(train_start, train_end)
            training_bet_history = train_result.bet_history
            logger.info(
                "Fold %d: training-phase backtest completed (%d bets, ROI=%.1f%%)",
                fold_idx, train_result.total_bets, train_result.total_roi * 100,
            )
        except Exception as e:
            logger.warning("Fold %d: training-phase backtest failed: %s — skipping calibration",
                           fold_idx, e)

        # 2b. Optuna値のregime_overridesで上書き (テスト期間用)
        regime_overrides = strategy_config.get("regime_overrides")
        if regime_overrides:
            models.regime_detector._override_params = regime_overrides
    ```

    line 143-148の既存のregime_overrides注入コードを削除し、上記に置き換える。ステップ4-5（line 173-192）は変更なし。

    **5. tests/test_strategy_optimizer.py にテストを追加:**

    新しいテストクラス `TestBuildDefaultConfig` を追加し、_build_default_config()がデリゲートとして正しく動作することを確認:

    ```python
    class TestBuildDefaultConfig:
        def test_delegates_to_default_strategy(self, optimizer):
            """_build_default_config()がbuild_default_strategy_config()にデリゲートする"""
            from betting.default_strategy import build_default_strategy_config
            expected = build_default_strategy_config()
            actual = optimizer._build_default_config()
            assert actual["fractional_kelly"] == expected["fractional_kelly"]
            assert actual["target_ev"] == expected["target_ev"]
            assert actual["max_scale"] == expected["max_scale"]
            assert actual["roi_threshold"] == expected["roi_threshold"]
    ```

    新しいテストメソッドを `TestRunSingleBacktest` に追加する:
    ```python
    def test_training_uses_default_config_not_optuna(self, optimizer):
        """D-04: training backtest が default_config を使用し、test backtest が
        Optuna提案のstrategy_configを使用することを検証 (ルックアヘッド防止)"""
        mock_models = MagicMock()
        mock_info = MagicMock()
        mock_result = MagicMock()
        mock_result.total_roi = 1.10
        mock_result.total_bets = 5000
        mock_result.total_stake = 500000
        mock_result.total_return = 550000
        mock_result.max_drawdown = 0.15
        mock_result.bet_history = [{"race_id": "1", "odds": 5.0, "result": 500, "stake": 100}]

        with patch("db.model_loader.ModelLoader") as MockLoader, \
             patch("backtest.engine.BacktestEngine") as MockEngine:
            MockLoader.return_value.load_from_dir.return_value = (mock_models, mock_info)
            MockEngine.return_value.run.return_value = mock_result

            optuna_config = optimizer._build_strategy_config({
                "rolling_window": 400, "dd_threshold_1": 0.10, "dd_threshold_2": 0.20,
                "multiplier_reduced": 0.5, "min_stay_races": 10,
                "fk_aggressive": 0.6, "ev_aggressive": 1.2, "edge_aggressive": 0.08,
                "fk_conservative": 0.3, "ev_conservative": 1.4, "edge_conservative": 0.07,
                "target_ev": 1.15, "max_scale": 2.5, "roi_threshold": 0.95,
            })
            optimizer._run_single_backtest(optuna_config, "2024-01-01", "2024-12-31")

            assert MockEngine.call_count == 2
            # ステップ3 (train_engine): default_config を使用
            train_call = MockEngine.call_args_list[0]
            train_params = train_call.kwargs.get("strategy_params", {})
            default_config = optimizer._build_default_config()
            assert train_params["fractional_kelly"] == default_config["fractional_kelly"]
            assert train_params["target_ev"] == default_config["target_ev"]
            # ステップ4 (test_engine): Optuna提案のstrategy_config を使用
            test_call = MockEngine.call_args_list[1]
            test_params = test_call.kwargs.get("strategy_params", {})
            assert test_params["fractional_kelly"] == optuna_config["fractional_kelly"]
            assert test_params["target_ev"] == optuna_config["target_ev"]

    def test_regime_overrides_switched_between_train_and_test(self, optimizer):
        """D-09: デフォルトregime_overridesがtrain用に注入され、
        その後Optuna regime_overridesで上書きされることを検証"""
        mock_models = MagicMock()
        mock_info = MagicMock()
        mock_result = MagicMock()
        mock_result.total_roi = 1.05
        mock_result.total_bets = 2000
        mock_result.total_stake = 200000
        mock_result.total_return = 210000
        mock_result.max_drawdown = 0.10
        mock_result.bet_history = []

        with patch("db.model_loader.ModelLoader") as MockLoader, \
             patch("backtest.engine.BacktestEngine") as MockEngine:
            MockLoader.return_value.load_from_dir.return_value = (mock_models, mock_info)
            MockEngine.return_value.run.return_value = mock_result

            optuna_config = optimizer._build_strategy_config({
                "rolling_window": 400, "dd_threshold_1": 0.10, "dd_threshold_2": 0.20,
                "multiplier_reduced": 0.5, "min_stay_races": 10,
                "fk_aggressive": 0.6, "ev_aggressive": 1.2, "edge_aggressive": 0.08,
                "fk_conservative": 0.3, "ev_conservative": 1.4, "edge_conservative": 0.07,
                "target_ev": 1.15, "max_scale": 2.5, "roi_threshold": 0.95,
            })
            optimizer._run_single_backtest(optuna_config, "2024-01-01", "2024-12-31")

            # 最終的にOptuna値で上書きされていること
            final_overrides = mock_models.regime_detector._override_params
            assert final_overrides["aggressive"]["fractional_kelly"] == 0.6
            assert final_overrides["conservative"]["fractional_kelly"] == 0.3
    ```
  </action>
  <acceptance_criteria>
    - grep -c "_build_default_config" src/tuning/strategy_optimizer.py の結果が 3 以上（メソッド定義 + docstring内の参照 + 呼び出し箇所）
    - grep "from betting.default_strategy import build_default_strategy_config" src/tuning/strategy_optimizer.py が1行マッチする
    - grep "strategy_params=default_config" src/tuning/strategy_optimizer.py が1行マッチする
    - grep "strategy_params=strategy_config" src/tuning/strategy_optimizer.py がtest_engine構築の1行のみマッチする（line 180付近）
    - grep "_override_params = default_regime_overrides" src/tuning/strategy_optimizer.py が1行マッチする
    - grep "_override_params = regime_overrides" src/tuning/strategy_optimizer.py が1行マッチする（Optuna値での上書き）
    - grep "build_default_strategy_config" src/betting/default_strategy.py が2行以上マッチ（定義 + docstring）
    - python -m pytest tests/test_default_strategy.py -v が6テストPASS
    - python -m pytest tests/test_strategy_optimizer.py -v が全テストPASS（既存テスト含む）
    - python -m pytest tests/test_strategy_optimizer.py::TestBuildDefaultConfig -v がPASS
    - python -m pytest tests/test_strategy_optimizer.py::TestRunSingleBacktest::test_training_uses_default_config_not_optuna -v がPASS
    - python -m pytest tests/test_strategy_optimizer.py::TestRunSingleBacktest::test_regime_overrides_switched_between_train_and_test -v がPASS
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_default_strategy.py tests/test_strategy_optimizer.py -v</automated>
  </verify>
  <done>
    - src/betting/default_strategy.pyにbuild_default_strategy_config()共通ユーティリティが存在する
    - _build_default_config()がbuild_default_strategy_config()にデリゲートする（重複実装排除）
    - _run_single_backtest()のステップ3がdefault_configを使用し、ステップ4-5がOptunaのstrategy_configを使用する
    - trainingとtestで異なるstrategy_configが使用されることがテストで検証済み
    - 既存テストが全てPASSする
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| default_strategy.py → RegimeDetector | _get_base_params()はハードコード値のみ返す（外部入力なし） |
| strategy_optimizer.py → default_strategy.py | デリゲート呼び出し、外部入力なし |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | build_default_strategy_config | accept | ハードコード値から構築、外部入力なし。改ざんリスクなし |
| T-16-02 | Information Disclosure | _run_single_backtest | mitigate | default_configとstrategy_configの分離でOptuna値の漏洩を防止 |
</threat_model>

<verification>
python -m pytest tests/test_default_strategy.py tests/test_strategy_optimizer.py -v が全テストPASS
grep "from betting.default_strategy import build_default_strategy_config" src/tuning/strategy_optimizer.py がマッチ
grep "strategy_params=default_config" src/tuning/strategy_optimizer.py がマッチ
</verification>

<success_criteria>
- build_default_strategy_config()がDDConfigデフォルト + RegimeDetector既定値からstrategy_config dictを構築する
- _build_default_config()がdefault_strategy.pyにデリゲートする（重複実装なし）
- _run_single_backtest()のステップ3がdefault_configを使用（strategy_params=default_config）
- テストでtrainingとtestのstrategy_paramsの差異が検証される
- 全既存テストがPASS
</success_criteria>

<output>
After completion, create `.planning/phases/16-odds-band-rebuild/16-01-SUMMARY.md`
</output>
