---
phase: 16-odds-band-rebuild
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - scripts/run_backtest.py
autonomous: true
requirements:
  - ODDS-01

must_haves:
  truths:
    - "run_backtest.py --ensemble 実行時にtraining_bet_historyがデフォルトパラメータで生成される"
    - "OddsBandFilter.calibrate()がデフォルトパラメータ由来のtraining_bet_historyでバンド別ROIを計算する"
    - "アンサンブルモデルの実際の精度を反映したオッズバンド除外が決定される"
  artifacts:
    - path: "scripts/run_backtest.py"
      provides: "_collect_training_bet_history()でデフォルトパラメータを使用"
      contains: "_build_default_strategy_config"
  key_links:
    - from: "scripts/run_backtest.py._collect_training_bet_history"
      to: "デフォルトパラメータ構築ロジック"
      via: "新規_build_default_strategy_config()関数"
      pattern: "_build_default_strategy_config"
    - from: "scripts/run_backtest.py"
      to: "src/betting/odds_band_filter.py"
      via: "BacktestEngine.run()内のcalibrate(training_bet_history)"
      pattern: "training_bet_history"
---

<objective>
run_backtest.pyの_collect_training_bet_history()がデフォルトパラメータでtraining_bet_historyを生成するように修正し、OddsBandFilterがアンサンブルモデルの実際の精度を反映してキャリブレーションされるようにする（ODDS-01）。

Purpose: 現在run_backtest.pyの_collect_training_bet_history()はOptuna最適化済みstrategy_paramsをそのままtraining_bet_history生成に使用している。strategy_optimizer.py（Plan 01）と同様にデフォルトパラメータを使用するように修正し、パイプライン全体でルックアヘッドバイアスを除去する。これによりOddsBandFilter.calibrate()がアンサンブルモデルの真の精度を反映したバンド別ROIを計算する。

Output: run_backtest.py修正 + テスト
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-odds-band-rebuild/16-CONTEXT.md
@.planning/phases/16-odds-band-rebuild/16-RESEARCH.md
@.planning/phases/16-odds-band-rebuild/16-01-SUMMARY.md
@scripts/run_backtest.py
@src/betting/odds_band_filter.py
@src/betting/drawdown_controller.py
@src/betting/stake_calculator.py
@src/models/regime_detector.py
</context>

<interfaces>
<!-- Executor needs these contracts from the codebase -->

From src/betting/drawdown_controller.py:
```python
@dataclass
class DDConfig:
    rolling_window: int = 400
    dd_threshold_1: float = 0.10
    dd_threshold_2: float = 0.20
    multiplier_normal: float = 1.0
    multiplier_reduced: float = 0.50
    multiplier_stop: float = 0.0
    min_stay_races: int = 10
```

From src/betting/stake_calculator.py:
```python
class StakeCalculator:
    def __init__(self, fractional_kelly=0.5, kelly_fraction_cap=0.25,
                 target_ev=1.10, max_scale=2.0) -> None: ...
```

From scripts/run_backtest.py:
```python
def _collect_training_bet_history(
    models, store, train_start, train_end,
    betting_mode, betting_target, strategy_params,
) -> list[dict[str, Any]]:
    """トレーニング期間のバックテストを実行し、OddsBandFilter キャリブレーション用の
    bet_history を収集する。"""
    # Currently passes strategy_params directly to BacktestEngine
    # Line 267-274: train_engine = BacktestEngine(strategy_params=strategy_params)
```

<!-- From Plan 01 (must be completed first) -->
From src/tuning/strategy_optimizer.py (added in Plan 01):
```python
def _build_default_config(self) -> dict[str, Any]:
    """RegimeDetector既定値からデフォルトstrategy_configを構築"""
    # Returns dict with: dd_config, regime_overrides, fractional_kelly, target_ev, max_scale, roi_threshold
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _collect_training_bet_history()をデフォルトパラメータ使用に修正</name>
  <files>scripts/run_backtest.py</files>
  <read_first>
    - scripts/run_backtest.py:239-281 (_collect_training_bet_history全文)
    - scripts/run_backtest.py:466-475 (単一年度の呼び出し箇所)
    - scripts/run_backtest.py:612-621 (マルチ年度の呼び出し箇所)
    - scripts/run_backtest.py:198-236 (_build_strategy_config_from_manifest — 既存のパターン)
    - src/betting/drawdown_controller.py:14-30 (DDConfigデフォルト値)
    - src/models/regime_detector.py:201-259 (_get_base_paramsのパラメータ値)
  </read_first>
  <behavior>
    - Test 1: _build_default_strategy_config()が必須キー(dd_config, regime_overrides, fractional_kelly, target_ev, max_scale, roi_threshold)を全て含むdictを返す
    - Test 2: _build_default_strategy_config()のdd_configがDDConfigデフォルト値を持つ
    - Test 3: _build_default_strategy_config()のregime_overrides["aggressive"]["fractional_kelly"]が0.50である
    - Test 4: _collect_training_bet_history()がstrategy_paramsに関わらず常にデフォルトパラメータでBacktestEngineを構築する
  </behavior>
  <action>
    **1. scripts/run_backtest.py に _build_default_strategy_config() 関数を追加:**

    `_build_strategy_config_from_manifest()`の直後（line 236以降）に追加する。

    strategy_optimizer.pyの_build_default_config()と同じロジックだが、関数（メソッドではない）として実装する:

    ```python
    def _build_default_strategy_config() -> dict[str, Any]:
        """RegimeDetector既定値からデフォルトstrategy_configを構築 (ルックアヘッド防止)。

        run_backtest.pyの_collect_training_bet_history()で使用。
        strategy_optimizer.py._build_default_config()と同じ値を返す。
        """
        from betting.drawdown_controller import DDConfig
        from domain.types import RegimeState
        from models.regime_detector import RegimeDetector

        detector = RegimeDetector()
        conservative_params = detector._get_base_params(RegimeState.CONSERVATIVE)
        dd_config = DDConfig()

        regime_overrides = {}
        for regime_key in ("aggressive", "conservative"):
            state = RegimeState(regime_key)
            base = detector._get_base_params(state)
            regime_overrides[regime_key] = {
                "fractional_kelly": base["fractional_kelly"],
                "ev_threshold": base["ev_threshold"],
                "edge_threshold": base["edge_threshold"],
            }

        return {
            "dd_config": dd_config,
            "regime_overrides": regime_overrides,
            "fractional_kelly": conservative_params["fractional_kelly"],
            "target_ev": 1.10,
            "max_scale": 2.0,
            "roi_threshold": 1.0,
        }
    ```

    **2. _collect_training_bet_history() を修正:**

    現在のline 267-274を変更する:

    Before:
    ```python
    train_engine = BacktestEngine(
        models=models,
        store=store,
        betting_mode=betting_mode,
        diag_prefix="bt_train",
        betting_target=betting_target,
        strategy_params=strategy_params,
    )
    ```

    After:
    ```python
    # D-07: デフォルトパラメータでtraining_bet_historyを生成 (ルックアヘッド防止)
    # strategy_paramsに関わらず常にデフォルトパラメータを使用する
    default_train_config = _build_default_strategy_config()
    train_engine = BacktestEngine(
        models=models,
        store=store,
        betting_mode=betting_mode,
        diag_prefix="bt_train",
        betting_target=betting_target,
        strategy_params=default_train_config,
    )
    ```

    `_collect_training_bet_history()`の`strategy_params`引数は保持する（呼び出し側のインターフェース変更を避けるため）が、関数内部では使用しなくなる。関数のdocstringに「常にデフォルトパラメータを使用」の説明を追加する。

    **3. テスト追加:**

    テストファイル`tests/test_run_backtest_training.py`を新規作成する:

    ```python
    """scripts/run_backtest.py の training_bet_history デフォルトパラメータテスト"""
    from __future__ import annotations

    from unittest.mock import MagicMock, patch

    import pytest

    # src/ is on pythonpath via pyproject.toml


    class TestBuildDefaultStrategyConfig:
        def test_returns_all_required_keys(self):
            """_build_default_strategy_config()が必須キーを全て含む"""
            import importlib
            import sys
            # run_backtest.pyはscripts/にあるため直接importできない
            # 代わりにロジックを直接テストする
            from betting.drawdown_controller import DDConfig
            from domain.types import RegimeState
            from models.regime_detector import RegimeDetector

            detector = RegimeDetector()
            conservative_params = detector._get_base_params(RegimeState.CONSERVATIVE)
            dd_config = DDConfig()

            regime_overrides = {}
            for regime_key in ("aggressive", "conservative"):
                state = RegimeState(regime_key)
                base = detector._get_base_params(state)
                regime_overrides[regime_key] = {
                    "fractional_kelly": base["fractional_kelly"],
                    "ev_threshold": base["ev_threshold"],
                    "edge_threshold": base["edge_threshold"],
                }

            config = {
                "dd_config": dd_config,
                "regime_overrides": regime_overrides,
                "fractional_kelly": conservative_params["fractional_kelly"],
                "target_ev": 1.10,
                "max_scale": 2.0,
                "roi_threshold": 1.0,
            }

            for key in ("dd_config", "regime_overrides", "fractional_kelly",
                        "target_ev", "max_scale", "roi_threshold"):
                assert key in config, f"Missing key: {key}"

        def test_dd_config_defaults(self):
            """DDConfigデフォルト値がOptuna探索範囲内にある"""
            from betting.drawdown_controller import DDConfig
            dd = DDConfig()
            assert dd.dd_threshold_1 == 0.10
            assert dd.dd_threshold_2 == 0.20
            assert 0.05 <= dd.dd_threshold_1 <= 0.20  # Optuna範囲
            assert 0.15 <= dd.dd_threshold_2 <= 0.35  # Optuna範囲

        def test_aggressive_fractional_kelly_is_0_50(self):
            """AGGRESSIVEのfractional_kellyが0.50"""
            from domain.types import RegimeState
            from models.regime_detector import RegimeDetector
            detector = RegimeDetector()
            base = detector._get_base_params(RegimeState.AGGRESSIVE)
            assert base["fractional_kelly"] == 0.50

        def test_collect_training_uses_default_config(self):
            """_collect_training_bet_history()がデフォルトパラメータで
            BacktestEngineを構築することを検証"""
            import sys
            from pathlib import Path
            from unittest.mock import MagicMock, patch

            # scripts/ をimport pathに追加
            scripts_dir = str(Path(__file__).resolve().parent.parent / "scripts")
            if scripts_dir not in sys.path:
                sys.path.insert(0, scripts_dir)

            # run_backtestをimport（mockで重い依存を回避）
            with patch("db.parquet_store.ParquetStore"), \
                 patch("pipelines.training_pipeline.TrainingPipelineV5"):
                import importlib
                import run_backtest
                importlib.reload(run_backtest)

                mock_models = MagicMock()
                mock_store = MagicMock()
                mock_result = MagicMock()
                mock_result.total_bets = 100
                mock_result.total_roi = 0.95
                mock_result.bet_history = [{"odds": 5.0, "result": 500, "stake": 100}]

                with patch("run_backtest.BacktestEngine") as MockEngine:
                    MockEngine.return_value.run.return_value = mock_result

                    # Optuna最適化済みパラメータを渡しても...
                    optuna_params = {
                        "dd_config": MagicMock(),
                        "regime_overrides": {"aggressive": {"fractional_kelly": 0.99}},
                        "fractional_kelly": 0.99,
                        "target_ev": 2.0,
                        "max_scale": 3.0,
                        "roi_threshold": 0.5,
                    }

                    run_backtest._collect_training_bet_history(
                        models=mock_models,
                        store=mock_store,
                        train_start="2020-01-01",
                        train_end="2023-12-31",
                        betting_mode="kelly",
                        betting_target="win",
                        strategy_params=optuna_params,
                    )

                    # ...デフォルトパラメータが使用されることを検証
                    engine_call = MockEngine.call_args
                    used_params = engine_call.kwargs.get("strategy_params", {})
                    assert used_params.get("fractional_kelly") == 0.25  # CONSERVATIVE値
                    assert used_params.get("target_ev") == 1.10
                    assert used_params.get("roi_threshold") == 1.0
    ```

    **注意:** テストのTestBuildDefaultStrategyConfig.test_collect_training_uses_default_configはscripts/run_backtest.pyのimportが重いため、失敗する場合は代替手段として`_collect_training_bet_history`のソースコードをgrepで検証する方法に切り替えること。
  </action>
  <acceptance_criteria>
    - grep "_build_default_strategy_config" scripts/run_backtest.py が2行以上マッチ（定義 + 呼び出し）
    - grep "default_train_config" scripts/run_backtest.py が2行マッチ（代入 + BacktestEngine引数）
    - grep "strategy_params=default_train_config" scripts/run_backtest.py が1行マッチ
    - grep "strategy_params=strategy_params" scripts/run_backtest.py の_collect_training_bet_history内でのマッチが0件（削除されていること）
    - _collect_training_bet_historyのdocstringに"デフォルトパラメータ"の記述がある
    - python -m pytest tests/test_run_backtest_training.py -v が全テストPASS
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_run_backtest_training.py -v</automated>
  </verify>
  <done>
    - _build_default_strategy_config()がRegimeDetector既定値からstrategy_config dictを構築する
    - _collect_training_bet_history()が常にデフォルトパラメータでBacktestEngineを構築する
    - テストでデフォルトパラメータ使用が検証される
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run_backtest.py → BacktestEngine | training_bet_history生成時のパラメータ注入 |
| BacktestEngine → OddsBandFilter | calibrate()に渡すbet_historyの品質 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Tampering | _collect_training_bet_history | mitigate | strategy_params引数を無視し常にdefault_train_configを使用 |
| T-16-04 | Elevation of Privilege | OddsBandFilter | accept | calibrate()はbet_historyからROI計算のみ、システム状態変更なし |
</threat_model>

<verification>
grep "strategy_params=default_train_config" scripts/run_backtest.py がマッチ
python -m pytest tests/test_run_backtest_training.py -v がPASS
</verification>

<success_criteria>
- _collect_training_bet_history()が常にデフォルトパラメータでtraining_bet_historyを生成する
- _build_default_strategy_config()がstrategy_optimizer.py._build_default_config()と同じ値を返す
- OddsBandFilter.calibrate()がデフォルトパラメータ由来のtraining_bet_historyでバンド別ROIを計算する
- テストがPASS
</success_criteria>

<output>
After completion, create `.planning/phases/16-odds-band-rebuild/16-02-SUMMARY.md`
</output>
