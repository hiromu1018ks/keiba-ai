---
phase: 17-optuna-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tuning/strategy_optimizer.py
  - tests/test_strategy_optimizer.py
autonomous: true
requirements: [OPT-01, OPT-02]

must_haves:
  truths:
    - "_suggest_params()が16次元を返す(14既存 + ev_lower_threshold_turf + ev_lower_threshold_dirt)"
    - "_generate_folds()がコンストラクタ引数n_foldsに基づいて動的にfoldを生成する"
    - "_objective()がtrial内でモデルロードを1回のみ実行し、training_bet_historyを1回のみキャッシュする"
    - "RegimeDetectorのmutable stateが各fold開始時にCR-01パターンでリセットされる"
    - "MedianPrunerが4fold環境に最適化された設定で動作する"
  artifacts:
    - path: "src/tuning/strategy_optimizer.py"
      provides: "StrategyOptimizer 4fold対応 + EV_lower 2次元 + モデルロード最適化"
      contains: "ev_lower_threshold_turf"
    - path: "tests/test_strategy_optimizer.py"
      provides: "4fold, EV_lower, モデルロード最適化のテスト"
      exports: ["TestSuggestParams", "TestGenerateFolds", "TestObjective"]
  key_links:
    - from: "src/tuning/strategy_optimizer.py"
      to: "src/betting/default_strategy.py"
      via: "_build_default_config() delegation"
      pattern: "build_default_strategy_config"
    - from: "src/tuning/strategy_optimizer.py"
      to: "src/domain/models.py"
      via: "SubmodelSet.ev_lower_threshold_turf/dirt属性設定"
      pattern: "ev_lower_threshold_turf"
---

<objective>
StrategyOptimizerを4fold対応にリファクタリングし、EV_lower閾値を15-16次元目として追加し、trial内モデルロード最適化を実装する。

Purpose: OPT-01(アンサンブル16次元最適化)とOPT-02(4fold過学習防止)を実現する。既存の2fold/14次元実装を拡張し、Phase 15-16で再キャリブレーションしたフィルター群に適合した最適化インフラを構築する。
Output: 4fold動的生成対応StrategyOptimizer + EV_lower 2次元追加 + モデルロード最適化 + 既存テスト更新
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-optuna-optimization/17-CONTEXT.md
@.planning/phases/17-optuna-optimization/17-RESEARCH.md

<interfaces>
<!-- Executor needs these contracts. No codebase exploration required. -->

From src/domain/models.py:255-257 (SubmodelSet EV_lower attributes):
```python
ev_lower_threshold_turf: float = 1.0
ev_lower_threshold_dirt: float = 1.0
```

From src/backtest/race_predictor.py:443-448 (EV_lower threshold read path):
```python
_sm = self.models.submodels.get(surf_key)
if _sm is not None:
    if surf_key == "turf":
        threshold = float(getattr(_sm, "ev_lower_threshold_turf", 1.0))
    elif surf_key == "dirt":
        threshold = float(getattr(_sm, "ev_lower_threshold_dirt", 1.0))
```

From src/domain/types.py:29-34 (RegimeState):
```python
class RegimeState(str, Enum):
    AGGRESSIVE = "aggressive"
    CONSERVATIVE = "conservative"
    COLLAPSED = "collapsed"
```

From src/backtest/parameter_freeze_protocol.py:107-128 (save_strategy_manifest):
```python
def save_strategy_manifest(params: dict[str, Any], path: Path) -> str:
    data = json.dumps(params, sort_keys=True, indent=2)
    sha256 = hashlib.sha256(data.encode()).hexdigest()
    manifest = {"params": params, "sha256": sha256}
    path.write_text(json.dumps(manifest, indent=2), encoding="utf-8")
    return sha256
```

From src/betting/default_strategy.py:20-67 (build_strategy_config_from_params):
```python
def build_strategy_config_from_params(params: dict[str, Any]) -> dict[str, Any]:
    # Returns dict with keys: dd_config, regime_overrides, fractional_kelly, target_ev, max_scale, roi_threshold
    # Does NOT include ev_lower_threshold_turf/dirt — those are set on SubmodelSet directly
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _generate_folds()動的4fold化 + _suggest_params()にEV_lower 2次元追加 + _objective()モデルロード最適化</name>
  <files>src/tuning/strategy_optimizer.py, tests/test_strategy_optimizer.py</files>
  <read_first>
    - src/tuning/strategy_optimizer.py (全体)
    - tests/test_strategy_optimizer.py (全体)
    - src/betting/default_strategy.py (全体)
    - src/domain/models.py:255-257 (SubmodelSet EV_lower属性)
    - src/backtest/race_predictor.py:430-460 (EV_lower閾値読み取り経路)
  </read_first>
  <behavior>
    - Test 1: _suggest_params()が16次元を返す(既存14 + ev_lower_threshold_turf + ev_lower_threshold_dirt)
    - Test 2: _generate_folds()がn_folds=4で["2022-01-01","2022-12-31"],["2023-01-01","2023-12-31"],["2024-01-01","2024-12-31"],["2025-01-01","2025-12-31"]の4foldを返す
    - Test 3: _generate_folds()がデフォルト(n_folds=2)で2foldを返す(後方互換)
    - Test 4: _objective()内でModelLoader.load_from_dir()がtrial内1回のみ呼ばれる
    - Test 5: _objective()内でtraining_bet_history生成がtrial内1回のみ実行される
    - Test 6: 各fold開始時にRegimeDetectorの4属性(_current_regime, _regime_counter, _pending_regime, _collapsed_consecutive)がリセットされる
    - Test 7: EV_lower閾値がSubmodelSet属性に設定される(turfとdirtそれぞれ)
  </behavior>
  <action>
    **strategy_optimizer.pyの変更:**

    1. **__init__()**: `fold_start_year: int = 2022` 引数を追加し、`self.fold_start_year = fold_start_year`を格納する。既存の`n_folds`デフォルト値を`2`から`4`に変更する(D-02)。

    2. **_suggest_params()**: 既存14次元の後にEV_lower 2次元を追加(D-07):
       ```python
       # D-07: EV_lower閾値 15-16次元目 (サーフェス別)
       params["ev_lower_threshold_turf"] = trial.suggest_float(
           "ev_lower_threshold_turf", 0.5, 1.5
       )
       params["ev_lower_threshold_dirt"] = trial.suggest_float(
           "ev_lower_threshold_dirt", 0.5, 1.5
       )
       ```
       docstringの"~16次元"を"16次元"に更新。戻り値の合計は16次元。

    3. **_generate_folds()**: ハードコード2foldを動的生成に変更(D-02):
       ```python
       def _generate_folds(self) -> list[tuple[str, str]]:
           """D-02: 年次fold動的生成 (コンストラクタ引数ベース)"""
           folds = []
           for i in range(self.n_folds):
               year = self.fold_start_year + i
               folds.append((f"{year}-01-01", f"{year}-12-31"))
           return folds
       ```

    4. **_objective()**: モデルロード最適化(D-05) — trial内1回ロード + 1回training_bet_history生成 + 各foldでリセット:
       現在の_objective()は`self._run_single_backtest()`をfold毎に呼び出し、その中で毎回ModelLoader.load_from_dir()を実行している。これを以下のようにリファクタリング:
       - _objective()の先頭で`ModelLoader.load_from_dir()`を1回呼び出す
       - _objective()内でデフォルトconfigによるtraining_bet_historyを1回のみ生成
       - 各foldでは`_run_single_backtest_with_models()`(新メソッド)を呼び出す
       - 各fold開始時にRegimeDetectorの4属性をリセット(CR-01パターン)
       - EV_lower閾値をSubmodelSet属性に設定(Pitfall 1回避)
       - MedianPruner用のtrial.report(step=fold_idx)はそのまま維持

       新しい_objective()の構造:
       ```python
       def _objective(self, trial: optuna.Trial) -> float:
           params = self._suggest_params(trial)
           strategy_config = self._build_strategy_config(params)

           # D-05: モデルロード最適化 — trial内1回
           from db.model_loader import ModelLoader
           loader = ModelLoader()
           models, info = loader.load_from_dir(self.models_dir, use_ensemble_override=True)

           # D-05: training_bet_historyキャッシュ — trial内1回
           default_config = self._build_default_config()
           default_regime_overrides = default_config.get("regime_overrides")
           if default_regime_overrides:
               models.regime_detector._override_params = default_regime_overrides
           training_bet_history = self._generate_training_bet_history(
               models, info, default_config,
           )

           # D-07: EV_lower閾値をSubmodelSet属性に設定
           ev_lower_turf = params.get("ev_lower_threshold_turf", 1.0)
           ev_lower_dirt = params.get("ev_lower_threshold_dirt", 1.0)
           for surf_key, sm in models.submodels.items():
               if surf_key == "turf":
                   sm.ev_lower_threshold_turf = ev_lower_turf
               elif surf_key == "dirt":
                   sm.ev_lower_threshold_dirt = ev_lower_dirt

           # Walk-forward fold評価
           folds = self._generate_folds()
           rois: list[float] = []
           total_bets = 0

           for fold_idx, (test_start, test_end) in enumerate(folds):
               # CR-01: RegimeDetector状態リセット
               from domain.types import RegimeState
               models.regime_detector._current_regime = RegimeState.CONSERVATIVE
               models.regime_detector._regime_counter = 0
               models.regime_detector._pending_regime = None
               models.regime_detector._collapsed_consecutive = 0

               # regime_overridesをOptuna値で上書き
               regime_overrides = strategy_config.get("regime_overrides")
               if regime_overrides:
                   models.regime_detector._override_params = regime_overrides

               result = self._run_single_backtest_with_models(
                   models, strategy_config, test_start, test_end,
                   training_bet_history, fold_idx,
               )
               rois.append(result.get("roi", 0.0))
               total_bets += result.get("n_bets", 0)

               if trial is not None:
                   trial.report(result.get("roi", 0.0), step=fold_idx)
                   if trial.should_prune():
                       raise optuna.TrialPruned()

           mean_roi = float(np.mean(rois)) if rois else 0.0
           avg_bets_per_fold = total_bets / max(len(folds), 1)
           if avg_bets_per_fold < self.min_bets_per_fold:
               return -1.0
           return mean_roi
       ```

    5. **新メソッド _generate_training_bet_history()**: デフォルトconfigでtraining_bet_historyを生成するヘルパー。既存_run_single_backtest()のステップ2-3を抽出:
       ```python
       def _generate_training_bet_history(
           self,
           models: Any,
           info: Any,
           default_config: dict[str, Any],
       ) -> list[dict[str, Any]] | None:
           """D-05: training_bet_historyをデフォルトパラメータで1回生成"""
           from backtest.engine import BacktestEngine
           try:
               train_engine = BacktestEngine(
                   models=models,
                   initial_bankroll=self.initial_bankroll,
                   betting_mode="kelly",
                   diag_prefix="opt_train",
                   betting_target="win",
                   strategy_params=default_config,
               )
               train_result = train_engine.run(info.train_start, info.train_end)
               return train_result.bet_history
           except Exception as e:
               logger.warning("Training-phase backtest failed: %s — skipping calibration", e)
               return None
       ```

    6. **新メソッド _run_single_backtest_with_models()**: 既存_run_single_backtest()からモデルロード部分を除去し、外部からmodelsとtraining_bet_historyを受け取る版:
       ```python
       def _run_single_backtest_with_models(
           self,
           models: Any,
           strategy_config: dict[str, Any],
           test_start: str,
           test_end: str,
           training_bet_history: list[dict[str, Any]] | None,
           fold_idx: int = 0,
       ) -> dict[str, Any]:
           """D-05: モデル共有版バックテスト実行"""
           from backtest.engine import BacktestEngine
           engine = BacktestEngine(
               models=models,
               initial_bankroll=self.initial_bankroll,
               betting_mode="kelly",
               diag_prefix=f"opt_fold{fold_idx}",
               betting_target="win",
               strategy_params=strategy_config,
           )
           result = engine.run(test_start, test_end, training_bet_history=training_bet_history)
           return {
               "roi": result.total_roi,
               "n_bets": result.total_bets,
               "total_stake": result.total_stake,
               "total_return": result.total_return,
               "max_drawdown": result.max_drawdown,
           }
       ```

    7. **既存_run_single_backtest()**: そのまま残す(後方互換。CLIからの単一fold実行等で使用可能性)。

    8. **optimize()**: MedianPrunerの設定を4fold環境に最適化(D-06):
       ```python
       pruner = MedianPruner(
           n_startup_trials=10,
           n_warmup_steps=0,
           interval_steps=1,
           n_min_trials=1,
       )
       ```

    **test_strategy_optimizer.pyの変更:**

    1. **TestSuggestParams.test_suggest_returns_all_dimensions**: assertを`len(params) >= 14`から`len(params) == 16`に更新。`ev_lower_threshold_turf`と`ev_lower_threshold_dirt`がparamsに含まれることをassert追加。

    2. **TestSuggestParams新規test**: EV_lower閾値の範囲が[0.5, 1.5]であることを確認:
       ```python
       def test_ev_lower_thresholds_in_range(self, optimizer):
           study = optuna.create_study(direction="maximize")
           trial = study.ask()
           params = optimizer._suggest_params(trial)
           assert 0.5 <= params["ev_lower_threshold_turf"] <= 1.5
           assert 0.5 <= params["ev_lower_threshold_dirt"] <= 1.5
       ```

    3. **TestGenerateFolds新規クラス**:
       ```python
       class TestGenerateFolds:
           def test_4fold_default(self):
               opt = StrategyOptimizer(n_folds=4, fold_start_year=2022)
               folds = opt._generate_folds()
               assert len(folds) == 4
               assert folds[0] == ("2022-01-01", "2022-12-31")
               assert folds[3] == ("2025-01-01", "2025-12-31")

           def test_2fold_backward_compat(self):
               opt = StrategyOptimizer(n_folds=2, fold_start_year=2024)
               folds = opt._generate_folds()
               assert len(folds) == 2
               assert folds[0] == ("2024-01-01", "2024-12-31")
               assert folds[1] == ("2025-01-01", "2025-12-31")

           def test_custom_start_year(self):
               opt = StrategyOptimizer(n_folds=3, fold_start_year=2021)
               folds = opt._generate_folds()
               assert len(folds) == 3
               assert folds[0] == ("2021-01-01", "2021-12-31")
       ```

    4. **TestObjective更新**: mock_backtestをmockする代わりに_run_single_backtest_with_modelsをmockするよう更新。ModelLoader.load_from_dirのmockも追加(trial内1回呼び出しを検証)。

    5. **TestObjective新規test(test_model_load_optimization)**: _objective()実行時にModelLoader.load_from_dir()が1回のみ呼ばれることを検証。

    6. **TestObjective新規test(test_regime_reset_per_fold)**: 複数fold実行時に各fold開始前にRegimeDetector状態がリセットされることを検証。

    7. **TestObjective新規test(test_ev_lower_set_on_submodels)**: EV_lower閾値がSubmodelSet属性に設定されることを検証。

    **重要: import path変更**
    `from domain.types import RegimeState`は_objective()内でのインポートのままでよい(トップレベル循環import回避)。同様に`from db.model_loader import ModelLoader`も関数内インポート。
  </action>
  <verify>
    <automated>cd C:\Users\hirom\develop\keiba-ai && python -m pytest tests/test_strategy_optimizer.py -v</automated>
  </verify>
  <done>
    - _suggest_params()が16次元を返す(ev_lower_threshold_turf + ev_lower_threshold_dirt含む)
    - _generate_folds()がfold_start_yearとn_foldsから動的にfoldを生成する
    - _objective()がtrial内でModelLoader.load_from_dir()を1回のみ呼び出す
    - _objective()がtraining_bet_historyを1回のみキャッシュする
    - 各fold開始時にRegimeDetector状態がCR-01パターンでリセットされる
    - EV_lower閾値がSubmodelSet属性(turf/dirt)に設定される
    - MedianPrunerがn_startup_trials=10, n_warmup_steps=0, interval_steps=1, n_min_trials=1で動作する
    - 全テストがPASS
  </done>
  <acceptance_criteria>
    - grep -c "ev_lower_threshold_turf" src/tuning/strategy_optimizer.py の結果が >= 2 (suggest + 属性設定)
    - grep -c "ev_lower_threshold_dirt" src/tuning/strategy_optimizer.py の結果が >= 2
    - grep "fold_start_year" src/tuning/strategy_optimizer.py の結果に self.fold_start_year が含まれる
    - grep "n_startup_trials=10" src/tuning/strategy_optimizer.py の結果が1行
    - grep "_run_single_backtest_with_models" src/tuning/strategy_optimizer.py の結果が3行以上(定義 + 呼び出し)
    - grep "_generate_training_bet_history" src/tuning/strategy_optimizer.py の結果が3行以上(定義 + 呼び出し)
    - python -m pytest tests/test_strategy_optimizer.py -v の結果が全テストPASS
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Optuna trial → BacktestEngine | 最適化パラメータがバックテスト評価に注入される。不正なパラメータ範囲が過学習を引き起こす可能性 |
| ModelLoader → StrategyOptimizer | 学習済みモデルのロード。モデル不存在や破損のリスク |
| RegimeDetector state | fold間でmutable stateがリークするリスク。CR-01リセットで対策済み |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | E (Elevation of Privilege) | _objective() モデルロード | mitigate | ModelLoader.load_from_dir()はtrial内1回のみ。不必要な再ロードを排除してI/O攻撃面を縮小 |
| T-17-02 | T (Tampering) | RegimeDetector mutable state | mitigate | CR-01パターン: 4属性を各fold開始時に初期値にリセット(_current_regime=CONSERVATIVE, _regime_counter=0, _pending_regime=None, _collapsed_consecutive=0) |
| T-17-03 | I (Information Disclosure) | EV_lower探索範囲 | accept | [0.5, 1.5]の範囲は設定値であり、機密性の高い情報ではない。Phase 15 OOF分布に基づく合理的な範囲 |
| T-17-04 | S (Spoofing) | Optuna params → SubmodelSet属性注入 | mitigate | EV_lower閾値はSubmodelSet属性に直接設定。getattr()のデフォルト値1.0がフォールバックとして機能 |
| T-17-05 | D (Denial of Service) | MedianPruner設定 | accept | 4fold環境でn_startup_trials=10は合理的。過剰pruningやpruning不足のリスクは低い |
</threat_model>

<verification>
1. `python -m pytest tests/test_strategy_optimizer.py -v` — 全テストPASS
2. `ruff check src/tuning/strategy_optimizer.py tests/test_strategy_optimizer.py` — lintエラーなし
3. `python -c "from tuning.strategy_optimizer import StrategyOptimizer; o = StrategyOptimizer(n_folds=4, fold_start_year=2022); print(len(o._generate_folds()))"` — 4を出力
4. `python -c "import optuna; from tuning.strategy_optimizer import StrategyOptimizer; o = StrategyOptimizer(); s = optuna.create_study(); t = s.ask(); p = o._suggest_params(t); print(len(p))"` — 16を出力
</verification>

<success_criteria>
- _suggest_params()が16次元のdictを返す
- _generate_folds()がn_folds=4, fold_start_year=2022で4foldを動的生成する
- _objective()がtrial内でモデルロードを1回のみ実行する
- training_bet_historyがtrial内で1回のみ生成される
- 各foldでRegimeDetector状態が完全リセットされる
- EV_lower閾値がSubmodelSet属性(turf/dirt)に正しく設定される
- MedianPrunerが4fold最適化設定で動作する
- 全テストがPASS
</success_criteria>

<output>
After completion, create `.planning/phases/17-optuna-optimization/17-01-SUMMARY.md`
</output>
