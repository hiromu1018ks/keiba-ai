---
phase: 17-optuna-optimization
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/tuning/strategy_optimizer.py
  - scripts/run_strategy_optimization.py
  - tests/test_strategy_optimizer.py
autonomous: true
requirements: [OPT-03]

must_haves:
  truths:
    - "3 seeds(42/43/44)でOptuna最適化を実行し、各seedのbest_paramsを取得できる"
    - "3 seed間のパラメータ安定性がCV(変動係数)で定量化され、不安定次元が特定される"
    - "不安定次元がデフォルト値に固定され、探索空間を縮小した再最適化が実行される"
    - "安定性レポートがJSONファイルとして出力される"
    - "CLIスクリプトで--seeds引数によりmulti-seed実行を指定できる"
  artifacts:
    - path: "src/tuning/strategy_optimizer.py"
      provides: "optimize_multi_seed() + _compute_stability_report() + _optimize_with_fixed_dims()"
      contains: "optimize_multi_seed"
    - path: "scripts/run_strategy_optimization.py"
      provides: "Multi-seed CLI拡張"
      contains: "--seeds"
    - path: "tests/test_strategy_optimizer.py"
      provides: "Multi-seed安定性検証テスト"
      contains: "TestMultiSeedStability"
  key_links:
    - from: "src/tuning/strategy_optimizer.py"
      to: "scripts/run_strategy_optimization.py"
      via: "optimize_multi_seed()呼び出し"
      pattern: "optimize_multi_seed"
    - from: "src/tuning/strategy_optimizer.py"
      to: "data/stability_report.json"
      via: "JSON出力"
      pattern: "stability_report"
---

<objective>
StrategyOptimizerにmulti-seed安定性検証を追加し、3 seed間のパラメータ安定性を定量化・報告する。不安定次元の自動固定化と再最適化機能を実装し、CLIスクリプトに--seeds引数を追加する。

Purpose: OPT-03(複数seedでパラメータ安定性検証)を実現する。単一seed最適化の過学習リスクを検出し、不安定な次元を特定して堅牢なパラメータセットを導出する。
Output: optimize_multi_seed() + 安定性レポートJSON + CLI --seeds拡張 + テスト
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-optuna-optimization/17-CONTEXT.md
@.planning/phases/17-optuna-optimization/17-RESEARCH.md
@.planning/phases/17-optuna-optimization/17-01-SUMMARY.md

<interfaces>
<!-- Plan 01 creates these. Executor must use them directly. -->

From src/tuning/strategy_optimizer.py (Plan 01 additions):
```python
class StrategyOptimizer:
    def __init__(self, ..., n_folds: int = 4, fold_start_year: int = 2022, ...) -> None: ...
    def _suggest_params(self, trial: optuna.Trial) -> dict[str, Any]:
        # Returns 16 dimensions (14 existing + ev_lower_threshold_turf + ev_lower_threshold_dirt)
    def _generate_folds(self) -> list[tuple[str, str]]:
        # Dynamic fold generation from fold_start_year + n_folds
    def _objective(self, trial: optuna.Trial) -> float:
        # Model load once per trial, training_bet_history cached once, CR-01 reset per fold
    def optimize(self, n_trials: int = 100, seed: int = 42, output_path: Path | str | None = None) -> dict[str, Any]:
        # Returns {"best_params": dict, "best_value": float, "n_trials": int, "n_pruned": int}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: optimize_multi_seed() + _compute_stability_report() + _optimize_with_fixed_dims() + CLI --seeds拡張</name>
  <files>src/tuning/strategy_optimizer.py, scripts/run_strategy_optimization.py, tests/test_strategy_optimizer.py</files>
  <read_first>
    - src/tuning/strategy_optimizer.py (全体 — Plan 01適用後の状態)
    - tests/test_strategy_optimizer.py (全体 — Plan 01適用後の状態)
    - scripts/run_strategy_optimization.py (全体)
    - src/backtest/parameter_freeze_protocol.py:107-128 (save_strategy_manifest)
  </read_first>
  <behavior>
    - Test 1: optimize_multi_seed()が3 seed(42/43/44)で最適化を実行し、各seed結果を返す
    - Test 2: _compute_stability_report()が各次元のCV(変動係数)を計算し、CV>0.20の次元を不安定と判定する
    - Test 3: _optimize_with_fixed_dims()が不安定次元をデフォルト値に固定し、探索空間を縮小して再最適化する
    - Test 4: 安定性レポートJSONが正しいスキーマ(version, timestamp, seeds, dimensions, best_roi_by_seed, mean_best_roi, reoptimization)で出力される
    - Test 5: CLI --seeds引数がパースされ、optimize_multi_seed()に渡される
    - Test 6: 主seed(42)が100 trials、追加seed(43/44)が50 trialsで実行される(D-08)
    - Test 7: 不安定次元がない場合、再最適化がスキップされる
  </behavior>
  <action>
    **strategy_optimizer.pyに追加するメソッド:**

    1. **optimize_multi_seed()**: D-08/D-09/D-10に基づくmulti-seed安定性検証のエントリポイント:
       ```python
       def optimize_multi_seed(
           self,
           n_trials: int = 100,
           seeds: list[int] | None = None,
           output_dir: Path | str | None = None,
       ) -> dict[str, Any]:
           """D-08/D-09/D-10: multi-seed安定性検証

           Args:
               n_trials: 主seedの試行回数。追加seedはn_trials//2
               seeds: seedリスト。デフォルト[42, 43, 44]
               output_dir: 安定性レポート出力ディレクトリ

           Returns:
               安定性レポートdict
           """
           import json
           from datetime import datetime, timezone

           if seeds is None:
               seeds = [42, 43, 44]

           # D-08: 各seedで最適化実行
           seed_results: dict[int, dict] = {}
           for i, seed in enumerate(seeds):
               n = n_trials if i == 0 else n_trials // 2  # D-08: 主100 + 追加50
               logger.info("Multi-seed optimization: seed=%d, n_trials=%d (%d/%d)",
                           seed, n, i + 1, len(seeds))
               result = self.optimize(n_trials=n, seed=seed)
               seed_results[seed] = result

           # D-09: 安定性分析
           stability_report = self._compute_stability_report(seed_results)
           stability_report["version"] = "1.0"
           stability_report["timestamp"] = datetime.now(timezone.utc).isoformat()
           stability_report["seeds"] = seeds

           # D-10: 不安定次元の固定化と再最適化
           unstable_dims = [
               d for d, info in stability_report["dimensions"].items()
               if info["is_unstable"]
           ]
           if unstable_dims:
               logger.info("Unstable dimensions detected: %s — running reoptimization", unstable_dims)
               fixed_report = self._optimize_with_fixed_dims(
                   unstable_dims, seed_results[seeds[0]], n_trials, seeds,
               )
               stability_report["reoptimization"] = fixed_report
           else:
               stability_report["reoptimization"] = None
               logger.info("All dimensions stable across seeds")

           # レポート出力
           if output_dir is not None:
               output_path = Path(output_dir)
               output_path.mkdir(parents=True, exist_ok=True)
               report_path = output_path / "stability_report.json"
               report_path.write_text(
                   json.dumps(stability_report, indent=2, default=str),
                   encoding="utf-8",
               )
               logger.info("Stability report saved: %s", report_path)

               # 最良パラメータのmanifest保存
               best_seed = max(stability_report["best_roi_by_seed"].items(), key=lambda x: x[1])
               manifest_path = output_path / "strategy_manifest.json"
               from backtest.parameter_freeze_protocol import save_strategy_manifest
               save_strategy_manifest(
                   seed_results[int(best_seed[0])]["best_params"], manifest_path,
               )

           return stability_report
       ```

    2. **_compute_stability_report()**: D-09に基づく安定性分析:
       ```python
       def _compute_stability_report(
           self,
           seed_results: dict[int, dict],
       ) -> dict[str, Any]:
           """D-09: 3 seed間のパラメータ安定性分析

           CV(変動係数) = std/mean で各次元の安定性を評価。
           CV > 0.20 を「不安定」と判定。
           """
           seeds = list(seed_results.keys())
           all_param_names = list(seed_results[seeds[0]]["best_params"].keys())

           dimensions: dict[str, dict[str, Any]] = {}
           for param_name in all_param_names:
               values = [
                   seed_results[s]["best_params"].get(param_name, 0.0)
                   for s in seeds
               ]
               mean_val = float(np.mean(values))
               std_val = float(np.std(values))
               cv = std_val / mean_val if abs(mean_val) > 1e-10 else 0.0

               dimensions[param_name] = {
                   "values": values,
                   "mean": mean_val,
                   "std": std_val,
                   "cv": cv,
                   "is_unstable": cv > 0.20,
               }

           best_roi_by_seed = {
               str(s): seed_results[s]["best_value"]
               for s in seeds
           }

           return {
               "dimensions": dimensions,
               "best_roi_by_seed": best_roi_by_seed,
               "mean_best_roi": float(np.mean(list(best_roi_by_seed.values()))),
           }
       ```

    3. **_optimize_with_fixed_dims()**: D-10に基づく不安定次元固定化 + 再最適化:
       ```python
       def _optimize_with_fixed_dims(
           self,
           unstable_dims: list[str],
           primary_result: dict,
           n_trials: int,
           seeds: list[int],
       ) -> dict[str, Any]:
           """D-10: 不安定次元をデフォルト値に固定し再最適化

           1回のみ再実行(Pitfall 5回避)。残りの不安定次元は
           レポートで報告しPhase 18の最終検証に委ねる。
           """
           # 主seedのbest_paramsをベースに固定次元を設定
           fixed_params = dict(primary_result["best_params"])

           # 不安定次元をデフォルト値に固定
           default_config = self._build_default_config()
           # _suggest_params()のデフォルト値マッピング
           default_param_values: dict[str, float] = {
               "fk_aggressive": 0.45, "fk_conservative": 0.30,
               "ev_aggressive": 1.10, "ev_conservative": 1.30,
               "edge_aggressive": 0.05, "edge_conservative": 0.06,
               "dd_threshold_1": 0.10, "dd_threshold_2": 0.25,
               "multiplier_reduced": 0.5, "rolling_window": 400,
               "min_stay_races": 10, "target_ev": 1.10,
               "max_scale": 2.0, "roi_threshold": 1.0,
               "ev_lower_threshold_turf": 1.0,
               "ev_lower_threshold_dirt": 1.0,
           }
           for dim in unstable_dims:
               fixed_params[dim] = default_param_values.get(dim, 1.0)

           # 固定次元を考慮した_objective_fixed()で再最適化
           original_suggest = self._suggest_params

           def _suggest_fixed(trial: optuna.Trial) -> dict[str, Any]:
               params = original_suggest(trial)
               for dim in unstable_dims:
                   if dim in params:
                       del params[dim]
               for dim in unstable_dims:
                   params[dim] = fixed_params[dim]
               return params

           # 一時的に_suggest_paramsを差し替え
           self._suggest_params = _suggest_fixed  # type: ignore[assignment]

           try:
               reopt_result = self.optimize(n_trials=n_trials, seed=seeds[0])
           finally:
               self._suggest_params = original_suggest  # type: ignore[assignment]

           return {
               "fixed_dimensions": unstable_dims,
               "fixed_values": {d: fixed_params[d] for d in unstable_dims},
               "reoptimized_best_params": reopt_result["best_params"],
               "reoptimized_best_roi": reopt_result["best_value"],
           }
       ```

    **run_strategy_optimization.pyの変更:**

    1. `--seeds`引数を追加:
       ```python
       parser.add_argument(
           "--seeds", type=str, default=None,
           help="Multi-seed安定性検証用のカンマ区切りseedリスト (例: 42,43,44)",
       )
       ```

    2. main()の分岐: `--seeds`が指定された場合`optimize_multi_seed()`を呼び出し、未指定の場合は従来通り`optimize()`を呼び出す:
       ```python
       if args.seeds is not None:
           seeds = [int(s.strip()) for s in args.seeds.split(",")]
           logger.info("Multi-seed stability optimization: seeds=%s", seeds)
           result = optimizer.optimize_multi_seed(
               n_trials=args.n_trials,
               seeds=seeds,
               output_dir=Path(args.output).parent,
           )
           logger.info("Stability report generated. Mean best ROI: %.4f",
                       result.get("mean_best_roi", 0.0))
       else:
           result = optimizer.optimize(
               n_trials=args.n_trials,
               seed=args.seed,
               output_path=Path(args.output),
           )
           logger.info(
               "Best ROI: %.4f, Best params: %s, Trials: %d, Pruned: %d",
               result["best_value"], result["best_params"],
               result["n_trials"], result["n_pruned"],
           )
       ```

    **test_strategy_optimizer.pyの追加テスト:**

    ```python
    class TestMultiSeedStability:
        def test_optimize_multi_seed_returns_stability_report(self, optimizer):
            """optimize_multi_seed()が安定性レポートを返す"""
            def mock_optimize(n_trials, seed, output_path=None):
                return {
                    "best_params": {
                        "fk_aggressive": 0.45 + seed * 0.01,
                        "fk_conservative": 0.30,
                        "ev_aggressive": 1.10,
                        "ev_conservative": 1.30,
                        "edge_aggressive": 0.05,
                        "edge_conservative": 0.06,
                        "dd_threshold_1": 0.10,
                        "dd_threshold_2": 0.25,
                        "multiplier_reduced": 0.5,
                        "rolling_window": 400,
                        "min_stay_races": 10,
                        "target_ev": 1.10,
                        "max_scale": 2.0,
                        "roi_threshold": 1.0,
                        "ev_lower_threshold_turf": 1.0,
                        "ev_lower_threshold_dirt": 1.0,
                    },
                    "best_value": 1.10 + seed * 0.01,
                    "n_trials": n_trials,
                    "n_pruned": 0,
                }

            optimizer.optimize = mock_optimize
            report = optimizer.optimize_multi_seed(n_trials=10, seeds=[42, 43, 44])
            assert "dimensions" in report
            assert "best_roi_by_seed" in report
            assert "mean_best_roi" in report
            assert report["version"] == "1.0"
            assert report["seeds"] == [42, 43, 44]

        def test_stability_report_detects_unstable_dims(self, optimizer):
            """CV>0.20の次元を不安定として検出する"""
            seed_results = {
                42: {"best_params": {"fk_aggressive": 0.45, "fk_conservative": 0.30},
                     "best_value": 1.10, "n_trials": 10, "n_pruned": 0},
                43: {"best_params": {"fk_aggressive": 0.70, "fk_conservative": 0.31},
                     "best_value": 1.08, "n_trials": 10, "n_pruned": 0},
                44: {"best_params": {"fk_aggressive": 0.20, "fk_conservative": 0.29},
                     "best_value": 1.09, "n_trials": 10, "n_pruned": 0},
            }
            report = optimizer._compute_stability_report(seed_results)
            # fk_aggressive: values=[0.45, 0.70, 0.20], mean=0.45, std~0.204, cv~0.45 > 0.20
            assert report["dimensions"]["fk_aggressive"]["is_unstable"] is True
            # fk_conservative: values=[0.30, 0.31, 0.29], mean~0.30, std~0.008, cv~0.027 < 0.20
            assert report["dimensions"]["fk_conservative"]["is_unstable"] is False

        def test_stability_report_no_unstable_skips_reopt(self, optimizer):
            """不安定次元なしの場合、reoptimization=None"""
            def mock_optimize(n_trials, seed, output_path=None):
                return {
                    "best_params": {"fk_aggressive": 0.45, "fk_conservative": 0.30,
                                    "ev_aggressive": 1.10, "ev_conservative": 1.30,
                                    "edge_aggressive": 0.05, "edge_conservative": 0.06,
                                    "dd_threshold_1": 0.10, "dd_threshold_2": 0.25,
                                    "multiplier_reduced": 0.5, "rolling_window": 400,
                                    "min_stay_races": 10, "target_ev": 1.10,
                                    "max_scale": 2.0, "roi_threshold": 1.0,
                                    "ev_lower_threshold_turf": 1.0,
                                    "ev_lower_threshold_dirt": 1.0},
                    "best_value": 1.10, "n_trials": n_trials, "n_pruned": 0,
                }
            optimizer.optimize = mock_optimize
            report = optimizer.optimize_multi_seed(n_trials=10, seeds=[42, 43, 44])
            assert report["reoptimization"] is None

        def test_primary_seed_gets_full_trials(self, optimizer):
            """D-08: 主seed(42)が100 trials、追加seed(43/44)が50 trials"""
            trial_counts = []
            original_optimize = optimizer.optimize

            def mock_optimize(n_trials, seed, output_path=None):
                trial_counts.append((seed, n_trials))
                return {"best_params": {}, "best_value": 1.0,
                        "n_trials": n_trials, "n_pruned": 0}

            optimizer.optimize = mock_optimize
            optimizer.optimize_multi_seed(n_trials=100, seeds=[42, 43, 44])

            assert trial_counts[0] == (42, 100)
            assert trial_counts[1] == (43, 50)
            assert trial_counts[2] == (44, 50)

        def test_saves_stability_report_json(self, optimizer, tmp_path):
            """安定性レポートがJSONで保存される"""
            def mock_optimize(n_trials, seed, output_path=None):
                return {"best_params": {}, "best_value": 1.0,
                        "n_trials": n_trials, "n_pruned": 0}
            optimizer.optimize = mock_optimize

            report = optimizer.optimize_multi_seed(
                n_trials=10, seeds=[42], output_dir=tmp_path,
            )
            report_path = tmp_path / "stability_report.json"
            assert report_path.exists()
            import json
            data = json.loads(report_path.read_text())
            assert "version" in data
            assert "dimensions" in data
    ```

    **重要な設計上の決定:**
    - CV閾値は0.20(20%)とする。D-09 Claude's discretion。Phase 17 RESEARCH A3を踏襲。
    - 不安定次元のデフォルト値マッピングは明示的にハードコード(_optimize_with_fixed_dims内)。RegimeDetector._get_base_params()の値と一致させる。
    - _optimize_with_fixed_dims()は1回のみ再実行(Pitfall 5回避)。
    - optimize_multi_seed()内で最良パラメータのmanifestも自動保存。
  </action>
  <verify>
    <automated>cd C:\Users\hirom\develop\keiba-ai && python -m pytest tests/test_strategy_optimizer.py -v</automated>
  </verify>
  <done>
    - optimize_multi_seed()が3 seedで最適化を実行する(主100 + 追加50 trials)
    - _compute_stability_report()がCV(変動係数)で安定性を定量化する(CV>0.20=不安定)
    - _optimize_with_fixed_dims()が不安定次元をデフォルト値に固定し再最適化する
    - 安定性レポートJSONが正しいスキーマで出力される
    - run_strategy_optimization.pyに--seeds引数が追加され、multi-seed実行をサポートする
    - 全テストがPASS
  </done>
  <acceptance_criteria>
    - grep "def optimize_multi_seed" src/tuning/strategy_optimizer.py の結果が1行
    - grep "def _compute_stability_report" src/tuning/strategy_optimizer.py の結果が1行
    - grep "def _optimize_with_fixed_dims" src/tuning/strategy_optimizer.py の結果が1行
    - grep "cv > 0.20" src/tuning/strategy_optimizer.py の結果が1行
    - grep "\-\-seeds" scripts/run_strategy_optimization.py の結果が1行以上
    - grep "TestMultiSeedStability" tests/test_strategy_optimizer.py の結果が1行
    - python -m pytest tests/test_strategy_optimizer.py::TestMultiSeedStability -v の結果が全テストPASS
    - python -m pytest tests/test_strategy_optimizer.py -v の結果が全テストPASS
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Multi-seed results → 安定性分析 | 3 seedのbest_paramsを統計的に比較。サンプルサイズ3は最小限であり、過度な信頼は避ける |
| 不安定次元固定 → 再最適化 | 固定値の選択が結果に影響する。デフォルト値(ハードコード)を使用することで任意性を排除 |
| 安定性レポート → Phase 18 | レポートはPhase 18の最終検証の入力。JSON改ざんリスクはPhase 18 ParameterFreezeProtocolで対策 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-06 | T (Tampering) | 安定性レポートJSON | accept | Phase 18のParameterFreezeProtocolでSHA256改ざん検知を適用予定 |
| T-17-07 | R (Repudiation) | _optimize_with_fixed_dims() デフォルト値 | mitigate | デフォルト値をハードコードし、RegimeDetector._get_base_params()と一致。変更時は明示的な値更新が必要 |
| T-17-08 | D (Denial of Service) | multi-seed実行の計算コスト | accept | 3 seeds x (100+50+50) trials = 200 trials。D-08で50に削減済み。実用的なコスト範囲 |
| T-17-09 | S (Spoofing) | CV閾値0.20 | accept | 統計的に合理的な閾値。低すぎると過度固定、高すぎると不安定次元を見逃す。Phase 18で人間が最終確認 |
</threat_model>

<verification>
1. `python -m pytest tests/test_strategy_optimizer.py -v` — 全テストPASS
2. `ruff check src/tuning/strategy_optimizer.py scripts/run_strategy_optimization.py tests/test_strategy_optimizer.py` — lintエラーなし
3. `python -c "from tuning.strategy_optimizer import StrategyOptimizer; o = StrategyOptimizer(); print(hasattr(o, 'optimize_multi_seed'))"` — Trueを出力
4. `python scripts/run_strategy_optimization.py --help | grep seeds` — --seeds引数が表示される
</verification>

<success_criteria>
- optimize_multi_seed()が3 seed(42/43/44)で最適化を実行する
- 主seed(42)がn_trials、追加seedがn_trials//2で実行される
- _compute_stability_report()がCV(変動係数)で各次元の安定性を定量化する
- CV>0.20の次元が不安定として検出される
- _optimize_with_fixed_dims()が不安定次元をデフォルト値に固定し再最適化する(1回のみ)
- 安定性レポートJSONがversion/timestamp/seeds/dimensions/best_roi_by_seed/mean_best_roi/reoptimizationスキーマで出力される
- CLI --seeds引数が動作する
- 全テストがPASS
</success_criteria>

<output>
After completion, create `.planning/phases/17-optuna-optimization/17-02-SUMMARY.md`
</output>
