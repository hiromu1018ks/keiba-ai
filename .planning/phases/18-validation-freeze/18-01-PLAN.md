---
phase: 18-validation-freeze
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backtest/engine.py
  - scripts/run_backtest.py
autonomous: true
requirements: [VAL-02]

must_haves:
  truths:
    - "BacktestEngine.run()がmanifest_pathを受け取った場合、run()先頭でverify_strategy_manifest()によるSHA256検証を実行する"
    - "BacktestEngine.run()がmanifest_pathを受け取った場合、PFP freeze()をrun()先頭で実行し、PFP verify()をrun()末尾(BacktestResult返却前)で実行する"
    - "PFP verify失敗時(RuntimeError)またはSHA256不一致時(ValueError)にバックテストが即時停止する"
    - "run_backtest.pyがmanifest_pathをBacktestEngineコンストラクタに渡す"
    - "--strategy-manifest単独(--ensembleなし)指定時に引数エラーとなる"
  artifacts:
    - path: "src/backtest/engine.py"
      provides: "BacktestEngine PFP freeze/verify + manifest SHA256再検証"
      contains: "manifest_path"
    - path: "scripts/run_backtest.py"
      provides: "manifest_path渡し + validate_args拡張"
      contains: "manifest_path"
  key_links:
    - from: "scripts/run_backtest.py"
      to: "src/backtest/engine.py"
      via: "BacktestEngine(manifest_path=...) コンストラクタ引数"
      pattern: "manifest_path"
    - from: "src/backtest/engine.py"
      to: "src/backtest/parameter_freeze_protocol.py"
      via: "verify_strategy_manifest + ParameterFreezeProtocol"
      pattern: "ParameterFreezeProtocol|verify_strategy_manifest"
---

<objective>
BacktestEngineにPFP freeze/verify二重検証を統合し、manifest SHA256改ざん検知をrun()内で実行できるようにする。run_backtest.pyがmanifest_pathをengineに渡す配線を追加する。

Purpose: VAL-02要件(OOS期間中のモデル不変性保証 + SHA256改ざん検知)をエンジン層で実現する。Phase 17で生成されたmanifestの検証をバックテスト実行フローに組み込む。
Output: engine.pyにPFP検証付きrun() + run_backtest.pyのmanifest_path渡し + --strategy-manifest/--ensemble組み合わせバリデーション
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-validation-freeze/18-CONTEXT.md
@.planning/phases/18-validation-freeze/18-RESEARCH.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/backtest/parameter_freeze_protocol.py:
```python
class ParameterFreezeProtocol:
    def __init__(self, models: TrainedModelsV5) -> None: ...
    def freeze(self) -> None: ...
    def verify(self) -> dict[str, Any]: ...  # {"passed": bool, "message": str}

def verify_strategy_manifest(path: Path) -> dict[str, Any]: ...  # Returns params dict, raises ValueError/FileNotFoundError
def save_strategy_manifest(params: dict, path: Path) -> str: ...  # Returns SHA256 hash
```

From src/backtest/engine.py (BacktestResult dataclass):
```python
@dataclass
class BacktestResult:
    total_bets: int = 0
    total_stake: float = 0.0
    total_return: float = 0.0
    winning_bets: int = 0
    total_roi: float = 0.0
    max_drawdown: float = 0.0
    final_bankroll: float = 0.0
    bet_history: list[dict[str, Any]] = field(default_factory=list)
    # ... more fields
```

From src/backtest/engine.py (BacktestEngine.__init__ signature, line ~359):
```python
def __init__(
    self,
    models: TrainedModelsV5,
    initial_bankroll: float = 100_000,
    store: ParquetStore | None = None,
    betting_mode: str = "flat",
    diag_prefix: str = "bt",
    betting_target: str = "win",
    strategy_params: dict[str, Any] | None = None,
) -> None:
```

From src/backtest/engine.py (BacktestEngine.run signature, line ~483):
```python
def run(
    self,
    test_start: str,
    test_end: str,
    training_bet_history: list[dict[str, Any]] | None = None,
) -> BacktestResult:
```

From scripts/run_backtest.py (BacktestEngine instantiation, line ~464):
```python
engine = BacktestEngine(
    models=models,
    store=store,
    betting_mode=args.betting_mode,
    diag_prefix=f"bt_{test_year}",
    betting_target=args.betting_target,
    strategy_params=strategy_params,
)
```

From scripts/run_backtest.py (validate_args, line ~113):
```python
def validate_args(parser: argparse.ArgumentParser, args: argparse.Namespace) -> None:
    if args.skip_train and not args.ensemble:
        parser.error("--skip-train requires --ensemble (キャッシュモデルはensemble前提)")
    if args.years:
        return
    single_year_args = [args.train_start, args.train_end, args.test_start, args.test_end]
    if all(single_year_args):
        return
    parser.error(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BacktestEngineにPFP freeze/verify二重検証を統合</name>
  <files>src/backtest/engine.py, tests/test_backtest_engine.py</files>
  <read_first>
    - src/backtest/engine.py (全体、特に__init__ ~359行目とrun() ~483行目とBacktestResult返却 ~1185行目)
    - src/backtest/parameter_freeze_protocol.py (全体187行)
    - tests/test_backtest_engine.py (既存テストパターン確認)
    - .planning/phases/18-validation-freeze/18-CONTEXT.md (D-02, D-03, D-04決定)
  </read_first>
  <behavior>
    - Test 1: manifest_pathを渡したBacktestEngineでrun()を実行すると、verify_strategy_manifest()が呼ばれること(モックで検証)
    - Test 2: manifest_pathを渡したBacktestEngineでrun()を実行すると、ParameterFreezeProtocol.freeze()とverify()が順番に呼ばれること
    - Test 3: PFP verify()が{"passed": False}を返した場合、run()がRuntimeErrorを送出すること(D-04)
    - Test 4: manifest_path=None(デフォルト)の場合、PFP/verify関連コードが一切実行されず従来動作と同じこと
    - Test 5: manifest_pathが存在しないパスの場合、FileNotFoundErrorが送出されること(D-04)
  </behavior>
  <action>
1. tests/test_backtest_engine.pyに新規テストクラスTestBacktestEnginePFPIntegrationを追加する。上記5つのテストケースを実装する。テストはmockベース(unittest.mock)で、BacktestEngineのrun()内でimportされるParameterFreezeProtocolとverify_strategy_manifestをモックする。BacktestEngineの__init__で必要なmodels等は既存テストパターンを踏襲してモック化する。run()のrace loop全体をスキップするため、load_racesが空DataFrameを返すようにモックする。

2. src/backtest/engine.pyを以下の通り変更する:

(2a) ファイル先頭のTYPE_CHECKING importブロックに追記:
```python
if TYPE_CHECKING:
    from domain.models import TrainedModelsV5
```
(既存)

(2b) importセクション(非TYPE_CHECKING)に追記:
```python
from backtest.parameter_freeze_protocol import (
    ParameterFreezeProtocol,
    verify_strategy_manifest,
)
```

(2c) BacktestEngine.__init__()のシグネチャにmanifest_path引数を追加:
```python
def __init__(
    self,
    models: TrainedModelsV5,
    initial_bankroll: float = 100_000,
    store: ParquetStore | None = None,
    betting_mode: str = "flat",
    diag_prefix: str = "bt",
    betting_target: str = "win",
    strategy_params: dict[str, Any] | None = None,
    manifest_path: Path | None = None,  # NEW: D-02
) -> None:
```
__init__内でself._manifest_path = manifest_pathとself._pfp: ParameterFreezeProtocol | None = Noneを保存する。

(2d) BacktestEngine.run()メソッド内にPFP二重検証を追加:

run()の先頭(行499「# 1. データロード」の直前)に以下を挿入:
```python
# --- D-03: PFP二重検証 (SHA256 + ParameterFreezeProtocol) ---
if self._manifest_path is not None:
    # D-03(1): SHA256再検証
    from pathlib import Path as _Path
    verified_params = verify_strategy_manifest(self._manifest_path)
    logger.info("Manifest SHA256 verified in engine.run(): %s", self._manifest_path)

    # D-03(2): PFP freeze -- OOS期間開始時のモデルスナップショット
    self._pfp = ParameterFreezeProtocol(self.models)
    self._pfp.freeze()
```

run()の末尾(return BacktestResult(...)の直前、行~1184)に以下を挿入:
```python
# --- D-03(2): PFP verify -- OOS期間終了時のモデル不変性確認 ---
pfp_result = None
if self._pfp is not None:
    pfp_result = self._pfp.verify()
    if not pfp_result["passed"]:
        raise RuntimeError(pfp_result["message"])  # D-04: 即時停止
    logger.info("PFP verification passed: %s", pfp_result["message"])
```

3. テストを実行して5テスト全てPASSを確認する。
  </action>
  <verify>
    <automated>cd C:\Users\hirom\develop\keiba-ai && python -m pytest tests/test_backtest_engine.py -v -k "PFPIntegration" 2>&1 | tail -20</automated>
  </verify>
  <done>
    - BacktestEngine.__init__()がmanifest_path引数を受け取る
    - run()の先頭でmanifest_pathがあればSHA256検証+PFP freezeを実行する
    - run()の末尾(BacktestResult返却前)でPFP verifyを実行する
    - PFP verify失敗時にRuntimeErrorを送出する(D-04)
    - manifest_path=Noneの場合は従来動作と完全に同じ
    - 5つのテストが全てPASSする
  </done>
</task>

<task type="auto">
  <name>Task 2: run_backtest.pyにmanifest_path渡し + --strategy-manifest/--ensemble組み合わせバリデーションを追加</name>
  <files>scripts/run_backtest.py</files>
  <read_first>
    - scripts/run_backtest.py (全体734行、特にvalidate_args ~113行目、_run_single_year ~394行目、_run_multi_year ~529行目)
    - src/backtest/engine.py (更新後の__init__シグネチャを確認)
  </read_first>
  <acceptance_criteria>
    - validate_args()関数に「--strategy-manifest requires --ensemble」チェックが含まれる
    - _run_single_year()のBacktestEngineコンストラクタ呼び出しにmanifest_path引数が追加されている
    - _run_multi_year()のBacktestEngineコンストラクタ呼び出しにmanifest_path引数が追加されている
    - manifest_pathはPath(args.strategy_manifest)として渡される(args.strategy_manifestがNoneの場合はNone)
  </acceptance_criteria>
  <action>
1. scripts/run_backtest.pyのvalidate_args()関数(行~113)に、--strategy-manifest/--ensemble組み合わせバリデーションを追加する。既存の`if args.skip_train and not args.ensemble:`チェックの直後に追加:

```python
if args.strategy_manifest and not args.ensemble:
    parser.error("--strategy-manifest requires --ensemble (manifest検証はアンサンブル前提)")
```

2. _run_single_year()内(行~464)のBacktestEngineコンストラクタ呼び出しにmanifest_path引数を追加:

```python
engine = BacktestEngine(
    models=models,
    store=store,
    betting_mode=args.betting_mode,
    diag_prefix=f"bt_{test_year}",
    betting_target=args.betting_target,
    strategy_params=strategy_params,
    manifest_path=Path(args.strategy_manifest) if args.strategy_manifest else None,  # D-01/D-02
)
```

3. _run_multi_year()内(行~606)のBacktestEngineコンストラクタ呼び出しに同じmanifest_path引数を追加:

```python
engine = BacktestEngine(
    models=models,
    store=store,
    betting_mode=args.betting_mode,
    diag_prefix=f"bt_{test_year}",
    betting_target=args.betting_target,
    strategy_params=strategy_params,
    manifest_path=Path(args.strategy_manifest) if args.strategy_manifest else None,  # D-01/D-02
)
```

4. validate_args()に追加した行の前に`from pathlib import Path`がimport済みであることを確認(既にスクリプト内でPathを使用しているため不要だが、念のためgrepで確認)。
  </action>
  <verify>
    <automated>cd C:\Users\hirom\develop\keiba-ai && grep -n "manifest_path" scripts/run_backtest.py | grep -c "BacktestEngine" | grep -q "2" && grep -n "strategy_manifest.*ensemble" scripts/run_backtest.py | head -3</automated>
  </verify>
  <done>
    - validate_args()が--strategy-manifest指定時に--ensemble未指定でエラーを出す
    - _run_single_year()と_run_multi_year()の両方でmanifest_pathがBacktestEngineに渡される
    - D-01(run_backtest.pyに--manifest引数で一本化)とD-02(run()内でmanifest読込)の配線が完了
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest file → engine | 外部JSONファイルから読み込むパラメータ。SHA256ハッシュで改ざん検知 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | manifest JSON | mitigate | verify_strategy_manifest()でSHA256照合。不一致時はValueError送出(D-04) |
| T-18-02 | Tampering | models during OOS | mitigate | ParameterFreezeProtocol.freeze()/verify()でpickle SHA256スナップショット検証(D-03) |
| T-18-03 | Spoofing | --strategy-manifest without --ensemble | mitigate | validate_args()で--strategy-manifest requires --ensembleを強制 |
</threat_model>

<verification>
1. `python -m pytest tests/test_backtest_engine.py -v -k "PFPIntegration"` が5テスト全てPASS
2. `grep -c "manifest_path" src/backtest/engine.py` が4以上
3. `grep -c "manifest_path" scripts/run_backtest.py` が3以上
4. `grep "strategy_manifest.*ensemble" scripts/run_backtest.py` がvalidate_args内のチェック行を含む
</verification>

<success_criteria>
- BacktestEngine.run()がmanifest_path付きで呼ばれた場合、SHA256検証+PFP freeze/verify二重検証を実行する
- PFP verify失敗時にRuntimeErrorで即時停止する(D-04)
- manifest_pathなしの場合は従来動作と完全に同じ(後方互換)
- run_backtest.pyがmanifest_pathをengineに正しく渡す(D-01, D-02)
- --strategy-manifest単独指定時に引数エラーとなる(Pitfall 5防止)
- 5つの新規テストが全てPASS
</success_criteria>

<output>
After completion, create `.planning/phases/18-validation-freeze/18-01-SUMMARY.md`
</output>
