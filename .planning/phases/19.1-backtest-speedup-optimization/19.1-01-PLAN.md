---
phase: 19.1-backtest-speedup-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/run_backtest.py
  - src/pipelines/training_pipeline.py
  - src/db/readers.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "デフォルト戦略(--strategy-manifestなし)実行時、Phase BキャリブレーションBTがスキップされ、空リストが返される"
    - "--strategy-manifest使用時、軽量キャリブレーションBT(12ヶ月)が実行される"
    - "mlflow.lightgbm.log_modelの全呼び出しにpip_requirementsが指定され、pip freezeがスキップされる"
    - "_coerce_typesが新ETL形式のデータで早期returnする"
  artifacts:
    - path: "scripts/run_backtest.py"
      provides: "P0 calibration BT conditional skip logic (D-01, D-02)"
      contains: "strategy_params is None"
    - path: "src/pipelines/training_pipeline.py"
      provides: "MLflow pip_requirements optimization (RESEARCH)"
      contains: "pip_requirements"
    - path: "src/db/readers.py"
      provides: "_coerce_types early return for new ETL (RESEARCH)"
      contains: "return df"
  key_links:
    - from: "scripts/run_backtest.py"
      to: "_collect_training_bet_history()"
      via: "strategy_params check"
      pattern: "strategy_params is None"
---

<objective>
P0+P1最適化: キャリブレーションBT条件付きスキップ、MLflow pip高速化、_coerce_types早期return

Purpose: バックテスト実行時間の最大ボトルネック(P0: -4900s, 60%)を排除し、P1改善(合計-112s)で追加の高速化を達成する。これらは結果に影響しない（P0-P1は予測値完全一致）ためリスクゼロ。

Output: 修正済み scripts/run_backtest.py, src/pipelines/training_pipeline.py, src/db/readers.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.1-backtest-speedup-optimization/19.1-CONTEXT.md
@.planning/phases/19.1-backtest-speedup-optimization/19.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: P0 キャリブレーションBT条件付きスキップ + P1 _coerce_types早期return</name>
  <files>scripts/run_backtest.py, src/db/readers.py</files>
  <read_first>
    - scripts/run_backtest.py (full file, especially lines 211-261 _collect_training_bet_history)
    - src/db/readers.py (especially lines 140-171 _coerce_types and lines 50-82 _STRING_COLUMNS)
    - .planning/spikes/003-calibration-shorten/README.md (calibration degeneration verification)
  </read_first>
  <action>
実装する2つの変更:

**変更1: P0 キャリブレーションBT条件付きスキップ (D-01, D-02)**

`scripts/run_backtest.py`の`_collect_training_bet_history()`関数(行211-)を修正する。

現在の実装(行239-261)を以下のロジックに置き換える:

```python
def _collect_training_bet_history(
    models: Any,
    store: Any,
    train_start: str,
    train_end: str,
    betting_mode: str,
    betting_target: str,
    strategy_params: dict[str, Any] | None,
) -> list[dict[str, Any]]:
    """トレーニング期間のバックテストを実行し、OddsBandFilter キャリブレーション用の
    bet_history を収集する。

    D-01: デフォルト戦略(strategy_params is None)ではキャリブレーションBTを完全スキップ。
          退化現象(roi_threshold=1.0 + JRA控除率25% → 全バンドROI<1.0 → 空bet_history)を確認済み。
    D-02: --strategy-manifest使用時(strategy_params is not None)のみ軽量キャリブレーション(直近12ヶ月)を実行。
    """
    # D-01: デフォルト戦略では完全スキップ (退化現象: Spike 003 VALIDATED)
    if strategy_params is None:
        logger.info("デフォルト戦略: キャリブレーションBTスキップ (退化現象 confirmed)")
        return []

    # D-02: strategy_manifest使用時のみ軽量キャリブレーション
    from datetime import datetime, timedelta

    from backtest.engine import BacktestEngine
    from betting.default_strategy import build_default_strategy_config

    # 直近12ヶ月に短縮 (Spike 003: 12ヶ月で十分安定、10-30バンド除く)
    train_end_dt = datetime.strptime(train_end, "%Y-%m-%d")
    cal_start_dt = train_end_dt - timedelta(days=365)
    cal_start = cal_start_dt.strftime("%Y-%m-%d")

    default_train_config = build_default_strategy_config()

    logger.info(
        "軽量キャリブレーションBT (直近12ヶ月): %s ~ %s",
        cal_start,
        train_end,
    )
    train_engine = BacktestEngine(
        models=models,
        store=store,
        betting_mode=betting_mode,
        diag_prefix="bt_calib",
        betting_target=betting_target,
        strategy_params=default_train_config,
    )
    train_result = train_engine.run(cal_start, train_end)
    logger.info(
        "キャリブレーションBT完了: %d bets, ROI=%.1f%%",
        train_result.total_bets,
        train_result.total_roi * 100,
    )
    return train_result.bet_history
```

**変更2: P1 _coerce_types早期return (Claude's Discretion)**

`src/db/readers.py`の`_coerce_types()`関数(行140-)の最初に、新ETL形式かどうかのチェックを追加する。関数の冒頭(行148の直前、既存の`if df.empty`チェックの後に)に以下を追加:

```python
def _coerce_types(df: pd.DataFrame) -> pd.DataFrame:
    """旧ETL互換: 文字列型の数値列を適切な型に変換する。"""
    if df.empty:
        return df

    # 新ETL形式では既に正しい型 → 早期return
    # race_dateがdatetimeで、object型の数値列が存在しない場合はスキップ
    if "race_date" in df.columns and pd.api.types.is_datetime64_any_dtype(df["race_date"]):
        object_needing_coercion = [
            c for c in df.columns
            if df[c].dtype == object and c not in _STRING_COLUMNS
        ]
        if not object_needing_coercion:
            return df

    # ... 既存の処理をそのまま継続 ...
```

注意: 既存の`if "race_date" in df.columns and not pd.api.types.is_datetime64_any_dtype(df["race_date"]):`の行は、早期returnチェックの後になるのでそのまま残す。
  </action>
  <verify>
    <automated>python -m pytest tests/test_backtest_engine.py -v -x --timeout=60 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - "scripts/run_backtest.py"に"strategy_params is None"という文字列が含まれ、D-01スキップロジックが実装されていること
    - "scripts/run_backtest.py"に"キャリブレーションBTスキップ"または"退化現象"というログメッセージが含まれていること
    - "scripts/run_backtest.py"に"timedelta(days=365)"という軽量キャリブレーション期間(D-02)が含まれていること
    - "src/db/readers.py"の"_coerce_types"関数に早期returnロジックが含まれ、"object_needing_coercion"チェックが行われていること
    - `python -m pytest tests/test_backtest_engine.py -v -x` が合格すること
    - `python -m ruff check scripts/run_backtest.py src/db/readers.py` がruffエラーを出さないこと
  </acceptance_criteria>
  <done>
    - デフォルト戦略時、_collect_training_bet_history()が即座に空リストを返す (D-01)
    - --strategy-manifest使用時、直近12ヶ月の軽量キャリブレーションBTが実行される (D-02)
    - _coerce_typesが新ETL形式データで早期returnする (P1)
    - 既存テストが全て通る
    - ruff + mypyエラーなし
  </done>
</task>

<task type="auto">
  <name>Task 2: P1 MLflow pip高速化</name>
  <files>src/pipelines/training_pipeline.py</files>
  <read_first>
    - src/pipelines/training_pipeline.py (especially lines 1213-1357 _log_to_mlflow method, all mlflow.lightgbm.log_model calls)
    - .planning/spikes/005-p1-quick-wins/README.md (MLflow pip benchmark: 4.377s->0.076s/call)
  </read_first>
  <action>
`src/pipelines/training_pipeline.py`の`_log_to_mlflow()`メソッド内の全`mlflow.lightgbm.log_model()`呼び出しに`pip_requirements`パラメータを追加する。

**手順:**

1. `_log_to_mlflow()`メソッドの先頭（`with mlflow.start_run()`の前）にクラス定数を追加:

```python
_MLFLOW_PIP_REQS: list[str] = ["lightgbm", "scikit-learn", "pandas", "numpy", "joblib"]
```

これはメソッド内ローカル変数ではなくモジュールレベル定数にする（メソッド外で定義）。

2. `_log_to_mlflow()`内の全`mlflow.lightgbm.log_model(model, name=...)`呼び出しを検索し、各呼び出しに`pip_requirements=_MLFLOW_PIP_REQS`を追加する。

該当箇所（Spike 002で26回確認、RESEARCH.mdで実測）:
- 行1227: `mlflow.lightgbm.log_model(stage1_model, name=f"stage1_{surface}", pip_requirements=_MLFLOW_PIP_REQS)`
- 行1230: `mlflow.lightgbm.log_model(sub.market.model, name=f"market_{surface}", pip_requirements=_MLFLOW_PIP_REQS)`
- 行1245: `mlflow.lightgbm.log_model(sub.win.hit_model, name=f"win_hit_{surface}", pip_requirements=_MLFLOW_PIP_REQS)`
- 行1246: `mlflow.lightgbm.log_model(sub.win.return_model, name=f"win_ret_{surface}", pip_requirements=_MLFLOW_PIP_REQS)`
- 行1249-1251: ev_corrector.p_correction_model → add pip_requirements
- 行1253-1255: ev_corrector.e_correction_model → add pip_requirements
- 行1259-1261: place_ev_corrector.p_correction_model → add pip_requirements
- 行1263-1265: place_ev_corrector.e_correction_model → add pip_requirements
- 行1311: place.hit_model → add pip_requirements
- 行1312: place.return_model → add pip_requirements
- その他全てのmlflow.lightgbm.log_model呼び出し

注意: `mlflow.sklearn.log_model`や`mlflow.log_artifact`の呼び出しは変更しない。`mlflow.lightgbm.log_model`のみ。
  </action>
  <verify>
    <automated>python -m pytest tests/ -v -x -k "train" --timeout=60 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - "src/pipelines/training_pipeline.py"に`_MLFLOW_PIP_REQS`（または同等の定数）が定義され、`["lightgbm", "scikit-learn", "pandas", "numpy", "joblib"]`が含まれていること
    - `grep -c "pip_requirements" src/pipelines/training_pipeline.py` を実行した結果、`mlflow.lightgbm.log_model`の呼び出し回数と一致していること
    - `grep "mlflow.lightgbm.log_model" src/pipelines/training_pipeline.py | grep -v "pip_requirements"` を実行した結果、`pip_requirements`が設定されていない呼び出しが0であること
    - `python -m ruff check src/pipelines/training_pipeline.py` がエラーを出さないこと
  </acceptance_criteria>
  <done>
    - 全mlflow.lightgbm.log_model呼び出しにpip_requirementsが指定されている
    - pip freeze子プロセス起動が回避される (57.6x高速化, Spike 005 VALIDATED)
    - 既存テストが全て通る
    - ruff + mypyエラーなし
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → _collect_training_bet_history | strategy_params is None check gates calibration skip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-01 | S | run_backtest.py calibration skip | accept | Skip logic is deterministic (strategy_params is None), no spoofing risk |
| T-19.1-02 | I | MLflow pip_requirements list | accept | Static list of known dependencies, no integrity risk |
</threat_model>

<verification>
1. `python -m pytest tests/ -v -x` -- 全テスト通過
2. `python -m ruff check scripts/run_backtest.py src/pipelines/training_pipeline.py src/db/readers.py` -- lintエラーなし
3. `python -m mypy src/pipelines/training_pipeline.py src/db/readers.py` -- 型エラーなし
4. `grep -c "pip_requirements" src/pipelines/training_pipeline.py` -- log_model呼び出し回数以上
5. `grep "strategy_params is None" scripts/run_backtest.py` -- D-01ロジック存在確認
</verification>

<success_criteria>
- デフォルト戦略時のキャリブレーションBTスキップ (D-01) が確認できるログ出力
- strategy_manifest使用時の軽量12ヶ月キャリブレーション (D-02) が実行される
- 全mlflow.lightgbm.log_modelにpip_requirementsが指定されている
- _coerce_typesが新ETLデータで早期returnする
- 既存テスト全通過、lint/型エラーなし
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-backtest-speedup-optimization/19.1-01-SUMMARY.md`
</output>
