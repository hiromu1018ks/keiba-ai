---
phase: 19.1-backtest-speedup-optimization
plan: 01
subsystem: backtest, training, db
tags: [performance, P0, P1, calibration-skip, mlflow, early-return]
dependency_graph:
  requires: []
  provides: [calibration-bt-skip, mlflow-pip-optimization, coerce-types-early-return]
  affects: [scripts/run_backtest.py, src/pipelines/training_pipeline.py, src/db/readers.py]
tech_stack:
  added: []
  patterns: [conditional-skip, pip-requirements-bypass, early-return]
key_files:
  created: []
  modified:
    - scripts/run_backtest.py
    - src/pipelines/training_pipeline.py
    - src/db/readers.py
decisions:
  - D-01 calibration BT skip (strategy_params is None)
  - D-02 lightweight 12-month calibration with manifest
  - MLflow pip_requirements bypass (57.6x speedup per call)
  - _coerce_types early return for new ETL format
metrics:
  duration: 14m
  completed: "2026-05-08"
---

# Phase 19.1 Plan 01: P0+P1最適化 Summary

P0キャリブレーションBT条件付きスキップ(D-01/D-02)、P1 MLflow pip高速化(14呼び出し)、P1 _coerce_types早期returnを実装。推定-5012s(61%)のバックテスト実行時間短縮を達成。

## Changes

### Task 1: P0キャリブレーションBT条件付きスキップ + P1 _coerce_types早期return

**Commit:** 438ccfc

- `scripts/run_backtest.py`: `_collect_training_bet_history()`を修正
  - D-01: `strategy_params is None`時（デフォルト戦略）はキャリブレーションBTを完全スキップ、空リストを返す
  - D-02: `--strategy-manifest`使用時のみ直近12ヶ月の軽量キャリブレーションBTを実行
  - 退化現象（roi_threshold=1.0 + JRA控除率25% → 全バンドROI<1.0 → 空bet_history）をSpike 003で確認済み
- `src/db/readers.py`: `_coerce_types()`に早期returnを追加
  - 新ETL形式（race_dateがdatetime、object型の数値列なし）の場合は型変換処理をスキップ

### Task 2: P1 MLflow pip高速化

**Commit:** 637546a

- `src/pipelines/training_pipeline.py`: `_log_to_mlflow()`内の全14回の`mlflow.lightgbm.log_model()`呼び出しに`pip_requirements=_MLFLOW_PIP_REQS`を追加
- `_MLFLOW_PIP_REQS`定数（`["lightgbm", "scikit-learn", "pandas", "numpy", "joblib"]`）をモジュールレベルに定義
- pip freeze子プロセス起動を回避（4.377s → 0.076s/call、57.6x高速化、Spike 005 VALIDATED）

## Verification Results

- 全テスト: 1349 passed, 1 skipped, 0 failed
- Ruff: 今回の変更範囲にエラーなし（既存のN814等は非関連）
- `strategy_params is None` ロジック: 実装済み
- `timedelta(days=365)` 軽量キャリブレーション: 実装済み
- `object_needing_coercion` 早期return: 実装済み
- `pip_requirements` 設定率: 14/14 (100%)

## Deviations from Plan

None - plan executed exactly as written.

## Threat Flags

None - no new security surface introduced. All changes are configuration-only with no new network endpoints, auth paths, or data access patterns.

## Self-Check: PASSED

- scripts/run_backtest.py: FOUND
- src/pipelines/training_pipeline.py: FOUND
- src/db/readers.py: FOUND
- 19.1-01-SUMMARY.md: FOUND
- Commit 438ccfc: FOUND
- Commit 637546a: FOUND
