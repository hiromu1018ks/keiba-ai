---
phase: 19.1-backtest-speedup-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backtest/engine.py
  - src/db/readers.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "BacktestEngine.run()がodds_ts DataFrameを外部から受け取れる"
    - "odds_tsが渡された場合、load_odds_time_series_rangeの重複呼び出しが回避される"
    - "odds_tsが渡されなかった場合、従来通り内部でロードされる（後方互換）"
  artifacts:
    - path: "src/backtest/engine.py"
      provides: "preloaded_odds_ts constructor parameter + run() usage"
      contains: "preloaded_odds_ts"
    - path: "src/db/readers.py"
      provides: "load_odds_time_series_range with cache parameter"
      contains: "cached_odds_ts"
  key_links:
    - from: "src/backtest/engine.py"
      to: "load_odds_time_series_range"
      via: "preloaded_odds_ts bypass"
      pattern: "preloaded_odds_ts"
---

<objective>
P1 oddsデータ受け渡し最適化: 452MB odds時系列データの3回ロードを回避

Purpose: Training/Calibration BT/Test BT間でodds時系列データ(452MB)が3回ロードされる重複を排除する。コンストラクタ注入で事前ロード済みDataFrameを渡す方式を採用する（既存のコンストラクタ注入パターンに合致、RESEARCH.md推奨）。

Output: 修正済み src/backtest/engine.py, src/db/readers.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.1-backtest-speedup-optimization/19.1-CONTEXT.md
@.planning/phases/19.1-backtest-speedup-optimization/19.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BacktestEngineにpreloaded_odds_tsコンストラクタ引数を追加</name>
  <files>src/backtest/engine.py, src/db/readers.py</files>
  <read_first>
    - src/backtest/engine.py (constructor lines 363-382, run() method lines 501-560, especially line 553 odds_ts load)
    - src/db/readers.py (load_odds_time_series_range function lines 198-219)
    - scripts/run_backtest.py (lines 433-475: pipeline.run() → _collect_training_bet_history() → engine.run() flow)
  </read_first>
  <action>
**注意: このタスクはPlan 01と並行実行される。Plan 01がrun_backtest.pyを、このタスクがengine.py/readers.pyを変更する。ファイルの競合はない。**

**変更1: BacktestEngine constructor に preloaded_odds_ts 引数追加**

`src/backtest/engine.py`の`__init__()`（行363-）に`preloaded_odds_ts`パラメータを追加:

```python
def __init__(
    self,
    models: TrainedModelsV5,
    initial_bankroll: float = 100_000,
    store: ParquetStore | None = None,
    betting_mode: str = "flat",
    diag_prefix: str = "bt",
    betting_target: str = "win",
    strategy_params: dict[str, Any] | None = None,
    manifest_path: Path | None = None,
    preloaded_odds_ts: pd.DataFrame | None = None,  # P1: odds時系列データ受け渡し
) -> None:
    # ... 既存の初期化処理 ...
    self._preloaded_odds_ts = preloaded_odds_ts
```

**変更2: run()内のodds_tsロードをpreloaded_odds_tsでバイパス**

`run()`メソッド内の行553付近:

```python
# 現在:
odds_ts_df = load_odds_time_series_range(self.store, start, end)

# 変更後:
if self._preloaded_odds_ts is not None:
    odds_ts_df = self._preloaded_odds_ts
    # 日付フィルタはload済みデータに対して適用
    s_dt = pd.Timestamp(start)
    e_dt = pd.Timestamp(end)
    if "race_date" in odds_ts_df.columns:
        mask = (odds_ts_df["race_date"] >= s_dt) & (odds_ts_df["race_date"] <= e_dt)
        odds_ts_df = odds_ts_df[mask]
    logger.debug("Using preloaded odds_ts (%d rows for %s ~ %s)", len(odds_ts_df), start, end)
else:
    odds_ts_df = load_odds_time_series_range(self.store, start, end)
```

**変更3: run_backtest.pyでのデータ受け渡し (run_backtest.pyはPlan 01が担当)**

注意: `scripts/run_backtest.py`の変更はPlan 01が担当している。Plan 01の完了後、run_backtest.pyで以下のような接続が必要になる:

```python
# Plan 01の_collect_training_bet_history内で、または
# main()フロー内でodds_tsを事前ロードして受け渡す
```

しかし、Plan 01と02は並行実行されるため、このファイルの変更はPlan 01またはWave 2のPlan 03で接続される。このタスクではengine.pyとreaders.pyの変更のみを行う。

**後方互換性:** `preloaded_odds_ts`のデフォルトは`None`なので、既存の呼び出し元には影響しない。
  </action>
  <verify>
    <automated>python -m pytest tests/test_backtest_engine.py -v -x --timeout=60 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - "src/backtest/engine.py"の`__init__()`に`preloaded_odds_ts: pd.DataFrame | None = None`パラメータが追加されていること
    - "src/backtest/engine.py"に`self._preloaded_odds_ts`のインスタンス変数が設定されていること
    - "src/backtest/engine.py"の`run()`メソッド内で`self._preloaded_odds_ts is not None`のチェックがあること
    - preloaded_odds_tsがNoneの場合、従来通り`load_odds_time_series_range`が呼ばれること
    - `python -m pytest tests/test_backtest_engine.py -v -x` が合格すること
    - `python -m ruff check src/backtest/engine.py` がエラーを出さないこと
  </acceptance_criteria>
  <done>
    - BacktestEngineがpreloaded_odds_tsを受け取れる
    - preloaded_odds_ts渡し時はload_odds_time_series_rangeをスキップする
    - preloaded_odds_ts未指定時は従来通り動作する（後方互換）
    - 既存テスト全通過
    - ruff + mypyエラーなし
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preloaded_odds_ts → BacktestEngine | External DataFrame passed to engine; must be same schema as load_odds_time_series_range output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-03 | I | preloaded_odds_ts data integrity | accept | Same process, same data source, no tampering surface |
| T-19.1-04 | D | preloaded_odds_ts memory usage | accept | Single 452MB DataFrame kept alive instead of re-loaded; net memory same or less |
</threat_model>

<verification>
1. `python -m pytest tests/test_backtest_engine.py -v -x` -- 全テスト通過
2. `python -m ruff check src/backtest/engine.py` -- lintエラーなし
3. `python -m mypy src/backtest/engine.py` -- 型エラーなし
4. `grep "preloaded_odds_ts" src/backtest/engine.py` -- パラメータと使用箇所が確認できる
</verification>

<success_criteria>
- BacktestEngineにpreloaded_odds_tsコンストラクタ引数が追加されている
- run()内でpreloaded_odds_tsが活用される
- 後方互換性が維持されている（preloaded_odds_ts=Noneで従来動作）
- 既存テスト全通過、lint/型エラーなし
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-backtest-speedup-optimization/19.1-02-SUMMARY.md`
</output>
