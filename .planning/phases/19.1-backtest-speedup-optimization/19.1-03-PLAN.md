---
phase: 19.1-backtest-speedup-optimization
plan: 03
type: execute
wave: 2
depends_on:
  - 19.1-01
  - 19.1-02
files_modified:
  - src/db/parquet_store.py
  - src/db/readers.py
  - src/db/repository.py
  - src/features/horse_history_features.py
  - src/features/jockey_context_features.py
  - src/features/trainer_context_features.py
  - src/features/sire_features.py
  - src/features/pace_aptitude_features.py
  - src/features/course_features.py
  - src/features/market_bias_features.py
  - src/features/interaction_features.py
  - src/features/intra_race_features.py
  - src/features/odds_dynamics_features.py
  - src/features/odds_deviation_features.py
  - src/features/info_asymmetry_features.py
  - src/features/jockey_trainer_combo.py
  - src/features/horse_career_stats.py
  - src/features/feature_engine.py
  - src/pipelines/training_pipeline.py
  - src/backtest/engine.py
  - src/backtest/race_predictor.py
  - src/db/odds_extractor.py
  - src/models/ev_diagnostics.py
  - scripts/run_backtest.py
  - tests/test_backtest_engine.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "ParquetStore.read()が返すDataFrameの対象列がpd.Categorical型に変換されている"
    - "src/内の全groupby呼び出しにobserved=Trueが指定されている"
    - "HorseHistoryFeaturesのitertuplesループがvector化され、groupby+rollingに置換されている"
    - "BacktestEngineのper-race add_race_transforms重複が排除されている"
    - "oddsデータがTraining→Calibration→Test間で渡され、重複ロードが回避されている"
    - "段階的検証テストがP0-P2完全一致、P3統計的許容範囲を確認できる"
  artifacts:
    - path: "src/db/parquet_store.py"
      provides: "_optimize_dtypes() Categorical conversion (D-04)"
      contains: "CATEGORICAL_COLUMNS"
    - path: "src/features/horse_history_features.py"
      provides: "observed=True on all groupby + vectorized itertuples (P2, P3)"
      contains: "observed=True"
    - path: "tests/test_backtest_engine.py"
      provides: "段階的検証テスト (D-06, D-07)"
      contains: "test_optimization_stages"
  key_links:
    - from: "src/db/parquet_store.py"
      to: "all feature generators"
      via: "_optimize_dtypes() applied in read()"
      pattern: "_optimize_dtypes"
    - from: "tests/test_backtest_engine.py"
      to: "all optimization stages"
      via: "stage verification tests"
      pattern: "test_optimization"
---

<objective>
P2 Categorical包括的適用 + P3 vector化 + 段階的検証テスト + oddsデータ受け渡し接続

Purpose: P2で全groupby呼び出しにobserved=Trueを追加しCategorical化でメモリ92%削減、P3でHorseHistoryFeaturesのitertuplesループをvector化して200-400s短縮、oddsデータ受け渡しをrun_backtest.pyに接続、段階的検証テストを追加する。D-05(observed=True必須)は破滅的劣化防止のため最も厳格な要件。

Output: 修正済み20+ファイル、段階的検証テスト
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.1-backtest-speedup-optimization/19.1-CONTEXT.md
@.planning/phases/19.1-backtest-speedup-optimization/19.1-RESEARCH.md
@.planning/phases/19.1-backtest-speedup-optimization/19.1-01-SUMMARY.md
@.planning/phases/19.1-backtest-speedup-optimization/19.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: P2 Categorical包括的適用 + 全groupbyにobserved=True追加 + oddsデータ受け渡し接続</name>
  <files>
    src/db/parquet_store.py, src/db/readers.py, src/db/repository.py,
    src/features/horse_history_features.py, src/features/jockey_context_features.py,
    src/features/trainer_context_features.py, src/features/sire_features.py,
    src/features/pace_aptitude_features.py, src/features/course_features.py,
    src/features/market_bias_features.py, src/features/interaction_features.py,
    src/features/intra_race_features.py, src/features/odds_dynamics_features.py,
    src/features/odds_deviation_features.py, src/features/info_asymmetry_features.py,
    src/features/jockey_trainer_combo.py, src/features/horse_career_stats.py,
    src/features/feature_engine.py, src/pipelines/training_pipeline.py,
    src/backtest/engine.py, src/backtest/race_predictor.py,
    src/db/odds_extractor.py, src/models/ev_diagnostics.py,
    scripts/run_backtest.py
  </files>
  <read_first>
    - src/db/parquet_store.py (full file, especially read() method)
    - src/db/readers.py (especially load_races, load_entries, load_odds_time_series_range functions)
    - src/db/repository.py (any read calls that should inherit Categorical)
    - .planning/phases/19.1-backtest-speedup-optimization/19.1-RESEARCH.md (complete groupby call site inventory in Appendix)
    - .planning/spikes/004-gpu-cpu-memory/README.md (Categorical 70x regression validation)
    - .planning/phases/19.1-backtest-speedup-optimization/19.1-01-SUMMARY.md (P0+P1 changes)
    - .planning/phases/19.1-backtest-speedup-optimization/19.1-02-SUMMARY.md (preloaded_odds_ts changes)
  </read_first>
  <action>
このタスクは3つの変更を実装する:

**変更A: P2 Categorical変換 (D-04) — ParquetStore.read()に最適化を追加**

`src/db/parquet_store.py`に以下を追加:

```python
# モジュールレベル定数
CATEGORICAL_COLUMNS: frozenset[str] = frozenset({
    "race_id", "kettonum", "kisyucode", "chokyosicode",
    "sire_id", "bms_id", "umaban",
})


def _optimize_dtypes(df: pd.DataFrame) -> pd.DataFrame:
    """高カーディナリティ文字列列をpd.Categoricalに変換 (P2: D-04)"""
    for col in CATEGORICAL_COLUMNS & set(df.columns):
        if df[col].dtype == object:
            df[col] = df[col].astype("category")
    return df
```

そして`read()`メソッドの全return文で`_optimize_dtypes(df)`を適用する。`read()`には4つのreturn文がある:
- 行47: `return table.to_pandas()` → `return _optimize_dtypes(table.to_pandas())`
- 行52: `return self._apply_filters(df, filters)` → `return _optimize_dtypes(self._apply_filters(df, filters))`
- 行54: `return pd.read_parquet(...)` → `return _optimize_dtypes(pd.read_parquet(...))`
- 行57: `return self._apply_filters(df, filters)` → `return _optimize_dtypes(self._apply_filters(df, filters))`

**変更B: P2 全groupbyにobserved=True追加 (D-05) — CRITICAL**

RESEARCH.mdのAppendixにリストされた全groupby呼び出しに`observed=True`を追加する。

既に`observed=True`が設定されている箇所（変更不要）:
- `src/models/ev_correction_model.py:86` -- already has observed=True
- `src/models/win_selection_gate.py:261,289,312,345` -- already has observed=True
- `src/models/place_selection_gate.py:255,283,306` -- already has observed=True
- `src/features/feature_engine.py:108` -- already has observed=True

追加が必要な全箇所（RESEARCH.md Appendix + grep結果から完全リスト）:

`src/features/horse_history_features.py`:
- 行424: `.groupby("kettonum")` → `.groupby("kettonum", observed=True)`
- 行432: `.groupby("kisyucode")` → `.groupby("kisyucode", observed=True)`
- 行439: `.groupby("kisyucode")` → `.groupby("kisyucode", observed=True)`
- 行553: `.groupby(group_cols)` → `.groupby(group_cols, observed=True)`
- 行1208: `.groupby("race_id")` → `.groupby("race_id", observed=True)`

`src/features/jockey_context_features.py`:
- 行72: `.groupby(["race_id", "umaban", "kisyucode"])` → `.groupby(["race_id", "umaban", "kisyucode"], observed=True)`

`src/features/trainer_context_features.py`:
- 行72: `.groupby(["race_id", "umaban", "chokyosicode"])` → `.groupby(["race_id", "umaban", "chokyosicode"], observed=True)`

`src/features/market_bias_features.py`:
- 行36: `.groupby(df["race_id"])` → `.groupby(df["race_id"], observed=True)`
- 行40: `.groupby(df["race_id"])` → `.groupby(df["race_id"], observed=True)`
- 行51: `.groupby("race_id")` → `.groupby("race_id", observed=True)`
- 行84: `.groupby("race_id")` → `.groupby("race_id", observed=True)`

`src/features/interaction_features.py`:
- 行62: `.groupby("race_id")` → `.groupby("race_id", observed=True)`
- 行66: `.groupby("race_id")` → `.groupby("race_id", observed=True)`
- 行71-72: `.groupby("race_id")` → `.groupby("race_id", observed=True)` (2箇所)
- 行78: `.groupby("race_id")` → `.groupby("race_id", observed=True)`
- 行98: `.groupby("race_id")` → `.groupby("race_id", observed=True)`
- 行101: `.groupby(df["race_id"])` → `.groupby(df["race_id"], observed=True)`
- 行102: `.groupby(df["race_id"])` → `.groupby(df["race_id"], observed=True)`

`src/features/intra_race_features.py`:
- 行23: `.groupby("race_id")` → `.groupby("race_id", observed=True)`
- 行28: `.groupby("race_id")` → `.groupby("race_id", observed=True)`

`src/features/odds_dynamics_features.py`:
- 行41: `.groupby(["race_id", "umaban"])` → `.groupby(["race_id", "umaban"], observed=True)`
- 行157: `.groupby(["race_id", "umaban"], as_index=False)` → `.groupby(["race_id", "umaban"], as_index=False, observed=True)`
- 行160: `.groupby(["race_id", "umaban"])` → `.groupby(["race_id", "umaban"], observed=True)`
- 行199: `.groupby(["race_id", "umaban"])` → `.groupby(["race_id", "umaban"], observed=True)`
- 行205: `.groupby(["race_id", "umaban"])` → `.groupby(["race_id", "umaban"], observed=True)`
- 行226: `.groupby(["race_id", "umaban"])` → `.groupby(["race_id", "umaban"], observed=True)`
- 行227: `.groupby(["race_id", "umaban"])` → `.groupby(["race_id", "umaban"], observed=True)`
- 行252: `.groupby(["race_id", "umaban"])` → `.groupby(["race_id", "umaban"], observed=True)`
- 行307: `.groupby("race_id")` → `.groupby("race_id", observed=True)`
- 行311: `.groupby("race_id")` → `.groupby("race_id", observed=True)`
- 行343: `.groupby(df["race_id"])` → `.groupby(df["race_id"], observed=True)`
- 行347: `.groupby(df["race_id"])` → `.groupby(df["race_id"], observed=True)`
- 行354: `.groupby(df["race_id"])` → `.groupby(df["race_id"], observed=True)`
- 行360: `.groupby("race_id")` → `.groupby("race_id", observed=True)`
- 行373: `.groupby("race_id")` → `.groupby("race_id", observed=True)`

`src/features/odds_deviation_features.py`:
- 行37: `.groupby(df["race_id"])` → `.groupby(df["race_id"], observed=True)`
- 行43: `.groupby(df["race_id"])` → `.groupby(df["race_id"], observed=True)`
- 行44: `.groupby(df["race_id"])` → `.groupby(df["race_id"], observed=True)`

`src/features/info_asymmetry_features.py`:
- 行57: `.groupby("_condition")` → `.groupby("_condition", observed=True)`
- 行61: `.groupby("_condition")` → `.groupby("_condition", observed=True)`

`src/features/jockey_trainer_combo.py`:
- 行62: `.groupby("jt_combo")` → `.groupby("jt_combo", observed=True)`

`src/features/horse_career_stats.py`:
- 行42: `.groupby(group_col)` → `.groupby(group_col, observed=True)`

`src/features/feature_engine.py`:
- 行416: `.groupby("race_id")` → `.groupby("race_id", observed=True)`

`src/pipelines/training_pipeline.py`:
- 行1028: `.groupby("race_id")` → `.groupby("race_id", observed=True)`
- 行1056: `.groupby("race_id")` → `.groupby("race_id", observed=True)`
- 行1147: `.groupby("race_id")` → `.groupby("race_id", observed=True)`
- 行1190: `.groupby("race_id")` → `.groupby("race_id", observed=True)`
- 行1199: `.groupby("race_id")` → `.groupby("race_id", observed=True)`
- 行1206: `.groupby("race_id")` → `.groupby("race_id", observed=True)`

`src/backtest/engine.py`:
- 行164: `.groupby(["race_id", "umaban"])` → `.groupby(["race_id", "umaban"], observed=True)`
- 行315: `.groupby(["race_id", "lo", "hi"])` → `.groupby(["race_id", "lo", "hi"], observed=True)`
- 行342: `.groupby(group_col)` → `.groupby(group_col, observed=True)`

`src/backtest/race_predictor.py`:
- 行233: `.groupby(race_df["race_id"])` → `.groupby(race_df["race_id"], observed=True)`
- 行237: `.groupby(race_df["race_id"])` → `.groupby(race_df["race_id"], observed=True)`
- 行245: `.groupby(race_df["race_id"])` → `.groupby(race_df["race_id"], observed=True)`
- 行256: `.groupby(race_df["race_id"])` → `.groupby(race_df["race_id"], observed=True)`

`src/db/odds_extractor.py`:
- 行123: `.groupby(["race_id", "umaban"])` → `.groupby(["race_id", "umaban"], observed=True)`

`src/models/ev_diagnostics.py`:
- 行125: `.groupby("year")` → `.groupby("year", observed=True)`

`src/models/stage1_ability_model.py`:
- 行164: `.groupby("race_id")` → `.groupby("race_id", observed=True)`
- 行238: `.groupby(df.loc[mask, "race_id"])` → `.groupby(df.loc[mask, "race_id"], observed=True)`

`src/models/market_model.py`:
- 行128: `.groupby(df["race_id"])` → `.groupby(df["race_id"], observed=True)`
- 行207: `.groupby(df["race_id"])` → `.groupby(df["race_id"], observed=True)`

`src/models/place_ability_model.py`:
- 行207: `.groupby(df["race_id"])` → `.groupby(df["race_id"], observed=True)`
- 行213: `.groupby("race_id")` → `.groupby("race_id", observed=True)`

`src/models/robust_confidence_estimator.py`:
- 行72: `.groupby(["surface", "distance_bin"])` → `.groupby(["surface", "distance_bin"], observed=True)`
- 行209: `.groupby(win_df["race_id"])` → `.groupby(win_df["race_id"], observed=True)`

`src/models/wide_pair_builder.py`:
- 行34: `.groupby("race_id")` → `.groupby("race_id", observed=True)`

`src/models/win_benter_gate.py`:
- 行78: `.groupby("race_id")` → `.groupby("race_id", observed=True)`

`src/models/win_selection_gate.py` (already has some observed=True, check remaining):
- 行172: `.groupby(df["race_id"])` → `.groupby(df["race_id"], observed=True)`
- 行590: `.groupby("race_id", as_index=False, sort=False)` → `.groupby("race_id", as_index=False, sort=False, observed=True)`
- 行655: `.groupby(prepared["race_id"])` → `.groupby(prepared["race_id"], observed=True)`
- 行656: `.groupby(prepared["race_id"])` → `.groupby(prepared["race_id"], observed=True)`
- 行663-675: `.groupby(prepared["race_id"])` → `.groupby(prepared["race_id"], observed=True)` (5箇所)
- 行707: `.groupby("race_id", as_index=False, sort=False)` → `.groupby("race_id", as_index=False, sort=False, observed=True)`
- 行940: `.groupby(prepared["race_id"])` → `.groupby(prepared["race_id"], observed=True)`
- 行1054: `.groupby("race_id", as_index=False, sort=False)` → `.groupby("race_id", as_index=False, sort=False, observed=True)`

`src/models/place_selection_gate.py` (check remaining):
- 行166: `.groupby(df["race_id"])` → `.groupby(df["race_id"], observed=True)`
- 行520: `.groupby("race_id", as_index=False, sort=False)` → `.groupby("race_id", as_index=False, sort=False, observed=True)`
- 行585: `.groupby(prepared["race_id"])` → `.groupby(prepared["race_id"], observed=True)`
- 行586: `.groupby(prepared["race_id"])` → `.groupby(prepared["race_id"], observed=True)`
- 行593-605: `.groupby(prepared["race_id"])` → `.groupby(prepared["race_id"], observed=True)` (5箇所)
- 行637: `.groupby("race_id", as_index=False, sort=False)` → `.groupby("race_id", as_index=False, sort=False, observed=True)`
- 行862: `.groupby(prepared["race_id"])` → `.groupby(prepared["race_id"], observed=True)`
- 行976: `.groupby("race_id", as_index=False, sort=False)` → `.groupby("race_id", as_index=False, sort=False, observed=True)`

**変更C: oddsデータ受け渡し接続 (scripts/run_backtest.py)**

Plan 01で_collect_training_bet_historyがD-01/D-02に更新され、Plan 02でengine.pyにpreloaded_odds_tsが追加された。この接続を`scripts/run_backtest.py`のmainフローに実装する。

TrainingPipelineV5.run()はodds_tsを内部でロードするが、それを外部に返さない。そのため、odds_tsを事前ロードして両方に渡すアプローチは、TrainingPipelineV5の戻り値を変更する必要がある。

よりシンプルなアプローチ: Plan 02で追加したpreloaded_odds_tsをTest BT engineにのみ適用する（Trainingは内部ロードのまま）。Calibration BTはPlan 01でデフォルト時にスキップされるため、odds_tsの再利用はTest BTのみで有効。

```python
# scripts/run_backtest.py mainフロー内（行447-475付近）:

# calibration bet_history収集 (D-01/D-02: Plan 01で更新済み)
training_bet_history = _collect_training_bet_history(...)

# Test BT用にodds_tsを事前ロードして受け渡し (P1: Plan 02で追加)
from db.readers import load_odds_time_series_range
preloaded_odds = load_odds_time_series_range(store, test_start, test_end)

engine = BacktestEngine(
    models=models,
    store=store,
    betting_mode=args.betting_mode,
    diag_prefix=f"bt_{test_year}",
    betting_target=args.betting_target,
    strategy_params=strategy_params,
    manifest_path=Path(args.strategy_manifest) if args.strategy_manifest else None,
    preloaded_odds_ts=preloaded_odds,  # P1: 重複ロード回避
)
```

同様に、_collect_training_bet_history内の軽量キャリブレーション(D-02)にもpreloaded_odds_tsを渡す。ただし、Calibration BTは学習期間のデータをロードするため、Test BTのpreloaded_oddsとは期間が異なる点に注意。Calibration用には別途ロードが必要。
  </action>
  <verify>
    <automated>python -m pytest tests/ -v -x --timeout=120 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "observed=True" src/features/horse_history_features.py` が5以上であること（5箇所のgroupby呼び出し）
    - `grep -c "observed=True" src/features/odds_dynamics_features.py` が15以上であること
    - `grep -c "observed=True" src/models/win_selection_gate.py` が10以上であること
    - `grep -c "observed=True" src/models/place_selection_gate.py` が10以上であること
    - `grep "CATEGORICAL_COLUMNS" src/db/parquet_store.py` がCategorical定数を返すこと
    - `grep "_optimize_dtypes" src/db/parquet_store.py` が最適化関数を返すこと
    - `grep "preloaded_odds" scripts/run_backtest.py` がデータ受け渡しを返すこと
    - `python -m ruff check src/ scripts/run_backtest.py` がエラーを出さないこと
    - `python -m pytest tests/ -v -x` が全テスト合格すること
    - `grep -v "^#" src/features/horse_history_features.py | grep -c "\.groupby(" | grep -v "observed"` でobserved=Trueなしのgroupbyが0であること
  </acceptance_criteria>
  <done>
    - ParquetStore.read()が対象列をCategoricalに変換する (D-04)
    - src/内の全groupby呼び出しにobserved=Trueが追加されている (D-05)
    - oddsデータがTest BTに事前ロード渡しされる (P1)
    - 既存テスト全通過（Categorical型対応含む）
    - ruff + mypyエラーなし
  </done>
</task>

<task type="auto">
  <name>Task 2: P3 HorseHistoryFeatures vector化 + 段階的検証テスト (D-06, D-07)</name>
  <files>src/features/horse_history_features.py, tests/test_backtest_engine.py</files>
  <read_first>
    - src/features/horse_history_features.py (especially lines 580-730 itertuples loop, lines 410-470 pre-indexing)
    - tests/test_backtest_engine.py (existing test patterns for mock-based testing)
    - .planning/phases/19.1-backtest-speedup-optimization/19.1-RESEARCH.md (P3 vectorization pattern, code example)
    - .planning/phases/19.1-backtest-speedup-optimization/19.1-CONTEXT.md (D-06 verification criteria, D-07 test requirement)
  </read_first>
  <action>
**注意: このタスクはTask 1の完了後に実行する。Task 1がhorse_history_features.pyにobserved=Trueを追加した後、このタスクがvector化を適用する。**

**変更A: P3 HorseHistoryFeatures itertuples vector化**

`src/features/horse_history_features.py`の行592付近のitertuplesループは、個別馬の過去成績から特徴量を計算している。完全なvector化は複雑なため、**部分的vector化**アプローチを採用する:

1. ループ内の最も計算コストの高い操作を特定する（Spike 002で566sの大部分を占めるのはnumpy配列操作自体ではなく、Pythonループのオーバーヘッド）
2. インスタンスキャッシュを強化: 現在の`_entries_cache`/`_races_cache`に加えて、`past_by_ketto`/`past_by_kisyu`/`expanding_stats`のキャッシュをクラス変数に昇格

```python
# クラスレベルキャッシュの追加 (HorseHistoryFeaturesクラス内)
_class_cache: dict[str, tuple[np.ndarray, dict[str, np.ndarray]]] = {}

def _get_horse_arrays(self, ketto: str, past_by_ketto_arr: dict) -> dict[str, np.ndarray] | None:
    """インスタンス間でキャッシュを共有"""
    cache_key = f"{id(past_by_ketto_arr)}_{ketto}"
    if cache_key in self._class_cache:
        return self._class_cache[cache_key]
    result = past_by_ketto_arr.get(ketto)
    if result is not None:
        self._class_cache[cache_key] = result
    return result
```

3. itertuplesループの最適化: 既にdict-of-numpy-arraysパターン(行442-)を使用しているため、Pythonループ自体は残しつつ、以下を改善:
   - `_class_cache`で3回呼び出し（芝submodel、ダートsubmodel、test BT）間で前計算結果を共有
   - progress print をloggingに変更（print→logger.debug でIOオーバーヘッド削減）
   - searchsorted結果のキャッシュ

**実際のvector化:** itertuplesループの内容は既にnumpy配列ベース(行602-)であり、各馬ごとのsearchsorted + スライス操作が主体。完全なgroupby+rollingへの置換は、各行のfeature計算ロジックが複雑すぎる（form cycle、class adjustment、hierarchical z-score等）。そのため:
- インスタンスキャッシュ強化で~100s削減（3回呼び出し→1回計算）
- ループ内の不要なprint IOをlogger.debugに変更

**変更B: 段階的検証テスト (D-06, D-07)**

`tests/test_backtest_engine.py`に以下のテストを追加する:

```python
class TestBacktestOptimizationStages:
    """P0-P3最適化の段階的検証テスト (D-06, D-07)

    各最適化段階でバックテスト結果が許容範囲内に収まることを確認:
    - P0-P2: 予測値完全一致（結果に影響する変更なし）
    - P3: 統計的許容範囲（ROI/bet_count差異<5%、的中率差異<1%）
    """

    def test_calibration_skip_returns_empty_for_default_strategy(self):
        """D-01: デフォルト戦略(strategy_params=None)で空リストが返される"""
        # _collect_training_bet_historyを直接テスト
        # strategy_params=None → return []
        pass

    def test_calibration_runs_with_strategy_manifest(self):
        """D-02: strategy_manifest使用時に軽量キャリブレーションが実行される"""
        # strategy_params != None → 12ヶ月期間でBacktestEngine.run()が呼ばれる
        pass

    def test_preloaded_odds_ts_bypasses_load(self):
        """P1: preloaded_odds_tsが渡された場合、load_odds_time_series_rangeが呼ばれない"""
        # engine._preloaded_odds_ts = DataFrame
        # run()内でload_odds_time_series_rangeが呼ばれないことをmockで確認
        pass

    def test_categorical_columns_in_parquet_store(self):
        """D-04: ParquetStore.read()が対象列をCategoricalに変換する"""
        # CATEGORICAL_COLUMNSに含まれる列がcategory dtypeであることを確認
        pass

    def test_observed_true_on_all_groupby(self):
        """D-05: observed=Trueが全groupbyに追加されていることをgrepで確認"""
        # このテストはコード品質のインベントリチェック
        pass

    def test_p3_vectorization_tolerance(self):
        """D-06: P3 vector化の統計的許容範囲確認"""
        # itertuples版とvector化版のROI差異<5%、bet_count差異<5%
        pass
```

注意: 実際のテスト実装では、unittest.mockを使用してDB依存を排除する（プロジェクトのテストパターンに従う）。各テストの具体的なassert内容は、Task 1で追加された変更に対するスモークテストとして実装する。
  </action>
  <verify>
    <automated>python -m pytest tests/test_backtest_engine.py::TestBacktestOptimizationStages -v --timeout=60 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_backtest_engine.py`に`TestBacktestOptimizationStages`クラスが追加されていること
    - `TestBacktestOptimizationStages`に6つ以上のテストメソッドが含まれていること
    - `src/features/horse_history_features.py`にインスタンスキャッシュ強化（_class_cacheまたは同等）が追加されていること
    - `src/features/horse_history_features.py`のprint文がlogger.debugに変更されていること
    - `python -m pytest tests/test_backtest_engine.py -v -x` が合格すること
    - `python -m ruff check src/features/horse_history_features.py tests/test_backtest_engine.py` がエラーを出さないこと
  </acceptance_criteria>
  <done>
    - HorseHistoryFeaturesにクラスレベルキャッシュが追加され、3回呼び出し間でデータが共有される
    - itertuplesループ内のprintがlogger.debugに変更される
    - TestBacktestOptimizationStagesにD-01〜D-07の検証テストが含まれる
    - 全テスト通過、lint/型エラーなし
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ParquetStore → all consumers | Categorical dtype propagates to all downstream DataFrames |
| groupby observed=True | Must cover ALL call sites — missing one causes 70x regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-05 | D | Categorical merge incompatibility | mitigate | _optimize_dtypes applies at read time so all DataFrames share same category pool; test with TestBacktestOptimizationStages |
| T-19.1-06 | D | Incomplete observed=True coverage | mitigate | grep-based acceptance criteria + test_observed_true_on_all_groupby inventory check |
| T-19.1-07 | T | Test fixture Categorical mismatch | mitigate | Mock-based tests create DataFrames with object dtype; _optimize_dtypes only applies on ParquetStore.read() which tests mock |
| T-19.1-08 | I | P3 vectorization float precision | accept | D-06 allows <5% ROI/bet_count tolerance for P3 changes |
</threat_model>

<verification>
1. `python -m pytest tests/ -v -x` -- 全テスト通過
2. `python -m ruff check src/ tests/` -- lintエラーなし
3. `python -m mypy src/` -- 型エラーなし
4. Complete observed=True coverage verification:
   - `grep -rn "\.groupby(" src/ | grep -v "observed" | grep -v "^#"` should return 0 results
5. Categorical conversion verification:
   - `grep "CATEGORICAL_COLUMNS" src/db/parquet_store.py` exists
   - `grep "_optimize_dtypes" src/db/parquet_store.py` exists
6. Stage verification test:
   - `python -m pytest tests/test_backtest_engine.py::TestBacktestOptimizationStages -v` passes
</verification>

<success_criteria>
- ParquetStore.read()がCATEGORICAL_COLUMNSの列をpd.Categoricalに変換する (D-04)
- src/内にobserved=Trueなしのgroupby呼び出しが0件である (D-05)
- HorseHistoryFeaturesのキャッシュが3回呼び出し間で共有される (P3)
- 段階的検証テストTestBacktestOptimizationStagesが6テスト以上含まれる (D-07)
- oddsデータがpreloaded_odds_tsでTest BTに渡される (P1)
- 全テスト通過、lint/型エラーなし
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-backtest-speedup-optimization/19.1-03-SUMMARY.md`
</output>
