---
phase: 19.1-backtest-speedup-optimization
plan: 04
type: execute
wave: 4
depends_on:
  - 19.1-03
files_modified:
  - src/models/stage1_ability_model.py
  - src/models/market_model.py
  - src/models/place_ability_model.py
  - src/models/robust_confidence_estimator.py
  - src/models/wide_pair_builder.py
  - src/models/win_benter_gate.py
  - src/models/win_selection_gate.py
  - src/models/place_selection_gate.py
  - src/models/ev_diagnostics.py
  - src/backtest/engine.py
  - src/backtest/race_predictor.py
  - src/db/odds_extractor.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "src/models/冁E�E全groupby呼び出しにobserved=Trueが指定されてぁE��"
    - "src/backtest/冁E�E全groupby呼び出しにobserved=Trueが指定されてぁE��"
    - "src/db/odds_extractor.pyのgroupby呼び出しにobserved=Trueが指定されてぁE��"
    - "Categorical変換後もモチE��推論�EバックチE��ト結果が完�E一致する�E�E2は結果に影響しなぁE��更�E�E
  artifacts:
    - path: "src/models/win_selection_gate.py"
      provides: "observed=True on all groupby calls (D-05)"
      contains: "observed=True"
    - path: "src/models/place_selection_gate.py"
      provides: "observed=True on all groupby calls (D-05)"
      contains: "observed=True"
    - path: "src/backtest/engine.py"
      provides: "observed=True on groupby calls (D-05)"
      contains: "observed=True"
  key_links:
    - from: "src/models/"
      to: "src/db/parquet_store.py"
      via: "Categorical columns from _optimize_dtypes consumed by model groupby"
      pattern: "groupby.*observed=True"
    - from: "src/backtest/"
      to: "src/db/parquet_store.py"
      via: "Categorical columns from _optimize_dtypes consumed by backtest groupby"
      pattern: "groupby.*observed=True"
---

<objective>
P2 モチE��ファイル + バックチE��トファイルにobserved=True追加 (D-05)

Purpose: Plan 03でParquetStoreレベルにCategorical変換を追加した後、src/models/, src/backtest/, src/db/odds_extractor.pyのgroupby呼び出しにobserved=Trueを追加する、E-05要件の「�Egroupbyにobserved=True」を完�E達�Eする。これらのファイルをPlan 03から刁E��することで、Eタスクあたり�Eファイル数めE3�E�E5以下）に抑える、E
Output: 修正済み12ファイル (全モチE��ファイル + バックチE��トファイル)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.1-backtest-speedup-optimization/19.1-CONTEXT.md
@.planning/phases/19.1-backtest-speedup-optimization/19.1-RESEARCH.md
@.planning/phases/19.1-backtest-speedup-optimization/19.1-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: P2 モチE��ファイル + バックチE��トファイル + odds_extractorにobserved=True追加</name>
  <files>
    src/models/stage1_ability_model.py, src/models/market_model.py,
    src/models/place_ability_model.py, src/models/robust_confidence_estimator.py,
    src/models/wide_pair_builder.py, src/models/win_benter_gate.py,
    src/models/win_selection_gate.py, src/models/place_selection_gate.py,
    src/models/ev_diagnostics.py,
    src/backtest/engine.py, src/backtest/race_predictor.py,
    src/db/odds_extractor.py
  </files>
  <read_first>
    - .planning/phases/19.1-backtest-speedup-optimization/19.1-RESEARCH.md (Appendix: complete groupby call site inventory)
    - .planning/phases/19.1-backtest-speedup-optimization/19.1-03-SUMMARY.md (Categorical conversion already applied)
  </read_first>
  <action>
Plan 03でParquetStore.read()にCategorical変換(_optimize_dtypes)が追加され、feature/pipelineファイルのgroupbyにobserved=Trueが追加された。このタスクは残りのモチE��・バックチE��トファイルのgroupbyにobserved=Trueを追加する、E
**追加が忁E��な全箁E���E�EESEARCH.md Appendix + grep結果から完�Eリスト！E**

`src/models/stage1_ability_model.py`:
- 衁E64: `.groupby("race_id")` ↁE`.groupby("race_id", observed=True)`
- 衁E38: `.groupby(df.loc[mask, "race_id"])` ↁE`.groupby(df.loc[mask, "race_id"], observed=True)`

`src/models/market_model.py`:
- 衁E28: `.groupby(df["race_id"])` ↁE`.groupby(df["race_id"], observed=True)`
- 衁E07: `.groupby(df["race_id"])` ↁE`.groupby(df["race_id"], observed=True)`

`src/models/place_ability_model.py`:
- 衁E07: `.groupby(df["race_id"])` ↁE`.groupby(df["race_id"], observed=True)`
- 衁E13: `.groupby("race_id")` ↁE`.groupby("race_id", observed=True)`

`src/models/robust_confidence_estimator.py`:
- 衁E2: `.groupby(["surface", "distance_bin"])` ↁE`.groupby(["surface", "distance_bin"], observed=True)`
- 衁E09: `.groupby(win_df["race_id"])` ↁE`.groupby(win_df["race_id"], observed=True)`

`src/models/wide_pair_builder.py`:
- 衁E4: `.groupby("race_id")` ↁE`.groupby("race_id", observed=True)`

`src/models/win_benter_gate.py`:
- 衁E8: `.groupby("race_id")` ↁE`.groupby("race_id", observed=True)`

`src/models/win_selection_gate.py` (一部はalready observed=True、残りを追加):
- 衁E72: `.groupby(df["race_id"])` ↁE`.groupby(df["race_id"], observed=True)`
- 衁E90: `.groupby("race_id", as_index=False, sort=False)` ↁE`.groupby("race_id", as_index=False, sort=False, observed=True)`
- 衁E55, 656: `.groupby(prepared["race_id"])` ↁE`.groupby(prepared["race_id"], observed=True)`
- 衁E63, 666, 669, 672, 675: `.groupby(prepared["race_id"])` ↁE`.groupby(prepared["race_id"], observed=True)` (5箁E��)
- 衁E07: `.groupby("race_id", as_index=False, sort=False)` ↁE`.groupby("race_id", as_index=False, sort=False, observed=True)`
- 衁E40: `.groupby(prepared["race_id"])` ↁE`.groupby(prepared["race_id"], observed=True)`
- 衁E054: `.groupby("race_id", as_index=False, sort=False)` ↁE`.groupby("race_id", as_index=False, sort=False, observed=True)`

`src/models/place_selection_gate.py`:
- 衁E66: `.groupby(df["race_id"])` ↁE`.groupby(df["race_id"], observed=True)`
- 衁E20: `.groupby("race_id", as_index=False, sort=False)` ↁE`.groupby("race_id", as_index=False, sort=False, observed=True)`
- 衁E85, 586: `.groupby(prepared["race_id"])` ↁE`.groupby(prepared["race_id"], observed=True)`
- 衁E93, 596, 599, 602, 605: `.groupby(prepared["race_id"])` ↁE`.groupby(prepared["race_id"], observed=True)` (5箁E��)
- 衁E37: `.groupby("race_id", as_index=False, sort=False)` ↁE`.groupby("race_id", as_index=False, sort=False, observed=True)`
- 衁E62: `.groupby(prepared["race_id"])` ↁE`.groupby(prepared["race_id"], observed=True)`
- 衁E76: `.groupby("race_id", as_index=False, sort=False)` ↁE`.groupby("race_id", as_index=False, sort=False, observed=True)`

`src/models/ev_diagnostics.py`:
- 衁E25: `.groupby("year")` ↁE`.groupby("year", observed=True)`

`src/backtest/engine.py`:
- 衁E64: `.groupby(["race_id", "umaban"])` ↁE`.groupby(["race_id", "umaban"], observed=True)`
- 衁E15: `.groupby(["race_id", "lo", "hi"])` ↁE`.groupby(["race_id", "lo", "hi"], observed=True)`
- 衁E42: `.groupby(group_col)` ↁE`.groupby(group_col, observed=True)`

`src/backtest/race_predictor.py`:
- 衁E33, 237, 245, 256: `.groupby(race_df["race_id"])` ↁE`.groupby(race_df["race_id"], observed=True)`

`src/db/odds_extractor.py`:
- 衁E23: `.groupby(["race_id", "umaban"])` ↁE`.groupby(["race_id", "umaban"], observed=True)`

**クロスプランチE�Eタ契紁E��誁E** Plan 03のCategorical変換後、これらのモチE��・バックチE��トファイルはCategorical DataFrameを受け取る。observed=Trueを追加することで、Categorical groupbyの70倍遅延を回避する。P2の変更は型変換のみで計算結果に影響しなぁE��め、予測値は完�E一致するはず、E  </action>
  <verify>
    <automated>python -m pytest tests/ -v -x --timeout=120 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "observed=True" src/models/win_selection_gate.py` ぁE0以上であること
    - `grep -c "observed=True" src/models/place_selection_gate.py` ぁE0以上であること
    - `grep -rn "\.groupby(" src/models/ src/backtest/ src/db/odds_extractor.py | grep -v "observed" | grep -v "^#"` ぁE件であること
    - `python -m ruff check src/models/ src/backtest/ src/db/odds_extractor.py` がエラーを�EさなぁE��と
    - `python -m pytest tests/ -v -x` が�EチE��ト合格すること
  </acceptance_criteria>
  <done>
    - src/models/ 冁E�E全groupby呼び出しにobserved=Trueが追加されてぁE�� (D-05)
    - src/backtest/ 冁E�E全groupby呼び出しにobserved=Trueが追加されてぁE�� (D-05)
    - src/db/odds_extractor.py のgroupby呼び出しにobserved=Trueが追加されてぁE�� (D-05)
    - Categorical変換後もモチE��推論�EバックチE��ト結果が完�E一致する (P2)
    - 既存テスト�E通過、lint/型エラーなぁE  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Categorical DataFrames ↁEmodel inference | Models receive Categorical dtype from ParquetStore, observed=True prevents Cartesian product expansion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-10 | D | Incomplete observed=True in models/ | mitigate | grep-based acceptance criteria verifying 0 un-observed groupby calls in models/ + backtest/ |
| T-19.1-11 | I | Categorical merge in model inference | mitigate | All DataFrames loaded from same ParquetStore share category pool; model inference uses merge on race_id which is Categorical but consistent |
</threat_model>

<verification>
1. `python -m pytest tests/ -v -x` -- 全チE��ト通過
2. `python -m ruff check src/models/ src/backtest/ src/db/odds_extractor.py` -- lintエラーなぁE3. `python -m mypy src/` -- 型エラーなぁE4. Complete observed=True coverage verification:
   - `grep -rn "\.groupby(" src/models/ src/backtest/ src/db/odds_extractor.py | grep -v "observed" | grep -v "^#"` should return 0 results
</verification>

<success_criteria>
- src/models/ + src/backtest/ + src/db/odds_extractor.py にobserved=Trueなし�Egroupby呼び出しが0件である (D-05)
- 全モチE��推論がCategorical DataFrameで正常動作すめE- 全チE��ト通過、lint/型エラーなぁE</success_criteria>

<output>
After completion, create `.planning/phases/19.1-backtest-speedup-optimization/19.1-04-SUMMARY.md`
</output>
