---
phase: 19.1-backtest-speedup-optimization
plan: 05
subsystem: features, backtest, tests
tags: [performance, P3, class-cache, per-race-dedup, stage-verification, D-06, D-07]
dependency_graph:
  requires:
    - phase: 19.1-03
      provides: "CATEGORICAL_COLUMNS constant and _optimize_dtypes() in ParquetStore (D-04)"
    - phase: 19.1-04
      provides: "observed=True on all groupby calls in src/models/ and src/backtest/ (D-05)"
  provides:
    - "HorseHistoryFeatures class-level cache for cross-instance data sharing (P3: ~100s)"
    - "Per-race add_race_transforms direct rank optimization (P3: ~50-100s)"
    - "TestBacktestOptimizationStages with 7 stage verification tests (D-06, D-07)"
  affects: [HorseHistoryFeatures, RacePredictor, BacktestEngine]
tech_stack:
  added: []
  patterns: [class-level-cache-for-cross-instance-sharing, single-race-direct-rank, stage-verification-tests]
key_files:
  created: []
  modified:
    - src/features/horse_history_features.py
    - src/backtest/race_predictor.py
    - src/backtest/engine.py
    - tests/test_backtest_engine.py
decisions:
  - "HorseHistoryFeaturesクラスレベルキャッシュを採用 — 3回呼び出し間(芝/ダートsubmodel + test BT)で前計算結果を共有 (~100s削減)"
  - "per-race add_race_transformsを直接rank()に最適化 — groupby("race_id")オーバーヘッド排除 (~50-100s削減)"
  - "print文をlogger.debugに変更 — 本番バックテスト実行時のIO削減"
  - "段階的検証テスト(D-06/D-07)を7つのテストメソッドで実装 — P0-P3の全最適化段階をカバー"
key-decisions:
  - "P3最適化としてキャッシュ強化 + per-race重複排除を採用(full vector化はMEDIUM confidenceのため保留)"
  - "race_predictor.pyのHorseHistoryFeatures importを削除 — add_race_transforms呼び出しが不要になったため"

patterns-established:
  - "class-level cache: HorseHistoryFeatures._class_cacheでインスタンス間データ共有。キャッシュキーにid(past_by_ketto_arr)を含め呼び出しコンテキストを分離"

requirements-completed: []

metrics:
  duration: 14m
  completed: "2026-05-08"
---

# Phase 19.1 Plan 05: P3 HorseHistoryFeaturesキャッシュ強化 + per-race重複排除 + 段階的検証テスト Summary

HorseHistoryFeaturesにクラスレベルキャッシュ(_class_cache)を追加し、3回呼び出し間(芝/ダートsubmodel + test BT)で前計算結果を共有して~100s削減。per-race add_race_transformsのgroupby("race_id").rank()を直接rank()に最適化して~50-100s削減。段階的検証テストTestBacktestOptimizationStages(7テスト)を追加しD-06/D-07要件を達成。

## Performance

- **Duration:** 14 min
- **Started:** 2026-05-08T10:11:14Z
- **Completed:** 2026-05-08T10:25:43Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- HorseHistoryFeaturesに_class_cacheクラス変数 + clear_class_cache()クラスメソッドを追加
- _get_cached_horse_arrays()メソッドを追加し、itertuplesループ内のpast_by_ketto_arr.get()をキャッシュ対応版に置換
- print文(プログレス表示2箇所)をlogger.debugに変更
- race_predictor.pyのpredict()内add_race_transforms呼び出しを直接rank計算に最適化(groupbyオーバーヘッド排除)
- HorseHistoryFeatures importをrace_predictor.pyから削除(未使用になったため)
- BacktestEngine.run()のreturn直前にHorseHistoryFeatures.clear_class_cache()を追加(メモリリーク防止)
- TestBacktestOptimizationStagesクラスに7つの段階的検証テストを追加
- 全1356テスト通過(新規7テスト含む)、lintエラーなし

## Task Commits

Each task was committed atomically:

1. **Task 1: P3 HorseHistoryFeaturesキャッシュ強化 + per-race add_race_transforms重複排除** - `b868530` (feat)
2. **Task 2: P0-P3段階的検証テスト (D-06, D-07)** - `470b436` (test)

## Files Created/Modified

- `src/features/horse_history_features.py` - _class_cache, clear_class_cache(), _get_cached_horse_arrays()追加、print→logger.debug変更
- `src/backtest/race_predictor.py` - per-race直接rank最適化、HorseHistoryFeatures import削除
- `src/backtest/engine.py` - run()終了時にclear_class_cache()追加
- `tests/test_backtest_engine.py` - TestBacktestOptimizationStagesクラス(7テスト)追加

## Decisions Made

- クラスレベルキャッシュ(_class_cache)を採用。3回のcompute()呼び出し間で前計算したpast_by_ketto_arrデータを共有 (~100s削減)
- per-race add_race_transformsのgroupby("race_id").rank()を直接rank()に置換。単一race DataFrameではgroupby不要であり、5000レースx7列のオーバーヘッドを排除 (~50-100s削減)
- training_pipeline.pyからのadd_race_transforms呼び出し(全レース一括)はstaticメソッドのまま変更なし
- 段階的検証テストでD-05のobserved=True検証に5行コンテキストチェックを導入(複数行groupby呼び出しの誤検知防止)
- D-02テストでpatch("backtest.engine.BacktestEngine")を使用(scripts.run_backtest.py内のローカルimportに対応)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] D-02テストのpatch先をbacktest.engine.BacktestEngineに変更**
- **Found during:** Task 2 (テスト実行時)
- **Issue:** scripts.run_backtest.pyの_collect_training_bet_history()内でBacktestEngineがローカルimportされるため、patch("scripts.run_backtest.BacktestEngine")がAttributeError
- **Fix:** patch先をbacktest.engine.BacktestEngineに変更。関数内のfrom importは元のモジュールを参照するため、元モジュール側をパッチする必要がある
- **Files modified:** tests/test_backtest_engine.py
- **Commit:** 470b436

**2. [Rule 3 - Blocking] D-05テストの複数行groupby対応**
- **Found during:** Task 2 (テスト実行時)
- **Issue:** groupby呼び出しが複数行にまたがる場合、observed=Trueが次行にあっても単一行チェックで誤検知される(17件の偽陽性)
- **Fix:** groupby呼び出し検出時に5行のコンテキストを確認し、その範囲内にobservedが含まれればviolationから除外
- **Files modified:** tests/test_backtest_engine.py
- **Commit:** 470b436

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 19.1の全5プラン完了 — P0-P3最適化が全て実装済み
- 段階的検証テスト(D-06, D-07)でP0-P2完全一致 + P3統計的許容範囲を確認可能
- BacktestEngine.run()のキャッシュクリアでメモリリークを防止
- 次フェーズ(Phase 19/20)に進行可能

## Self-Check: PASSED

- src/features/horse_history_features.py: FOUND
- src/backtest/race_predictor.py: FOUND
- src/backtest/engine.py: FOUND
- tests/test_backtest_engine.py: FOUND
- 19.1-05-SUMMARY.md: FOUND
- Commit b868530: FOUND
- Commit 470b436: FOUND

---
*Phase: 19.1-backtest-speedup-optimization*
*Completed: 2026-05-08*
