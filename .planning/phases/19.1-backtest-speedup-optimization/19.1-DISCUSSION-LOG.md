# Phase 19.1: バックテスト高速化 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-08
**Phase:** 19.1-バックテスト高速化（Spike 002ボトルネック改善）
**Areas discussed:** キャリブレーションBT扱い, 最適化の対象範囲, Categorical化の適用方針, 結果検証基準

---

## キャリブレーションBT扱い

| Option | Description | Selected |
|--------|-------------|----------|
| --skip-calibration フラグ追加 | デフォルトでスキップ、--strategy-manifest時のみ実行。効果: -4900s | |
| --calibration-months N パラメータ追加 | 4年からNヶ月に短縮。デフォルト6-12ヶ月 | |
| 両方実装 | フラグ + 期間パラメータの両方 | |

**User's choice:** 条件付きスキップ（デフォルトスキップ、manifest時のみ軽量実行）
**Notes:** ユーザーは「Phase Bが機能していないのなら削ればいいだけ」と指摘。Spike 003で退化現象を確認済みなので、デフォルトスキップは合理的。`--strategy-manifest`時のみ6-12ヶ月の軽量キャリブレーションを実行する推奨案を採用。

---

## 最適化の対象範囲

| Option | Description | Selected |
|--------|-------------|----------|
| P0のみ（最小） | Calib BTスキップのみ。半減達成 | |
| P0+P1 | + MLflow pip + odds受け渡し + _coerce_types。合計-5058s | |
| P0+P1+P2（野心） | + Categorical化 + キャッシュ強化。更に-300~500s | |
| 全実装（最大） | P0〜P3全部。HorseHistoryFeatures vector化含む | ✓ |

**User's choice:** 全実装に挑戦
**Notes:** ユーザーはリスクを質問。P2はCategorical化のクロスカッティング変更、P3はfloat演算差異のリスクを説明した上で全実装を選択。

---

## Categorical化の適用方針

| Option | Description | Selected |
|--------|-------------|----------|
| 包括的適用 | ParquetStore/DataRepositoryレベル。全コンシューマー自動適用 | ✓ |
| 対象モジュール限定 | HorseHistoryFeatures等4モジュールのみ | |
| You decide | Claudeに判断を任せる | |

**User's choice:** 包括的適用
**Notes:** Spike 004で1.3-3.3x高速、メモリ92.6%削減を実測済み。observed=Trueの全groupbyへの追加が必須条件。

---

## 結果検証基準

| Option | Description | Selected |
|--------|-------------|----------|
| 統計的許容範囲 | ROI/bet_count差異<5%、的中率差異<1% | |
| 予測値完全一致 | P3 vector化では不可能な場合あり | |
| You decide | Claudeに判断を任せる | ✓ |

**User's choice:** You decide → Claude裁量で段階的検証基準を採用
**Notes:** P0-P2は予測値完全一致を期待（結果に影響する変更なし）。P3は統計的許容範囲を許容。各最適化段階で段階的に検証。

---

## Claude's Discretion

- 結果検証基準の具体的な閾値設定
- _coerce_types早期returnの実装詳細
- キャリブレーション期間のデフォルト月数（6-12ヶ月範囲内）
- HorseHistoryFeatures vector化のgroupby+rollingパターン
- インスタンスキャッシュの実装方式
- Categorical変換のタイミング

## Deferred Ideas

None — discussion stayed within phase scope
