---
phase: 22-integrated-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/backtest/backtest_result.json
  - data/validation/validation_report.json
  - data/backtest/bt_2024_win_flat.{}.csv
autonomous: true
requirements: [VAL-01, VAL-02]

must_haves:
  truths:
    - "全既存テストが通過する（回帰なし）"
    - "Phase 19/20/21改善適用後のバックテストが正常完了する"
    - "バックテスト結果のROIがv1.4ベースライン(83.1%)と比較可能"
    - "EV過大評価倍率がv1.4ベースライン(2.42倍)と比較可能"
    - "validation_report.jsonがPASS/FAIL判定を含む"
  artifacts:
    - path: "data/backtest/backtest_result.json"
      provides: "バックテスト結果JSON（ROI、的中率、ベット数等）"
      contains: "roi"
    - path: "data/validation/validation_report.json"
      provides: "PASS/FAIL判定を含む検証レポート"
      contains: "verdict"
  key_links:
    - from: "scripts/run_backtest.py"
      to: "src/backtest/engine.py"
      via: "CLI -> BacktestEngine.run()"
      pattern: "BacktestEngine"
    - from: "src/backtest/engine.py"
      to: "src/backtest/validation_report.py"
      via: "generate_validation_report()"
      pattern: "generate_validation_report"
---

<objective>
Phase 19（Isotonic EVキャリブレーション）、Phase 20（高オッズ的中パターン特徴量）、Phase 21（CQR Conformal EV予測区間）の全改善を適用した統合バックテストを実行し、ROI改善を確認する。既存テスト全通過で回帰なしを確認し、結果をv1.4ベースライン（ROI 83.1%、EV過大評価2.42倍）と比較する。

Purpose: v1.5マイルストーンの全改善効果を定量的に検証し、モデル精度向上を確認する
Output: テスト通過確認、バックテスト結果JSON、検証レポート、セグメント別分析サマリ
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-integrated-validation/22-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 既存テストスイート実行で回帰確認</name>
  <files>（出力なし — 読み取り専用確認タスク）</files>
  <read_first>
    - tests/ （テストディレクトリ構造確認用）
    - pyproject.toml （テスト設定確認用）
  </read_first>
  <action>
1. 以下のコマンドで全テストを実行:
   ```
   python -m pytest tests/ -v --tb=short 2>&1 | tail -50
   ```
2. テスト結果の最終行で "passed" 数と "failed" 数を確認。1,393テスト前後が通過するはず。
3. テスト失敗が0件であることを確認。失敗がある場合は、失敗テスト名とエラーメッセージをそのままレポートし、回帰として扱う。

Per D-12: テストは既存1,393テストの通過確認のみ。統合E2Eテストの追加はしない。
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/ -v --tb=short 2>&1 | grep -E "^(FAILED|ERROR)"` が0行であること
    - `python -m pytest tests/ -v --tb=short 2>&1 | tail -5` に "passed" が含まれ、"failed" が含まれないこと（0 failed）
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ -v --tb=short -q 2>&1 | tail -5</automated>
  </verify>
  <done>全テスト通過（failed=0）。テスト数をログに記録。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 統合バックテスト実行</name>
  <what_built>
Task 1で回帰なしを確認した後、Phase 19/20/21の全改善を適用したバックテストを実行する。
PostgreSQL（localhost:5432/everydb2）が必要で、所要時間は約57分。
  </what_built>
  <files>
    - data/backtest/backtest_result.json （自動生成）
    - data/validation/validation_report.json （--report で自動生成）
  </files>
  <read_first>
    - scripts/run_backtest.py （CLI引数確認）
    - src/backtest/engine.py （BacktestResult構造確認）
  </read_first>
  <action>
以下のコマンドで2024単年度バックテストを実行（per D-01: テスト年度2024、学習2020-2023）:

```
python scripts/run_backtest.py `
  --train-start 20200101 --train-end 20231231 `
  --test-start 20240101 --test-end 20241231 `
  --ensemble `
  --betting-mode flat `
  --calibration-bt `
  --report
```

フラグの根拠:
- `--ensemble`: アンサンブルモデル有効化（Phase 7以降の標準）
- `--betting-mode flat`: per D-02 — Kelly sizing影響を除外し純粋なモデル改善効果を測定
- `--calibration-bt`: per D-03 — OddsBandFilter再キャリブレーション有効化（~16分追加）
- `--report`: per D-04 — HTMLレポート + parquet + validation_report.json自動生成
- `--strategy-manifest` なし: per D-05 — manifestファイルは使用しない

所要時間: ~57分（キャリブレーションBT込み）

**注意:** このタスクはPostgreSQL接続（localhost:5432/everydb2）が必要なため、ローカル環境でのみ実行可能。ClaudeがPostgreSQLに接続できない場合は、ユーザーが手動でコマンドを実行する。
  </action>
  <acceptance_criteria>
    - `data/backtest/backtest_result.json` が存在し、JSONとしてパース可能
    - backtest_result.json に "roi" キーが含まれる
    - `data/validation/validation_report.json` が存在し、"verdict" キーが含まれる
  </acceptance_criteria>
  <how-to-verify>
    1. バックテストコマンドが正常終了（exit code 0）すること
    2. `data/backtest/backtest_result.json` が生成され、ROI値が含まれること
    3. `data/validation/validation_report.json` が生成され、verdictが記載されること
    4. コンソール出力に "Backtest complete" または最終ROI表示があること
  </how-to-verify>
  <resume-signal>Type "approved" if backtest completed successfully, or describe any errors</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 結果分析とベースライン比較レポート</name>
  <files>.planning/phases/22-integrated-validation/22-01-SUMMARY.md</files>
  <read_first>
    - data/backtest/backtest_result.json （バックテスト結果）
    - data/validation/validation_report.json （PASS/FAIL判定）
    - src/backtest/report.py （レポート生成内容の理解）
    - src/models/ev_diagnostics.py （EV診断メトリクスの理解）
  </read_first>
  <action>
1. backtest_result.json を読み込み、以下のメトリクスを抽出:
   - ROI（全体）
   - 高オッズ帯(20+)のROI
   - EV過大評価倍率（全体・高オッズ帯）
   - レジーム別ROI（aggressive/conservative/collapsed）
   - 的中率、平均オッズ
   - ベット数、n_ev_excluded

2. v1.4ベースライン（per D-06/D-07）と比較:
   - v1.4全体ROI: 83.1% → v1.5全体ROI: ?（改善率を計算）
   - v1.4 EV過大評価: 2.42倍 → v1.5 EV過大評価: ?（改善率を計算）
   - 高オッズ帯(20+)ROI比較
   - Success Criteria判定:
     * ROI 95%以上 → PASS/FAIL
     * 高オッズ帯(20+) ROI 50%以上 → PASS/FAIL

3. validation_report.json の verdict を確認（PASS/FAIL）

4. --report で生成されたHTML/parquetファイルが存在するか確認:
   ```
   ls data/backtest/bt_2024_*.{csv,parquet} 2>/dev/null
   ```

5. 結果をSUMMARY.mdのアーカイブ形式で記録:
   - バックテスト条件（D-01〜D-05の設定値）
   - 主要メトリクス表（v1.4 vs v1.5）
   - Success Criteria判定結果
   - per D-11: ROI 95%不達の場合でも現状でv1.5完了とする旨を記載

6. 結論を記述:
   - v1.5改善効果の総合評価
   - 次マイルストーンへの推奨事項（改善が必要な場合）
  </action>
  <acceptance_criteria>
    - 22-01-SUMMARY.md に "ROI" 文字列が含まれる
    - 22-01-SUMMARY.md に "83.1%" （v1.4ベースライン）が含まれる
    - 22-01-SUMMARY.md に "2.42" （v1.4 EV過大評価倍率）が含まれる
    - 22-01-SUMMARY.md に Success Criteria 判定（PASS or FAIL）が含まれる
  </acceptance_criteria>
  <verify>
    <automated>grep -E "(ROI|83\.1|2\.42|PASS|FAIL)" ".planning/phases/22-integrated-validation/22-01-SUMMARY.md"</automated>
  </verify>
  <done>
v1.4 vs v1.5の比較表を含む分析レポートがSUMMARY.mdに記録されている。
Success Criteria（ROI 95%+、高オッズ帯ROI 50%+）の判定結果が明記されている。
  </done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/ -v --tb=short` が0 failuresで完了
2. `data/backtest/backtest_result.json` が存在しROI値を含む
3. `data/validation/validation_report.json` が存在しverdictを含む
4. SUMMARY.mdにv1.4ベースライン比較が記載されている
</verification>

<success_criteria>
1. 全既存テスト通過（回帰なし） — VAL-01
2. バックテストが正常完了しROIが算出される — VAL-01
3. EV診断メトリクスがv1.4ベースラインと比較可能 — VAL-02
4. Success Criteria判定（ROI 95%+、高オッズ帯ROI 50%+）が記録されている
5. per D-11: ROI不達の場合でも現状でv1.5完了と判定し、次マイルストーンで改善
</success_criteria>

<output>
After completion, create `.planning/phases/22-integrated-validation/22-01-SUMMARY.md`
</output>
