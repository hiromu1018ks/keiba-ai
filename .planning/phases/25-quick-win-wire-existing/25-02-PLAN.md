---
phase: 25-quick-win-wire-existing
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - scripts/run_backtest.py
autonomous: false
requirements:
  - WIRE-01
  - WIRE-02
  - WIRE-03

must_haves:
  truths:
    - "フルバックテストが正常に完了し、backtest_result.jsonが生成される"
    - "Phase 24後ベースラインと比較してROIが報告される"
  artifacts:
    - path: "backtest_result.json"
      provides: "バックテストROI結果"
    - path: "data/validation/validation_report.json"
      provides: "検証レポート"
  key_links:
    - from: "scripts/run_backtest.py"
      to: "src/models/two_stage_return_model.py"
      via: "FEATURE_COLS経由で12特徴量を使用した学習・推論"
      pattern: "FEATURE_COLS"
---

<objective>
フルバックテストを実行し、12特徴量配線後のROIを確認する (per D-04)。

Purpose: Phase 26-27の新特徴量追加前に、既存12特徴量のROI改善効果を定量化する。Phase 24プルーニング後のベースラインも同時に確立できる。
Output: backtest_result.json, data/validation/validation_report.json
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-quick-win-wire-existing/25-CONTEXT.md
@.planning/phases/25-quick-win-wire-existing/25-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: バックテスト実行コマンド準備と実行</name>
  <files></files>
  <read_first>
    - scripts/run_backtest.py (CLI引数定義の確認)
  </read_first>
  <action>
    per D-04: フルバックテストを実行し、配線前後のROIを比較する。

    バックテストコマンド:
    ```
    python scripts/run_backtest.py --years 2024 --train-window 4 --ensemble --calibration-bt --report
    ```

    このコマンドは:
    - `--years 2024`: 2024年をテスト期間
    - `--train-window 4`: 4年間の学習データ (2020-2023)
    - `--ensemble`: アンサンブルモデル有効化
    - `--calibration-bt`: キャリブレーションBT有効化（ROI改善）
    - `--report`: HTMLレポート + parquet出力

    所要時間: ~57分

    完了後、backtest_result.jsonのROIを確認する。
    v1.5ベースライン: 84.4%
    Phase 24プルーニング後ベースライン: 未確立（このBTで確立される）
  </action>
  <verify>
    <automated>python -c "import json; r=json.load(open('backtest_result.json')); print(f'ROI: {r.get(\"roi\", r.get(\"total_roi\", \"N/A\"))}')" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - backtest_result.json が存在する
    - backtest_result.json に "roi" または "total_roi" キーが存在する
    - data/validation/validation_report.json が存在する
  </acceptance_criteria>
  <done>フルバックテストが完了し、ROI結果がbacktest_result.jsonに記録される</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>12特徴量配線後のフルバックテスト結果</what-built>
  <how-to-verify>
    1. `python -c "import json; r=json.load(open('backtest_result.json')); print(json.dumps({k:v for k,v in r.items() if 'roi' in k.lower() or 'return' in k.lower()}, indent=2))"` でROI確認
    2. v1.5ベースライン(84.4%)と比較:
       - 改善: Phase 25成功、Phase 26へ進む
       - 悪化: ロールバック検討が必要
    3. data/validation/validation_report.json で PASS/FAIL 確認
    4. data/backtest/ ディレクトリのBT CSVでベット数・的中数が妥当か確認
  </how-to-verify>
  <resume-signal>Type "approved" if ROI is acceptable, or describe issues for rollback</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BT実行環境 | PostgreSQL接続(localhost:5432/everydb2)が必要。Parquetデータが最新であること |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03 | D (Denial of Service) | バックテスト実行 | accept | ~57分の長時間実行。正常な動作。リソース枯渇リスクは低い |
</threat_model>

<verification>
1. backtest_result.json が生成される
2. data/validation/validation_report.json が生成される
3. data/backtest/ にBT CSV/parquetファイルが生成される
4. ROIがv1.5ベースライン(84.4%)と比較可能
</verification>

<success_criteria>
- フルバックテストが正常に完了する
- ROI結果がbacktest_result.jsonに記録される
- ユーザーがROI結果を確認し、Phase 26への進行を承認する
</success_criteria>

<output>
After completion, create `.planning/phases/25-quick-win-wire-existing/25-02-SUMMARY.md`
</output>
