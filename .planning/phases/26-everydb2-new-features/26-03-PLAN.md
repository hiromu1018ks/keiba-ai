---
phase: 26-everydb2-new-features
plan: 03
type: execute
wave: 3
depends_on:
  - 26-02
files_modified:
  - src/features/relative_features.py
  - src/pipelines/training_pipeline.py
  - src/models/stage1_ability_model.py
  - src/models/two_stage_return_model.py
  - tests/test_relative_features.py
autonomous: true
requirements:
  - DATA-03

must_haves:
  truths:
    - "relative_features.pyがgroupby('race_id')でレース内相対比較特徴量を7つ生成する"
    - "相対特徴量がz-score、rank、vs_meanの3種の計算手法で生成される"
    - "NaNハンドリングが適切に行われる（skipna=True、std=0フォールバック）"
    - "新特徴量が各モデルのFEATURE_COLSに追加されている"
    - "POST_RACE漏洩テストが通過する"
  artifacts:
    - path: "src/features/relative_features.py"
      provides: "compute_relative_features()関数。7つのレース内相対比較特徴量を生成"
      exports: ["compute_relative_features", "RELATIVE_FEATURE_COLS"]
    - path: "tests/test_relative_features.py"
      provides: "relative_featuresのmock-basedテスト"
  key_links:
    - from: "src/features/relative_features.py"
      to: "src/features/intra_race_features.py"
      via: "同じgroupby('race_id')パターン、別モジュール"
      pattern: "groupby.*race_id"
    - from: "src/pipelines/training_pipeline.py"
      to: "src/features/relative_features.py"
      via: "_train_submodel()で全per-horse features計算後に呼び出し"
      pattern: "compute_relative_features"
---

<objective>
レース内全馬の相対比較特徴量を7つ生成するrelative_features.pyモジュールを実装する。既存のper-horse特徴量（norm_finish_logit_avg, harontimel5_avg, blood_total_wr等）をgroupby("race_id")で相対化し、z-score/rank/vs_meanの3種の変換を適用する。_train_submodel()の全per-house features計算後に統合する。

Purpose: 競馬は相対スポーツ。馬の絶対的能力よりも「このレースの中でどの位置にいるか」が予測に重要。相対特徴量はLightGBMが個別には学習困難なrace-field contextを明示的にエンコードする。
Output: relative_features.py, テスト, FEATURE_COLS更新, _train_submodel()統合
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-everydb2-new-features/26-CONTEXT.md
@.planning/phases/26-everydb2-new-features/26-RESEARCH.md
@.planning/phases/26-everydb2-new-features/26-02-SUMMARY.md

<interfaces>
<!-- intra_race_features.py: existing 2-feature pattern to follow -->

From src/features/intra_race_features.py:
```python
def compute_intra_race_features(df: pd.DataFrame) -> pd.DataFrame:
    df = df.copy()
    if "bataijyu" in df.columns:
        weight_mean = df.groupby("race_id", observed=True)["bataijyu"].transform("mean")
        df["weight_diff_from_mean"] = df["bataijyu"] - weight_mean
    if "odds" in df.columns:
        df["odds_rank"] = (
            df.groupby("race_id", observed=True)["odds"]
            .rank(method="min", ascending=True)
            .astype("Int64")
        )
    return df
```

From src/pipelines/training_pipeline.py _train_submodel():
- HorseHistoryFeatures: L364-371
- PaceAptitudeFeatures: L373-405
- CourseFeatures: L407-426
- SireFeatures: L428-454
- DamPedigreeFeatures: added by Plan 26-02
- MiningFeatures: added by Plan 26-01
- InteractionFeatures: L456-460
- MarketModel: L471-480
- compute_relative_features should be called AFTER all per-horse features (including DamPedigree, Mining, Interaction) but BEFORE MarketModel

Available base features at relative_features call point:
- norm_finish_logit_avg (from HorseHistoryFeatures)
- harontimel5_avg (from HorseHistoryFeatures)
- timediff_avg (from HorseHistoryFeatures)
- blood_total_wr (from BloodlineFeatures via build_all)
- sire_wr (from SireFeatures)
- weight_zscore (from build_all _map_basic_features)
- closing_index_avg (from HorseHistoryFeatures)

From src/models/stage1_ability_model.py FEATURE_COLS:
- "race_rank" features already exist (L58-63): norm_finish_logit_avg_race_rank, etc.
- These are ordinal rank features. New relative features add z-score and vs_mean variants.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: relative_features.pyの実装とテスト</name>
  <files>src/features/relative_features.py, tests/test_relative_features.py</files>
  <behavior>
    - Test 1: compute_relative_features() produces all 7 RELATIVE_FEATURE_COLS columns
    - Test 2: rel_norm_finish_zscore = (value - race_mean) / race_std for each horse in race
    - Test 3: rel_haron_vs_mean = value - race_mean for harontimel5_avg
    - Test 4: rel_timediff_rank = rank within race (ascending: lower timediff = rank 1)
    - Test 5: rel_blood_quality_rank = rank within race (descending: higher blood_total_wr = rank 1)
    - Test 6: rel_sire_quality_rank = rank within race (descending: higher sire_wr = rank 1)
    - Test 7: rel_weight_zscore = z-score of weight_zscore within race
    - Test 8: rel_closing_index_rank = rank within race (ascending: lower closing index = rank 1 for closing power)
    - Test 9: std=0 fallback: when all values in race are identical, z-score outputs 0.0 (not NaN/inf)
    - Test 10: Missing base feature column is skipped silently (no error, no column added for that feature)
    - Test 11: NaN base values produce NaN relative features (not affecting group mean/std)
    - Test 12: RELATIVE_FEATURE_COLS has exactly 7 entries, no duplicates
  </behavior>
  <action>
    per D-09, D-10, D-11:

    **src/features/relative_features.py**

    Create compute_relative_features() function following intra_race_features.py pattern:

    RELATIVE_FEATURE_COLS: list[str] = [
        "rel_norm_finish_zscore",    # z-score of norm_finish_logit_avg within race
        "rel_haron_vs_mean",         # (harontimel5_avg - race_mean) for late speed
        "rel_timediff_rank",         # rank of timediff_avg within race (ascending)
        "rel_blood_quality_rank",    # rank of blood_total_wr within race (descending)
        "rel_sire_quality_rank",     # rank of sire_wr within race (descending)
        "rel_weight_zscore",         # z-score of weight_zscore within race
        "rel_closing_index_rank",    # rank of closing_index_avg within race (ascending)
    ]

    Define base feature configuration:
    ```python
    _BASE_FEATURES: list[dict] = [
        {"base": "norm_finish_logit_avg", "output": "rel_norm_finish_zscore", "transform": "zscore"},
        {"base": "harontimel5_avg", "output": "rel_haron_vs_mean", "transform": "vs_mean"},
        {"base": "timediff_avg", "output": "rel_timediff_rank", "transform": "rank_asc"},
        {"base": "blood_total_wr", "output": "rel_blood_quality_rank", "transform": "rank_desc"},
        {"base": "sire_wr", "output": "rel_sire_quality_rank", "transform": "rank_desc"},
        {"base": "weight_zscore", "output": "rel_weight_zscore", "transform": "zscore"},
        {"base": "closing_index_avg", "output": "rel_closing_index_rank", "transform": "rank_asc"},
    ]
    ```

    Implement compute_relative_features(df: pd.DataFrame) -> pd.DataFrame:
    ```python
    def compute_relative_features(df: pd.DataFrame) -> pd.DataFrame:
        df = df.copy()
        for spec in _BASE_FEATURES:
            base = spec["base"]
            output = spec["output"]
            transform = spec["transform"]
            if base not in df.columns:
                continue
            grp = df.groupby("race_id", observed=True)[base]
            if transform == "zscore":
                mean = grp.transform("mean")
                std = grp.transform("std").fillna(0)
                std = std.replace(0, 1)  # fallback: std=0 -> zscore=0
                df[output] = (df[base] - mean) / std
            elif transform == "vs_mean":
                mean = grp.transform("mean")
                df[output] = df[base] - mean
            elif transform == "rank_asc":
                df[output] = grp.rank(method="min", ascending=True, na_option="keep")
            elif transform == "rank_desc":
                df[output] = grp.rank(method="min", ascending=False, na_option="keep")
        return df
    ```

    **tests/test_relative_features.py**

    Create comprehensive mock-based tests:

    Fixture: DataFrame with 2 races, 4 horses each.
    Race 1: horses with distinct norm_finish_logit_avg values [0.8, 0.5, 0.3, 0.1]
    Race 2: horses with values [0.7, 0.7, 0.7, 0.7] (all identical -- tests std=0 fallback)

    Also include NaN in one horse's harontimel5_avg to test NaN propagation.

    For each test in <behavior>, write a specific test method that verifies the expected output.
  </action>
  <verify>
    <automated>python -m pytest tests/test_relative_features.py -v -x 2>&1 | tail -30</automated>
  </verify>
  <done>relative_features.pyが7特徴量を生成し、全テストが通過する</done>
</task>

<task type="auto">
  <name>Task 2: _train_submodel()統合 + FEATURE_COLS更新 + 全テスト通過確認</name>
  <files>src/pipelines/training_pipeline.py, src/models/stage1_ability_model.py, src/models/two_stage_return_model.py, tests/test_two_stage_return_model.py</files>
  <read_first>
    - src/pipelines/training_pipeline.py (L456-470: interaction features block, Plan 26-01 mining insertion point)
    - src/models/stage1_ability_model.py (last FEATURE_COLS entries after Plan 26-01/02 additions)
    - src/models/two_stage_return_model.py (last entries in all 3 FEATURE_COLS lists)
    - tests/test_two_stage_return_model.py (feature_df fixture)
  </read_first>
  <action>
    **src/pipelines/training_pipeline.py: _train_submodel() integration**

    After the mining_features block (added by Plan 26-01) and interaction_features block (L459-460), but BEFORE the market_model block (L471), add:

    ```python
    # Group G: レース内相対比較特徴量 (all per-horse features の後)
    from features.relative_features import compute_relative_features

    with TimingContext(f"{surface}/relative_features"):
        df = compute_relative_features(df)
    ```

    This placement ensures all per-horse features (history, pace, course, sire, dam, mining, interaction) are available for relative computation.

    **src/models/stage1_ability_model.py: FEATURE_COLS additions**

    After the Plan 26-02 additions (dam_wr, bms_distance_wr, etc.), add 4 relative features:
    ```python
    # レース内相対比較特徴量 (DATA-03)
    "rel_norm_finish_zscore",
    "rel_haron_vs_mean",
    "rel_blood_quality_rank",
    "rel_sire_quality_rank",
    ```
    Only 4 of 7 go to Stage1 (the z-score and rank features that capture ability context).

    **src/models/two_stage_return_model.py: FEATURE_COLS additions**

    WinTwoStageModel.FEATURE_COLS -- add 3 relative features:
    ```python
    # レース内相対比較特徴量 (DATA-03)
    "rel_norm_finish_zscore",
    "rel_timediff_rank",
    "rel_closing_index_rank",
    ```

    PlaceTwoStageModel.HIT_FEATURE_COLS -- add 3 relative features:
    ```python
    # レース内相対比較特徴量 (DATA-03)
    "rel_norm_finish_zscore",
    "rel_sire_quality_rank",
    "rel_haron_vs_mean",
    ```

    PlaceTwoStageModel.RETURN_FEATURE_COLS -- add 2 relative features:
    ```python
    # レース内相対比較特徴量 (DATA-03)
    "rel_norm_finish_zscore",
    "rel_timediff_rank",
    ```

    **tests/test_two_stage_return_model.py: fixture updates**

    Add the 7 relative feature columns to the feature_df fixture:
    - "rel_norm_finish_zscore": [1.2, 0.3, -0.5, -1.0, 0.8, 0.1, -0.4, -0.5]
    - "rel_haron_vs_mean": [0.5, 0.2, -0.1, -0.3, 0.4, 0.1, -0.2, -0.3]
    - "rel_timediff_rank": [1, 2, 3, 4, 1, 2, 3, 4]
    - "rel_blood_quality_rank": [1, 2, 3, 4, 1, 2, 3, 4]
    - "rel_sire_quality_rank": [1, 2, 3, 4, 1, 2, 3, 4]
    - "rel_weight_zscore": [0.8, -0.3, 0.1, -0.6, 0.7, -0.2, 0.2, -0.5]
    - "rel_closing_index_rank": [4, 3, 2, 1, 4, 3, 2, 1]

    After all 3 plans are complete, run the full test suite to confirm everything passes together.
  </action>
  <verify>
    <automated>python -m pytest tests/ -v --tb=short 2>&1 | tail -50</automated>
  </verify>
  <done>compute_relative_features()が_train_submodel()に統合され、全モデルFEATURE_COLSが更新され、全テストが通過する</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| intra-race transform | groupby("race_id") transforms. All base features are PRE-race. Relative transforms preserve PRE status. |
| NaN propagation | NaN in base features -> NaN in relative features. LightGBM handles NaN natively. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03-01 | I (Information Disclosure) | relative features | accept | All base features are PRE-race (verified by prior phases). Relative transforms (z-score, rank) of PRE features remain PRE. |
| T-26-03-02 | D (Denial of Service) | groupby transform | mitigate | std=0 fallback to prevent division by zero. skipna implicit in pandas groupby. |
| T-26-03-03 | T (Tampering) | rank method | accept | method="min" ensures deterministic ranking. No edge cases with ties. |

</threat_model>

<verification>
1. `python -m pytest tests/test_relative_features.py -v` passes all tests
2. `python -m pytest tests/test_post_race_leakage.py -v` passes
3. `python -m pytest tests/test_two_stage_return_model.py -v` passes
4. `python -m pytest tests/ -v --tb=short` passes all tests
5. `ruff check src/features/relative_features.py src/pipelines/training_pipeline.py` no errors
6. `mypy src/features/relative_features.py` passes
</verification>

<success_criteria>
- relative_features.py exports RELATIVE_FEATURE_COLS with exactly 7 entries
- compute_relative_features() produces all 7 columns when base features are present
- std=0 fallback produces 0.0 (not NaN/inf) when all race values are identical
- Missing base features are skipped silently
- Stage1AbilityModel.FEATURE_COLS contains rel_norm_finish_zscore, rel_haron_vs_mean, rel_blood_quality_rank, rel_sire_quality_rank (4 new)
- WinTwoStageModel.FEATURE_COLS contains rel_norm_finish_zscore, rel_timediff_rank, rel_closing_index_rank (3 new)
- PlaceTwoStageModel.HIT_FEATURE_COLS contains rel_norm_finish_zscore, rel_sire_quality_rank, rel_haron_vs_mean (3 new)
- PlaceTwoStageModel.RETURN_FEATURE_COLS contains rel_norm_finish_zscore, rel_timediff_rank (2 new)
- _train_submodel() calls compute_relative_features() after all per-horse features, before market_model
- All POST_RACE leakage tests pass
- All existing tests pass (full test suite green)
</success_criteria>

<output>
After completion, create `.planning/phases/26-everydb2-new-features/26-03-SUMMARY.md`
</output>
