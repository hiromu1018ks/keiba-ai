---
phase: 27-feature-interactions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/relative_features.py
  - src/models/stage1_ability_model.py
  - src/models/two_stage_return_model.py
  - src/pipelines/training_pipeline.py
  - tests/test_relative_features.py
  - tests/test_two_stage_return_model.py
autonomous: true
requirements: [INTER-01]

must_haves:
  truths:
    - "オッズ相対特徴量 (rel_popularity_rank_zscore, rel_fuku_odds_zscore) がfukuoddslow/popularity_rankのDataFrame内相対zscoreとして計算される"
    - "能力値相対特徴量 (rel_p_ability_win_zscore, rel_p_ability_win_rank, rel_odds_ability_deviation) がStage1 OOF後のdf_oof上で計算される"
    - "Phase 26残り4特徴量がStage1 + Win FEATURE_COLSに含まれる"
    - "新規オッズ相対・能力値相対特徴量がWin FEATURE_COLSに含まれる"
    - "全テスト通過、POST_RACE漏洩テスト通過"
  artifacts:
    - path: "src/features/relative_features.py"
      provides: "拡張_BASE_FEATURES (オッズ相対) + compute_stage2_relative_features() (能力値相対)"
    - path: "src/models/stage1_ability_model.py"
      provides: "FEATURE_COLS更新 (rel_haron_vs_mean, rel_blood_quality_rank, rel_sire_quality_rank, rel_weight_zscore)"
    - path: "src/models/two_stage_return_model.py"
      provides: "WinTwoStageModel.FEATURE_COLS更新 (オッズ相対+能力値相対)"
  key_links:
    - from: "src/features/relative_features.py"
      to: "src/pipelines/training_pipeline.py"
      via: "_train_submodel() Group G + Stage1 OOF後フック"
      pattern: "compute_relative_features|compute_stage2_relative_features"
    - from: "src/models/two_stage_return_model.py"
      to: "src/features/relative_features.py"
      via: "FEATURE_COLS参照"
      pattern: "rel_p_ability_win|rel_odds_ability|rel_popularity_rank|rel_fuku_odds"
---

<objective>
INTER-01: オッズ・能力値等のレース内相対ランク特徴量を生成する。

Phase 26のrelative_features.pyを2方向に拡張:
1. _BASE_FEATURESにオッズ系特徴量を追加 (fukuoddslow, popularity_rank) -- Stage1 OOF前に計算可能
2. 新関数 compute_stage2_relative_features() で p_ability_win / odds_to_ability_ratio 依存の相対特徴量をStage1 OOF後に計算

Phase 26残り4個の相対特徴量 (rel_haron_vs_mean, rel_blood_quality_rank, rel_sire_quality_rank, rel_weight_zscore) は既にrelative_features.pyに定義済みだが、Stage1AbilityModelのFEATURE_COLSに未追加のため追加する (per D-01)。

Purpose: レース内での相対的位置づけを特徴量化し、LightGBMがレース間比較ではなくレース内比較を学習できるようにする。
Output: 拡張relative_features.py + FEATURE_COLS更新 + テスト
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-feature-interactions/27-CONTEXT.md
@.planning/phases/27-feature-interactions/27-RESEARCH.md
@.planning/phases/26-everydb2-new-features/26-03-SUMMARY.md

@src/features/relative_features.py
@src/features/odds_deviation_features.py
@src/models/stage1_ability_model.py
@src/models/two_stage_return_model.py
@src/pipelines/training_pipeline.py

<interfaces>
<!-- Executor needs these contracts from codebase -->

From src/features/relative_features.py:
- RELATIVE_FEATURE_COLS: list[str] -- 7 existing feature names
- _BASE_FEATURES: list[dict[str, str]] -- spec list with {base, output, transform}
- compute_relative_features(df: pd.DataFrame) -> pd.DataFrame -- groupby("race_id") transforms

From src/models/stage1_ability_model.py line 28:
- AbilityModel.FEATURE_COLS: list[str] -- ~94 features, needs rel_haron_vs_mean etc.

From src/models/two_stage_return_model.py line 48:
- WinTwoStageModel.FEATURE_COLS: list[str] -- ~67 features, needs new relative features

From src/models/two_stage_return_model.py line 323:
- PlaceTwoStageModel.HIT_FEATURE_COLS: list[str]
- PlaceTwoStageModel.RETURN_FEATURE_COLS: list[str]

From src/pipelines/training_pipeline.py lines 546-568:
- Stage1 OOF -> df_oof creation -> odds_to_ability_ratio -> odds_deviation_features call chain
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: relative_features.py拡張 -- _BASE_FEATURES追加 + compute_stage2_relative_features()新規</name>
  <files>src/features/relative_features.py, tests/test_relative_features.py</files>
  <read_first>
    src/features/relative_features.py
    tests/test_relative_features.py
    src/features/odds_deviation_features.py
  </read_first>
  <behavior>
    - Test 1: compute_relative_features()にfukuoddslow列を含むDataFrameを渡すと、rel_fuku_odds_zscoreがrace内zscoreとして計算される
    - Test 2: compute_relative_features()にpopularity_rank列を含むDataFrameを渡すと、rel_popularity_rank_zscoreがrace内zscoreとして計算される
    - Test 3: compute_relative_features()にfukuoddslow/popularity_rankがない場合はエラーなくスキップされる (既存パターン踏襲)
    - Test 4: compute_stage2_relative_features(df_oof)にp_ability_win列を含むDataFrameを渡すと、rel_p_ability_win_zscore と rel_p_ability_win_rank がrace内で計算される
    - Test 5: compute_stage2_relative_features(df_oof)にodds_to_ability_ratio列を含むDataFrameを渡すと、rel_odds_ability_deviationがrace内zscoreとして計算される
    - Test 6: compute_stage2_relative_features()にp_ability_win/odds_to_ability_ratioがない場合はNaN列が生成される (エラーなし)
    - Test 7: RELATIVE_FEATURE_COLSにrel_fuku_odds_zscoreとrel_popularity_rank_zscoreが含まれる
    - Test 8: STAGE2_RELATIVE_FEATURE_COLS定数がrel_p_ability_win_zscore, rel_p_ability_win_rank, rel_odds_ability_deviationを含む
  </behavior>
  <action>
    **per D-04**: _BASE_FEATURESリストに2個の新規specを追加する。既存7個のパターン (dict with base/output/transform) に従う:
    - {"base": "fukuoddslow", "output": "rel_fuku_odds_zscore", "transform": "zscore"}
    - {"base": "popularity_rank", "output": "rel_popularity_rank_zscore", "transform": "zscore"}

    RELATIVE_FEATURE_COLSリストに上記2個のoutput名を追加する (合計9個)。

    新規定数 STAGE2_RELATIVE_FEATURE_COLS を定義:
    - rel_p_ability_win_zscore
    - rel_p_ability_win_rank
    - rel_odds_ability_deviation

    新規関数 compute_stage2_relative_features(df: pd.DataFrame) -> pd.DataFrame を定義。
    p_ability_winの相対特徴量:
    - groupby("race_id")["p_ability_win"]でzscore (mean/std, std=0->1) -> rel_p_ability_win_zscore
    - groupby("race_id")["p_ability_win"]でrank(method="min", ascending=False, na_option="keep") -> rel_p_ability_win_rank

    odds_to_ability_ratioの相対特徴量:
    - groupby("race_id")["odds_to_ability_ratio"]でzscore (odds_deviation_features.pyパターンと同じ) -> rel_odds_ability_deviation

    各base列がdf.columnsにない場合はスキップ (NaN列として設定、エラーなし)。odds_deviation_features.pyのガードパターンを参照。
  </action>
  <verify>
    <automated>cd C:/Users/hirom/develop/keiba-ai && python -m pytest tests/test_relative_features.py -v</automated>
  </verify>
  <done>
    - _BASE_FEATURESが9個 (既存7 + オッズ2)
    - RELATIVE_FEATURE_COLSが9個
    - STAGE2_RELATIVE_FEATURE_COLS定数が3個 (p_ability_win系2 + odds_ability系1)
    - compute_stage2_relative_features()がp_ability_winとodds_to_ability_ratioの両方から相対特徴量を生成
    - 全テスト通過 (RED->GREEN確認済み)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FEATURE_COLS更新 + _train_submodel()統合 + Placeモデル更新</name>
  <files>src/models/stage1_ability_model.py, src/models/two_stage_return_model.py, src/pipelines/training_pipeline.py, tests/test_two_stage_return_model.py</files>
  <read_first>
    src/models/stage1_ability_model.py
    src/models/two_stage_return_model.py
    src/pipelines/training_pipeline.py
    tests/test_two_stage_return_model.py
    src/features/relative_features.py
  </read_first>
  <behavior>
    - Test 1: Stage1AbilityModel.FEATURE_COLSにrel_haron_vs_mean, rel_blood_quality_rank, rel_sire_quality_rank, rel_weight_zscoreが含まれる (per D-01)
    - Test 2: WinTwoStageModel.FEATURE_COLSにrel_popularity_rank_zscore, rel_fuku_odds_zscore, rel_p_ability_win_zscore, rel_p_ability_win_rank, rel_odds_ability_deviationが含まれる
    - Test 3: PlaceTwoStageModel.HIT_FEATURE_COLSとRETURN_FEATURE_COLSにオッズ相対特徴量が含まれる (Winと整合)
    - Test 4: _train_submodel内でStage1 OOF後にcompute_stage2_relative_features()が呼ばれている (grep検証)
    - Test 5: POST_RACE漏洩テストが通過する
  </behavior>
  <action>
    **per D-01**: Stage1AbilityModel.FEATURE_COLSにPhase 26残り4個を追加する。
    現在のStage1 FEATURE_COLS (~94個) の"# レース内相対比較特徴量 (DATA-03)"セクションに4個追加:
    - rel_haron_vs_mean
    - rel_blood_quality_rank
    - rel_sire_quality_rank
    - rel_weight_zscore

    WinTwoStageModel.FEATURE_COLSに新規特徴量を追加する。
    "# レース内相対比較特徴量 (DATA-03)"セクションに5個追加:
    - rel_popularity_rank_zscore
    - rel_fuku_odds_zscore
    - rel_p_ability_win_zscore
    - rel_p_ability_win_rank
    - rel_odds_ability_deviation

    PlaceTwoStageModel.HIT_FEATURE_COLSとRETURN_FEATURE_COLSにも同じ相対特徴量を追加する。Phase 26のSUMMARYに記録されたパターン (Win全列がPlace RETURNに含まれる必要) に従う。

    _train_submodel()内のStage1 OOF後フックにcompute_stage2_relative_features()を追加する。
    挿入位置: src/pipelines/training_pipeline.py の AbilityModel.train_oof() (行549) -> df_oof作成 (行551) -> odds_to_ability_ratio計算 (行564) の直後、compute_odds_deviation_features() (行568) の直前。
    importを追加: from features.relative_features import compute_stage2_relative_features
    呼び出し: df_oof = compute_stage2_relative_features(df_oof)

    tests/test_two_stage_return_model.pyのfeature_df fixtureに新規列を追加する。
  </action>
  <verify>
    <automated>cd C:/Users/hirom/develop/keiba-ai && python -m pytest tests/test_relative_features.py tests/test_two_stage_return_model.py tests/test_post_race_leakage.py -v</automated>
  </verify>
  <done>
    - Stage1AbilityModel.FEATURE_COLSに4個追加 (rel_haron_vs_mean等)
    - WinTwoStageModel.FEATURE_COLSに5個追加 (オッズ相対+能力値相対)
    - Place HIT/RETURNに対応する相対特徴量が追加されている
    - _train_submodel()でStage1 OOF後にcompute_stage2_relative_features()が呼ばれている
    - 全テスト通過 (既存テスト回帰なし)
    - POST_RACE漏洩テスト通過
  </done>
</task>

</tasks>

<verification>
1. python -m pytest tests/test_relative_features.py tests/test_two_stage_return_model.py tests/test_interaction_features.py tests/test_post_race_leakage.py -v -- 全テスト通過
2. grep -c "compute_stage2_relative_features" src/pipelines/training_pipeline.py -- 1以上
3. grep -c "rel_p_ability_win_zscore" src/models/two_stage_return_model.py -- 2以上 (Win + Place)
4. grep -c "rel_haron_vs_mean" src/models/stage1_ability_model.py -- 1
</verification>

<success_criteria>
- _BASE_FEATURESが9個に拡張 (オッズ2追加)
- STAGE2_RELATIVE_FEATURE_COLS定数が3個 (p_ability_win系2 + odds_ability系1)
- compute_stage2_relative_features()がtrain_pipeline.pyのStage1 OOF後フックで呼ばれる
- Stage1AbilityModel.FEATURE_COLSに4個追加 (Phase 26残り分)
- WinTwoStageModel.FEATURE_COLSに5個追加 (オッズ相対+能力値相対)
- 全テスト通過、POST_RACE漏洩テスト通過
</success_criteria>

<output>
After completion, create `.planning/phases/27-feature-interactions/27-01-SUMMARY.md`
</output>
