---
phase: 28-validation-freeze
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/freeze_feature_manifest.py
  - data/feature_freeze_manifest.json
autonomous: true
requirements:
  - pytest-regression-check
  - feature-freeze-manifest

must_haves:
  truths:
    - "全1,392+テストが通過し、回帰がない"
    - "全モデルのFEATURE_COLSがJSON manifestに記録されている"
    - "各モデルのFEATURE_COLSにSHA256ハッシュが付与されている"
  artifacts:
    - path: "scripts/freeze_feature_manifest.py"
      provides: "特徴量凍結manifest生成スクリプト"
      contains: "freeze_feature_manifest"
    - path: "data/feature_freeze_manifest.json"
      provides: "全モデルFEATURE_COLS + SHA256ハッシュ"
      contains: "overall_sha256"
  key_links:
    - from: "scripts/freeze_feature_manifest.py"
      to: "src/models/*.py"
      via: "各モデルクラスのFEATURE_COLS import"
      pattern: "from models\\."
---

<objective>
Phase 28の第一歩: pytest全テスト通過確認 (回帰なし) + 特徴量凍結manifest生成。

Purpose: Phase 23-27の全コード変更後に回帰がないことを確認し、v1.6の特徴量セットをJSON+SHA256で凍結する。バックテスト実行 (Plan 02) とは独立して実行可能。
Output: 全テスト通過確認 + `data/feature_freeze_manifest.json` (全12モデルのFEATURE_COLS + SHA256)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-validation-freeze/28-CONTEXT.md
@.planning/phases/28-validation-freeze/28-RESEARCH.md

<interfaces>
<!-- Feature freeze script imports these model classes -->

From src/models/stage1_ability_model.py:
  class AbilityModel:
      FEATURE_COLS: list[str]  # 95 features

From src/models/two_stage_return_model.py:
  class WinTwoStageModel:
      FEATURE_COLS: list[str]  # 81 features
  class PlaceTwoStageModel:
      HIT_FEATURE_COLS: list[str]   # 84 features
      RETURN_FEATURE_COLS: list[str] # 86 features

From src/models/ev_correction_model.py:
  class EVCorrectionModel:
      FEATURE_COLS: list[str]        # 24 features
  class PlaceEVCorrectionModel:
      FEATURE_COLS: list[str]        # 24 features

From src/models/conformal_ev_model.py:
  class ConformalEVModel:
      FEATURE_COLS: list[str]        # 131 features

From src/models/regime_detector.py:
  class RegimeDetector:
      FEATURE_COLS: list[str]        # 8 features

From src/models/market_model.py:
  class MarketModel:
      FEATURE_COLS: list[str]        # 7 features

From src/models/place_ability_model.py:
  class PlaceAbilityModel:
      FEATURE_COLS: list[str]        # 61 features

From src/models/race_quality_screener.py:
  class RaceQualityScreener:
      FEATURE_COLS: list[str]        # 22 features

From src/models/wide_two_stage_model.py:
  class WideTwoStageModel:
      SHARED_FEATURE_COLS: list[str] # 5 features

From src/backtest/parameter_freeze_protocol.py:
  def save_strategy_manifest(params, path) -> str  # JSON + SHA256 pattern (sort_keys=True, indent=2)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pytest全テスト実行 + 回帰確認</name>
  <files></files>
  <read_first>
    - tests/ (全テストディレクトリ構造)
  </read_first>
  <action>
    pytestで全テストを実行し、回帰がないことを確認する。

    実行コマンド: `python -m pytest tests/ -v --tb=short`

    確認事項:
    - 全テスト通過 (1,392+テスト)
    - テスト失敗数 = 0
    - エラー数 = 0
    - 特に以下のテストファイルが通過すること:
      - tests/test_post_race_leak.py (SAFE-01: POST_RACE漏洩テスト)
      - tests/test_win_feature_analysis.py (FEATURE_COLS一貫性テスト)
      - tests/test_target_encoding.py (INTER-03: TEテスト)
      - tests/test_two_stage_return_model.py (Stage2 FEATURE_COLS)

    NOTE: 3つのパイプラインテストが `record_df has duplicate race_ids: 3600` で失敗する可能性がある (Phase 27-03 SUMMARYの既知の問題)。この場合は回帰ではないため、計画を続行してよい。新規の失敗があればPlan 02に進まず、修正が必要。

    結果をROADMAP.md更新のために記録: 通過テスト数、失敗テスト数 (0であるべき)。
  </action>
  <verify>
    <automated>python -m pytest tests/ -v --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>
    - 全テスト通過 (失敗数 = 0)
    - 通過テスト数が1,392+であることを確認
    - テスト結果サマリーをテキストとして保持 (SUMMARY記録用)
  </done>
</task>

<task type="auto">
  <name>Task 2: 特徴量凍結manifest生成スクリプト作成 + 実行</name>
  <files>scripts/freeze_feature_manifest.py, data/feature_freeze_manifest.json</files>
  <read_first>
    - src/backtest/parameter_freeze_protocol.py (save_strategy_manifestパターン: sort_keys=True + indent=2 + SHA256)
    - src/models/stage1_ability_model.py (AbilityModel.FEATURE_COLS)
    - src/models/two_stage_return_model.py (WinTwoStageModel.FEATURE_COLS, PlaceTwoStageModel.HIT_FEATURE_COLS, RETURN_FEATURE_COLS)
    - src/models/ev_correction_model.py (EVCorrectionModel.FEATURE_COLS, PlaceEVCorrectionModel.FEATURE_COLS)
    - src/models/conformal_ev_model.py (ConformalEVModel.FEATURE_COLS)
    - src/models/regime_detector.py (RegimeDetector.FEATURE_COLS)
    - src/models/market_model.py (MarketModel.FEATURE_COLS)
    - src/models/place_ability_model.py (PlaceAbilityModel.FEATURE_COLS)
    - src/models/race_quality_screener.py (RaceQualityScreener.FEATURE_COLS)
    - src/models/wide_two_stage_model.py (WideTwoStageModel.SHARED_FEATURE_COLS)
  </read_first>
  <action>
    `scripts/freeze_feature_manifest.py` を新規作成し、全12モデルのFEATURE_COLSをJSON manifest + SHA256で凍結する。per D-07 (ParameterFreezeProtocolパターン) と D-08 (SHA256 hashはモデル毎)。

    スクリプト仕様:
    - sys.pathに `src/` を追加 (他のスクリプトと同じパターン)
    - 12モデルをインポートし、各FEATURE_COLSを収集:
      - AbilityModel.FEATURE_COLS (95)
      - WinTwoStageModel.FEATURE_COLS (81)
      - PlaceTwoStageModel.HIT_FEATURE_COLS (84)
      - PlaceTwoStageModel.RETURN_FEATURE_COLS (86)
      - EVCorrectionModel.FEATURE_COLS (24)
      - PlaceEVCorrectionModel.FEATURE_COLS (24)
      - ConformalEVModel.FEATURE_COLS (131)
      - RegimeDetector.FEATURE_COLS (8)
      - MarketModel.FEATURE_COLS (7)
      - PlaceAbilityModel.FEATURE_COLS (61)
      - RaceQualityScreener.FEATURE_COLS (22)
      - WideTwoStageModel.SHARED_FEATURE_COLS (5)
    - 各モデルについて:
      - cols_json = json.dumps(cols, sort_keys=True, indent=2) (D-07: deterministic)
      - sha256 = hashlib.sha256(cols_json.encode()).hexdigest() (D-08: モデル毎hash)
    - manifest dict構造:
      ```
      {
        "model_name": {
          "feature_count": int,
          "features": list[str],
          "sha256": str
        }
      }
      ```
    - 全体manifest:
      ```
      {
        "version": "v1.6",
        "timestamp": UTC ISO format,
        "overall_sha256": hash of full manifest json,
        "models": { ... }
      }
      ```
    - 出力先: `data/feature_freeze_manifest.json`
    - JSON書き込み: sort_keys=True, indent=2, ensure_ascii=False (D-07: deterministic)
    - 実行後にmanifest内容の概要を標準出力に表示 (モデル名、特徴量数、SHA256先頭8文字)

    実行コマンド: `python scripts/freeze_feature_manifest.py`
  </action>
  <verify>
    <automated>python scripts/freeze_feature_manifest.py && python -c "import json; m=json.load(open('data/feature_freeze_manifest.json')); assert 'overall_sha256' in m; assert len(m['models']) == 12; print(f'OK: {len(m[\"models\"])} models, sha256={m[\"overall_sha256\"][:12]}...')"</automated>
  </verify>
  <done>
    - `scripts/freeze_feature_manifest.py` が存在し、単独で実行可能
    - `data/feature_freeze_manifest.json` が生成され、12モデル分のFEATURE_COLS + SHA256が含まれる
    - version フィールドが "v1.6"
    - overall_sha256 フィールドが存在
    - 各モデルエントリに feature_count, features, sha256 が含まれる
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | このPlanは既存コードの読み取り + 新規スクリプト作成のみ。外部通信なし、DB接続なし |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | freeze_feature_manifest.py | accept | 新規スクリプト、データ入力なし。SHA256 hashの改ざんリスク低 |
| T-28-02 | Tampering | data/feature_freeze_manifest.json | mitigate | sort_keys=True + indent=2でdeterministic。git管理で改ざん検知可能 |
</threat_model>

<verification>
1. `python -m pytest tests/ -v --tb=short` 全テスト通過
2. `python scripts/freeze_feature_manifest.py` エラーなしで完了
3. `data/feature_freeze_manifest.json` が存在し、12モデル + overall_sha256 を含む
4. 各モデルの feature_count が期待値と一致:
   - AbilityModel: 95, WinTwoStageModel: 81, PlaceTwoStageModel.HIT: 84,
   - PlaceTwoStageModel.RETURN: 86, EVCorrectionModel: 24, PlaceEVCorrectionModel: 24,
   - ConformalEVModel: 131, RegimeDetector: 8, MarketModel: 7, PlaceAbilityModel: 61,
   - RaceQualityScreener: 22, WideTwoStageModel.SHARED: 5
</verification>

<success_criteria>
- 全テスト通過 (失敗数 = 0)
- `data/feature_freeze_manifest.json` が生成済み
- 12モデル全てのFEATURE_COLSがSHA256付きで記録されている
- `scripts/freeze_feature_manifest.py` が冪等実行可能 (同じコードから同じmanifest生成)
</success_criteria>

<output>
Create `.planning/phases/28-validation-freeze/28-01-SUMMARY.md` when done
</output>
