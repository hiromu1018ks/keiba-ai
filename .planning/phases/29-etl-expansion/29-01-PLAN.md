---
phase: 29-etl-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/etl_tables.yaml
  - src/db/etl.py
autonomous: true
requirements: [ETL-01, ETL-02, ETL-03]

must_haves:
  truths:
    - "PK definitions in etl_tables.yaml use kumi (not umaban1/2/3) for odds_sanren, odds_sanrentan, odds_umaren, odds_wide"
    - "_TABLE_TYPE_RULES has entries for odds_sanren, odds_umaren, odds_sanrentan with odds10 and int conversions"
    - "run_etl.py --mode full --tables can extract the 6 new tables without error"
  artifacts:
    - path: "config/etl_tables.yaml"
      provides: "Correct PK definitions for 12+2 table entries"
      contains: "kumi"
    - path: "src/db/etl.py"
      provides: "Type conversion rules for 3 new odds tables"
      contains: "odds_sanren"
  key_links:
    - from: "config/etl_tables.yaml"
      to: "src/db/etl.py"
      via: "load_table_config() loads YAML -> run_full_load uses pk + _TABLE_TYPE_RULES"
      pattern: "kumi.*umaban"
---

<objective>
Fix PK definitions in etl_tables.yaml and add type conversion rules in etl.py so the ETL pipeline can correctly extract trio/quinella/trifecta odds tables.

Purpose: The current YAML has wrong PK columns (umaban1/2/3 instead of kumi) for 14 entries, which breaks delta merge. Without _TABLE_TYPE_RULES entries, odds columns remain as strings in Parquet. These fixes are prerequisites for data extraction (ETL-01, ETL-02, ETL-03).

Output: Updated config/etl_tables.yaml (14 PK fixes) and src/db/etl.py (3 new type rules).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-etl-expansion/29-CONTEXT.md
@.planning/phases/29-etl-expansion/29-RESEARCH.md

<interfaces>
<!-- Key types and patterns the executor needs. Extracted from codebase. -->

From config/etl_tables.yaml — current WRONG PKs to fix:
  n_odds_sanren (line 99): pk: [year, monthday, jyocd, kaiji, nichiji, racenum, umaban1, umaban2, umaban3]
  n_odds_sanrentan (line 111): pk: [year, monthday, jyocd, kaiji, nichiji, racenum, umaban1, umaban2, umaban3]
  n_odds_umaren (line 75): pk: [year, monthday, jyocd, kaiji, nichiji, racenum, umaban1, umaban2]
  n_odds_wide (line 44): pk: [year, monthday, jyocd, kaiji, nichiji, racenum, umaban1, umaban2]
  s_odds_sanren (line 424): pk: [year, monthday, jyocd, kaiji, nichiji, racenum, umaban1, umaban2, umaban3]
  s_odds_sanrentan (line 436): pk: [year, monthday, jyocd, kaiji, nichiji, racenum, umaban1, umaban2, umaban3]
  s_odds_umaren (line 400): pk: [year, monthday, jyocd, kaiji, nichiji, racenum, umaban1, umaban2]
  s_odds_wide (line 370): pk: [year, monthday, jyocd, kaiji, nichiji, racenum, umaban1, umaban2]

  ALSO CHECK: n_odds_umatan (line 87) and s_odds_umatan (line 412) — may have same umaban1/2 issue.
  NOTE: _head tables already have correct pk without umaban columns (only race-level PK).

From src/db/etl.py lines 83-115 — _TABLE_TYPE_RULES dict:
  Existing pattern:
    "odds_wide": {
        "odds100": ["oddslow", "oddshigh"],
    },
  New entries needed after "odds_wide" entry:
    "odds_sanren": {
        "int": ["ninki"],
        "odds10": ["odds"],
    },
    "odds_umaren": {
        "int": ["ninki"],
        "odds10": ["odds"],
    },
    "odds_sanrentan": {
        "int": ["ninki"],
        "odds10": ["odds"],
    },
  Note: _head tables need NO entries (no odds column per D-07 / RESEARCH Pattern 2).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix PK definitions in etl_tables.yaml</name>
  <files>config/etl_tables.yaml</files>
  <behavior>
    - After fix: grep for "umaban1" in etl_tables.yaml should return 0 matches in odds_sanren/sanrentan/umaren/wide entries
    - After fix: each of these 8 entries (n_ and s_ variants) should have pk ending with "kumi" instead of "umaban1, umaban2[, umaban3]"
    - n_odds_umatan and s_odds_umatan PK should also be checked and fixed if they use umaban1/umaban2
  </behavior>
  <action>Fix PK definitions in config/etl_tables.yaml per D-09 and D-10. Replace umaban1/umaban2/umaban3 with kumi in the pk list for these 8 table entries: n_odds_sanren (line 99), n_odds_sanrentan (line 111), n_odds_umaren (line 75), n_odds_wide (line 44), s_odds_sanren (line 424), s_odds_sanrentan (line 436), s_odds_umaren (line 400), s_odds_wide (line 370). Also check n_odds_umatan (line 87) and s_odds_umatan (line 412) — if they also use umaban1/umaban2, fix them to kumi as well (same JRA-VAN encoding pattern). The corrected pk should be [year, monthday, jyocd, kaiji, nichiji, racenum, kumi]. Do NOT modify _head tables — their pk is already correct (race-level only, no combination column). All 6 _head entries (odds_sanren_head, odds_umaren_head, odds_sanrentan_head for both n_ and s_) already have pk without umaban columns.</action>
  <verify>
    <automated>cd C:/Users/hirom/develop/keiba-ai && python -c "import yaml; c=yaml.safe_load(open('config/etl_tables.yaml')); targets=['odds_sanren','odds_sanrentan','odds_umaren','odds_wide']; entries=[t for t in c['tables'] if any(t['parquet_key']==x for x in targets)]; bad=[t for t in entries if any('umaban' in str(p) for p in t['pk'])]; assert len(bad)==0, f'Entries still with umaban in pk: {[t[\"db_table\"] for t in bad]}'; print(f'All {len(entries)} entries verified: pk uses kumi')"</automated>
  </verify>
  <done>All n_/s_ entries for odds_sanren, odds_sanrentan, odds_umaren, odds_wide (and odds_umatan if applicable) have pk ending with kumi. No umaban references remain in their pk fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add type conversion rules for 3 new odds tables in etl.py</name>
  <files>src/db/etl.py</files>
  <behavior>
    - _TABLE_TYPE_RULES dict should contain keys "odds_sanren", "odds_umaren", "odds_sanrentan"
    - Each entry should have "int": ["ninki"] and "odds10": ["odds"]
    - No entries needed for _head tables (they have no odds column)
  </behavior>
  <action>Add 3 new entries to _TABLE_TYPE_RULES dict in src/db/etl.py after the existing "odds_wide" entry (around line 106). Add entries for parquet keys "odds_sanren", "odds_umaren", "odds_sanrentan" per D-09/D-11. Each entry maps: "int": ["ninki"] (converts varchar ninki to int), "odds10": ["odds"] (converts varchar odds to float by dividing by 10). The odds10 divisor matches the entries table pattern per A1 in RESEARCH.md. Do NOT add entries for _head tables — they have no odds column and _apply_type_conversions returns df unchanged when no rule exists. Do NOT modify any other function in etl.py.</action>
  <verify>
    <automated>cd C:/Users/hirom/develop/keiba-ai && python -c "import sys; sys.path.insert(0,'src'); from db.etl import _TABLE_TYPE_RULES; keys=['odds_sanren','odds_umaren','odds_sanrentan']; missing=[k for k in keys if k not in _TABLE_TYPE_RULES]; assert not missing, f'Missing rules: {missing}'; [print(f'{k}: {_TABLE_TYPE_RULES[k]}') for k in keys]; assert all(_TABLE_TYPE_RULES[k].get('odds10')==['odds'] for k in keys), 'odds10 rule wrong'; assert all(_TABLE_TYPE_RULES[k].get('int')==['ninki'] for k in keys), 'int rule wrong'; print('All 3 type rules verified')"</automated>
  </verify>
  <done>_TABLE_TYPE_RULES contains "odds_sanren", "odds_umaren", "odds_sanrentan" with correct "int" and "odds10" rules. ruff check passes on src/db/etl.py.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| etl_tables.yaml -> etl.py | Config loaded at runtime; malformed PK could cause delta merge failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | S | etl_tables.yaml | accept | Static config file, not user-modifiable at runtime |
| T-29-02 | T | _TABLE_TYPE_RULES | accept | Type conversion is data-cleaning, not security boundary |
</threat_model>

<verification>
1. `python -c "import yaml; ..."` — verify no umaban in PK for target tables
2. `python -c "from db.etl import _TABLE_TYPE_RULES; ..."` — verify 3 new entries exist
3. `ruff check src/db/etl.py` — lint passes
4. `python -m pytest tests/ -v` — existing tests still pass (no behavior change)
</verification>

<success_criteria>
- All 8+ PK entries fixed from umaban1/2[/3] to kumi
- 3 new _TABLE_TYPE_RULES entries for odds_sanren, odds_umaren, odds_sanrentan
- ruff check passes
- Existing test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/29-etl-expansion/29-01-SUMMARY.md`
</output>
