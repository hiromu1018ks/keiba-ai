---
phase: 29-etl-expansion
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/run_etl.py
  - tests/test_etl_coverage.py
autonomous: true
requirements: [ETL-04]

must_haves:
  truths:
    - "run_etl.py runs coverage verification after ETL extraction completes"
    - "Coverage report logs row count, year coverage, and max missing rate per table"
    - "Missing years trigger a WARNING log; missing rate above 30% triggers a WARNING log"
    - "Coverage gracefully skips tables whose Parquet file does not exist"
  artifacts:
    - path: "scripts/run_etl.py"
      provides: "Coverage verification post-ETL"
      contains: "_verify_coverage"
    - path: "tests/test_etl_coverage.py"
      provides: "Mock-based tests for coverage verification"
      contains: "TestVerifyCoverage"
  key_links:
    - from: "scripts/run_etl.py"
      to: "src/db/parquet_store.py"
      via: "ParquetStore.exists() and .read() for coverage check"
      pattern: "_verify_coverage"
---

<objective>
Add post-ETL coverage verification to run_etl.py that reports row counts, year coverage (2015-2025), and missing rate for extracted tables.

Purpose: Validates that ETL extraction produced usable data within quality thresholds (D-04, D-05). Without coverage verification, there is no automated way to detect extraction failures or data gaps before downstream phases use the data.

Output: Updated scripts/run_etl.py with _verify_coverage function and tests/test_etl_coverage.py with mock-based tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-etl-expansion/29-CONTEXT.md
@.planning/phases/29-etl-expansion/29-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From scripts/run_etl.py lines 27-73 — current main() structure:
```python
def main() -> None:
    # ... argparse setup ...
    config = load_table_config()
    store = ParquetStore()
    db = DatabaseConnection()
    engine = db.get_engine()
    # ... ETL execution ...
    counts = run_full_load(store, engine, config, args.start, args.end, args.tables)
    # ... finally block with engine.dispose() ...
    elapsed = time.time() - t0
    logger.info("ETL完了 (%.0f秒)", elapsed)
    for table, n in counts.items():
        logger.info("  %s: %d行", table, n)
```

From src/db/parquet_store.py:
```python
class ParquetStore:
    def read(self, category: str, name: str, filters: list[tuple] | None = None) -> pd.DataFrame: ...
    def exists(self, category: str, name: str) -> bool: ...
```

From .planning/phases/29-etl-expansion/29-CONTEXT.md D-05:
  Verification criteria: 2015-2025 coverage, missing rate <= 30%

Target tables for coverage verification (6 tables):
  odds_sanren, odds_sanren_head, odds_umaren, odds_umaren_head,
  odds_sanrentan, odds_sanrentan_head
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add coverage verification to run_etl.py and create tests</name>
  <files>scripts/run_etl.py, tests/test_etl_coverage.py</files>
  <behavior>
    - Test: _verify_coverage with a mock store that returns a DataFrame with race_date covering 2015-2025 logs coverage info and no warnings
    - Test: _verify_coverage with a DataFrame missing some years logs a warning for missing years
    - Test: _verify_coverage with a DataFrame having >30% missing rate logs a warning
    - Test: _verify_coverage gracefully handles store.exists() returning False (skips with warning)
    - Test: _verify_coverage handles empty DataFrame without error
  </behavior>
  <action>Add a _verify_coverage function to scripts/run_etl.py per D-04 and D-05. The function signature: _verify_coverage(store: ParquetStore, tables: list[str], start_year: int, end_year: int) -> None. For each table in tables: (1) check store.exists("odds", table) — if False, log warning "Coverage SKIP: {table} (file not found)" and continue; (2) read the full DataFrame with store.read("odds", table); (3) compute row count (len(df)); (4) extract years from df["race_date"].dt.year.unique() if "race_date" column exists; (5) compute max missing rate across all columns (df.isnull().mean().max() * 100); (6) log "Coverage {key}: {n_rows} rows, years={years}, max_missing={pct}%"; (7) check that all years from start_year to end_year are present — if any missing, log warning "Coverage WARN: {key} missing years {sorted_missing}"; (8) check that max_missing <= 30 — if exceeded, log warning "Coverage WARN: {key} missing rate {pct}% exceeds 30%".

Then integrate _verify_coverage into main() after the row count logging loop. Call it only in full mode: after "for table, n in counts.items()" loop, add a call to _verify_coverage(store, list(counts.keys()), int(args.start[:4]), int(args.end[:4])). This verifies only the tables that were actually extracted.

Create tests/test_etl_coverage.py with mock-based tests. Use MagicMock for ParquetStore. Create test fixtures that return DataFrames with known year coverage and missing rates. Test: (1) full coverage case (all years, no missing) produces info log, (2) missing years case produces warning, (3) high missing rate case produces warning, (4) nonexistent file is skipped gracefully, (5) empty DataFrame handled without error. Use caplog fixture or mock the logger to verify log messages.</action>
  <verify>
    <automated>cd C:/Users/hirom/develop/keiba-ai && python -m pytest tests/test_etl_coverage.py -v</automated>
  </verify>
  <done>_verify_coverage function added to run_etl.py, called after ETL in full mode. Tests verify all 5 behavioral cases. ruff check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run_etl.py -> ParquetStore | Internal trusted boundary; coverage reads local files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04 | D | _verify_coverage | accept | Coverage is informational logging; no security-sensitive decisions made from it |
</threat_model>

<verification>
1. `python -m pytest tests/test_etl_coverage.py -v` — all coverage tests pass
2. `ruff check scripts/run_etl.py tests/test_etl_coverage.py` — lint passes
3. `python -m pytest tests/ -v` — full suite passes
</verification>

<success_criteria>
- _verify_coverage function exists in run_etl.py
- Coverage report logs row count, year range, and max missing rate per table
- Missing years and >30% missing rate trigger warnings
- Missing Parquet files are skipped gracefully
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/29-etl-expansion/29-03-SUMMARY.md`
</output>
