---
phase: 30-residual-ic-evaluation-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/ic_evaluator.py
  - tests/test_ic_evaluator.py
autonomous: true
requirements:
  - RIC-01
  - RIC-02
  - RIC-03
  - RIC-04
  - RIC-05
  - RIC-06

must_haves:
  truths:
    - "4種IC指標(B差分/C直交/E Incremental/Per-race)がOOF予測に対して計算され、dictとして返される"
    - "Surface別(turf/dirt)+全体の3パターンでIC値が計算される"
    - "方向一致性チェックが4指標の符号を検証し、矛盾時にWARNINGをログ出力する"
    - "IC評価結果がJSONファイルとして data/baseline/ic_baseline.json に保存される"
  artifacts:
    - path: "src/models/ic_evaluator.py"
      provides: "IC evaluation module with 4 IC formulations + consistency check + JSON output"
      exports: ["run_ic_evaluation", "console_summary", "_compute_b_difference_ic", "_compute_c_orthogonal_ic", "_compute_e_incremental_ic", "_compute_per_race_ic", "_check_direction_consistency"]
    - path: "tests/test_ic_evaluator.py"
      provides: "Unit tests for all IC computation functions"
      contains: "test_b_difference"
  key_links:
    - from: "src/models/ic_evaluator.py"
      to: "scipy.stats.spearmanr"
      via: "Spearman rank correlation computation"
      pattern: "from scipy.stats import spearmanr"
    - from: "src/models/ic_evaluator.py"
      to: "numpy.linalg.lstsq"
      via: "OLS residual computation for C-orthogonal IC"
      pattern: "numpy.linalg.lstsq"
---

<objective>
IC評価モジュール (src/models/ic_evaluator.py) とテスト (tests/test_ic_evaluator.py) を作成する。

Purpose: v1.6モデルの市場独立予測力を4定式化バッテリー(B差分IC/C直交IC/E Incremental IC/Per-race IC)で定量的に測定する評価フレームワークのコアモジュールを構築する。Phase 31/32で追加する新特徴量の効果を客観的に評価できる状態にする。

Output: src/models/ic_evaluator.py (IC評価モジュール), tests/test_ic_evaluator.py (テスト)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-residual-ic-evaluation-framework/30-CONTEXT.md
@.planning/phases/30-residual-ic-evaluation-framework/30-RESEARCH.md

<interfaces>
<!-- Key patterns and contracts the executor needs -->

From src/models/ev_diagnostics.py (pattern reference):
- Module-level constants: EV_PRED_COLUMN, EV_ACTUAL_COLUMN, MIN_SAMPLE_SIZE, N_BINS
- Private computation functions: _compute_ece(), _brier_decomposition()
- Public orchestration: compute_ev_diagnostics(df_oof, output_path, surface) -> dict
- Console output: console_summary(result) -> None
- Logger: logger = logging.getLogger("models.ev_diagnostics")
- JSON output: json.dump(result, f, indent=2, ensure_ascii=False)
- NaN-safe serialization: pd.to_numeric(..., errors="coerce").dropna()

From src/models/win_benter_gate.py (column names):
- Model prediction column: "p_win_corrected"
- Market odds column: "tanodds"
- Market probability: 1.0 / tanodds (clipped to [0.01, 0.99])
- Outcome column: "kakuteijyuni" (binary: 1 = win)
- Surface column: "surface"
- Race ID column: "race_id"

OOF DataFrame required columns (for IC evaluation):
- "p_win_corrected" or "p_win_pred" -- model prediction
- "tanodds" -- raw odds (fallback: compute implied_prob from 1/tanodds)
- "implied_prob" -- optional pre-computed market probability
- "kakuteijyuni" -- target variable (1 = win, 0 = lose)
- "surface" -- turf/dirt for surface-specific IC
- "race_id" -- race identifier for per-race IC
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create IC evaluation module with 4 formulations</name>
  <files>src/models/ic_evaluator.py</files>

  <read_first>
    src/models/ev_diagnostics.py (full file -- pattern reference for diagnostic module structure)
    src/models/drift_diagnostics.py (full file -- JSON output pattern with _json_default)
    .planning/phases/30-residual-ic-evaluation-framework/30-RESEARCH.md (code examples in Code Examples section)
    .planning/phases/30-residual-ic-evaluation-framework/30-CONTEXT.md (decisions D-01 through D-09)
  </read_first>

  <behavior>
    - Test 1: _compute_b_difference_ic -- known model_pred, market_prob, y arrays produce expected rho. Delta = model - market should correlate positively with y when model adds value beyond market.
    - Test 2: _compute_c_orthogonal_ic -- OLS residual of model on market correlates with y. With model = 0.5*market + 0.5*signal, C-orthogonal IC should be positive.
    - Test 3: _compute_e_incremental_ic -- IC(model, y) - IC(market, y). Returns dict with ic_model, ic_market, delta_ic.
    - Test 4: _compute_per_race_ic -- groupby race_id, compute Spearman within each race (min 5 horses), average. Returns mean_rho, n_races, skipped_races.
    - Test 5: _check_direction_consistency -- all 4 IC metrics positive returns consistent=True. Mixed signs returns consistent=False with warning.
    - Test 6: run_ic_evaluation with synthetic DataFrame returns all 4 IC metrics x 3 surface patterns (turf/dirt/all) = 12 IC values + consistency_check section.
    - Test 7: run_ic_evaluation writes JSON to output_path with correct structure.
    - Test 8: Fallback from missing implied_prob to 1/tanodds computation.
    - Test 9: MIN_SAMPLE_SIZE threshold returns NaN IC values with n < threshold.
    - Test 10: console_summary logs without error for valid result dict.
  </behavior>

  <action>
    Create src/models/ic_evaluator.py following the diagnostic module pattern from ev_diagnostics.py exactly.

    Module structure per D-01, D-02:
    - Module-level logger: logging.getLogger("models.ic_evaluator")
    - Module-level constants: MIN_SAMPLE_SIZE = 30, IC_TARGET_COLUMN = "kakuteijyuni", MODEL_PRED_COLUMN = "p_win_corrected", MARKET_ODDS_COLUMN = "tanodds", MIN_HORSES_PER_RACE = 5

    Private computation functions (per RESEARCH.md Code Examples):
    - _compute_b_difference_ic(model_pred: np.ndarray, market_prob: np.ndarray, y: np.ndarray) -> dict -- RIC-01. Computes delta = model_pred - market_prob, then Spearman(delta, y). Returns {"rho": float, "p_value": float, "n": int}. Pre-filter NaN with np.isfinite before spearmanr. Return NaN values if n < MIN_SAMPLE_SIZE.
    - _compute_c_orthogonal_ic(model_pred: np.ndarray, market_prob: np.ndarray, y: np.ndarray) -> dict -- RIC-02. OLS regression: model_pred = beta * market_prob + intercept + residual. Use numpy.linalg.lstsq with rcond=None. x_with_intercept = np.column_stack([np.ones(len(x)), x]). Compute Spearman(residuals, y). Returns {"rho": float, "p_value": float, "n": int}.
    - _compute_e_incremental_ic(model_pred: np.ndarray, market_prob: np.ndarray, y: np.ndarray) -> dict -- RIC-03. IC(model, y) - IC(market, y). Returns {"ic_model": float, "ic_market": float, "delta_ic": float, "n": int}.
    - _compute_per_race_ic(df: pd.DataFrame, pred_col: str, y_col: str, group_col: str = "race_id", min_horses: int = MIN_HORSES_PER_RACE) -> dict -- RIC-04. Group by race_id, compute Spearman within each race, average. Returns {"mean_rho": float, "std_rho": float, "median_rho": float, "n_races": int, "skipped_races": int}.
    - _check_direction_consistency(ic_results: dict) -> dict -- RIC-06. Extract rho/delta_ic/mean_rho from each of the 4 IC result dicts. Check all non-zero values have same sign. Returns {"consistent": bool, "n_metrics_checked": int, "details": dict}. If inconsistent, include "warning" key with message. Log WARNING via logger if inconsistent per D-08.
    - _json_default(obj) -- NaN-safe serializer following drift_diagnostics.py pattern. Handle np.integer, np.floating, np.ndarray, pd.Timestamp.

    Market probability extraction per D-05:
    - _get_market_probability(df: pd.DataFrame) -> np.ndarray. Check for "implied_prob" column first. If missing, compute from 1.0 / df["tanodds"].replace(0, np.nan) and clip to [0.01, 0.99] following win_benter_gate.py extract_market_probability pattern.

    Public orchestration function per D-09, D-10:
    - run_ic_evaluation(df_oof: pd.DataFrame, output_path: Path | None = None, mlflow_log: bool = False) -> dict. Main entry point.
      1. Validate required columns exist in df_oof (p_win_corrected or p_win_pred, tanodds or implied_prob, kakuteijyuni, surface, race_id).
      2. Extract model_pred from p_win_corrected (fallback p_win_pred), market_prob via _get_market_probability, y from kakuteijyuni (binary: 1 = win == 1, 0 otherwise).
      3. For each surface filter (turf, dirt, all): compute all 4 IC metrics. Store in results dict under keys "turf", "dirt", "all".
      4. Run _check_direction_consistency on each surface's results. Store in results["consistency_check"].
      5. Add metadata: timestamp (datetime.now().isoformat()), n_total, n_valid.
      6. If output_path is not None: write JSON via json.dump with _json_default. Create parent directories. Path: data/baseline/ic_baseline.json per D-06.
      7. If mlflow_log is True: log metrics (ic_b_diff_rho, ic_c_orth_rho, ic_e_incr_delta, ic_per_race_mean) and tag consistency status per D-06. Use mlflow.log_metric and mlflow.set_tag.
      8. Return results dict.

    Console summary per D-10:
    - console_summary(result: dict) -> None. Log IC values for each surface pattern and consistency check status. Follow ev_diagnostics.py console_summary pattern with logger.info calls.

    Import only: numpy, pandas, scipy.stats.spearmanr, json, logging, pathlib.Path. No new external dependencies.
  </action>

  <verify>
    <automated>cd C:\Users\hirom\develop\keiba-ai && python -m pytest tests/test_ic_evaluator.py -v</automated>
  </verify>

  <done>
    - src/models/ic_evaluator.py exists with 4 IC computation functions + consistency check + run_ic_evaluation + console_summary
    - All 10 test cases pass
    - Module follows ev_diagnostics.py pattern (module-level constants, private computation functions, public orchestration, JSON output, console_summary)
    - No new external dependencies imported
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OOF Parquet → ic_evaluator | File read boundary; malformed data could cause NaN propagation |
| ic_evaluator → JSON output | File write boundary; path traversal risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | OOF input data | accept | OOF data generated by local pipeline; no external input |
| T-30-02 | Information Disclosure | ic_baseline.json | accept | Contains only aggregate IC metrics; no PII or secrets |
| T-30-SC | Tampering | pip installs | mitigate | No new packages installed in this phase — all pre-existing |
</threat_model>

<verification>
python -m pytest tests/test_ic_evaluator.py -v --tb=short
ruff check src/models/ic_evaluator.py
mypy src/models/ic_evaluator.py
</verification>

<success_criteria>
- 4 IC formulations (B差分/C直交/E Incremental/Per-race) compute correct values on synthetic data
- Surface-specific (turf/dirt) + overall (all) IC computed for each formulation
- Direction consistency check detects sign contradictions
- JSON output written to data/baseline/ic_baseline.json
- All tests pass, ruff check clean, mypy clean
</success_criteria>

<output>
Create `.planning/phases/30-residual-ic-evaluation-framework/30-01-SUMMARY.md` when done
</output>
