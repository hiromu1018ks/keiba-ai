---
phase: 30-residual-ic-evaluation-framework
plan: 02
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - src/pipelines/training_pipeline.py
  - scripts/run_ic_eval.py
autonomous: true
requirements:
  - RIC-05

must_haves:
  truths:
    - "TrainingPipelineの学習完了後にOOF予測DataFrameがdata/oof/oof_predictions.parquetに保存される"
    - "CLIスクリプトがOOF Parquetパスを受け取りIC評価を実行し、JSONを出力する"
    - "再トレーニングなしで保存済みOOF ParquetからIC評価を再実行できる"
  artifacts:
    - path: "scripts/run_ic_eval.py"
      provides: "CLI entry point for offline IC evaluation"
      contains: "argparse"
    - path: "data/oof/oof_predictions.parquet"
      provides: "OOF prediction persistence for offline evaluation"
    - path: "data/baseline/ic_baseline.json"
      provides: "IC baseline values for v1.6 model"
  key_links:
    - from: "src/pipelines/training_pipeline.py"
      to: "data/oof/oof_predictions.parquet"
      via: "OOF save hook after pd.concat(oof_dfs)"
      pattern: "to_parquet"
    - from: "scripts/run_ic_eval.py"
      to: "src/models/ic_evaluator.py"
      via: "from models.ic_evaluator import run_ic_evaluation, console_summary"
      pattern: "run_ic_evaluation"
---

<objective>
TrainingPipelineにOOF予測保存フックを追加し、CLIスクリプトでオフラインIC評価を実行できるようにする。

Purpose: 再トレーニングなしでIC評価を再実行可能にする(D-04)。TrainingPipeline統合(D-10, D-11)とCLIアクセスパス(D-10)の2つのエントリポイントを提供する。

Output: 修正済みtraining_pipeline.py (OOF保存フック), 新規scripts/run_ic_eval.py (CLIスクリプト)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-residual-ic-evaluation-framework/30-CONTEXT.md
@.planning/phases/30-residual-ic-evaluation-framework/30-RESEARCH.md
@.planning/phases/30-residual-ic-evaluation-framework/30-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output: ic_evaluator.py exports -->

From src/models/ic_evaluator.py (created in Plan 01):
- run_ic_evaluation(df_oof: pd.DataFrame, output_path: Path | None = None, mlflow_log: bool = False) -> dict
- console_summary(result: dict) -> None

From src/pipelines/training_pipeline.py:
- Line 243-249: After pd.concat(oof_dfs), save_features() is called. OOF save hook should be added HERE (after line 249).
- Line 958: df_oof_for_save = df_oof.copy() -- per-surface OOF copy in _train_submodel(). NOT the right save point for combined OOF.
- Line 244: full_features_df = pd.concat(oof_dfs, ignore_index=True) -- this IS the combined OOF DataFrame with all surfaces.
- Line 245: save_features(self.store, full_features_df) -- saves to data/features/horse_features.parquet.
- Required columns in full_features_df: p_win_corrected, tanodds, kakuteijyuni, surface, race_id (verified present at line 244).

From existing scripts (pattern reference):
- scripts/run_etl.py -- argparse pattern, logging setup, Path handling
- scripts/run_train.py -- MLflow integration pattern
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OOF Parquet save hook to TrainingPipeline</name>
  <files>src/pipelines/training_pipeline.py</files>

  <read_first>
    src/pipelines/training_pipeline.py (lines 240-260 for save hook location)
    src/pipelines/training_pipeline.py (lines 1-55 for imports and logger)
    .planning/phases/30-residual-ic-evaluation-framework/30-CONTEXT.md (D-03, D-04, D-10, D-11)
  </read_first>

  <action>
    Add OOF Parquet save hook to TrainingPipeline.run() method, at the point after line 249 where full_features_df is already saved to features parquet.

    Specific changes:
    1. After line 249 (logger.info "Saved full feature set..."), add OOF Parquet save:
       - Create oof_path = Path("data/oof/oof_predictions.parquet") per D-03.
       - Create parent directory: oof_path.parent.mkdir(parents=True, exist_ok=True).
       - Save full_features_df to Parquet: full_features_df.to_parquet(oof_path, index=False).
       - Log: logger.info("Saved OOF predictions: %d rows -> %s", len(full_features_df), oof_path).

    2. The save must be inside the existing `if oof_dfs:` block (line 243) since full_features_df only exists there.

    3. Do NOT modify _train_submodel() or any model training logic. The save is purely additive.

    4. Import Path at module level -- pathlib.Path is already available in the module's existing imports (verify first; if not present, add to imports).

    The saved Parquet will contain all columns from full_features_df including: p_win_corrected, tanodds, kakuteijyuni, surface, race_id, race_date, and all feature columns. This enables offline IC evaluation without retraining per D-04.
  </action>

  <verify>
    <automated>cd C:\Users\hirom\develop\keiba-ai && python -c "from pipelines.training_pipeline import TrainingPipeline; print('Import OK')" && python -m pytest tests/ -v -k "not test_training_pipeline" --co -q 2>&1 | head -5</automated>
  </verify>

  <done>
    - training_pipeline.py contains "data/oof/oof_predictions.parquet" string
    - full_features_df.to_parquet is called after save_features()
    - Import succeeds without error
    - Existing tests still pass (excluding known-failing test_training_pipeline.py tests)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CLI script for offline IC evaluation</name>
  <files>scripts/run_ic_eval.py</files>

  <read_first>
    scripts/run_etl.py (full file -- argparse and logging pattern reference)
    scripts/run_train.py (full file -- MLflow integration pattern)
    src/models/ic_evaluator.py (created in Plan 01 -- exports: run_ic_evaluation, console_summary)
    .planning/phases/30-residual-ic-evaluation-framework/30-CONTEXT.md (D-06, D-10, D-11)
  </read_first>

  <action>
    Create scripts/run_ic_eval.py as a thin CLI wrapper per D-10, D-11.

    CLI arguments via argparse:
    - --oof-path: Required positional argument. Path to OOF predictions Parquet file (e.g., data/oof/oof_predictions.parquet).
    - --output: Optional. Path for JSON output. Default: data/baseline/ic_baseline.json per D-06.
    - --mlflow: Optional flag. Enable MLflow logging for IC baseline. Default: False.
    - --experiment: Optional. MLflow experiment name. Default: "keiba-v5-ic-eval".

    Script flow:
    1. Parse arguments.
    2. Configure logging: logging.basicConfig(level=logging.INFO, format="%(name)s - %(levelname)s - %(message)").
    3. Load OOF Parquet: df_oof = pd.read_parquet(args.oof_path). Log row/column count.
    4. If --mlflow: mlflow.set_experiment(args.experiment), start mlflow.start_run(run_name="ic_baseline_eval").
    5. Call result = run_ic_evaluation(df_oof, output_path=Path(args.output), mlflow_log=args.mlflow).
    6. Call console_summary(result).
    7. If --mlflow: mlflow.end_run().
    8. Log completion: "IC evaluation complete. Output: {args.output}".

    Follow run_etl.py pattern for:
    - sys.path.insert(0, "src") at top (if needed for imports).
    - if __name__ == "__main__": guard.
    - from models.ic_evaluator import run_ic_evaluation, console_summary.

    Python shebang: #!/usr/bin/env python3.
    Docstring: "OOF予測のIC評価を実行するCLIスクリプト (Phase 30)."

    Make the script runnable: python scripts/run_ic_eval.py data/oof/oof_predictions.parquet --output data/baseline/ic_baseline.json
  </action>

  <verify>
    <automated>cd C:\Users\hirom\develop\keiba-ai && python -c "import ast; ast.parse(open('scripts/run_ic_eval.py').read()); print('Syntax OK')"</automated>
  </verify>

  <done>
    - scripts/run_ic_eval.py exists with argparse CLI accepting --oof-path, --output, --mlflow, --experiment
    - Script imports run_ic_evaluation and console_summary from models.ic_evaluator
    - Script parses without syntax error
    - Default output path is data/baseline/ic_baseline.json
    - --mlflow flag enables MLflow logging
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system → run_ic_eval.py | Path argument could point to arbitrary files |
| run_ic_eval.py → MLflow | Local MLflow tracking server connection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | Tampering | --oof-path argument | accept | Local script, user-controlled input; no external attack surface |
| T-30-04 | Information Disclosure | MLflow metrics | accept | Local MLflow server; IC metrics are not sensitive |
| T-30-SC | Tampering | pip installs | mitigate | No new packages in this plan |
</threat_model>

<verification>
python -c "import ast; ast.parse(open('scripts/run_ic_eval.py').read()); print('OK')"
python -c "from pipelines.training_pipeline import TrainingPipeline; print('Pipeline import OK')"
python -m pytest tests/test_ic_evaluator.py -v --tb=short
</verification>

<success_criteria>
- TrainingPipeline.run() saves OOF predictions to data/oof/oof_predictions.parquet after feature save
- scripts/run_ic_eval.py accepts OOF Parquet path and runs IC evaluation
- JSON output written to data/baseline/ic_baseline.json with default settings
- --mlflow flag enables MLflow experiment tracking
- All existing tests pass (excluding known-failing test_training_pipeline.py)
- No modification to model training logic in training_pipeline.py
</success_criteria>

<output>
Create `.planning/phases/30-residual-ic-evaluation-framework/30-02-SUMMARY.md` when done
</output>
