---
phase: 31-race-level-aggregation-features
plan: 02
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - src/features/feature_engine.py
  - tests/test_post_race_leakage.py
  - scripts/freeze_feature_manifest.py
autonomous: true
requirements:
  - RLF-07
  - EFP-03

must_haves:
  truths:
    - "build_all() で compute_race_level_features() が呼び出され rl_* 列が出力に含まれる"
    - "build_features() で compute_race_level_features() が呼び出され rl_* 列が出力に含まれる"
    - "rl_* 特徴量が POST_RACE 情報漏洩テストで安全であることが確認される"
    - "feature_freeze_manifest.json が更新され全12モデルのSHA256が再計算される"
  artifacts:
    - path: "src/features/feature_engine.py"
      provides: "build_all() と build_features() からの compute_race_level_features() 呼び出し"
      contains: "compute_race_level_features"
    - path: "tests/test_post_race_leakage.py"
      provides: "rl_* 特徴量のPOST_RACE安全性検証拡張"
      contains: "rl_"
    - path: "data/feature_freeze_manifest.json"
      provides: "更新された特徴量凍結manifest"
  key_links:
    - from: "src/features/feature_engine.py"
      to: "src/features/race_level_features.py"
      via: "import + 呼び出し"
      pattern: "from features.race_level_features import compute_race_level_features"
    - from: "src/features/feature_engine.py::build_all()"
      to: "compute_race_level_features()"
      via: "TimingContext付き呼び出し (difficulty_scoreの後)"
      pattern: "TimingContext.*race_level"
    - from: "src/features/feature_engine.py::build_features()"
      to: "compute_race_level_features()"
      via: "_map_basic_features() の後の呼び出し"
      pattern: "compute_race_level_features"
---

<objective>
feature_engineへの統合とテスト検証・manifest更新

Purpose: Plan 01で作成したrace_level_featuresモジュールをfeature_engineの両パス(build_all/build_features)に統合し、POST_RACE安全性を検証し、manifestを更新する。これによりtrain/inference両パスで同じrace-level特徴量が計算される (RLF-07)。

Output: feature_engine.py更新、テスト拡張、feature_freeze_manifest.json更新
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-race-level-aggregation-features/31-CONTEXT.md
@.planning/phases/31-race-level-aggregation-features/31-01-SUMMARY.md

<interfaces>
<!-- Plan 01で作成されたモジュール -->
From src/features/race_level_features.py (created in Plan 01):
```python
def compute_race_level_features(df: pd.DataFrame) -> pd.DataFrame:
    """レース構造特徴量を計算 (RLF-01~06)

    Args:
        df: race_id, tanodds, field_size を含むDataFrame

    Returns:
        rl_* 列が追加されたDataFrame
    """
```

<!-- feature_engine.py のbuild_all()統合ポイント -->
From src/features/feature_engine.py:
```python
# line 340-343: 既存のdifficulty_score呼び出し (この直後に追加)
from features.race_difficulty_model import compute_difficulty_score
with TimingContext("build_all/difficulty"):
    result_df = compute_difficulty_score(result_df)

# line 450-455: build_features()の_map_basic_features()呼び出し (この直後に追加)
df = self._map_basic_features(df)
# 6. サブモジュールの特徴量計算（推論用 — hist特徴量は除く）
return df
```

<!-- テストパターン -->
From tests/test_post_race_leakage.py:
```python
def test_build_all_output_no_post_race_cols(self) -> None:
    # Layer 1: build_all出力のPOST_RACE列チェック
    post_race_in_output = set(result.columns) & set(POST_RACE_COLS)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate compute_race_level_features into build_all() and build_features()</name>
  <files>src/features/feature_engine.py</files>
  <read_first>
    src/features/feature_engine.py lines 318-383 (build_all サブモジュール呼び出しセクション + SAFE-01)
    src/features/feature_engine.py lines 385-455 (build_features 全体)
    src/features/race_level_features.py (Plan 01で作成、compute_race_level_featuresのシグネチャ確認)
  </read_first>
  <action>
    feature_engine.py に compute_race_level_features() の呼び出しを追加 (per D-04, D-05, RLF-07)。

    build_all() への統合 (line 343 の直後、bloodline_features の前):
    1. import文を追加: `from features.race_level_features import compute_race_level_features`
    2. TimingContext付きで呼び出し:
       ```
       with TimingContext("build_all/race_level"):
           result_df = compute_race_level_features(result_df)
       ```
    3. 挿入位置: compute_difficulty_score() の直後 (line 343の後)、Group B bloodline_featuresの前 (line 346の前)
    4. この位置はSAFE-01 (POST_RACE stripping, lines 362-371) の前であるため、rl_*列もPOST_RACEチェックの対象となる

    build_features() へのパリティ統合 (line 451 の直後):
    1. 同じimport文を追加 (または関数先頭で一括import)
    2. _map_basic_features() の直後に呼び出し:
       `df = compute_race_level_features(df)`
    3. TimingContextなし (build_featuresは単レース推論でオーバーヘッド不要)
    4. コメント "# 6b. レース構造特徴量 (RLF-07 parity)" を追加

    注意: build_features()はrace_id列を含む単一レースDataFrameを処理するが、compute_race_level_features()はPlan 01でrace_idなしでも動作する設計になっている。どちらのパスでも同じ関数を呼び出す (per D-04推奨)。
  </action>
  <verify>
    <automated>python -c "from features.feature_engine import FeatureEngine; import inspect; src = inspect.getsource(FeatureEngine.build_all); src2 = inspect.getsource(FeatureEngine.build_features); assert 'compute_race_level_features' in src, 'build_all missing race_level call'; assert 'compute_race_level_features' in src2, 'build_features missing race_level call'; assert 'TimingContext' in src.split('race_level')[0].split('race_difficulty')[1] or 'race_level' in src, 'timing context check'; print('OK: both build_all and build_features call compute_race_level_features')"</automated>
  </verify>
  <acceptance_criteria>
    - build_all() の compute_difficulty_score() 直後に compute_race_level_features() が TimingContext 付きで呼び出される
    - build_features() の _map_basic_features() 直後に compute_race_level_features() が呼び出される
    - 既存テストが通過する: python -m pytest tests/test_domain.py -v
  </acceptance_criteria>
  <done>
    - build_all() と build_features() の両方で compute_race_level_features() が呼び出される
    - 既存テストが通過する
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend POST_RACE leakage test and regenerate feature manifest</name>
  <files>tests/test_post_race_leakage.py, data/feature_freeze_manifest.json</files>
  <read_first>
    tests/test_post_race_leakage.py (全文 — 3層テストアーキテクチャ)
    src/features/race_level_features.py (Plan 01で作成)
    scripts/freeze_feature_manifest.py (manifest生成スクリプト)
  </read_first>
  <action>
    POST_RACE漏洩テストの拡張とmanifest再生成 (EFP-03)。

    tests/test_post_race_leakage.py にテストを追加:
    1. 新規テストクラス TestRaceLevelFeatures を追加
    2. test_race_level_features_no_post_race_input: compute_race_level_features() のソースコードに POST_RACE_COLS に含まれる列名("kakuteijyuni", "confirmed_odds", "ninki" 等)が直接参照されていないことを検証。import ast でソースをパースし、文字列リテラルを確認
    3. test_rl_feature_cols_not_in_post_race: 6つの rl_* 列名が POST_RACE_COLS に含まれないことを検証 (自明だが二重チェック)
    4. test_build_all_produces_rl_features: mockデータで build_all() を実行し、出力に rl_log_odds_entropy, rl_odds_dispersion, rl_top3_odds_gap, rl_top1_odds, rl_favorite_rank_gap, rl_n_horses が含まれることを検証。_make_race_df, _make_entry_df, _make_odds_df ヘルパーを再利用

    scripts/freeze_feature_manifest.py の再実行:
    1. スクリプトに変更不要 (コード変更不要 per D-07)
    2. python scripts/freeze_feature_manifest.py を実行して data/feature_freeze_manifest.json を再生成
    3. 全12モデルのfeature_countが更新されていることを確認 (AbilityModel: +2, MarketModel: +2, RegimeDetector: +2等)

    注意: freeze_feature_manifest.py は既に12モデルのFEATURE_COLSを収集する設計になっているため、Plan 01でFEATURE_COLSが更新されていれば再実行だけで正しいmanifestが生成される。
  </action>
  <verify>
    <automated>python -m pytest tests/test_post_race_leakage.py -v && python scripts/freeze_feature_manifest.py</automated>
  </verify>
  <acceptance_criteria>
    - test_race_level_features_no_post_race_input が通過する (POST_RACE列の非使用確認)
    - test_build_all_produces_rl_features が通過する (6つのrl_*列がbuild_all出力に含まれる)
    - data/feature_freeze_manifest.json が再生成され、各モデルのfeature_countが更新されている
    - manifest内のAbilityModel feature_count が元の値+2になっている
  </acceptance_criteria>
  <done>
    - POST_RACE漏洩テストにrl_*特徴量の検証が追加され全テストが通過する
    - feature_freeze_manifest.json が更新され全12モデルのSHA256が再計算されている
  </done>
</task>

</tasks>

<verification>
python -m pytest tests/test_post_race_leakage.py tests/test_race_level_features.py -v
</verification>

<success_criteria>
- build_all() と build_features() の両方で rl_* 特徴量が計算される
- POST_RACE情報漏洩テストが全て通過する (Layer 1/2/3 + rl_* 追加テスト)
- feature_freeze_manifest.json が更新され全12モデルの正しいSHA256が記録されている
</success_criteria>

<output>
Create `.planning/phases/31-race-level-aggregation-features/31-02-SUMMARY.md` when done
</output>
