---
phase: 32-market-cross-consistency-features
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/features/market_cross_features.py
  - src/db/repository.py
  - tests/test_market_cross_features.py
autonomous: true
requirements: [MCF-01, MCF-02, MCF-03, MCF-04, MCF-05, MCF-06]

must_haves:
  truths:
    - "Harville公式でワイド理論確率が計算され、実オッズ/理論オッズ比率が生成される"
    - "Harville公式で三連複理論確率が計算され、実オッズ/理論オッズ比率が生成される"
    - "1番人気がワイドninki=1組合せに含まれるかの0/1フラグが生成される"
    - "三連複ninki=1組合せと単勝上位3頭のオーバーラップ数(0-3)が生成される"
    - "1番人気が三連複ninki=1組合せに含まれるかの0/1フラグが生成される"
    - "全特徴量はpre-race tanoddsのみを使用しPOST_RACE列を一切参照しない"
    - "wide_df/trio_dfがNoneの場合は全MCF列がNaNとなる"
  artifacts:
    - path: "src/features/market_cross_features.py"
      provides: "compute_market_cross_features()とMCF_COLS"
      exports: ["compute_market_cross_features", "MCF_COLS"]
    - path: "src/db/repository.py"
      provides: "DataRepository.load_wide_odds()メソッド"
      contains: "def load_wide_odds"
    - path: "tests/test_market_cross_features.py"
      provides: "market_cross_featuresのユニットテスト"
      min_lines: 50
  key_links:
    - from: "src/features/market_cross_features.py"
      to: "src/db/repository.py"
      via: "wide_df/trio_dfをcompute_market_cross_features()に渡す"
      pattern: "compute_market_cross_features.*wide_df.*trio_df"
---

<objective>
Market Cross-Consistency Featuresのコア計算モジュールとデータアクセス層を新規作成する。

Purpose: Harville理論オッズによる馬券種クロス整合性特徴量の計算エンジンを構築し、DataRepository経由でワイドオッズデータにアクセスできるようにする (D-05, D-08, D-10)
Output: market_cross_features.py (5特徴量の計算), repository.py (load_wide_odds), テストスイート
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-market-cross-consistency-features/32-CONTEXT.md
@.planning/phases/32-market-cross-consistency-features/32-RESEARCH.md
@.planning/phases/31-race-level-aggregation-features/31-01-SUMMARY.md
@src/features/race_level_features.py
@src/db/repository.py
@src/db/readers.py

<interfaces>
<!-- Executorが使用する既存インターフェース -->

From src/db/repository.py:
```python
class DataRepository:
    def __init__(self, store: ParquetStore | None = None) -> None: ...
    def load_trio_odds(self, start: str, end: str) -> pd.DataFrame: ...
    def load_exacta_odds(self, start: str, end: str) -> pd.DataFrame: ...
    def load_trifecta_odds(self, start: str, end: str) -> pd.DataFrame: ...
```

From src/db/readers.py (line 251):
```python
def load_wide_odds(store: ParquetStore, start: str, end: str) -> pd.DataFrame:
    df = store.read("odds", "odds_wide", filters=date_filters(start, end))
    return coerce_types(df)
```

From src/features/race_level_features.py (pattern template):
```python
RL_COLS: list[str] = [...]
def compute_race_level_features(df: pd.DataFrame) -> pd.DataFrame: ...
def _compute_for_single_race(df, tanodds) -> pd.DataFrame: ...
def _compute_for_multi_race(df, tanodds) -> pd.DataFrame: ...
```

Wide odds schema (data/odds/odds_wide.parquet):
- kumi: str (4-digit, "0102"), oddslow: float64, oddshigh: float64, ninki: str ("001")
- race_id, umaban1, umaban2 (may not exist -- kumi parse required)

Trio odds schema (data/odds/odds_sanren.parquet):
- kumi: str (6-digit, "010203"), odds: float64, ninki: Int64
- race_id
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: market_cross_features.py + DataRepository.load_wide_odds() (TDD)</name>
  <files>src/features/market_cross_features.py, src/db/repository.py, tests/test_market_cross_features.py</files>
  <read_first>
    src/features/race_level_features.py
    src/db/repository.py
    src/db/readers.py (lines 251-253 for load_wide_odds reference)
    .planning/phases/32-market-cross-consistency-features/32-RESEARCH.md (Code Examples section)
  </read_first>
  <behavior>
    - Test 1: 単一レース (race_idなし) でwide_df/trio_dfがNoneの場合、5つのMCF列が全てNaNとなる
    - Test 2: 単一レースでwide_df/trio_dfが空DataFrameの場合、5つのMCF列が全てNaNとなる
    - Test 3: 複数レース (race_idあり) で正常なwide_df/trio_dfが与えられた場合:
      - rl_favorite_in_wide_top1: 1番人気がワイドTOP1(ninki=1)組合せに含まれるなら1、そうでないなら0
      - rl_trio_overlap: 三連複ninki=1のkumi構成馬と単勝上位3頭のオーバーラップ数(0-3)
      - rl_market_consistency: 1番人気が三連複ninki=1組合せに含まれるなら1、そうでないなら0
    - Test 4: Harville理論オッズ比率:
      - rl_wide_harville_ratio: 実ワイドninki=1中間オッズ / Harville理論ワイドオッズ
      - rl_trio_odds_ratio: 実三連複ninki=1オッズ / Harville理論三連複オッズ
    - Test 5: 少頭数(2頭)レースのエッジケースで例外が発生せずNaNとなる
    - Test 6: P(i)が1.0に近い極端な本命レースでもdivision-by-zeroが発生しない (epsilon=1e-10保護)
    - Test 7: ninki型違い(文字列"001"と整数1)の両方でninki=1フィルタが動作する
    - Test 8: DataRepository.load_wide_odds()が呼び出し可能で正しいParquetを読み込む (mock検証)
  </behavior>
  <action>
    RED: テストファイル tests/test_market_cross_features.py を作成。上記8テストケースを記述。実行して全テストが失敗(ModuleNotFoundError/ImportError)することを確認。

    GREEN: src/features/market_cross_features.py を新規作成 (D-10)。

    モジュール構成 (race_level_features.pyパターンを踏襲):
    - MCF_COLS: 5特徴量名のリスト
    - _parse_wide_kumi(kumi: str) -> tuple[int, int]: 4桁文字列を馬番ペアに変換
    - _parse_trio_kumi(kumi: str) -> tuple[int, int, int]: 6桁文字列を馬番トリプルに変換
    - _harville_wide_prob(p_i, p_j) -> float: D-08公式。両順序の和。epsilon=1e-10で分母保護
    - _harville_trio_prob(p_i, p_j, p_k) -> float: D-08公式。6順列の和。epsilon=1e-10で分母保護
    - _compute_for_single_race(df, tanodds, wide_race, trio_race) -> DataFrame
    - _compute_for_multi_race(df, tanodds, wide_df, trio_df) -> DataFrame
    - compute_market_cross_features(df, wide_df=None, trio_df=None) -> DataFrame: メインエントリポイント

    実装の要点:
    - wide_df/trio_dfがNoneまたは空の場合、全MCF列をNaNで初期化して返す (D-06)
    - ninki正規化: pd.to_numeric(df["ninki"], errors="coerce")後==1でフィルタ (Pitfall 3対策)
    - ワイドオッズ中間値: (oddslow + oddshigh) / 2 (D-01)
    - 人気順位はtanoddsから計算 (POST_RACE安全)。feature_engine.pyの_compute_popularity_rank_from_tanoddsパターンと同じ方法
    - Harville計算のP(i)はtanoddsから: inv_odds = 1/tanodds, p_i = inv_odds / sum(inv_odds) per race
    - kumi文字列とumabanの照合でhorse番号を特定
    - レース内全馬にブロードキャスト (groupby("race_id")でレース単位計算後、transform/mapで全行に反映)
    - df.copy()で入力保護

    DataRepository.load_wide_odds()をsrc/db/repository.pyに追加 (D-05):
    - readers.pyのload_wide_odds()と同じパターン: store.read("odds", "odds_wide", filters=date_filters(start, end)), return coerce_types(df)

    REFACTOR: 不要 (実装直後はクリーン)。
  </action>
  <verify>
    <automated>cd C:\Users\hirom\develop\keiba-ai && python -m pytest tests/test_market_cross_features.py -v</automated>
  </verify>
  <done>
    - 8テスト全て通過
    - compute_market_cross_features()が5つのrl_*列を返す
    - Harville公式が両順序(ワイド)と6順列(三連複)を正しく計算
    - division-by-zero保護(epsilon)が動作
    - wide_df/trio_df=NoneでNaNフォールバック
    - POST_RACE列を一切参照しない
    - DataRepository.load_wide_odds()が呼び出し可能
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parquet -> compute | 外部Parquetファイルからのデータ入力。kumi文字列の不正値・欠損が想定される |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | kumi文字列パース | mitigate | int()変換のValueErrorをキャッチ、NaNでフォールバック |
| T-32-02 | Denial of Service | Harville division-by-zero | mitigate | epsilon=1e-10で分母をクランプ (D-08) |
| T-32-03 | Information Disclosure | POST_RACE情報漏洩 | mitigate | tanoddsのみ使用、AST source scanで検証 (Plan 02) |
| T-32-SC | Tampering | pip install | accept | 新規パッケージインストールなし |
</threat_model>

<verification>
- python -m pytest tests/test_market_cross_features.py -v 全テスト通過
- python -c "from features.market_cross_features import compute_market_cross_features, MCF_COLS; print(len(MCF_COLS))" で5を出力
- python -c "from db.repository import DataRepository; print(hasattr(DataRepository, 'load_wide_odds'))" でTrueを出力
</verification>

<success_criteria>
- market_cross_features.pyが存在し、compute_market_cross_features()がMCF_COLSの5列を返す
- Harville公式がワイド(両順序和)と三連複(6順列和)を正しく計算する
- DataRepository.load_wide_odds()が追加されている
- 全8テストが通過する
- POST_RACE列を一切参照しない (ソースコード検査)
</success_criteria>

<output>
Create `.planning/phases/32-market-cross-consistency-features/32-01-SUMMARY.md` when done
</output>
