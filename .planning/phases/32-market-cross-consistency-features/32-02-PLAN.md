---
phase: 32-market-cross-consistency-features
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - src/features/feature_engine.py
  - src/models/stage1_ability_model.py
  - src/models/market_model.py
  - src/models/regime_detector.py
  - src/models/place_ability_model.py
  - src/models/race_quality_screener.py
  - src/models/wide_two_stage_model.py
  - src/models/two_stage_return_model.py
  - src/models/ev_correction_model.py
  - src/models/conformal_ev_model.py
  - tests/test_post_race_leakage.py
autonomous: true
requirements: [MCF-07]

must_haves:
  truths:
    - "build_all()がwide/trioオッズをDataRepositoryからロードし、compute_market_cross_features()に渡す"
    - "build_features()でもmarket_cross_featuresが計算される (wide_df/trio_dfなしでNaNフォールバック)"
    - "全12モデルのFEATURE_COLSに5つのMCF特徴量が追加される"
    - "POST_RACE情報漏洩テストのAST scanがcompute_market_cross_features()でPASSする"
    - "build_all()の出力に5つのMCF列が含まれる"
  artifacts:
    - path: "src/features/feature_engine.py"
      provides: "build_all()とbuild_features()へのMCF統合"
      contains: "compute_market_cross_features"
    - path: "src/models/stage1_ability_model.py"
      provides: "AbilityModel.FEATURE_COLSに5 MCF特徴量追加"
      contains: "rl_favorite_in_wide_top1"
    - path: "tests/test_post_race_leakage.py"
      provides: "TestMarketCrossFeaturesテストクラス"
      contains: "class TestMarketCrossFeatures"
  key_links:
    - from: "src/features/feature_engine.py"
      to: "src/features/market_cross_features.py"
      via: "from features.market_cross_features import compute_market_cross_features"
      pattern: "compute_market_cross_features"
    - from: "src/features/feature_engine.py"
      to: "src/db/repository.py"
      via: "DataRepository().load_wide_odds(start, end)"
      pattern: "load_wide_odds"
    - from: "src/features/feature_engine.py"
      to: "src/db/repository.py"
      via: "DataRepository().load_trio_odds(start, end)"
      pattern: "load_trio_odds"
---

<objective>
feature_engineにmarket_cross_featuresを統合し、全12モデルのFEATURE_COLSに5特徴量を追加し、POST_RACE安全性を検証する。

Purpose: training/backtestパスでMCF特徴量が自動計算され、推論パスでもパリティを保つ。FEATURE_COLS昇格により全モデルがMCF特徴量を利用可能になる (D-04, MCF-07)
Output: feature_engine.py (MCF統合), 12モデルFEATURE_COLS更新, POST_RACEテスト拡張
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-market-cross-consistency-features/32-CONTEXT.md
@.planning/phases/32-market-cross-consistency-features/32-RESEARCH.md
@.planning/phases/31-race-level-aggregation-features/31-02-SUMMARY.md

<interfaces>
<!-- Plan 01が作成するインターフェース -->

From src/features/market_cross_features.py (Plan 01 output):
```python
MCF_COLS: list[str] = [
    "rl_favorite_in_wide_top1",
    "rl_trio_overlap",
    "rl_market_consistency",
    "rl_trio_odds_ratio",
    "rl_wide_harville_ratio",
]

def compute_market_cross_features(
    df: pd.DataFrame,
    wide_df: pd.DataFrame | None = None,
    trio_df: pd.DataFrame | None = None,
) -> pd.DataFrame: ...
```

From src/db/repository.py (Plan 01 output):
```python
class DataRepository:
    def load_wide_odds(self, start: str, end: str) -> pd.DataFrame: ...
    def load_trio_odds(self, start: str, end: str) -> pd.DataFrame: ...
```

From src/features/feature_engine.py (current integration points):
- build_all() line 345-348: compute_race_level_features()の直後がMCF追加先
- build_all() line 367-376: SAFE-01 POST_RACE stripping (MCFはこの前に計算)
- build_features() line 460-462: compute_race_level_features()の直後がMCF追加先

From src/features/race_level_features.py (integration pattern template):
```python
from features.race_level_features import compute_race_level_features
# in build_all():
with TimingContext("build_all/race_level"):
    result_df = compute_race_level_features(result_df)
```

全12モデルFEATURE_COLS (追加先リスト):
- AbilityModel.FEATURE_COLS (src/models/stage1_ability_model.py)
- MarketModel.FEATURE_COLS (src/models/market_model.py)
- RegimeDetector.FEATURE_COLS (src/models/regime_detector.py)
- PlaceAbilityModel.FEATURE_COLS (src/models/place_ability_model.py)
- RaceQualityScreener.FEATURE_COLS (src/models/race_quality_screener.py)
- WideTwoStageModel.SHARED_FEATURE_COLS (src/models/wide_two_stage_model.py)
- WinTwoStageModel.FEATURE_COLS (src/models/two_stage_return_model.py)
- PlaceTwoStageModel.HIT_FEATURE_COLS (src/models/two_stage_return_model.py)
- PlaceTwoStageModel.RETURN_FEATURE_COLS (src/models/two_stage_return_model.py)
- EVCorrectionModel.FEATURE_COLS (src/models/ev_correction_model.py)
- PlaceEVCorrectionModel.FEATURE_COLS (src/models/ev_correction_model.py)
- ConformalEVModel.FEATURE_COLS (src/models/conformal_ev_model.py)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: feature_engine統合 + FEATURE_COLS更新 + POST_RACEテスト</name>
  <files>
    src/features/feature_engine.py,
    src/models/stage1_ability_model.py,
    src/models/market_model.py,
    src/models/regime_detector.py,
    src/models/place_ability_model.py,
    src/models/race_quality_screener.py,
    src/models/wide_two_stage_model.py,
    src/models/two_stage_return_model.py,
    src/models/ev_correction_model.py,
    src/models/conformal_ev_model.py,
    tests/test_post_race_leakage.py
  </files>
  <read_first>
    src/features/feature_engine.py (lines 320-388 for build_all integration, lines 440-464 for build_features)
    src/features/market_cross_features.py (Plan 01 output -- exports and function signature)
    src/features/race_level_features.py (RL_COLS -- existing pattern reference)
    src/models/stage1_ability_model.py (FEATURE_COLS -- insertion point for MCF)
    src/models/market_model.py (FEATURE_COLS)
    src/models/regime_detector.py (FEATURE_COLS)
    src/models/place_ability_model.py (FEATURE_COLS)
    src/models/race_quality_screener.py (FEATURE_COLS)
    src/models/wide_two_stage_model.py (SHARED_FEATURE_COLS)
    src/models/two_stage_return_model.py (FEATURE_COLS, HIT_FEATURE_COLS, RETURN_FEATURE_COLS)
    src/models/ev_correction_model.py (FEATURE_COLS x2 classes)
    src/models/conformal_ev_model.py (FEATURE_COLS)
    tests/test_post_race_leakage.py (TestRaceLevelFeatures class -- pattern to replicate)
  </read_first>
  <behavior>
    - Test 1 (AST scan): compute_market_cross_features()のソースコード内にPOST_RACE_COLSの列名が文字列リテラルとして含まれない
    - Test 2 (col overlap): MCF_COLSの5列名がPOST_RACE_COLSと重複しない
    - Test 3 (build_all output): build_all()の出力に5つのMCF列が含まれる (NaNでも可、列が存在すること)
    - Test 4 (FEATURE_COLS): 全12モデルのFEATURE_COLSに5つのMCF列が含まれる
  </behavior>
  <action>
    **A. feature_engine.py統合**

    build_all()に以下を追加 -- compute_race_level_features()の直後(line 348直後)、SAFE-01 strippingの前:
    - DataRepositoryインスタンスを生成: `from db.repository import DataRepository; repo = DataRepository(store if isinstance(store, ParquetStore) else None)`
    - race_dfから日付範囲を抽出: `rd = pd.to_datetime(result_df["race_date"], errors="coerce"); rd_valid = rd.dropna()`
    - wide_df/trio_dfをロード: `wide_df = repo.load_wide_odds(str(rd_valid.min().date()), str(rd_valid.max().date()))`、trioも同様
    - storeがNoneの場合やdate抽出失敗時はwide_df/trio_df=Noneでフォールバック
    - TimingContext("build_all/market_cross")付きでcompute_market_cross_features()を呼び出し

    build_features()に以下を追加 -- compute_race_level_features()の直後(line 462直後):
    - `from features.market_cross_features import compute_market_cross_features`
    - `df = compute_market_cross_features(df)` -- wide_df/trio_dfなしで呼び出し (NaNフォールバック)
    - コメント: `# 7. 市場クロス整合性特徴量 (MCF-07 parity)`

    **B. 全12モデルFEATURE_COLSに5 MCF特徴量を追加**

    各モデルのFEATURE_COLSリストの末尾に以下の5列を追加:
    "rl_favorite_in_wide_top1", "rl_trio_overlap", "rl_market_consistency", "rl_trio_odds_ratio", "rl_wide_harville_ratio"

    追加先 (Phase 31のimplied_prob_hhi/odds_skewnessと同じ位置関係):
    - AbilityModel.FEATURE_COLS: "odds_skewness"の直後に5列追加
    - MarketModel.FEATURE_COLS: "odds_skewness"の直後に5列追加
    - RegimeDetector.FEATURE_COLS: "odds_skewness"の直後に5列追加
    - PlaceAbilityModel.FEATURE_COLS: "odds_skewness"の直後に5列追加
    - RaceQualityScreener.FEATURE_COLS: "odds_skewness"の直後に5列追加
    - WideTwoStageModel.SHARED_FEATURE_COLS: "odds_skewness"の直後に5列追加
    - WinTwoStageModel.FEATURE_COLS: 最後尾に5列追加
    - PlaceTwoStageModel.HIT_FEATURE_COLS: 最後尾に5列追加
    - PlaceTwoStageModel.RETURN_FEATURE_COLS: 最後尾に5列追加
    - EVCorrectionModel.FEATURE_COLS: "odds_skewness"の直後に5列追加
    - PlaceEVCorrectionModel.FEATURE_COLS: "odds_skewness"の直後に5列追加
    - ConformalEVModel.FEATURE_COLS: "implied_prob_hhi"の直後に5列追加

    **C. POST_RACEテスト拡張**

    tests/test_post_race_leakage.pyに TestMarketCrossFeatures クラスを追加 (TestRaceLevelFeaturesパターンを踏襲):
    - test_market_cross_features_no_post_race_input: ast.parse()でcompute_market_cross_features()のソースをスキャンしPOST_RACE_COLS参照がないことを確認
    - test_mcf_cols_not_in_post_race: MCF_COLSの5列がPOST_RACE_COLSと重複しないことを確認
    - test_build_all_produces_mcf_features: build_all()出力に5つのMCF列が含まれることを確認 (NaNでも可)
  </action>
  <verify>
    <automated>cd C:\Users\hirom\develop\keiba-ai && python -m pytest tests/test_market_cross_features.py tests/test_post_race_leakage.py tests/test_race_level_features.py -v</automated>
  </verify>
  <acceptance_criteria>
    - feature_engine.pyのbuild_all()に"compute_market_cross_features"呼び出しが存在する
    - feature_engine.pyのbuild_features()に"compute_market_cross_features"呼び出しが存在する
    - feature_engine.pyのbuild_all()に"load_wide_odds"と"load_trio_odds"呼び出しが存在する
    - AbilityModel.FEATURE_COLSに"rl_favorite_in_wide_top1"が含まれる
    - MarketModel.FEATURE_COLSに"rl_favorite_in_wide_top1"が含まれる
    - RegimeDetector.FEATURE_COLSに"rl_favorite_in_wide_top1"が含まれる
    - PlaceAbilityModel.FEATURE_COLSに"rl_favorite_in_wide_top1"が含まれる
    - RaceQualityScreener.FEATURE_COLSに"rl_favorite_in_wide_top1"が含まれる
    - WideTwoStageModel.SHARED_FEATURE_COLSに"rl_favorite_in_wide_top1"が含まれる
    - WinTwoStageModel.FEATURE_COLSに"rl_wide_harville_ratio"が含まれる
    - PlaceTwoStageModel.HIT_FEATURE_COLSに"rl_wide_harville_ratio"が含まれる
    - PlaceTwoStageModel.RETURN_FEATURE_COLSに"rl_wide_harville_ratio"が含まれる
    - EVCorrectionModel.FEATURE_COLSに"rl_wide_harville_ratio"が含まれる
    - PlaceEVCorrectionModel.FEATURE_COLSに"rl_wide_harville_ratio"が含まれる
    - ConformalEVModel.FEATURE_COLSに"rl_wide_harville_ratio"が含まれる
    - test_post_race_leakage.pyに"TestMarketCrossFeatures"クラスが存在する
    - 全テストが通過する
  </acceptance_criteria>
  <done>
    - build_all()がDataRepository経由でwide/trioオッズをロードしMCF特徴量を計算
    - build_features()がcompute_market_cross_features()を呼び出す (NaNフォールバック)
    - 全12モデルのFEATURE_COLSに5 MCF特徴量が追加されている
    - POST_RACEテスト(3テスト)が全て通過する
    - 既存テスト(test_race_level_features, test_market_cross_features)も全て通過する
  </done>
</task>

<task type="auto">
  <name>Task 2: manifest再生成</name>
  <files>data/feature_freeze_manifest.json</files>
  <read_first>
    scripts/freeze_feature_manifest.py
  </read_first>
  <action>
    scripts/freeze_feature_manifest.pyを実行してdata/feature_freeze_manifest.jsonを再生成する。
    全12モデルのfeature_countが+5され、SHA256ハッシュが更新されることを確認する。
    実行コマンド: python scripts/freeze_feature_manifest.py
  </action>
  <verify>
    <automated>cd C:\Users\hirom\develop\keiba-ai && python scripts/freeze_feature_manifest.py && python -c "import json; m=json.load(open('data/feature_freeze_manifest.json')); print(m['models']['AbilityModel']['feature_count'])"</automated>
  </verify>
  <acceptance_criteria>
    - AbilityModel feature_countが100以上である (Phase 31の97 + MCF 5 = 102)
    - manifest JSONが更新されている
  </acceptance_criteria>
  <done>
    - feature_freeze_manifest.jsonが再生成され全12モデルのSHA256が更新されている
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feature_engine -> DataRepository | build_all()内での外部データロード。store=Noneや日付抽出失敗時のNaNフォールバックが必要 |
| FEATURE_COLS -> LightGBM | 不正な列名やPOST_RACE列がFEATURE_COLSに混入すると情報漏洩 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04 | Information Disclosure | MCF列のPOST_RACE混入 | mitigate | AST source scanテスト + FEATURE_COLS whitelistテスト (3層検証) |
| T-32-05 | Tampering | build_all()のwide/trioデータ不在 | mitigate | DataRepository/storeがNone時はNaNフォールバック、ログ出力で監視 |
| T-32-SC | Tampering | pip install | accept | 新規パッケージインストールなし |
</threat_model>

<verification>
- python -m pytest tests/test_market_cross_features.py tests/test_post_race_leakage.py tests/test_race_level_features.py -v 全テスト通過
- python -c "from features.market_cross_features import MCF_COLS; print(len(MCF_COLS))" で5を出力
- python scripts/freeze_feature_manifest.py が成功
- grep -c "rl_favorite_in_wide_top1" src/models/stage1_ability_model.py で1以上を出力
</verification>

<success_criteria>
- build_all()とbuild_features()の両方でcompute_market_cross_features()が呼び出される
- 全12モデルのFEATURE_COLSに5 MCF特徴量が追加される
- POST_RACE AST scanテストが通過する
- 既存テストが全て通過する
- manifestが再生成される
</success_criteria>

<output>
Create `.planning/phases/32-market-cross-consistency-features/32-02-SUMMARY.md` when done
</output>
