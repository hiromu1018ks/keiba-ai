---
phase: 33-gain-per-depth-diagnostic
plan: 02
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - scripts/run_gpd.py
  - tests/test_run_gpd.py
autonomous: true
requirements:
  - GPD-02

must_haves:
  truths:
    - "CLIスクリプト run_gpd.py が学習済みモデルを読み込みGPD診断を実行する"
    - "matplotlib stacked bar + cumulative gain line PNGがモデル毎に生成される"
    - "PNGグラフにMarket/Fundamental/Categoricalの3色stacked barがdepth別に表示される"
  artifacts:
    - path: "scripts/run_gpd.py"
      provides: "CLI entry point for GPD diagnostics"
      contains: "argparse"
    - path: "tests/test_run_gpd.py"
      provides: "CLI script and visualization tests"
      min_lines: 100
  key_links:
    - from: "scripts/run_gpd.py"
      to: "src/db/model_loader.py::ModelLoader"
      via: "load_from_dir() for model loading"
      pattern: "ModelLoader"
    - from: "scripts/run_gpd.py"
      to: "src/models/gpd_diagnostics.py"
      via: "compute_gpd_diagnostics() + console_summary()"
      pattern: "compute_gpd_diagnostics"
    - from: "scripts/run_gpd.py"
      to: "data/gpd/"
      via: "PNG + JSON output files"
      pattern: "savefig"
---

<objective>
CLI script and matplotlib visualization for GPD diagnostics.

Purpose: Provides the user-facing CLI tool to run GPD diagnostics on trained models and generates per-model depth-by-category PNG charts for visual analysis of the Two-Stage hypothesis.
Output: `scripts/run_gpd.py` + `tests/test_run_gpd.py`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-gain-per-depth-diagnostic/33-CONTEXT.md
@.planning/phases/33-gain-per-depth-diagnostic/33-RESEARCH.md
@.planning/phases/33-gain-per-depth-diagnostic/33-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output (gpd_diagnostics.py) -->

From src/models/gpd_diagnostics.py:
```python
# Module-level constant
FEATURE_CATEGORY_MAP: dict[str, str]  # key=feature name, value="market"|"fundamental"|"categorical"

def compute_gpd_diagnostics(
    models: TrainedModelsV5,
    output_dir: Path | None = None,
) -> dict:
    """Run GPD analysis on all Boosters. Returns structured result dict.
    Writes JSON to {output_dir}/gpd_report.json if output_dir provided."""

def console_summary(result: dict) -> None:
    """Log formatted GPD summary to console."""
```

From src/db/model_loader.py:
```python
class ModelLoader:
    def load_from_dir(
        self, models_dir: Path, *, use_ensemble_override: bool | None = None
    ) -> tuple[TrainedModelsV5, ModelInfo]:
        """Load all models from a local directory."""
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create run_gpd.py CLI script and matplotlib visualization with tests</name>
  <files>scripts/run_gpd.py, tests/test_run_gpd.py</files>
  <behavior>
    - Test 1 (CLI argument parsing): Verify argparse accepts --models-dir, --output-dir, --ensemble flags with correct defaults.
    - Test 2 (PNG generation): Given a mock GPD result dict with controlled depth/category gain data, call the plotting function. Verify PNG file is created at expected path and has non-zero size. Use matplotlib agg backend to avoid display requirements.
    - Test 3 (PNG per model): Verify one PNG is generated per model in the result dict. File naming: gpd_{model_name}.png (e.g., gpd_stage1_turf.png, gpd_win_hit_turf.png).
    - Test 4 (Stacked bar content): Verify the chart function handles edge cases: single depth, single category, zero gains, very large depth count (>20).
    - Test 5 (Output directory creation): Verify output_dir is created if it does not exist (parents=True, exist_ok=True).
    - Test 6 (Integration with compute_gpd_diagnostics): Mock TrainedModelsV5, run compute_gpd_diagnostics, verify the plotting function receives the expected result structure and generates PNGs.
  </behavior>
  <action>
    Create `scripts/run_gpd.py` with the following structure:

    1. argparse setup with:
       - `--models-dir` (default="data/models", type=Path): directory containing trained model files
       - `--output-dir` (default="data/gpd", type=Path): output directory for JSON + PNGs
       - `--ensemble` (action="store_true"): enable StackedEnsemble model loading

    2. main() function:
       - Parse args
       - Call ModelLoader().load_from_dir(args.models_dir, use_ensemble_override=args.ensemble)
       - Call compute_gpd_diagnostics(trained_models, output_dir=args.output_dir)
       - Call console_summary(result)
       - Call plot_gpd_charts(result, output_dir=args.output_dir)
       - Log completion message with output file paths

    3. `plot_gpd_charts(result: dict, output_dir: Path) -> list[Path]` function:
       For each model in result["models"]:
       - Create a figure with 2 subplots (top: stacked bar, bottom: cumulative gain line)
       - Stacked bar: x-axis = depth levels (node_depth values), y-axis = gain contribution, 3 bars per depth (Market=blue, Fundamental=green, Categorical=orange), stacked
       - Cumulative gain line: x-axis = depth, y-axis = cumulative gain percentage, 3 lines (Market, Fundamental, Categorical) showing how gain accumulates from depth 1 to max depth
       - Title: "GPD: {model_name} (tier={tier})"
       - Annotate MDR and FAD values on the plot
       - Save to output_dir / f"gpd_{model_name}.png" at 150 DPI
       - Use matplotlib.use("Agg") backend for headless operation
       - Close figure after saving to prevent memory leaks
       - Return list of saved PNG paths

    4. Color scheme (Claude's discretion per D-09):
       - Market: "#2196F3" (blue)
       - Fundamental: "#4CAF50" (green)
       - Categorical: "#FF9800" (orange)
       - Cumulative lines: same colors, dashed style, thicker linewidth

    Create `tests/test_run_gpd.py` with 6 test cases matching the <behavior> block above. Use `matplotlib.use("Agg")` at module level. For PNG tests, write to tmp_path fixture. Mock TrainedModelsV5 and Booster objects as needed.

    Follow the existing CLI script pattern from other scripts in this project (e.g., run_etl.py, run_train.py): proper if __name__ == "__main__" guard, logger setup, sys.excepthook for unhandled exceptions.
  </action>
  <verify>
    <automated>cd "C:/Users/hirom/develop/keiba-ai" && python -m pytest tests/test_run_gpd.py -v</automated>
  </verify>
  <done>
    - run_gpd.py accepts --models-dir, --output-dir, --ensemble flags
    - plot_gpd_charts generates one PNG per model with stacked bar + cumulative gain line
    - PNG files named gpd_{model_name}.png in output directory
    - MDR and FAD annotated on chart
    - All 6 tests pass
    - No ruff or mypy errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → ModelLoader | Path arguments. No injection risk (file read only). |
| matplotlib → PNG file | File write to local directory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03 | Tampering | CLI path arguments | accept | Path traversal not a risk for local dev tool |
| T-33-04 | Denial of Service | matplotlib rendering | accept | Runs on developer machine; resource bounded |
| T-33-SC | Tampering | pip install (none) | accept | No new packages installed |
</threat_model>

<verification>
1. `python -m pytest tests/test_run_gpd.py -v` -- all 6 tests pass
2. `python -m pytest tests/test_gpd_diagnostics.py tests/test_run_gpd.py -v` -- all 14 tests pass
3. `ruff check scripts/run_gpd.py tests/test_run_gpd.py` -- no errors
4. `python scripts/run_gpd.py --help` -- prints usage without error
</verification>

<success_criteria>
- CLI script loads models, runs diagnostics, outputs JSON + PNGs + console summary
- PNG charts show stacked bar (Market/Fundamental/Categorical by depth) and cumulative gain line
- One PNG per model with correct naming convention
- All tests pass, no lint errors
</success_criteria>

<output>
Create `.planning/phases/33-gain-per-depth-diagnostic/33-02-SUMMARY.md` when done
</output>
