---
phase: 34-validation-and-manifest-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/stage1_ability_model.py
  - src/models/two_stage_return_model.py
  - src/models/ev_correction_model.py
  - src/models/conformal_ev_model.py
  - src/models/market_model.py
  - src/models/place_ability_model.py
  - src/models/race_quality_screener.py
  - src/models/regime_detector.py
  - src/models/wide_two_stage_model.py
  - src/models/gpd_diagnostics.py
  - tests/test_post_race_leakage.py
autonomous: true
requirements: [VAL-05]

must_haves:
  truths:
    - "rl_* 6 features appear in all 12 model FEATURE_COLS lists"
    - "rl_* 6 features are categorized as 'market' in GPD FEATURE_CATEGORY_MAP"
    - "POST_RACE leakage test passes with rl_* in FEATURE_COLS"
  artifacts:
    - path: "src/models/stage1_ability_model.py"
      provides: "AbilityModel.FEATURE_COLS with rl_*"
      contains: "rl_log_odds_entropy"
    - path: "src/models/two_stage_return_model.py"
      provides: "WinTwoStageModel.FEATURE_COLS + PlaceTwoStageModel HIT/RETURN with rl_*"
      contains: "rl_log_odds_entropy"
    - path: "src/models/gpd_diagnostics.py"
      provides: "FEATURE_CATEGORY_MAP with 6 rl_* entries in market category"
      contains: "rl_log_odds_entropy"
    - path: "tests/test_post_race_leakage.py"
      provides: "rl_* whitelist verification in leakage test"
  key_links:
    - from: "src/features/race_level_features.py"
      to: "src/models/*/FEATURE_COLS"
      via: "RL_COLS list"
      pattern: "rl_log_odds_entropy.*rl_odds_dispersion.*rl_top3_odds_gap"
---

<objective>
Register 6 rl_* race-level features (RLF-01~06) into all model FEATURE_COLS and GPD FEATURE_CATEGORY_MAP, then update POST_RACE leakage test to verify rl_* presence in the whitelist.

Purpose: rl_* features were computed by FeatureEngine in Phase 31 but never registered in model FEATURE_COLS. Per user decision, they MUST be added before backtest so LightGBM can use them.

Output: 11 source files modified with rl_* registered in all 12 model feature lists + GPD map + leakage test updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-validation-and-manifest-update/34-CONTEXT.md
@.planning/phases/34-validation-and-manifest-update/34-RESEARCH.md
@src/features/race_level_features.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 6 rl_* features to all 9 model files' FEATURE_COLS (12 lists total)</name>
  <files>
    src/models/stage1_ability_model.py
    src/models/two_stage_return_model.py
    src/models/ev_correction_model.py
    src/models/conformal_ev_model.py
    src/models/market_model.py
    src/models/place_ability_model.py
    src/models/race_quality_screener.py
    src/models/regime_detector.py
    src/models/wide_two_stage_model.py
  </files>
  <read_first>
    src/features/race_level_features.py
    src/models/stage1_ability_model.py
    src/models/two_stage_return_model.py
    src/models/ev_correction_model.py
    src/models/conformal_ev_model.py
    src/models/market_model.py
    src/models/place_ability_model.py
    src/models/race_quality_screener.py
    src/models/regime_detector.py
    src/models/wide_two_stage_model.py
  </read_first>
  <action>
Add the 6 rl_* features from RL_COLS to the END of each model's FEATURE_COLS list, right after the existing "rl_wide_harville_ratio" MCF entry (the last MCF feature). The 6 features to add in this exact order:
  "rl_log_odds_entropy", "rl_odds_dispersion", "rl_top3_odds_gap", "rl_top1_odds", "rl_favorite_rank_gap", "rl_n_horses"

Add a comment "# レースレベル集約 (RLF-01~06)" before the first rl_* entry.

Target 12 FEATURE_COLS lists across 9 files:

1. src/models/stage1_ability_model.py — AbilityModel.FEATURE_COLS: append after the line "rl_wide_harville_ratio" (currently at line ~156)
2. src/models/two_stage_return_model.py — WinTwoStageModel.FEATURE_COLS: append after "rl_wide_harville_ratio" (line ~163)
3. src/models/two_stage_return_model.py — PlaceTwoStageModel.HIT_FEATURE_COLS: append after "rl_wide_harville_ratio" (currently this list does NOT have MCF features — check if rl_* should be added here too. If PlaceTwoStageModel.HIT_FEATURE_COLS does not contain MCF features, still add rl_* with the same pattern). Read lines 357-465 to verify the end of HIT_FEATURE_COLS.
4. src/models/two_stage_return_model.py — PlaceTwoStageModel.RETURN_FEATURE_COLS: append after "rl_wide_harville_ratio" (line ~463)
5. src/models/ev_correction_model.py — EVCorrectionModel.FEATURE_COLS: append after "rl_wide_harville_ratio" (line ~191)
6. src/models/ev_correction_model.py — PlaceEVCorrectionModel.FEATURE_COLS: append after "rl_wide_harville_ratio" (line ~452)
7. src/models/conformal_ev_model.py — ConformalEVModel.FEATURE_COLS: append after "rl_wide_harville_ratio" (line ~154)
8. src/models/market_model.py — MarketModel.FEATURE_COLS: append after "rl_wide_harville_ratio" (line ~40)
9. src/models/place_ability_model.py — PlaceAbilityModel.FEATURE_COLS: append after "rl_wide_harville_ratio" (line ~110)
10. src/models/race_quality_screener.py — RaceQualityScreener.FEATURE_COLS: append after "rl_wide_harville_ratio" (line ~62)
11. src/models/regime_detector.py — RegimeDetector.FEATURE_COLS: append after "rl_wide_harville_ratio" (line ~70)
12. src/models/wide_two_stage_model.py — WideTwoStageModel.SHARED_FEATURE_COLS: append after "rl_wide_harville_ratio" (line ~58)

IMPORTANT: For PlaceTwoStageModel.HIT_FEATURE_COLS, check whether MCF features (rl_favorite_in_wide_top1 etc.) are present. If not present, only add rl_* after the current last feature. Do NOT add MCF features — only add the 6 rl_* features.
  </action>
  <verify>
    <automated>python -c "from models.stage1_ability_model import AbilityModel; from models.two_stage_return_model import WinTwoStageModel, PlaceTwoStageModel; from models.ev_correction_model import EVCorrectionModel, PlaceEVCorrectionModel; from models.conformal_ev_model import ConformalEVModel; from models.market_model import MarketModel; from models.place_ability_model import PlaceAbilityModel; from models.race_quality_screener import RaceQualityScreener; from models.regime_detector import RegimeDetector; from models.wide_two_stage_model import WideTwoStageModel; rl=['rl_log_odds_entropy','rl_odds_dispersion','rl_top3_odds_gap','rl_top1_odds','rl_favorite_rank_gap','rl_n_horses']; lists=[('AbilityModel',AbilityModel.FEATURE_COLS),('WinTwoStage',WinTwoStageModel.FEATURE_COLS),('PlaceHit',PlaceTwoStageModel.HIT_FEATURE_COLS),('PlaceReturn',PlaceTwoStageModel.RETURN_FEATURE_COLS),('EVCorr',EVCorrectionModel.FEATURE_COLS),('PlaceEVCorr',PlaceEVCorrectionModel.FEATURE_COLS),('Conformal',ConformalEVModel.FEATURE_COLS),('Market',MarketModel.FEATURE_COLS),('PlaceAbility',PlaceAbilityModel.FEATURE_COLS),('RQS',RaceQualityScreener.FEATURE_COLS),('Regime',RegimeDetector.FEATURE_COLS),('Wide',WideTwoStageModel.SHARED_FEATURE_COLS)]; missing=[(n,[f for f in rl if f not in c]) for n,c in lists if any(f not in c for f in rl)]; assert not missing, f'Missing rl_* features: {missing}'; print(f'All 12 model lists verified with 6 rl_* features')"
    </automated>
  </verify>
  <done>All 12 model FEATURE_COLS lists contain all 6 rl_* features (rl_log_odds_entropy, rl_odds_dispersion, rl_top3_odds_gap, rl_top1_odds, rl_favorite_rank_gap, rl_n_horses).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 6 rl_* features to GPD FEATURE_CATEGORY_MAP as "market" + update POST_RACE leakage test</name>
  <files>
    src/models/gpd_diagnostics.py
    tests/test_post_race_leakage.py
  </files>
  <read_first>
    src/models/gpd_diagnostics.py
    tests/test_post_race_leakage.py
    src/features/race_level_features.py
  </read_first>
  <behavior>
    - GPD FEATURE_CATEGORY_MAP contains all 6 rl_* entries mapped to "market"
    - POST_RACE leakage test verifies rl_* features appear in model FEATURE_COLS whitelist (Layer 2)
  </behavior>
  <action>
Part A — GPD FEATURE_CATEGORY_MAP update:

In src/models/gpd_diagnostics.py, add 6 entries to FEATURE_CATEGORY_MAP, in the "Market" section (after the existing rl_wide_harville_ratio line at approximately line 70):
  "rl_log_odds_entropy": "market",
  "rl_odds_dispersion": "market",
  "rl_top3_odds_gap": "market",
  "rl_top1_odds": "market",
  "rl_favorite_rank_gap": "market",
  "rl_n_horses": "market",

These are race-level market structure features derived from odds, so "market" category is correct.

Part B — POST_RACE leakage test update:

In tests/test_post_race_leakage.py, update test_all_models_have_mcf_features (line 334) to ALSO verify rl_* features in model FEATURE_COLS. Add a new test method test_all_models_have_rl_features in the TestMarketCrossFeatures class (or create a new test class TestRaceLevelFeatureRegistration) that mirrors the pattern of test_all_models_have_mcf_features but uses RL_COLS instead of MCF_COLS.

The new test must:
1. Import RL_COLS from features.race_level_features
2. Check all 12 model feature lists (same model_feature_lists as test_all_models_have_mcf_features)
3. Assert each rl_* feature is in each model's feature list
4. Include the additional model lists: EVCorrectionModel.FEATURE_COLS, PlaceEVCorrectionModel.FEATURE_COLS, ConformalEVModel.FEATURE_COLS that are NOT currently in test_model_feature_cols_no_post_race but ARE in test_all_models_have_mcf_features
  </action>
  <verify>
    <automated>python -m pytest tests/test_post_race_leakage.py -v -k "test_all_models_have_rl_features or test_all_models_have_mcf" 2>&1 | tail -30</automated>
  </verify>
  <done>
    - GPD FEATURE_CATEGORY_MAP has 6 new rl_* entries all mapped to "market"
    - New test_all_models_have_rl_features test passes, verifying all 12 model FEATURE_COLS contain all 6 rl_* features
    - Existing test_all_models_have_mcf_features still passes
    - All 11 test_post_race_leakage tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FEATURE_COLS registration | rl_* features must be safe (pre-race only) before registration |
| GPD categorization | Correct market category assignment affects diagnostic accuracy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Information Disclosure | rl_* features in FEATURE_COLS | mitigate | POST_RACE leakage test (VAL-05) verifies rl_* are pre-race safe |
| T-34-02 | Tampering | GPD FEATURE_CATEGORY_MAP | accept | Read-only config, no user input |
| T-34-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
1. All 12 model FEATURE_COLS lists contain all 6 rl_* features
2. GPD FEATURE_CATEGORY_MAP has 6 new "market" entries for rl_*
3. python -m pytest tests/test_post_race_leakage.py -v passes all tests (11+)
4. python -m pytest tests/ -v --co -q shows no collection errors
</verification>

<success_criteria>
- All 12 model FEATURE_COLS lists contain exactly 6 rl_* features each
- GPD FEATURE_CATEGORY_MAP categorizes all 6 rl_* as "market"
- All POST_RACE leakage tests pass (including new rl_* registration test)
- Zero test regressions in existing test suite
</success_criteria>

<output>
Create `.planning/phases/34-validation-and-manifest-update/34-01-SUMMARY.md` when done
</output>
