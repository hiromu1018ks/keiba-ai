---
phase: 34-validation-and-manifest-update
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - tests/test_post_race_leakage.py
autonomous: true
requirements: [VAL-05]

must_haves:
  truths:
    - "All 11+ POST_RACE leakage tests pass with rl_* features registered in FEATURE_COLS"
    - "No POST_RACE_COLS found in build_all() output"
    - "No POST_RACE_COLS found in any model FEATURE_COLS"
  artifacts:
    - path: "tests/test_post_race_leakage.py"
      provides: "Complete POST_RACE safety verification including rl_* features"
  key_links:
    - from: "src/features/race_level_features.py"
      to: "tests/test_post_race_leakage.py"
      via: "RL_COLS safety verification"
      pattern: "test_all_models_have_rl_features"
---

<objective>
Run full POST_RACE leakage test suite (VAL-05) to verify that rl_* and MCF features registered in Plan 01 are safe and no information leakage exists.

Purpose: Per D-12 execution order, leakage test must run FIRST (fastest, early detection) before the expensive backtest.

Output: Confirmed POST_RACE safety of all features. If leakage detected, backtest is blocked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-validation-and-manifest-update/34-CONTEXT.md
@.planning/phases/34-validation-and-manifest-update/34-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run POST_RACE leakage test suite (VAL-05) and record results</name>
  <files>
    tests/test_post_race_leakage.py
  </files>
  <read_first>
    tests/test_post_race_leakage.py
    src/domain/types.py
  </read_first>
  <action>
Execute the full POST_RACE leakage test suite. This is VAL-05 per D-12 step 1.

Command: python -m pytest tests/test_post_race_leakage.py -v

The test suite has 3 layers (11+ test cases):
- Layer 1: TestPostRaceLeakage (4 tests) — build_all output, model FEATURE_COLS whitelist, EV correction odds, conformal whitelist
- Layer 2: TestRaceLevelFeatures (3 tests) — rl_* source code audit, RL_COLS vs POST_RACE, build_all produces rl_*
- Layer 3: TestMarketCrossFeatures (4+ tests) — MCF source code audit, MCF_COLS vs POST_RACE, build_all produces MCF, all 12 models have MCF, all 12 models have rl_* (added by Plan 01)

Record the output. If ALL tests pass, note the test count and confirm VAL-05 is complete. If ANY test fails, do NOT proceed to Plan 03 (backtest). Instead, report the failure in SUMMARY.md with the exact test name and failure message.
  </action>
  <verify>
    <automated>python -m pytest tests/test_post_race_leakage.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All POST_RACE leakage tests pass (11+ test cases)
    - VAL-05 is confirmed complete
    - Test output recorded for audit trail
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Feature computation -> model input | POST_RACE columns must not propagate through build_all() or FEATURE_COLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03 | Information Disclosure | rl_* features in build_all() | mitigate | 3-layer leakage test verifies safety |
| T-34-SC | Tampering | npm/pip/cargo installs | accept | No package installs |
</threat_model>

<verification>
1. python -m pytest tests/test_post_race_leakage.py -v — all tests pass
2. Zero POST_RACE_COLS found in build_all() output
3. Zero POST_RACE_COLS found in any model FEATURE_COLS
</verification>

<success_criteria>
- All 11+ POST_RACE leakage tests pass
- VAL-05 requirement satisfied
- No information leakage detected for rl_* or MCF features
</success_criteria>

<output>
Create `.planning/phases/34-validation-and-manifest-update/34-02-SUMMARY.md` when done
</output>
