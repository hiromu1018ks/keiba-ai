---
phase: 34-validation-and-manifest-update
plan: 03
type: execute
wave: 3
depends_on: ["34-02"]
files_modified: []
autonomous: true
requirements: [VAL-01, VAL-02, VAL-03]

must_haves:
  truths:
    - "Single-year BT 2024 completes with ROI result (VAL-01)"
    - "IC evaluation produces 4 formulations x 3 surfaces values (VAL-02)"
    - "GPD diagnostic produces MDR and FAD values (VAL-03)"
  artifacts:
    - path: "data/backtest/multi_year_result.json"
      provides: "BT 2024 ROI and betting results"
    - path: "data/oof/oof_predictions.parquet"
      provides: "OOF predictions for IC evaluation"
      min_lines: 1
    - path: "data/baseline/ic_baseline.json"
      provides: "v1.7 IC baseline values (4 formulations x 3 surfaces)"
    - path: "data/gpd/gpd_report.json"
      provides: "GPD depth-gain analysis with MDR/FAD"
  key_links:
    - from: "scripts/run_backtest.py"
      to: "data/oof/oof_predictions.parquet"
      via: "TrainingPipeline OOF save hook"
      pattern: "oof_predictions"
    - from: "data/oof/oof_predictions.parquet"
      to: "scripts/run_ic_eval.py"
      via: "IC evaluation reads OOF"
      pattern: "ic_baseline"
    - from: "data/models-backtest/2024/"
      to: "scripts/run_gpd.py"
      via: "GPD reads trained models"
      pattern: "gpd_report"
---

<objective>
Execute the three core validation steps in sequence: (1) single-year BT 2024 with rl_* features now in FEATURE_COLS, (2) IC evaluation from BT-saved OOF predictions, (3) GPD diagnostic from trained models.

Purpose: Per D-12 execution order steps 2-4, these are the substantive validation steps that measure whether rl_* + MCF features improve model performance. D-01: target 2024 only. D-02: no strategy manifest. D-03: flat 100 yen. D-04: skip calibration-bt.

Output: ROI result, IC baseline values, GPD MDR/FAD metrics — all recorded for manifest freeze in Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-validation-and-manifest-update/34-CONTEXT.md
@.planning/phases/34-validation-and-manifest-update/34-RESEARCH.md
@.planning/phases/34-validation-and-manifest-update/34-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute single-year BT 2024 (VAL-01) — ~60 min expected</name>
  <files>
    (no source files modified — produces data outputs)
  </files>
  <read_first>
    scripts/run_backtest.py
  </read_first>
  <action>
Execute the single-year backtest for 2024 per D-01/D-02/D-03/D-04:

  python scripts/run_backtest.py --years 2024 --train-window 4 --ensemble --betting-mode flat --betting-target win

Key parameters:
- --years 2024: single test year per D-01
- --train-window 4: train on 2020-2023 (4 years)
- --ensemble: StackedEnsemble enabled (project convention)
- --betting-mode flat: 100 yen fixed per D-03
- --betting-target win: win model per scope
- No --strategy-manifest per D-02
- No --calibration-bt per D-04

Expected outputs:
- data/backtest/multi_year_result.json — ROI + betting summary
- data/validation/multi_year_validation_report.json — validation report
- data/oof/oof_predictions.parquet — OOF predictions (used by VAL-02)
- data/models-backtest/2024/ — trained models (used by VAL-03)

Expected duration: ~41-60 min (first time with rl_* features).

After BT completes, extract and record:
1. ROI value from multi_year_result.json
2. Total bets count
3. Win rate
4. Compare ROI to v1.6 baseline (85.7% for context only — D-10 says record as-is)

If BT fails with an error, record the error and STOP. Do not proceed to IC eval or GPD. Per CONTEXT D-10, if validation fails, record results as-is.
  </action>
  <verify>
    <automated>python -c "import json; r=json.load(open('data/backtest/multi_year_result.json')); print(f'ROI: {r.get(\"roi\",\"N/A\")}'); print(f'Bets: {r.get(\"total_bets\",\"N/A\")}'); print(f'WinRate: {r.get(\"win_rate\",\"N/A\")}')" 2>&1</automated>
  </verify>
  <done>
    - BT 2024 completes successfully
    - data/backtest/multi_year_result.json exists with ROI value
    - data/oof/oof_predictions.parquet exists with non-zero rows
    - data/models-backtest/2024/ contains trained models
    - ROI value recorded in SUMMARY
  </done>
</task>

<task type="auto">
  <name>Task 2: Execute IC evaluation (VAL-02) from BT-saved OOF</name>
  <files>
    (no source files modified — produces data outputs)
  </files>
  <read_first>
    scripts/run_ic_eval.py
    src/models/ic_evaluator.py
  </read_first>
  <action>
Execute IC evaluation using OOF predictions saved by BT training per D-06/D-07:

  python scripts/run_ic_eval.py data/oof/oof_predictions.parquet --output data/baseline/ic_baseline.json

This produces v1.7 IC baseline values (D-06: new baseline for future comparison):
- 4 formulations: B-diff, C-orthogonal, E-incremental, Per-race
- 3 surfaces: turf, dirt, all
- Direction consistency check

After IC eval completes, record:
1. All IC values from ic_baseline.json
2. Claude's judgment on whether values are "good" (D-06 discretion)
3. Direction consistency verdict

If IC eval fails (e.g., insufficient OOF data), record the error. Per D-10, record results as-is.
  </action>
  <verify>
    <automated>python -c "import json; ic=json.load(open('data/baseline/ic_baseline.json')); print(json.dumps({k:v for k,v in ic.items() if k in ['version','overall_b_diff','overall_c_orth','overall_e_incr','overall_per_race']}, indent=2, default=str))" 2>&1</automated>
  </verify>
  <done>
    - data/baseline/ic_baseline.json exists with IC values
    - 4 IC formulations x 3 surfaces recorded
    - Direction consistency checked
    - IC values recorded as v1.7 baseline per D-06
  </done>
</task>

<task type="auto">
  <name>Task 3: Execute GPD diagnostic (VAL-03) from trained models</name>
  <files>
    (no source files modified — produces data outputs)
  </files>
  <read_first>
    scripts/run_gpd.py
    src/models/gpd_diagnostics.py
  </read_first>
  <action>
Execute GPD diagnostic using trained models per D-08/D-09:

  python scripts/run_gpd.py --models-dir data/models-backtest/2024 --ensemble --output-dir data/gpd

This analyzes depth-by-category gain with the updated FEATURE_CATEGORY_MAP (now includes 6 rl_* features as "market").

After GPD completes:
1. Read data/gpd/gpd_report.json
2. Extract MDR (Market Dominance Ratio) and FAD (Fundamental Activation Depth) values
3. Claude judges PASS/WARN per D-08: MDR > 0 AND FAD <= 5 = PASS
4. Verify rl_* features appear in the "market" category analysis (D-09)
5. Check whether new features (rl_* + MCF) function at shallow depth (3-5)

If GPD fails, record the error. Per D-10, record results as-is and continue to manifest freeze.
  </action>
  <verify>
    <automated>python -c "import json; r=json.load(open('data/gpd/gpd_report.json')); print(json.dumps({k:v for k,v in r.items() if 'mdr' in k.lower() or 'fad' in k.lower() or 'verdict' in k.lower()}, indent=2, default=str))" 2>&1</automated>
  </verify>
  <done>
    - data/gpd/gpd_report.json exists with MDR/FAD values
    - GPD PASS/WARN judgment recorded per D-08
    - rl_* features visible in market category analysis
    - PNG charts generated in data/gpd/
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Training data -> model | OOF predictions must use train-only data (time split) |
| Model -> diagnostic | GPD reads Booster directly, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04 | Repudiation | BT model timestamps | mitigate | Verify model meta.json timestamp matches BT run |
| T-34-05 | Information Disclosure | OOF predictions file | accept | Local file, no external exposure |
| T-34-SC | Tampering | pip installs | accept | No package installs |
</threat_model>

<verification>
1. data/backtest/multi_year_result.json exists with numeric ROI
2. data/oof/oof_predictions.parquet exists with non-zero row count
3. data/baseline/ic_baseline.json exists with 4 IC formulations
4. data/gpd/gpd_report.json exists with MDR/FAD values
</verification>

<success_criteria>
- BT 2024 ROI measured and recorded
- IC baseline values recorded as v1.7 baseline (D-06)
- GPD diagnostic PASS/WARN judged by Claude (D-08)
- All three validation results available for manifest freeze in Plan 04
</success_criteria>

<output>
Create `.planning/phases/34-validation-and-manifest-update/34-03-SUMMARY.md` when done
</output>
