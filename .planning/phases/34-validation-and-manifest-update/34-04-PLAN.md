---
phase: 34-validation-and-manifest-update
plan: 04
type: execute
wave: 4
depends_on: ["34-03"]
files_modified:
  - scripts/freeze_feature_manifest.py
autonomous: true
requirements: [VAL-04]

must_haves:
  truths:
    - "FEATURE_COLS manifest frozen at v1.7 with SHA256 hashes for all 12 models"
    - "All 12 models include rl_* and MCF features in manifest"
  artifacts:
    - path: "data/feature_freeze_manifest.json"
      provides: "Frozen manifest v1.7 with SHA256 per model"
      contains: "v1.7"
    - path: "scripts/freeze_feature_manifest.py"
      provides: "Version string updated from v1.6 to v1.7"
      contains: "v1.7"
  key_links:
    - from: "src/models/*/FEATURE_COLS"
      to: "data/feature_freeze_manifest.json"
      via: "freeze_feature_manifest.py"
      pattern: "sha256"
---

<objective>
Update manifest version from "v1.6" to "v1.7" and freeze the FEATURE_COLS manifest with SHA256 hashes for all 12 models. This is the final validation step per D-12 execution order.

Purpose: Per VAL-04, freeze the manifest to lock in the v1.7 feature set (including rl_* and MCF features). Per D-10, freeze regardless of validation results.

Output: data/feature_freeze_manifest.json with version "v1.7" and SHA256 hashes for all 12 models.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-validation-and-manifest-update/34-CONTEXT.md
@.planning/phases/34-validation-and-manifest-update/34-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update manifest version to v1.7 and freeze (VAL-04)</name>
  <files>
    scripts/freeze_feature_manifest.py
  </files>
  <read_first>
    scripts/freeze_feature_manifest.py
  </read_first>
  <action>
Step 1: In scripts/freeze_feature_manifest.py, update the version string from "v1.6" to "v1.7" at line 77. Change:
  "version": "v1.6",
to:
  "version": "v1.7",

Step 2: Execute the manifest freeze:
  python scripts/freeze_feature_manifest.py

Step 3: Verify the output:
- data/feature_freeze_manifest.json exists
- Version is "v1.7"
- 12 models are listed
- Each model has feature_count, features list, and sha256
- rl_* features (rl_log_odds_entropy, rl_odds_dispersion, rl_top3_odds_gap, rl_top1_odds, rl_favorite_rank_gap, rl_n_horses) appear in each model's features list
- MCF features (rl_favorite_in_wide_top1, rl_trio_overlap, rl_market_consistency, rl_trio_odds_ratio, rl_wide_harville_ratio) appear in each model's features list
- overall_sha256 is populated

Step 4: Record the manifest summary in SUMMARY.md — version, model counts, overall SHA256 prefix.
  </action>
  <verify>
    <automated>python -c "import json; m=json.load(open('data/feature_freeze_manifest.json')); assert m['version']=='v1.7', f'Expected v1.7, got {m[\"version\"]}'; assert len(m['models'])==12, f'Expected 12 models, got {len(m[\"models\"])}'; assert all('rl_log_odds_entropy' in v['features'] for v in m['models'].values()), 'Missing rl_log_odds_entropy'; print(f'Version: {m[\"version\"]}'); print(f'Models: {len(m[\"models\"])}'); print(f'SHA256: {m[\"overall_sha256\"][:16]}...')" 2>&1</automated>
  </verify>
  <done>
    - scripts/freeze_feature_manifest.py version updated to "v1.7"
    - data/feature_freeze_manifest.json generated with version "v1.7"
    - All 12 models present with SHA256 hashes
    - rl_* features present in all model feature lists
    - MCF features present in all model feature lists
    - VAL-04 requirement satisfied
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model FEATURE_COLS -> manifest | SHA256 hash integrity verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-06 | Tampering | Manifest SHA256 | mitigate | Deterministic JSON serialization (sort_keys=True, indent=2) |
| T-34-SC | Tampering | pip installs | accept | No package installs |
</threat_model>

<verification>
1. data/feature_freeze_manifest.json exists with "version": "v1.7"
2. 12 models listed with feature_count and sha256
3. rl_* and MCF features in all model feature lists
4. overall_sha256 populated
</verification>

<success_criteria>
- Manifest frozen at v1.7 with correct SHA256 hashes
- All 12 models include rl_* (6) + MCF (5) new features
- VAL-04 requirement satisfied
- Phase 34 complete (all VAL-01~05 requirements addressed)
</success_criteria>

<output>
Create `.planning/phases/34-validation-and-manifest-update/34-04-SUMMARY.md` when done
</output>
