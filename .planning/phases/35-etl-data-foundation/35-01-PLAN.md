---
phase: 35-etl-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/etl.py
  - src/db/readers.py
  - tests/test_etl_type_conversion.py
autonomous: true
requirements: [ETL-01, ETL-02, ETL-03]

must_haves:
  truths:
    - "HaronTimeL3/L4 sentinels (000/999) are converted to NaN in entries Parquet"
    - "LapTime1~25 sentinels (000) are converted to NaN and divided by 10 in races Parquet"
    - "Jyuni2c/3c sentinels are converted to NaN in entries Parquet"
    - "HaronTimeL3 is removed from regular float rules (migrated to sentinel_float)"
    - "readers.py _FLOAT_COLS includes harontimel4 and laptime1~25"
    - "readers.py _INT_COLS includes jyuni2c and jyuni3c"
  artifacts:
    - path: "src/db/etl.py"
      provides: "sentinel_float rule processing in _apply_type_conversions"
      contains: "sentinel_float"
    - path: "src/db/etl.py"
      provides: "Updated _TABLE_TYPE_RULES for entries and races"
      contains: "sentinel_float"
    - path: "src/db/readers.py"
      provides: "Updated _INT_COLS and _FLOAT_COLS for backward compatibility"
      contains: "harontimel4"
    - path: "tests/test_etl_type_conversion.py"
      provides: "Sentinel rule tests"
      contains: "test_sentinel"
  key_links:
    - from: "src/db/etl.py::_apply_type_conversions"
      to: "_TABLE_TYPE_RULES sentinel_float entries"
      via: "rules.get('sentinel_float')"
      pattern: "sentinel_float"
    - from: "src/db/etl.py::_apply_type_conversions"
      to: "_TABLE_TYPE_RULES sentinel_float races"
      via: "rules.get('sentinel_float') with divisor"
      pattern: "divisor"
---

<objective>
ETLエンジンにsentinel_float/sentinel_intルールを追加し、HaronTimeL3/L4、LapTime1~25、Jyuni2c/3cのセンチネル値をNaN化してfloat64変換する。

Purpose: HaronTimeL3の000/999センチネル値が現在0.0/999.0として不正確に格納されている問題を修正し、LapTime/Jyuni列の新規float64化を有効にする。
Output: _TABLE_TYPE_RULES拡張、_apply_type_conversionsのsentinel処理、readers.pyの後方互換更新、単体テスト
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-etl-data-foundation/35-CONTEXT.md
@.planning/phases/35-etl-data-foundation/35-RESEARCH.md
@src/db/etl.py
@src/db/readers.py
@tests/test_etl_type_conversion.py
</context>

<interfaces>
<!-- Executor needs these contracts from existing codebase -->

From src/db/etl.py lines 83-131:
```python
_TABLE_TYPE_RULES: dict[str, dict[str, list[str]]] = {
    "races": {
        "int": ["trackcd", "kyori", "tenkocd", "syussotosu", "honsyokin"],
    },
    "entries": {
        "int": ["umaban", "kakuteijyuni", "ninki", "kyakusitukubun", "jyuni1c", "jyuni4c", "zogenfugo"],
        "float": ["time", "bataijyu", "zogensa", "harontimel3", "timediff"],
        "odds10": ["odds"],
    },
    # ... other tables ...
}
```

From src/db/etl.py lines 134-176 (_apply_type_conversions):
```python
def _apply_type_conversions(df: pd.DataFrame, table_key: str) -> pd.DataFrame:
    # processes int, float, odds10, odds100 rule types sequentially
```

From src/db/readers.py lines 26-47:
```python
_INT_COLS: set[str] = {
    "trackcd", "kyori", "tenkocd", "syussotosu", "honsyokin",
    "umaban", "kakuteijyuni", "ninki", "kyakusitukubun",
    "jyuni1c", "jyuni4c", "zogenfugo", "tanninki",
}
_FLOAT_COLS: set[str] = {
    "time", "bataijyu", "zogensa", "harontimel3", "timediff",
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sentinel_float/sentinel_intルール追加と_TABLE_TYPE_RULES更新</name>
  <files>src/db/etl.py, tests/test_etl_type_conversion.py</files>
  <read_first>
    - src/db/etl.py (全体、特に_TABLE_TYPE_RULES lines 83-131と_apply_type_conversions lines 134-176)
    - tests/test_etl_type_conversion.py (既存テストパターン)
    - .planning/phases/35-etl-data-foundation/35-CONTEXT.md (D-07, D-08決定)
    - .planning/phases/35-etl-data-foundation/35-RESEARCH.md (Technical Approach)
  </read_first>
  <behavior>
    - test_sentinel_float_replaces_sentinels: "000"/"999"がNaNに変換され、有効値("345")がfloat64として保持される
    - test_sentinel_float_with_divisor: "345"が34.5に変換され、"000"がNaNになる (divisor=10)
    - test_sentinel_float_missing_columns: 対象列がDataFrameに存在しなくてもエラーにならない
    - test_haron_timel3_migrated_from_float: harontimel3がentries.floatリストに含まれない(sentinel_floatに移行済み)
    - test_sentinel_float_no_double_processing: sentinel_floatとfloatで同じ列が二重処理されない
  </behavior>
  <action>
    **Step 1: テストを先に書く (RED)**

    tests/test_etl_type_conversion.py に TestSentinelRules クラスを追加:

    (a) test_sentinel_float_replaces_sentinels:
    - DataFrame({"harontimel3": ["000", "999", "345", ""]}) を _apply_type_conversions(df, "entries") に渡す
    - 結果: [NaN, NaN, 345.0, NaN] を検証 (math.isnan でNaN確認)

    (b) test_sentinel_float_with_divisor:
    - DataFrame({"laptime1": ["000", "345", "120", ""]}) を _apply_type_conversions(df, "races") に渡す
    - 結果: [NaN, 34.5, 12.0, NaN] を検証

    (c) test_sentinel_float_missing_columns:
    - DataFrame({"other": ["1"]}) を _apply_type_conversions(df, "races") に渡す
    - エラーなく "1" がそのまま返ることを検証

    (d) test_haron_timel3_migrated_from_float:
    - _TABLE_TYPE_RULES["entries"]["float"] に "harontimel3" が含まれないことを検証
    - _TABLE_TYPE_RULES["entries"]["sentinel_float"]["columns"] に "harontimel3" が含まれることを検証

    (e) test_sentinel_float_no_double_processing:
    - DataFrame({"harontimel3": ["345"]}) を _apply_type_conversions(df, "entries") に渡す
    - harontimel3 == 345.0 (NaNではなく正しい値) を検証

    **Step 2: 実装 (GREEN)**

    src/db/etl.py の変更:

    (a) _TABLE_TYPE_RULES の型アノテーションを変更:
    dict[str, dict[str, list[str]]] → dict[str, dict[str, list[str] | dict]]

    (b) _TABLE_TYPE_RULES["entries"] を更新:
    - "float" リストから "harontimel3" を削除 (float = ["time", "bataijyu", "zogensa", "timediff"])
    - "sentinel_float" を追加: {"columns": ["harontimel3", "harontimel4", "jyuni2c", "jyuni3c"], "sentinels": ["000", "999", "00"]}
      (注: Jyuni2c/3c は varchar(2) なので "00" もセンチネルに含む。RESEARCH.md Open Question #1 対応)

    (c) _TABLE_TYPE_RULES["races"] に "sentinel_float" を追加:
    - {"columns": [f"laptime{i}" for i in range(1, 26)], "sentinels": ["000"], "divisor": 10}
    (LapTime は varchar(3)、値 "345" = 34.5秒なので /10 が必要)

    (d) _apply_type_conversions に sentinel_float 処理ブロックを追加:
    - 既存の int/float/odds10/odds100 処理の後に追加
    - rules.get("sentinel_float") で dict を取得
    - isinstance(sentinel_float_rule, dict) で型ガード
    - cols = rule.get("columns", [])
    - sentinels = rule.get("sentinels", [])
    - divisor = rule.get("divisor", 1)
    - 各colについて: df[col].replace(sentinels, float("nan")) → pd.to_numeric(errors="coerce") → divisor != 1 の場合 df[col] / divisor

    (e) sentinel_int 処理ブロックも同様に追加 (将来拡張用、今回使用なし):
    - rules.get("sentinel_int") で処理
    - sentinel置換 → pd.to_numeric(errors="coerce") → astype("Int64")

    全関数に型アノテーションを付与 (mypy disallow_untyped_defs 対応)。
  </action>
  <verify>
    <automated>python -m pytest tests/test_etl_type_conversion.py -v</automated>
  </verify>
  <done>
    - sentinel_float ルールが _TABLE_TYPE_RULES の entries と races に定義されている
    - _apply_type_conversions が sentinel_float を処理する (sentinel NaN化 + 型変換 + optional divisor)
    - HaronTimeL3 が float リストから sentinel_float に移行済み
    - HaronTimeL4, Jyuni2c, Jyuni3c が sentinel_float entries に追加済み
    - LapTime1~25 が sentinel_float races に divisor=10 で追加済み
    - 5つのテストが全て通過する
  </done>
  <acceptance_criteria>
    - _TABLE_TYPE_RULES["entries"]["float"] == ["time", "bataijyu", "zogensa", "timediff"] (harontimel3削除済み)
    - _TABLE_TYPE_RULES["entries"]["sentinel_float"]["columns"] に "harontimel3", "harontimel4", "jyuni2c", "jyuni3c" が含まれる
    - _TABLE_TYPE_RULES["entries"]["sentinel_float"]["sentinels"] に "000", "999", "00" が含まれる
    - _TABLE_TYPE_RULES["races"]["sentinel_float"]["columns"] が ["laptime1", ..., "laptime25"] (25要素)
    - _TABLE_TYPE_RULES["races"]["sentinel_float"]["divisor"] == 10
    - _apply_type_conversions に "sentinel_float" 処理ループが存在する (grep -c "sentinel_float" src/db/etl.py >= 3)
    - python -m pytest tests/test_etl_type_conversion.py -v が 0 failed
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: readers.py _INT_COLS/_FLOAT_COLS更新</name>
  <files>src/db/readers.py, tests/test_etl_type_conversion.py</files>
  <read_first>
    - src/db/readers.py (特に_INT_COLS lines 26-40, _FLOAT_COLS lines 41-47, coerce_types lines 140-186)
    - tests/test_etl_type_conversion.py (Task 1の変更後)
    - .planning/phases/35-etl-data-foundation/35-CONTEXT.md (D-09決定)
  </read_first>
  <behavior>
    - test_float_cols_includes_new_columns: _FLOAT_COLS に "harontimel4" と "laptime1"~"laptime25" が含まれる
    - test_int_cols_includes_new_columns: _INT_COLS に "jyuni2c" と "jyuni3c" が含まれる
  </behavior>
  <action>
    **Step 1: テストを先に書く (RED)**

    tests/test_etl_type_conversion.py に TestReadersCompat クラスを追加:

    (a) test_float_cols_includes_harontimel4_and_laptimes:
    - from db.readers import _FLOAT_COLS
    - assert "harontimel4" in _FLOAT_COLS
    - for i in range(1, 26): assert f"laptime{i}" in _FLOAT_COLS

    (b) test_int_cols_includes_jyuni23c:
    - from db.readers import _INT_COLS
    - assert "jyuni2c" in _INT_COLS
    - assert "jyuni3c" in _INT_COLS

    **Step 2: 実装 (GREEN)**

    src/db/readers.py を更新:

    (a) _FLOAT_COLS に追加: "harontimel4" と f"laptime{i}" for i in range(1, 26)
    (b) _INT_COLS に追加: "jyuni2c", "jyuni3c"

    追加位置は各セットの末尾にコメント付きで追加 (e.g., "harontimel4",  # ETL-01)。
  </action>
  <verify>
    <automated>python -m pytest tests/test_etl_type_conversion.py -v</automated>
  </verify>
  <done>
    - _FLOAT_COLS に harontimel4 と laptime1~25 が追加されている
    - _INT_COLS に jyuni2c と jyuni3c が追加されている
    - テストが全て通過する
  </done>
  <acceptance_criteria>
    - "harontimel4" in _FLOAT_COLS が True
    - "laptime1" in _FLOAT_COLS が True
    - "laptime25" in _FLOAT_COLS が True
    - "jyuni2c" in _INT_COLS が True
    - "jyuni3c" in _INT_COLS が True
    - python -m pytest tests/test_etl_type_conversion.py -v が 0 failed
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EveryDB2 -> ETL | 外部DBからのvarcharデータをfloat64に変換。センチネル値が不正確な数値として流入するリスク |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Tampering | _apply_type_conversions sentinel | mitigate | sentinel値の完全なリストを明示的に定義し、pd.to_numeric(errors="coerce")で不正値をNaN化 |
| T-35-02 | Information Disclosure | HaronTimeL3 old data | accept | 既存Parquetの0.0/999.0不正確値はfull ETL再実行で修正 (PostgreSQL依存) |
| T-35-SC | Tampering | pip installs | accept | 新規パッケージインストールなし (標準ライブラリ pandas, pyarrow のみ) |
</threat_model>

<verification>
python -m pytest tests/test_etl_type_conversion.py tests/test_etl.py tests/test_post_race_leakage.py -v
ruff check src/db/etl.py src/db/readers.py
mypy src/db/etl.py src/db/readers.py
</verification>

<success_criteria>
- sentinel_float ルールが _TABLE_TYPE_RULES の entries/races 両方に定義されている
- _apply_type_conversions が sentinel_float を正しく処理する (NaN化 + divisor)
- HaronTimeL3 が float から sentinel_float に移行済み (二重処理なし)
- readers.py の _INT_COLS/_FLOAT_COLS が新列に対応
- tests/test_etl_type_conversion.py の全テスト通過
- ruff/mypy エラーなし
</success_criteria>

<output>
Create `.planning/phases/35-etl-data-foundation/35-01-SUMMARY.md` when done
</output>
