---
phase: 35-etl-data-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/types.py
  - tests/test_paper_trading_guards.py
  - scripts/run_paper_trading.py
  - tests/test_post_race_leakage.py
autonomous: true
requirements: [ETL-04, ETL-05]

must_haves:
  truths:
    - "POST_RACE_COLS in types.py contains all 41 entries (original 16 + laptime1~25)"
    - "test_paper_trading_guards.py imports POST_RACE_COLS from domain.types (no inline definition)"
    - "scripts/run_paper_trading.py imports POST_RACE_COLS from domain.types (no inline definition)"
    - "3-layer CI leakage tests pass with expanded POST_RACE_COLS"
    - "HaronTimeL3/L4 mutual exclusivity is documented with analysis approach"
  artifacts:
    - path: "src/domain/types.py"
      provides: "Single source of truth POST_RACE_COLS with 41 entries"
      min_lines: 42
    - path: "tests/test_paper_trading_guards.py"
      provides: "POST_RACE_COLS imported from domain.types"
      contains: "from domain.types import POST_RACE_COLS"
    - path: "scripts/run_paper_trading.py"
      provides: "POST_RACE_COLS imported from domain.types"
      contains: "from domain.types import POST_RACE_COLS"
  key_links:
    - from: "tests/test_paper_trading_guards.py"
      to: "src/domain/types.py"
      via: "from domain.types import POST_RACE_COLS"
      pattern: "from domain.types import POST_RACE_COLS"
    - from: "scripts/run_paper_trading.py"
      to: "src/domain/types.py"
      via: "from domain.types import POST_RACE_COLS"
      pattern: "from domain.types import POST_RACE_COLS"
---

<objective>
POST_RACE_COLSをtypes.pyに集約し、LapTime1~25を追加して41列に拡張する。重複定義をimportに置き換え、3層CI漏洩テストが正しく機能することを確認する。HaronTimeL3/L4の相互排他性検証アプローチを文書化する。

Purpose: POST_RACE安全性の単一ソース化 (D-10) とLapTime保護 (D-01)。Phase 36の特徴量計算がPOST_RACE列に安全にアクセスできる基盤を構築。
Output: types.py (41列), import統合, 漏洩テスト通過, ETL-05検証アプローチ文書
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-etl-data-foundation/35-CONTEXT.md
@.planning/phases/35-etl-data-foundation/35-RESEARCH.md
@src/domain/types.py
@tests/test_paper_trading_guards.py
@tests/test_post_race_leakage.py
@scripts/run_paper_trading.py
</context>

<interfaces>
<!-- Executor needs these contracts -->

From src/domain/types.py lines 37-55:
```python
POST_RACE_COLS: list[str] = [
    "kakuteijyuni", "confirmed_odds", "ninki", "kyakusitukubun",
    "time", "timediff", "harontimel3", "harontimel4",
    "jyuni1c", "jyuni2c", "jyuni3c", "jyuni4c",
    "honsyokin", "chakusacd", "dmjyuni", "dmtime",
]
```

From tests/test_paper_trading_guards.py lines 5-22:
```python
POST_RACE_COLS = (
    "kakuteijyuni", "confirmed_odds", "ninki", "kyakusitukubun",
    "time", "timediff", "harontimel3", "harontimel4",
    "jyuni1c", "jyuni2c", "jyuni3c", "jyuni4c",
    "honsyokin", "chakusacd", "dmjyuni", "dmtime",
)
```

From scripts/run_paper_trading.py lines 57-74:
```python
POST_RACE_COLS = (
    "kakuteijyuni", "confirmed_odds", "ninki", "kyakusitukubun",
    "time", "timediff", "harontimel3", "harontimel4",
    "jyuni1c", "jyuni2c", "jyuni3c", "jyuni4c",
    "honsyokin", "chakusacd", "dmjyuni", "dmtime",
)
```

From tests/test_post_race_leakage.py line 16:
```python
from domain.types import POST_RACE_COLS  # already imports correctly
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: POST_RACE_COLS拡張と重複解消</name>
  <files>src/domain/types.py, tests/test_paper_trading_guards.py, scripts/run_paper_trading.py</files>
  <read_first>
    - src/domain/types.py (全体、特にPOST_RACE_COLS lines 37-55)
    - tests/test_paper_trading_guards.py (全体、POST_RACE_COLS lines 5-22 と _drop_post_race_cols)
    - scripts/run_paper_trading.py (lines 50-80、POST_RACE_COLS定義と_drop_post_race_cols)
    - tests/test_post_race_leakage.py (line 16のimportパターン)
    - .planning/phases/35-etl-data-foundation/35-CONTEXT.md (D-01, D-10決定)
  </read_first>
  <action>
    **Step 1: types.py に laptime1~25 を追加**

    src/domain/types.py の POST_RACE_COLS に25列を追加:
    - 既存16列の後に f"laptime{i}" for i in range(1, 26) を追加
    - コメント: "# ETL-02: LapTime1~25 (RA table, race-level POST_RACE)" を付記
    - 型アノテーションは list[str] のまま変更なし
    - 合計41列になることを確認

    **Step 2: test_paper_trading_guards.py の重複定義をimportに置換**

    lines 5-22 の inline POST_RACE_COLS タプル定義を削除し、以下に置換:
    `from domain.types import POST_RACE_COLS`

    _drop_post_race_cols 関数はタプルからlistに変わっても動作する (set(result.columns) & set(POST_RACE_COLS) はlistでも動作)。ただし POST_RACE_COLS が list[str] なので、drop(columns=[...]) の内包表記はそのまま機能する。

    **Step 3: scripts/run_paper_trading.py の重複定義をimportに置換**

    lines 57-74 の inline POST_RACE_COLS タプル定義を削除し、以下に置換:
    `from domain.types import POST_RACE_COLS`

    import文はファイル先頭のimportセクションに追加する (標準ライブラリ→サードパーティ→ローカルの順)。

    **Step 4: テスト実行で検証**

    - python -m pytest tests/test_paper_trading_guards.py -v
    - python -m pytest tests/test_post_race_leakage.py -v
    - 両方通過することを確認
  </action>
  <verify>
    <automated>python -m pytest tests/test_paper_trading_guards.py tests/test_post_race_leakage.py -v</automated>
  </verify>
  <done>
    - types.py POST_RACE_COLS が41列 (元16 + laptime1~25)
    - test_paper_trading_guards.py が domain.types から import
    - run_paper_trading.py が domain.types から import
    - 既存テストが全て通過
  </done>
  <acceptance_criteria>
    - POST_RACE_COLS の len() == 41
    - "laptime1" in POST_RACE_COLS and "laptime25" in POST_RACE_COLS
    - test_paper_trading_guards.py に "from domain.types import POST_RACE_COLS" が存在する
    - test_paper_trading_guards.py に "POST_RACE_COLS = (" という重複定義が存在しない
    - run_paper_trading.py に "from domain.types import POST_RACE_COLS" が存在する
    - run_paper_trading.py に inline の "POST_RACE_COLS = (" が存在しない
    - python -m pytest tests/test_paper_trading_guards.py tests/test_post_race_leakage.py -v が 0 failed
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ETL-05 HaronTimeL3/L4相互排他性検証アプローチの文書化</name>
  <files>.planning/phases/35-etl-data-foundation/35-HARONTIME-ANALYSIS.md</files>
  <read_first>
    - .planning/phases/35-etl-data-foundation/35-CONTEXT.md (D-05, D-06決定)
    - .planning/phases/35-etl-data-foundation/35-RESEARCH.md (ETL-05, Open Questions)
    - docs/everydb2/04-UMA_RACE.md (fields 58-59: HaronTimeL4/L3)
    - docs/everydb2/03-RACE.md (fields 96-97: RA table HaronTimeL3/L4)
  </read_first>
  <action>
    .planning/phases/35-etl-data-foundation/35-HARONTIME-ANALYSIS.md を作成:

    **内容:**
    1. HaronTimeL3/L4のEveryDB2スキーマ定義:
       - SE table (UMA_RACE): fields 58-59, varchar(4), センチネル 000/999
       - RA table (RACE): fields 96-97, varchar(4), race-level values

    2. 相互排他性の仮説:
       - L3 = 上がり3Fタイム (3ハロン, 最後の600m)
       - L4 = 上がり4Fタイム (4ハロン, 最後の800m)
       - 両方NaN → 出走取消/障害/データなし
       - L3のみ → 3F計測のみ (標準)
       - L4のみ → 4F計測のみ (一部競馬場/条件)
       - 両方あり → 両方計測 (標準的、距離による)

    3. ETL実行後のClaude検証手順 (D-03, D-06):
       - Pythonスクリプトでentries.parquetを読み込み
       - 4分類の分布を集計: (L3のみ, L4のみ, 両方, なし)
       - NaN率の確認
       - 分布をこのファイルに追記

    4. Phase 36統合ロジックへの引き渡し事項:
       - harontime_last3f の決定はこの分析結果に依存
       - 候補: coalesce(L3, L4), 距離別選択, L3優先
       - Phase 36で最終決定

    このタスクは文書化のみ。コード変更なし。
  </action>
  <verify>
    <automated>test -f .planning/phases/35-etl-data-foundation/35-HARONTIME-ANALYSIS.md && grep -c "HaronTimeL3" .planning/phases/35-etl-data-foundation/35-HARONTIME-ANALYSIS.md</automated>
  </verify>
  <done>
    - HaronTimeL3/L4のスキーマ定義とセンチネル値が文書化されている
    - 相互排他性の4分類仮説が記載されている
    - ETL実行後のClaude検証手順が記載されている
    - Phase 36への引き渡し事項が記載されている
  </done>
  <acceptance_criteria>
    - 35-HARONTIME-ANALYSIS.md が存在する
    - "HaronTimeL3" がファイル内に 3回以上 出現する
    - "Phase 36" がファイル内に少なくとも1回 出現する
    - "4分類" or "mutual exclusivity" がファイル内に含まれる
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| types.py -> consumers | POST_RACE_COLSが正しく伝播する必要がある。import失敗は漏洩検出の無効化を招く |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-03 | Tampering | POST_RACE_COLS import | mitigate | import失敗時はテストが落ちる (CI検出可能) |
| T-35-04 | Information Disclosure | LapTime in POST_RACE | accept | LapTimeはrace-levelのPOST_RACE、build_all()には含まれない (D-02) |
| T-35-SC | Tampering | pip installs | accept | 新規パッケージインストールなし |
</threat_model>

<verification>
python -m pytest tests/test_paper_trading_guards.py tests/test_post_race_leakage.py -v
ruff check src/domain/types.py tests/test_paper_trading_guards.py scripts/run_paper_trading.py
mypy src/domain/types.py
</verification>

<success_criteria>
- types.py POST_RACE_COLS が41列 (laptime1~25 追加済み)
- test_paper_trading_guards.py が domain.types から import
- run_paper_trading.py が domain.types から import
- 3層CI漏洩テストが全て通過 (D-02確認)
- HaronTime相互排他性分析ファイルが存在
- ruff/mypy エラーなし
</success_criteria>

<output>
Create `.planning/phases/35-etl-data-foundation/35-02-SUMMARY.md` when done
</output>
