---
phase: 36-feature-computation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/horse_history_features.py
  - src/features/interaction_features.py
  - src/backtest/race_predictor.py
  - src/models/stage1_ability_model.py
  - src/models/two_stage_return_model.py
  - src/models/ev_correction_model.py
  - src/models/conformal_ev_model.py
  - src/models/market_model.py
  - src/models/place_ability_model.py
  - src/models/race_quality_screener.py
  - src/models/regime_detector.py
  - src/models/wide_two_stage_model.py
  - tests/test_trf_features.py
  - tests/test_interaction_features.py
autonomous: true
requirements:
  - TRF-01
  - TRF-02
  - TRF-03
  - INT-01
  - INT-02
  - INT-03
  - INT-04

must_haves:
  truths:
    - "form_trend_race_rank, blood_total_wr_race_rank, blood_surface_wr_race_rank appear as columns in add_race_transforms() output"
    - "weighted_recent_form_finish and weighted_recent_form_time are computed via EMA(halflife=3) in compute() results"
    - "grade_x_form_trend, distance_x_closing_index, grade_x_blood_prize_log appear in interaction_features.py output"
    - "All TRF/INT features appear in all 12 model FEATURE_COLS lists (except StackedEnsemble which has no FEATURE_COLS)"
    - "RacePredictor.predict() mirrors all new race_rank cols and interactions"
  artifacts:
    - path: "src/features/horse_history_features.py"
      provides: "TRF-01 race_rank cols + TRF-02 weighted_recent_form"
      contains: "weighted_recent_form_finish"
    - path: "src/features/interaction_features.py"
      provides: "INT-01/02/03 interaction products"
      contains: "grade_x_form_trend"
    - path: "tests/test_trf_features.py"
      provides: "TRF PIT-safety + dual-path parity tests"
    - path: "tests/test_interaction_features.py"
      provides: "INT interaction product correctness tests"
  key_links:
    - from: "src/features/horse_history_features.py"
      to: "src/features/interaction_features.py"
      via: "form_trend column used by INT-01"
      pattern: "form_trend"
    - from: "src/backtest/race_predictor.py"
      to: "src/features/horse_history_features.py"
      via: "_race_rank_cols list mirrors add_race_transforms"
      pattern: "form_trend.*race_rank"
---

<objective>
TRF (Turf Relative) and INT (Interaction) features: compute logic, model registration, dual-path mirroring.

Purpose: Add 5 new features (3 race-rank transforms + 2 weighted_recent_form) and 3 interaction products. Register all 8 features across 12 model FEATURE_COLS. Mirror in inference path (RacePredictor).

Output: horse_history_features.py extended, interaction_features.py extended, 10 model files updated, RacePredictor updated, 2 new test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-feature-computation/36-CONTEXT.md
@.planning/phases/36-feature-computation/36-RESEARCH.md
@src/features/horse_history_features.py
@src/features/interaction_features.py
@src/backtest/race_predictor.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/features/horse_history_features.py:
- BASE_COLS (line 282): class-level list of feature column names; new features MUST be added here
- add_race_transforms() (line 1326): static method; race_rank_cols list at line 1332; extends with {col}_race_rank pattern
- compute() (line 377): main method; results.append({}) at line 1261; new feature values go into this dict
- cols_horse (line 503): list of columns loaded into dict-of-numpy arrays; no change needed (form_trend/blood cols already computed)
- EMA pattern (lines 740-749): halflife=3, weights = (1-decay)^arange, reversed, normalized

From src/features/interaction_features.py:
- INTERACTION_COLS (line 9): list of interaction feature names; new names MUST be added here
- compute_interaction_features() (line 28): adds interaction columns to df; NaN-safe .where(notna) pattern
- _GRADE_MAP (line 129): {"G1": 5, "G2": 4, "G3": 3, "OP": 2, "J.G1": 5, "J.G2": 4, "J.G3": 3}

From src/backtest/race_predictor.py:
- _race_rank_cols (line 84): list that mirrors add_race_transforms race_rank_cols; MUST be kept in sync

From 36-CONTEXT.md locked decisions:
- D-07: weighted_recent_form uses EMA (halflife=3)
- D-08: 2 indicators: weighted_recent_form_finish (norm_finish_logit), weighted_recent_form_time (timediff)
- D-10: INT-01 = grade_code x form_trend (numeric product)
- D-11: INT-02 = kyori x closing_index_avg (numeric product)
- D-12: INT-03 = grade_code x blood_prize_log (numeric product, NaN-safe)
- D-13: add_race_transforms() gets 3 new race_rank cols: form_trend, blood_total_wr, blood_surface_wr
- D-15: All new features registered in 12 model FEATURE_COLS
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TRF-01 race-rank + TRF-02 weighted_recent_form in horse_history_features.py</name>
  <files>src/features/horse_history_features.py, tests/test_trf_features.py</files>
  <read_first>
    src/features/horse_history_features.py (full file, ~1351 lines)
    .planning/phases/36-feature-computation/36-CONTEXT.md (D-07, D-08, D-13)
    .planning/phases/36-feature-computation/36-RESEARCH.md (Patterns 3, 4)
  </read_first>
  <behavior>
    - Test 1: compute() returns weighted_recent_form_finish and weighted_recent_form_time columns for a horse with 3+ past races
    - Test 2: compute() returns NaN for weighted_recent_form_* when n_past == 0
    - Test 3: add_race_transforms() produces form_trend_race_rank, blood_total_wr_race_rank, blood_surface_wr_race_rank columns with values in (0, 1]
    - Test 4: BASE_COLS contains weighted_recent_form_finish and weighted_recent_form_time
  </behavior>
  <action>
    **Part A: weighted_recent_form_finish and weighted_recent_form_time (TRF-02, per D-07/D-08)**

    In compute(), after the form_trend computation (around line 1016), add weighted_recent_form computation. This uses the same EMA(halflife=3) pattern as harontimel5_avg (lines 740-749).

    1. For `weighted_recent_form_finish`: Use norm_finish_logit values from the past n_past races. Compute logits via `_norm_finish_logit_vec(hp_kakuteijyuni, hp_syussotosu)` (already available at line 727). Filter valid (non-NaN), then apply EMA weights (halflife=3, same decay formula). Result is the EMA-weighted average of logits.

    2. For `weighted_recent_form_time`: Use timediff values from horse_arrs["timediff"][valid_mask][start:idx]. Filter non-NaN, apply same EMA weights. Result is the EMA-weighted average of timediff.

    3. Both default to float("nan") when n_past == 0 or no valid values.

    Add both to BASE_COLS list (line 282) -- add after "cond_change_exp_count" and before the closing bracket.

    Add both to the results.append({}) dict (line 1261) -- add entries "weighted_recent_form_finish" and "weighted_recent_form_time".

    **Part B: Extend add_race_transforms race_rank_cols (TRF-01, per D-13)**

    In add_race_transforms() (line 1326), append 3 columns to race_rank_cols list (line 1332):
    - "form_trend" (already in BASE_COLS, computed by compute_form_features)
    - "blood_total_wr" (computed by bloodline features, available in df at this point)
    - "blood_surface_wr" (computed by bloodline features, available in df at this point)

    These 3 columns follow the exact same pattern as the existing 7 columns: groupby("race_id").rank(pct=True, method="average") producing {col}_race_rank. The existing for-loop at line 1344 handles column-not-present via "if col not in df.columns: continue", so blood_total_wr / blood_surface_wr gracefully skip if bloodline features are absent.

    **Part C: Create tests/test_trf_features.py**

    Create test file with 4 tests covering the behaviors above. Use mock data: create HorseHistoryFeatures with mocked store, construct minimal past_df with valid entries, verify outputs. For add_race_transforms tests, create a DataFrame with race_id, form_trend, blood_total_wr, blood_surface_wr columns and verify race_rank columns appear.
  </action>
  <verify>
    <automated>python -m pytest tests/test_trf_features.py -v</automated>
  </verify>
  <done>
    - weighted_recent_form_finish and weighted_recent_form_time appear in BASE_COLS
    - add_race_transforms() generates 3 new _race_rank columns
    - All 4 tests in test_trf_features.py pass
    - Existing tests still pass: python -m pytest tests/test_horse_history_features.py tests/test_interaction_features.py -v
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: INT-01/02/03 interaction features + TRF-03/INT-04 model registration + RacePredictor mirror</name>
  <files>src/features/interaction_features.py, src/models/stage1_ability_model.py, src/models/two_stage_return_model.py, src/models/ev_correction_model.py, src/models/conformal_ev_model.py, src/models/market_model.py, src/models/place_ability_model.py, src/models/race_quality_screener.py, src/models/regime_detector.py, src/models/wide_two_stage_model.py, src/backtest/race_predictor.py, tests/test_interaction_features.py</files>
  <read_first>
    src/features/interaction_features.py (full file, 226 lines)
    src/backtest/race_predictor.py (lines 51-100)
    src/models/stage1_ability_model.py (FEATURE_COLS list)
    src/models/conformal_ev_model.py (FEATURE_COLS list -- most comprehensive)
    .planning/phases/36-feature-computation/36-CONTEXT.md (D-10, D-11, D-12, D-15)
  </read_first>
  <behavior>
    - Test 1: compute_interaction_features produces grade_x_form_trend column when grade_code and form_trend are present
    - Test 2: compute_interaction_features produces distance_x_closing_index when kyori and closing_index_avg are present
    - Test 3: compute_interaction_features produces grade_x_blood_prize_log when grade_code and blood_prize_log are present
    - Test 4: All 3 interaction products are NaN when any input column is NaN
    - Test 5: All 12 model FEATURE_COLS (including WinTwoStageModel HIT+RETURN, PlaceTwoStageModel HIT+RETURN, EVCorrectionModel, PlaceEVCorrectionModel) contain the 3 new INT features + 5 new TRF features (form_trend_race_rank, blood_total_wr_race_rank, blood_surface_wr_race_rank, weighted_recent_form_finish, weighted_recent_form_time)
  </behavior>
  <action>
    **Part A: Add 3 interaction features (INT-01/02/03, per D-10/D-11/D-12)**

    In compute_interaction_features() (interaction_features.py), after the weight_x_class block (around line 135), add 3 new numeric product blocks:

    1. INT-01: `grade_x_form_trend` -- Use _GRADE_MAP (existing at line 129) to map grade_code to numeric. Compute grade_num = df["grade_code"].map(_GRADE_MAP).fillna(1.0). Then df["grade_x_form_trend"] = (grade_num * df["form_trend"]).where(grade_num.notna() & df["form_trend"].notna(), other=float("nan")). Guard with: if "grade_code" in df.columns and "form_trend" in df.columns.

    2. INT-02: `distance_x_closing_index` -- df["distance_x_closing_index"] = (df["kyori"] * df["closing_index_avg"]).where(df["kyori"].notna() & df["closing_index_avg"].notna(), other=float("nan")). Guard with: if "kyori" in df.columns and "closing_index_avg" in df.columns.

    3. INT-03: `grade_x_blood_prize_log` -- Reuse grade_num from INT-01 (or recompute). df["grade_x_blood_prize_log"] = (grade_num * df["blood_prize_log"]).where(grade_num.notna() & df["blood_prize_log"].notna(), other=float("nan")). Guard with: if "grade_code" in df.columns and "blood_prize_log" in df.columns.

    Add all 3 names to INTERACTION_COLS list at top of file (line 9).

    **Part B: Register TRF + INT features in 12 model FEATURE_COLS (TRF-03 + INT-04, per D-15)**

    Add these 8 feature names to every model's FEATURE_COLS (or SHARED_FEATURE_COLS for WideTwoStageModel):

    New TRF features (5):
    - "form_trend_race_rank"
    - "blood_total_wr_race_rank"
    - "blood_surface_wr_race_rank"
    - "weighted_recent_form_finish"
    - "weighted_recent_form_time"

    New INT features (3):
    - "grade_x_form_trend"
    - "distance_x_closing_index"
    - "grade_x_blood_prize_log"

    Target model files and their FEATURE_COLS locations:
    - src/models/stage1_ability_model.py -- AbilityModel.FEATURE_COLS
    - src/models/two_stage_return_model.py -- WinTwoStageModel.FEATURE_COLS (HIT) + RETURN list; PlaceTwoStageModel.HIT_FEATURE_COLS + RETURN_FEATURE_COLS
    - src/models/ev_correction_model.py -- EVCorrectionModel.FEATURE_COLS + PlaceEVCorrectionModel.FEATURE_COLS
    - src/models/conformal_ev_model.py -- ConformalEVModel.FEATURE_COLS
    - src/models/market_model.py -- MarketModel.FEATURE_COLS
    - src/models/place_ability_model.py -- PlaceAbilityModel.FEATURE_COLS
    - src/models/race_quality_screener.py -- RaceQualityScreener.FEATURE_COLS
    - src/models/regime_detector.py -- RegimeDetector.FEATURE_COLS
    - src/models/wide_two_stage_model.py -- WideTwoStageModel.SHARED_FEATURE_COLS

    Note: StackedEnsemble has NO FEATURE_COLS -- do NOT modify it (per Pitfall 6 in RESEARCH.md).

    Insert the 8 names in a logical position within each list (after existing interaction/history features, before odds/market features). Add a comment "# TRF-01/02/03 + INT-01/02/03/04" on the first new line.

    **Part C: Mirror race_rank in RacePredictor (per D-14/D-16)**

    In src/backtest/race_predictor.py, extend the _race_rank_cols list (line 84) with the 3 new TRF-01 columns:
    - "form_trend"
    - "blood_total_wr"
    - "blood_surface_wr"

    These mirror the additions in add_race_transforms(). The existing loop at line 94-96 generates {col}_race_rank from direct .rank(pct=True) for single-race inference.

    **Part D: Create tests/test_interaction_features.py**

    Create test file with 5 tests:
    - Tests 1-3: Call compute_interaction_features with DataFrame containing the required input columns, verify output columns exist and have correct values (e.g., grade_x_form_trend == grade_numeric * form_trend).
    - Test 4: Verify NaN propagation -- when form_trend is NaN, grade_x_form_trend is also NaN.
    - Test 5: Verify all 12 model FEATURE_COLS contain the 8 new feature names. Import all 12 model classes and check. Use the same model_feature_lists pattern from test_post_race_leakage.py lines 352-365.
  </action>
  <verify>
    <automated>python -m pytest tests/test_interaction_features.py tests/test_trf_features.py tests/test_post_race_leakage.py -v</automated>
  </verify>
  <done>
    - INTERACTION_COLS contains 15 names (12 existing + 3 new)
    - compute_interaction_features produces 3 new interaction columns
    - All 12 model FEATURE_COLS contain all 8 new TRF/INT feature names
    - RacePredictor._race_rank_cols contains 10 items (7 existing + 3 new)
    - All new tests pass; existing test_post_race_leakage Layer 2 still passes (no POST_RACE cols in FEATURE_COLS)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feature computation -> model input | Derived features cross into model FEATURE_COLS; must not contain POST_RACE data |
| interaction products | Numeric multiplication of two columns; NaN must propagate, not become 0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering | interaction_features.py | mitigate | .where(notna) pattern prevents fillna(0) false signals |
| T-36-02 | Information Disclosure | race_rank cols | mitigate | race_rank computed from BASE_COLS only; add_race_transforms guards missing columns |
| T-36-SC | Tampering | pip installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
1. python -m pytest tests/test_trf_features.py tests/test_interaction_features.py -v -- all tests pass
2. python -m pytest tests/test_post_race_leakage.py -v -- Layer 2 still passes (no POST_RACE in FEATURE_COLS)
3. python -m pytest tests/test_horse_history_features.py -v -- existing tests still pass
4. ruff check src/features/horse_history_features.py src/features/interaction_features.py src/backtest/race_predictor.py src/models/ -- no errors
</verification>

<success_criteria>
- form_trend_race_rank, blood_total_wr_race_rank, blood_surface_wr_race_rank appear in add_race_transforms() output
- weighted_recent_form_finish and weighted_recent_form_time appear in compute() output and BASE_COLS
- grade_x_form_trend, distance_x_closing_index, grade_x_blood_prize_log appear in interaction_features.py output
- All 8 new features appear in all 12 model FEATURE_COLS lists (verified by test)
- RacePredictor._race_rank_cols mirrors the 3 new race_rank source columns
- All tests pass including existing test_post_race_leakage.py
</success_criteria>

<output>
Create `.planning/phases/36-feature-computation/36-01-SUMMARY.md` when done
</output>
