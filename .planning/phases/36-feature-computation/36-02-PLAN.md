---
phase: 36-feature-computation
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - src/features/horse_history_features.py
  - src/backtest/race_predictor.py
  - tests/test_hlf_features.py
autonomous: true
requirements:
  - HLF-01
  - HLF-02
  - HLF-03
  - HLF-04
  - HLF-05

must_haves:
  truths:
    - "harontimel4_avg, harontimel4_zscore, harontimel4_trend appear in compute() output (HLF-01)"
    - "harontime_last3f_avg, harontime_last3f_zscore, harontime_last3f_trend appear in compute() output (HLF-01 unified)"
    - "harontimel3_avg_race_rank, harontimel4_avg_race_rank, harontime_last3f_avg_race_rank appear in add_race_transforms() output (HLF-02)"
    - "pace_ratio, pace_ratio_avg, pace_ratio_zscore, pace_ratio_trend, pace_early_avg, pace_late_avg appear in compute() output (HLF-03)"
    - "All HLF features appear in all 12 model FEATURE_COLS lists (HLF-04)"
    - "RacePredictor.predict() mirrors all new HLF race_rank columns (HLF-05)"
  artifacts:
    - path: "src/features/horse_history_features.py"
      provides: "HaronTime L4 history stats + unified column + LapTime pace features"
      min_lines: 1400
    - path: "tests/test_hlf_features.py"
      provides: "HLF PIT-safety + HaronTime stats + LapTime pace + dual-path tests"
  key_links:
    - from: "src/features/horse_history_features.py::compute()"
      to: "entries.parquet::harontimel4"
      via: "horse_arrs dict-of-numpy lookup"
      pattern: "harontimel4.*valid_mask"
    - from: "src/features/horse_history_features.py::compute()"
      to: "races.parquet::laptime1~25"
      via: "races_hist merge + pace_ratio calculation"
      pattern: "pace_ratio"
    - from: "src/features/horse_history_features.py::add_race_transforms()"
      to: "src/backtest/race_predictor.py::_race_rank_cols"
      via: "race_rank col name sync"
      pattern: "harontimel._avg.*race_rank"
---

<objective>
HLF (Haron/Lap Feature) computation: HaronTime L4 history stats, unified harontime_last3f column, LapTime pace features, model registration, dual-path verification.

Purpose: Compute 12 new HaronTime features (avg/zscore/trend/race_rank across L3/L4/unified) and LapTime pace features. All must be PIT-safe using existing searchsorted pattern. Register in 12 model FEATURE_COLS and mirror in inference path.

Output: horse_history_features.py extended with ~20 new features, 12 model FEATURE_COLS updated, RacePredictor updated, new test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-feature-computation/36-CONTEXT.md
@.planning/phases/36-feature-computation/36-RESEARCH.md
@.planning/phases/36-feature-computation/36-01-SUMMARY.md
@src/features/horse_history_features.py
@src/backtest/race_predictor.py
@src/db/readers.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/features/horse_history_features.py (after Plan 01 changes):
- BASE_COLS: contains TRF features from Plan 01; HLF features MUST be appended after those
- cols_horse (line 503): MUST add "harontimel4" to this list so it gets loaded into dict-of-numpy arrays
- _has_harontimel4 flag: MUST be added alongside existing _has_harontimel3 at line 570
- expanding_stats dict (line 584): Pre-computed for harontimel3 z-score; MUST create parallel expanding_stats_hl4 for L4
- _lookup_expanding_stats() (line 174): Reusable for L4 z-scores -- pass same expanding_stats or new dict
- EMA pattern (lines 740-749): halflife=3, weights = (1-decay)^arange, reversed, normalized. Reuse for L4 avg
- Linear regression trend: np.polyfit(x, values, 1)[0] pattern from haron_zscore_trend (line 836)
- results.append({}) at line 1261: All new HLF feature values MUST be added to this dict
- add_race_transforms() race_rank_cols: MUST extend with HLF-02 columns
- harontimel5_avg (line 736): Pattern for L4 avg -- identical but using harontimel4 array
- harontimel5_zscore (line 788): Pattern for L4 zscore -- identical but using harontimel4 + separate expanding_stats

From src/db/readers.py:
- _FLOAT_COLS (line 43): Already includes "harontimel4" and laptime1~25 (Phase 35)
- load_history_races (line 276): Returns full races.parquet including laptime columns

From src/domain/types.py:
- POST_RACE_COLS (line 38): Contains "harontimel3", "harontimel4", "laptime1"~"laptime25"
- CRITICAL: These are POST_RACE columns; past-race values are PIT-safe (searchsorted filters < target_date)
- Current-race values are POST_RACE and MUST NOT be used in features

From 36-CONTEXT.md locked decisions:
- D-01: harontime_last3f unified column with distance-based auto-selection (default 2000m)
- D-02: Independent history stats for L3, L4, and unified
- D-03: 12 HaronTime features = 4 stats (avg/zscore/trend/race_rank) x 3 columns (L3/L4/unified)
- D-04: LapTime 25 cols split into 3 equal segments; n_laps = kyori/200
- D-05: pace_ratio = late_avg / early_avg
- D-06: LapTime history: avg/zscore/trend of pace_ratio + segment averages; PIT-safe
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HLF-01/02 HaronTime L4 history + unified column + race_rank</name>
  <files>src/features/horse_history_features.py, src/backtest/race_predictor.py</files>
  <read_first>
    src/features/horse_history_features.py (full file, focus on lines 503-522 cols_horse, 569-577 _has flags, 584-645 expanding_stats, 736-855 harontimel5 avg/zscore/trend)
    .planning/phases/36-feature-computation/36-CONTEXT.md (D-01, D-02, D-03)
    .planning/phases/36-feature-computation/36-RESEARCH.md (Patterns 1-3, Code Examples section)
  </read_first>
  <behavior>
    - Test 1: compute() returns harontimel4_avg with correct EMA-weighted values when harontimel4 data exists
    - Test 2: compute() returns harontimel4_zscore as NaN when no expanding_stats data exists for that group
    - Test 3: compute() returns harontimel4_trend as linear regression slope of last 3 L4 values
    - Test 4: compute() returns harontime_last3f_avg selecting L4 for kyori >= 2000 and L3 for kyori < 2000
    - Test 5: add_race_transforms() produces harontimel3_avg_race_rank, harontimel4_avg_race_rank, harontime_last3f_avg_race_rank
    - Test 6: harontimel4 values default to NaN when harontimel4 column is absent from data (backward compat)
  </behavior>
  <action>
    **Part A: Enable harontimel4 in compute() data pipeline**

    1. Add "harontimel4" to cols_horse list (line 503) -- insert after "harontimel3" at line 508.

    2. Add `_has_harontimel4 = "harontimel4" in _sample_arrs` flag (line 569 area, after _has_harontimel3).

    **Part B: Pre-compute expanding_stats for L4 z-score**

    After the existing expanding_stats computation block for L3 (lines 584-645), add a parallel block for L4. Use the same FALLBACK_LEVELS structure. Create a separate dict `expanding_stats_hl4` to avoid key collision with L3 stats. The logic is identical to lines 584-645 but uses harontimel4 instead of harontimel3. Guard with `_has_harontimel4 and _has_distance_bin`.

    **Part C: Compute HaronTime L4 stats in per-horse loop**

    After the existing harontimel5_zscore computation (line 842), add L4 stats computation. Mirror the L3 patterns:

    1. `harontimel4_avg`: EMA(halflife=3) of valid L4 values from horse_arrs["harontimel4"]. Same pattern as harontimel5_avg (lines 736-753). Guard: `_has_harontimel4 and n_past > 0`. Filter NaN, apply EMA weights.

    2. `harontimel4_zscore`: Use expanding_stats_hl4 with _lookup_expanding_stats(). Same pattern as harontimel5_zscore (lines 788-842) but querying expanding_stats_hl4. Get mean/std from lookup, compute zscore for each past race, take tail(5).mean().

    3. `harontimel4_trend`: Linear regression slope of last 3 (or all available) valid L4 values. Same pattern as haron_zscore_trend (line 836): np.polyfit(np.arange(len(valid_values)), valid_values, 1)[0]. This gives the raw time trend in seconds (negative = improving).

    **Part D: Compute harontime_last3f unified column (per D-01)**

    After L4 stats, compute unified stats. The unified column selects the "best" haron time per past race based on distance:

    1. For each past race, determine which value to use:
       - If current_kyori >= DISTANCE_THRESHOLD (2000m): prefer L4, fallback to L3
       - If current_kyori < DISTANCE_THRESHOLD: prefer L3, fallback to L4
       - Where both are NaN, the unified value is NaN
       - This produces a raw array of unified haron times per past race

    2. From the unified raw array, compute 3 stats:
       - `harontime_last3f_avg`: EMA(halflife=3) of unified values
       - `harontime_last3f_zscore`: For z-score, lookup expanding stats. Use the L3 expanding_stats for unified (since L3 has more data coverage) -- or compute separate unified expanding_stats. The simplest PIT-safe approach: use a combined expanding_stats that includes both L3 and L4 races. Since L3 and L4 measure different segments, use the L3 expanding_stats as proxy for z-score (document this as a known approximation).
       - `harontime_last3f_trend`: Linear regression slope of last 3 unified values

    DISTANCE_THRESHOLD = 2000 constant at module level.

    **Part E: Extend BASE_COLS and results dict**

    Add to BASE_COLS (after Plan 01 additions):
    - "harontimel4_avg"
    - "harontimel4_zscore"
    - "harontimel4_trend"
    - "harontime_last3f_avg"
    - "harontime_last3f_zscore"
    - "harontime_last3f_trend"

    Add corresponding entries to results.append({}) dict.

    **Part F: Extend add_race_transforms race_rank_cols (HLF-02)**

    Add 3 columns to race_rank_cols in add_race_transforms():
    - "harontimel5_avg" already exists -- no change needed (already produces harontimel5_avg_race_rank)
    - Add "harontimel4_avg" -- produces harontimel4_avg_race_rank
    - Add "harontime_last3f_avg" -- produces harontime_last3f_avg_race_rank
    - For harontimel3_avg: Note harontimel3 is NOT in BASE_COLS (harontimel5_avg IS, which uses L3 data via EMA). If we want harontimel3_avg_race_rank we need harontimel3_avg in BASE_COLS. Per HLF-02, the race_rank is for "harontime_l3/l4/unified_race_rank". The L3 race-rank can reuse the existing harontimel5_avg_race_rank (since harontimel5_avg is computed from L3). Alternatively, add "harontimel3_avg" as a new BASE_COLS entry for the simple mean of L3 values (no EMA). Per D-03, the L3 features reuse harontimel5_avg (already has race_rank) -- so no new L3 race_rank needed.

    Final race_rank_cols additions for HLF-02:
    - "harontimel4_avg"
    - "harontime_last3f_avg"

    **Part G: Mirror in RacePredictor**

    In src/backtest/race_predictor.py _race_rank_cols list (line 84), add the HLF-02 source columns:
    - "harontimel4_avg"
    - "harontime_last3f_avg"

    This mirrors the add_race_transforms additions.
  </action>
  <verify>
    <automated>python -m pytest tests/test_hlf_features.py -v -k "haron"</automated>
  </verify>
  <done>
    - cols_horse includes "harontimel4"
    - _has_harontimel4 flag exists
    - expanding_stats_hl4 dict is computed (parallel to existing expanding_stats)
    - harontimel4_avg, harontimel4_zscore, harontimel4_trend appear in BASE_COLS and results dict
    - harontime_last3f_avg, harontime_last3f_zscore, harontime_last3f_trend appear in BASE_COLS and results dict
    - DISTANCE_THRESHOLD = 2000 constant at module level
    - add_race_transforms race_rank_cols extended with harontimel4_avg, harontime_last3f_avg
    - RacePredictor._race_rank_cols extended with same 2 columns
    - harontimel4 is backward compatible (NaN when column absent from data)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HLF-03 LapTime pace features + HLF-04 model registration + HLF-05 dual-path verification</name>
  <files>src/features/horse_history_features.py, tests/test_hlf_features.py</files>
  <read_first>
    src/features/horse_history_features.py (focus on compute() past_df merge with races_hist at lines 430-450, per-horse loop starting at line 650, results dict at line 1261)
    src/db/readers.py (load_history_races returns races with laptime columns)
    src/domain/types.py (POST_RACE_COLS line 38 -- laptime1~25 are POST_RACE)
    .planning/phases/36-feature-computation/36-CONTEXT.md (D-04, D-05, D-06)
    .planning/phases/36-feature-computation/36-RESEARCH.md (Pitfalls 2, 3, 7)
  </read_first>
  <behavior>
    - Test 1: compute() returns pace_ratio_avg as NaN when no past races have LapTime data (backward compat)
    - Test 2: compute() returns pace_ratio_avg as valid float when past races have laptime data and n_laps >= 3
    - Test 3: pace_ratio computed correctly: late segment avg / early segment avg
    - Test 4: All 12 model FEATURE_COLS contain all HLF features (harontimel4_avg/zscore/trend, harontime_last3f_avg/zscore/trend, pace_ratio_avg/zscore/trend, pace_early_avg, pace_late_avg)
    - Test 5: POST_RACE leakage test: None of the new HLF feature names appear in POST_RACE_COLS
  </behavior>
  <action>
    **Part A: Pre-compute LapTime pace_ratio per past race**

    The key challenge: LapTime1~25 live in races_hist (not entries_hist). The compute() method already loads races_hist via _get_history() and merges with entries at line 446. The merged past_df already has race-level columns including kyori and (after Phase 35) laptime1~25.

    Before the per-horse loop (before line 647), pre-compute a per-horse array of past pace_ratio values:

    1. Check if laptime columns exist: `_has_laptime = any(c in past_df_sorted.columns for c in ["laptime1", "laptime2"])`

    2. If _has_laptime: Build a per-kettonum lookup of past race pace_ratios. For each past race row that has valid laptime data:
       - n_laps = int(kyori / 200) if kyori is valid and >= 600, else skip
       - Extract laptime values: laptimes = [past_df_row[f"laptime{i}"] for i in range(1, n_laps+1)]
       - Skip if any laptime is NaN or n_laps < 3
       - Split into 3 segments using np.array_split
       - Compute early_avg = nanmean(segments[0]), late_avg = nanmean(segments[2])
       - Compute pace_ratio = late_avg / early_avg if early_avg > 0
       - Also store early_avg and late_avg as individual values

    3. Store in a dict: `_pace_lookup: dict[str, dict]` where key is kettonum and value is dict with keys "pace_ratios" (sorted by race_date), "early_avgs", "late_avgs", "race_dates" -- all as numpy arrays, sorted by race_date.

    For performance, compute this vectorized on past_df_sorted rather than per-row. Group by kettonum, iterate groups, compute pace_ratio for each row in the group.

    **Part B: Compute LapTime history features in per-horse loop**

    After the HaronTime L4/unified stats computation (from Task 1), add LapTime pace feature computation:

    1. Look up this horse's pace_ratio history: `_pace_data = _pace_lookup.get(ketto, None)`

    2. If _pace_data exists and has valid entries before target_date:
       - Use searchsorted on race_dates array to get PIT-safe cutoff (same pattern as line 669)
       - Extract past pace_ratios, early_avgs, late_avgs

    3. Compute 6 features:
       - `pace_ratio_avg`: EMA(halflife=3) of past pace_ratio values. Same EMA pattern as harontimel5_avg.
       - `pace_ratio_zscore`: Use expanding_stats (can use L3 expanding stats as proxy, or compute separate pace_ratio expanding_stats). Simplest: use hierarchical expanding_stats with pace_ratio values grouped by distance_bin. If that is too complex for this context, use global z-score (mean/std of all past pace_ratios for this horse).
       - `pace_ratio_trend`: Linear regression slope of last 3 pace_ratio values. np.polyfit pattern.
       - `pace_early_avg`: Simple mean of past early_avg values (not EMA, since segment averages are already smoothed)
       - `pace_late_avg`: Simple mean of past late_avg values
       - `pace_ratio` (current race's pace_ratio is POST_RACE, so this is NOT a feature -- only past pace_ratios are used)

    4. All default to float("nan") when no pace data available.

    **Part C: Add to BASE_COLS and results dict**

    Add to BASE_COLS:
    - "pace_ratio_avg"
    - "pace_ratio_zscore"
    - "pace_ratio_trend"
    - "pace_early_avg"
    - "pace_late_avg"

    Add to results.append({}) dict with corresponding variable names.

    **Part D: Register HLF features in 12 model FEATURE_COLS (HLF-04)**

    Add these HLF feature names to all 12 model FEATURE_COLS (same target files as Plan 01 Task 2 Part B):

    HaronTime features (6):
    - "harontimel4_avg"
    - "harontimel4_zscore"
    - "harontimel4_trend"
    - "harontime_last3f_avg"
    - "harontime_last3f_zscore"
    - "harontime_last3f_trend"

    LapTime pace features (5):
    - "pace_ratio_avg"
    - "pace_ratio_zscore"
    - "pace_ratio_trend"
    - "pace_early_avg"
    - "pace_late_avg"

    Also add to the HaronTime race_rank features that produce *_race_rank columns (already handled via add_race_transforms):
    - The race_rank columns (harontimel4_avg_race_rank, harontime_last3f_avg_race_rank) are generated dynamically by add_race_transforms and DO NOT need to be in FEATURE_COLS (same as existing race_rank columns -- they are not explicitly listed in model FEATURE_COLS, they are picked up by the [c for c in FEATURE_COLS if c in df.columns] pattern... WAIT -- actually looking at the existing code, race_rank columns ARE in FEATURE_COLS. Verify this.)

    Check existing model FEATURE_COLS for "norm_finish_logit_avg_race_rank" -- if race_rank columns are listed, then add the new ones too. If not, they are dynamically derived and don't need explicit listing. The executor must check and follow the existing pattern.

    **Part E: Create tests/test_hlf_features.py**

    Create comprehensive test file covering:
    - HaronTime L4 stats (avg/zscore/trend) with mock data
    - harontime_last3f unified column distance-based selection
    - LapTime pace_ratio computation with mock laptime data
    - All 12 model FEATURE_COLS contain all HLF features (parameterized test)
    - POST_RACE safety: HLF feature names are NOT in POST_RACE_COLS
    - PIT-safety: pace_ratio features use only past-race data (verify with date boundary)
  </action>
  <verify>
    <automated>python -m pytest tests/test_hlf_features.py -v</automated>
  </verify>
  <done>
    - LapTime pace features computed from past races only (PIT-safe via searchsorted)
    - pace_ratio_avg/zscore/trend, pace_early_avg, pace_late_avg in BASE_COLS and results
    - All 11 HLF features (6 HaronTime + 5 LapTime) registered in all 12 model FEATURE_COLS
    - HaronTime L4 features backward compatible (NaN when harontimel4 absent)
    - LapTime features backward compatible (NaN when laptime columns absent)
    - All tests pass including existing test suite
    - POST_RACE leakage test confirms no new features overlap with POST_RACE_COLS
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| past_df -> current race | HaronTime/LapTime from past races is PIT-safe; current race values are POST_RACE |
| expanding_stats lookup | Pre-computed stats use searchsorted for PIT-safe date boundary |
| LapTime current-race access | LapTime1~25 are POST_RACE_COLS; MUST only access from past_df, never current race |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-03 | Information Disclosure | HaronTime/LapTime features | mitigate | searchsorted(target_date, side="left") ensures strict < comparison; current-race data never accessed |
| T-36-04 | Tampering | LapTime pace_ratio | mitigate | pace_ratio computed only from past races in _pace_lookup; current race LapTime never accessed |
| T-36-05 | Tampering | expanding_stats | mitigate | expanding_stats pre-computed on sorted past_df; searchsorted lookup is read-only |
| T-36-SC | Tampering | pip installs | accept | No new packages in this phase |
</threat_model>

<verification>
1. python -m pytest tests/test_hlf_features.py -v -- all HLF tests pass
2. python -m pytest tests/test_post_race_leakage.py -v -- Layer 2 passes (no POST_RACE in FEATURE_COLS)
3. python -m pytest tests/test_horse_history_features.py -v -- existing tests still pass
4. ruff check src/features/horse_history_features.py -- no errors
5. Verify harontimel4 backward compat: if harontimel4 column missing from data, all L4 features are NaN (no KeyError)
</verification>

<success_criteria>
- harontimel4_avg, harontimel4_zscore, harontimel4_trend in BASE_COLS and compute() output
- harontime_last3f_avg, harontime_last3f_zscore, harontime_last3f_trend in BASE_COLS and compute() output
- DISTANCE_THRESHOLD = 2000 constant at module level, used in unified column logic
- pace_ratio_avg, pace_ratio_zscore, pace_ratio_trend, pace_early_avg, pace_late_avg in BASE_COLS and compute() output
- add_race_transforms generates harontimel4_avg_race_rank and harontime_last3f_avg_race_rank
- RacePredictor._race_rank_cols mirrors new HLF-02 source columns
- All 11 HLF features registered in all 12 model FEATURE_COLS
- All tests pass, no POST_RACE leakage
</success_criteria>

<output>
Create `.planning/phases/36-feature-computation/36-02-SUMMARY.md` when done
</output>
