---
phase: 36.1-harontime-l4-laptime-feature-redesign-backtest-engine-hist-f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/horse_history_features.py
  - src/backtest/race_predictor.py
  - tests/test_hlf_features.py
  - tests/test_horse_history_features.py
autonomous: true
requirements:
  - RED-01
  - RED-02
  - RED-03
  - RED-04

must_haves:
  truths:
    - "race_cols_all に harontimel4 が含まれ、races.parquet からレースレベルL4が取得される"
    - "closing_speed_ratio = harontimel3 / harontimel4_race が過去レースで計算される（L3有効 AND L4有効のレースのみ）"
    - "haron_race_gap = harontimel3 - harontimel4_race * 0.75 が過去レースで計算される"
    - "pace_adj_finish_avg = norm_finish * pace_ratio の過去平均が計算される"
    - "harontime_last3f は常に harontimel3 ベースで計算され DISTANCE_THRESHOLD 分岐が撤廃される"
    - "harontimel4_avg/zscore/trend が closing_speed_ratio_avg/zscore/trend に置換される"
  artifacts:
    - path: "src/features/horse_history_features.py"
      provides: "closing_speed_ratio_avg/zscore/trend, haron_race_gap_avg/zscore/trend, pace_adj_finish_avg, harontime_last3f L3-only"
      contains: "closing_speed_ratio_avg"
    - path: "src/backtest/race_predictor.py"
      provides: "_race_rank_cols updated: harontimel4_avg -> closing_speed_ratio_avg"
  key_links:
    - from: "src/features/horse_history_features.py race_cols_all"
      to: "races.parquet harontimel4"
      via: "merge on race_id"
      pattern: "harontimel4.*race_cols_all"
    - from: "horse_history_features.py per-horse loop"
      to: "closing_speed_ratio computation"
      via: "L3 / L4 race-level values"
      pattern: "closing_speed_ratio"
---

<objective>
HaronTime L4 データソース修正とクロスレベル派生特徴量の新規実装

Purpose: Phase 36 で発見された harontimel4 データソース誤り（entries 全NaNではなく races のレースレベルデータを使用すべき）を修正し、L3/L4 の比率・差分から高価値なクロスレベル派生特徴量（closing_speed_ratio, haron_race_gap, pace_adj_finish）を新規実装する。同時に harontime_last3f の L3 ベース統一と harontimel4 系特徴量の closing_speed_ratio 系への置換を行う。

Output: 20個のHaron/Lap特徴量（Tier1=7新規、Tier2=6既存保持、Tier3=7修正済み）が PIT-safe に計算される horse_history_features.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.1-harontime-l4-laptime-feature-redesign-backtest-engine-hist-f/36.1-CONTEXT.md
@.planning/phases/36.1-harontime-l4-laptime-feature-redesign-backtest-engine-hist-f/36.1-RESEARCH.md
@.planning/phases/36-feature-computation/36-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/features/horse_history_features.py:
- BASE_COLS (line 285-360): list[str] — all feature column names, currently 62 entries
- DISTANCE_THRESHOLD (line 59): int = 2000 — to be REMOVED per D-07
- race_cols_all (line 453-463): list of race-level columns merged from races_hist — "harontimel4" to be ADDED per D-09
- cols_horse (line 524-544): list of horse-level columns for numpy array lookup — "harontimel4" stays per D-10
- expanding_stats (line 607-668): dict for harontimel3 z-score
- expanding_stats_hl4 (line 674-728): dict for harontimel4 z-score — to be REBUILT for closing_speed_ratio
- _pace_lookup (line 741-815): per-horse pace history from LapTime — pace_adj_finish uses this
- Per-horse loop (line 820-1782): main computation loop
- harontimel4_avg/zscore/trend (line 1017-1083): to be REPLACED by closing_speed_ratio
- harontime_last3f (line 1089-1182): to be SIMPLIFIED to L3-only per D-07
- results dict (line 1706-1782): output mapping

From src/backtest/race_predictor.py:
- _race_rank_cols (line 84-99): list[str] — harontimel4_avg -> closing_speed_ratio_avg
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-09 データソース修正 + D-07 harontime_last3f L3統一 + D-08 harontimel4 置換</name>
  <files>src/features/horse_history_features.py, src/backtest/race_predictor.py, tests/test_hlf_features.py</files>
  <read_first>
    src/features/horse_history_features.py (lines 453-463 race_cols_all, lines 59 DISTANCE_THRESHOLD, lines 1017-1182 harontimel4 + harontime_last3f sections, lines 285-360 BASE_COLS, lines 1706-1782 results dict, lines 1788-1821 add_race_transforms)
    src/backtest/race_predictor.py (lines 84-99 _race_rank_cols)
    tests/test_hlf_features.py (existing test patterns)
    .planning/phases/36.1-harontime-l4-laptime-feature-redesign-backtest-engine-hist-f/36.1-CONTEXT.md (D-07, D-08, D-09, D-10 decisions)
  </read_first>
  <behavior>
    - Test 1: race_cols_all に "harontimel4" が含まれることを確認
    - Test 2: harontime_last3f が L3 ベースのみで計算されることを確認（L4 データが存在しても使用されない）
    - Test 3: DISTANCE_THRESHOLD 定数が参照されなくなったことを確認（定数自体は残してもよいが、last3f 計算で使用されない）
    - Test 4: BASE_COLS に closing_speed_ratio_avg/zscore/trend が含まれ、harontimel4_avg/zscore/trend が含まれない
    - Test 5: results dict のキーが harontimel4_* から closing_speed_ratio_* に置換される
    - Test 6: race_predictor._race_rank_cols で harontimel4_avg が closing_speed_ratio_avg に置換される
  </behavior>
  <action>
    **D-09: race_cols_all に "harontimel4" を追加 (per D-09)**
    horse_history_features.py の race_cols_all リスト（line 453-463）に "harontimel4" を追加。これにより races_hist からのマージに harontimel4 列が含まれ、past_df 経由で per-horse ループに race-level L4 が供給される。LapTime pace（line 731-815）と同じアプローチ。

    **D-07: harontime_last3f を L3 ベース統一 (per D-07)**
    harontime_last3f セクション（line 1089-1182）の DISTANCE_THRESHOLD 分岐ロジックを撤廃。l3_raw のみを使用し l4_raw からのフォールバックを削除。unified_raw は常に l3_raw に等しくなる。DISTANCE_THRESHOLD 定数自体は他から参照されていないことを確認の上、削除（またはコメントアウト）。

    具体的な変更:
    1. DISTANCE_THRESHOLD 定数（line 59）を削除
    2. harontime_last3f セクションで current_kyori チェックと L4/L3 切替ロジックを削除
    3. unified_raw を常に l3_raw に設定（l4_raw の取得自体を削除）
    4. harontime_last3f_avg/zscore/trend の計算ロジックは EMA/zscore/trend パターンを維持（L3 データのみで計算）

    **D-08: harontimel4_avg/zscore/trend を closing_speed_ratio_avg/zscore/trend に置換 (per D-08)**
    1. expanding_stats_hl4 セクション（line 674-728）を削除（harontimel4 の直接 z-score は不要。closing_speed_ratio 用の expanding_stats は Task 2 で追加）
    2. harontimel4_avg/zscore/trend 計算セクション（line 1017-1083）を closing_speed_ratio 計算に置換。ただし、このタスクでは harontimel4_avg/zscore/trend を NaN プレースホルダに置き、closing_speed_ratio 計算は Task 2 で実装する。あるいは、このタスクで closing_speed_ratio の計算も併せて実装する（Task 2 の分量次第）。

    **方針:** このタスクでは以下の変更を一括実施する:
    1. D-09: race_cols_all に "harontimel4" 追加
    2. D-07: harontime_last3f L3-only 統一
    3. D-08 準備: BASE_COLS で harontimel4_avg/zscore/trend → closing_speed_ratio_avg/zscore/trend にリネーム。results dict キーも同様。add_race_transforms の race_rank_cols で harontimel4_avg → closing_speed_ratio_avg。race_predictor._race_rank_cols で harontimel4_avg → closing_speed_ratio_avg。
    4. 旧 harontimel4_avg/zscore/trend の計算コードを、closing_speed_ratio の計算コードに置換（Task 2 で closing_speed_ratio の実装を追加するため、ここではプレースホルダとして NaN を返す設定にする）
    5. expanding_stats_hl4 を削除し、closing_speed_ratio 用の expanding_stats_csr プレースホルダを追加

    **race_predictor.py:**
    _race_rank_cols（line 84-99）の "harontimel4_avg" を "closing_speed_ratio_avg" に変更。
    "harontime_last3f_avg" はそのまま（L3-only 統一後も名前は変更なし）。
  </action>
  <verify>
    <automated>python -m pytest tests/test_hlf_features.py tests/test_horse_history_features.py -v -x 2>&1 | tail -30</automated>
  </verify>
  <done>
    - race_cols_all に "harontimel4" が含まれる
    - harontime_last3f が常に harontimel3 のみで計算される（DISTANCE_THRESHOLD 分岐なし）
    - BASE_COLS に closing_speed_ratio_avg/zscore/trend が含まれ、harontimel4_avg/zscore/trend が含まれない
    - results dict のキーが closing_speed_ratio_* に更新される
    - race_predictor._race_rank_cols の "harontimel4_avg" が "closing_speed_ratio_avg" に更新される
    - 全テストが通過する
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-01 closing_speed_ratio + D-02 haron_race_gap + D-03 pace_adj_finish の新規実装</name>
  <files>src/features/horse_history_features.py, tests/test_hlf_features.py, tests/test_horse_history_features.py</files>
  <read_first>
    src/features/horse_history_features.py (lines 1230-1244 closing_index_avg パターン、lines 1017-1083 現在の harontimel4/closing_speed_ratio セクション、lines 731-815 _pace_lookup、lines 1644-1704 LapTime pace features セクション、lines 285-360 BASE_COLS)
    tests/test_hlf_features.py (既存テストパターン)
    .planning/phases/36.1-harontime-l4-laptime-feature-redesign-backtest-engine-hist-f/36.1-CONTEXT.md (D-01, D-02, D-03, specifics セクションの計算例)
    .planning/phases/36.1-harontime-l4-laptime-feature-redesign-backtest-engine-hist-f/36.1-RESEARCH.md (Pattern 1: closing_index_avg, Pattern 3: EMA, Pitfall 1-2)
  </read_first>
  <behavior>
    - Test 1: closing_speed_ratio = L3/L4 が正しく計算される（L3=34.5, L4=46.0 → ratio=0.750）
    - Test 2: closing_speed_ratio_avg が EMA(halflife=3) で計算される
    - Test 3: closing_speed_ratio_zscore が expanding_stats を用いた PIT-safe z-score で計算される
    - Test 4: closing_speed_ratio_trend が線形回帰 slope で計算される
    - Test 5: L3 または L4 が NaN の場合、closing_speed_ratio が NaN になる
    - Test 6: haron_race_gap = L3 - L4*0.75 が正しく計算される（L3=34.5, L4=46.0 → gap=0.0）
    - Test 7: haron_race_gap_avg/zscore/trend が正しく計算される
    - Test 8: pace_adj_finish_avg = norm_finish * pace_ratio の過去平均が計算される
    - Test 9: BASE_COLS に haron_race_gap_avg/zscore/trend, pace_adj_finish_avg が含まれる
    - Test 10: BASE_COLS の総数が正しい（旧62 - 3削除 + 7追加 = 66）
  </behavior>
  <action>
    **D-01: closing_speed_ratio 実装 (per D-01)**
    per-horse ループ内の closing_speed_ratio セクション（Task 1 でプレースホルダ化済み）に実装を追加。closing_index_avg（line 1230-1244）のパターンに従う。

    実装詳細:
    1. expanding_stats_csr: closing_speed_ratio 用の expanding_stats を新規 dict で構築。past_df に一時列 "closing_speed_ratio" = harontimel3 / harontimel4 を追加してから、FALLBACK_LEVELS と同じパターンで expanding mean/std を計算（既存 expanding_stats のパターンと同一、別 dict で key collision を回避 per RESEARCH Pitfall 2）
    2. per-hose loop 内: L3 と L4 の両方が有効な過去レースで ratio = L3/L4 を計算（L4 > 0 ガード付き）。有効な ratio 値に対して EMA(halflife=3) で avg、trend（線形回帰 slope）を計算。zscore は expanding_stats_csr を searchsorted で PIT-safe に参照
    3. NaN 処理: L3 NaN または L4 NaN のレースは除外。LightGBM が NaN を処理可能なため、fallback 値は設定しない（per RESEARCH Pitfall 1）

    **D-02: haron_race_gap 実装 (per D-02)**
    同じ per-horse ループ内で haron_race_gap = harontimel3 - harontimel4 * 0.75 を計算。
    スケール係数 0.75 は 3F/4F = 0.75（物理的に正確 per RESEARCH Assumption A2）。

    実装詳細:
    1. expanding_stats_hrg: haron_race_gap 用の expanding_stats を別 dict で構築。past_df に一時列 "haron_race_gap" = harontimel3 - harontimel4 * 0.75 を追加してから、FALLBACK_LEVELS パターンで構築
    2. per-hose loop 内: L3 と L4 の両方が有効な過去レースで gap = L3 - L4*0.75 を計算。EMA(halflife=3) avg、trend、zscore を計算（closing_speed_ratio と同一パターン）

    **D-03: pace_adj_finish_avg 実装 (per D-03)**
    pace_adj_finish = norm_finish * pace_ratio の過去平均。既存 _pace_lookup から pace_ratio を取得し、個人の着順データ（kakuteijyuni, syussotosu）と組み合わせる。

    実装詳細:
    1. _pace_lookup は pace_ratios を race_dates 順に格納済み
    2. per-hose loop 内: _pace_lookup[ketto] から過去の pace_ratios を取得（searchsorted で PIT-safe）。同じ期間の hp_kakuteijyuni, hp_syussotosu から norm_finish = (kj-1)/(ss-1) を計算。pace_adj_finish = norm_finish * pace_ratio をレースごとに計算し、平均を取る
    3. 重要: pace_dates と valid_mask[start:idx] のインデックスが一致しない可能性あり（RESEARCH Open Question 1）。pace_lookup の構築は entries_hist の race_id ベースで行われるため、race_id マッチングで着順データと pace データを結合する必要がある。具体的には: past_df から各馬の過去レースの (race_id, norm_finish, pace_ratio) を結合し、target_date より前のレースのみで平均を計算する

    **BASE_COLS 更新:**
    追加: "closing_speed_ratio_avg", "closing_speed_ratio_zscore", "closing_speed_ratio_trend"（Task 1 で追加済み）, "haron_race_gap_avg", "haron_race_gap_zscore", "haron_race_gap_trend", "pace_adj_finish_avg"
    削除: "harontimel4_avg", "harontimel4_zscore", "harontimel4_trend"（Task 1 で削除済み）
    最終: 旧62 - 3 + 7 = 66 entries（Task 1 で 62-3+3=62、この Task で +4 = 66）

    **results dict 更新:**
    追加: "haron_race_gap_avg", "haron_race_gap_zscore", "haron_race_gap_trend", "pace_adj_finish_avg"

    **add_race_transforms の race_rank_cols 更新:**
    追加: "closing_speed_ratio_avg"（Task 1 で追加済み）。haron_race_gap_avg は追加しない（closing_speed_ratio が既に代表として race_rank に含まれる）。
  </action>
  <verify>
    <automated>python -m pytest tests/test_hlf_features.py tests/test_horse_history_features.py -v -x 2>&1 | tail -30</automated>
  </verify>
  <done>
    - closing_speed_ratio_avg/zscore/trend が L3/L4 比率から正しく計算される
    - haron_race_gap_avg/zscore/trend が L3 - L4*0.75 から正しく計算される
    - pace_adj_finish_avg が norm_finish * pace_ratio の過去平均として計算される
    - 全て PIT-safe（searchsorted で current-race データを含まない）
    - BASE_COLS が66 entries（旧 harontimel4_* 3個削除、新規7個追加）
    - 全テストが通過する
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| races_hist → past_df | レースレベルデータ（harontimel4）が馬特徴量計算に流入。POST_RACE_COL だが searchsorted で PIT-safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.1-01 | Information Disclosure | harontimel4 in past_df | mitigate | searchsorted(target_date, side="left") で未来データを排除。POST_RACE_COLS として domain/types.py に登録済み |
| T-36.1-02 | Tampering | closing_speed_ratio NaN率 | accept | L4 56%カバレッジ → NaNはLightGBMが処理。fallback値は情報を薄めるため設定しない |
| T-36.1-SC | Tampering | pip installs | accept | 新規外部パッケージなし |
</threat_model>

<verification>
1. `python -m pytest tests/test_hlf_features.py tests/test_horse_history_features.py -v` — 全テスト通過
2. `python -c "from features.horse_history_features import HorseHistoryFeatures; h=HorseHistoryFeatures.__new__(HorseHistoryFeatures); print(len(h.BASE_COLS))"` — BASE_COLS が66であることを確認
3. `grep -c "closing_speed_ratio" src/features/horse_history_features.py` — closing_speed_ratio 参照が10箇所以上
4. `grep -c "harontimel4_avg" src/features/horse_history_features.py` — 旧名が0であることを確認（置換完了）
5. `grep "DISTANCE_THRESHOLD" src/features/horse_history_features.py` — 定数が存在しないか、last3f 計算で参照されていない
</verification>

<success_criteria>
- race_cols_all に "harontimel4" が含まれる（D-09）
- closing_speed_ratio_avg/zscore/trend が PIT-safe に計算される（D-01, D-08）
- haron_race_gap_avg/zscore/trend が PIT-safe に計算される（D-02）
- pace_adj_finish_avg が PIT-safe に計算される（D-03）
- harontime_last3f が常に L3 ベースで計算される（D-07）
- harontimel4_avg/zscore/trend が BASE_COLS から削除され、closing_speed_ratio 系に置換（D-08）
- BASE_COLS が66 entries
- 全テスト通過（275+ tests）
</success_criteria>

<output>
Create `.planning/phases/36.1-harontime-l4-laptime-feature-redesign-backtest-engine-hist-f/36.1-01-SUMMARY.md` when done
</output>
