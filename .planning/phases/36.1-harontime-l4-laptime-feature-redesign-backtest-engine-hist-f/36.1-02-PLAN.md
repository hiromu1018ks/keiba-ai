---
phase: 36.1-harontime-l4-laptime-feature-redesign-backtest-engine-hist-f
plan: 02
type: execute
wave: 2
depends_on:
  - 36.1-01
files_modified:
  - src/backtest/engine.py
  - src/models/stage1_ability_model.py
  - src/models/two_stage_return_model.py
  - src/models/ev_correction_model.py
  - src/models/conformal_ev_model.py
  - src/models/market_model.py
  - src/models/place_ability_model.py
  - src/models/race_quality_screener.py
  - src/models/regime_detector.py
  - src/models/wide_two_stage_model.py
  - tests/test_hlf_features.py
autonomous: true
requirements:
  - RED-05
  - RED-06

must_haves:
  truths:
    - "backtest engine で hist_df_all が feat_df に事前マージされ、テストデータに hist 特徴量が含まれる"
    - "predict() に hist_features=None を渡しても正常動作する（二重マージなし）"
    - "全10モデルクラスの FEATURE_COLS に closing_speed_ratio_avg/zscore/trend, haron_race_gap_avg/zscore/trend, pace_adj_finish_avg が登録される"
    - "全10モデルクラスの FEATURE_COLS から harontimel4_avg/zscore/trend が削除される"
  artifacts:
    - path: "src/backtest/engine.py"
      provides: "hist_df_all → feat_df pre-merge before predict()"
      contains: "hist_df_all.*merge"
    - path: "src/models/stage1_ability_model.py"
      provides: "FEATURE_COLS with closing_speed_ratio_* replacing harontimel4_*"
      contains: "closing_speed_ratio_avg"
  key_links:
    - from: "engine.py hist_df_all"
      to: "feat_df"
      via: "merge on race_id, umaban"
      pattern: "feat_df.*merge.*hist_df_all"
    - from: "engine.py predict() call"
      to: "hist_features=None"
      via: "pre-merged feat_df"
      pattern: "hist_features=None"
---

<objective>
Backtest engine の hist_features マージ修正 + 全モデル FEATURE_COLS 更新

Purpose: backtest engine で hist_df_all が feat_df に事前マージされず、テストデータで22特徴量が欠落するバグ（投票数2,463→888、ROI 97.8%→78.7%）を修正する。同時に、Plan 01 で新規実装・リネームされた特徴量を全10モデルクラスの FEATURE_COLS に登録・更新する。

Output: hist 特徴量が backtest parquet に反映される engine.py と、新特徴量名で統一された全モデルの FEATURE_COLS
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.1-harontime-l4-laptime-feature-redesign-backtest-engine-hist-f/36.1-CONTEXT.md
@.planning/phases/36.1-harontime-l4-laptime-feature-redesign-backtest-engine-hist-f/36.1-RESEARCH.md
@.planning/phases/36.1-harontime-l4-laptime-feature-redesign-backtest-engine-hist-f/36.1-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output — executor must understand these new feature names -->

New features added by Plan 01:
- closing_speed_ratio_avg, closing_speed_ratio_zscore, closing_speed_ratio_trend (D-01/D-08: replaces harontimel4_avg/zscore/trend)
- haron_race_gap_avg, haron_race_gap_zscore, haron_race_gap_trend (D-02: new)
- pace_adj_finish_avg (D-03: new)

Features removed by Plan 01:
- harontimel4_avg, harontimel4_zscore, harontimel4_trend (replaced by closing_speed_ratio_*)

Features renamed in race_rank:
- harontimel4_avg_race_rank → closing_speed_ratio_race_rank

From src/backtest/engine.py:
- hist_df_all computation (line 661-663): HorseHistoryFeatures.compute() result
- hist_groups (line 788): build_race_groups(hist_df_all)
- predict() call (line 873-879): hist_features=hist_df_race passed to RacePredictor

From src/backtest/race_predictor.py:
- predict() line 80-81: `if hist_features is not None: df = df.merge(hist_features, ...)`
- _race_rank_cols (line 84-99): already updated in Plan 01

Model files with FEATURE_COLS (from 36-02-SUMMARY.md):
- AbilityModel.FEATURE_COLS (stage1_ability_model.py)
- WinTwoStageModel.FEATURE_COLS (two_stage_return_model.py)
- PlaceTwoStageModel.HIT_FEATURE_COLS + RETURN_FEATURE_COLS (two_stage_return_model.py)
- EVCorrectionModel.FEATURE_COLS + PlaceEVCorrectionModel.FEATURE_COLS (ev_correction_model.py)
- ConformalEVModel.FEATURE_COLS (conformal_ev_model.py)
- MarketModel.FEATURE_COLS (market_model.py)
- PlaceAbilityModel.FEATURE_COLS (place_ability_model.py)
- RaceQualityScreener.FEATURE_COLS (race_quality_screener.py)
- RegimeDetector.FEATURE_COLS (regime_detector.py)
- WideTwoStageModel.SHARED_FEATURE_COLS (wide_two_stage_model.py)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-11 Backtest engine の hist_features 事前マージ修正</name>
  <files>src/backtest/engine.py, tests/test_backtest_engine.py</files>
  <read_first>
    src/backtest/engine.py (lines 650-676 hist_df_all 計算、lines 780-788 feat_groups/hist_groups 構築、lines 862-879 hist_df_race 使用箇所)
    src/backtest/race_predictor.py (lines 79-81 hist_features マージロジック)
    .planning/phases/36.1-harontime-l4-laptime-feature-redesign-backtest-engine-hist-f/36.1-CONTEXT.md (D-11 decision)
    .planning/phases/36.1-harontime-l4-laptime-feature-redesign-backtest-engine-hist-f/36.1-RESEARCH.md (Pitfall 3: 二重マージ回避)
  </read_first>
  <behavior>
    - Test 1: engine.run() 後、出力 feat_df に hist 特徴量列（closing_speed_ratio_avg 等）が含まれる
    - Test 2: predict() 呼び出し時に hist_features=None が渡される
    - Test 3: 二重マージが発生しない（出力に _x/_y サフィックス列が存在しない）
    - Test 4: hist_groups が互換性のため維持される（他のコンシューマーが参照する可能性）
  </behavior>
  <action>
    **D-11: hist_df_all を feat_df に事前マージ (per D-11)**

    engine.py の run() メソッドで以下の変更を行う:

    1. hist_df_all 計算後（line 663 直後）、feat_df に hist_df_all を事前マージ:
       - マージキー: ["race_id", "umaban"]
       - マージ方法: how="left"（hist_df_all に含まれない馬は NaN）
       - ログ出力: "Merged hist features into feat_df: %d columns"

    2. feat_groups 構築（line 787）は、hist 特徴量を含む feat_df から構築されるため、自動的に hist 特徴量を含む

    3. hist_groups（line 788）は互換性のため残す。他の箇所で参照されている可能性があるため、削除せず維持

    4. predict() 呼び出し（line 873-879）で hist_features 引数を hist_df_race から None に変更:
       - 事前マージ済みのため、predict() 内部でのマージは不要
       - 二重マージ（_x/_y サフィックス問題）を回避 per RESEARCH Pitfall 3

    5. POST_RACE_COLS ドロップ（line 868-871）で hist 特徴量の POST_RACE 列が含まれないことを確認（hist 特徴量自体は POST_RACE ではなく、hist_df_all からマージされた値）

    **注意:** feat_df に hist 特徴量をマージすると、feat_df のカラム数が増加する。POST_RACE_COLS ドロップで hist 特徴量が誤って削除されないことを確認。hist 特徴量名（closing_speed_ratio_avg 等）は POST_RACE_COLS に含まれないため、安全。

    テストは mock ベースで DB 不要。既存テストパターン（test_backtest_engine.py）を拡張。
  </action>
  <verify>
    <automated>python -m pytest tests/test_backtest_engine.py tests/test_hlf_features.py -v -x -k "hist or merge or engine" 2>&1 | tail -30</automated>
  </verify>
  <done>
    - feat_df に hist_df_all が事前マージされる
    - predict() に hist_features=None が渡される
    - 二重マージが発生しない
    - テストが通過する
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED-06 全10モデルクラスの FEATURE_COLS 更新</name>
  <files>
    src/models/stage1_ability_model.py,
    src/models/two_stage_return_model.py,
    src/models/ev_correction_model.py,
    src/models/conformal_ev_model.py,
    src/models/market_model.py,
    src/models/place_ability_model.py,
    src/models/race_quality_screener.py,
    src/models/regime_detector.py,
    src/models/wide_two_stage_model.py,
    tests/test_hlf_features.py
  </files>
  <read_first>
    src/models/stage1_ability_model.py (FEATURE_COLS section, ~line 160)
    src/models/two_stage_return_model.py (FEATURE_COLS in WinTwoStageModel ~line 163, PlaceTwoStageModel ~line 496, ~line 652)
    src/models/ev_correction_model.py (FEATURE_COLS in EVCorrectionModel, PlaceEVCorrectionModel)
    src/models/conformal_ev_model.py (FEATURE_COLS)
    src/models/market_model.py (FEATURE_COLS)
    src/models/place_ability_model.py (FEATURE_COLS)
    src/models/race_quality_screener.py (FEATURE_COLS)
    src/models/regime_detector.py (FEATURE_COLS)
    src/models/wide_two_stage_model.py (SHARED_FEATURE_COLS)
    tests/test_hlf_features.py (既存モデル登録テストパターン)
  </read_first>
  <behavior>
    - Test 1: 全モデルの FEATURE_COLS に "closing_speed_ratio_avg", "closing_speed_ratio_zscore", "closing_speed_ratio_trend" が含まれる
    - Test 2: 全モデルの FEATURE_COLS に "haron_race_gap_avg", "haron_race_gap_zscore", "haron_race_gap_trend" が含まれる
    - Test 3: 全モデルの FEATURE_COLS に "pace_adj_finish_avg" が含まれる
    - Test 4: 全モデルの FEATURE_COLS に "harontimel4_avg", "harontimel4_zscore", "harontimel4_trend" が含まれない
    - Test 5: 全モデルの race_rank 系 FEATURE_COLS に "closing_speed_ratio_avg_race_rank" が含まれる（harontimel4_avg_race_rank の代わり）
    - Test 6: 既存の pace_ratio_avg/zscore/trend, pace_early/mid/late_avg, harontimel3 系が全モデルに維持される
    - Test 7: FEATURE_COLS の総数が旧数 - 3(harontimel4) - 1(harontimel4_race_rank) + 3(closing_speed_ratio) + 3(haron_race_gap) + 1(pace_adj_finish) + 1(closing_speed_ratio_race_rank) = 旧数 + 4 になる
  </behavior>
  <action>
    **全10モデルクラスの FEATURE_COLS を一括更新:**

    各モデルの FEATURE_COLS（または SHARED_FEATURE_COLS）リストで以下の置換・追加を行う:

    **削除:**
    - "harontimel4_avg"
    - "harontimel4_zscore"
    - "harontimel4_trend"

    **追加:**
    - "closing_speed_ratio_avg" （harontimel4_avg の位置に配置）
    - "closing_speed_ratio_zscore" （harontimel4_zscore の位置に配置）
    - "closing_speed_ratio_trend" （harontimel4_trend の位置に配置）
    - "haron_race_gap_avg"
    - "haron_race_gap_zscore"
    - "haron_race_gap_trend"
    - "pace_adj_finish_avg"

    **race_rank 置換（stage1, two_stage Win/Place, ev_correction Win/Place のみ）:**
    - "harontimel4_avg_race_rank" → "closing_speed_ratio_avg_race_rank"

    **対象モデルファイル（12 FEATURE_COLS リスト）:**
    1. stage1_ability_model.py: AbilityModel.FEATURE_COLS + race_rank 系
    2. two_stage_return_model.py: WinTwoStageModel.FEATURE_COLS + PlaceTwoStageModel.HIT_FEATURE_COLS + RETURN_FEATURE_COLS + 各 race_rank 系
    3. ev_correction_model.py: EVCorrectionModel.FEATURE_COLS + PlaceEVCorrectionModel.FEATURE_COLS
    4. conformal_ev_model.py: ConformalEVModel.FEATURE_COLS
    5. market_model.py: MarketModel.FEATURE_COLS
    6. place_ability_model.py: PlaceAbilityModel.FEATURE_COLS
    7. race_quality_screener.py: RaceQualityScreener.FEATURE_COLS
    8. regime_detector.py: RegimeDetector.FEATURE_COLS
    9. wide_two_stage_model.py: WideTwoStageModel.SHARED_FEATURE_COLS

    **一貫性確認:** test_hlf_features.py の既存モデル登録テスト（140 tests）を新特徴量名で更新し、全テスト通過を確認。
  </action>
  <verify>
    <automated>python -m pytest tests/test_hlf_features.py -v -x -k "model" 2>&1 | tail -30</automated>
  </verify>
  <done>
    - 全12 FEATURE_COLS リストに closing_speed_ratio_avg/zscore/trend, haron_race_gap_avg/zscore/trend, pace_adj_finish_avg が含まれる
    - 全12 FEATURE_COLS リストから harontimel4_avg/zscore/trend が削除される
    - race_rank 系で harontimel4_avg_race_rank が closing_speed_ratio_avg_race_rank に置換される
    - 140+ モデル登録テストが全て通過する
    - 既存の pace_ratio, harontimel3 系特徴量が維持される
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feat_df + hist_df_all merge | backtest engine 内での DataFrame 結合。型不一致や重複列のリスク |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.1-03 | Tampering | feat_df merge columns | mitigate | how="left" + on=["race_id","umaban"] で意図しない行増殖を防止 |
| T-36.1-04 | Information Disclosure | hist_features=None in predict | accept | predict() の `if hist_features is not None` ガードが安全にスキップする |
| T-36.1-05 | Tampering | FEATURE_COLS inconsistency | mitigate | 全10ファイル横断更新 + 140テストで一貫性検証 |
</threat_model>

<verification>
1. `python -m pytest tests/test_hlf_features.py tests/test_horse_history_features.py tests/test_backtest_engine.py -v` — 全テスト通過
2. `grep -r "harontimel4_avg" src/models/ | grep -v "closing_speed_ratio" | grep -v "__pycache__"` — モデルファイルに旧名が0件
3. `grep -r "closing_speed_ratio_avg" src/models/ | grep -v "__pycache__" | wc -l` — 新名が10箇所以上
4. `grep -c "hist_features=None" src/backtest/engine.py` — predict() 呼び出しで None が1件以上
</verification>

<success_criteria>
- engine.py で hist_df_all が feat_df に事前マージされる（D-11）
- predict() に hist_features=None が渡される
- 全10モデルクラスの FEATURE_COLS が新特徴量名で統一される（RED-06）
- harontimel4_avg/zscore/trend が全モデルから削除される
- 275+ テストが通過する
</success_criteria>

<output>
Create `.planning/phases/36.1-harontime-l4-laptime-feature-redesign-backtest-engine-hist-f/36.1-02-SUMMARY.md` when done
</output>
