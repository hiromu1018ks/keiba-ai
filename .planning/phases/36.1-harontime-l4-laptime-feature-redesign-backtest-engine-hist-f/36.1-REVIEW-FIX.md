---
phase: 36.1
fixed_at: "2026-05-20T05:40:00Z"
review_path: ".planning/phases/36.1-harontime-l4-laptime-feature-redesign-backtest-engine-hist-f/36.1-REVIEW.md"
iteration: 1
findings_in_scope: 8
fixed: 3
skipped: 5
status: partial
---

# Phase 36.1: Code Review Fix Report

**Fixed at:** 2026-05-20T05:40:00Z
**Source review:** .planning/phases/36.1-harontime-l4-laptime-feature-redesign-backtest-engine-hist-f/36.1-REVIEW.md
**Iteration:** 1

**Summary:**
- Findings in scope: 8 (1 Critical, 7 Warnings)
- Fixed: 3
- Skipped: 5

## Fixed Issues

### CR-01: Dead-code filter condition produces unintended column list

**Files modified:** `src/features/horse_history_features.py`
**Commit:** 621eb25
**Applied fix:** Replaced hardcoded `entries_filtered.drop(columns=["race_date"], errors="ignore")` with a dynamic filter `_race_only_cols = [c for c in ["race_date", "harontimel4"] if c in entries_filtered.columns]` that only drops columns actually present in the DataFrame. This correctly handles both `race_date` and `harontimel4` while avoiding silent failures when columns are absent.

### WR-02: In-place DataFrame mutation via column assignment in conformal_ev_model

**Files modified:** `src/models/conformal_ev_model.py`
**Commit:** 6989a21
**Applied fix:** Replaced the `for c in missing: win_df[c] = 0.0` loop with explicit `pd.concat` of a fresh DataFrame for missing feature columns. This avoids in-place mutation of `win_df` and makes the missing-column fill safe against future refactoring.

### WR-03: Potential index misalignment in race_predictor place column transfer

**Files modified:** `src/backtest/race_predictor.py`
**Commit:** 4227696
**Applied fix:** Removed `.values` from `place_df["EV_lower_place"].reindex(df.index).values` so pandas alignment checks are preserved rather than bypassed by raw numpy array transfer.

## Skipped Issues

### WR-01: Class-level mutable cache grows without bounds

**File:** `src/features/horse_history_features.py:280`
**Reason:** Design-level change requiring LRU eviction strategy. Affects production memory behavior and needs careful testing.
**Original issue:** `HorseHistoryFeatures._class_cache` grows without bounds during training pipeline execution.

### WR-04: Duplicated _safe_feature_cols helper across 4 model files

**File:** `src/models/market_model.py`, `src/models/regime_detector.py`, `src/models/race_quality_screener.py`, `src/models/wide_two_stage_model.py`
**Reason:** Larger refactoring across 4 files that risks breaking imports. Should be done as a separate refactoring task.
**Original issue:** `_safe_feature_cols` is copy-pasted with minor variations; `race_quality_screener.py` omits `.copy()`.

### WR-05: Class cache key includes id() which is non-deterministic across runs

**File:** `src/features/horse_history_features.py:397`
**Reason:** Design-level change that could affect caching behavior. Risk of breaking the cache outweighs the benefit of fixing this in isolation.
**Original issue:** `cache_key = f"{id(past_by_ketto_arr)}_{ketto}"` uses non-deterministic `id()`.

### WR-06: _compute_haron_stats returns NaN z-score placeholder

**File:** `src/features/horse_history_features.py:262-266`
**Reason:** Intentional design pattern documented in comments. The NaN placeholder is explicitly filled in by the `compute()` method. Needs documentation, not a code fix.
**Original issue:** Function returns partially computed results with NaN placeholder for z-score.

### WR-07: EMA weight formula differs from standard EMA convention

**File:** `src/features/horse_history_features.py:1097`
**Reason:** Changing the EMA formula post-training would invalidate existing model results. This is a numerical behavior change that should only be done with a full retrain cycle.
**Original issue:** Weight formula uses `(1 - decay)` base which differs slightly from standard EMA convention used by pandas.

---

_Fixed: 2026-05-20T05:40:00Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
