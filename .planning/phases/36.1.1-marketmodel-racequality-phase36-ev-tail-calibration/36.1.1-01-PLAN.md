---
phase: 36.1.1-marketmodel-racequality-phase36-ev-tail-calibration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/market_model.py
  - src/models/race_quality_screener.py
  - src/models/regime_detector.py
  - tests/test_market_model.py
  - tests/test_race_quality_screener.py
  - tests/test_regime_detector.py
autonomous: true
requirements: [RTG-01]

must_haves:
  truths:
    - "MarketModel.FEATURE_COLSにPhase36 fundamental特徴量(TRF/INT/HLF/D-02/D-03)が1つも含まれない"
    - "RaceQualityScreener.FEATURE_COLSにPhase36 horse-level特徴量が1つも含まれない"
    - "RegimeDetector.FEATURE_COLSにPhase36 fundamental特徴量が1つも含まれない"
    - "既存テストが全て通過する (FEATURE_COLS長変更に追従)"
  artifacts:
    - path: "src/models/market_model.py"
      provides: "MarketModel with market-only FEATURE_COLS"
      contains: "implied_prob_hhi"
    - path: "src/models/race_quality_screener.py"
      provides: "RaceQualityScreener without horse-level Phase36 features"
      contains: "market_log_error_max_abs"
    - path: "src/models/regime_detector.py"
      provides: "RegimeDetector with market-indicator-only FEATURE_COLS"
      contains: "overround_rolling"
  key_links:
    - from: "src/models/market_model.py"
      to: "market-only features"
      via: "FEATURE_COLS list"
      pattern: "FEATURE_COLS.*implied_prob_hhi"
---

<objective>
Phase36 fundamental特徴量をMarketModel, RaceQualityScreener, RegimeDetectorの3モデルFEATURE_COLSから外科的に除外する。

Purpose: Phase36/36.1で全モデルに一律追加された27個のfundamental horse-level特徴量(TRF=3, weighted_form=2, INT=3, HLF-Haron=6, D-02=3, D-03=7, HLF-rank=2)がMarketModelの市場歪み検出信号を支配(gain share 80%+)し、RaceQualityScreenerのNaN注入によるスコア退化、RegimeDetectorの市場状態分類崩壊を引き起こしている。各モデルのアーキテクチャ契約に基づき、不要な特徴量を削除する。

Output: 3モデルから27個のPhase36特徴量を除外したFEATURE_COLS。更新済みテストフィクスチャ。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.1.1-marketmodel-racequality-phase36-ev-tail-calibration/36.1.1-RESEARCH.md

<interfaces>
<!-- Key constants and contracts the executor needs. -->

Phase36 features to REMOVE from 3 models (27 total per model):
TRF: form_trend_race_rank, blood_total_wr_race_rank, blood_surface_wr_race_rank
Weighted form: weighted_recent_form_finish, weighted_recent_form_time
INT: grade_x_form_trend, distance_x_closing_index, grade_x_blood_prize_log
HLF HaronTime: closing_speed_ratio_avg, closing_speed_ratio_zscore, closing_speed_ratio_trend, harontime_last3f_avg, harontime_last3f_zscore, harontime_last3f_trend
D-02 haron_race_gap: haron_race_gap_avg, haron_race_gap_zscore, haron_race_gap_trend
D-03 pace_adj_finish: pace_adj_finish_avg, pace_ratio_avg, pace_ratio_zscore, pace_ratio_trend, pace_early_avg, pace_mid_avg, pace_late_avg
HLF race-rank: closing_speed_ratio_avg_race_rank, harontime_last3f_avg_race_rank

Models that KEEP Phase36 features (DO NOT MODIFY):
AbilityModel, WinTwoStageModel, PlaceTwoStageModel (HIT+RETURN), EVCorrectionModel, PlaceEVCorrectionModel, ConformalEVModel, PlaceAbilityModel, WideTwoStageModel
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove Phase36 features from 3 model FEATURE_COLS + update tests</name>
  <files>src/models/market_model.py, src/models/race_quality_screener.py, src/models/regime_detector.py, tests/test_market_model.py, tests/test_race_quality_screener.py, tests/test_regime_detector.py</files>
  <read_first>src/models/market_model.py, src/models/race_quality_screener.py, src/models/regime_detector.py, tests/test_market_model.py, tests/test_race_quality_screener.py, tests/test_regime_detector.py</read_first>
  <behavior>
    - Test 1: MarketModel.FEATURE_COLS does NOT contain any of the 27 Phase36 features (form_trend_race_rank, closing_speed_ratio_avg, etc.)
    - Test 2: RaceQualityScreener.FEATURE_COLS does NOT contain any of the 27 Phase36 features
    - Test 3: RegimeDetector.FEATURE_COLS does NOT contain any of the 27 Phase36 features
    - Test 4: MarketModel.FEATURE_COLS still contains market-only features (implied_prob_hhi, odds_skewness, rl_log_odds_entropy, overround-related)
    - Test 5: RaceQualityScreener.FEATURE_COLS still contains screener-appropriate features (market_log_error_max_abs, market_entropy, overround)
    - Test 6: RegimeDetector.FEATURE_COLS still contains market indicators (overround_rolling, entropy_rolling, market_error_std)
    - Test 7: Full test suite passes (python -m pytest tests/ -x -q)
  </behavior>
  <action>
    Per RTG-01: Remove all 27 Phase36 fundamental features from FEATURE_COLS in 3 models.

    **market_model.py lines 66-97**: Delete the entire block from `# TRF-01/02/03 + INT-01/02/03/04: Phase 36` through `harontime_last3f_avg_race_rank`. The FEATURE_COLS list should end at `"rl_n_horses"` (line 65). No new features added.

    **race_quality_screener.py lines 84-115**: Delete the entire block from `# TRF-01/02/03 + INT-01/02/03/04: Phase 36` through `harontime_last3f_avg_race_rank`. The FEATURE_COLS list should end at `"rl_n_horses"` (line 83). No new features added yet (race-level aggregates come in Plan 02).

    **regime_detector.py lines 89-120**: Delete the entire block from `# TRF-01/02/03 + INT-01/02/03/04: Phase 36` through `harontime_last3f_avg_race_rank`. The FEATURE_COLS list should end at `"rl_n_horses"` (line 88). No new features added.

    **Test updates**: Read test files first to check if fixtures reference Phase36 feature columns or check FEATURE_COLS length/count. Update fixture DataFrames to remove Phase36 columns. Add explicit assertions that the 3 models' FEATURE_COLS do not contain any Phase36 features. Check test_market_model.py sample_df fixture -- it does not include Phase36 features currently, so no fixture changes needed there. Check test_race_quality_screener.py race_features_df fixture -- it does not include Phase36 features currently, so no fixture changes needed there. Check test_regime_detector.py for any FEATURE_COLS length assertions.

    **Verification list of all 27 features to verify absence:**
    form_trend_race_rank, blood_total_wr_race_rank, blood_surface_wr_race_rank, weighted_recent_form_finish, weighted_recent_form_time, grade_x_form_trend, distance_x_closing_index, grade_x_blood_prize_log, closing_speed_ratio_avg, closing_speed_ratio_zscore, closing_speed_ratio_trend, harontime_last3f_avg, harontime_last3f_zscore, harontime_last3f_trend, haron_race_gap_avg, haron_race_gap_zscore, haron_race_gap_trend, pace_adj_finish_avg, pace_ratio_avg, pace_ratio_zscore, pace_ratio_trend, pace_early_avg, pace_mid_avg, pace_late_avg, closing_speed_ratio_avg_race_rank, harontime_last3f_avg_race_rank.
  </action>
  <verify>
    <automated>python -m pytest tests/test_market_model.py tests/test_race_quality_screener.py tests/test_regime_detector.py -v -x</automated>
  </verify>
  <done>
    - MarketModel.FEATURE_COLS contains 0 Phase36 features
    - RaceQualityScreener.FEATURE_COLS contains 0 Phase36 horse-level features
    - RegimeDetector.FEATURE_COLS contains 0 Phase36 features
    - All 3 model FEATURE_COLS still contain their legitimate pre-Phase36 features
    - All tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model FEATURE_COLS → training data | Feature list must match available columns; missing cols filled with NaN by _safe_feature_select |
| model FEATURE_COLS → inference path | Same FEATURE_COLS used in both training and inference |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.1.1-01 | Tampering | FEATURE_COLS removal | mitigate | Verify removal with explicit test assertions checking all 27 feature names are absent |
| T-36.1.1-02 | Denial of Service | Test fixture breakage | mitigate | Run full test suite after modification |
| T-36.1.1-SC | Tampering | pip installs | accept | No new packages in this plan |
</threat_model>

<verification>
1. `python -m pytest tests/test_market_model.py tests/test_race_quality_screener.py tests/test_regime_detector.py -v` -- all pass
2. `python -c "from models.market_model import MarketModel; ph36=['form_trend_race_rank','closing_speed_ratio_avg']; assert not any(f in MarketModel.FEATURE_COLS for f in ph36)"` -- no output (success)
3. `python -c "from models.race_quality_screener import RaceQualityScreener; ph36=['form_trend_race_rank','closing_speed_ratio_avg']; assert not any(f in RaceQualityScreener.FEATURE_COLS for f in ph36)"` -- no output (success)
4. `python -c "from models.regime_detector import RegimeDetector; ph36=['form_trend_race_rank','closing_speed_ratio_avg']; assert not any(f in RegimeDetector.FEATURE_COLS for f in ph36)"` -- no output (success)
</verification>

<success_criteria>
- MarketModel, RaceQualityScreener, RegimeDetector の FEATURE_COLS から Phase36 fundamental特徴量27個が完全に除外されている
- 3モデルの正当な(market/screener/regime)特徴量が残っている
- テスト全通過
</success_criteria>

<output>
Create `.planning/phases/36.1.1-marketmodel-racequality-phase36-ev-tail-calibration/36.1.1-01-SUMMARY.md` when done
</output>
