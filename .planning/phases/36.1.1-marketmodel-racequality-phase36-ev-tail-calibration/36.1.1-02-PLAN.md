---
phase: 36.1.1-marketmodel-racequality-phase36-ev-tail-calibration
plan: "02"
type: execute
wave: 2
depends_on: ["36.1.1-01"]
files_modified:
  - src/models/race_quality_screener.py
  - src/backtest/race_predictor.py
  - src/pipelines/training_pipeline.py
  - src/backtest/engine.py
  - src/backtest/diagnostic_logger.py
  - tests/test_race_quality_screener.py
  - tests/test_backtest_engine.py
autonomous: true
requirements: [RTG-02, RTG-03]

must_haves:
  truths:
    - "RaceQualityScreenerがrace aggregate Phase36特徴量(phase36_top1_strength等)を使用する"
    - "build_race_features()がrl_*列とimplied_prob_hhi/odds_skewnessを返す"
    - "_build_race_level_features()が5つのphase36 race-level aggregateを計算する"
    - "BacktestEngineのquality_scoreが0.0固定ではなく実際のスコアを表示する"
    - "RaceQualityScreenerにpredict_score()メソッドが存在する"
  artifacts:
    - path: "src/models/race_quality_screener.py"
      provides: "predict_score() method + race-aggregate FEATURE_COLS"
      contains: "predict_score"
    - path: "src/backtest/race_predictor.py"
      provides: "build_race_features() with rl_*, implied_prob_hhi, phase36_* columns"
      pattern: "phase36_top1_strength"
    - path: "src/pipelines/training_pipeline.py"
      provides: "_build_race_level_features() with phase36 race aggregates"
      pattern: "phase36_top1_strength"
    - path: "src/backtest/engine.py"
      provides: "quality_score from actual screener output (not 0.0)"
      pattern: "quality_score.*predict_score\\|get_quality"
  key_links:
    - from: "src/backtest/race_predictor.py"
      to: "src/models/race_quality_screener.py"
      via: "build_race_features() dict → should_bet() / predict_score()"
      pattern: "build_race_features.*phase36"
    - from: "src/backtest/engine.py"
      to: "src/backtest/race_predictor.py"
      via: "quality_score extraction from should_bet result"
      pattern: "quality_score.*predict_score"
---

<!-- Scope note: 7 files across 2 tasks, but Task 1 (RTG-02/03 feature routing) and Task 2 (quality_score fix) share race_predictor.py and engine.py. Splitting into separate plans would create a file conflict requiring sequential waves, whereas the combined plan keeps them in one executor context (~35% estimated). The modifications are all small: FEATURE_COLS additions (2-3 lines), build_race_features() additions (10-15 lines), engine.py fixes (3-4 lines each). Not split. -->
<objective>
RaceQualityScreenerにrace-level Phase36 aggregate特徴量を追加し、build_race_features()と_build_race_level_features()の両パスで計算し、BacktestEngineのquality_score=0.0固定問題を修正する。

Purpose: RaceQualityScreenerがPhase36 horse-level特徴量のNaNで退化していた問題を、race-level aggregate特徴量(5個)に置き換えて解決する。同時にbuild_race_features()がrl_*列やimplied_prob_hhi/odds_skewnessを含んでいなかった問題も修正する。さらにquality_score=0.0固定により診断情報が失われていた問題を、predict_score()メソッドの追加で解決する。

Output: 5個のPhase36 race-level aggregate特徴量。両パス(training + inference)での計算。quality_score実値表示。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.1.1-marketmodel-racequality-phase36-ev-tail-calibration/36.1.1-RESEARCH.md
@.planning/phases/36.1.1-marketmodel-racequality-phase36-ev-tail-calibration/36.1.1-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output: Phase36 horse-level features removed from screener -->

<!-- New race-level aggregate features to ADD to RaceQualityScreener.FEATURE_COLS: -->
<!-- phase36_top1_strength: max(closing_speed_ratio_avg) per race -->
<!-- phase36_top1_top2_gap: nlargest(2).diff().iloc[-1] of closing_speed_ratio_avg -->
<!-- phase36_field_dispersion: std(closing_speed_ratio_avg) per race -->
<!-- phase36_form_signal_dispersion: std(form_trend_race_rank) per race -->
<!-- phase36_weighted_form_mean: mean(weighted_recent_form_finish) per race -->

<!-- rl_* columns that build_race_features() must propagate (from result_df): -->
<!-- rl_log_odds_entropy, rl_odds_dispersion, rl_top3_odds_gap, rl_top1_odds, -->
<!-- rl_favorite_rank_gap, rl_n_horses, rl_favorite_in_wide_top1, rl_trio_overlap, -->
<!-- rl_market_consistency, rl_trio_odds_ratio, rl_wide_harville_ratio, -->
<!-- implied_prob_hhi, odds_skewness -->

<!-- RaceQualityScreener.should_bet() currently returns bool -->
<!-- Need to add predict_score() that returns float without changing should_bet() signature -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase36 race aggregates + rl_* propagation to both paths</name>
  <files>src/models/race_quality_screener.py, src/backtest/race_predictor.py, src/pipelines/training_pipeline.py, tests/test_race_quality_screener.py, tests/test_backtest_engine.py</files>
  <read_first>src/models/race_quality_screener.py, src/backtest/race_predictor.py, src/pipelines/training_pipeline.py, tests/test_race_quality_screener.py</read_first>
  <behavior>
    - Test 1: RaceQualityScreener.FEATURE_COLS contains all 5 phase36_* aggregate features
    - Test 2: build_race_features() returns a dict containing phase36_top1_strength, phase36_top1_top2_gap, phase36_field_dispersion, phase36_form_signal_dispersion, phase36_weighted_form_mean
    - Test 3: build_race_features() returns a dict containing rl_log_odds_entropy, rl_odds_dispersion, rl_top3_odds_gap, rl_top1_odds, rl_favorite_rank_gap, rl_n_horses, rl_favorite_in_wide_top1, rl_trio_overlap, rl_market_consistency, rl_trio_odds_ratio, rl_wide_harville_ratio, implied_prob_hhi, odds_skewness
    - Test 4: _build_race_level_features() output DataFrame contains phase36_top1_strength, phase36_top1_top2_gap, phase36_field_dispersion, phase36_form_signal_dispersion, phase36_weighted_form_mean columns
    - Test 5: RaceQualityScreener has predict_score() method that returns float
    - Test 6: should_bet() still returns bool and works with race aggregate features
    - Test 7: Full test suite passes
  </behavior>
  <action>
    **Part A: RaceQualityScreener.FEATURE_COLS (src/models/race_quality_screener.py)**
    Add 5 new race-level aggregate features to FEATURE_COLS after the existing `"rl_n_horses"` entry:
    `phase36_top1_strength`, `phase36_top1_top2_gap`, `phase36_field_dispersion`, `phase36_form_signal_dispersion`, `phase36_weighted_form_mean`.
    These are race-level aggregations, NOT horse-level features, so they are appropriate for the screener.

    **Part B: RaceQualityScreener.predict_score() (src/models/race_quality_screener.py)**
    Add a new method `predict_score(self, race_features: dict) -> float` that:
    1. Takes the same race_features dict as should_bet()
    2. Prepares features via _prepare_features + _safe_feature_cols
    3. Returns float(self.model.predict(features, num_iteration=best_iter)[0])
    Does NOT modify should_bet() -- it stays as is. should_bet() internally computes the same score and compares to threshold, but predict_score() exposes the raw value.

    **Part C: build_race_features() rl_* and phase36 propagation (src/backtest/race_predictor.py)**
    In the build_race_features() static method (line ~921), add the following keys to the returned dict:
    1. All rl_* columns from row (using row.get with NaN default): rl_log_odds_entropy, rl_odds_dispersion, rl_top3_odds_gap, rl_top1_odds, rl_favorite_rank_gap, rl_n_horses, rl_favorite_in_wide_top1, rl_trio_overlap, rl_market_consistency, rl_trio_odds_ratio, rl_wide_harville_ratio, implied_prob_hhi, odds_skewness
    2. Phase36 race-level aggregates computed from race_df:
       - phase36_top1_strength: float(race_df["closing_speed_ratio_avg"].max()) if column exists, else 0.0
       - phase36_top1_top2_gap: computed from closing_speed_ratio_avg nlargest(2) diff
       - phase36_field_dispersion: float(race_df["closing_speed_ratio_avg"].std()) if column exists and len > 1, else 0.0
       - phase36_form_signal_dispersion: float(race_df["form_trend_race_rank"].std()) if column exists and len > 1, else 0.0
       - phase36_weighted_form_mean: float(race_df["weighted_recent_form_finish"].mean()) if column exists, else 0.0
    Use the same NaN-safe pattern: check column exists in race_df before computing, with 0.0 fallback.

    **Part D: _build_race_level_features() phase36 aggregates (src/pipelines/training_pipeline.py)**
    In _build_race_level_features() (line ~1182), after the rl_* column propagation block (line ~1306), add computation of the 5 phase36 race-level aggregates from feat_df:
    1. Check if `closing_speed_ratio_avg` is in feat_df.columns
    2. If yes, compute: groupby("race_id") → max (top1_strength), nlargest(2) diff (top1_top2_gap), std (field_dispersion)
    3. Check if `form_trend_race_rank` is in feat_df.columns → groupby → std (form_signal_dispersion)
    4. Check if `weighted_recent_form_finish` is in feat_df.columns → groupby → mean (weighted_form_mean)
    5. Map all 5 to race_feat using race_id
    6. Fill NaN with 0.0
    This follows the same pattern as the rl_* propagation at lines 1301-1306.

    **Part E: Test updates**
    Update test_race_quality_screener.py:
    - Add phase36_* columns to race_features_df fixture with numeric values
    - Add rl_* columns to race_features_df fixture if not already present
    - Add test for predict_score() method: mock model.predict, verify it returns float
    - Add test that all 5 phase36_* features are in FEATURE_COLS
    - Add test that build_race_features() returns phase36_* keys
  </action>
  <verify>
    <automated>python -m pytest tests/test_race_quality_screener.py tests/test_backtest_engine.py -v -x</automated>
  </verify>
  <done>
    - RaceQualityScreener.FEATURE_COLS contains 5 phase36_* race-level aggregates
    - build_race_features() returns rl_* columns, implied_prob_hhi, odds_skewness, and phase36_* aggregates
    - _build_race_level_features() computes and returns 5 phase36_* aggregates
    - predict_score() method exists on RaceQualityScreener and returns float
    - should_bet() still works correctly
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix quality_score=0.0 hardcoded in BacktestEngine</name>
  <files>src/backtest/engine.py, src/backtest/race_predictor.py, src/backtest/diagnostic_logger.py</files>
  <read_first>src/backtest/engine.py, src/backtest/race_predictor.py, src/backtest/diagnostic_logger.py</read_first>
  <action>
    **Part A: RacePredictor.get_quality_score() (src/backtest/race_predictor.py)**
    Add a new method `get_quality_score(self, race_df: pd.DataFrame) -> float` that:
    1. Calls build_race_features(race_df) to get the dict
    2. Calls self.models.quality_screener.predict_score(features) to get the float score
    3. Returns the score
    This is a thin wrapper that mirrors should_bet() but returns the raw score instead of bool.

    **Part B: BacktestEngine quality_score extraction (src/backtest/engine.py)**
    At line ~991 (the `if not self._race_predictor.should_bet(result_df):` block), before the should_bet() call, capture the quality_score:
    ```python
    _quality_score = self._race_predictor.get_quality_score(result_df)
    ```
    Then replace `quality_score=0.0` at line 998 with `quality_score=_quality_score`.

    Similarly, at line ~1089 (the quality_passed=True block), compute the same _quality_score:
    Replace `quality_score=0.0` at line 1089 with `quality_score=_quality_score`.

    The _quality_score should be computed ONCE per race (before the if/else branch) and used in both branches. Move the should_bet() call to capture both the bool result and the score:
    ```python
    _quality_score = self._race_predictor.get_quality_score(result_df)
    _quality_passed = self._race_predictor.should_bet(result_df)
    ```
    Then use _quality_score in both log_race() calls and _quality_passed for the if condition.

    **Part C: DiagnosticLogger (no changes needed)**
    The RaceDiagnostic dataclass already has `quality_score: float = 0.0` -- it just needs to receive the real value. No structural changes needed to diagnostic_logger.py.
  </action>
  <verify>
    <automated>python -m pytest tests/test_backtest_engine.py -v -x</automated>
  </verify>
  <done>
    - BacktestEngine line ~998 uses actual quality_score instead of hardcoded 0.0
    - BacktestEngine line ~1089 uses actual quality_score instead of hardcoded 0.0
    - RacePredictor has get_quality_score() method
    - Diagnostic CSV shows non-zero quality_score values when screener model is trained
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| horse-level feat_df → race-level aggregates | Aggregation must use groupby("race_id"), never leak individual horse data |
| predict_score() → engine logging | Score is diagnostic only, does not affect bet decisions (should_bet controls that) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.1.1-03 | Information Disclosure | quality_score in diagnostics | accept | Score is internal diagnostic, not exposed to user |
| T-36.1.1-04 | Tampering | race aggregate computation | mitigate | Use same groupby pattern as existing rl_* propagation |
| T-36.1.1-SC | Tampering | pip installs | accept | No new packages |
</threat_model>

<verification>
1. `python -m pytest tests/test_race_quality_screener.py tests/test_backtest_engine.py -v` -- all pass
2. `python -c "from models.race_quality_screener import RaceQualityScreener; assert 'phase36_top1_strength' in RaceQualityScreener.FEATURE_COLS"` -- success
3. `python -c "from backtest.race_predictor import RacePredictor; assert hasattr(RacePredictor, 'get_quality_score')"` -- success
4. `python -c "from models.race_quality_screener import RaceQualityScreener; s=RaceQualityScreener(); assert hasattr(s, 'predict_score')"` -- success
</verification>

<success_criteria>
- RaceQualityScreenerが5つのphase36 race-level aggregate特徴量を使用
- build_race_features()がrl_*列とphase36_* aggregateを返す
- _build_race_level_features()が5つのphase36_* aggregateを計算
- BacktestEngineのquality_scoreが実際のスコアを表示 (0.0固定でない)
- テスト全通過
</success_criteria>

<output>
Create `.planning/phases/36.1.1-marketmodel-racequality-phase36-ev-tail-calibration/36.1.1-02-SUMMARY.md` when done
</output>
