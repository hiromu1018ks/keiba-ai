---
phase: 36.1.1-marketmodel-racequality-phase36-ev-tail-calibration
verified: 2026-05-20T12:00:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification: false
deferred:
  - truth: "BT 2024再学習で ROI >= 97.8% (v1.7水準) に回復、ベット数 >= 1500"
    addressed_in: "Phase 38"
    evidence: "ROADMAP.md SC-6: '[Phase 38で検証] -- Phase 36.1.1はコード修正を完了し、Phase 38 (Integrated Validation) でBT再実行によるROI検証を実施'"
human_verification: []
---

# Phase 36.1.1: MarketModel/RaceQuality配線修正 Verification Report

**Phase Goal:** Phase36/36.1の強いfundamental特徴量が全モデルに一律登録されたことで、MarketModelの市場歪み検出役割とRaceQualityScreenerのレース品質判定が崩壊した問題を修復する。
**Verified:** 2026-05-20T12:00:00Z
**Status:** passed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC-1 | MarketModel.FEATURE_COLSからPhase36 fundamental特徴量を除外、market-only特徴量に戻す | VERIFIED | `MarketModel.FEATURE_COLS`: 20 features, 0 Phase36 features. `implied_prob_hhi`, `odds_skewness`, `rl_log_odds_entropy`, `rl_odds_dispersion` confirmed present. 27 Phase36 features (TRF/INT/HLF/D-02/D-03/HLF-rank/weighted_form) absent. |
| SC-2 | RaceQualityScreenerがquality_score=0固定問題を解消、実効的なscore/thresholdを出力 | VERIFIED | `predict_score()` method exists at line 166 of `race_quality_screener.py`. `BacktestEngine` uses `_quality_score = self._race_predictor.get_quality_score(result_df)` at line 991. No hardcoded `quality_score=0.0` found in `engine.py`. |
| SC-3 | RaceQualityScreenerが馬単位Phase36列ではなくrace aggregate特徴量を使用 | VERIFIED | `RaceQualityScreener.FEATURE_COLS`: 40 features including `phase36_top1_strength`, `phase36_top1_top2_gap`, `phase36_field_dispersion`, `phase36_form_signal_dispersion`, `phase36_weighted_form_mean`. 0 horse-level Phase36 features. |
| SC-4 | EV>=1.5高EV長穴をfeature family合意度で分類、単独跳ねは縮小、複数合意はaggressive | VERIFIED | `EVTailCalibrator` in `src/betting/ev_tail_calibration.py` with constants: NO_FAMILY_FACTOR=0.70, SINGLE_FAMILY_FACTOR=0.85, MULTI_FAMILY_FACTOR=1.05, EV_THRESHOLD=1.5. Integrated in `get_win_candidates()` at line 546. `_calibrated_edge` column created, used for sorting, then dropped before return (line 593). |
| SC-5 | v1.7 vs 現行 共通632レースの同馬/別馬差分レポートでPhase36寄与分解が可能 | VERIFIED | `scripts/diagnose_phase36_diff.py` with 6 functions: `load_backtest_results`, `find_common_races`, `compute_horse_overlap`, `compute_roi_breakdown`, `generate_report`, `main`. CLI with `--baseline`, `--current`, `--output` args. 31 tests pass. Phase36 contribution decomposition includes: `roi_same`, `roi_new`, `roi_removed`, `net_contribution_pct`, `screener_fix_count`, `ev_tail_count`. |
| SC-6 | BT 2024 ROI >= 97.8% (v1.7水準) | DEFERRED | Explicitly deferred to Phase 38 per ROADMAP.md: "[Phase 38で検証]" |

**Score:** 5/5 truths verified (SC-6 deferred, not counted as failure)

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | BT 2024 ROI >= 97.8% verification requires full backtest re-run | Phase 38 | ROADMAP.md SC-6: "Phase 36.1.1はコード修正を完了し、Phase 38 (Integrated Validation) でBT再実行によるROI検証を実施" |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/models/market_model.py` | MarketModel with market-only FEATURE_COLS (20) | VERIFIED | 20 features, `implied_prob_hhi` present, 0 Phase36 features |
| `src/models/race_quality_screener.py` | RaceQualityScreener with predict_score() + race aggregate FEATURE_COLS (40) | VERIFIED | 40 features, `phase36_top1_strength` present, `predict_score()` at line 166 |
| `src/models/regime_detector.py` | RegimeDetector with market-indicator-only FEATURE_COLS (21) | VERIFIED | 21 features, `overround_rolling` present, 0 Phase36 features |
| `src/betting/ev_tail_calibration.py` | EVTailCalibrator class with calibrate() | VERIFIED | `EVTailCalibrator.calibrate()` with 5 families, 0.70/0.85/1.05 factors |
| `src/backtest/race_predictor.py` | Race-level aggregates + quality score + EV calibration integration | VERIFIED | `_compute_phase36_aggregates()`, `get_quality_score()`, EVTtailCalibrator import at line 546 |
| `src/pipelines/training_pipeline.py` | _build_race_level_features() with phase36 race aggregates | VERIFIED | 5 phase36_* aggregates computed via groupby("race_id") at lines 1338-1371 |
| `src/backtest/engine.py` | quality_score from actual screener output (not 0.0) | VERIFIED | `_quality_score = self._race_predictor.get_quality_score(result_df)` at line 991, used in both branches (lines 1001, 1092) |
| `scripts/diagnose_phase36_diff.py` | v1.7 vs current backtest diff diagnostic script | VERIFIED | 6 functions, CLI with --baseline/--current/--output, phase36_contribution decomposition |
| `tests/test_ev_tail_calibration.py` | Unit tests for EV tail calibration | VERIFIED | 20 tests across 8 classes, all pass |
| `tests/test_diagnose_phase36_diff.py` | Unit tests for diff computation | VERIFIED | 31 tests across 7 classes, all pass |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `market_model.py` | market-only features | `FEATURE_COLS` list | WIRED | 20 features, 0 Phase36 |
| `race_quality_screener.py` | race aggregate features | `FEATURE_COLS` + `predict_score()` | WIRED | 40 features including 5 phase36_* aggregates |
| `race_predictor.py` | `race_quality_screener.py` | `build_race_features()` dict -> `should_bet()` / `predict_score()` | WIRED | `build_race_features()` returns rl_* + phase36_* at lines 1034-1049 |
| `engine.py` | `race_predictor.py` | `get_quality_score()` -> `quality_score` in diagnostic log | WIRED | Line 991: `get_quality_score()`, lines 1001/1092: actual score in log |
| `ev_tail_calibration.py` | `race_predictor.py` | `EVTailCalibrator.calibrate()` in `get_win_candidates()` | WIRED | Import at line 546, calibration applied for EV >= 1.5, _calibrated_edge used for sort then dropped |
| `diagnose_phase36_diff.py` | `data/backtest/*.csv` | CSV file reading via `pd.read_csv` | WIRED | `load_backtest_results()` function reads CSV with standardization |
| `training_pipeline.py` | phase36 race aggregates | `_build_race_level_features()` groupby | WIRED | 5 aggregates computed from feat_df via groupby("race_id") at lines 1338-1371 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `race_quality_screener.py` predict_score() | `model.predict(features)` output | `FEATURE_COLS` selection from input dict | Yes -- model prediction on real feature values | FLOWING |
| `race_predictor.py` build_race_features() | `phase36_*` aggregates | `_compute_phase36_aggregates(race_df)` from horse-level columns | Yes -- `max()`, `std()`, `mean()` on closing_speed_ratio_avg etc. with NaN-safe fallback | FLOWING |
| `engine.py` quality_score | `_quality_score` | `get_quality_score()` -> `predict_score()` -> `model.predict()` | Yes -- chain from screener model through predictor to engine | FLOWING |
| `race_predictor.py` EVTtailCalibrator | `_calibrated_edge` | `calibrate(row, masked_race, raw_edge)` per candidate | Yes -- z-score computation on feature families, scaling applied | FLOWING |
| `training_pipeline.py` phase36 aggregates | `race_feat["phase36_*"]` | `feat_df.groupby("race_id")` on closing_speed_ratio_avg, form_trend_race_rank, weighted_recent_form_finish | Yes -- groupby aggregation with NaN fill | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| No Phase36 features in 3 models | `PYTHONPATH=src python -c "..."` (27-feature check) | MarketModel: 0, RaceQualityScreener: 0, RegimeDetector: 0 | PASS |
| EVTtailCalibrator constants | `PYTHONPATH=src python -c "from betting.ev_tail_calibration import ..."` | NO=0.70, SINGLE=0.85, MULTI=1.05, THRESHOLD=1.5 | PASS |
| predict_score / get_quality_score exist | `PYTHONPATH=src python -c "..."` | Both `True` | PASS |
| Diagnostic script CLI | `python scripts/diagnose_phase36_diff.py --help` | Usage with --baseline, --current, --output | PASS |
| Phase-specific tests pass | `python -m pytest tests/test_market_model.py tests/test_race_quality_screener.py tests/test_regime_detector.py tests/test_ev_tail_calibration.py tests/test_diagnose_phase36_diff.py tests/test_backtest_engine.py -v` | 170 passed | PASS |
| Full suite regression | `python -m pytest tests/ -x -q` | 560 passed, 1 failed (pre-existing), 1 skipped | PASS |

### Probe Execution

Step 7c: SKIPPED (no probes declared in PLAN files, no conventional probe scripts found)

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| RTG-01 | 36.1.1-01 | MarketModel.FEATURE_COLSからPhase36 fundamental特徴量を除外 | SATISFIED | 27 features removed from 3 models, verified programmatically |
| RTG-02 | 36.1.1-02 | RaceQualityScreener quality_score=0固定問題を解消 | SATISFIED | `predict_score()` method + engine uses actual score via `get_quality_score()` |
| RTG-03 | 36.1.1-02 | RaceQualityScreenerがrace aggregate特徴量を使用 | SATISFIED | 5 phase36_* aggregates in FEATURE_COLS, computed in both training and inference paths |
| RTG-04 | 36.1.1-03 | EV>=1.5高EV長穴をfeature family合意度で分類 | SATISFIED | EVTtailCalibrator with 5 families, integrated in get_win_candidates() |
| RTG-05 | 36.1.1-04 | v1.7 vs 現行 差分診断スクリプト | SATISFIED | diagnose_phase36_diff.py with CLI, 31 tests pass |

Note: RTG-01 through RTG-05 are phase-internal requirements defined in RESEARCH.md, not in REQUIREMENTS.md (which covers v1.8 top-level requirements). All 5 are accounted for in PLAN frontmatter and verified above.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | -- | -- | -- | No debt markers (TBD/FIXME/XXX) or stub patterns found in any modified file |

### Human Verification Required

None. All truths are programmatically verified.

### Gaps Summary

No gaps found. All 5 ROADMAP success criteria are verified in codebase:

1. **Feature routing fix (RTG-01):** 27 Phase36 fundamental features surgically removed from MarketModel, RaceQualityScreener, RegimeDetector. Market-only features preserved.
2. **Quality score fix (RTG-02):** `predict_score()` method added to screener, `get_quality_score()` added to predictor, engine no longer hardcodes `quality_score=0.0`.
3. **Race aggregate features (RTG-03):** 5 race-level aggregates computed in both training pipeline and inference path, added to screener FEATURE_COLS.
4. **EV tail calibration (RTG-04):** EVTtailCalibrator with 5 feature families and 0.70/0.85/1.05 scaling factors, integrated in get_win_candidates() for EV >= 1.5 candidates.
5. **Diagnostic script (RTG-05):** Complete diff diagnostic script with Phase36 contribution decomposition, CLI interface, and comprehensive test coverage.

One pre-existing test failure (`test_gpd_diagnostics FEATURE_CATEGORY_MAP completeness`) is unrelated to this phase.

ROI verification (SC-6) explicitly deferred to Phase 38 per ROADMAP specification.

---

_Verified: 2026-05-20T12:00:00Z_
_Verifier: Claude (gsd-verifier)_
