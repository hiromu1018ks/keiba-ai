# Phase 36.1.1.1: roi-diagnostic-fixes-and-feature-sanity-optimization - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-21
**Phase:** 36.1.1.1-roi-diagnostic-fixes-and-feature-sanity-optimization
**Areas discussed:** 実ベット診断の正本化, 高EV・高オッズ尾部の防御ルール, 真のOOF成果物, 特徴量健全化の優先順位, 予測時点 manifest の線引き

---

## 実ベット診断の正本化

| Option | Description | Selected |
|--------|-------------|----------|
| `stake`基準 | `stake.notna()` を実ベット正本にし、`is_actual_bet` はそこから派生させる | ✓ |
| `is_bet`修正 | 既存 `is_bet` を残して、生成箇所で `stake` と一致させる | |
| 両方保持 | `stake` と `is_bet` を並列保持し、不一致検査で失敗させる | |

**User's choice:** 専門的なことはわからないため、実装難易度を問わずベストプラクティスを追求してほしい。
**Notes:** The agent selected `stake` as canonical source of truth, with `is_actual_bet` derived from it and mismatch validation required.

---

## 高EV・高オッズ尾部の防御ルール

| Option | Description | Selected |
|--------|-------------|----------|
| Hard block only | `odds>=100` and `ev>=5` を禁止する | |
| Configured risk filter | 禁止・縮小・rank/probability gate を設定化し、理由を保存する | ✓ |
| EV calibrationへ延期 | Phase 37 の calibration に任せる | |

**User's choice:** Best-practice delegation.
**Notes:** The agent selected a configurable risk filter with conservative defaults, pre-registered thresholds, and machine-readable exclusion/shrink reasons.

---

## 真のOOF成果物

| Option | Description | Selected |
|--------|-------------|----------|
| Current fileを修正 | `oof_predictions.parquet` に最低限の列だけ追加する | |
| True time-series OOF | expanding/walk-forward foldで、学習に使っていない行だけ予測する | ✓ |
| Phase 37へ延期 | calibration実装時にOOFを作り直す | |

**User's choice:** Best-practice delegation.
**Notes:** The agent selected true time-series OOF with `fold_id`, `is_oof`, fold boundaries, model/feature hashes, and fail-fast validation if absent or fake.

---

## 特徴量健全化の優先順位

| Option | Description | Selected |
|--------|-------------|----------|
| 異常列を除外 | 怪しい列を `FEATURE_COLS` から落として安全側に倒す | |
| Root-cause fix + validator | 原因修正、境界検査、train/BT差分検査を追加する | ✓ |
| LightGBMに任せる | NaN/外れ値はモデルに吸収させる | |

**User's choice:** Best-practice delegation.
**Notes:** The agent selected root-cause fixes for `sire_distance_wr`, `weight_change_zone/ratio`, `deviation_*`, and `actual_pace_fit` timing proof, plus feature availability/distribution validation.

---

## 予測時点 manifest の線引き

| Option | Description | Selected |
|--------|-------------|----------|
| Current model only | 現状の発走前モデルとして明示する | |
| Timing manifest | 各特徴量の利用可能時点をmanifest化し、mode不一致を失敗させる | ✓ |
| 前日モデルも作る | このフェーズで前日モデルの別FEATURE_COLSも構築する | |

**User's choice:** Best-practice delegation.
**Notes:** The agent selected a timing manifest and fail-fast mode validation. Building a new full 前日 model is deferred.

---

## the agent's Discretion

- User delegated all technical decisions to the agent with best-practice priority.
- The agent fixed the decision basis as diagnostic reliability, leakage resistance, and reproducibility over implementation simplicity.
- Exact implementation factoring, metadata schema names, and tail shrink multipliers remain planner discretion within the locked decisions.

## Deferred Ideas

- Month-specific filters from the 2024 ROI report.
- Wide-odds 153-column dimensionality reduction unless feature audit proves it is urgent.
- Building a separate full 前日 model in this phase.
- Larger rolling-window CV expansion beyond the validation needed for this phase.
