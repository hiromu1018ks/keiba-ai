---
phase: 37-ev-calibration-layers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/validation/__init__.py
  - src/validation/oof_health_validator.py
  - src/models/stage1_ability_model.py
  - tests/test_oof_health_validator.py
  - tests/test_oof_leakage.py
autonomous: true
requirements:
  - OOF-01
  - OOF-02
  - OOF-03
  - OOF-04
  - OOF-05
  - OOF-06
  - OOF-07
  - OOF-08
  - XCT-05
  - XCT-08

must_haves:
  truths:
    - "OOFHealthValidator.validate() raises ValueError on empty DataFrame (OOF-01)"
    - "OOFHealthValidator.validate() fails when fold_count < 3 (OOF-05)"
    - "OOFHealthValidator.validate() fails when same race_id appears in multiple folds (OOF-06)"
    - "OOFHealthValidator.validate() fails when required columns or fold_col are missing (OOF-07)"
    - "OOFHealthValidator.validate() fails when row_coverage_ratio < 0.70 threshold (OOF-04)"
    - "OOFHealthValidator.validate() detects top1 hit rate > 0.35 or top1 ROI > 2.0 anomalies (OOF-03, profile-dependent)"
    - "OOFHealthValidator.validate() infrastructure for OOF-02 exists: when profile.enable_train_valid_overlap=True and split_metadata is expected but None, raises ValueError (D-04 fail-fast)"
    - "generate_manifest() produces deterministic JSON with all D-10 fields plus XCT-08 fields (OOF-08, XCT-08)"
    - "schema_hash and schema_dtype_hash are SHA256 of sorted column names / column:dtype pairs (XCT-05)"
    - "AbilityModel.train_oof() records ability_oof_fold column on output DataFrame (OOF-07, D-05)"
    - "ability_oof_fold column is verified by automated tests: fold assignment correctness, NA for non-validation rows"
  artifacts:
    - path: "src/validation/__init__.py"
      provides: "Package init for validation module"
    - path: "src/validation/oof_health_validator.py"
      provides: "OOFHealthValidator class, OOFHealthProfile dataclass, ValidationResult dataclass, load_validated_oof consumer function"
      exports: ["OOFHealthValidator", "OOFHealthProfile", "ValidationResult", "OOF_PREDICTIONS_PROFILE", "WIN_SELECTION_OOF_PROFILE", "load_validated_oof"]
    - path: "src/models/stage1_ability_model.py"
      provides: "Modified train_oof() with ability_oof_fold column"
      contains: "ability_oof_fold"
    - path: "tests/test_oof_health_validator.py"
      provides: "Comprehensive unit tests for all health checks"
    - path: "tests/test_oof_leakage.py"
      provides: "Extended tests for ability_oof_fold column behavior"
  key_links:
    - from: "src/validation/oof_health_validator.py"
      to: "src/pipelines/training_pipeline.py"
      via: "import OOFHealthValidator"
      pattern: "from validation.oof_health_validator import"
    - from: "src/models/stage1_ability_model.py"
      to: "src/validation/oof_health_validator.py"
      via: "ability_oof_fold column consumed by OOFHealthProfile.fold_col check"
      pattern: "ability_oof_fold"
---

<objective>
Create the OOFHealthValidator infrastructure and add fold column to AbilityModel.train_oof().

Purpose: Establish the reusable validation framework that all OOF artifacts must pass through before being consumed by downstream components (Phase 38-40). Without this, corrupted OOF artifacts silently propagate into calibrators and rankers.

Output: OOFHealthValidator class with artifact profiles, comprehensive tests, AbilityModel fold column recording, automated tests for fold column behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-ev-calibration-layers/37-CONTEXT.md
@.planning/phases/37-ev-calibration-layers/37-RESEARCH.md
@src/pipelines/training_pipeline.py
@src/models/stage1_ability_model.py
@scripts/freeze_feature_manifest.py

<interfaces>
<!-- Existing patterns the executor must replicate -->

From src/pipelines/training_pipeline.py:
```python
# Constants (lines 62-64) -- migrate to profile fields
WIN_SELECTION_OOF_MAX_TOP1_HIT_RATE = 0.35
WIN_SELECTION_OOF_MAX_TOP1_ROI = 2.0
WIN_SELECTION_OOF_MIN_GUARD_RACES = 30

# _prepare_oof_artifact (line 80): sets is_oof=True, oof_artifact_version=1
# _win_selection_oof_return_unit (line 176): ROI calculation helper
# _walk_forward_race_splits (line 197): race-level expanding window splits
# _validate_win_selection_oof_health (line 237): existing validation (to be replaced in Plan 02)
```

From src/models/stage1_ability_model.py train_oof() (line 350):
```python
# Current: oof_preds = pd.Series(np.nan, index=df.index, dtype=np.float64)
# Fold boundaries: [dates[n_dates * (i + 1) // (n_folds + 1)] for i in range(n_folds)]
# Assignment: oof_preds.loc[test_mask] = test_df["p_ability_win"].values
```

From scripts/freeze_feature_manifest.py:
```python
# SHA256 pattern: json.dumps(cols, sort_keys=True, indent=2) -> encode -> sha256
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create OOFHealthValidator with artifact profiles and comprehensive tests</name>
  <files>src/validation/__init__.py, src/validation/oof_health_validator.py, tests/test_oof_health_validator.py</files>
  <read_first>
    src/pipelines/training_pipeline.py (lines 62-64, 80-86, 176-297 for existing validation patterns and ROI calculation)
    scripts/freeze_feature_manifest.py (lines 65-88 for SHA256 manifest pattern)
    .planning/phases/37-ev-calibration-layers/37-CONTEXT.md (D-01 through D-15 for locked decisions)
    .planning/phases/37-ev-calibration-layers/37-RESEARCH.md (Pattern 1-3 code examples)
  </read_first>
  <behavior>
    - Test OOF-01: empty DataFrame raises ValueError with "OOF-01" in message
    - Test OOF-04: DataFrame with row count below 70% of expected_row_count results in FAIL status
    - Test OOF-05: DataFrame with fold_count < 3 results in FAIL status
    - Test OOF-06: DataFrame where a race_id appears in 2+ folds results in FAIL status
    - Test OOF-07: DataFrame missing required_columns or fold_col raises ValueError
    - Test OOF-03 (profile-dependent): DataFrame with top1 hit rate > 0.35 or top1 ROI > 2.0 results in FAIL status; profile without score_col skips this check
    - Test OOF-02 (profile-dependent): profile with enable_train_valid_overlap=True and split_metadata=None raises ValueError (D-04: fail-fast when metadata expected but missing); profile with enable_train_valid_overlap=False skips this check without error
    - Test XCT-05: generate_manifest() called twice on same input produces byte-identical JSON (sort_keys=True, indent=2, ensure_ascii=False)
    - Test XCT-08: manifest dict contains keys "artifact_version", "schema_hash", "source_oof_manifest_path" (placeholder for now), "train_date_range"
    - Test consumer-side: load_validated_oof() raises ValueError when manifest status is FAIL
    - Test consumer-side: load_validated_oof() raises ValueError when artifact_hash does not match file hash
    - Test schema_hash: same columns in different order produce identical schema_hash
  </behavior>
  <action>
Per D-01: Create `src/validation/__init__.py` (empty) and `src/validation/oof_health_validator.py` with:

1. `OOFHealthProfile` frozen dataclass per D-02 with fields: artifact_name (str), required_columns (tuple[str,...]), fold_col (str), score_col (str|None=None), return_cols (tuple[str,...]=()), max_top1_hit_rate (float=0.35), max_top1_roi (float=2.0), min_fold_count (int=3), row_coverage_threshold (float=0.70), min_guard_races (int=30), enable_train_valid_overlap (bool=False), manifest_path (str=""), strict_schema (bool=False). All thresholds from existing module-level constants (lines 62-64 of training_pipeline.py).

2. Two concrete profile instances: `OOF_PREDICTIONS_PROFILE` (artifact_name="oof_predictions", fold_col="ability_oof_fold", score_col="p_win_oof", return_cols=("confirmed_odds","tanodds"), required_columns=("race_id","race_date","is_oof","oof_artifact_version")) and `WIN_SELECTION_OOF_PROFILE` (artifact_name="win_selection_oof", fold_col="win_selection_oof_fold", score_col="win_market_selection_score", return_cols=("win_return_unit","win_return","confirmed_odds","tanodds"), required_columns=("race_id","race_date","is_oof","oof_artifact_version","kakuteijyuni")).

3. `ValidationResult` frozen dataclass: status (str), failures (list[str]), warnings (list[str]), metrics (dict).

4. `OOFHealthValidator` class with VALIDATOR_VERSION="1.0.0" and methods:

   a. `validate(self, df, profile, *, train_date_range=None, expected_row_count=None, split_metadata=None) -> dict[str, Any]`:
      - OOF-01 (D-03 always-on): if df.empty, raise ValueError("OOF artifact is empty (OOF-01)")
      - OOF-07 (D-03 always-on): check profile.required_columns + profile.fold_col exist in df.columns; missing -> raise ValueError with "OOF-07"
      - OOF-05 (D-03 always-on): fold_count = df[profile.fold_col].nunique(); if < profile.min_fold_count -> append failure with "OOF-05"
      - OOF-06 (D-03 always-on): df.groupby("race_id")[profile.fold_col].nunique(); max > 1 -> append failure with "OOF-06"
      - OOF-04 (D-03 always-on): if expected_row_count provided, compute coverage = len(df)/expected_row_count; if < profile.row_coverage_threshold -> append failure with "OOF-04"
      - OOF-03 (D-04 profile-dependent): only if profile.score_col is not None and profile.return_cols is non-empty. Replicate top1 calculation pattern from _validate_win_selection_oof_health lines 271-296: sort by score_col desc per race_id, take top1, compute hit_rate from kakuteijyuni==1, compute roi using return_unit pattern from _win_selection_oof_return_unit lines 176-194. If n_races >= profile.min_guard_races and (hit_rate > profile.max_top1_hit_rate or roi > profile.max_top1_roi) -> append failure with "OOF-03". If score_col or return_cols are missing from df when profile defines them -> raise ValueError (fail-fast per D-04).
      - OOF-02 (D-04 profile-dependent): only if profile.enable_train_valid_overlap is True. If split_metadata is None when this check is enabled -> raise ValueError("OOF-02 check requires split_metadata but none provided (D-04 fail-fast)"). This ensures the infrastructure exists and will fail-fast per D-04 when metadata is expected but missing. If split_metadata provided, check for overlap between train and valid race_ids; overlap count > 0 -> append failure with "OOF-02".
      - Return dict with status ("PASS" if no failures, else "FAIL"), failures, warnings, and all computed metrics.

   b. `generate_manifest(self, df, profile, artifact_hash, *, train_date_range=None, split_metadata=None) -> dict[str, Any]`:
      - Compute schema_hash: sorted(df.columns) -> json.dumps(sort_keys=True, indent=2) -> sha256 (same pattern as freeze_feature_manifest.py line 66)
      - Compute schema_dtype_hash: sorted(f"{col}:{df[col].dtype}" for col in df.columns) -> json.dumps(sort_keys=True, indent=2) -> sha256
      - Build manifest dict per D-10 with all fields: artifact_name, artifact_path, artifact_hash, artifact_version (from df["oof_artifact_version"].iloc[0] if exists), schema_hash, schema_dtype_hash, row_count, expected_row_count (if provided), row_coverage_ratio, race_count, horse_count (if "umaban" in df), fold_col, fold_count, fold_row_counts (dict of fold_num->count), fold_race_counts, fold_race_id_uniqueness (True if no OOF-06 violation), train_valid_overlap_count, same_race_multiple_fold_count, top1_score_col, top1_hit_rate, top1_roi (if profile-dependent checks apply), return_col_used, date_min, date_max, train_date_range, source_model_hash (empty string placeholder), source_code_version (empty string placeholder), generated_at (UTC isoformat), validator_version, status, failures, warnings.
      - XCT-08: include artifact_version, schema_hash, source_oof_manifest_path (empty string for self), train_date_range.

   c. `_compute_schema_hashes(df)` static method per D-11: returns (schema_hash, schema_dtype_hash) tuple.

5. `load_validated_oof(artifact_name, *, force_revalidate=False)` function per D-14:
   - Read `data/oof/manifests/index.json` to get manifest path for artifact_name
   - Load manifest JSON, check status=="PASS"
   - Read artifact Parquet file, compute SHA256 of file bytes, compare to manifest["artifact_hash"]
   - If force_revalidate: run OOFHealthValidator.validate() with appropriate profile
   - Return (df, manifest) tuple

6. `_update_index(artifact_name, manifest_path, artifact_hash)` helper:
   - Read/create `data/oof/manifests/index.json`, update artifact_name entry, write with sort_keys=True, indent=2, ensure_ascii=False

Create `tests/test_oof_health_validator.py` with pytest test functions covering all behaviors listed above. Use unittest.mock for any file I/O. No database required. Follow existing test patterns from tests/test_training_pipeline.py (pd.DataFrame construction with race_id, race_date, kakuteijyuni columns).
  </action>
  <verify>
    <automated>python -m pytest tests/test_oof_health_validator.py -v</automated>
  </verify>
  <done>
    - OOFHealthValidator.validate() raises ValueError for empty DF (OOF-01)
    - OOFHealthValidator.validate() returns FAIL for fold_count < 3 (OOF-05)
    - OOFHealthValidator.validate() returns FAIL for same race_id in multiple folds (OOF-06)
    - OOFHealthValidator.validate() raises ValueError for missing required columns (OOF-07)
    - OOFHealthValidator.validate() returns FAIL for low row coverage (OOF-04)
    - OOFHealthValidator.validate() returns FAIL for top1 hit rate/ROI anomalies (OOF-03)
    - OOFHealthValidator.validate() raises ValueError when OOF-02 enabled but split_metadata=None (D-04 fail-fast)
    - OOFHealthValidator.validate() skips OOF-02 check when profile.enable_train_valid_overlap=False
    - generate_manifest() produces deterministic JSON with all D-10 fields (OOF-08)
    - manifest includes schema_hash, schema_dtype_hash, artifact_version, train_date_range (XCT-05, XCT-08)
    - All tests pass: python -m pytest tests/test_oof_health_validator.py -v
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ability_oof_fold column to AbilityModel.train_oof() with tests</name>
  <files>src/models/stage1_ability_model.py, tests/test_oof_leakage.py</files>
  <read_first>
    src/models/stage1_ability_model.py (lines 350-401: train_oof method, fold boundary computation)
    tests/test_oof_leakage.py (existing test patterns for train_oof, _make_oof_df helper)
    .planning/phases/37-ev-calibration-layers/37-CONTEXT.md (D-05, D-06)
    .planning/phases/37-ev-calibration-layers/37-RESEARCH.md (Code Example section: fold column addition pattern)
  </read_first>
  <behavior>
    - Test: train_oof() result contains "ability_oof_fold" column
    - Test: rows assigned to test fold i have ability_oof_fold == i (0-based)
    - Test: rows not in any test fold have ability_oof_fold == NA (pd.isna)
    - Test: ability_oof_fold values are Int64Dtype (nullable integer)
    - Test: fold assignment is consistent with oof_preds assignment (same rows get fold numbers as get predictions)
  </behavior>
  <action>
**Step 1: Add tests to tests/test_oof_leakage.py**

Add 3-4 new test functions at the end of test_oof_leakage.py that verify ability_oof_fold behavior. Use the existing `_make_oof_df()` helper and the same mock patterns already in the file (patch LightGBM train/predict). Test cases:
1. `test_train_oof_records_ability_oof_fold_column`: Call train_oof(), assert "ability_oof_fold" in result.columns.
2. `test_train_oof_fold_assignment_values`: Call train_oof(), verify rows where p_ability_win is not NaN have ability_oof_fold matching their fold index (0-based). Verify rows where p_ability_win is NaN have ability_oof_fold == pd.NA.
3. `test_train_oof_fold_dtype_is_nullable_int`: Call train_oof(), verify ability_oof_fold dtype is pd.Int64Dtype() or equivalent nullable integer.

**Step 2: Modify AbilityModel.train_oof()**

Per D-05: Modify `AbilityModel.train_oof()` (starting at line 350) to record which fold each row belongs to:

1. After line 364 (`oof_preds = pd.Series(np.nan, index=df.index, dtype=np.float64)`), add a new Series: `oof_folds = pd.Series(pd.NA, index=df.index, dtype=pd.Int64Dtype())`. Use pd.Int64Dtype() nullable integer to match existing pattern for fold columns (see win_selection_oof_fold in training_pipeline.py line 139).

2. Inside the fold loop (line 377 `for i in range(n_folds):`), after line 394 (`oof_preds.loc[test_mask] = test_df["p_ability_win"].values`), add: `oof_folds.loc[test_mask] = i`. The fold index `i` is the 0-based fold number matching the boundaries array index.

3. After the fold loop completes (after line 394), before line 400 (`df["p_ability_win"] = oof_preds`), add: `df["ability_oof_fold"] = oof_folds`. Column name is "ability_oof_fold" per D-05.

4. Per D-06: Do NOT add any fold inference/guessing logic. The fold column is only populated by the actual training split. Rows that fall outside all test folds remain NA.

This is the only change to stage1_ability_model.py. No changes to train() or add_ability_probs().

**Step 3: Verify all tests pass**

Run both test files to confirm the fold column is correct and no existing tests break.
  </action>
  <verify>
    <automated>python -m pytest tests/test_oof_leakage.py -v && python -c "from models.stage1_ability_model import AbilityModel; print('import OK')"</automated>
  </verify>
  <done>
    - AbilityModel.train_oof() returns DataFrame with "ability_oof_fold" column (D-05)
    - Rows in test fold i have ability_oof_fold == i (0-based)
    - Rows not in any test fold have ability_oof_fold == NA
    - No fold inference/guessing logic added (D-06)
    - Automated tests verify fold column existence, assignment correctness, and NA behavior
    - All tests pass: python -m pytest tests/test_oof_leakage.py -v
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OOF artifact -> OOFHealthValidator | Untrusted OOF data crosses into validation. Malformed/empty artifacts must not proceed. |
| OOFHealthValidator -> Manifest JSON | Validation results written to filesystem. Manifest integrity verified by hash. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Tampering | OOF artifact files | mitigate | artifact_hash SHA256 verification on consumer-side load_validated_oof() |
| T-37-02 | Tampering | Health manifest JSON | mitigate | schema_hash detects column changes; manifest written with deterministic JSON (sort_keys=True) |
| T-37-03 | Repudiation | Legacy artifact without fold column | accept | D-07: legacy artifacts fail validation; migration is explicit one-time command |
| T-37-SC | Tampering | pip install (no new packages) | accept | No external package installs in this phase -- stdlib + existing deps only |

</threat_model>

<verification>
1. All OOFHealthValidator tests pass: `python -m pytest tests/test_oof_health_validator.py -v`
2. AbilityModel.train_oof() tests pass with ability_oof_fold verification: `python -m pytest tests/test_oof_leakage.py -v`
3. No new external dependencies introduced
4. OOFHealthProfile instances defined for both artifact types
5. OOF-02 infrastructure test confirms fail-fast when enabled without metadata
</verification>

<success_criteria>
- OOFHealthValidator class with validate() and generate_manifest() methods operational
- All 8 OOF health checks (OOF-01 through OOF-08) implemented with tests
- OOF-02 infrastructure exists: profile.enable_train_valid_overlap=True fails fast without split_metadata (D-04)
- XCT-05 deterministic JSON output verified
- XCT-08 manifest fields (version, schema_hash, source path, train date range) present
- AbilityModel.train_oof() records ability_oof_fold column with automated test verification
- Zero new external dependencies
</success_criteria>

<output>
Create `.planning/phases/37-ev-calibration-layers/37-01-SUMMARY.md` when done
</output>
