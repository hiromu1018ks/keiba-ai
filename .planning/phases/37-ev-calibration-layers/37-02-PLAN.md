---
phase: 37-ev-calibration-layers
plan: 02
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - src/pipelines/training_pipeline.py
  - tests/test_training_pipeline.py
autonomous: true
requirements:
  - OOF-01
  - OOF-02
  - OOF-03
  - OOF-04
  - OOF-05
  - OOF-06
  - OOF-07
  - OOF-08
  - XCT-05
  - XCT-08

must_haves:
  truths:
    - "oof_predictions.parquet is saved only after OOFHealthValidator.validate() passes (D-13)"
    - "win_selection_oof.parquet is saved only after OOFHealthValidator.validate() passes (D-13)"
    - "Health manifest JSON files are written after each artifact save with status=PASS (D-08, D-13)"
    - "Consumer-side manifest check occurs when WinSelectionGate reads win_selection_oof during training (D-14)"
    - "generate_ev_oof_predictions() call site records ev_oof_fold column on the resulting DataFrame (D-05)"
    - "generate_win_oof_predictions() arrays are mapped to win_oof_fold on the resulting DataFrame (D-05)"
    - "_validate_win_selection_oof_health() is fully replaced by OOFHealthValidator calls"
    - "Existing tests updated to mock OOFHealthValidator instead of _validate_win_selection_oof_health"
    - "Module-level constants WIN_SELECTION_OOF_MAX_TOP1_HIT_RATE/ROI/MIN_GUARD_RACES are removed (migrated to profile)"
  artifacts:
    - path: "src/pipelines/training_pipeline.py"
      provides: "Integrated OOFHealthValidator calls at save points and fold column wiring"
      contains: "OOFHealthValidator"
    - path: "tests/test_training_pipeline.py"
      provides: "Updated mock paths for OOFHealthValidator"
  key_links:
    - from: "src/pipelines/training_pipeline.py"
      to: "src/validation/oof_health_validator.py"
      via: "from validation.oof_health_validator import OOFHealthValidator, OOF_PREDICTIONS_PROFILE, WIN_SELECTION_OOF_PROFILE"
      pattern: "OOFHealthValidator"
    - from: "src/pipelines/training_pipeline.py"
      to: "src/models/stage1_ability_model.py"
      via: "train_oof() returns ability_oof_fold, consumed by _prepare_oof_artifact()"
      pattern: "ability_oof_fold"
---

<objective>
Wire OOFHealthValidator into the training pipeline, add fold columns to EV and Win OOF generation sites, and update tests.

Purpose: Connect the validation infrastructure from Plan 01 to actual OOF generation and save points. Without this wiring, the validator is inert and OOF artifacts still lack fold columns for EV/Win predictions.

Output: Training pipeline with producer-side validation at both save points, consumer-side check at WinSelectionGate training, fold columns on all OOF artifacts, updated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-ev-calibration-layers/37-CONTEXT.md
@.planning/phases/37-ev-calibration-layers/37-RESEARCH.md
@.planning/phases/37-ev-calibration-layers/37-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: OOFHealthValidator contracts this plan consumes -->

From src/validation/oof_health_validator.py (created in Plan 01):
```python
class OOFHealthValidator:
    VALIDATOR_VERSION = "1.0.0"
    def validate(self, df, profile, *, train_date_range=None, expected_row_count=None, split_metadata=None) -> dict[str, Any]: ...
    def generate_manifest(self, df, profile, artifact_hash, *, train_date_range=None, split_metadata=None) -> dict[str, Any]: ...

OOF_PREDICTIONS_PROFILE = OOFHealthProfile(
    artifact_name="oof_predictions",
    fold_col="ability_oof_fold",
    score_col="p_win_oof",
    ...
)
WIN_SELECTION_OOF_PROFILE = OOFHealthProfile(
    artifact_name="win_selection_oof",
    fold_col="win_selection_oof_fold",
    score_col="win_market_selection_score",
    ...
)

def load_validated_oof(artifact_name, *, force_revalidate=False) -> tuple[pd.DataFrame, dict]: ...
def _update_index(artifact_name, manifest_path, artifact_hash) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace _validate_win_selection_oof_health with OOFHealthValidator and wire fold columns at all OOF generation sites</name>
  <files>src/pipelines/training_pipeline.py</files>
  <read_first>
    src/pipelines/training_pipeline.py (lines 62-64: constants to remove; lines 80-86: _prepare_oof_artifact; lines 176-194: _win_selection_oof_return_unit; lines 237-297: _validate_win_selection_oof_health to replace; lines 500-560: OOF save points; lines 1140-1168: generate_ev_oof_predictions call site; lines 1278-1292: generate_win_oof_predictions call site; lines 1540-1554: WinSelectionGate training _validate call; lines 1646-1698: generate_win_selection_oof_frame)
    src/validation/oof_health_validator.py (Plan 01 output: OOFHealthValidator, profiles, load_validated_oof)
    .planning/phases/37-ev-calibration-layers/37-CONTEXT.md (D-05, D-06, D-13, D-14, D-15)
  </read_first>
  <action>
This task makes 5 coordinated changes to training_pipeline.py:

**Change 1: Add import and remove deprecated constants**

At the top of the file, add import: `from validation.oof_health_validator import OOFHealthValidator, OOF_PREDICTIONS_PROFILE, WIN_SELECTION_OOF_PROFILE, _update_index`

Remove the 3 module-level constants at lines 62-64: `WIN_SELECTION_OOF_MAX_TOP1_HIT_RATE`, `WIN_SELECTION_OOF_MAX_TOP1_ROI`, `WIN_SELECTION_OOF_MIN_GUARD_RACES`. These thresholds are now in the artifact profiles.

Keep `_validate_win_selection_oof_health` function body for now but add a deprecation comment — it will still be referenced internally during the transition. Actually, replace all call sites and then remove the function entirely.

**Change 2: Wire OOFHealthValidator at oof_predictions save point (lines 531-541)**

Replace the bare `oof_predictions_df.to_parquet(oof_path, index=False)` block with the producer-side validation flow per D-13:
1. Compute train_date_range from full_features_df (str of min/max race_date).
2. Call `OOFHealthValidator().validate(oof_predictions_df, OOF_PREDICTIONS_PROFILE, train_date_range=train_date_range, expected_row_count=len(full_features_df))`.
3. If result["status"] != "PASS", raise ValueError with failures.
4. Save the Parquet file.
5. Compute artifact_hash = hashlib.sha256(oof_path.read_bytes()).hexdigest().
6. Generate manifest dict via OOFHealthValidator().generate_manifest().
7. Write manifest to `data/oof/manifests/oof_predictions.health.json` with json.dumps(sort_keys=True, indent=2, ensure_ascii=False).
8. Call `_update_index("oof_predictions", manifest_path, artifact_hash)`.

Need to add `import hashlib` at top of file if not already present.

**Change 3: Wire OOFHealthValidator at win_selection_oof save point (lines 543-560)**

Replace the `_validate_win_selection_oof_health(win_oof_df, context="combined_win_selection_oof_save")` call with:
1. Call `OOFHealthValidator().validate(win_oof_df, WIN_SELECTION_OOF_PROFILE, train_date_range=train_date_range, expected_row_count=len(win_oof_df))`.
2. If result["status"] != "PASS", raise ValueError.
3. Save the Parquet file.
4. Compute artifact_hash, generate manifest, write to `data/oof/manifests/win_selection_oof.health.json`, call `_update_index`.

**Change 4: Wire fold columns at generate_ev_oof_predictions call site (line 1145)**

Per D-05 and RESEARCH Pitfall 1: `generate_ev_oof_predictions()` returns 3 numpy arrays, not a DataFrame. The fold column must be added at the call site.

At the call site (lines 1145-1149), after receiving `(oof_ev, oof_actual, oof_odds)`, the training_pipeline needs to reconstruct which fold each row belonged to. However, D-06 forbids fold inference. The solution per RESEARCH Open Question 1 recommendation: modify `generate_ev_oof_predictions()` to also return fold assignment information.

Modify `generate_ev_oof_predictions()` static method (line 1701): add a 4th return value `oof_fold_assignments` (numpy int array, NaN for non-validation rows). Inside the fold loop (line 1718), after `oof_ev_corrected[val_idx] = ...`, add `oof_fold_assignments[val_idx] = fold_idx`. Initialize as `np.full(len(df), np.nan)` before the loop. Change return signature to `tuple[np.ndarray, np.ndarray, np.ndarray, np.ndarray]`.

At the call site (line 1145), update destructuring to 4 values: `oof_ev, oof_actual, oof_odds, oof_fold_assignments = ...`. After the call, the arrays are used for `fit_ev_calibration()` — no changes needed there since it only uses the first 3 arrays. The `oof_fold_assignments` array is available for downstream use in Plan 01's artifact.

However, the actual OOF predictions Parquet at line 534 uses `full_features_df` which comes from `AbilityModel.train_oof()` output (already has ability_oof_fold from Plan 01 Task 2). The EV OOF fold array is separate from the main OOF artifact and feeds into `fit_ev_calibration()`. No Parquet change needed for ev_oof_fold — it's used internally for calibration, not saved as a separate artifact. If CONTEXT D-05 requires saving it, add `ev_oof_fold` column to the appropriate DataFrame.

Per D-05: `generate_ev_oof_predictions() -> ev_oof_fold column` — this means the call site should record fold assignments on the DataFrame used for EV calibration. Since this is internal (not a separate Parquet artifact), record the fold on df_oof as a new column: `df_oof["ev_oof_fold"] = pd.NA` (initialize), then map from the fold assignments after the call.

**Change 5: Replace _validate at WinSelectionGate training (line 1550)**

Replace `_validate_win_selection_oof_health(wsg_train_df, context=f"{surface}/win_selection_train")` with consumer-side validation using `load_validated_oof("win_selection_oof")` or direct `OOFHealthValidator().validate()` call. Since wsg_train_df is a fresh DataFrame from `generate_win_selection_oof_frame()`, use direct validation: `OOFHealthValidator().validate(wsg_train_df, WIN_SELECTION_OOF_PROFILE, train_date_range=train_date_range)`.

**Change 6: Remove deprecated _validate_win_selection_oof_health and _win_selection_oof_return_unit**

After all call sites are replaced:
- Remove `_validate_win_selection_oof_health()` function (lines 237-297). Its logic is now in OOFHealthValidator.
- Keep `_win_selection_oof_return_unit()` (lines 176-194) since OOFHealthValidator may still need the same pattern internally (it was extracted into the validator in Plan 01). If OOFHealthValidator has its own ROI calculation, this helper can be removed. Check Plan 01 output to confirm.

Add `import hashlib` at top of file alongside existing `import json` on line 9.
  </action>
  <verify>
    <automated>python -m pytest tests/test_training_pipeline.py -v --timeout=60</automated>
  </verify>
  <done>
    - OOFHealthValidator.validate() is called before oof_predictions.parquet save (D-13)
    - OOFHealthValidator.validate() is called before win_selection_oof.parquet save (D-13)
    - Health manifest JSON files are written at data/oof/manifests/ after each save (D-08)
    - generate_ev_oof_predictions() returns 4-tuple with fold assignments, and ev_oof_fold column is recorded on df_oof (D-05)
    - WinSelectionGate training uses OOFHealthValidator for validation (D-14)
    - _validate_win_selection_oof_health() function removed
    - Module-level constants WIN_SELECTION_OOF_MAX_TOP1_HIT_RATE/ROI/MIN_GUARD_RACES removed
    - All tests pass: python -m pytest tests/test_training_pipeline.py -v
  </done>
</task>

<task type="auto">
  <name>Task 2: Update test_training_pipeline.py mock paths and verify integration</name>
  <files>tests/test_training_pipeline.py</files>
  <read_first>
    tests/test_training_pipeline.py (lines 21, 319, 348, 381, 434, 495, 550: all _validate_win_selection_oof_health references)
    src/pipelines/training_pipeline.py (after Task 1 changes: new import path for OOFHealthValidator)
    .planning/phases/37-ev-calibration-layers/37-CONTEXT.md (D-13, D-14)
  </read_first>
  <action>
Per RESEARCH Pitfall 4: Update all mock references from `_validate_win_selection_oof_health` to `OOFHealthValidator.validate`:

1. At line 21: Change `from pipelines.training_pipeline import _validate_win_selection_oof_health` to `from validation.oof_health_validator import OOFHealthValidator`. If the import was used directly, update accordingly.

2. Lines 319, 348, 381: These are direct function tests of `_validate_win_selection_oof_health`. Replace with tests of `OOFHealthValidator().validate()` using `WIN_SELECTION_OOF_PROFILE`. The test inputs (DataFrame construction with race_id, kakuteijyuni, score_col) remain the same; only the function call changes.

3. Lines 434, 495, 550: These are `@patch("pipelines.training_pipeline._validate_win_selection_oof_health", ...)` decorators in integration tests. Change the patch target to `"validation.oof_health_validator.OOFHealthValidator.validate"`. The mock return_value should be a dict with "status": "PASS" and required metrics keys (top1_hit_rate, top1_roi, n_races) to match what the pipeline expects.

4. If any test asserts on `_validate_win_selection_oof_health` being called with specific keyword args (like context=, max_top1_hit_rate=), update assertions to match OOFHealthValidator.validate() signature (df, profile, train_date_range=, expected_row_count=).

5. Add 1-2 new test cases verifying that when OOFHealthValidator.validate() returns FAIL status, the pipeline raises ValueError and does NOT save the artifact. This tests the D-13 fail-fast behavior.

Run all tests to confirm: `python -m pytest tests/test_training_pipeline.py -v`
  </action>
  <verify>
    <automated>python -m pytest tests/test_training_pipeline.py -v --timeout=60</automated>
  </verify>
  <done>
    - All 3 patch decorators (lines 434, 495, 550) target OOFHealthValidator.validate instead of _validate_win_selection_oof_health
    - Direct function tests (lines 319, 348, 381) test OOFHealthValidator().validate() with WIN_SELECTION_OOF_PROFILE
    - Import at line 21 updated to import OOFHealthValidator
    - New test case: validation failure prevents artifact save (D-13)
    - All tests pass: python -m pytest tests/test_training_pipeline.py -v
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pipeline → OOFHealthValidator | Training pipeline delegates validation to OOFHealthValidator. Fail-fast prevents bad data propagation. |
| Manifest write → Filesystem | Manifest JSON written to data/oof/manifests/. Directory creation with parents=True. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-04 | Tampering | generate_ev_oof_predictions return signature change | mitigate | 4th return value is fold assignment array; callers that ignore it using unpacking will raise TypeError if not updated |
| T-37-05 | Repudiation | Removed _validate_win_selection_oof_health leaves no audit trail | mitigate | OOFHealthValidator.validate() generates manifest with status field — more auditable than old function |
| T-37-SC | Tampering | pip install (no new packages) | accept | No external package installs — stdlib + existing deps only |

</threat_model>

<verification>
1. All training pipeline tests pass: `python -m pytest tests/test_training_pipeline.py -v`
2. OOFHealthValidator tests still pass: `python -m pytest tests/test_oof_health_validator.py -v`
3. Full test suite: `python -m pytest tests/ -v`
4. No import of deprecated _validate_win_selection_oof_health remains
5. grep -r "_validate_win_selection_oof_health" returns zero results in src/ and tests/
</verification>

<success_criteria>
- OOFHealthValidator wired at both OOF artifact save points (producer-side validation per D-13)
- Health manifests generated and saved for both artifacts (D-08)
- Consumer-side validation at WinSelectionGate training (D-14)
- ev_oof_fold column recorded at generate_ev_oof_predictions call site (D-05)
- _validate_win_selection_oof_health fully removed
- All tests pass with updated mock paths
- Full test suite green: python -m pytest tests/ -v
</success_criteria>

<output>
Create `.planning/phases/37-ev-calibration-layers/37-02-SUMMARY.md` when done
</output>
