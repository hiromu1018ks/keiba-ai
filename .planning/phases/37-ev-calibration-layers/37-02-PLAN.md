---
phase: 37-ev-calibration-layers
plan: 02
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - src/pipelines/training_pipeline.py
  - tests/test_training_pipeline.py
autonomous: true
requirements:
  - OOF-01
  - OOF-03
  - OOF-04
  - OOF-05
  - OOF-06
  - OOF-07
  - OOF-08
  - XCT-05
  - XCT-08

must_haves:
  truths:
    - "oof_predictions.parquet is saved only after OOFHealthValidator.validate() passes (D-13)"
    - "win_selection_oof.parquet is saved only after OOFHealthValidator.validate() passes (D-13)"
    - "Health manifest JSON files are written after each artifact save with status=PASS (D-08, D-13)"
    - "Consumer-side manifest check occurs when WinSelectionGate reads win_selection_oof during training (D-14)"
    - "generate_ev_oof_predictions() returns 4-tuple with fold assignments and ev_oof_fold column is recorded on df_oof before df_oof_for_save copy (D-05)"
    - "ev_oof_fold is persisted in oof_predictions.parquet via df_oof_for_save"
    - "generate_win_selection_oof_frame() records win_selection_oof_fold per fold (existing line 1692, D-05 satisfied)"
    - "_validate_win_selection_oof_health() is fully replaced by OOFHealthValidator calls"
    - "Existing tests updated to mock OOFHealthValidator instead of _validate_win_selection_oof_health"
    - "Module-level constants WIN_SELECTION_OOF_MAX_TOP1_HIT_RATE/ROI/MIN_GUARD_RACES are removed (migrated to profile)"
  artifacts:
    - path: "src/pipelines/training_pipeline.py"
      provides: "Integrated OOFHealthValidator calls at save points, ev_oof_fold column wiring, generate_ev_oof_predictions 4-tuple return"
      contains: "OOFHealthValidator"
    - path: "tests/test_training_pipeline.py"
      provides: "Updated mock paths for OOFHealthValidator, new validation failure tests"
  key_links:
    - from: "src/pipelines/training_pipeline.py"
      to: "src/validation/oof_health_validator.py"
      via: "from validation.oof_health_validator import OOFHealthValidator, OOF_PREDICTIONS_PROFILE, WIN_SELECTION_OOF_PROFILE"
      pattern: "OOFHealthValidator"
    - from: "src/pipelines/training_pipeline.py"
      to: "src/models/stage1_ability_model.py"
      via: "train_oof() returns ability_oof_fold, consumed by _prepare_oof_artifact()"
      pattern: "ability_oof_fold"
    - from: "src/pipelines/training_pipeline.py"
      to: "src/models/win_benter_gate.py"
      via: "generate_win_oof_predictions() called for Benter fitting; no modification needed (arrays consumed by BenterCombination.fit(), win_selection_oof_fold recorded separately by generate_win_selection_oof_frame)"
      pattern: "generate_win_oof_predictions"
---

<objective>
Wire OOFHealthValidator into the training pipeline, add ev_oof_fold column at EV OOF generation, and update tests.

Purpose: Connect the validation infrastructure from Plan 01 to actual OOF generation and save points. Without this wiring, the validator is inert and OOF artifacts still lack fold columns for EV predictions.

Output: Training pipeline with producer-side validation at both save points, consumer-side check at WinSelectionGate training, ev_oof_fold column persisted in oof_predictions.parquet, updated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-ev-calibration-layers/37-CONTEXT.md
@.planning/phases/37-ev-calibration-layers/37-RESEARCH.md
@.planning/phases/37-ev-calibration-layers/37-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: OOFHealthValidator contracts this plan consumes -->

From src/validation/oof_health_validator.py (created in Plan 01):
```python
class OOFHealthValidator:
    VALIDATOR_VERSION = "1.0.0"
    def validate(self, df, profile, *, train_date_range=None, expected_row_count=None, split_metadata=None) -> dict[str, Any]: ...
    def generate_manifest(self, df, profile, artifact_hash, *, train_date_range=None, split_metadata=None) -> dict[str, Any]: ...

OOF_PREDICTIONS_PROFILE = OOFHealthProfile(
    artifact_name="oof_predictions",
    fold_col="ability_oof_fold",
    score_col="p_win_oof",
    ...
)
WIN_SELECTION_OOF_PROFILE = OOFHealthProfile(
    artifact_name="win_selection_oof",
    fold_col="win_selection_oof_fold",
    score_col="win_market_selection_score",
    ...
)

def load_validated_oof(artifact_name, *, force_revalidate=False) -> tuple[pd.DataFrame, dict]: ...
def _update_index(artifact_name, manifest_path, artifact_hash) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire OOFHealthValidator at save points and replace deprecated validation function</name>
  <files>src/pipelines/training_pipeline.py</files>
  <read_first>
    src/pipelines/training_pipeline.py (lines 1-15: imports; lines 62-64: constants to remove; lines 237-297: _validate_win_selection_oof_health to replace; lines 520-560: OOF save points; lines 1540-1554: WinSelectionGate training _validate call)
    src/validation/oof_health_validator.py (Plan 01 output: OOFHealthValidator, profiles, load_validated_oof)
    .planning/phases/37-ev-calibration-layers/37-CONTEXT.md (D-05, D-06, D-13, D-14, D-15)
  </read_first>
  <action>
This task makes 3 coordinated changes to training_pipeline.py. Each change is independent and sequential.

**Change 1: Add import, remove deprecated constants, remove _validate_win_selection_oof_health**

At the top of the file, add import: `from validation.oof_health_validator import OOFHealthValidator, OOF_PREDICTIONS_PROFILE, WIN_SELECTION_OOF_PROFILE, _update_index`

Also add `import hashlib` at top if not already present.

Remove the 3 module-level constants at lines 62-64: `WIN_SELECTION_OOF_MAX_TOP1_HIT_RATE`, `WIN_SELECTION_OOF_MAX_TOP1_ROI`, `WIN_SELECTION_OOF_MIN_GUARD_RACES`. These thresholds are now in the artifact profiles.

Remove the entire `_validate_win_selection_oof_health()` function (lines 237-297). Its logic is now in OOFHealthValidator.

Before removing the function, grep to find ALL call sites: `grep -n "_validate_win_selection_oof_health" src/pipelines/training_pipeline.py`. Expect 2 call sites (oof save + WinSelectionGate) plus the function definition. Replace both call sites (see Changes 2-3 below), then remove the function.

**Change 2: Wire OOFHealthValidator at oof_predictions save point (lines ~531-541)**

Locate the block where oof_predictions_df is saved to parquet (search for `oof_predictions_df.to_parquet` or the `oof_path` save block). Replace the bare save with the producer-side validation flow per D-13:
1. Compute train_date_range from full_features_df (str of min/max race_date).
2. Call `validator = OOFHealthValidator(); result = validator.validate(oof_predictions_df, OOF_PREDICTIONS_PROFILE, train_date_range=train_date_range, expected_row_count=len(full_features_df))`.
3. If result["status"] != "PASS", raise ValueError with failures.
4. Save the Parquet file.
5. Compute artifact_hash = hashlib.sha256(oof_path.read_bytes()).hexdigest().
6. Generate manifest dict via validator.generate_manifest().
7. Write manifest to `data/oof/manifests/oof_predictions.health.json` with json.dumps(sort_keys=True, indent=2, ensure_ascii=False). Ensure directory exists with mkdir(parents=True, exist_ok=True).
8. Call `_update_index("oof_predictions", manifest_path, artifact_hash)`.

**Change 3: Wire OOFHealthValidator at win_selection_oof save point (lines ~543-560) and WinSelectionGate training (line ~1550)**

For the win_selection_oof save point: replace the `_validate_win_selection_oof_health(win_oof_df, context=...)` call with the same producer-side flow as Change 2 but using WIN_SELECTION_OOF_PROFILE. Save manifest to `data/oof/manifests/win_selection_oof.health.json`.

For the WinSelectionGate training validation call (line ~1550, search for `_validate_win_selection_oof_health` in the WinSelectionGate section): replace with consumer-side validation. Since wsg_train_df is generated fresh from `generate_win_selection_oof_frame()`, use direct validation: `OOFHealthValidator().validate(wsg_train_df, WIN_SELECTION_OOF_PROFILE, train_date_range=train_date_range)`. If status != "PASS", raise ValueError.
  </action>
  <verify>
    <automated>python -m pytest tests/test_training_pipeline.py -v --timeout=60</automated>
  </verify>
  <done>
    - OOFHealthValidator.validate() is called before oof_predictions.parquet save (D-13)
    - OOFHealthValidator.validate() is called before win_selection_oof.parquet save (D-13)
    - Health manifest JSON files are written at data/oof/manifests/ after each save (D-08)
    - WinSelectionGate training uses OOFHealthValidator for validation (D-14)
    - _validate_win_selection_oof_health() function removed entirely
    - Module-level constants WIN_SELECTION_OOF_MAX_TOP1_HIT_RATE/ROI/MIN_GUARD_RACES removed
    - Zero remaining references to _validate_win_selection_oof_health in src/
  </done>
</task>

<task type="auto">
  <name>Task 2: Add ev_oof_fold column via generate_ev_oof_predictions() fold tracking</name>
  <files>src/pipelines/training_pipeline.py</files>
  <read_first>
    src/pipelines/training_pipeline.py (lines 1140-1168: generate_ev_oof_predictions call site; lines 1700-1746: generate_ev_oof_predictions static method definition; lines 1450-1460: df_oof_for_save copy timing)
    src/models/win_benter_gate.py (lines 127-190: generate_win_oof_predictions, read-only for caller audit)
    .planning/phases/37-ev-calibration-layers/37-CONTEXT.md (D-05: fold columns at generation time)
  </read_first>
  <action>
**Part A: Modify generate_ev_oof_predictions() to return fold assignments**

Per D-05: `generate_ev_oof_predictions()` must record fold assignments at generation time. This function returns numpy arrays, so the fold information must be returned as a 4th array.

1. Before modifying, grep for ALL callers: `grep -rn "generate_ev_oof_predictions" src/`. Expect exactly 2 matches: the call site (line 1145) and the definition (line 1701). If additional callers exist, plan to update all of them to handle the 4th return value.

2. Modify `generate_ev_oof_predictions()` static method (line 1701): Change return type annotation to `tuple[np.ndarray, np.ndarray, np.ndarray, np.ndarray]`. Add initialization: `oof_fold_assignments = np.full(len(df), np.nan)` before the fold loop (alongside oof_ev_corrected, oof_actual_return, oof_odds). Change the fold loop to `for fold_idx, (train_idx, val_idx) in enumerate(splits):` to get the fold index. Inside the fold loop, after `oof_ev_corrected[val_idx] = fold_val["ev_win_corrected"].values` (line 1733), add: `oof_fold_assignments[val_idx] = fold_idx`. Do NOT filter oof_fold_assignments by the `valid` mask -- return the full-length array so the caller can map it back to df_oof rows. Return as: `oof_ev_corrected[valid], oof_actual_return[valid], oof_odds[valid], oof_fold_assignments` (4th value is full-length, not filtered).

3. At the call site (line 1145): Update destructuring from 3 to 4 values: `oof_ev, oof_actual, oof_odds, ev_fold_full = self.generate_ev_oof_predictions(...)`. After the call, record ev_oof_fold on df_oof: `df_oof["ev_oof_fold"] = pd.array(ev_fold_full, dtype=pd.Int64Dtype())`. Since df_oof was sorted by race_date at line 1077 and generate_ev_oof_predictions() sorts its internal copy the same way, the indices align.

IMPORTANT timing constraint: df_oof_for_save is created at line 1456 as `df_oof.copy()`. The ev_oof_fold must be added to df_oof BEFORE line 1456 to be included in the saved artifact. Since generate_ev_oof_predictions() is called at line 1145 (well before 1456), adding ev_oof_fold to df_oof at the call site ensures it appears in df_oof_for_save and thus in oof_predictions.parquet. This satisfies D-05's requirement that fold columns are recorded at generation time and persisted.

**Part B: Confirm generate_win_oof_predictions() requires no changes**

Per D-05: `generate_win_oof_predictions()` should record `win_oof_fold`. However, its output (3 arrays) feeds into `BenterCombination.fit()` which consumes raw arrays with no DataFrame context. The persisted win selection OOF artifact is `win_selection_oof.parquet`, which already has `win_selection_oof_fold` recorded by `generate_win_selection_oof_frame()` at line 1692. Since the persisted artifact already satisfies D-05, no change needed to `generate_win_oof_predictions()`.

Confirm by grepping: `grep -rn "generate_win_oof_predictions" src/`. Expect 2 matches: import at line 1283 and call at line 1286. No modification to win_benter_gate.py required.
  </action>
  <verify>
    <automated>python -m pytest tests/test_training_pipeline.py tests/test_oof_health_validator.py -v --timeout=60</automated>
  </verify>
  <done>
    - generate_ev_oof_predictions() returns 4-tuple (3 filtered arrays + 1 full-length fold array) (D-05)
    - ev_oof_fold column is recorded on df_oof at the call site before df_oof_for_save copy (D-05)
    - ev_oof_fold is persisted in oof_predictions.parquet via df_oof_for_save
    - All callers of generate_ev_oof_predictions() handle the 4th return value (confirmed by grep)
    - generate_win_oof_predictions() requires no changes (win_selection_oof_fold recorded by generate_win_selection_oof_frame)
    - All tests pass: python -m pytest tests/test_training_pipeline.py tests/test_oof_health_validator.py -v
  </done>
</task>

<task type="auto">
  <name>Task 3: Update test_training_pipeline.py mock paths and add validation failure tests</name>
  <files>tests/test_training_pipeline.py</files>
  <read_first>
    tests/test_training_pipeline.py (lines 20-21: imports; lines 295-390: direct function tests of _validate_win_selection_oof_health; lines 434, 495, 550: patch decorators for integration tests)
    src/pipelines/training_pipeline.py (after Task 1 changes: new import path for OOFHealthValidator)
    .planning/phases/37-ev-calibration-layers/37-CONTEXT.md (D-13, D-14)
  </read_first>
  <action>
Per RESEARCH Pitfall 4: Update all mock references from `_validate_win_selection_oof_health` to `OOFHealthValidator.validate`:

1. At line 20-21: Change the import of `_validate_win_selection_oof_health` to `from validation.oof_health_validator import OOFHealthValidator`. If the import was used only for direct testing, remove it entirely since tests will mock at the pipeline's import location.

2. Lines 295, 319, 348, 381: These are direct function tests of `_validate_win_selection_oof_health`. Replace with tests of `OOFHealthValidator().validate()` using `WIN_SELECTION_OOF_PROFILE`. The test inputs (DataFrame construction with race_id, kakuteijyuni, score_col) remain the same; only the function call changes to use the new API (df, profile, keyword args).

3. Lines 434, 495, 550: These are `@patch("pipelines.training_pipeline._validate_win_selection_oof_health", ...)` decorators in integration tests. Change the patch target to `"pipelines.training_pipeline.OOFHealthValidator"` (since OOFHealthValidator is imported into training_pipeline.py module namespace) or to `"validation.oof_health_validator.OOFHealthValidator.validate"` depending on import pattern. The mock return_value should be a dict with "status": "PASS" and required metrics keys (top1_hit_rate, top1_roi, n_races) to match what the pipeline expects.

4. If any test asserts on `_validate_win_selection_oof_health` being called with specific keyword args (like context=, max_top1_hit_rate=), update assertions to match OOFHealthValidator.validate() signature (df, profile, train_date_range=, expected_row_count=).

5. Add 2 new test cases verifying the D-13 fail-fast behavior:
   - Test that when OOFHealthValidator.validate() is mocked to return {"status": "FAIL", "failures": ["test failure"]}, the pipeline raises ValueError and does NOT save the artifact.
   - Test that when validate() returns {"status": "PASS"}, the pipeline proceeds to save the artifact normally.

6. Update any tests that destructure `generate_ev_oof_predictions()` return value from 3 to 4 values, matching the signature change from Task 2.

Run all tests to confirm: `python -m pytest tests/test_training_pipeline.py -v`
  </action>
  <verify>
    <automated>python -m pytest tests/test_training_pipeline.py -v --timeout=60</automated>
  </verify>
  <done>
    - All 3 patch decorators (lines 434, 495, 550) target OOFHealthValidator instead of _validate_win_selection_oof_health
    - Direct function tests (lines ~295-390) test OOFHealthValidator().validate() with WIN_SELECTION_OOF_PROFILE
    - Import at lines 20-21 updated to import OOFHealthValidator (or removed if only used for old function)
    - New test case: validation failure prevents artifact save and raises ValueError (D-13)
    - New test case: validation pass allows artifact save normally
    - Tests updated for generate_ev_oof_predictions() 4-tuple return
    - grep -r "_validate_win_selection_oof_health" in tests/ returns zero results
    - All tests pass: python -m pytest tests/test_training_pipeline.py -v
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pipeline -> OOFHealthValidator | Training pipeline delegates validation to OOFHealthValidator. Fail-fast prevents bad data propagation. |
| Manifest write -> Filesystem | Manifest JSON written to data/oof/manifests/. Directory creation with parents=True. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-04 | Tampering | generate_ev_oof_predictions return signature change (3->4 tuple) | mitigate | 4th return value is fold assignment array; grep confirms only 1 caller in codebase (line 1145), updated in Task 2 |
| T-37-05 | Repudiation | Removed _validate_win_selection_oof_health leaves no audit trail | mitigate | OOFHealthValidator.validate() generates manifest with status field -- more auditable than old function |
| T-37-SC | Tampering | pip install (no new packages) | accept | No external package installs -- stdlib + existing deps only |

</threat_model>

<verification>
1. All training pipeline tests pass: `python -m pytest tests/test_training_pipeline.py -v`
2. OOFHealthValidator tests still pass: `python -m pytest tests/test_oof_health_validator.py -v`
3. Full test suite: `python -m pytest tests/ -v`
4. No import of deprecated _validate_win_selection_oof_health remains: `grep -r "_validate_win_selection_oof_health" src/ tests/` returns zero
5. generate_ev_oof_predictions() has exactly one caller: `grep -rn "generate_ev_oof_predictions" src/`
</verification>

<success_criteria>
- OOFHealthValidator wired at both OOF artifact save points (producer-side validation per D-13)
- Health manifests generated and saved for both artifacts (D-08)
- Consumer-side validation at WinSelectionGate training (D-14)
- ev_oof_fold column recorded at generate_ev_oof_predictions call site and persisted in oof_predictions.parquet (D-05)
- win_selection_oof_fold already recorded by generate_win_selection_oof_frame() (D-05 satisfied for win selection OOF)
- _validate_win_selection_oof_health fully removed from both src/ and tests/
- All tests pass with updated mock paths
- Full test suite green: python -m pytest tests/ -v
</success_criteria>

<output>
Create `.planning/phases/37-ev-calibration-layers/37-02-SUMMARY.md` when done
</output>
