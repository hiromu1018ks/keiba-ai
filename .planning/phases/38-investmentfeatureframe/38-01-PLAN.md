---
phase: 38-investmentfeatureframe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/investment/__init__.py
  - src/investment/schema_registry.py
  - src/investment/leakage.py
autonomous: true
requirements:
  - IFF-04
  - IFF-05

must_haves:
  truths:
    - "InvestmentFeatureSpec frozen dataclass defines all 90-130 feature specs with complete metadata"
    - "FEATURE_SPECS dict contains specs for all 9 categories with correct column counts"
    - "validate_no_post_race_leakage() rejects any POST_RACE column in output"
    - "validate_oof_safe_sources() rejects in-sample-only columns in train_sources"
    - "validate_schema_identity() asserts train/infer column name, order, and dtype equivalence"
  artifacts:
    - path: "src/investment/__init__.py"
      provides: "Package initialization with public API exports"
    - path: "src/investment/schema_registry.py"
      provides: "InvestmentFeatureSpec frozen dataclass + FEATURE_SPECS dict"
      exports: ["InvestmentFeatureSpec", "FEATURE_SPECS", "CATEGORY_ORDER", "ALL_IF_COLUMNS"]
    - path: "src/investment/leakage.py"
      provides: "Leakage detection functions"
      exports: ["validate_no_post_race_leakage", "validate_oof_safe_sources", "validate_schema_identity", "IN_SAMPLE_ONLY_COLS"]
  key_links:
    - from: "src/investment/leakage.py"
      to: "src/domain/types.py"
      via: "import POST_RACE_COLS"
      pattern: "from domain\\.types import POST_RACE_COLS"
    - from: "src/investment/__init__.py"
      to: "src/investment/schema_registry.py"
      via: "import InvestmentFeatureSpec, FEATURE_SPECS"
      pattern: "from investment\\.schema_registry import"
---

<objective>
InvestmentFeatureFrameの基盤モジュールを構築する。

Purpose: 9カテゴリ90-130列の投資特徴量スキーマレジストリと漏洩検出バリデータを定義し、後続のビルダー・キャッシュ・テストが依存する型安全な契約を提供する。
Output: src/investment/ パッケージ (schema_registry.py, leakage.py, __init__.py)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-investmentfeatureframe/38-CONTEXT.md
@.planning/phases/38-investmentfeatureframe/38-RESEARCH.md
@src/domain/types.py
@src/validation/oof_health_validator.py
@tests/test_post_race_leakage.py

<interfaces>
<!-- Phase 37 OOF validator patterns -->
From src/validation/oof_health_validator.py:
```python
class OOFHealthValidator:
    VALIDATOR_VERSION = "1.0.0"
    @staticmethod
    def _compute_schema_hashes(df: pd.DataFrame) -> tuple[str, str]:
        cols_sorted = sorted(df.columns.tolist())
        schema_hash = hashlib.sha256(json.dumps(cols_sorted).encode()).hexdigest()
        ...
```

From src/domain/types.py:
```python
POST_RACE_COLS: list[str] = ["kakuteijyuni", "confirmed_odds", "ninki", "kyakusitukubun", "time", ...]  # 41 items total
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: InvestmentFeatureSpec frozen dataclass + FEATURE_SPECS dict</name>
  <files>src/investment/__init__.py, src/investment/schema_registry.py</files>
  <read_first>
    - src/domain/types.py (POST_RACE_COLS reference for leakage_class assignment)
    - .planning/phases/38-investmentfeatureframe/38-RESEARCH.md (Category Feature Selection table for all 9 categories)
    - .planning/phases/38-investmentfeatureframe/38-CONTEXT.md (D-05, D-06, D-07, D-09, D-16 decisions)
  </read_first>
  <behavior>
    - Test 1: InvestmentFeatureSpec is frozen -- assigning to a field after construction raises FrozenInstanceError
    - Test 2: FEATURE_SPECS contains entries for all 9 categories (model_prob, market_prob, model_market_gap, race_relative, odds_band, late_odds, ability_form, course_pace, uncertainty)
    - Test 3: Every spec has unique name and name starts with "if_"
    - Test 4: CATEGORY_ORDER lists all 9 categories in the correct order
    - Test 5: No spec has a name that appears in POST_RACE_COLS
    - Test 6: ALL_IF_COLUMNS returns a deduplicated list matching all spec names
    - Test 7: Required features have default_value=None and missing_indicator=None; optional features have non-None default_value
  </behavior>
  <action>
    src/investment/ パッケージディレクトリを作成。

    schema_registry.py に以下を実装 per D-16:

    1. InvestmentFeatureSpec frozen dataclass を定義。フィールド: name(str), category(str), dtype(str), train_sources(tuple[str,...]), infer_sources(tuple[str,...]), required(bool), default_value(float|int|None), missing_indicator(str|None), leakage_class(str), description(str)。__post_init__ で name prefix "if_" を検証。

    2. CATEGORY_ORDER = ("model_prob", "market_prob", "model_market_gap", "race_relative", "odds_band", "late_odds", "ability_form", "course_pace", "uncertainty") を定義。

    3. FEATURE_SPECS: dict[str, InvestmentFeatureSpec] を定義。RESEARCH.md "Category Feature Selection" 表の全90-130列を投機。各カテゴリの列数はD-05の範囲内: model_prob(8-12), market_prob(6-10), model_market_gap(10-16), race_relative(12-18), odds_band(6-10), late_odds(8-12), ability_form(15-25), course_pace(10-18), uncertainty(10-16)。具体的なspec定義はRESEARCH.mdの各カテゴリ表の「出力名」「ソース(train)」「ソース(infer)」「派生」「Required」列に従う。

    4. train_sources に in-sample-only 列 (p_win_pred, ev_win, ev_win_calibrated, p_win_final, edge_win) を含めない (D-13)。

    5. POST_RACE_COLS に含まれる列を source に持つ spec の leakage_class は "post_race" とし、FEATURE_SPECS には含めない (D-05, IFF-05)。

    6. derived features (if_ev_raw, if_edge_win, if_logit_gap, if_abs_logit_gap, if_edge_rank_in_race, if_edge_zscore_in_race, if_top3_gap, if_field_ev_dispersion, if_conformal_width, if_ev_uncertainty_ratio, if_odds_band_median_ev, if_odds_band_count 等) は train_sources/infer_sources を空タプルにし、builder が計算することを示す。

    7. ALL_IF_COLUMNS: list[str] を関数またはプロパティとして定義、FEATURE_SPECS.values() の name を CATEGORY_ORDER 順で返す。

    8. src/investment/__init__.py に公開APIのimportを記述: InvestmentFeatureSpec, FEATURE_SPECS, CATEGORY_ORDER, ALL_IF_COLUMNS。
  </action>
  <verify>
    <automated>python -m pytest tests/test_investment_schema.py -v -x</automated>
  </verify>
  <done>
    - InvestmentFeatureSpec frozen dataclass が全10フィールドを持つ
    - FEATURE_SPECS が9カテゴリ90-130列のspecを含む
    - 全spec名が "if_" プレフィックス付き
    - POST_RACE_COLS と名前重複なし
    - CATEGORY_ORDER が9カテゴリを正順で含む
    - __init__.py が正しい公開APIをexport
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Leakage detection module (leakage.py)</name>
  <files>src/investment/leakage.py</files>
  <read_first>
    - src/domain/types.py (POST_RACE_COLS list)
    - tests/test_post_race_leakage.py (3-layer leakage test pattern)
    - .planning/phases/38-investmentfeatureframe/38-CONTEXT.md (D-29, D-13 decisions)
    - .planning/phases/38-investmentfeatureframe/38-RESEARCH.md (Leakage Detection pattern, Common Pitfalls section)
  </read_first>
  <behavior>
    - Test 1: validate_no_post_race_leakage(["race_id", "if_p_win", "kakuteijyuni"]) raises ValueError mentioning "kakuteijyuni"
    - Test 2: validate_no_post_race_leakage(["race_id", "if_p_win"]) passes without error
    - Test 3: validate_oof_safe_sources() with a spec having train_sources=("p_win_pred",) returns violation containing spec name
    - Test 4: validate_oof_safe_sources() with all specs having only OOF-safe sources returns empty list
    - Test 5: validate_schema_identity() with matching columns and dtypes passes
    - Test 6: validate_schema_identity() with different column order raises AssertionError
    - Test 7: validate_schema_identity() with different dtype raises AssertionError
    - Test 8: IN_SAMPLE_ONLY_COLS contains "p_win_pred", "ev_win", "ev_win_calibrated", "p_win_final", "edge_win"
  </behavior>
  <action>
    leakage.py に3つの検証関数と1つの定数を実装 per D-29, IFF-05, VAL-01:

    1. IN_SAMPLE_ONLY_COLS: frozenset[str] = frozenset({"p_win_pred", "ev_win", "ev_win_calibrated", "p_win_final", "edge_win"}) を定義。train mode で拒否すべき列。

    2. validate_no_post_race_leakage(output_columns: list[str]) -> None: output_columns と POST_RACE_COLS の積集合が空であることを検証。重複がある場合は ValueError を raise、メッセージに重複列名を含む。test_post_race_leakage.py Layer 2 パターンを踏襲。

    3. validate_oof_safe_sources(specs: dict[str, InvestmentFeatureSpec]) -> list[str]: 全spec の train_sources と IN_SAMPLE_ONLY_COLS の積集合を検証。違反があれば "spec_name: {overlap}" 形式の文字列リストを返す。例外は投げず、呼び出し元が処理。

    4. validate_schema_identity(train_df: pd.DataFrame, infer_df: pd.DataFrame) -> None: (a) 列リストの同一性を assert (list comparison, not set), (b) 各列の dtype 同一性を assert per IFF-03。失敗時は差分情報を含むメッセージ。

    型アノテーション必須 (mypy disallow_untyped_defs)。
  </action>
  <verify>
    <automated>python -m pytest tests/test_investment_leakage.py -v -x</automated>
  </verify>
  <done>
    - validate_no_post_race_leakage() がPOST_RACE列を検出してValueError
    - validate_oof_safe_sources() がin-sample列を含むspecを検出
    - validate_schema_identity() が列名・列順・dtypeの不一致を検出
    - IN_SAMPLE_ONLY_COLS が5つのin-sample-only列を含む
    - 全関数に型アノテーションあり
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema_registry → POST_RACE_COLS | スキーマ定義がPOST_RACE列を含まないことを保証 |
| leakage.py → domain/types | POST_RACE_COLS を外部定義から参照、ハードコードしない |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Information Disclosure | schema_registry.py | mitigate | leakage_class="post_race" のspecをFEATURE_SPECSに含めない |
| T-38-02 | Information Disclosure | leakage.py | mitigate | IN_SAMPLE_ONLY_COLS と POST_RACE_COLS による二重チェック |
| T-38-SC | Tampering | pip installs | accept | 新規外部パッケージなし |
</threat_model>

<verification>
python -m pytest tests/test_investment_schema.py tests/test_investment_leakage.py -v
</verification>

<success_criteria>
- InvestmentFeatureSpec frozen dataclass がFEATURE_SPECS dict と共に定義済み
- 9カテゴリ90-130列のspecが全て定義済み
- 3つのleakage検証関数がテスト通過
- 全モジュールがmypy strict互換 (型アノテーション完備)
- src/investment/__init__.py が正しくexport
</success_criteria>

<output>
Create `.planning/phases/38-investmentfeatureframe/38-01-SUMMARY.md` when done
</output>
