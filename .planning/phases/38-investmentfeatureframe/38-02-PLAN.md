---
phase: 38-investmentfeatureframe
plan: 02
type: execute
wave: 2
depends_on:
  - 38-01
files_modified:
  - src/investment/manifest.py
  - src/investment/cache.py
  - src/investment/feature_frame.py
  - src/investment/__init__.py
autonomous: true
requirements:
  - IFF-01
  - IFF-02
  - IFF-03
  - IFF-06
  - IFF-07

must_haves:
  truths:
    - "build_frame(df, mode='train') produces 90-130 if_* columns using OOF-safe sources"
    - "build_frame(df, mode='infer') produces identical schema (same columns, order, dtypes) using production sources"
    - "Required feature missing from input raises ValueError in both modes"
    - "Optional feature missing from input produces NaN + missing indicator column"
    - "Output columns are sorted: identity cols, then CATEGORY_ORDER spec order, then missing indicators"
    - "Manifest contains feature_version, schema_hash, source_artifact_hash, builder_version, mode, generated_at"
    - "Cache hit returns identical DataFrame as fresh compute; cache miss stores computed result + sidecar manifest"
  artifacts:
    - path: "src/investment/manifest.py"
      provides: "InvestmentFrameManifest generation + schema hash computation"
      exports: ["generate_investment_manifest", "compute_investment_schema_hash"]
    - path: "src/investment/cache.py"
      provides: "InvestmentFrameCache with Parquet + sidecar JSON pattern"
      exports: ["InvestmentFrameCache"]
    - path: "src/investment/feature_frame.py"
      provides: "InvestmentFeatureFrameBuilder with build_frame(), build_train_frame(), build_inference_frame()"
      exports: ["InvestmentFeatureFrameBuilder", "build_frame"]
  key_links:
    - from: "src/investment/feature_frame.py"
      to: "src/investment/schema_registry.py"
      via: "import FEATURE_SPEPS, CATEGORY_ORDER, ALL_IF_COLUMNS"
      pattern: "from investment\\.schema_registry import"
    - from: "src/investment/feature_frame.py"
      to: "src/investment/leakage.py"
      via: "import validate_no_post_race_leakage"
      pattern: "from investment\\.leakage import"
    - from: "src/investment/cache.py"
      to: "src/investment/manifest.py"
      via: "import generate_investment_manifest"
      pattern: "from investment\\.manifest import"
    - from: "src/investment/manifest.py"
      to: "src/validation/oof_health_validator.py"
      via: "pattern: hashlib.sha256 + json.dumps(sort_keys=True)"
      pattern: "hashlib\\.sha256"
---

<objective>
投資特徴量フレームのビルダー、マニフェスト生成、キャッシュ機構を実装する。

Purpose: build_frame(df, mode) 単一API でデュアルモード対応の投資特徴量を生成し、Parquet + sidecar manifest による決定性キャッシュを実現する。
Output: feature_frame.py (ビルダー), manifest.py (マニフェスト), cache.py (キャッシュ)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-investmentfeatureframe/38-CONTEXT.md
@.planning/phases/38-investmentfeatureframe/38-RESEARCH.md
@.planning/phases/38-investmentfeatureframe/38-01-SUMMARY.md
@src/validation/oof_health_validator.py
@src/domain/types.py

<interfaces>
<!-- From Plan 01 (must be completed first) -->
From src/investment/schema_registry.py:
```python
@dataclass(frozen=True)
class InvestmentFeatureSpec:
    name: str
    category: str
    dtype: str
    train_sources: tuple[str, ...]
    infer_sources: tuple[str, ...]
    required: bool
    default_value: float | int | None
    missing_indicator: str | None
    leakage_class: str
    description: str

FEATURE_SPECS: dict[str, InvestmentFeatureSpec]  # 90-130 entries
CATEGORY_ORDER: tuple[str, ...]  # 9 categories
ALL_IF_COLUMNS: list[str]
```

From src/investment/leakage.py:
```python
IN_SAMPLE_ONLY_COLS: frozenset[str]
def validate_no_post_race_leakage(output_columns: list[str]) -> None: ...
def validate_oof_safe_sources(specs: dict[str, InvestmentFeatureSpec]) -> list[str]: ...
def validate_schema_identity(train_df: pd.DataFrame, infer_df: pd.DataFrame) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Manifest module (manifest.py)</name>
  <files>src/investment/manifest.py</files>
  <read_first>
    - src/validation/oof_health_validator.py (lines 280-335: generate_manifest and _compute_schema_hashes patterns)
    - .planning/phases/38-investmentfeatureframe/38-CONTEXT.md (D-30: manifest fields)
    - .planning/phases/38-investmentfeatureframe/38-RESEARCH.md (Manifest pattern code example)
  </read_first>
  <behavior>
    - Test 1: compute_investment_schema_hash produces deterministic hash for same columns+dtypes
    - Test 2: compute_investment_schema_hash produces different hash for different columns
    - Test 3: generate_investment_manifest returns dict with keys: artifact_name, builder_version, feature_version, generated_at, mode, row_count, schema_hash, schema_dtype_hash, source_artifact_hash, source_oof_manifest_path, column_count
    - Test 4: generated_at is ISO 8601 UTC format
    - Test 5: manifest schema_hash matches compute_investment_schema_hash output
  </behavior>
  <action>
    manifest.py に2つの関数を実装 per D-30, IFF-07:

    1. compute_investment_schema_hash(df: pd.DataFrame) -> str: OOFHealthValidator._compute_schema_hashes パターンを踏襲。sorted(df.columns.tolist()) を SHA256 ハッシュ化。戻り値は hexdigest文字列。

    2. generate_investment_manifest(df: pd.DataFrame, *, feature_version: str, builder_version: str, mode: str, source_artifact_hash: str, source_oof_manifest_path: str | None = None) -> dict[str, Any]: 戻り値の必須キー: artifact_name("investment_feature_frame"), builder_version, feature_version, generated_at(datetime.now(timezone.utc).isoformat()), mode, row_count, schema_hash, schema_dtype_hash, source_artifact_hash, source_oof_manifest_path, column_count。schema_hash と schema_dtype_hash は _compute_schema_hashes パターンで計算。

    3. __all__ に関数名をexport。

    型アノテーション必須。json.dumps(sort_keys=True) で決定性保証。
  </action>
  <verify>
    <automated>python -m pytest tests/test_investment_manifest.py -v -x</automated>
  </verify>
  <done>
    - compute_investment_schema_hash が決定的ハッシュを生成
    - generate_investment_manifest が全D-30必須フィールドを含むdictを返す
    - generated_at がISO 8601形式
    - テスト全通過
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cache module (cache.py)</name>
  <files>src/investment/cache.py</files>
  <read_first>
    - src/db/parquet_store.py (ParquetStore pattern for Parquet I/O)
    - .planning/phases/38-investmentfeatureframe/38-CONTEXT.md (D-21~D-27: cache design)
    - .planning/phases/38-investmentfeatureframe/38-RESEARCH.md (Caching Patterns)
    - src/investment/manifest.py (from Task 1 of this plan)
  </read_first>
  <behavior>
    - Test 1: InvestmentFrameCache._compute_cache_key returns deterministic string for same inputs
    - Test 2: _compute_cache_key returns different string for different mode
    - Test 3: _compute_cache_key returns different string for different source_artifact_hash
    - Test 4: Cache miss (no parquet file) returns None from load_cached
    - Test 5: Cache hit returns DataFrame and manifest dict; DataFrame matches stored data
    - Test 6: Cache save creates .parquet + .manifest.json sidecar file
    - Test 7: Corrupted sidecar manifest (wrong schema_hash) triggers cache miss (returns None)
  </behavior>
  <action>
    cache.py に InvestmentFrameCache クラスを実装 per D-21~D-27, IFF-06:

    1. InvestmentFrameCache クラス: __init__(self, cache_dir: str = "data/features/investment_frame") で初期化。

    2. _compute_cache_key(self, mode: str, feature_version: str, source_artifact_hash: str, schema_hash: str, builder_version: str) -> str: mode + feature_version + source_artifact_hash + schema_hash + builder_version を連結してSHA256ハッシュ化 (D-23)。戻り値は "{mode}/{feature_version}_{hash_prefix}.parquet" 形式のパス文字列 (D-22)。

    3. load_cached(self, cache_key: str, expected_schema_hash: str) -> tuple[pd.DataFrame, dict] | None: parquetファイル + sidecar manifest JSON を読込。manifest の schema_hash と expected_schema_hash が一致する場合のみ DataFrame + manifest を返す。不一致または不在時は None (D-24)。

    4. save(self, cache_key: str, df: pd.DataFrame, manifest: dict) -> None: parquetファイルを pyarrow で書込、sidecar manifest JSON を json.dumps(sort_keys=True, indent=2) で書込 (D-21)。

    5. load_or_compute(self, *, df: pd.DataFrame, mode: str, feature_version: str, source_artifact_hash: str, builder_version: str, compute_fn: Callable) -> tuple[pd.DataFrame, dict]): キャッシュを確認、hitなら返す。missなら compute_fn(df, mode) を呼んで結果をキャッシュ保存して返す。

    型アノテーション必須。pathlib.Path を使用してパス構築。
  </action>
  <verify>
    <automated>python -m pytest tests/test_investment_cache.py -v -x</automated>
  </verify>
  <done>
    - _compute_cache_key が決定的
    - load_cached がキャッシュヒット時に正しいDataFrameを返す
    - save がparquet + sidecar JSONを生成
    - schema_hash不一致時にキャッシュミスを返す
    - テスト全通過
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Feature frame builder (feature_frame.py) + __init__.py update</name>
  <files>src/investment/feature_frame.py, src/investment/__init__.py</files>
  <read_first>
    - src/investment/schema_registry.py (FEATURE_SPECS, CATEGORY_ORDER, ALL_IF_COLUMNS)
    - src/investment/leakage.py (validate_no_post_race_leakage)
    - src/investment/manifest.py (generate_investment_manifest)
    - src/investment/cache.py (InvestmentFrameCache)
    - .planning/phases/38-investmentfeatureframe/38-CONTEXT.md (D-10, D-11, D-12, D-13, D-14, D-15, D-18, D-19)
    - .planning/phases/38-investmentfeatureframe/38-RESEARCH.md (Dual Mode Design, Pattern 1 & 2, code examples)
  </read_first>
  <behavior>
    - Test 1: build_frame(df_with_oof_cols, mode="train") returns DataFrame with 90-130 "if_*" columns
    - Test 2: build_frame(df_with_prod_cols, mode="infer") returns DataFrame with identical column names and order as train mode
    - Test 3: build_frame raises ValueError when mode is not "train" or "infer"
    - Test 4: train mode raises ValueError when required source column (e.g. p_win_oof) is missing
    - Test 5: infer mode raises ValueError when required source column (e.g. p_win_pred) is missing
    - Test 6: Optional feature missing from input produces NaN values + missing_indicator column with value 1
    - Test 7: Optional feature present in input produces values + missing_indicator column with value 0
    - Test 8: Derived features (if_ev_raw = if_p_win * if_e_return) are computed correctly
    - Test 9: Output column order matches CATEGORY_ORDER then spec definition order within each category
    - Test 10: build_train_frame() calls build_frame with mode="train"
    - Test 11: build_inference_frame() calls build_frame with mode="infer"
    - Test 12: validate_no_post_race_leakage is called during build_frame and raises on contamination
    - Test 13: Deterministic output: same input produces byte-identical output (stable row order by race_id/umaban)
  </behavior>
  <action>
    feature_frame.py に InvestmentFeatureFrameBuilder クラスを実装 per D-10, D-11, IFF-01, IFF-02, IFF-03:

    1. BUILDERS_VERSION = "1.0.0" クラス定数。

    2. _resolve_source(self, df: pd.DataFrame, spec: InvestmentFeatureSpec, mode: str) -> pd.Series: mode に応じて train_sources または infer_sources からソース列を検索。見つからない場合: required=True なら ValueError、required=False なら default_value の Series を返す (D-18, D-19)。

    3. _compute_derived(self, result: pd.DataFrame, spec: InvestmentFeatureSpec, df: pd.DataFrame) -> pd.Series: 派生特徴量の計算。if_ev_raw = if_p_win * if_e_return、if_logit_gap = logit(if_p_win) - logit(if_implied_prob)、if_abs_logit_gap = abs(if_logit_gap)、if_conformal_width = if_conformal_upper - if_conformal_lower 等。spec.name をキーにした dispatch。

    4. build_frame(self, df: pd.DataFrame, mode: Literal["train", "infer"], *, builder_version: str = BUILDERS_VERSION) -> pd.DataFrame: メインAPI (D-10)。(a) mode検証 (D-11), (b) identity列(race_id, umaban)コピー, (c) FEATURE_SPEPS を CATEGORY_ORDER 順で走査し _resolve_source または _compute_derived を呼ぶ, (d) dtype を spec.dtype に astype, (e) missing indicator 列を追加, (f) validate_no_post_race_leakage 呼び出し, (g) 列順を identity + CATEGORY_ORDER順 + missing indicators に固定, (h) race_id/umaban で sort して返す (D-27)。

    5. build_train_frame(self, df: pd.DataFrame, **kwargs) -> pd.DataFrame: build_frame(df, mode="train", **kwargs) のwrapper (D-10)。

    6. build_inference_frame(self, df: pd.DataFrame, **kwargs) -> pd.DataFrame: build_frame(df, mode="infer", **kwargs) のwrapper (D-10)。

    7. __init__.py を更新: InvestmentFeatureFrameBuilder, build_frame を追加export。

    logit関数は numpy.log(p / (1 - p)) で実装。p=0, p=1 のエッジケースでは clip(1e-15, 1-1e-15) で回避。

    型アノテーション必須。FeatureEngine の import は禁止 (D-35)。
  </action>
  <verify>
    <automated>python -m pytest tests/test_investment_feature_frame.py -v -x</automated>
  </verify>
  <done>
    - build_frame() がtrain/infer両モードで90-130列を生成
    - train/infer出力スキーマ同一(列名・列順・dtype)
    - required feature欠損時ValueError
    - optional feature欠損時NaN + missing_indicator
    - 派生特徴量が正しく計算される
    - build_train_frame/build_inference_frame wrapperが動作
    - FeatureEngineのimportなし (D-35)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| input df → builder | 信頼できない入力DataFrame。required/optional検証必須 |
| builder → cache | 計算結果のキャッシュ保存。schema_hash検証で整合性保証 |
| cache → disk | Parquet + JSON永続化。ファイル改ざんの可能性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-03 | Information Disclosure | feature_frame.py | mitigate | train mode で in-sample-only 列を使用しない (IN_SAMPLE_ONLY_COLS) |
| T-38-04 | Tampering | cache.py | mitigate | sidecar manifest の schema_hash 検証でキャッシュ不整合を検出 |
| T-38-05 | Repudiation | manifest.py | mitigate | generated_at + source_artifact_hash で成果物のトレーサビリティ保証 |
| T-38-SC | Tampering | pip installs | accept | 新規外部パッケージなし |
</threat_model>

<verification>
python -m pytest tests/test_investment_manifest.py tests/test_investment_cache.py tests/test_investment_feature_frame.py -v
</verification>

<success_criteria>
- build_frame(df, mode="train") と build_frame(df, mode="infer") が同一スキーマの90-130列出力を生成
- required/optional graceful degradation が正しく動作
- 派生特徴量が正確に計算される
- manifest が D-30 の全フィールドを含む
- キャッシュが schema_hash 検証付きで動作
- FeatureEngine の import なし
</success_criteria>

<output>
Create `.planning/phases/38-investmentfeatureframe/38-02-SUMMARY.md` when done
</output>
