---
phase: 38-investmentfeatureframe
plan: 03
type: tdd
wave: 3
depends_on:
  - 38-02
files_modified:
  - tests/test_investment_schema.py
  - tests/test_investment_leakage.py
  - tests/test_investment_feature_frame.py
  - tests/test_investment_manifest.py
  - tests/test_investment_cache.py
autonomous: true
requirements:
  - IFF-01
  - IFF-02
  - IFF-03
  - IFF-04
  - IFF-05
  - IFF-06
  - IFF-07
  - VAL-01

must_haves:
  truths:
    - "All 90-130 features have corresponding specs in FEATURE_SPECS with complete metadata (IFF-04)"
    - "train mode output contains zero in-sample-only columns (IFF-02)"
    - "train and infer mode produce identical column names, order, and dtypes (IFF-03)"
    - "No POST_RACE column appears in InvestmentFeatureFrame output (IFF-05, VAL-01)"
    - "Cache determinism: same input + same builder_version produces identical output (IFF-06)"
    - "Manifest contains all D-30 required fields (IFF-07)"
    - "Required feature missing causes ValueError; optional feature missing produces NaN + indicator"
  artifacts:
    - path: "tests/test_investment_schema.py"
      provides: "InvestmentFeatureSpec + FEATURE_SPECS validation tests"
    - path: "tests/test_investment_leakage.py"
      provides: "Leakage detection function tests"
    - path: "tests/test_investment_feature_frame.py"
      provides: "Build frame integration tests with dual mode, derived features, schema identity"
    - path: "tests/test_investment_manifest.py"
      provides: "Manifest generation tests"
    - path: "tests/test_investment_cache.py"
      provides: "Cache hit/miss/determinism tests"
  key_links:
    - from: "tests/test_investment_feature_frame.py"
      to: "src/investment/feature_frame.py"
      via: "import InvestmentFeatureFrameBuilder"
      pattern: "from investment import InvestmentFeatureFrameBuilder"
    - from: "tests/test_investment_leakage.py"
      to: "src/domain/types.py"
      via: "import POST_RACE_COLS for cross-validation"
      pattern: "from domain.types import POST_RACE_COLS"
---

<objective>
Phase 38 全要件をカバーする包括的テストスイートを構築し、全テストが通過することを確認する。

Purpose: IFF-01~07 + VAL-01 の全要件が満たされていることを自動テストで検証する。Plan 01/02 で実装されたテストが全て通過する状態にする。
Output: 5テストファイルが全て通過する状態
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-investmentfeatureframe/38-CONTEXT.md
@.planning/phases/38-investmentfeatureframe/38-RESEARCH.md
@.planning/phases/38-investmentfeatureframe/38-01-SUMMARY.md
@.planning/phases/38-investmentfeatureframe/38-02-SUMMARY.md
@src/domain/types.py
@tests/test_post_race_leakage.py

<interfaces>
From src/investment/__init__.py:
```python
# Public API exports (Plan 01 + Plan 02)
from investment.schema_registry import InvestmentFeatureSpec, FEATURE_SPECS, CATEGORY_ORDER, ALL_IF_COLUMNS
from investment.leakage import validate_no_post_race_leakage, validate_oof_safe_sources, validate_schema_identity, IN_SAMPLE_ONLY_COLS
from investment.manifest import generate_investment_manifest, compute_investment_schema_hash
from investment.cache import InvestmentFrameCache
from investment.feature_frame import InvestmentFeatureFrameBuilder, build_frame
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema registry + leakage test files (from Plan 01 TDD tests)</name>
  <files>tests/test_investment_schema.py, tests/test_investment_leakage.py</files>
  <read_first>
    - src/investment/schema_registry.py (InvestmentFeatureSpec, FEATURE_SPECS, CATEGORY_ORDER, ALL_IF_COLUMNS)
    - src/investment/leakage.py (validate_no_post_race_leakage, validate_oof_safe_sources, validate_schema_identity, IN_SAMPLE_ONLY_COLS)
    - src/domain/types.py (POST_RACE_COLS)
    - tests/test_post_race_leakage.py (3-layer test pattern reference)
    - .planning/phases/38-investmentfeatureframe/38-CONTEXT.md (D-05, D-13, D-16, D-20)
  </read_first>
  <acceptance_criteria>
    - test_investment_schema.py contains tests for: frozen dataclass enforcement, FEATURE_SPECS category coverage, unique "if_" prefix names, CATEGORY_ORDER completeness, POST_RACE non-overlap, ALL_IF_COLUMNS correctness, required/optional field consistency
    - test_investment_leakage.py contains tests for: POST_RACE detection, OOF-safe source validation, schema identity assertion, IN_SAMPLE_ONLY_COLS completeness
    - All tests pass with `python -m pytest tests/test_investment_schema.py tests/test_investment_leakage.py -v`
  </acceptance_criteria>
  <action>
    Plan 01 で定義されたテストケースを実装する。Plan 01 の Task 1 と Task 2 の behavior ブロックに記載されたテストケースをそれぞれのファイルに実装。

    test_investment_schema.py には以下のテストクラスを作成:
    - TestInvestmentFeatureSpec: frozen dataclass の検証 (FieldAssignmentError)、__post_init__ の "if_" prefix 検証
    - TestFeatureSpecs: FEATURE_SPECS の9カテゴリ覆盖、列数範囲(90-130)、重複名なし、POST_RACE非重複、required/optional整合性
    - TestCategoryOrder: 9カテゴリの正順
    - TestAllIfColumns: ALL_IF_COLUMNS が FEATURE_SPECS の全nameと一致

    test_investment_leakage.py には以下のテストクラスを作成:
    - TestPostRaceLeakage: validate_no_post_race_leakage の検出/通過
    - TestOofSafeSources: validate_oof_safe_sources の違反検出/空リスト
    - TestSchemaIdentity: validate_schema_identity の一致/不一致(列名・列順・dtype)
    - TestInSampleOnlyCols: IN_SAMPLE_ONLY_COLS の要素確認

    テストはDB不要。unittest.mock は不要 (純粋関数テスト)。pytest 使用。
  </action>
  <verify>
    <automated>python -m pytest tests/test_investment_schema.py tests/test_investment_leakage.py -v -x</automated>
  </verify>
  <done>
    - test_investment_schema.py 全テスト通過
    - test_investment_leakage.py 全テスト通過
    - 全テストがDB不要で実行可能
  </done>
</task>

<task type="auto">
  <name>Task 2: Feature frame builder + manifest + cache test files (from Plan 02 TDD tests)</name>
  <files>tests/test_investment_feature_frame.py, tests/test_investment_manifest.py, tests/test_investment_cache.py</files>
  <read_first>
    - src/investment/feature_frame.py (InvestmentFeatureFrameBuilder, build_frame, build_train_frame, build_inference_frame, BUILDERS_VERSION)
    - src/investment/manifest.py (generate_investment_manifest, compute_investment_schema_hash)
    - src/investment/cache.py (InvestmentFrameCache)
    - src/investment/schema_registry.py (FEATURE_SPECS, CATEGORY_ORDER)
    - src/investment/leakage.py (IN_SAMPLE_ONLY_COLS)
    - .planning/phases/38-investmentfeatureframe/38-CONTEXT.md (D-10~D-15, D-27)
    - .planning/phases/38-investmentfeatureframe/38-RESEARCH.md (Category Feature Selection tables, Dual Mode Design)
  </read_first>
  <acceptance_criteria>
    - test_investment_feature_frame.py contains tests for: train mode column generation (90-130), infer mode schema identity with train, mode validation, required feature fail-fast, optional feature graceful degradation, derived feature computation, column order verification, wrapper functions, POST_RACE leakage guard, deterministic output
    - test_investment_manifest.py contains tests for: deterministic hash, different-columns-different-hash, all D-30 manifest fields, ISO 8601 generated_at, schema_hash consistency
    - test_investment_cache.py contains tests for: deterministic cache key, cache key differentiation, cache miss, cache hit, cache save creates parquet+JSON, corrupted manifest triggers miss
    - All tests pass with `python -m pytest tests/test_investment_feature_frame.py tests/test_investment_manifest.py tests/test_investment_cache.py -v`
  </acceptance_criteria>
  <action>
    Plan 02 で定義されたテストケースを実装する。Plan 02 の Task 1, 2, 3 の behavior ブロックに記載されたテストケースをそれぞれのファイルに実装。

    test_investment_manifest.py には以下のテストを作成:
    - TestComputeSchemaHash: 決定性、列違い→ハッシュ違い
    - TestGenerateManifest: 全D-30フィールド存在確認、ISO 8601形式、schema_hash一貫性

    test_investment_cache.py には以下のテストを作成:
    - TestCacheKey: _compute_cache_key 決定性、mode/Hash違い→キー違い
    - TestCacheHitMiss: 不在時None返却、保存後のDataFrame整合性、sidecar JSON存在確認
    - TestCorruptedCache: schema_hash不一致時のキャッシュミス
    ※ tmp_path fixture を使用して一時ディレクトリでテスト

    test_investment_feature_frame.py には以下のテストを作成:
    - _make_train_df() helper: train mode で必要な OOF-safe 列を含む最小 DataFrame
    - _make_infer_df() helper: infer mode で必要な本番列を含む最小 DataFrame
    - TestBuildFrameTrain: 90-130列生成、全列 "if_" prefix、identity列含む
    - TestBuildFrameInfer: infer出力の列数・列名がtrainと同一
    - TestSchemaIdentity: train_df.columns == infer_df.columns の assert、dtype 同一性
    - TestModeValidation: "invalid" mode で ValueError
    - TestRequiredFailFast: required source 欠損で ValueError
    - TestOptionalGraceful: optional source 欠損で NaN + missing_indicator=1、存在時は missing_indicator=0
    - TestDerivedFeatures: if_ev_raw = if_p_win * if_e_return 等
    - TestColumnOrder: 出力列が CATEGORY_ORDER + spec定義順
    - TestWrappers: build_train_frame が mode="train"、build_inference_frame が mode="infer" を呼ぶ
    - TestDeterminism: 同一入力で同一出力 (row order安定: race_id/umaban sort)

    テストはDB不要。tmp_path fixture でキャッシュテスト。pytest 使用。
  </action>
  <verify>
    <automated>python -m pytest tests/test_investment_manifest.py tests/test_investment_cache.py tests/test_investment_feature_frame.py -v -x</automated>
  </verify>
  <done>
    - test_investment_manifest.py 全テスト通過
    - test_investment_cache.py 全テスト通過
    - test_investment_feature_frame.py 全テスト通過
    - 全テストがDB不要・mock最小限で実行可能
  </done>
</task>

<task type="auto">
  <name>Task 3: End-to-end integration test + full suite verification</name>
  <files>tests/test_investment_integration.py</files>
  <read_first>
    - src/investment/__init__.py (全公開API)
    - src/investment/feature_frame.py (build_frame)
    - src/investment/schema_registry.py (FEATURE_SPECS, ALL_IF_COLUMNS)
    - src/investment/leakage.py (validate_oof_safe_sources)
    - .planning/phases/38-investmentfeatureframe/38-CONTEXT.md (D-20: 全検証要件)
    - .planning/ROADMAP.md (D-36: Phase 38 success criteria — already comprehensive, pre-completed)
    - .planning/REQUIREMENTS.md (D-37/D-38: VAL redistribution — already reflected, pre-completed)
  </read_first>
  <acceptance_criteria>
    - test_investment_integration.py contains: full pipeline test (build_frame train → build_frame infer → schema identity → manifest generation), leakage audit (all FEATURE_SPECS train_sources have zero IN_SAMPLE_ONLY overlap), required+optional behavior verification, column count in 90-130 range
    - python -m pytest tests/test_investment_integration.py -v passes
    - python -m pytest tests/test_investment_*.py -v passes with all tests green
    - D-36 confirmed: ROADMAP.md Phase 38 success criteria already comprehensive (pre-completed during discuss-phase)
    - D-38 confirmed: Phase 36.1.1 ROI verification migration to Phase 39/40 already recorded in REQUIREMENTS.md traceability table (pre-completed)
  </acceptance_criteria>
  <action>
    test_investment_integration.py にエンドツーエンドテストを実装:

    1. TestEndToEnd: _make_train_df() と _make_infer_df() を使って、build_frame(train) → build_frame(infer) → validate_schema_identity → generate_investment_manifest の完全パイプラインをテスト。列数が90-130の範囲内であることを確認。

    2. TestLeakageAudit: FEATURE_SPECS の全spec について validate_oof_safe_sources を実行し、違反が空リストであることを確認 (D-20)。全spec名と POST_RACE_COLS の積集合が空であることを確認 (VAL-01)。

    3. TestRequiredOptionalBehavior: required feature のソースを欠損させた DataFrame で ValueError を確認。optional feature を欠損させた DataFrame で NaN + missing_indicator を確認。

    4. TestColumnCount: 生成された特徴量の列数が RESEARCH.md の範囲内 (90-130) であることを確認。カテゴリ別の列数も D-05 の範囲内であることを確認。

    テスト実行後に full suite を実行して全テスト通過を確認:
    python -m pytest tests/test_investment_*.py -v
  </action>
  <verify>
    <automated>python -m pytest tests/test_investment_*.py -v</automated>
  </verify>
  <done>
    - 全テストスイート (test_investment_*.py) が通過
    - エンドツーエンドパイプラインが正常動作
    - 漏洩監査が全specでクリア
    - 列数が90-130の範囲内
    - 既存テスト (tests/ 全体) がregressionなし
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests → src/investment/ | テストが全信頼境界を検証 |
| tests → src/domain/types | POST_RACE_COLS を使った漏洩テスト |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-06 | Information Disclosure | test suite | mitigate | 漏洩監査テストが CI で全specを検証 |
| T-38-SC | Tampering | test infrastructure | accept | テストは外部依存なし |
</threat_model>

<verification>
python -m pytest tests/test_investment_*.py -v
python -m pytest tests/ -v --co -q 2>$null | findstr "test_investment"
</verification>

<success_criteria>
- 5テストファイル全てが通過
- エンドツーエンドパイプラインテストが通過
- 漏洩監査テストが全specでクリア
- 列数が90-130範囲内
- 既存テストにregressionなし
</success_criteria>

<output>
Create `.planning/phases/38-investmentfeatureframe/38-03-SUMMARY.md` when done
</output>
