---
phase: 39-marketawarewincalibrator
plan: 02
type: execute
wave: 2
depends_on:
  - 39-01
files_modified:
  - src/pipelines/training_pipeline.py
  - src/domain/models.py
autonomous: true
requirements:
  - CAL-01
  - CAL-03
  - CAL-04
  - CAL-05

must_haves:
  truths:
    - "TrainingPipeline trains MarketAwareWinCalibrator instead of WinBenterGate grid search + calibration comparison"
    - "generate_win_oof_predictions() extended to emit additional columns needed for calibrator feature construction"
    - "SubmodelSet contains market_aware_win_calibrator field and no longer contains win_benter, win_isotonic_calibrator, win_temperature_scaler, win_segment_calibrator"
    - "MLflow and local save/load handle the new calibrator artifact"
  artifacts:
    - path: "src/pipelines/training_pipeline.py"
      provides: "MarketAwareWinCalibrator training replacing WinBenterGate + WinSegmentCalibrator training"
      contains: "MarketAwareWinCalibrator"
    - path: "src/domain/models.py"
      provides: "Updated SubmodelSet with market_aware_win_calibrator field"
      contains: "market_aware_win_calibrator"
  key_links:
    - from: "src/pipelines/training_pipeline.py"
      to: "src/models/market_aware_win_calibrator.py"
      via: "import MarketAwareWinCalibrator, call train()"
      pattern: "from models.market_aware_win_calibrator import MarketAwareWinCalibrator"
    - from: "src/pipelines/training_pipeline.py"
      to: "src/domain/models.py"
      via: "SubmodelSet constructor with market_aware_win_calibrator="
      pattern: "market_aware_win_calibrator="
---

<objective>
TrainingPipelineにMarketAwareWinCalibrator訓練を統合し、SubmodelSetのフィールド定義を更新する。

Purpose: WinBenterGate訓練ブロック(lines 1329-1417)とWinSegmentCalibrator訓練ブロック(lines 1624-1634)をMarketAwareWinCalibrator訓練に置換する。OOF生成関数を拡張してセグメント特徴量に必要な列を追加する。SubmodelSetフィールドを更新する(CAL-04)。
Output: src/pipelines/training_pipeline.py, src/domain/models.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-marketawarewincalibrator/39-CONTEXT.md
@.planning/phases/39-marketawarewincalibrator/39-RESEARCH.md
@.planning/phases/39-marketawarewincalibrator/39-01-SUMMARY.md
@src/pipelines/training_pipeline.py
@src/domain/models.py
@src/models/market_aware_win_calibrator.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update SubmodelSet in domain/models.py -- remove old fields, add market_aware_win_calibrator</name>
  <files>src/domain/models.py</files>
  <read_first>
    src/domain/models.py (lines 240-273: SubmodelSet dataclass definition)
    src/models/market_aware_win_calibrator.py (MarketAwareWinCalibrator class to be referenced in type hint)
  </read_first>
  <behavior>
    - Test 1: SubmodelSet dataclass has field market_aware_win_calibrator: MarketAwareWinCalibrator | None = None
    - Test 2: SubmodelSet does NOT have fields: win_benter, win_isotonic_calibrator, win_temperature_scaler, win_segment_calibrator
    - Test 3: Import of MarketAwareWinCalibrator from models.market_aware_win_calibrator exists in domain/models.py
  </behavior>
  <action>
In src/domain/models.py, modify the SubmodelSet dataclass (around line 257-264):

REMOVE these 4 fields:
- win_benter: BenterCombination | None = None (line 257)
- win_isotonic_calibrator: IsotonicRegression | None = None (line 258)
- win_temperature_scaler: TemperatureScaling | None = None (line 259)
- win_segment_calibrator: WinSegmentCalibrator | None = None (line 264)

ADD this field after win_profit_selector:
- market_aware_win_calibrator: MarketAwareWinCalibrator | None = None

Add import at top of file: from models.market_aware_win_calibrator import MarketAwareWinCalibrator (using TYPE_CHECKING guard if needed to avoid circular imports, consistent with existing import pattern in the file).

Remove the import of WinSegmentCalibrator if it exists only for the SubmodelSet field (check if used elsewhere in the file first).

Update any comments that reference the old fields (e.g., "# Win Benter fields (D-12)" comment block).

BenterCombination import and isotonic_calibrator/temperature_scaler fields for PLACE prediction MUST remain unchanged -- only the win_ prefixed fields are removed.
  </action>
  <verify>
    <automated>cd C:\Users\hirom\develop\keiba-ai && python -c "from domain.models import SubmodelSet; s = SubmodelSet(); assert hasattr(s, 'market_aware_win_calibrator'); assert not hasattr(s, 'win_benter'); assert not hasattr(s, 'win_segment_calibrator'); print('SubmodelSet fields OK')"</automated>
  </verify>
  <done>
    - SubmodelSet has market_aware_win_calibrator field
    - SubmodelSet does NOT have win_benter, win_isotonic_calibrator, win_temperature_scaler, win_segment_calibrator fields
    - Place prediction fields (benter_combo, isotonic_calibrator, temperature_scaler) remain intact
    - Import of MarketAwareWinCalibrator added
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace WinBenterGate + WinSegmentCalibrator training in TrainingPipeline with MarketAwareWinCalibrator training</name>
  <files>src/pipelines/training_pipeline.py</files>
  <read_first>
    src/pipelines/training_pipeline.py (lines 1329-1430: Win Benter training block to replace)
    src/pipelines/training_pipeline.py (lines 1620-1695: WinSegmentCalibrator training + SubmodelSet construction)
    src/pipelines/training_pipeline.py (lines 2310-2330: MLflow save for win_segment_calibrator)
    src/pipelines/training_pipeline.py (lines 2495-2533: local save for win_benter, win_isotonic, win_temp_scaler, win_segment_calibrator)
    src/models/market_aware_win_calibrator.py (train/apply API)
    src/models/win_benter_gate.py (generate_win_oof_predictions current signature and return type)
  </read_first>
  <behavior>
    - Test 1: generate_win_oof_predictions returns DataFrame with columns: p_win_oof, p_market_norm, tanodds, popularity_rank, field_size, p_win_race_rank_pct, race_id, race_date, umaban, kakuteijyuni, surface (D-18/D-19/D-20)
    - Test 2: Training pipeline creates MarketAwareWinCalibrator and calls train() on the extended OOF DataFrame
    - Test 3: SubmodelSet construction uses market_aware_win_calibrator= parameter instead of win_benter=, win_isotonic_calibrator=, win_temperature_scaler=, win_segment_calibrator=
    - Test 4: MLflow save logs market_aware_win_calibrator_{surface} artifact
    - Test 5: Local save writes market_aware_win_calibrator_{surface}.joblib
  </behavior>
  <action>
**Step A: Extend generate_win_oof_predictions() (lines 127-190 in win_benter_gate.py, called from training_pipeline.py)**

The function is defined in win_benter_gate.py and imported into training_pipeline.py. Change its return type from tuple of 3 arrays to a DataFrame with rich columns. Specifically:

1. Change signature to return pd.DataFrame instead of tuple[np.ndarray, np.ndarray, np.ndarray]
2. Inside the WF loop, accumulate rows into a list of dicts/DataFrame chunks instead of 3 arrays
3. Each fold's output DataFrame should contain these columns (D-18/D-19/D-20):
   - OOF-dependent (recomputed): p_win_oof (from fold_val["p_win_oof"]), p_win_race_rank_pct (computed as groupby(race_id).rank(pct=True, ascending=False, method="min") on p_win_oof)
   - Market/static (from fold_val): tanodds, popularity_rank, field_size, race_id, race_date, umaban, surface, kakuteijyuni
   - p_market_norm (computed as clip(1/tanodds, 0.01, 0.99))
4. Concatenate all folds into single DataFrame
5. Update the call site in training_pipeline.py to handle DataFrame return

If the function signature change is too invasive (it is also called from other locations), consider creating a NEW function generate_win_oof_calibrator_frame() that wraps generate_win_oof_predictions() and enriches its output. Check all call sites first.

**Step B: Replace WinBenterGate training block (lines 1329-1417)**

Remove the entire block that does:
- BenterCombination import and grid search (lines 1329-1406)
- compare_calibrations (lines 1412-1430+)

Replace with:
- Import MarketAwareWinCalibrator from models.market_aware_win_calibrator
- Call generate_win_oof_calibrator_frame() (or extended generate_win_oof_predictions) to get the enriched OOF DataFrame
- Instantiate MarketAwareWinCalibrator()
- Call calibrator.train(oof_df) which handles C-selection internally
- Log training_summary (best_c, beta_market guard result, c_selection_results)
- If not calibrator.is_trained (beta_market guard failed), log warning and store as shadow

Remove win_benter, win_isotonic_cal, win_temp_scaler variable declarations and assignments.

**Step C: Replace WinSegmentCalibrator training block (lines 1624-1639)**

Remove the entire block. No replacement needed -- segment conditioning is now inside MarketAwareWinCalibrator.

**Step D: Update SubmodelSet construction (lines 1664-1691)**

Remove keyword arguments: win_benter, win_isotonic_calibrator, win_temperature_scaler, win_segment_calibrator.
Add keyword argument: market_aware_win_calibrator=calibrator (from Step B).

**Step E: Update MLflow save (lines 2318-2327)**

Remove the win_segment_calibrator MLflow save block.
Add: if sub.market_aware_win_calibrator is not None and sub.market_aware_win_calibrator.is_trained: save via joblib to temp file, log_artifact as f"market_aware_win_calibrator_{surface}"

Remove win_benter MLflow save if it exists (search for benter_combo_win in MLflow section).

**Step F: Update local save (lines 2500-2533)**

Remove: win_segment_calibrator save, benter_combo_win save, isotonic_win save, temp_scale_win save.
Add: if sub.market_aware_win_calibrator is not None: sub.market_aware_win_calibrator.save(models_dir / f"market_aware_win_calibrator_{surface}.joblib")

**Step G: Update imports**

Add: from models.market_aware_win_calibrator import MarketAwareWinCalibrator
Remove imports that become unused: check if WinSegmentCalibrator import is used elsewhere before removing. The compare_calibrations, generate_reliability_data imports from win_benter_gate may no longer be needed -- verify.
  </action>
  <verify>
    <automated>cd C:\Users\hirom\develop\keiba-ai && python -m pytest tests/ -v -k "test_" --co -q 2>&1 | head -5 && python -c "from pipelines.training_pipeline import TrainingPipelineV5; print('Import OK')"</automated>
  </verify>
  <done>
    - WinBenterGate training block replaced with MarketAwareWinCalibrator training
    - WinSegmentCalibrator training block removed entirely
    - SubmodelSet constructed with market_aware_win_calibrator parameter
    - MLflow save handles market_aware_win_calibrator_{surface} artifact
    - Local save writes market_aware_win_calibrator_{surface}.joblib
    - OOF generation emits all columns needed by calibrator (D-18/D-19/D-20)
    - No references to win_benter, win_isotonic_calibrator, win_temperature_scaler, win_segment_calibrator remain in training_pipeline.py
    - TrainingPipelineV5 imports successfully
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OOF predictions -> calibrator training | OOF data must not contain train-mode predictions |
| Training pipeline -> SubmodelSet | Field changes propagate to all consumers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-04 | Tampering | OOF generation extension | mitigate | D-22: verify p_win_oof is used, not p_win_pred, in extended function |
| T-39-05 | Elevation of Privilege | SubmodelSet field removal | mitigate | Verify all consumers (RacePredictor, ModelLoader) updated in Plan 03 |
| T-39-SC | Tampering | pip installs | mitigate | No new packages; existing verified |
</threat_model>

<verification>
- TrainingPipelineV5 imports without error
- SubmodelSet construction uses market_aware_win_calibrator=
- No references to removed fields in training_pipeline.py
- OOF generation returns DataFrame with required columns
</verification>

<success_criteria>
- TrainingPipeline trains MarketAwareWinCalibrator via extended OOF data
- SubmodelSet updated with market_aware_win_calibrator field, old fields removed
- MLflow and local save/load handle new artifact
- No dangling references to win_benter/win_segment_calibrator in training_pipeline.py
</success_criteria>

<output>
Create `.planning/phases/39-marketawarewincalibrator/39-02-SUMMARY.md` when done
</output>
