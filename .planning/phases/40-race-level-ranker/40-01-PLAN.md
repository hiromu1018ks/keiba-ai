---
phase: 40-race-level-ranker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/models/race_level_ranker.py
  - src/domain/models.py
autonomous: true
requirements:
  - RNK-01
  - RNK-02
  - RNK-03
  - RNK-04

must_haves:
  truths:
    - "RaceLevelRanker exposes train() that fits Ridge models per surface for relevance and value scoring"
    - "RaceLevelRanker exposes score() that produces investment_score columns for all runners in a race"
    - "Shadow mode is_trained + deployment_status follows MarketAwareWinCalibrator pattern exactly"
    - "Combination uses fixed 0.35/0.35/0.20/0.10 weights on race-level robust percentile ranks"
  artifacts:
    - path: "src/models/race_level_ranker.py"
      provides: "RaceLevelRanker class with train(), score(), save(), load(), is_trained"
      min_lines: 150
    - path: "src/domain/models.py"
      provides: "win_race_level_ranker field on SubmodelSet"
      contains: "win_race_level_ranker"
  key_links:
    - from: "src/models/race_level_ranker.py"
      to: "src/domain/models.py"
      via: "SubmodelSet.win_race_level_ranker field"
      pattern: "win_race_level_ranker.*RaceLevelRanker"
---

<objective>
Create the RaceLevelRanker class and SubmodelSet field.

Purpose: Establish the core ranker component -- a container for per-surface Ridge models (relevance_scorer + value_scorer) with shadow mode, training, scoring, and persistence. This is the foundation that TrainingPipeline (Plan 02) and RacePredictor (Plan 03) will integrate.
Output: src/models/race_level_ranker.py (new), src/domain/models.py (updated SubmodelSet)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-race-level-ranker/40-CONTEXT.md
@.planning/phases/40-race-level-ranker/40-RESEARCH.md
@src/models/market_aware_win_calibrator.py
@src/domain/models.py

<interfaces>
<!-- Shadow mode pattern from MarketAwareWinCalibrator -->
From src/models/market_aware_win_calibrator.py:
```python
class MarketAwareWinCalibrator:
    calibrator: LogisticRegression | None = None
    _trained: bool = False
    training_summary: dict[str, Any] = field(default_factory=dict)

    @property
    def is_trained(self) -> bool:
        return self._trained and self.calibrator is not None

    def save(self, path: Path) -> None: ...
    @classmethod
    def load(cls, path: Path) -> "MarketAwareWinCalibrator": ...
```

From src/domain/models.py:
```python
@dataclass
class SubmodelSet:
    # ... existing fields ...
    market_aware_win_calibrator: MarketAwareWinCalibrator | None = None
    # Phase 40: Race-Level Ranker
    win_race_level_ranker: RaceLevelRanker | None = None  # NEW FIELD
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RaceLevelRanker class with training, scoring, persistence</name>
  <files>src/models/race_level_ranker.py</files>
  <read_first>
    src/models/market_aware_win_calibrator.py
    src/investment/schema_registry.py
    .planning/phases/40-race-level-ranker/40-CONTEXT.md (D-01 through D-28)
    .planning/phases/40-race-level-ranker/40-RESEARCH.md (Feature Availability Audit section)
  </read_first>
  <behavior>
    - Test: RaceLevelRanker() initializes with all None models, _trained=False, is_trained returns False
    - Test: train() with valid per-surface OOF DataFrame sets _trained=True, populates relevance_scorer_turf/dirt and value_scorer_turf/dirt as Ridge instances
    - Test: train() selects best alpha from [0.03, 0.1, 0.3, 1.0, 3.0, 10.0] per D-06 using NDCG@3 for relevance (tie-breaker: larger alpha) and rank correlation for value (tie-breaker: larger alpha)
    - Test: score() with _trained=True on a multi-horse DataFrame adds columns: relevance_score, value_score, relevance_score_pct, value_score_pct, calibrated_log_ev_pct, uncertainty_penalty_pct, investment_score
    - Test: investment_score = 0.35*relevance_score_pct + 0.35*value_score_pct + 0.20*calibrated_log_ev_pct - 0.10*uncertainty_penalty_pct per D-03
    - Test: score() when _trained=False returns DataFrame unchanged (shadow mode guard per D-16)
    - Test: save()/load() round-trip preserves all models, _trained state, training_summary
    - Test: _compute_relevance_target maps kakuteijyuni to [1.00, 0.55, 0.30, 0.10, 0.00] per D-08
    - Test: _compute_value_target produces composite OOF-safe target per D-09
    - Test: _race_pct_rank uses groupby(race_id).rank(pct=True, method="average") per D-27
  </behavior>
  <action>
    Create src/models/race_level_ranker.py with @dataclass class RaceLevelRanker.

    Follow the EXACT shadow mode pattern from MarketAwareWinCalibrator:
    - Fields: relevance_scorer_turf (Ridge | None), relevance_scorer_dirt (Ridge | None), value_scorer_turf (Ridge | None), value_scorer_dirt (Ridge | None), all defaulting to None
    - Field: _trained (bool, default False, repr False)
    - Field: training_summary (dict[str, Any], default_factory=dict)
    - Property: is_trained returns self._trained and self.relevance_scorer_turf is not None
    - Field: ALPHA_GRID = [0.03, 0.1, 0.3, 1.0, 3.0, 10.0] (ClassVar per D-06)

    RELEVANCE SCORER FEATURES (D-23, verified against schema_registry.py):
    Use these EXACT feature names: if_p_win_final, if_p_win_race_rank, if_p_ability_win, if_norm_finish_avg, if_closing_index, if_weighted_recent_form, if_jockey_wr, if_trainer_wr, if_blood_surface_wr, if_class_level, if_surface, if_distance_bin, if_grade_code, if_n_horses
    Add: rel_p_ability_win_rank (compute from if_p_ability_win via groupby(race_id).rank(pct=True) since it is NOT in IFF schema but source exists in relative_features.py)

    VALUE SCORER FEATURES (D-24, verified against schema_registry.py):
    Use these EXACT feature names: if_logit_gap, if_edge_win, if_ev_calibrated, if_odds_log, if_odds_band_id, if_odds_drop_60_10, if_odds_drop_30_10, if_overround, if_market_entropy, if_conformal_width, if_ev_uncertainty_ratio, if_p_win_race_rank, if_n_horses
    Derive at training/scoring time: if_odds_rank (from groupby(race_id)[if_odds_log].rank), if_abs_logit_gap (abs of if_logit_gap, substitutes for if_model_market_disagreement)

    TARGET CONSTRUCTION:
    _compute_relevance_target(kakuteijyuni): np.select([pos==1, pos==2, pos==3, pos.isin([4,5])], [1.00, 0.55, 0.30, 0.10], default=0.00) per D-08
    _compute_value_target(df): clipped_log_ev + mispricing_bonus - uncertainty_penalty per D-09 where:
    - clipped_log_ev = np.log(calibrated_ev.clip(lower=1e-6)).clip(-1.0, 1.0) using OOF calibrated EV
    - mispricing_bonus = (logit(p_model_oof) - logit(p_market_norm)).clip(-1.0, 1.0)
    - uncertainty_penalty = if_conformal_width.fillna(0) * 0.1

    TRAINING (train method):
    1. Split df by surface column into turf/dirt subsets
    2. For each surface subset: compute targets, extract feature matrices, handle NaN with fillna(0)
    3. Alpha selection: use walk_forward_race_splits (from models.win_benter_gate import _walk_forward_race_splits) with same fold definitions
    4. For each alpha in ALPHA_GRID: fit Ridge on train folds, predict on val fold, compute metric
    5. Relevance metric: NDCG@3 (sklearn.metrics.ndcg_score) -- select best alpha, tie-breaker larger alpha per D-06
    6. Value metric: Spearman rank correlation (scipy.stats.spearmanr) -- select best alpha, tie-breaker larger alpha per D-06
    7. Refit Ridge with best alpha on full surface data
    8. Store in relevance_scorer_turf/dirt, value_scorer_turf/dirt

    SCORING (score method):
    1. Guard: if not self.is_trained, return df unchanged (shadow mode)
    2. Determine surface from df[if_surface] (value 0=turf, 1=dirt per schema)
    3. Select appropriate Ridge models for each surface
    4. Extract feature matrices, fillna(0), predict with Ridge.predict()
    5. Add columns: relevance_score, value_score
    6. Compute calibrated_log_ev = np.log(df[if_ev_calibrated].clip(lower=1e-6))
    7. Compute uncertainty from if_conformal_width fillna(0)
    8. Compute race-level robust percentile ranks via _race_pct_rank for each component
    9. Combine: investment_score = 0.35*rel_pct + 0.35*val_pct + 0.20*log_ev_pct - 0.10*uncertainty_pct per D-03
    10. Add all component columns to df (per D-05 diagnostic requirement)

    PERSISTENCE (save/load):
    Follow MarketAwareWinCalibrator pattern exactly:
    - save(path): joblib.dump dict with all Ridge models, feature_names, training_summary, _trained, best_alpha records
    - load(path): classmethod, joblib.load, reconstruct RaceLevelRanker

    training_summary must contain per D-05: per-surface best_alpha, metrics, deployment_status="shadow_only", component_names listing all added columns.

    Import sklearn.linear_model.Ridge, sklearn.metrics.ndcg_score, scipy.stats.spearmanr, joblib, numpy, pandas. From models.win_benter_gate import _walk_forward_race_splits.
  </action>
  <verify>
    <automated>python -m pytest tests/test_race_level_ranker.py -v</automated>
  </verify>
  <done>
    - RaceLevelRanker class created with train(), score(), save(), load(), is_trained
    - train() fits per-surface Ridge models with alpha grid selection (NDCG@3 for relevance, rank correlation for value, tie-breaker larger alpha)
    - score() adds investment_score columns using fixed 0.35/0.35/0.20/0.10 weights on race-level robust percentile ranks
    - Shadow mode guard: score() returns df unchanged when not trained
    - save()/load() round-trip preserves all state
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Add win_race_level_ranker field to SubmodelSet</name>
  <files>src/domain/models.py</files>
  <read_first>
    src/domain/models.py (lines 233-270, SubmodelSet dataclass)
    src/models/race_level_ranker.py (created in Task 1)
  </read_first>
  <action>
    In src/domain/models.py SubmodelSet dataclass:
    1. Add import for RaceLevelRanker at top of file: add `from models.race_level_ranker import RaceLevelRanker` (follow existing import pattern for MarketAwareWinCalibrator)
    2. Add field after market_aware_win_calibrator field (line 261): `win_race_level_ranker: RaceLevelRanker | None = None` with comment "# Phase 40: Race-Level Ranker (RNK-01/02/03)"

    Ensure the import follows the same lazy/deferred pattern used by MarketAwareWinCalibrator import in the same file. Check existing imports in models.py for how TYPE_CHECKING or runtime imports are structured.
  </action>
  <verify>
    <automated>python -c "from domain.models import SubmodelSet; sm = SubmodelSet.__dataclass_fields__; assert 'win_race_level_ranker' in sm; print('OK')" 2>&1 || echo "Import check needs sys.path"</automated>
  </verify>
  <done>
    - SubmodelSet has win_race_level_ranker field typed as RaceLevelRanker | None with default None
    - Import of RaceLevelRanker follows existing pattern in domain/models.py
    - Existing tests still pass: python -m pytest tests/test_domain.py -v
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OOF data -> Ranker training | OOF predictions cross into ranker feature matrix; leakage risk if train-mode probabilities used |
| Ranker inference -> RacePredictor | investment_score added as diagnostic column only; must not alter baseline selection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Tampering | OOF-derived features in ranker | mitigate | D-15: probability-derived ranks recomputed from OOF only, _compute_relevance_target uses kakuteijyuni as label not feature |
| T-40-02 | Elevation | Premature ranker deployment | mitigate | Shadow mode: is_trained=False default, deployment_status="shadow_only", score() returns df unchanged when not trained |
| T-40-03 | Tampering | Ridge overfitting on small dataset | mitigate | L2 regularization via alpha grid, walk-forward validation, curated feature subsets (not full 94-feature IFF) per D-22 |
| T-40-SC | Tampering | pip installs | accept | No new packages -- all dependencies existing |
</threat_model>

<verification>
- python -m pytest tests/test_race_level_ranker.py -v passes all tests
- python -m pytest tests/test_domain.py -v passes (SubmodelSet field addition)
- RaceLevelRanker class has is_trained property, save/load round-trip works
</verification>

<success_criteria>
- RaceLevelRanker class exists with train(), score(), save(), load(), is_trained following MAWC pattern
- SubmodelSet.win_race_level_ranker field added with None default
- Alpha grid selection uses NDCG@3 for relevance, rank correlation for value, tie-breaker larger alpha
- investment_score = 0.35*rel_pct + 0.35*val_pct + 0.20*log_ev_pct - 0.10*uncertainty_pct
- All tests pass
</success_criteria>

<output>
Create `.planning/phases/40-race-level-ranker/40-01-SUMMARY.md` when done
</output>
