---
phase: 42-feature-routing-audit-safety-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/audit/__init__.py
  - src/audit/feature_routing_registry.py
  - scripts/run_feature_routing_audit.py
  - tests/test_feature_routing_audit.py
autonomous: true
requirements:
  - SAF-01

must_haves:
  truths:
    - "Feature routing audit confirms calibrator features (51 from MAWC build_feature_matrix) are NOT in MarketModel or RaceQualityScreener FEATURE_COLS"
    - "Feature routing audit confirms ranker features (28 from RLR RELEVANCE+VALUE+DERIVED_VALUE) are NOT in MarketModel or RaceQualityScreener FEATURE_COLS"
    - "Unit tests fail-fast if any forbidden feature appears in critical target models"
    - "Diff test catches stale registry when actual model feature lists change"
    - "Audit script produces JSON and Markdown reports with per-model status"
  artifacts:
    - path: "src/audit/feature_routing_registry.py"
      provides: "Single source of truth for forbidden features and target models"
      contains: "FORBIDDEN_CALIBRATOR_FEATURES"
    - path: "scripts/run_feature_routing_audit.py"
      provides: "CLI audit script producing JSON + Markdown"
      exports: ["run_audit"]
    - path: "tests/test_feature_routing_audit.py"
      provides: "Fail-fast unit tests + diff test"
      contains: "TestFeatureRoutingAudit"
  key_links:
    - from: "tests/test_feature_routing_audit.py"
      to: "src/audit/feature_routing_registry.py"
      via: "import FORBIDDEN_* / CRITICAL_TARGET_MODELS"
      pattern: "from audit\\.feature_routing_registry import"
    - from: "scripts/run_feature_routing_audit.py"
      to: "src/audit/feature_routing_registry.py"
      via: "import registry constants"
      pattern: "from audit\\.feature_routing_registry import"
---

<objective>
Feature Routing Audit infrastructure (SAF-01)

Purpose: Prevent the v1.8 ROI collapse recurrence by ensuring calibrator/ranker
features never leak into MarketModel or RaceQualityScreener. The audit registry
is the single source of truth consumed by both CI tests (fail-fast) and a review
script (JSON + Markdown reports).

Output: src/audit/ directory with registry, audit CLI script, and comprehensive tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-feature-routing-audit-safety-gates/42-CONTEXT.md
@.planning/phases/42-feature-routing-audit-safety-gates/42-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/models/market_aware_win_calibrator.py:
  - MarketAwareWinCalibrator -- class with build_feature_matrix(df) -> (X, feature_names)
  - build_feature_matrix() produces exactly 51 features (assert at line 194)
  - No class-level FEATURE_COLS attribute -- features constructed dynamically
  - 6 main: logit_model, logit_market, log_odds, popularity_rank_pct, p_win_race_rank_pct, field_size
  - 7 odds band one-hot: "1-2", "2-3", "3-5", "5-10", "10-30", "30-100", "100+"
  - 5 pop bucket one-hot: pop_1, pop_2_3, pop_4_6, pop_7_9, pop_10_plus
  - 3 p_rank one-hot: top_25, mid_25_75, bottom_25
  - 15 logit_model_x_{segment} interactions
  - 15 logit_market_x_{segment} interactions

From src/models/race_level_ranker.py:
  - RaceLevelRanker -- class with RELEVANCE_FEATURES (15), VALUE_FEATURES (13), DERIVED_VALUE_FEATURES (2)
  - 28 unique features (if_p_win_race_rank and if_n_horses appear in both RELEVANCE and VALUE)
  - No single FEATURE_COLS attribute -- three separate ClassVar lists

From src/models/market_model.py:
  - MarketModel.FEATURE_COLS: list[str] -- 27 features (lines 41-68)
  - CRITICAL target per D-03

From src/models/race_quality_screener.py:
  - RaceQualityScreener.FEATURE_COLS: list[str] -- 41 features (lines 39-92)
  - CRITICAL target per D-03

Advisory target models (D-03: warning/report only, not fail-fast):
  - src/models/ev_correction_model.py -- EvCorrectionModel.FEATURE_COLS (line 154) + EPCorrectionModel.FEATURE_COLS (line 485)
  - src/models/conformal_ev_model.py -- ConformalEVModel.FEATURE_COLS (line 84)
  - src/models/regime_detector.py -- RegimeDetector.FEATURE_COLS (line 61)
  - src/models/place_ability_model.py -- PlaceAbilityModel.FEATURE_COLS (line 28)
  - src/models/stage1_ability_model.py -- Stage1AbilityModel.FEATURE_COLS (line 29)
  - src/models/two_stage_return_model.py -- WinModel.FEATURE_COLS (line 59)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create audit registry with forbidden feature sets and target model definitions</name>
  <files>src/audit/__init__.py, src/audit/feature_routing_registry.py, tests/test_feature_routing_audit.py</files>
  <read_first>
    src/models/market_aware_win_calibrator.py
    src/models/race_level_ranker.py
    src/models/market_model.py
    src/models/race_quality_screener.py
    .planning/phases/42-feature-routing-audit-safety-gates/42-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: FORBIDDEN_CALIBRATOR_FEATURES has exactly 51 elements matching build_feature_matrix() output
    - Test 2: FORBIDDEN_RANKER_FEATURES has exactly 28 unique elements (union of RELEVANCE_FEATURES + VALUE_FEATURES + DERIVED_VALUE_FEATURES from RaceLevelRanker)
    - Test 3: CRITICAL_TARGET_MODELS contains exactly 2 AuditTarget entries (MarketModel, RaceQualityScreener)
    - Test 4: MarketModel.FEATURE_COLS intersection with FORBIDDEN_CALIBRATOR_FEATURES is empty
    - Test 5: MarketModel.FEATURE_COLS intersection with FORBIDDEN_RANKER_FEATURES is empty
    - Test 6: RaceQualityScreener.FEATURE_COLS intersection with FORBIDDEN_CALIBRATOR_FEATURES is empty
    - Test 7: RaceQualityScreener.FEATURE_COLS intersection with FORBIDDEN_RANKER_FEATURES is empty
    - Test 8 (diff test): FORBIDDEN_CALIBRATOR_FEATURES matches actual features from MarketAwareWinCalibrator().build_feature_matrix() on a minimal DataFrame
    - Test 9 (diff test): FORBIDDEN_RANKER_FEATURES matches union of RaceLevelRanker.RELEVANCE_FEATURES + VALUE_FEATURES + DERIVED_VALUE_FEATURES
    - Test 10: ADVISORY_TARGET_MODELS contains entries for EvCorrectionModel, EPCorrectionModel, ConformalEVModel, RegimeDetector, PlaceAbilityModel, Stage1AbilityModel, WinModel
  </behavior>
  <action>
Per D-01 and D-02: Create src/audit/__init__.py (empty with docstring) and src/audit/feature_routing_registry.py as the single source of truth for the audit.

In feature_routing_registry.py:
1. Define a frozen dataclass AuditTarget with fields: model_class_name (str), model_module (str), feature_cols_attr (str). Per D-02.
2. Define FORBIDDEN_CALIBRATOR_FEATURES as frozenset[str] containing all 51 output features from MarketAwareWinCalibrator.build_feature_matrix(). Per RESEARCH Pitfall 3: these are the DERIVED features (logit_model, logit_market_x_top_25, etc.), NOT the raw inputs (p_model, p_market, tanodds, popularity_rank, field_size, p_win_race_rank_pct). Raw inputs are legitimate features other models may use.
   - 6 main effects: logit_model, logit_market, log_odds, popularity_rank_pct, p_win_race_rank_pct, field_size
   - 7 odds band one-hot: "1-2", "2-3", "3-5", "5-10", "10-30", "30-100", "100+"
   - 5 pop bucket one-hot: pop_1, pop_2_3, pop_4_6, pop_7_9, pop_10_plus
   - 3 p_rank one-hot: top_25, mid_25_75, bottom_25
   - 15 logit_model_x_{segment}: logit_model_x_1-2, logit_model_x_2-3, logit_model_x_3-5, logit_model_x_5-10, logit_model_x_10-30, logit_model_x_30-100, logit_model_x_100+, logit_model_x_pop_1, logit_model_x_pop_2_3, logit_model_x_pop_4_6, logit_model_x_pop_7_9, logit_model_x_pop_10_plus, logit_model_x_top_25, logit_model_x_mid_25_75, logit_model_x_bottom_25
   - 15 logit_market_x_{segment}: same segment suffixes as above
3. Define FORBIDDEN_RANKER_FEATURES as frozenset[str] containing all 28 unique features from RaceLevelRanker -- union of RELEVANCE_FEATURES (15), VALUE_FEATURES (13), DERIVED_VALUE_FEATURES (2). Note if_p_win_race_rank and if_n_horses appear in both RELEVANCE and VALUE but count once. Per RESEARCH Pitfall 2.
4. Define CRITICAL_TARGET_MODELS as tuple[AuditTarget, ...] with exactly 2 entries: MarketModel from models.market_model (attr FEATURE_COLS) and RaceQualityScreener from models.race_quality_screener (attr FEATURE_COLS). Per D-03.
5. Define ADVISORY_TARGET_MODELS as tuple[AuditTarget, ...] with entries for: EvCorrectionModel from models.ev_correction_model, EPCorrectionModel from models.ev_correction_model, ConformalEVModel from models.conformal_ev_model, RegimeDetector from models.regime_detector, PlaceAbilityModel from models.place_ability_model, Stage1AbilityModel from models.stage1_ability_model, WinModel from models.two_stage_return_model. Per D-03 advisory targets.
6. Define REGISTRY_VERSION = "1.0" string constant for report tracking.
7. Add function run_feature_audit() that dynamically imports each AuditTarget model, reads its feature_cols_attr, computes intersection with forbidden sets, returns a dict with per-model results (model_name, status PASS/FAIL/WARN, forbidden_intersections, warning_intersections, checked_feature_count). This function is the shared core used by both tests and the audit script.

In tests/test_feature_routing_audit.py:
1. Create class TestFeatureRoutingAudit following the Test<ComponentName> convention from CONVENTIONS.md.
2. Write test_calibration_features_count: assert len(FORBIDDEN_CALIBRATOR_FEATURES) == 51.
3. Write test_ranker_features_count: assert len(FORBIDDEN_RANKER_FEATURES) == 28.
4. Write test_critical_targets_count: assert len(CRITICAL_TARGET_MODELS) == 2.
5. Write test_market_model_no_calibrator_leak: import MarketModel, assert its FEATURE_COLS has zero intersection with FORBIDDEN_CALIBRATOR_FEATURES.
6. Write test_market_model_no_ranker_leak: same for FORBIDDEN_RANKER_FEATURES.
7. Write test_race_quality_screener_no_calibrator_leak: import RaceQualityScreener, assert zero intersection with FORBIDDEN_CALIBRATOR_FEATURES.
8. Write test_race_quality_screener_no_ranker_leak: same for FORBIDDEN_RANKER_FEATURES.
9. Write diff test test_calibrator_features_match_build_feature_matrix: construct minimal DataFrame (3 rows with p_model, p_market, tanodds, popularity_rank, field_size, p_win_race_rank_pct, race_id), call MarketAwareWinCalibrator().build_feature_matrix(df), assert returned feature_names set equals FORBIDDEN_CALIBRATOR_FEATURES. Per RESEARCH Pitfall 1.
10. Write diff test test_ranker_features_match_class_attributes: assert FORBIDDEN_RANKER_FEATURES equals set(RaceLevelRanker.RELEVANCE_FEATURES) | set(RaceLevelRanker.VALUE_FEATURES) | set(RaceLevelRanker.DERIVED_VALUE_FEATURES). Per RESEARCH Pitfall 2.
11. Write test_advisory_targets_exist: verify ADVISORY_TARGET_MODELS has at least 5 entries.
12. Write test_run_feature_audit_passes: call run_feature_audit(), assert all critical targets have status "PASS" and advisory targets have status "PASS" or "WARN".
  </action>
  <verify>
    <automated>python -m pytest tests/test_feature_routing_audit.py -v</automated>
  </verify>
  <done>
    - src/audit/__init__.py and src/audit/feature_routing_registry.py exist
    - FORBIDDEN_CALIBRATOR_FEATURES has 51 elements, FORBIDDEN_RANKER_FEATURES has 28 elements
    - MarketModel.FEATURE_COLS and RaceQualityScreener.FEATURE_COLS have zero intersection with both forbidden sets
    - Diff tests verify registry matches actual model feature lists
    - run_feature_audit() function returns structured results
    - All 12 tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Create audit CLI script producing JSON + Markdown reports</name>
  <files>scripts/run_feature_routing_audit.py</files>
  <read_first>
    src/audit/feature_routing_registry.py
    scripts/run_shadow_comparison.py
    .planning/phases/42-feature-routing-audit-safety-gates/42-CONTEXT.md
  </read_first>
  <action>
Per D-01 (audit script for review) and D-04 (JSON + Markdown output): Create scripts/run_feature_routing_audit.py following the pattern of scripts/run_shadow_comparison.py.

1. Script entry point with argparse: --output-dir (default "data/audit"), --registry-version (optional, for verification).
2. Import run_feature_audit from audit.feature_routing_registry.
3. Call run_feature_audit() to get results dict.
4. Write JSON report to {output_dir}/feature_routing_audit.json containing: registry_version (REGISTRY_VERSION), generated_at (ISO timestamp), overall_status (PASS if all critical PASS, FAIL otherwise), models (list of per-model results from run_feature_audit with model_name, status, forbidden_intersections, warning_intersections, checked_feature_count). Per D-04 JSON format.
5. Write Markdown report to {output_dir}/feature_routing_audit.md with sections: Header (title, registry version, generated_at, overall status), Fail-Fast Models (critical targets with PASS/FAIL status and any intersections), Advisory Models (warning targets with status and any intersections), Summary (total models checked, total features checked per model, overall verdict). Per D-04 Markdown format.
6. Log summary to stdout via print().
7. Exit with code 0 if all critical models PASS, code 1 if any FAIL.
8. Include if __name__ == "__main__" block.

The script should NOT import any model classes directly -- it delegates to run_feature_audit() in the registry module which handles dynamic imports. Per D-02 (single source of truth).
  </action>
  <verify>
    <automated>python -m pytest tests/test_feature_routing_audit.py -v && python scripts/run_feature_routing_audit.py --output-dir data/audit_test && cat data/audit_test/feature_routing_audit.json | python -m json.tool > /dev/null && echo "PASS"</automated>
  </verify>
  <done>
    - scripts/run_feature_routing_audit.py exists and runs successfully
    - Produces feature_routing_audit.json with overall_status "PASS"
    - Produces feature_routing_audit.md with fail-fast/advisory sections
    - Exits code 0 when all critical models pass
    - JSON contains per-model status, intersections, and checked_feature_count
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry -> model classes | Dynamic import of model FEATURE_COLS (trusted codebase introspection) |
| script -> filesystem | Output files to data/audit/ (local filesystem) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01-SC | Tampering | pip installs | mitigate | slopcheck -- zero new packages this phase |
| T-42-02 | Elevation of Privilege | Stale registry | mitigate | Diff test catches registry drift vs actual model FEATURE_COLS |
| T-42-03 | Tampering | Audit report output | accept | Local filesystem output only, no external transmission |
</threat_model>

<verification>
1. python -m pytest tests/test_feature_routing_audit.py -v -- all 12 tests pass
2. python scripts/run_feature_routing_audit.py --output-dir data/audit -- exits 0
3. cat data/audit/feature_routing_audit.json -- overall_status is "PASS"
4. No forbidden feature intersections in MarketModel or RaceQualityScreener
</verification>

<success_criteria>
- Feature routing audit registry is the single source of truth for forbidden features (51 calibrator + 28 ranker)
- Fail-fast unit tests detect any forbidden feature in MarketModel or RaceQualityScreener
- Diff tests catch stale registry entries when model feature lists change
- Audit script produces valid JSON and Markdown reports
- Advisory targets produce warnings (not failures) for non-critical models
</success_criteria>

<output>
Create `.planning/phases/42-feature-routing-audit-safety-gates/42-01-SUMMARY.md` when done
</output>
