---
phase: 43-shadow-diagnosis
plan: 02
type: execute
wave: 2
depends_on:
  - 43-01
files_modified:
  - scripts/run_shadow_diagnosis.py
  - src/backtest/shadow_diagnosis.py
  - src/backtest/templates/shadow_diagnosis_report.html
  - tests/test_shadow_diagnosis.py
autonomous: true
requirements:
  - DIAG-01
  - DIAG-02
  - DIAG-03

must_haves:
  truths:
    - "CLI から run_shadow_diagnosis.py が実行でき、shadow_diagnosis_result.json が生成される"
    - "HTML レポートに3ステップ段階的分析がセクション化表示される"
    - "shadow_diagnosis_summary.md に主要な劣化次元とPhase 44/45への推奨が記録される"
    - "missing_inputs が3出力ファイル全てに含まれる"
  artifacts:
    - path: "scripts/run_shadow_diagnosis.py"
      provides: "CLI エントリポイント — Phase 41 成果物を読み込み ShadowDiagnosis を実行"
      exports: ["main", "build_parser"]
    - path: "src/backtest/templates/shadow_diagnosis_report.html"
      provides: "Jinja2 HTML テンプレート — 3ステップ診断レポート"
    - path: "src/backtest/shadow_diagnosis.py"
      provides: "save_diagnosis_results / ShadowDiagnosisReportGenerator の追加"
  key_links:
    - from: "scripts/run_shadow_diagnosis.py"
      to: "src/backtest/shadow_diagnosis.py"
      via: "from backtest.shadow_diagnosis import ShadowDiagnosis"
      pattern: "from backtest.shadow_diagnosis import"
    - from: "src/backtest/shadow_diagnosis.py"
      to: "src/backtest/templates/shadow_diagnosis_report.html"
      via: "Jinja2 FileSystemLoader"
      pattern: "shadow_diagnosis_report.html"
---

<objective>
ShadowDiagnosis の出力層を実装する: CLI スクリプト、JSON 出力、HTML レポート、Markdown 要約。Phase 44/45 が消費する shadow_diagnosis_result.json を生成する。

Purpose: D-04 の3ファイル出力構成 (JSON/HTML/MD) を実現し、Phase 41 CLI パターンに従う使いやすい診断ツールを提供する。
Output: scripts/run_shadow_diagnosis.py (CLI), src/backtest/templates/shadow_diagnosis_report.html (Jinja2 テンプレート), shadow_diagnosis.py への追加分 (save_diagnosis_results, ShadowDiagnosisReportGenerator)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-shadow-diagnosis/43-CONTEXT.md
@.planning/phases/43-shadow-diagnosis/43-RESEARCH.md
@.planning/phases/43-shadow-diagnosis/43-01-SUMMARY.md

<interfaces>
<!-- Plan 01 で作成される ShadowDiagnosis インターフェース -->

From src/backtest/shadow_diagnosis.py (Plan 01 出力):
```python
class ShadowDiagnosis:
    def __init__(self, input_dir: Path) -> None: ...
    def run(self) -> ShadowDiagnosisResult: ...

@dataclass
class ShadowDiagnosisResult:
    step1: ProbabilityQualityResult
    step2: SelectionPatternResult
    step3: CalibrationResult
    missing_inputs: list[str]
    variant_names: list[str]
    generated_at: str

@dataclass
class ProbabilityQualityResult:
    baseline_brier: float
    baseline_logloss: float
    baseline_ece: float
    baseline_actual_predicted_ratio: float
    baseline_n_horses: int
    shadow_brier: float
    shadow_logloss: float
    shadow_ece: float
    shadow_actual_predicted_ratio: float
    shadow_n_horses: int
    delta_brier: float
    delta_logloss: float
    delta_ece: float
    delta_apr: float

@dataclass
class SelectionPatternResult:
    changed: SelectionGroupMetrics
    unchanged: SelectionGroupMetrics
    n_changed_races: int
    n_unchanged_races: int
    delta_roi: float
    delta_hit_rate: float

@dataclass
class CalibrationResult:
    segments: list[SegmentCalibration]
```

From src/backtest/shadow_report.py (Phase 41 HTML パターン):
```python
class ShadowComparisonReportGenerator:
    def __init__(self, output_dir: Path) -> None:
        self.template_dir = Path(__file__).parent / "templates"
    def generate(self, comparison_results, variant_configs, metrics_json) -> Path:
        env = Environment(loader=FileSystemLoader(str(self.template_dir)), autoescape=True)
        template = env.get_template("shadow_comparison_report.html")
```

From scripts/run_shadow_comparison.py (Phase 41 CLI パターン):
```python
def build_parser() -> argparse.ArgumentParser:
    parser.add_argument("--output-dir", type=Path, default=Path("data/backtest/shadow"))
    parser.add_argument("--report", action="store_true")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLI スクリプト + save_diagnosis_results + 出力テスト</name>
  <files>scripts/run_shadow_diagnosis.py, src/backtest/shadow_diagnosis.py, tests/test_shadow_diagnosis.py</files>
  <read_first>
    - scripts/run_shadow_comparison.py (CLI パターン: build_parser, main, import 構造)
    - src/backtest/shadow_comparison.py (save_results パターン: JSON + Parquet 出力)
    - src/backtest/shadow_report.py (ShadowComparisonReportGenerator パターン)
    - src/backtest/shadow_diagnosis.py (Plan 01 で作成した ShadowDiagnosis クラス)
    - tests/test_shadow_diagnosis.py (Plan 01 で作成したテスト)
  </read_first>
  <action>
    1. src/backtest/shadow_diagnosis.py に以下を追加:

    save_diagnosis_results(diagnosis_result: ShadowDiagnosisResult, output_dir: Path) -> dict[str, Path] 関数:
    - output_dir を mkdir(parents=True, exist_ok=True) で作成。
    - shadow_diagnosis_result.json を出力。構造:
      {
        "generated_at": ISO文字列,
        "variant_names": [...],
        "missing_inputs": [...],
        "step1_probability_quality": {
          "baseline": {"brier": ..., "logloss": ..., "ece": ..., "actual_predicted_ratio": ..., "n_horses": ...},
          "shadow": {"brier": ..., "logloss": ..., "ece": ..., "actual_predicted_ratio": ..., "n_horses": ...},
          "delta": {"brier": ..., "logloss": ..., "ece": ..., "actual_predicted_ratio": ...}
        },
        "step2_selection_pattern": {
          "changed": {"roi": ..., "hit_rate": ..., "avg_odds": ..., "actual_predicted_ratio": ..., "bet_count": ..., "n_races": ...},
          "unchanged": {同上},
          "delta": {"roi": ..., "hit_rate": ...},
          "n_changed_races": ..., "n_unchanged_races": ...
        },
        "step3_calibration": {
          "segments": [
            {"segment_name": "popularity_band", "segment_value": "1-3", "n_samples": ..., "baseline_apr": ..., "shadow_apr": ..., "baseline_ece": ..., "shadow_ece": ..., "delta_apr": ..., "delta_ece": ...},
            ...
          ]
        },
        "recommendations": [] (空リスト、Phase 44で活用)
      }
    - shadow_diagnosis_summary.md を出力。構造:
      - ヘッダー: "Phase 43: Shadow Diagnosis Summary"
      - セクション1: "Probability Quality" — baseline/shadow Brier/logloss/ECE を表形式
      - セクション2: "Selection Pattern" — changed/unchanged ROI/HR/avg_odds を表形式
      - セクション3: "Top Calibration Gaps" — delta_apr/delta_ece の絶対値が大きいセグメント上位5件
      - セクション4: "Missing Inputs" — missing_inputs リスト (空なら "None")
      - セクション5: "Recommendations for Phase 44/45" — 空 (Phase 44で活用)
    - 戻り値: {"result_json": Path, "summary_md": Path}

    2. scripts/run_shadow_diagnosis.py を新規作成。Phase 41 CLI パターンに従う:

    build_parser() 関数:
    - --input-dir (type=Path, required=True): Phase 41 成果物のディレクトリ
    - --output-dir (type=Path, default=Path("data/backtest/shadow/diagnosis")): 出力ディレクトリ
    - --report (action="store_true"): HTML レポート生成フラグ

    main(args) 関数:
    - ShadowDiagnosis(args.input_dir) を初期化
    - diagnosis_result = sd.run() を実行
    - save_diagnosis_results(diagnosis_result, args.output_dir) で JSON + MD を出力
    - --report 指定時は ShadowDiagnosisReportGenerator(args.output_dir).generate(diagnosis_result) で HTML を生成
    - サマリーを stdout に印刷 (Phase 41 パターン)

    Windows cp932 対策: sys.stdout.reconfigure(encoding="utf-8", errors="replace") を含める (Phase 41 パターン)。
    ROOT / sys.path 設定: Phase 41 パターンを踏襲。

    3. tests/test_shadow_diagnosis.py にテストを追加:

    - test_save_diagnosis_results_json: 合成 ShadowDiagnosisResult で save_diagnosis_results を呼び出し、JSON ファイルが生成され、"step1_probability_quality" / "step2_selection_pattern" / "step3_calibration" / "missing_inputs" キーが含まれることを検証
    - test_save_diagnosis_summary_md: summary.md が生成され、"Probability Quality" / "Selection Pattern" / "Calibration Gaps" セクションが含まれることを検証
    - test_cli_dry_run: subprocess で "python scripts/run_shadow_diagnosis.py --help" が exit code 0 で完了することを検証

    import path: "from backtest.shadow_diagnosis import ShadowDiagnosis, save_diagnosis_results, ShadowDiagnosisResult" (pythonpath = ["src"] 設定済み)。
  </action>
  <verify>
    <automated>python -m pytest tests/test_shadow_diagnosis.py -v</automated>
  </verify>
  <done>
    - scripts/run_shadow_diagnosis.py が --input-dir / --output-dir / --report 引数を受け付ける
    - save_diagnosis_results() が shadow_diagnosis_result.json と shadow_diagnosis_summary.md を出力する
    - JSON に step1/step2/step3/missing_inputs セクションが含まれる
    - summary.md に Probability Quality / Selection Pattern / Calibration Gaps / Missing Inputs セクションが含まれる
    - 全テスト PASS
  </done>
</task>

<task type="auto">
  <name>Task 2: Jinja2 HTML レポートテンプレート + レポート生成テスト</name>
  <files>src/backtest/templates/shadow_diagnosis_report.html, src/backtest/shadow_diagnosis.py, tests/test_shadow_diagnosis.py</files>
  <read_first>
    - src/backtest/templates/shadow_comparison_report.html (Phase 41 HTML テンプレートの構造・CSS変数・Jinja2 ブロック)
    - src/backtest/shadow_report.py (ShadowComparisonReportGenerator の render コンテキスト構築パターン)
    - src/backtest/shadow_diagnosis.py (Plan 01 の ShadowDiagnosisResult データクラス定義)
  </read_first>
  <action>
    1. src/backtest/shadow_diagnosis.py に ShadowDiagnosisReportGenerator クラスを追加:

    class ShadowDiagnosisReportGenerator:
    - __init__(self, output_dir: Path): self.template_dir = Path(__file__).parent / "templates"
    - generate(self, diagnosis_result: ShadowDiagnosisResult) -> Path:
      - Jinja2 Environment(loader=FileSystemLoader, autoescape=True) を設定
      - template = env.get_template("shadow_diagnosis_report.html")
      - context dict に diagnosis_result の全データを渡す:
        - step1: ProbabilityQualityResult の全フィールド
        - step2: SelectionPatternResult の全フィールド
        - step3_segments: CalibrationResult.segments のリスト
        - missing_inputs: list[str]
        - variant_names: list[str]
        - generated_at: str
      - HTML を output_dir / "shadow_diagnosis_report.html" に書き出し
      - 戻り値: 出力パス

    2. src/backtest/templates/shadow_diagnosis_report.html を新規作成:

    Phase 41 shadow_comparison_report.html の構造・CSS変数を踏襲。自己完結型HTML。

    構造:
    - head: meta charset="utf-8", title "Shadow Diagnosis Report", CSS インラインスタイル (Phase 41 パターン: body font-family sans-serif, table borders, .section クラス等)
    - body:
      - h1: "Shadow Diagnosis Report"
      - メタデータ: generated_at, variant_names
      - セクション1 "Step 1: Probability Quality":
        - テーブル: baseline vs shadow の Brier/logloss/ECE/APR + Delta値。Delta が悪化方向なら赤色 (style="color: red")
      - セクション2 "Step 2: Selection Pattern":
        - テーブル: changed vs unchanged の ROI/HR/avg_odds/APR + Delta値
        - n_changed_races / n_unchanged_races 表示
      - セクション3 "Step 3: Calibration by Segment":
        - segment_name ごとにサブテーブル: segment_value, n_samples, baseline/shadow APR, baseline/shadow ECE, delta_apr, delta_ece
        - delta の絶対値が大きい順にソート
      - セクション4 "Missing Inputs":
        - missing_inputs が空なら "None detected"
        - 空でないなら箇条書きで列名を表示
      - フッター: "Generated by ShadowDiagnosis v1.0"

    Jinja2 の {% for %}, {% if %} を使用。autoescape=True でXSS対策。

    3. tests/test_shadow_diagnosis.py にテストを追加:

    - test_report_generator_html: 合成 ShadowDiagnosisResult で ShadowDiagnosisReportGenerator.generate() を呼び出し、HTMLファイルが生成され、"Step 1: Probability Quality" / "Step 2: Selection Pattern" / "Step 3: Calibration" が含まれることを検証
    - test_report_missing_inputs_section: missing_inputs=["popularity", "surface"] の場合に HTML に "popularity" と "surface" が含まれることを検証
  </action>
  <verify>
    <automated>python -m pytest tests/test_shadow_diagnosis.py -v</automated>
  </verify>
  <done>
    - shadow_diagnosis_report.html テンプレートが3ステップ段階的分析をセクション化表示する
    - ShadowDiagnosisReportGenerator が ShadowDiagnosisResult から HTML を生成する
    - Delta値の悪化方向が赤色で表示される
    - missing_inputs セクションが正しく表示される
    - 全テスト PASS、lint/mypy エラーなし
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 41 成果物 → 診断スクリプト | Parquet/JSON の入力データ。読み取り専用 |
| 診断スクリプト → 出力ファイル | JSON/HTML/MD の書き出し。ローカルファイルシステム |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-03 | Information Disclosure | HTML レポート出力 | accept | ローカル分析ツール。機密データなし (PII は含まれない) |
| T-43-04 | Tampering | output_dir パストラバーサル | mitigate | CLI の argparse type=Path でバリデーション。出力先は data/backtest/shadow/diagnosis に固定 (default) |
| T-43-SC | Tampering | pip install (新規なし) | accept | 外部パッケージインストールなし |
</threat_model>

<verification>
1. `python -m pytest tests/test_shadow_diagnosis.py -v` — 全テスト PASS
2. `ruff check src/backtest/shadow_diagnosis.py scripts/run_shadow_diagnosis.py` — lint エラーなし
3. `mypy src/backtest/shadow_diagnosis.py` — 型エラーなし
4. `python scripts/run_shadow_diagnosis.py --help` — ヘルプが表示される
</verification>

<success_criteria>
- CLI が --input-dir / --output-dir / --report 引数を受け付ける
- shadow_diagnosis_result.json が step1/step2/step3/missing_inputs を含む
- shadow_diagnosis_summary.md が劣化次元と推奨を記録する
- HTML レポートが3ステップ段階的分析をセクション化表示する
- missing_inputs が3出力ファイル全てに含まれる
- 全テスト PASS、lint/mypy エラーなし
</success_criteria>

<output>
Create `.planning/phases/43-shadow-diagnosis/43-02-SUMMARY.md` when done
</output>
