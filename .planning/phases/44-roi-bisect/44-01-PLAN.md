---
phase: 44-roi-bisect
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backtest/component_attribution.py
  - tests/test_component_attribution.py
autonomous: true
requirements: [BISECT-01, BISECT-02]

must_haves:
  truths:
    - "ECE悪化 (shadow 0.0156 vs baseline 0.0093) が MAWC 直接効果か選定後母集団効果か、odds_band/popularity_band/probability_rank_band 別に切り分けられている"
    - "APR上振れ (shadow 1.15 vs baseline 0.93) が全馬ベースと選定馬ベースで分けられ、MAWC 確率水準問題と Ranker 選定偏りが分離されている"
    - "bet_count低下 (shadow ~2580 vs baseline ~3330) について、Ranker による候補除外件数と OBF 通過率変化が定量化されている"
    - "MAWC coef_ (51-dim) で logit_market/logit_model/segment 交互作用項の悪化寄与がセグメント別に特定されている"
    - "Ranker coef_ (relevance 15-dim + value 15-dim) で投資スコアの偏りが特徴量別に特定されている"
    - "changed/dropped/retained レース別に MAWC と Ranker 係数寄与分布が比較されている"
  artifacts:
    - path: "src/backtest/component_attribution.py"
      provides: "ComponentAttribution class with 4-step sequential attribution + coefficient analysis"
      exports: ["ComponentAttribution", "ComponentAttributionResult", "CoefficientAnalysisResult"]
    - path: "tests/test_component_attribution.py"
      provides: "Unit tests for attribution logic and coefficient extraction"
      min_lines: 200
  key_links:
    - from: "src/backtest/component_attribution.py"
      to: "data/backtest/shadow/shadow_horse_diff.parquet"
      via: "pd.read_parquet"
      pattern: "read_parquet.*shadow_horse_diff"
    - from: "src/backtest/component_attribution.py"
      to: "data/backtest/shadow/diagnosis/shadow_diagnosis_result.json"
      via: "json.load"
      pattern: "shadow_diagnosis_result"
    - from: "src/backtest/component_attribution.py"
      to: "data/models-backtest/2024/market_aware_win_calibrator_turf.joblib"
      via: "joblib.load + coef_ extraction"
      pattern: "market_aware_win_calibrator.*joblib"
    - from: "src/backtest/component_attribution.py"
      to: "src/backtest/shadow_diagnosis.py"
      via: "segment constants reuse (POPULARITY_BAND_EDGES etc.)"
      pattern: "from backtest.shadow_diagnosis import"
---

<objective>
Component-level attribution of DeploymentGate FAIL root causes using post-hoc analysis of Phase 41/43 artifacts and MAWC/Ranker coefficient extraction.

Purpose: Identify which component (MAWC / Ranker / OddsBandFilter / Selection logic) causes each of the 4 DeploymentGate FAILs (ECE degradation, APR deviation, bet_count loss, OBF impact), providing Phase 45 with "1-2 components to fix" plus supporting metrics.

Output: ComponentAttribution class with 4-step sequential analysis (ECE -> APR -> bet_count -> OBF per D-02) + MAWC/Ranker coefficient analysis (BISECT-02 per D-03). JSON result consumable by Phase 45.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-roi-bisect/44-CONTEXT.md
@.planning/phases/44-roi-bisect/44-RESEARCH.md
@.planning/phases/43-shadow-diagnosis/43-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/backtest/shadow_diagnosis.py:
```python
POPULARITY_BAND_EDGES: list[float] = [0, 3, 6, 9, 14, float("inf")]
POPULARITY_BAND_NAMES: list[str] = ["1-3", "4-6", "7-9", "10-14", "15+"]
PROB_RANK_BAND_EDGES: list[float] = [0, 1, 3, 6, float("inf")]
PROB_RANK_BAND_NAMES: list[str] = ["top1", "2-3", "4-6", "7+"]
ODDS_BAND_EDGES: list[float] = [0, 3, 5, 10, 30, float("inf")]
ODDS_BAND_NAMES: list[str] = ["1-3", "3-5", "5-10", "10-30", "30+"]
```

From src/backtest/shadow_comparison.py:
```python
class VariantConfig:
    variant_name: str
    model_dir: Path
    enable_market_aware_calibrator: bool
    enable_race_level_ranker: bool

class ShadowComparisonFramework:
    @staticmethod
    def _compute_ece(y_pred, y_true, n_bins=10) -> float
```

From src/models/market_aware_win_calibrator.py:
```python
class MarketAwareWinCalibrator:
    calibrator: LogisticRegression  # coef_ shape (1, 51)
    feature_names: list[str]  # 51 items
    training_summary: dict  # beta_market_contribution, best_c, etc.
    # Key coef indices: logit_model (idx 0), logit_market (idx 1), log_odds (idx 2)
    # Segment one-hot: indices 3-20 (odds_band, pop_band, prob_rank_band, top_25, surface)
    # Interaction terms: indices 21-50 (logit_model_x_*, logit_market_x_*)
```

From src/models/race_level_ranker.py:
```python
class RaceLevelRanker:
    RELEVANCE_FEATURES: ClassVar[list[str]] = ["if_p_win_final", "if_p_win_race_rank", "if_p_ability_win", ...]  # ~8 features
    VALUE_FEATURES: ClassVar[list[str]] = ["if_logit_gap", "if_edge_win", "if_ev_calibrated", ...]  # ~5 features
    DERIVED_VALUE_FEATURES: ClassVar[list[str]] = ["if_odds_rank", "if_abs_logit_gap"]
    # state dict contains: relevance_scorer_{surface}, value_scorer_{surface} (Ridge models)
    # investment_score = 0.35*rel + 0.35*val + 0.20*log_ev - 0.10*uncertainty
```

From data/backtest/shadow/shadow_horse_diff.parquet (7368 rows x 18 cols):
```
Columns: race_id, umaban,
  baseline_p_win_final, baseline_investment_score, baseline_stake,
  baseline_win_market_selection_score, baseline_selected,
  ridge_shadow_p_win_final, ridge_shadow_investment_score, ridge_shadow_stake,
  ridge_shadow_win_market_selection_score, ridge_shadow_selected,
  kakuteijyuni, surface, tanodds, closing_win_odds, popularity, fold_year
```

From data/backtest/shadow/shadow_race_diff.parquet (6662 rows x 21 cols):
```
Columns: race_id, baseline_selected_umaban, shadow_selected_umaban, selected_changed,
  baseline/shadow: tanodds, p_win_final, win_selection_ev,
  win_market_selection_score, result, stake, closing_win_odds, investment_score, fold_year
```

From data/backtest/shadow/gates/deployment_gate_result.json:
```
FAIL conditions: ece_fold_2025, bet_count_preservation_fold_2024, bet_count_preservation_fold_2025
WARN conditions: actual_predicted_ratio_fold_2024, actual_predicted_ratio_fold_2025
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ComponentAttribution post-hoc analysis engine</name>
  <files>src/backtest/component_attribution.py, tests/test_component_attribution.py</files>
  <read_first>
    src/backtest/shadow_diagnosis.py
    src/backtest/shadow_comparison.py
    data/backtest/shadow/shadow_horse_diff.parquet
    data/backtest/shadow/shadow_race_diff.parquet
    data/backtest/shadow/diagnosis/shadow_diagnosis_result.json
    data/backtest/shadow/gates/deployment_gate_result.json
    src/models/market_aware_win_calibrator.py
    src/models/race_level_ranker.py
  </read_first>
  <behavior>
    - Test 1: ComponentAttribution loads horse_diff, race_diff, diagnosis_result, gate_result from fixture data and initializes without error
    - Test 2: attribute_ece_degradation() returns per-segment (odds_band, popularity_band, probability_rank_band) attribution with baseline vs shadow ECE, delta, and p_win shift, correctly identifying MAWC direct effect vs selection population effect
    - Test 3: attribute_apr_deviation() separates all-horse APR from selected-horse APR, quantifying MAWC probability level issue vs Ranker selection bias
    - Test 4: attribute_bet_count_loss() decomposes bet_count gap into Ranker exclusion (selected_changed count), selection stack changes, and OBF pass-through rate changes
    - Test 5: analyze_mawc_coefficients() loads MAWC joblib, extracts 51-dim coef_, identifies top contributing segments (logit_market, interaction terms) for odds_band 1-3 / popularity 10-14 / probability_rank top1
    - Test 6: analyze_ranker_coefficients() loads Ranker joblib, extracts Ridge coef_ for relevance and value scorers, identifies feature weights and bias toward specific feature dimensions
    - Test 7: analyze_segment_coefficient_contribution() splits changed/dropped/retained races and compares per-group MAWC and Ranker coefficient contribution distributions
    - Test 8: run_full_attribution() executes ECE -> APR -> bet_count -> OBF sequence and returns complete ComponentAttributionResult
  </behavior>
  <action>
Create ComponentAttribution class in src/backtest/component_attribution.py. This is a post-hoc analysis engine (no model retraining, no BacktestEngine re-runs) that reads Phase 41/43/42 artifacts and trained model files to attribute each DeploymentGate FAIL to a specific component.

Per D-02, implement 4 sequential analysis steps in strict order:

**Step 1 — attribute_ece_degradation():**
- Load shadow_horse_diff.parquet. For each horse, compute p_win delta = ridge_shadow_p_win_final - baseline_p_win_final. This is the MAWC + race normalization effect (Ranker does not modify p_win, only adds investment_score).
- Segment horses by odds_band / popularity_band / probability_rank_band (reuse POPULARITY_BAND_EDGES etc. from shadow_diagnosis.py).
- Per segment: compute (a) ECE with baseline p_win, (b) ECE with shadow p_win, (c) delta ECE, (d) mean p_win shift, (e) actual_predicted_ratio with baseline vs shadow p_win.
- Determine attribution: if ECE worsens in a segment AND p_win shift is significant, attribute to MAWC direct effect. If ECE worsens only in selected subpopulation, attribute to selection population effect.
- Key finding from Phase 43: odds_band 1-3 shadow ECE=0.1444 vs baseline=0.0447 (3x worse). MAWC beta_market_contribution=0.90 suggests logit(p_market) over-weighting.

**Step 2 — attribute_apr_deviation():**
- Compute APR = actual_predicted_ratio for all-horse baseline vs all-horse shadow (Phase 43 step1 data: baseline APR=0.928, shadow APR=1.154).
- Compute APR for selected-horses only (baseline_selected vs ridge_shadow_selected subpopulations).
- If all-horse APR deviation is large AND selected-only APR is closer to 1.0, attribute APR to MAWC probability level issue. If selected-only APR deviates more, attribute to Ranker selection bias.
- Key data: shadow overall APR=1.15 (over-prediction), baseline=0.93 (under-prediction). ideal=1.0.

**Step 3 — attribute_bet_count_loss():**
- Total bet_count gap: baseline ~3327 vs shadow ~2580 for 2024 (per gate result).
- Decompose: (a) races where selection changed (selected_changed=True) — count baseline_selected vs ridge_shadow_selected, quantify Ranker-driven exclusions. (b) For unchanged races, compare OBF pass-through rates if data available (from shadow_race_diff: stake>0 filtering).
- Per D-04, integrate OddsBandFilter analysis here. Compare excluded_bands and pass rates between baseline and shadow variants. training_bet_history ROI overstatement is documented as in-sample calibration risk but not causally linked without ablation.
- Key data from Phase 43: changed_shadow bet_count=706 vs changed_baseline bet_count=2123. Ranker reduces 2123 to 706 in changed races (67% reduction).

**Step 4 — Coefficient analysis (BISECT-02 per D-03):**

analyze_mawc_coefficients():
- Load data/models-backtest/{2024,2025}/market_aware_win_calibrator_{turf,dirt}.joblib via joblib.load.
- Extract state["calibrator"].coef_[0] (shape 51,) and state["feature_names"].
- For each feature: compute absolute coefficient value and identify dominant terms.
- Focus: logit_market (idx 1, coef~0.39), logit_model (idx 0, coef~0.04), log_odds (idx 2, coef~-0.36), and their interaction terms with segments.
- Compute per-segment effective contribution = coef[main] + sum(coef[interaction_with_main * segment_indicator]).
- Identify segments where MAWC over-corrects: odds_band 1-3 (ECE 3x), popularity 10-14 (APR 2.3x), probability_rank top1.

analyze_ranker_coefficients():
- Load data/models-backtest/{2024,2025}/win_race_level_ranker_turf.joblib. Ranker is turf-only (per RESEARCH Pitfall 3).
- Extract state["relevance_scorer_turf"].coef_ (shape ~15,) with relevance_feature_names.
- Extract state["value_scorer_turf"].coef_ (shape ~15,) with value_feature_names.
- Identify which features dominate: if_p_win_final (rel weight ~0.80), if_ev_calibrated (val weight ~0.83), if_overround (val weight ~-0.74).
- Assess bias: if investment_score heavily weights p_win_final, ranker favors favorites in a way that may reduce bet_count.

analyze_segment_coefficient_contribution():
- Split horse_diff into 3 groups: (a) changed races where shadow selected different horse (selected_changed=True in race_diff), (b) dropped races where baseline had bet but shadow does not (baseline_selected=True AND ridge_shadow_selected=False), (c) retained races where both select same horse.
- For each group: compute MAWC effective contribution (p_win delta distribution), Ranker effective contribution (investment_score distribution), and compare.
- This shows whether dropped/changed races have systematically different coefficient impact.

**Dataclass outputs:**
- ComponentAttributionResult: ece_attribution, apr_attribution, bet_count_attribution, coefficient_analysis, recommendations (list of components to fix with supporting metrics).
- CoefficientAnalysisResult: mawc_coef_analysis (per-feature and per-segment), ranker_coef_analysis (relevance + value), segment_contribution_comparison.

**Test approach:** Create test fixtures from the actual artifact structure. Use small synthetic DataFrames matching the column schema of shadow_horse_diff.parquet (18 cols) and shadow_race_diff.parquet (21 cols). Mock joblib.load for coefficient tests (return dict with "calibrator", "feature_names", "relevance_scorer_turf", etc.).
  </action>
  <verify>
    <automated>python -m pytest tests/test_component_attribution.py -v</automated>
  </verify>
  <done>
    - ComponentAttribution class loads Phase 41/43/42 artifacts and runs 4-step sequential attribution (ECE -> APR -> bet_count -> OBF)
    - MAWC coefficient analysis extracts and interprets 51-dim LogisticRegression coef_ per segment
    - Ranker coefficient analysis extracts and interprets Ridge coef_ for relevance and value scorers
    - Segment-level coefficient contribution comparison across changed/dropped/retained race groups
    - All 8+ unit tests pass
    - ruff check passes on both new files
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File I/O -> Analysis | Parquet/JSON files are trusted inputs from Phase 41/43/42. No untrusted external input. |
| joblib.load -> Memory | Model artifacts from data/models-backtest/ are trusted. No network download. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Information Disclosure | JSON output | accept | Attribution results contain no PII or secrets — model coefficients and aggregate metrics only |
| T-44-SC | Tampering | pip installs | accept | No new packages installed in Phase 44 — all analysis uses existing project dependencies |
</threat_model>

<verification>
1. All tests pass: `python -m pytest tests/test_component_attribution.py -v`
2. Lint clean: `ruff check src/backtest/component_attribution.py tests/test_component_attribution.py`
3. Import check: `python -c "from backtest.component_attribution import ComponentAttribution, ComponentAttributionResult, CoefficientAnalysisResult"`
4. Coefficient extraction works: `python -c "import joblib; s=joblib.load('data/models-backtest/2024/market_aware_win_calibrator_turf.joblib'); print(len(s['feature_names']), s['calibrator'].coef_.shape)"`
</verification>

<success_criteria>
- ComponentAttribution loads all Phase 41/43/42 artifacts without error
- ECE attribution identifies odds_band 1-3 as the worst segment with quantified MAWC effect
- APR attribution separates all-horse vs selected-horse effects
- bet_count attribution quantifies Ranker exclusion count and OBF pass-through impact
- MAWC coefficient analysis identifies logit_market dominance and segment interaction patterns
- Ranker coefficient analysis identifies feature weight bias
- Segment contribution comparison shows changed/dropped/retained race differences
- All tests pass, lint clean
</success_criteria>

<output>
Create `.planning/phases/44-roi-bisect/44-01-SUMMARY.md` when done
</output>
