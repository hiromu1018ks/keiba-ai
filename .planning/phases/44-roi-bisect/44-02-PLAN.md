---
phase: 44-roi-bisect
plan: 02
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - scripts/run_component_attribution.py
  - src/backtest/component_attribution.py
  - tests/test_component_attribution.py
autonomous: true
requirements: [BISECT-01, BISECT-02]

must_haves:
  truths:
    - "CLI から run_component_attribution.py が実行でき、bisect_result.json が生成される"
    - "coefficient_analysis.json に MAWC/Ranker 係数分析結果が構造化出力される"
    - "bisect_summary.md に主要な帰属結果と Phase 45 推奨修正が記録される"
    - "HTML レポートに ECE/APR/bet_count/OBF 逐次分析がセクション化表示される"
  artifacts:
    - path: "scripts/run_component_attribution.py"
      provides: "CLI entry point for component attribution"
      exports: ["build_parser", "main"]
    - path: "data/backtest/shadow/bisect/bisect_result.json"
      provides: "Machine-readable attribution results for Phase 45 consumption"
    - path: "data/backtest/shadow/bisect/coefficient_analysis.json"
      provides: "MAWC/Ranker coefficient analysis details"
    - path: "data/backtest/shadow/bisect/bisect_summary.md"
      provides: "Human-readable summary with Phase 45 recommendations"
  key_links:
    - from: "scripts/run_component_attribution.py"
      to: "src/backtest/component_attribution.py"
      via: "from backtest.component_attribution import ComponentAttribution"
      pattern: "from backtest.component_attribution"
    - from: "data/backtest/shadow/bisect/bisect_result.json"
      to: "Phase 45 Structural Fix"
      via: "Phase 45 reads bisect_result to identify fix targets"
      pattern: "bisect_result"
---

<objective>
CLI script + JSON/HTML/Markdown output layer for ComponentAttribution, producing Phase 45-consumable attribution artifacts.

Purpose: Wrap the ComponentAttribution analysis engine in a CLI entry point and produce 4 output files (JSON result, coefficient analysis JSON, Markdown summary, HTML report) following the established Phase 41/43 output pattern.

Output: scripts/run_component_attribution.py + save_attribution_results() function in component_attribution.py + optional HTML report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-roi-bisect/44-CONTEXT.md
@.planning/phases/44-roi-bisect/44-RESEARCH.md
@.planning/phases/44-roi-bisect/44-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — ComponentAttribution contracts that this plan consumes. -->

From src/backtest/component_attribution.py (created in Plan 01):
```python
class ComponentAttribution:
    def __init__(self, input_dir: Path, model_dir: Path | None = None): ...
    def attribute_ece_degradation(self) -> dict: ...
    def attribute_apr_deviation(self) -> dict: ...
    def attribute_bet_count_loss(self) -> dict: ...
    def analyze_mawc_coefficients(self) -> CoefficientAnalysisResult: ...
    def analyze_ranker_coefficients(self) -> CoefficientAnalysisResult: ...
    def analyze_segment_coefficient_contribution(self) -> dict: ...
    def run_full_attribution(self) -> ComponentAttributionResult: ...

class ComponentAttributionResult:
    ece_attribution: dict
    apr_attribution: dict
    bet_count_attribution: dict
    coefficient_analysis: CoefficientAnalysisResult
    recommendations: list[dict]
```

From src/backtest/shadow_diagnosis.py (Phase 43 output pattern to follow):
```python
def save_diagnosis_results(result, output_dir: Path) -> dict[str, Path]:
    # Outputs: JSON + MD. Pattern to replicate for attribution results.
```

From scripts/run_shadow_diagnosis.py (CLI pattern to follow):
```python
def build_parser() -> argparse.ArgumentParser:
    # --input-dir, --output-dir, --report flags
def main() -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLI script + save_attribution_results + output tests</name>
  <files>scripts/run_component_attribution.py, src/backtest/component_attribution.py, tests/test_component_attribution.py</files>
  <read_first>
    src/backtest/component_attribution.py
    scripts/run_shadow_diagnosis.py
    src/backtest/shadow_diagnosis.py
    src/backtest/templates/shadow_diagnosis_report.html
  </read_first>
  <acceptance_criteria>
    - scripts/run_component_attribution.py exists and has build_parser() function with --input-dir, --output-dir, --model-dir, --report arguments
    - python scripts/run_component_attribution.py --help exits with code 0
    - save_attribution_results() function added to component_attribution.py, outputs bisect_result.json + coefficient_analysis.json + bisect_summary.md
    - bisect_result.json contains keys: ece_attribution, apr_attribution, bet_count_attribution, coefficient_analysis_summary, recommendations, generated_at
    - coefficient_analysis.json contains keys: mawc_coefficients (per-feature + per-segment), ranker_coefficients (relevance + value), segment_contribution_comparison
    - bisect_summary.md has sections: ECE Attribution, APR Attribution, Bet Count Attribution, Coefficient Analysis, Recommendations for Phase 45
    - Tests verify JSON output structure, MD summary sections, and CLI dry-run
  </acceptance_criteria>
  <action>
Add save_attribution_results() function to src/backtest/component_attribution.py. This function takes a ComponentAttributionResult and output directory, producing 3 files:

1. bisect_result.json — Full machine-readable attribution result. Schema: {generated_at, ece_attribution, apr_attribution, bet_count_attribution, coefficient_analysis_summary, recommendations}. The coefficient_analysis_summary is a condensed version (top-5 contributing features per component) while full details go to coefficient_analysis.json.

2. coefficient_analysis.json — Detailed MAWC/Ranker coefficient data. Schema: {mawc: {per_feature: [{name, coef, abs_coef, category}], per_segment: [{segment, contribution, features}], ranker: {relevance: [...], value: [...]}}, segment_contribution: {changed: {...}, dropped: {...}, retained: {...}}}.

3. bisect_summary.md — Human-readable summary with 5 sections: ECE Attribution (worst segments, MAWC effect), APR Attribution (all-horse vs selected, dominant cause), Bet Count Attribution (Ranker exclusion count, OBF impact), Coefficient Analysis (top contributing MAWC features, ranker bias), Recommendations for Phase 45 (1-2 components to fix with supporting metrics).

Create scripts/run_component_attribution.py CLI entry point following the exact pattern from scripts/run_shadow_diagnosis.py:
- build_parser() with --input-dir (default: data/backtest/shadow), --output-dir (default: data/backtest/shadow/bisect), --model-dir (default: data/models-backtest), --report flag for HTML output.
- main() loads artifacts, runs ComponentAttribution.run_full_attribution(), calls save_attribution_results(), optionally generates HTML.
- Windows cp932 encoding handling: sys.stdout.reconfigure(encoding="utf-8", errors="replace").

Add save_attribution_results tests to tests/test_component_attribution.py:
- test_save_attribution_results_json: verify JSON file created with expected keys
- test_save_coefficient_analysis_json: verify coefficient JSON structure
- test_save_attribution_summary_md: verify MD has 5 required sections
- test_cli_dry_run: verify --help exits 0

No new packages required. Follow Phase 41/43 JSON+MD output conventions exactly.
  </action>
  <verify>
    <automated>python -m pytest tests/test_component_attribution.py -v</automated>
  </verify>
  <done>
    - scripts/run_component_attribution.py CLI with --input-dir/--output-dir/--model-dir/--report flags
    - save_attribution_results() outputs bisect_result.json + coefficient_analysis.json + bisect_summary.md
    - All new tests pass
    - python scripts/run_component_attribution.py --help exits 0
    - ruff check clean
  </done>
</task>

<task type="auto">
  <name>Task 2: HTML report with 4-step attribution visualization</name>
  <files>src/backtest/component_attribution.py, src/backtest/templates/component_attribution_report.html, tests/test_component_attribution.py</files>
  <read_first>
    src/backtest/component_attribution.py
    src/backtest/templates/shadow_diagnosis_report.html
    src/backtest/shadow_diagnosis.py
  </read_first>
  <acceptance_criteria>
    - ComponentAttributionReportGenerator class exists in component_attribution.py
    - HTML template at src/backtest/templates/component_attribution_report.html has 4 sections: ECE Attribution, APR Attribution, Bet Count Attribution, Coefficient Analysis
    - Delta degradation shown in red via CSS .negative class (matching Phase 43 pattern)
    - Coefficient analysis section has tables for MAWC top features and Ranker feature weights
    - Recommendations section lists components to fix with metrics
    - Test verifies HTML generation produces non-empty output with expected section headings
    - --report flag in CLI triggers HTML generation
  </acceptance_criteria>
  <action>
Add ComponentAttributionReportGenerator class to src/backtest/component_attribution.py following the exact pattern of ShadowDiagnosisReportGenerator from shadow_diagnosis.py:

- Use Jinja2 FileSystemLoader pointing to src/backtest/templates/
- Template: component_attribution_report.html

Create src/backtest/templates/component_attribution_report.html Jinja2 template with 4 main sections plus a recommendations footer:

Section 1 "ECE Attribution": Table with segment (odds_band/popularity_band/probability_rank_band), baseline_ECE, shadow_ECE, delta_ECE, p_win_shift, attribution (MAWC direct / selection effect). Delta ECE > 0 highlighted in red via .negative CSS class.

Section 2 "APR Attribution": All-horse and selected-horse APR comparison table. MAWC effect vs Ranker effect attribution row.

Section 3 "Bet Count Attribution": Stacked decomposition — total gap, Ranker exclusion count, OBF pass-through change, other. Bar visualization if feasible with inline CSS, otherwise table.

Section 4 "Coefficient Analysis": Two sub-tables — MAWC top-15 features by |coef|, Ranker relevance+value features with weights. Segment contribution comparison: changed vs dropped vs retained race coefficient distribution summary.

Recommendations: Numbered list of 1-2 components to fix in Phase 45 with supporting metrics (e.g., "Fix MAWC logit_market weight: beta_market=0.90 causes ECE 3x in odds_band 1-3").

Add test: test_report_generator_html verifying ComponentAttributionReportGenerator.generate() returns non-empty HTML string containing "ECE Attribution" and "Coefficient Analysis" headings.

Wire HTML generation into CLI: when --report flag is set, call ComponentAttributionReportGenerator.generate() and write to output_dir / component_attribution_report.html.
  </action>
  <verify>
    <automated>python -m pytest tests/test_component_attribution.py -v</automated>
  </verify>
  <done>
    - ComponentAttributionReportGenerator generates self-contained HTML report
    - HTML has 4 attribution sections + recommendations
    - Delta degradation highlighted in red
    - Coefficient analysis tables present with MAWC and Ranker features
    - --report flag triggers HTML output
    - All tests pass, lint clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File I/O -> HTML output | All inputs are trusted project artifacts. HTML is generated locally, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-02 | Information Disclosure | HTML report | accept | Report contains model diagnostics only — no secrets |
</threat_model>

<verification>
1. All tests pass: `python -m pytest tests/test_component_attribution.py -v`
2. Lint clean: `ruff check scripts/run_component_attribution.py src/backtest/component_attribution.py tests/test_component_attribution.py`
3. CLI --help: `python scripts/run_component_attribution.py --help` exits 0
4. Import check: `python -c "from backtest.component_attribution import ComponentAttributionReportGenerator"`
</verification>

<success_criteria>
- CLI script runs end-to-end: python scripts/run_component_attribution.py --input-dir data/backtest/shadow --output-dir data/backtest/shadow/bisect --model-dir data/models-backtest --report
- 4 output files generated: bisect_result.json, coefficient_analysis.json, bisect_summary.md, component_attribution_report.html
- JSON output has correct schema with attribution results
- MD summary has 5 sections including Phase 45 recommendations
- HTML report has 4 attribution sections with delta highlighting
- All tests pass, lint clean
</success_criteria>

<output>
Create `.planning/phases/44-roi-bisect/44-02-SUMMARY.md` when done
</output>
