---
phase: 47-etl-data-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/track_condition_data.py
  - scripts/precompute_track_condition.py
  - tests/test_track_condition_data.py
autonomous: true
requirements: [ETL-01, ETL-02]

must_haves:
  truths:
    - "ダート含水率CSV(189K行)がParquetに変換され、race-level集約済みDataFrameとして保存される"
    - "芝クッション値CSV(133K行)がParquetに変換され、同様にrace-level集約される"
    - "同一race_id内で値が不一致の場合はエラーで停止する"
    - "物理的異常値(含水率<=0/>=100, クッション値<=0)はNaN化されてログ出力される"
  artifacts:
    - path: "src/features/track_condition_data.py"
      provides: "CSV→Parquet変換ロジック (parse_track_condition_csv, convert_track_conditions)"
      exports: ["parse_track_condition_csv", "convert_track_conditions"]
    - path: "scripts/precompute_track_condition.py"
      provides: "thin orchestratorで両CSVを処理しtrack_conditions.parquetを出力"
    - path: "data/raw/track_conditions.parquet"
      provides: "race_id, race_date, dirt_moisture, turf_cushion 列を持つ統合Parquet"
      contains: "race_id"
  key_links:
    - from: "scripts/precompute_track_condition.py"
      to: "src/features/track_condition_data.py"
      via: "import convert_track_conditions"
      pattern: "from features\\.track_condition_data import"
    - from: "src/features/track_condition_data.py"
      to: "data/raw/track_conditions.parquet"
      via: "ParquetStore.write('raw', 'track_conditions', df)"
      pattern: "store\\.write\\(.raw., .track_conditions."
---

<objective>
外部CSV(ダート含水率189K行・芝クッション値133K行)を読み込み、18桁entry_id→16桁race_id集約でrace-level表に変換し、単一Parquet(track_conditions.parquet)として出力する。

Purpose: Phase 48以降のFeatureEngineがDataRepository経由でtrack conditionデータを利用可能にするためのETL基盤を構築する。
Output: src/features/track_condition_data.py (変換ロジック), scripts/precompute_track_condition.py (orchestrator), data/raw/track_conditions.parquet (出力)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-etl-data-pipeline/47-CONTEXT.md

@scripts/precompute_career_stats.py
@src/db/parquet_store.py

<interfaces>
<!-- Executor needs these contracts from the codebase. -->

From src/db/parquet_store.py:
```python
class ParquetStore:
    def __init__(self, data_dir: str = "data") -> None: ...
    def read(self, category: str, name: str, filters: list[tuple] | None = None) -> pd.DataFrame: ...
    def write(self, category: str, name: str, df: pd.DataFrame, partition_cols: list[str] | None = None) -> None: ...
    def exists(self, category: str, name: str) -> bool: ...
```

From src/db/readers.py:
```python
def coerce_types(df: pd.DataFrame) -> pd.DataFrame: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create track_condition_data.py with CSV parsing and race-level aggregation logic</name>
  <files>src/features/track_condition_data.py, tests/test_track_condition_data.py</files>
  <behavior>
    - Test parse_track_condition_csv: 18-digit IDs correctly split into race_id (16 chars) and umaban (2 chars); race_date derived from first 8 chars of race_id; value column converted to float64 with pd.to_numeric(errors="coerce")
    - Test aggregate_to_race_level: same race_id with identical values produces single row; NaN/non-NaN mix picks non-NaN value; same race_id with different non-NaN values raises ValueError; multiple race_ids produce correct row count
    - Test validate_physical_range: dirt_moisture with value 0 or 100 or -1 becomes NaN with warning count returned; turf_cushion with value 0 or -5 becomes NaN; valid values (moisture 2.8, cushion 11.2) pass through unchanged
    - Test convert_track_conditions: end-to-end with both CSVs, produces DataFrame with columns [race_id, race_date, dirt_moisture, turf_cushion]; NaN preserved for races without cushion data; ParquetStore.write called with ("raw", "track_conditions", df)
    - Test cross_validation_with_races: when races_df provided, logs count of track_condition race_ids not found in races.parquet; does not raise error
  </behavior>
  <action>
    Create src/features/track_condition_data.py with two public functions:

    1. parse_track_condition_csv(csv_path: str | Path, value_name: str) -> pd.DataFrame
       - Read headerless CSV with pd.read_csv(csv_path, header=None, names=["entry_id", value_name], dtype={"entry_id": str})
       - Derive race_id = entry_id.str[:16], umaban = entry_id.str[16:18], race_date = pd.to_datetime(race_id.str[:8], format="%Y%m%d") per D-04
       - Coerce value column with pd.to_numeric(errors="coerce")
       - Return df with columns [entry_id, race_id, umaban, race_date, value_name]

    2. aggregate_to_race_level(df: pd.DataFrame, value_col: str) -> pd.DataFrame
       - Group by race_id, race_date
       - For each group: if all non-NaN values are identical (or only one non-NaN), take that value; if multiple distinct non-NaN values exist, raise ValueError with race_id and values per D-05
       - Return df with columns [race_id, race_date, value_col]

    3. validate_physical_range(df: pd.DataFrame, col: str, low: float, high: float) -> tuple[pd.DataFrame, int]
       - Replace values outside (low, high) exclusive range with NaN per D-09
       - Return (modified df, count_of_replaced_values)

    4. convert_track_conditions(dirt_csv: str | Path, cushion_csv: str | Path, store: ParquetStore, races_df: pd.DataFrame | None = None) -> pd.DataFrame
       - Parse both CSVs via parse_track_condition_csv (value_name="dirt_moisture" / "turf_cushion")
       - Validate physical ranges: dirt_moisture (0, 100), turf_cushion (0, inf) per D-09
       - Aggregate both to race_level via aggregate_to_race_level
       - Outer merge on race_id to produce unified DataFrame with columns [race_id, race_date, dirt_moisture, turf_cushion]
       - Use race_date from dirt_moisture when available (earlier data start), fallback to cushion race_date
       - Cross-validate against races_df if provided per D-07 (log unmatched race_ids, do not raise)
       - Write to store.write("raw", "track_conditions", df) per D-03
       - Log: input row counts, unique race counts per source, NaN stats, output row count, physical anomaly counts
       - Return the DataFrame

    Follow mypy strict typing (disallow_untyped_defs=true). Use logging not print. Import Path from pathlib, pd from pandas. All function signatures must have complete type annotations.

    Create tests/test_track_condition_data.py with unittest.TestCase or pytest-style tests covering all behaviors above. Use small synthetic CSV strings via io.StringIO for test data -- do NOT read actual CSV files in tests. Mock ParquetStore in convert_track_conditions tests.
  </action>
  <verify>
    <automated>cd C:/Users/hirom/develop/keiba-ai && python -m pytest tests/test_track_condition_data.py -v</automated>
  </verify>
  <done>
    - src/features/track_condition_data.py exports parse_track_condition_csv, aggregate_to_race_level, validate_physical_range, convert_track_conditions
    - All tests pass: parse, aggregate (including ValueError on conflicting values), physical validation, end-to-end convert
    - mypy src/features/track_condition_data.py passes with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create precompute_track_condition.py thin orchestrator and run conversion</name>
  <files>scripts/precompute_track_condition.py</files>
  <action>
    Create scripts/precompute_track_condition.py as a thin orchestrator following the pattern in scripts/precompute_career_stats.py per D-01:

    - sys.path setup: insert ROOT and ROOT/src at position 0
    - logging.basicConfig with INFO level and standard format
    - main() function:
      1. Instantiate ParquetStore()
      2. Resolve CSV paths: data/20180728~20260531ダート含水率.csv and data/20200912~20260531クッション値.csv (use glob or Path since filenames contain tilde/date range)
      3. Optionally load races.parquet via store.read("raw", "races") for cross-validation (guard with try/except, log warning if missing)
      4. Call convert_track_conditions(dirt_csv, cushion_csv, store, races_df=races_df)
      5. Log result shape, NaN stats for dirt_moisture and turf_cushion, date ranges
    - __name__ == "__main__" guard calls main()

    The script does NOT contain transformation logic itself -- all logic lives in src/features/track_condition_data.py per D-02.

    Then run the script to generate data/raw/track_conditions.parquet:
    python scripts/precompute_track_condition.py

    Verify the output Parquet file exists and has the expected shape (columns: race_id, race_date, dirt_moisture, turf_cushion; row count consistent with ~unique race_ids from both sources; dirt_moisture covers 2018-07 onward, turf_cushion covers 2020-09 onward).
  </action>
  <verify>
    <automated>cd C:/Users/hirom/develop/keiba-ai && python -m pytest tests/test_track_condition_data.py -v && python scripts/precompute_track_condition.py 2>&1 | tail -20 && python -c "import pandas as pd; df=pd.read_parquet('data/raw/track_conditions.parquet'); assert set(df.columns)=={'race_id','race_date','dirt_moisture','turf_cushion'}; assert len(df)>0; print(f'OK: {len(df)} rows, {df.dirt_moisture.notna().sum()} dirt, {df.turf_cushion.notna().sum()} cushion')"</automated>
  </verify>
  <done>
    - scripts/precompute_track_condition.py runs successfully
    - data/raw/track_conditions.parquet exists with columns [race_id, race_date, dirt_moisture, turf_cushion]
    - dirt_moisture covers races from 2018-07 onward
    - turf_cushion covers races from 2020-09 onward (NaN for earlier races)
    - Physical anomalies (if any) logged with count
    - Pre-existing tests still pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSV file → Python | Untrusted external CSV data (no header, potential encoding issues) |
| Parquet write | Local filesystem write; no network boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Tampering | CSV input parsing | mitigate | pd.to_numeric(errors="coerce") for safe numeric conversion; validate ID length == 18 |
| T-47-02 | Tampering | Physical range values | mitigate | validate_physical_range NaN-ifies impossible values and logs count per D-09 |
| T-47-03 | Tampering | Aggregation consistency | mitigate | ValueError on conflicting non-NaN values within same race_id per D-05 |
| T-47-SC | Tampering | pip installs | accept | No new packages installed -- uses existing pandas/pyarrow only |
</threat_model>

<verification>
1. python -m pytest tests/test_track_condition_data.py -v -- all tests pass
2. python scripts/precompute_track_condition.py -- runs without error, produces track_conditions.parquet
3. python -c "import pandas as pd; df=pd.read_parquet('data/raw/track_conditions.parquet'); assert set(df.columns)=={'race_id','race_date','dirt_moisture','turf_cushion'}" -- schema verified
4. ruff check src/features/track_condition_data.py scripts/precompute_track_condition.py -- no lint errors
5. mypy src/features/track_condition_data.py -- no type errors
</verification>

<success_criteria>
- data/raw/track_conditions.parquet exists with [race_id, race_date, dirt_moisture, turf_cushion] columns
- dirt_moisture has non-NaN values for dirt races from 2018-07 onward
- turf_cushion has non-NaN values for turf races from 2020-09 onward
- All 5+ unit tests pass covering parse, aggregate, validation, and convert
- mypy and ruff pass on new files
</success_criteria>

<output>
Create `.planning/phases/47-etl-data-pipeline/47-01-SUMMARY.md` when done
</output>
