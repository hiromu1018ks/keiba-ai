---
phase: 47-etl-data-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["47-01"]
files_modified:
  - src/db/repository.py
  - tests/test_etl_type_conversion.py
autonomous: true
requirements: [ETL-03, ETL-04]

must_haves:
  truths:
    - "DataRepository.load_track_conditions(start, end) がtrack_conditions.parquetからDataFrameを返す"
    - "Parquetが存在しない場合は空DataFrameが返る"
    - "start/end日付フィルタが正しく動作する"
    - "含水率/クッション値がPOST_RACE_COLSに含まれていないことがCIテストで確認される"
  artifacts:
    - path: "src/db/repository.py"
      provides: "load_track_conditions(start, end) メソッド"
      exports: ["DataRepository.load_track_conditions"]
    - path: "tests/test_etl_type_conversion.py"
      provides: "POST_RACE_COLS CI検証テスト追加"
      contains: "test_track_condition_not_in_post_race_cols"
  key_links:
    - from: "src/db/repository.py"
      to: "data/raw/track_conditions.parquet"
      via: "store.read('raw', 'track_conditions', filters=date_filters(start, end))"
      pattern: "store\\.read\\(.raw., .track_conditions."
    - from: "tests/test_etl_type_conversion.py"
      to: "src/domain/types.py"
      via: "POST_RACE_COLS assertion"
      pattern: "POST_RACE_COLS"
---

<objective>
DataRepositoryにload_track_conditionsメソッドを追加し、FeatureEngineからDataRepository経由でtrack conditionデータにアクセス可能にする。また含水率/クッション値がPOST_RACE_COLSに含まれていないことをCI検証する。

Purpose: Phase 48のFeatureEngine統合でDataRepository.load_track_conditions()を呼び出すためのAPIを確立する。POST_RACE分類のCI検証でデータリークを防ぐ(ETL-04)。
Output: DataRepository.load_track_conditions(), POST_RACE CI test
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-etl-data-pipeline/47-CONTEXT.md
@.planning/phases/47-etl-data-pipeline/47-01-SUMMARY.md

@src/db/repository.py
@src/domain/types.py
@tests/test_etl_type_conversion.py

<interfaces>
<!-- Executor needs these contracts from the codebase and Plan 01 output. -->

From src/db/repository.py:
```python
class DataRepository:
    def __init__(self, store: ParquetStore | None = None) -> None: ...
    def load_wide_odds(self, start: str, end: str) -> pd.DataFrame: ...
    # ... other load_* methods follow pattern:
    # store.exists() -> empty df | store.read(filters=date_filters(start, end)) -> coerce_types()
```

From src/db/readers.py:
```python
def date_filters(start: str, end: str) -> list[tuple]: ...
def coerce_types(df: pd.DataFrame) -> pd.DataFrame: ...
```

From src/domain/types.py:
```python
POST_RACE_COLS: list[str] = [
    "kakuteijyuni", "confirmed_odds", "ninki", "kyakusitukubun",
    "time", "timediff", "harontimel3", "harontimel4",
    "jyuni1c", "jyuni2c", "jyuni3c", "jyuni4c",
    "honsyokin", "chakusacd", "dmjyuni", "dmtime",
    *[f"laptime{i}" for i in range(1, 26)],
    "nyusenjyuni", "fukasyokin", "dochakukubun", "dochakutosu",
    "nyusentosu", "recordupkubun", "harontimes3", "harontimes4", "ijyocd",
]
```

From Plan 01 output (data/raw/track_conditions.parquet):
```python
# Columns: race_id (str), race_date (datetime64), dirt_moisture (float64), turf_cushion (float64)
# Aggregated to race-level from entry-level CSV data
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DataRepository.load_track_conditions() and POST_RACE CI test</name>
  <files>src/db/repository.py, tests/test_etl_type_conversion.py</files>
  <behavior>
    - Test load_track_conditions with mock store: when store.exists returns True and store.read returns DataFrame with track_conditions data, method returns filtered coerce_types'd DataFrame
    - Test load_track_conditions missing parquet: when store.exists returns False, method returns empty DataFrame
    - Test load_track_conditions date filtering: date_filters(start, end) passed to store.read
    - Test POST_RACE_COLS excludes track condition: assert "dirt_moisture" not in POST_RACE_COLS and "turf_cushion" not in POST_RACE_COLS
  </behavior>
  <action>
    **Modify src/db/repository.py:**

    Add load_track_conditions method following the existing load_* odds methods pattern per D-10, D-11, D-12:

    ```python
    def load_track_conditions(self, start: str, end: str) -> pd.DataFrame:
        """含水率・クッション値のtrack conditionデータを読み込む。

        Args:
            start: 開始日 (YYYYMMDD)
            end: 終了日 (YYYYMMDD)

        Returns:
            フィルタ・型変換済みのDataFrame (columns: race_id, race_date, dirt_moisture, turf_cushion)
            track_conditions.parquetが存在しない場合は空DataFrame。
        """
        if not self._store.exists("raw", "track_conditions"):
            return pd.DataFrame()
        df = self._store.read("raw", "track_conditions", filters=date_filters(start, end))
        return coerce_types(df)
    ```

    Place this method after the existing load_* odds methods (load_wide_odds) and before the class end, keeping alphabetical/logical grouping.

    **Modify tests/test_etl_type_conversion.py:**

    Add a new test class TestPostRaceCols at the end of the file:

    ```python
    class TestPostRaceCols:
        """ETL-04: 含水率・クッション値がPOST_RACE_COLSに含まれないことをCI検証。

        根拠: 含水率/クッション値はレース当日JRA発表値であり、
        締切前に利用可能な情報のためPOST_RACE(レース後のみ入手可能)ではない。
        """
        def test_dirt_moisture_not_in_post_race_cols(self):
            from domain.types import POST_RACE_COLS
            assert "dirt_moisture" not in POST_RACE_COLS

        def test_turf_cushion_not_in_post_race_cols(self):
            from domain.types import POST_RACE_COLS
            assert "turf_cushion" not in POST_RACE_COLS
    ```

    Also add DataRepository tests. Create tests/test_repository_track_conditions.py if adding a new file is cleaner, OR add to existing test_etl_type_conversion.py. Use unittest.mock to mock ParquetStore -- follow the project convention of mocking DB access.

    For DataRepository tests, use:
    - Mock store.exists("raw", "track_conditions") returning True/False
    - Mock store.read("raw", "track_conditions", filters=...) returning a small test DataFrame
    - Assert correct date_filters are passed
    - Assert coerce_types is applied (datetime race_date column preserved)
    - Assert empty DataFrame returned when parquet missing
  </action>
  <verify>
    <automated>cd C:/Users/hirom/develop/keiba-ai && python -m pytest tests/test_etl_type_conversion.py tests/test_repository_track_conditions.py -v</automated>
  </verify>
  <done>
    - DataRepository.load_track_conditions(start, end) method exists and follows existing pattern
    - Returns empty DataFrame when track_conditions.parquet missing
    - Returns date-filtered coerce_types'd DataFrame when parquet exists
    - POST_RACE_COLS CI tests pass: dirt_moisture and turf_cushion NOT in POST_RACE_COLS
    - mypy src/db/repository.py passes
    - ruff check src/db/repository.py passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Integration verification - DataRepository loads track_conditions.parquet end-to-end</name>
  <files></files>
  <action>
    Run a quick end-to-end integration check (not a unit test, but a manual verification script) that confirms DataRepository can load the track_conditions.parquet created by Plan 01:

    1. Instantiate ParquetStore() and DataRepository(store)
    2. Call repo.load_track_conditions("20200101", "20261231")
    3. Assert: DataFrame has columns [race_id, race_date, dirt_moisture, turf_cushion]
    4. Assert: dirt_moisture has non-NaN values for dirt races
    5. Assert: turf_cushion has non-NaN values for turf races from 2020-09 onward
    6. Assert: date range filtering works (call with "20240101"/"20241231" and verify all race_dates within range)

    Execute this as a one-liner python -c command. Do NOT create a new test file for this -- it is a runtime sanity check confirming the pipeline from CSV → Parquet → DataRepository works end-to-end.

    Also verify all existing tests still pass to ensure no regressions from repository.py changes.
  </action>
  <verify>
    <automated>cd C:/Users/hirom/develop/keiba-ai && python -m pytest tests/ -v --timeout=120 && python -c "from db.parquet_store import ParquetStore; from db.repository import DataRepository; s=ParquetStore(); r=DataRepository(s); df=r.load_track_conditions('20200101','20261231'); assert set(df.columns)=={'race_id','race_date','dirt_moisture','turf_cushion'}, f'cols={list(df.columns)}'; assert df.dirt_moisture.notna().any(); assert df.turf_cushion.notna().any(); df24=r.load_track_conditions('20240101','20241231'); assert len(df24)>0; print(f'Integration OK: {len(df)} total rows, {len(df24)} rows in 2024')"</automated>
  </verify>
  <done>
    - DataRepository.load_track_conditions() returns correct DataFrame from actual Parquet file
    - Date range filtering works correctly
    - All existing tests pass (no regressions)
    - End-to-end pipeline CSV → Parquet → DataRepository verified
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DataRepository → ParquetStore | Internal trusted boundary |
| POST_RACE classification | Data leakage boundary — incorrect classification leaks future information |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-04 | Information Disclosure | POST_RACE misclassification | mitigate | CI test asserts dirt_moisture and turf_cushion NOT in POST_RACE_COLS per ETL-04 |
| T-47-05 | Tampering | DataRepository date filter bypass | accept | Internal API only; date_filters validated by coerce_types |
| T-47-SC | Tampering | pip installs | accept | No new packages installed |
</threat_model>

<verification>
1. python -m pytest tests/ -v -- all tests pass including new POST_RACE CI tests
2. python -c integration check -- DataRepository loads track_conditions.parquet correctly
3. ruff check src/db/repository.py -- no lint errors
4. mypy src/db/repository.py -- no type errors
</verification>

<success_criteria>
- DataRepository.load_track_conditions(start, end) returns DataFrame with [race_id, race_date, dirt_moisture, turf_cushion]
- Empty DataFrame returned when Parquet missing
- Date filtering works (only rows within start/end range returned)
- CI test confirms dirt_moisture NOT in POST_RACE_COLS
- CI test confirms turf_cushion NOT in POST_RACE_COLS
- All existing tests pass (no regressions)
</success_criteria>

<output>
Create `.planning/phases/47-etl-data-pipeline/47-02-SUMMARY.md` when done
</output>
