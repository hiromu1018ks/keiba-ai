---
phase: 49-derived-higher-order-features
plan: 02
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - src/features/track_condition_features.py
  - src/pipelines/training_pipeline.py
  - src/backtest/race_predictor.py
  - src/domain/models.py
  - src/models/stage1_ability_model.py
  - src/models/two_stage_return_model.py
  - src/models/ev_correction_model.py
  - src/models/place_ability_model.py
  - src/models/wide_two_stage_model.py
  - tests/test_track_condition_features.py
  - config/settings.yaml
autonomous: true
requirements: [T3-04, T4-01, T4-02, T4-03, T4-04]

must_haves:
  truths:
    - "track_front_bias_score maps dirt_moisture and turf_cushion to 0-1 via linear interpolation with NaN propagation"
    - "kickback_risk_score is the inverse direction interpolation, also 0-1"
    - "expected_pace_class is 3-level numeric code (0=slow, 1=neutral, 2=fast) derived from front_bias and kickback_risk"
    - "cushion_season_deviation and moisture_season_deviation compute zscore using track x month training-period statistics"
    - "cushion_anomaly_flag and moisture_extreme_flag mark entries where |season_deviation| > 2"
    - "cushion_x_distance, moisture_x_weight, cushion_x_age are NaN-safe numeric products per D-16"
    - "surface_condition_transition computes delta from previous race track condition per surface type per D-17"
    - "race_condition_match_score is the mean of per-horse aptitude rates matching current track condition"
    - "race_field_front_bias = front_runner_ratio x track_front_bias_score aggregated at race level"
    - "All new features are surgically routed to appropriate model FEATURE_COLS per D-23 through D-26"
  artifacts:
    - path: "src/features/track_condition_features.py"
      provides: "Extended with T4-01/T4-03/T4-04 row-level features + T4-02 race-level features + T3-04 season deviation"
      exports: ["compute_track_condition_features", "compute_race_condition_features", "_compute_track_stats", "_compute_track_month_stats", "TRACK_CONDITION_COLS", "TRACK_DERIVED_COLS", "RACE_CONDITION_COLS"]
    - path: "src/pipelines/training_pipeline.py"
      provides: "Extended with track_month_stats computation + T3 merge + race_condition_features call"
    - path: "src/backtest/race_predictor.py"
      provides: "Extended with race_condition_features call"
    - path: "src/domain/models.py"
      provides: "SubmodelSet.track_month_stats field"
    - path: "config/settings.yaml"
      provides: "track_condition thresholds section"
  key_links:
    - from: "src/pipelines/training_pipeline.py"
      to: "src/features/track_condition_features.py"
      via: "compute_track_condition_features + compute_race_condition_features calls"
      pattern: "compute_track_condition_features|compute_race_condition_features|_compute_track_month_stats"
    - from: "src/backtest/race_predictor.py"
      to: "src/features/track_condition_features.py"
      via: "compute_track_condition_features + compute_race_condition_features"
      pattern: "compute_track_condition_features|compute_race_condition_features"
    - from: "src/models/stage1_ability_model.py"
      to: "TRACK_CONDITION_COLS + TRACK_DERIVED_COLS + RACE_CONDITION_COLS"
      via: "FEATURE_COLS registration"
      pattern: "TRACK_DERIVED_COLS|RACE_CONDITION_COLS"
---

<objective>
Implement T3-04, T4-01, T4-02, T4-03, T4-04 derived and higher-order track condition features with pipeline integration and surgical routing.

Purpose: Complete the full track condition feature stack. Row-level features (bias scores, pace prediction, anomaly detection, interactions) extend compute_track_condition_features(). Race-level aggregation (condition match, field front bias) adds compute_race_condition_features(). Season deviation uses training-period track x month statistics.

Output: Extended track_condition_features.py with T3/T4 features, pipeline integration in training_pipeline + race_predictor, SubmodelSet.track_month_stats, surgical FEATURE_COLS routing, settings.yaml thresholds section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-derived-higher-order-features/49-CONTEXT.md
@.planning/phases/49-derived-higher-order-features/49-01-SUMMARY.md
@.planning/phases/48-core-edge-features/48-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend track_condition_features.py with T4-01/T4-03/T4-04 row features + T3-04 season deviation + T4-02 race features + tests</name>
  <files>src/features/track_condition_features.py, tests/test_track_condition_features.py, config/settings.yaml</files>
  <read_first>
    src/features/track_condition_features.py (existing T1/T2 implementation to extend with T3/T4 features)
    tests/test_track_condition_features.py (existing 22 tests to extend)
    src/features/horse_track_aptitude.py (T3 aptitude output columns used by T4-02)
    src/features/horse_history_features.py (lines 620-630: kyakusitukubun generation; line 292-295: FEATURE_COLS output)
    .planning/phases/49-derived-higher-order-features/49-CONTEXT.md (D-08 through D-26: all locked decisions for T3/T4)
    config/settings.yaml (existing structure for adding track_condition section)
  </read_first>
  <behavior>
    T4-01 front_bias/kickback/pace:
    - Test: dirt moisture=15 -> front_bias = clip((15-3)/(12-3), 0, 1) = 1.0
    - Test: turf cushion=9 -> front_bias = clip((9-8)/(10-8), 0, 1) = 0.5
    - Test: dirt moisture=1 -> kickback = clip((12-1)/(12-3), 0, 1) = 1.0 (dry = high kickback)
    - Test: moisture NaN -> front_bias NaN, kickback NaN
    - Test: expected_pace_class: high front_bias + low kickback -> slow(0); low front_bias + high kickback -> fast(2)

    T3-04 season deviation:
    - Test: cushion_season_deviation = (cushion - track_month_mean) / track_month_std, NaN when std==0
    - Test: _compute_track_month_stats produces trackcd x month level mean/std from training data

    T4-03 anomaly flags:
    - Test: |season_deviation| > 2 -> flag=1.0; |season_deviation| <= 2 -> flag=0.0; NaN season_deviation -> NaN flag

    T4-04 interactions (per D-16: 3 numeric products only; moisture_x_kyakusitu EXCLUDED — Phase 48 dirt_moisture_x_kyakusitu already covers moisture x leg-type):
    - Test: cushion_x_distance = turf_cushion * kyori; NaN propagation when either NaN
    - Test: moisture_x_weight = dirt_moisture * bataijyu; NaN propagation
    - Test: cushion_x_age = turf_cushion * barei; NaN propagation
    - Test: surface_condition_transition = dirt_moisture - prev_dirt_moisture for dirt; turf_cushion - prev_turf_cushion for turf; NaN when prev NaN

    T4-02 race-level:
    - Test: race_condition_match_score = mean of matching aptitude rate across race entries
    - Test: race_condition_match_max = max of matching aptitude rate
    - Test: race_condition_match_ratio = count(aptitude >= threshold AND starts >= min_starts) / valid entries
    - Test: race_field_front_bias = front_runner_ratio * track_front_bias_score; NaN if either NaN
  </behavior>
  <action>
    Extend src/features/track_condition_features.py with 3 new sections of features. All additions preserve existing T1/T2 code.

    **New constants:**
    - TRACK_DERIVED_COLS: list of T4-01 + T4-03 + T4-04 column names (track_front_bias_score, kickback_risk_score, expected_pace_class, cushion_season_deviation, moisture_season_deviation, cushion_anomaly_flag, moisture_extreme_flag, cushion_x_distance, moisture_x_weight, cushion_x_age, surface_condition_transition) — 11 features total per amended D-16 (moisture_x_kyakusitu EXCLUDED because Phase 48 dirt_moisture_x_kyakusitu already covers moisture x leg-type interaction).
    - RACE_CONDITION_COLS: list of T4-02 column names (race_condition_match_score, race_condition_match_max, race_condition_match_ratio, race_field_front_bias)

    **T4-01 in compute_track_condition_features()** (per D-08, D-09, D-10):
    - track_front_bias_score: dirt -> clip((moisture - 3) / 9, 0, 1); turf -> clip((cushion - 8) / 2, 0, 1). Unified column, NaN propagation.
    - kickback_risk_score: dirt -> clip((12 - moisture) / 9, 0, 1); turf -> clip((10 - cushion) / 2, 0, 1). Unified column, NaN propagation.
    - expected_pace_class: 3-level numeric. front_bias > 0.6 AND kickback < 0.4 -> 0 (slow). front_bias < 0.4 AND kickback > 0.6 -> 2 (fast). else -> 1 (neutral). Both NaN -> NaN.

    **T3-04 new function _compute_track_month_stats(df)** (per D-18, D-22):
    - Extends _compute_track_stats pattern with month dimension
    - Input: training DataFrame with turf_cushion/dirt_moisture/trackcd/month columns
    - Output: dict mapping "trackcd_month" -> {"mean": float, "std": float} for both cushion and moisture
    - month column: derive from race_date if not present (pd.to_datetime(df["race_date"]).dt.month)
    - Requires >= 2 data points per trackcd x month group

    **T3-04 in compute_track_condition_features()** (per D-18):
    - New parameter track_month_stats (dict, optional)
    - cushion_season_deviation: requires turf_cushion, trackcd, month, track_month_stats. (cushion - track_month_mean) / track_month_std. NaN when std==0 or no stats.
    - moisture_season_deviation: same pattern for dirt_moisture.

    **T4-03 in compute_track_condition_features()** (per D-15):
    - cushion_anomaly_flag: |cushion_season_deviation| > 2 -> 1.0, else 0.0, NaN propagation
    - moisture_extreme_flag: |moisture_season_deviation| > 2 -> 1.0, else 0.0, NaN propagation

    **T4-04 in compute_track_condition_features()** (per D-16, D-17):
    - cushion_x_distance = turf_cushion * kyori. NaN propagation.
    - moisture_x_weight = dirt_moisture * bataijyu. NaN propagation.
    - cushion_x_age = turf_cushion * barei. NaN propagation.
    - NOTE: moisture_x_kyakusitu is NOT implemented. Per amended D-16, this interaction is excluded because Phase 48's dirt_moisture_x_kyakusitu already covers moisture x leg-type. Do NOT add any moisture x kyakusitu interaction here.
    - surface_condition_transition: dirt -> dirt_moisture - prev_dirt_moisture; turf -> turf_cushion - prev_turf_cushion. prev values from T3 precompute parquet columns. NaN when current or prev is NaN.

    **T4-02 new function compute_race_condition_features(df, *, track_month_stats=None)** (per D-11 through D-14, D-20):
    - Separate function for race-level aggregation (groupby race_id)
    - Input: DataFrame with T3 aptitude columns, dirt_moisture, turf_cushion, kyakusitukubun_cd, track_front_bias_score
    - race_condition_match_score: for each race, determine current condition bucket (high wet dirt, dry dirt, hard turf, soft turf, or middle). Take mean of matching aptitude rate. Per D-11: moisture >= 12 -> horse_dirt_wet_hit_rate mean; moisture < 3 -> horse_dirt_dry_hit_rate mean; cushion >= 10 -> horse_cushion_hard_hit_rate mean; cushion < 8 -> horse_cushion_soft_hit_rate mean; middle ranges -> mean of both applicable rates.
    - race_condition_match_max: max of the same matching rate per D-12
    - race_condition_match_ratio: count of horses with matching rate >= 0.3 AND starts_count >= 3, divided by valid entry count per D-13
    - race_field_front_bias: front_runner_ratio * track_front_bias_score. front_runner_ratio = count(kyakusitukubun_cd in [1,2]) / valid entries. Per D-14, NaN if either component is NaN.
    - Broadcast race-level results back to all entries in the same race.

    **config/settings.yaml** (per D-03):
    - Add track_condition section with threshold defaults:
      - dirt_wet_threshold: 12.0
      - dirt_dry_threshold: 3.0
      - turf_hard_threshold: 10.0
      - turf_soft_threshold: 8.0
      - hit_rate_threshold: 0.3
      - min_starts: 3

    **Update function signature** of compute_track_condition_features:
    - Add track_month_stats parameter (dict | None = None) alongside existing track_stats

    **Tests in tests/test_track_condition_features.py:**
    - Add ~20 new tests covering all T3/T4 features listed in behavior section
    - Use synthetic DataFrames with known values for deterministic assertions
    - Test NaN propagation for each feature
    - Test _compute_track_month_stats with multi-track, multi-month data
    - Test compute_race_condition_features with multi-entry race DataFrames
    - Update TRACK_CONDITION_COLS count test and add TRACK_DERIVED_COLS/RACE_CONDITION_COLS count tests
    - Update test_surgical_routing tests to verify new features (will be finalized after Task 2 routing)
  </action>
  <verify>
    <automated>python -m pytest tests/test_track_condition_features.py -v</automated>
  </verify>
  <done>
    - compute_track_condition_features() produces T4-01 (3), T3-04 (2), T4-03 (2), T4-04 (4) new row-level features
    - compute_race_condition_features() produces T4-02 (4) race-level features
    - _compute_track_month_stats() produces trackcd x month statistics
    - TRACK_DERIVED_COLS lists all 11 T3/T4 row-level feature names
    - RACE_CONDITION_COLS lists all 4 T4-02 feature names
    - config/settings.yaml has track_condition section with thresholds
    - All existing 22 tests still pass + ~20 new tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Pipeline integration + surgical FEATURE_COLS routing</name>
  <files>src/pipelines/training_pipeline.py, src/backtest/race_predictor.py, src/domain/models.py, src/models/stage1_ability_model.py, src/models/two_stage_return_model.py, src/models/ev_correction_model.py, src/models/place_ability_model.py, src/models/wide_two_stage_model.py, tests/test_track_condition_features.py</files>
  <read_first>
    src/pipelines/training_pipeline.py (lines 950-1005: _train_submodel feature computation sequence where T3/T4 features must be inserted)
    src/backtest/race_predictor.py (existing compute_track_condition_features call to extend)
    src/domain/models.py (SubmodelSet dataclass, line 274: track_stats field to add track_month_stats next to)
    src/models/stage1_ability_model.py (FEATURE_COLS with existing Phase 48 track condition features)
    src/models/two_stage_return_model.py (3 FEATURE_COLS lists with existing Phase 48 features)
    src/models/ev_correction_model.py (2 FEATURE_COLS lists with existing Phase 48 features)
    .planning/phases/49-derived-higher-order-features/49-CONTEXT.md (D-19 through D-26: pipeline ordering, surgical routing decisions)
  </read_first>
  <acceptance_criteria>
    - training_pipeline.py _train_submodel() calls _compute_track_month_stats() and passes result to compute_track_condition_features(df, track_stats=..., track_month_stats=...)
    - training_pipeline.py _train_submodel() calls compute_race_condition_features() after compute_track_condition_features()
    - training_pipeline.py _train_submodel() merges T3 aptitude data from horse_features before feature computation
    - SubmodelSet stores track_month_stats alongside track_stats
    - race_predictor.py predict() calls compute_race_condition_features() after compute_track_condition_features()
    - race_predictor.py predict() reads track_month_stats from SubmodelSet via getattr
    - AbilityModel.FEATURE_COLS includes TRACK_DERIVED_COLS + RACE_CONDITION_COLS
    - WinTwoStageModel.FEATURE_COLS includes TRACK_DERIVED_COLS + RACE_CONDITION_COLS
    - PlaceTwoStageModel (HIT/RETURN/FEATURE) includes TRACK_DERIVED_COLS + RACE_CONDITION_COLS
    - EVCorrectionModel.FEATURE_COLS includes TRACK_DERIVED_COLS + RACE_CONDITION_COLS
    - PlaceEVCorrectionModel.FEATURE_COLS includes TRACK_DERIVED_COLS + RACE_CONDITION_COLS
    - PlaceAbilityModel.FEATURE_COLS includes TRACK_DERIVED_COLS + RACE_CONDITION_COLS
    - WideTwoStageModel.SHARED_FEATURE_COLS includes TRACK_DERIVED_COLS + RACE_CONDITION_COLS
    - MarketModel, RaceQualityScreener, RegimeDetector, ConformalEVModel do NOT include new features (D-23, D-26 safety net)
    - test_surgical_routing_included_models verifies TRACK_DERIVED_COLS + RACE_CONDITION_COLS in included models
    - test_surgical_routing_excluded_models verifies absence in excluded models
    - All tests pass: test_track_condition_features.py + test_domain.py + test_interaction_features.py
  </acceptance_criteria>
  <action>
    **src/domain/models.py:**
    - Add field `track_month_stats: dict | None = None` to SubmodelSet dataclass, immediately after `track_stats` (line 274)

    **src/pipelines/training_pipeline.py** (per D-21, D-22):
    - In _train_submodel(), BEFORE the existing compute_track_condition_features() call:
      1. Compute _track_month_stats via _compute_track_month_stats(df) (import _compute_track_month_stats from track_condition_features)
      2. Ensure T3 aptitude columns are present in df (they should already be there from horse_features.parquet or FeatureEngine build_all merge)
    - Extend the existing compute_track_condition_features() call to pass track_month_stats=_track_month_stats
    - Save _track_month_stats on SubmodelSet: submodel.track_month_stats = _track_month_stats (parallel to existing track_stats save)
    - After compute_track_condition_features() and before compute_interaction_features():
      - Import and call compute_race_condition_features(df)
      - Per D-20, this is a separate function for race-level aggregation

    **src/backtest/race_predictor.py:**
    - In predict() method, where compute_track_condition_features() is already called:
      1. Read track_month_stats from SubmodelSet: _track_month_stats = getattr(submodel, "track_month_stats", None)
      2. Pass track_month_stats=_track_month_stats to compute_track_condition_features()
    - After compute_track_condition_features() call, add compute_race_condition_features(df) call
    - Follow same insertion order as training_pipeline: track_condition_features -> race_condition_features -> interaction_features

    **Surgical FEATURE_COLS routing** (per D-23 through D-26):
    - Import TRACK_DERIVED_COLS and RACE_CONDITION_COLS from track_condition_features in each target model file
    - Add TRACK_DERIVED_COLS + RACE_CONDITION_COLS to FEATURE_COLS for included models (D-23: AbilityModel, WinTwoStage, PlaceTwoStage, WideTwoStage, EVCorrection, PlaceEVCorrection, PlaceAbility)
    - Do NOT add to excluded models (MarketModel, RaceQualityScreener, RegimeDetector, ConformalEVModel)
    - D-24 notes T4-03 anomaly flags as MarketModel candidate, but defers to Phase 50 audit. For now, exclude from MarketModel per safety net D-26.
    - D-25 notes T4-02 race-level as RaceQualityScreener candidate, but defers to Phase 50 audit. For now, exclude per safety net D-26.

    **Update tests/test_track_condition_features.py:**
    - Update test_surgical_routing_included_models_have_track_condition_features to also verify TRACK_DERIVED_COLS and RACE_CONDITION_COLS in included models
    - Update test_surgical_routing_excluded_models to verify TRACK_DERIVED_COLS and RACE_CONDITION_COLS are NOT in excluded models
    - Update TRACK_CONDITION_COLS count test to reflect the new total (8 existing + TRACK_DERIVED_COLS count)
  </action>
  <verify>
    <automated>python -m pytest tests/test_track_condition_features.py tests/test_domain.py tests/test_interaction_features.py -v</automated>
  </verify>
  <done>
    - training_pipeline.py computes track_month_stats, passes to feature functions, saves on SubmodelSet
    - training_pipeline.py calls compute_race_condition_features() between track_condition_features and interaction_features
    - race_predictor.py calls both compute functions with track_month_stats from SubmodelSet
    - SubmodelSet has track_month_stats field
    - 6 included models have TRACK_DERIVED_COLS + RACE_CONDITION_COLS in FEATURE_COLS
    - 4 excluded models do NOT have new features
    - All existing tests pass + surgical routing tests pass
    - ruff check on all modified files passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| training data -> feature computation | Statistics derived from training period only (no lookahead) |
| precomputed T3 data -> T4-02 aggregation | Race-level aggregation depends on per-horse aptitude integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-03 | Tampering | track_month_stats lookup | mitigate | Training-only statistics, map-based lookup with NaN fallback for missing trackxmonth |
| T-49-04 | Elevation of Privilege | Surgical routing | mitigate | Exclude MarketModel/RegimeDetector per Phase 36 lesson; Phase 50 audit validates |
| T-49-05 | Information Disclosure | Feature values | accept | Non-sensitive race statistics |
| T-49-SC | Tampering | pip installs | mitigate | No new packages in this plan |
</threat_model>

<verification>
- python -m pytest tests/test_track_condition_features.py tests/test_domain.py tests/test_interaction_features.py -v
- python -c "from features.track_condition_features import TRACK_DERIVED_COLS, RACE_CONDITION_COLS; print(len(TRACK_DERIVED_COLS), len(RACE_CONDITION_COLS))"
- ruff check src/features/track_condition_features.py src/pipelines/training_pipeline.py src/backtest/race_predictor.py src/domain/models.py src/models/
</verification>

<success_criteria>
- compute_track_condition_features() produces all T1+T2+T3-04+T4-01+T4-03+T4-04 features with NaN-safe computation
- compute_race_condition_features() produces all T4-02 race-level features
- _compute_track_month_stats() produces training-period trackcd x month statistics
- Pipeline integration: training_pipeline and race_predictor both call the new functions in correct order
- SubmodelSet stores track_month_stats for inference-time reuse
- Surgical routing: 6 included models + 4 excluded models verified by tests
- All tests pass (existing + new)
</success_criteria>

<output>
Create `.planning/phases/49-derived-higher-order-features/49-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `src/features/track_condition_features.py` — TRACK_DERIVED_COLS (11 features), RACE_CONDITION_COLS, _compute_track_month_stats(), compute_race_condition_features() (extended)
- `src/pipelines/training_pipeline.py` — track_month_stats computation + race_condition_features call
- `src/backtest/race_predictor.py` — race_condition_features call + track_month_stats inference
- `src/domain/models.py` — SubmodelSet.track_month_stats field
- `src/models/stage1_ability_model.py` — FEATURE_COLS extended with TRACK_DERIVED_COLS + RACE_CONDITION_COLS
- `src/models/two_stage_return_model.py` — 3 FEATURE_COLS lists extended
- `src/models/ev_correction_model.py` — 2 FEATURE_COLS lists extended
- `src/models/place_ability_model.py` — FEATURE_COLS extended
- `src/models/wide_two_stage_model.py` — SHARED_FEATURE_COLS extended
- `config/settings.yaml` — track_condition thresholds section
- `tests/test_track_condition_features.py` — ~41 total tests (22 existing + ~19 new)
