---
phase: 51-settlement-integrity-training-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/betting/payout_maps.py
  - src/backtest/engine.py
  - tests/test_payout_maps.py
autonomous: true
requirements:
  - STL-02
  - STL-03

must_haves:
  truths:
    - "build_win_payout_map and build_payout_map are pure functions in payout_maps.py with no I/O or EveryDB2 imports"
    - "Both BacktestEngine and PaperReconciler import from the same payout_maps module"
    - "Payout maps return odds multipliers (pay/100), not raw yen amounts"
    - "Empty DataFrame input returns empty dict without error"
    - "Dead code in engine.py removed: local payout map functions replaced by imports"
  artifacts:
    - path: "src/betting/payout_maps.py"
      provides: "Pure payout map construction functions shared by BT and PT"
      exports: ["build_win_payout_map", "build_payout_map"]
    - path: "tests/test_payout_maps.py"
      provides: "Comprehensive tests for payout map functions"
  key_links:
    - from: "src/backtest/engine.py"
      to: "src/betting/payout_maps.py"
      via: "from betting.payout_maps import build_payout_map, build_win_payout_map"
      pattern: "from betting\\.payout_maps import"
    - from: "src/paper_trading/reconciler.py"
      to: "src/betting/payout_maps.py"
      via: "import in Plan 02"
      pattern: "from betting\\.payout_maps import"
---

<objective>
Extract payout map pure functions from BacktestEngine into a new shared module `src/betting/payout_maps.py`, so both BT and PT use identical payout construction logic.

Purpose: D-09 requires shared pure functions for payout map construction. D-12 requires no I/O or EveryDB2 access. This extraction is the foundation for PaperReconciler overhaul in Plan 02.
Output: New `payout_maps.py` module, modified `engine.py` with import-only, comprehensive test file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-settlement-integrity-training-pipeline/51-CONTEXT.md
@.planning/phases/51-settlement-integrity-training-pipeline/51-RESEARCH.md
@src/backtest/engine.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create payout_maps.py with extracted pure functions and tests</name>
  <files>src/betting/payout_maps.py, tests/test_payout_maps.py, src/backtest/engine.py</files>
  <read_first>
    src/backtest/engine.py (lines 163-260 for build_payout_map, build_win_payout_map, build_wide_payout_map)
    tests/test_paper_reconciler.py (for test patterns: mock-based, DB-free)
  </read_first>
  <action>
    Per D-09 and D-12, create `src/betting/payout_maps.py` as a pure-function module with NO EveryDB2 imports, NO file I/O, NO class definitions.

    1. Extract three functions from `src/backtest/engine.py`:
       - `build_payout_map(payouts_df: pd.DataFrame) -> dict[tuple[str, int], float]` -- lines 163-208, the place/fuku payout builder. Preserve the exact name `build_payout_map` (NOT `build_place_payout_map`). CONTEXT.md uses `build_place_payout_map` but the real function is `build_payout_map`.
       - `build_win_payout_map(payouts_df: pd.DataFrame) -> dict[tuple[str, int], float]` -- lines 211-229, the single-win payout builder.
       - `build_wide_payout_map(payouts_df: pd.DataFrame) -> dict[tuple[str, int, int], float]` -- lines 232-260+, the wide payout builder.

    2. Copy the implementations verbatim from engine.py. Each function converts `pay` columns from "100-yen units" to odds multipliers (pay / 100.0) per D-10.

    3. Add `build_place_payout_map = build_payout_map` as an alias for clarity, so CONTEXT.md references resolve correctly.

    4. Modify `src/backtest/engine.py`:
       - Add `from betting.payout_maps import build_payout_map, build_win_payout_map, build_wide_payout_map` at the import section.
       - Delete the local function definitions (lines 163-260+) from engine.py. The functions are now imported.
       - Verify any callers within engine.py (e.g., `_settle_bets`, `run_backtest`) still work because the imported functions have identical signatures.

    5. Create `tests/test_payout_maps.py` with test classes:
       - `TestBuildWinPayoutMap`: empty DataFrame returns `{}`, single payout row maps correctly (pay=250 -> multiplier 2.5), NaN values are skipped, multiple races handled correctly.
       - `TestBuildPayoutMap`: empty DataFrame returns `{}`, multiple place positions (payfukusyoumaban1-5 + payfukusyopay1-5) are melted correctly, same (race_id, umaban) with different positions keeps max value, NaN columns skipped.
       - `TestBuildWidePayoutMap`: empty DataFrame returns `{}`, valid wide pairs produce correct multipliers.
       - All tests use `unittest.mock` for any external deps if needed. No DB required.

    6. Run `python -m pytest tests/test_payout_maps.py -v` to verify all tests pass.
    7. Run `python -m pytest tests/ -v --timeout=60` to verify existing tests are not broken by the extraction.
  </action>
  <acceptance_criteria>
    - `src/betting/payout_maps.py` exists and contains exactly three public functions: `build_win_payout_map`, `build_payout_map`, `build_wide_payout_map` plus alias `build_place_payout_map`
    - Module has ZERO imports from db/, backtest/, paper_trading/ (D-12 compliance)
    - `src/backtest/engine.py` imports from `betting.payout_maps` instead of defining functions locally
    - `grep -c "def build_payout_map" src/backtest/engine.py` returns 0 (functions removed from engine.py)
    - `grep "from betting.payout_maps import" src/backtest/engine.py` returns a match
    - `python -m pytest tests/test_payout_maps.py -v` passes all tests
    - `python -m pytest tests/ -v --timeout=120` passes (no regressions)
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_payout_maps.py -v && python -m pytest tests/test_backtest_engine.py -v --timeout=120 2>/dev/null; python -c "from betting.payout_maps import build_win_payout_map, build_payout_map, build_wide_payout_map; print('OK')"</automated>
  </verify>
  <done>
    payout_maps.py created with three extracted pure functions. engine.py imports from new module. All tests pass including comprehensive coverage for edge cases (empty DataFrames, NaN handling, multiple positions).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal function extraction | No external trust boundary -- pure code reorganization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Tampering | payout_maps.py | accept | Pure functions with no external inputs; no attack surface |
| T-51-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan |
</threat_model>

<verification>
1. `python -m pytest tests/test_payout_maps.py -v` -- all payout map tests pass
2. `python -m pytest tests/ -v --timeout=120` -- no regressions from extraction
3. `python -c "from betting.payout_maps import build_win_payout_map, build_payout_map; print('imports OK')"` -- import verification
4. `grep -c "def build_payout_map" src/backtest/engine.py` returns 0 (dead code removed)
</verification>

<success_criteria>
- payout_maps.py is a zero-I/O pure function module with build_win_payout_map, build_payout_map, build_wide_payout_map
- engine.py imports from payout_maps.py, local function definitions deleted
- All existing tests pass without modification
- New test_payout_maps.py has comprehensive coverage including edge cases
</success_criteria>

## Artifacts this phase produces

| Symbol | Type | File | Description |
|--------|------|------|-------------|
| `build_win_payout_map` | function | src/betting/payout_maps.py | Win payout map constructor (extracted) |
| `build_payout_map` | function | src/betting/payout_maps.py | Place/fuku payout map constructor (extracted) |
| `build_wide_payout_map` | function | src/betting/payout_maps.py | Wide payout map constructor (extracted) |
| `build_place_payout_map` | alias | src/betting/payout_maps.py | Alias for build_payout_map |
| `TestBuildWinPayoutMap` | class | tests/test_payout_maps.py | Test class for win payout maps |
| `TestBuildPayoutMap` | class | tests/test_payout_maps.py | Test class for place payout maps |
| `TestBuildWidePayoutMap` | class | tests/test_payout_maps.py | Test class for wide payout maps |

<output>
Create `.planning/phases/51-settlement-integrity-training-pipeline/51-01-SUMMARY.md` when done
</output>
