---
phase: 51-settlement-integrity-training-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 51-01
files_modified:
  - src/paper_trading/reconciler.py
  - scripts/run_paper_trading.py
  - tests/test_paper_reconciler.py
autonomous: true
requirements:
  - STL-01
  - STL-02
  - STL-03
  - STL-04
  - STL-05

must_haves:
  truths:
    - "Every bet created by _run_predict has settlement_status='pending', outcome=None, payout=None, schema_version=2, bet_id (via compute_bet_id), and session_id columns"
    - "All bets (won AND lost) are settled; ROI is computed over effective_stake (won+lost only), not total_stake"
    - "PaperReconciler is the single implementation of settlement logic; _run_reconcile is a thin CLI wrapper under 60 lines"
    - "Invalid payout values keep bets as pending rather than marking them as lost"
    - "Retry mechanism fetches pending bets at 60s intervals up to 10 minutes after last race"
    - "Old schema (result column without payout) is explicitly rejected, not auto-converted"
    - "bet_id is SHA256(session_id|race_id|bet_type|umaban)[:32] and is unique in bets.parquet"
    - "bets.parquet writes use atomic replace via temp file"
  artifacts:
    - path: "src/paper_trading/reconciler.py"
      provides: "PaperReconciler with 3-column state model, retry, Win/Place settlement"
      exports: ["PaperReconciler"]
    - path: "scripts/run_paper_trading.py"
      provides: "_run_reconcile thin wrapper (arg construction, result display, exit code control); _run_predict with new schema columns"
    - path: "tests/test_paper_reconciler.py"
      provides: "Updated tests for new schema, state model, ROI calculation, retry logic"
  key_links:
    - from: "src/paper_trading/reconciler.py"
      to: "src/betting/payout_maps.py"
      via: "from betting.payout_maps import build_win_payout_map, build_payout_map"
      pattern: "from betting\\.payout_maps import"
    - from: "scripts/run_paper_trading.py"
      to: "src/paper_trading/reconciler.py"
      via: "from paper_trading.reconciler import PaperReconciler"
      pattern: "PaperReconciler\\("
    - from: "scripts/run_paper_trading.py"
      to: "src/paper_trading/reconciler.py"
      via: "PaperReconciler.compute_bet_id called in _run_predict for bet_id generation"
      pattern: "compute_bet_id"
---

<objective>
Overhaul PaperReconciler with the 3-column state model (D-03), Win/Place settlement via shared payout_maps (D-09), correct ROI calculation including losses (D-05), retry mechanism (D-06), and thin _run_reconcile wrapper (D-01). Modify _run_predict to create bet records with the new schema columns (STL-01, D-02, D-03, D-18, D-19).

Purpose: Fix the critical ROI overestimation bug where losses are never recorded, eliminate the duplicated reconciliation logic between PaperReconciler and _run_reconcile, and ensure bet records are created with settlement state columns from the moment of creation.
Output: Corrected PaperReconciler, thinned _run_reconcile, _run_predict with new schema columns, updated tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-settlement-integrity-training-pipeline/51-CONTEXT.md
@.planning/phases/51-settlement-integrity-training-pipeline/51-RESEARCH.md
@.planning/phases/51-settlement-integrity-training-pipeline/51-01-SUMMARY.md
@src/paper_trading/reconciler.py
@scripts/run_paper_trading.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Overhaul PaperReconciler with 3-column state model, settlement logic, and retry</name>
  <files>src/paper_trading/reconciler.py</files>
  <read_first>
    src/paper_trading/reconciler.py (entire file, 153 lines -- current implementation)
    src/betting/payout_maps.py (created in Plan 01 -- functions to import)
    src/backtest/parameter_freeze_protocol.py (lines 107-128 for SHA256 pattern)
    src/db/etl.py (lines 358-366 for atomic write pattern)
  </read_first>
  <action>
    Per D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-10, D-11, D-12, D-18, D-19, D-20, overhaul `PaperReconciler` class:

    1. Add imports: `hashlib`, `tempfile`, `logging`, `time`, `from betting.payout_maps import build_win_payout_map, build_payout_map`. Add `TYPE_CHECKING` guard for EveryDB2Queries and ParquetStore.

    2. Extend constructor signature: add `retry_interval: int = 60` and `retry_timeout: int = 600` parameters per D-06.

    3. Add static method `compute_bet_id(session_id: str, race_id: str, bet_type: str, umaban: int) -> str` per D-02: `SHA256(f"{session_id}|{race_id}|{bet_type}|{umaban}").hexdigest()[:32]`.

    4. Add static method `_atomic_write_parquet(df, target)` per D-07: write to NamedTemporaryFile in target.parent, then `tmp_path.replace(target)`.

    5. Add static method `_validate_bet_schema(df) -> list[str]` per D-20:
       - Reject old schema: if `result` in columns and `payout` not in columns, return error "Old schema detected"
       - Check `schema_version` column exists and all values == 2
       - pending rows: outcome must be NULL, payout must be NULL
       - settled rows: outcome must be non-NULL, payout >= 0
       - bet_id: non-NULL and unique
       - stake > 0
       Return list of error strings (empty = valid).

    6. Rewrite `reconcile(target_date)` method:
       a. Load `bets.parquet` (source of truth per D-08). If `result` column present without `payout`, raise ValueError per D-18.
       b. Filter pending bets for target_date: `settlement_status == "pending"` (NOT `result == 0.0`).
       c. If no pending bets, return `_compute_roi()` summary.
       d. Fetch payouts from `self.everydb2.get_payouts(ymd)`.
       e. Build payout maps: `build_win_payout_map(payouts_df)` for win bets, `build_payout_map(payouts_df)` for place bets.
       f. Settlement logic per D-11 order:
          - First check refund/void data (e.g., horse scratched, race cancelled) -> outcome = "refunded"/"voided", payout = stake
          - If race exists in payout data -> settleable
          - If horse exists in payout map -> outcome = "won", payout = stake * multiplier
          - If horse NOT in payout map (but race exists) -> outcome = "lost", payout = 0.0
          - If payout value is invalid (negative, zero for a supposed win) -> keep pending per D-11 item 6
          - Set `settlement_status = "settled"` for all decided bets
       g. Atomic write per D-07: `_atomic_write_parquet(bets_df, self.bets_path)`.
       h. Return `_compute_roi()` summary.

    7. Add `retry_pending(target_date: date, last_race_time: str | None = None)` method per D-06:
       a. Load bets.parquet, filter pending bets.
       b. If no pending, return immediately.
       c. Loop: sleep `retry_interval`, fetch payouts, attempt settlement (same logic as reconcile).
       d. Break when no pending remain or `retry_timeout` elapsed.
       e. If pending remain after timeout, log warning and return exit_code=2 indicator.

    8. Replace `_compute_summary` with `_compute_roi(bets_df)` per D-05:
       - `effective_stake = sum(stake WHERE outcome IN ('won', 'lost'))`
       - `total_return = sum(payout WHERE outcome IN ('won', 'lost'))`
       - `roi = total_return / effective_stake`
       - `net_profit = total_return - effective_stake`
       - Also return n_bets, n_wins, n_pending, n_refunded, n_voided for reporting.

    9. Keep `_empty_result` compatible with new return structure.

    The `reconcile` method should handle Win AND Place bets in a single pass, using the appropriate payout map based on `bet_type` column value. This satisfies STL-02 (Win settlement) and STL-03 (Place settlement).
  </action>
  <acceptance_criteria>
    - `PaperReconciler` constructor accepts `retry_interval` and `retry_timeout` parameters
    - `compute_bet_id` static method returns SHA256 hash truncated to 32 chars
    - `_validate_bet_schema` rejects DataFrames with `result` column but no `payout` column
    - `_validate_bet_schema` enforces all D-20 constraints (pending NULLs, settled non-NULLs, payout>=0, bet_id unique, stake>0, schema_version=2)
    - `_atomic_write_parquet` uses NamedTemporaryFile + replace pattern
    - `reconcile()` method uses `settlement_status` column (not `result`) to filter pending bets
    - `reconcile()` calls `build_win_payout_map` for win bets and `build_payout_map` for place bets
    - `_compute_roi` excludes refunded/voided from effective_stake per D-05
    - No `result == 0.0` filtering anywhere in the class (replaced by `settlement_status == "pending"`)
    - No iterrows-based payout map construction (uses shared payout_maps functions)
    - File has NO direct Parquet read without atomic write on save
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_paper_reconciler.py -v</automated>
  </verify>
  <done>
    PaperReconciler overhauled with 3-column state model (settlement_status/outcome/payout), Win/Place settlement via payout_maps, correct ROI with loss tracking, retry mechanism, schema validation, and atomic writes. All D-01~D-08, D-10~D-12, D-18~D-20 implemented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add new schema columns to _run_predict, thin _run_reconcile to CLI wrapper, and update tests</name>
  <files>scripts/run_paper_trading.py, tests/test_paper_reconciler.py</files>
  <read_first>
    scripts/run_paper_trading.py (lines 294-684 for _run_predict, lines 895-1115 for _run_reconcile)
    src/paper_trading/reconciler.py (overhauled in Task 1 above)
    tests/test_paper_reconciler.py (existing tests to update for new schema)
  </read_first>
  <action>
    Per D-01, D-02, D-03, D-18, D-19, STL-01, modify both _run_predict and _run_reconcile in `scripts/run_paper_trading.py`, then update tests.

    **Part A: Modify _run_predict to use new schema columns (lines 294-684)**

    1. At the top of `_run_predict` (after imports, around line 320), generate or load session_id:
       a. Define session file path: `session_file = config.paper_trading_dir / "sessions" / f"{ymd}.json"`.
       b. If session_file exists: load `session_id` from JSON (crash recovery per D-02).
       c. If not: generate `session_id = uuid4().hex[:16]`, save to session_file with timestamp.
       d. Import `uuid` and `json` for this logic.

    2. In the bet record construction loop (lines 542-563), replace the old dict with new schema columns per D-02, D-03, D-18, D-19:
       a. Compute bet_id: `"bet_id": PaperReconciler.compute_bet_id(session_id, race_id, bet.bet_type.value, bet.umaban)`.
       b. Add `"session_id": session_id`.
       c. Add `"schema_version": 2` per D-19.
       d. Replace `"result": 0.0` with three columns per D-03:
          - `"settlement_status": "pending"`
          - `"outcome": None`
          - `"payout": None`
       e. Keep all other existing fields (race_id, bet_type, umaban, horse_name, stake, odds, ev, surface, distance, bankroll_after, race_date, post_time, is_paper, predicted_at).

    3. After the bet loop, _run_predict currently saves to predictions/{date}.parquet (lines 575-581). This prediction file is an audit record (per D-08). No change to this logic -- predictions parquet keeps all columns including the new schema columns.

    4. Add a new block after predictions are saved (around line 582): append new bet rows to the cumulative `bets.parquet` (source of truth per D-08):
       a. `bets_path = config.paper_trading_dir / "bets.parquet"`.
       b. If bets_path exists: load existing, check schema_version column. If `result` column present without `payout`, raise ValueError per D-18.
       c. Concatenate existing + new bets, drop duplicates by `bet_id` (not by race_id/umaban/race_date/bet_type -- bet_id is now the dedup key).
       d. Validate with `PaperReconciler._validate_bet_schema(combined)` -- raise if errors.
       e. Write using `PaperReconciler._atomic_write_parquet(combined, bets_path)`.
       This means bets are written to bets.parquet at predict time (pending state), and updated in-place by PaperReconciler.reconcile() later.

    **Part B: Thin _run_reconcile to CLI wrapper (lines 895-1115)**

    5. Replace the entire function body of `_run_reconcile` (lines 899-1115) with:
       a. Parse `args.date` into `target_date = date.fromisoformat(args.date)`.
       b. Instantiate `EveryDB2Queries` with config connection string.
       c. Instantiate `PaperReconciler(store=store, bets_path=config.bets_path, everydb2=everydb2, retry_interval=60, retry_timeout=600)`.
       d. Call `result = reconciler.reconcile(target_date)`.
       e. If result has pending bets and args has a retry flag, call `reconciler.retry_pending(target_date)`.
       f. Format and display results to console (keep the venue formatting helper and race display logic).
       g. Control exit code: if pending remain, `sys.exit(2)` per D-06.
       h. Send Slack notification with result summary.
    6. DELETE all inline settlement logic: payout_map dict construction (lines 934-951), iterrows loop (lines 958-973), direct parquet writes (lines 980-994), inline ROI calculation (lines 1000-1003). This logic now lives exclusively in PaperReconciler.
    7. The thinned function should be under 60 lines total (argument construction, PaperReconciler instantiation, call, result display, exit code).

    **Part C: Update tests**

    8. Update `tests/test_paper_reconciler.py`:
       a. Replace all references to `result` column with `payout`/`outcome`/`settlement_status` per D-18.
       b. Add `schema_version=2` column to all test DataFrames per D-19.
       c. Add `bet_id` column to test DataFrames (use `PaperReconciler.compute_bet_id(...)` or a fixed test value).
       d. Add `session_id` column to test DataFrames.
       e. Add tests for:
          - Old schema rejection: DataFrame with `result` column and no `payout` column raises ValueError
          - Win bet settlement: won bet gets payout > 0, outcome="won", settlement_status="settled"
          - Win bet loss: horse not in payout map gets payout=0.0, outcome="lost", settlement_status="settled"
          - Place bet settlement: place payout map used for bet_type="place"
          - ROI calculation: verify effective_stake excludes refunded/voided
          - Schema validation: pending has NULL outcome/payout, settled has non-NULL
          - bet_id uniqueness enforcement
          - Invalid payout values keep bet as pending
       f. All tests use `unittest.mock.MagicMock` for EveryDB2Queries and ParquetStore. No DB required.

    9. Run `python -m pytest tests/test_paper_reconciler.py -v` to verify.
  </action>
  <acceptance_criteria>
    - _run_predict bet records include `bet_id`, `session_id`, `schema_version=2`, `settlement_status='pending'`, `outcome=None`, `payout=None` columns
    - _run_predict no longer uses `"result": 0.0` in bet records (D-18 compliance)
    - _run_predict appends new bets to cumulative bets.parquet at predict time with pending state
    - _run_predict generates or reuses session_id per date, persisted to session JSON file (D-02 crash recovery)
    - Deduplication in bets.parquet uses `bet_id` as the key (not race_id/umaban/race_date/bet_type)
    - `_run_reconcile` function body is under 60 lines (excluding docstring and blank lines)
    - `_run_reconcile` contains ZERO direct Parquet read/write operations
    - `_run_reconcile` contains ZERO iterrows loops for settlement
    - `_run_reconcile` calls `reconciler.reconcile(target_date)` for all settlement logic
    - `_run_reconcile` passes exit code 2 when pending bets remain after timeout
    - `tests/test_paper_reconciler.py` has tests for: old schema rejection, win settlement, place settlement, loss recording, ROI with effective_stake, schema validation, invalid payout handling
    - All test DataFrames include `schema_version=2`, `bet_id`, `session_id`, `settlement_status`, `outcome`, `payout` columns
    - No test DataFrame has a `result` column (old schema)
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_paper_reconciler.py -v && python -c "
import ast, pathlib
src = pathlib.Path('scripts/run_paper_trading.py').read_text()
lines = src.split('\n')
# Find _run_reconcile function body
start = end = None
for i, line in enumerate(lines):
    if 'def _run_reconcile(' in line:
        start = i
    if start and i > start and line.startswith('def ') and not line.startswith('def _fmt'):
        end = i
        break
body = '\n'.join(lines[start:end])
body_no_blank = [l for l in body.split('\n') if l.strip() and not l.strip().startswith('#')]
assert len(body_no_blank) < 70, f'_run_reconcile has {len(body_no_blank)} non-blank lines, expected < 70'
print(f'OK: _run_reconcile has {len(body_no_blank)} non-blank/comment lines')
" && python -c "
# Verify _run_predict uses new schema columns
import pathlib
src = pathlib.Path('scripts/run_paper_trading.py').read_text()
assert 'settlement_status' in src, 'Missing settlement_status in _run_predict'
assert 'compute_bet_id' in src, 'Missing compute_bet_id call in _run_predict'
assert 'schema_version' in src, 'Missing schema_version in _run_predict'
assert 'session_id' in src, 'Missing session_id in _run_predict'
# Verify old schema NOT used in bet creation
import re
# Find bet dict construction blocks and check result: 0.0 is gone
predict_section = src[src.index('def _run_predict'):src.index('def _run_diagnose')]
assert '\"result\": 0.0' not in predict_section, 'Old schema result: 0.0 still present in _run_predict'
print('OK: _run_predict uses new schema columns')
"</automated>
  </verify>
  <done>
    _run_predict creates bet records with new schema columns (bet_id, session_id, schema_version=2, settlement_status=pending, outcome=None, payout=None) and appends to cumulative bets.parquet at predict time. _run_reconcile thinned to a CLI wrapper under 60 lines. All settlement logic lives exclusively in PaperReconciler. Tests updated for new 3-column schema with comprehensive coverage for Win/Place settlement, loss tracking, ROI calculation, schema validation, and error cases.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bets.parquet integrity | Settlement state is source of truth; tampering could inflate ROI |
| EveryDB2 payout data | External data source; must validate before trusting |
| Session_id persistence | Crash recovery mechanism; must not be forgeable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-02 | Tampering | bets.parquet | mitigate | Atomic replace (D-07), SHA256 bet_id (D-02), schema validation (D-20) |
| T-51-03 | Spoofing | EveryDB2 payout data | accept | Internal DB, not externally accessible |
| T-51-04 | Tampering | Old schema injection | mitigate | Explicit rejection of v1 schema per D-18 |
| T-51-05 | Denial of Service | Invalid payout values | mitigate | Keep as pending, never auto-resolve to lost (D-11 item 6) |
| T-51-SC | Tampering | pip installs | mitigate | No new packages in this plan |

</threat_model>

<verification>
1. `python -m pytest tests/test_paper_reconciler.py -v` -- all reconciler tests pass
2. `_run_reconcile` body is under 60 non-blank lines (verified by script in Task 2)
3. `grep "result.*0.0" scripts/run_paper_trading.py` returns no matches in _run_predict section (old schema removed)
4. `grep "settlement_status" scripts/run_paper_trading.py` returns matches (new schema active)
5. `grep "compute_bet_id" scripts/run_paper_trading.py` returns matches (bet_id generation in _run_predict)
6. `grep "session_id" scripts/run_paper_trading.py` returns matches (session tracking)
7. `grep "schema_version" scripts/run_paper_trading.py` returns matches (v2 schema)
8. `grep "from betting.payout_maps" src/paper_trading/reconciler.py` returns a match (shared functions used)
</verification>

<success_criteria>
- _run_predict creates bet records with bet_id, session_id, schema_version=2, settlement_status, outcome, payout columns (STL-01, D-02, D-03, D-18, D-19)
- _run_predict appends pending bets to cumulative bets.parquet at predict time (D-08 source of truth)
- PaperReconciler uses 3-column state model (settlement_status/outcome/payout) with schema_version=2
- Win and Place bets are settled using shared payout_maps.py functions
- ROI calculated over effective_stake (won+lost only), excluding refunded/voided
- _run_reconcile is a thin CLI wrapper with ZERO inline settlement logic
- Retry mechanism with configurable interval and timeout
- Old schema (result column) explicitly rejected
- All tests pass with new schema, including edge cases
</success_criteria>

## Artifacts this phase produces

| Symbol | Type | File | Description |
|--------|------|------|-------------|
| `PaperReconciler.compute_bet_id` | static_method | src/paper_trading/reconciler.py | SHA256 bet_id generation (D-02) |
| `PaperReconciler._atomic_write_parquet` | static_method | src/paper_trading/reconciler.py | Atomic Parquet replace (D-07) |
| `PaperReconciler._validate_bet_schema` | static_method | src/paper_trading/reconciler.py | Schema validation (D-20) |
| `PaperReconciler.retry_pending` | method | src/paper_trading/reconciler.py | Retry mechanism (D-06) |
| `PaperReconciler._compute_roi` | method | src/paper_trading/reconciler.py | ROI with effective_stake (D-05) |
| `settlement_status` | column | bets.parquet | pending/settled state tracking |
| `outcome` | column | bets.parquet | NULL/won/lost/refunded/voided |
| `payout` | column | bets.parquet | NULL/float payout amount |
| `bet_id` | column | bets.parquet | SHA256 unique identifier |
| `session_id` | column | bets.parquet | Session tracking for crash recovery |
| `schema_version` | column | bets.parquet | Always 2 for new schema |

<output>
Create `.planning/phases/51-settlement-integrity-training-pipeline/51-02-SUMMARY.md` when done
</output>
