---
phase: 51-settlement-integrity-training-pipeline
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/run_train.py
  - src/pipelines/training_pipeline.py
  - src/features/feature_engine.py
  - src/db/model_loader.py
  - tests/test_model_loader.py
  - tests/test_training_pipeline.py
autonomous: true
requirements:
  - TRN-01
  - TRN-02
  - TRN-03
  - TRN-04
  - TRN-05

must_haves:
  truths:
    - "run_train.py accepts --betting-target win|place|wide and rejects wide with error exit"
    - "TrainingPipelineV5.run() scopes model training by betting_target (win: common+win, place: common+win+place)"
    - "Pre-training Parquet validation checks date range, NaN rates, update timestamps, and warns if track_conditions.parquet is missing"
    - "Feature cache key computation includes track_conditions.parquet and horse_track_aptitude.parquet as dependencies"
    - "track_stats and track_month_stats are saved as JSON files (local + MLflow) and restored by ModelLoader"
    - "ModelLoader.load() requires explicit --run-id or --models-dir; rejects both, rejects neither, never auto-selects latest"
    - "meta.json includes betting_target field"
  artifacts:
    - path: "scripts/run_train.py"
      provides: "--betting-target CLI argument, Parquet validation"
    - path: "src/pipelines/training_pipeline.py"
      provides: "Scoped training, track_stats JSON persistence, meta.json betting_target"
    - path: "src/features/feature_engine.py"
      provides: "Enhanced cache key with track_conditions dependencies"
    - path: "src/db/model_loader.py"
      provides: "Explicit source selection, track_stats restore"
    - path: "tests/test_model_loader.py"
      provides: "Priority fix tests"
    - path: "tests/test_training_pipeline.py"
      provides: "betting-target scope tests"
  key_links:
    - from: "scripts/run_train.py"
      to: "src/pipelines/training_pipeline.py"
      via: "pipeline.run(train_start, train_end, use_ensemble, betting_target=args.betting_target)"
      pattern: "betting_target"
    - from: "src/db/model_loader.py"
      to: "src/pipelines/training_pipeline.py"
      via: "track_stats JSON artifacts created by pipeline, restored by loader"
      pattern: "track_stats_.*\\.json"
    - from: "src/features/feature_engine.py"
      to: "data/raw/track_conditions.parquet"
      via: "source_paths cache dependency list"
      pattern: "track_conditions"
---

<objective>
Fix the training pipeline to support scoped training via --betting-target, persist track_stats to disk/MLflow, add pre-training Parquet validation, update feature cache dependencies, and fix ModelLoader priority so PT loads the correct model.

Purpose: Enable PT-specific model training and ensure track_stats survive across training and inference (fixing NaN in season deviation features).
Output: Modified run_train.py, TrainingPipelineV5, feature_engine.py, ModelLoader with tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-settlement-integrity-training-pipeline/51-CONTEXT.md
@.planning/phases/51-settlement-integrity-training-pipeline/51-RESEARCH.md
@scripts/run_train.py
@src/pipelines/training_pipeline.py
@src/features/feature_engine.py
@src/db/model_loader.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --betting-target to run_train.py, Parquet validation, track_stats persistence, and cache deps</name>
  <files>scripts/run_train.py, src/pipelines/training_pipeline.py, src/features/feature_engine.py</files>
  <read_first>
    scripts/run_train.py (entire file, 83 lines)
    src/pipelines/training_pipeline.py (lines 299-320 for run() signature, lines 970-990 for track_stats computation, lines 1543-1574 for SubmodelSet construction, lines 2336-2514 for _save_models_local and meta.json)
    src/features/feature_engine.py (lines 226-237 for source_paths cache dependency list)
    src/features/track_condition_features.py (lines 1-60 for _compute_track_stats, _compute_track_month_stats signatures)
  </read_first>
  <action>
    **scripts/run_train.py modifications (TRN-01, TRN-02, D-13):**

    1. Add `--betting-target` argument to argparse: `choices=["win", "place", "wide"]`, `default="place"`, help text explaining scope per D-13.
    2. After existing `store.exists("raw", "races")` check, add wide rejection per D-13: if `args.betting_target == "wide"`, log error and `sys.exit(1)`.
    3. Add pre-training Parquet validation (TRN-02):
       - Load `races_df = store.read("raw", "races")` and check date range: log `min_date` and `max_date` from `race_date` column.
       - Check `track_conditions.parquet` existence: if `store.exists("raw", "track_conditions")`, log its date range and row count. If missing, log warning: "track_conditions.parquet not found. Track features will use defaults."
       - Check `horse_track_aptitude.parquet` existence: same pattern. If missing, log warning (not error -- file may not exist per assumption A1).
       - For each required Parquet (races, entries), compute NaN rate on key columns and log. If NaN rate exceeds a threshold (e.g., >50% on critical columns), log error but do not exit (warning only per Claude's discretion).
    4. Pass `betting_target=args.betting_target` to `pipeline.run()`.

    **src/pipelines/training_pipeline.py modifications (TRN-01, TRN-04, D-13, D-14, D-15):**

    5. The `run()` method already accepts `betting_target: str = "place"` parameter and stores `self._betting_target`. Verify this is the case (confirmed at line 305). If not, add it.

    6. In `_save_models_local()` (lines 2336-2514), add track_stats persistence per D-15:
       - After the existing artifact save loop for each `(surface, sub)` pair (around line 2451 after target_encoder save), add:
         - If `sub.track_stats is not None`: write `track_stats_{surface}.json` to models_dir using `json.dump`. Compute SHA256 checksum and log it.
         - If `sub.track_month_stats is not None`: write `track_month_stats_{surface}.json` to models_dir using `json.dump`. Compute SHA256 checksum and log it.
       - Important: The existing cleanup code at lines 2349-2354 deletes all `*.json` files in models_dir. The track_stats JSON files will be written after this cleanup, so they will not be deleted. Verify this ordering is correct.

    7. In the MLflow logging block (around lines 2100-2330), add track_stats artifact logging:
       - After the existing CQR params logging (around line 2326), add a loop over `(surface, sub)` pairs:
         - If `sub.track_stats is not None` and `track_stats_{surface}.json` exists in models_dir: `mlflow.log_artifact(str(ts_path))`.
         - If `sub.track_month_stats is not None` and `track_month_stats_{surface}.json` exists in models_dir: `mlflow.log_artifact(str(tms_path))`.
       - Log SHA256 as MLflow param: `mlflow.log_param(f"track_stats_sha256_{surface}", sha256[:16])`.

    8. Add `betting_target` to `meta.json` (around line 2502-2511): add `"betting_target": self._betting_target` to the meta dict per D-14.

    9. In `_train_submodel()` (the method that calls _compute_track_stats at lines 978-986), verify the track_stats values are already wired into SubmodelSet at lines 1568-1569. No change needed here -- the stats are computed and assigned, just not persisted (which is fixed in steps 6-7 above).

    **src/features/feature_engine.py modifications (TRN-03):**

    10. In `build_all()` method, at the source_paths loop (lines 228-237), add two entries:
        - `("raw", "track_conditions")` -- after `("odds", "snapshots")`
        - `("raw", "horse_track_aptitude")` -- after track_conditions
        Both follow the existing pattern: check if `.parquet` file or directory exists, add to `source_paths` if present. Missing files are silently skipped (already handled by the existing pattern).

    Run all tests after modifications: `python -m pytest tests/ -v --timeout=120`.
  </action>
  <acceptance_criteria>
    - `run_train.py --help` shows `--betting-target` with choices `win|place|wide`
    - `run_train.py --betting-target wide` exits with code 1 and error message
    - Pre-training validation logs date range and checks for track_conditions.parquet existence
    - `TrainingPipelineV5.run()` accepts and stores `betting_target` parameter
    - `_save_models_local()` writes `track_stats_{surface}.json` and `track_month_stats_{surface}.json` when SubmodelSet has these values
    - MLflow logging includes track_stats artifacts and SHA256 params
    - `meta.json` includes `"betting_target"` key
    - `feature_engine.py` source_paths includes `track_conditions` and `horse_track_aptitude` entries
    - All existing tests pass
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/ -v --timeout=120 -k "not test_training_pipeline" 2>&1 | tail -5; python -c "
import subprocess, sys
r = subprocess.run([sys.executable, 'scripts/run_train.py', '--help'], capture_output=True, text=True)
assert '--betting-target' in r.stdout, 'Missing --betting-target in help'
assert 'wide' in r.stdout, 'Missing wide choice'
print('run_train.py --help OK')
"</automated>
  </verify>
  <done>
    run_train.py has --betting-target support with wide rejection. Pre-training Parquet validation checks date ranges and track_conditions availability. track_stats/track_month_stats persisted as JSON (local + MLflow). Feature cache deps include track_conditions and horse_track_aptitude. meta.json includes betting_target. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix ModelLoader priority and add track_stats restore with tests</name>
  <files>src/db/model_loader.py, tests/test_model_loader.py</files>
  <read_first>
    src/db/model_loader.py (lines 39-100 for load() method, full file for load_from_dir and _find_latest_run)
    tests/test_model_loader.py (existing test file for patterns)
    src/pipelines/training_pipeline.py (lines 2502-2514 for meta.json format)
  </read_first>
  <action>
    Per D-16, D-17, and TRN-05, modify `ModelLoader`:

    1. Modify `load()` method signature (currently at line 45-46):
       - Add `models_dir: Path | None = None` parameter.
       - Add mutual exclusivity check per D-16:
         - If both `run_id` and `models_dir` are specified -> raise `ValueError("Cannot specify both run_id and models_dir (mutually exclusive per D-16)")`
         - If neither `run_id` nor `models_dir` -> raise `ValueError("Must specify either run_id or models_dir (no implicit selection per D-16)")`
       - If `models_dir` is specified -> call `self.load_from_dir(models_dir, use_ensemble_override=use_ensemble)` and return immediately.
       - If `run_id` is specified -> proceed with MLflow loading only. NO local fallback.
       - Remove the `_find_latest_run()` auto-selection call (line 59). This is now dead code -- the neither-specified case already raises ValueError.

    2. Add track_stats restore to both `load()` (MLflow path) and `load_from_dir()` (local path):
       - After constructing each `SubmodelSet` for a surface, check for `track_stats_{surface}.json` and `track_month_stats_{surface}.json` artifacts:
         - MLflow path: try `mlflow.artifacts.download_artifacts(f"runs:/{run_id}/track_stats_{surface}.json")` and read JSON.
         - Local path: check `models_dir / f"track_stats_{surface}.json"` and read JSON.
       - Set the values on the SubmodelSet: `sub.track_stats = track_stats` and `sub.track_month_stats = track_month_stats`.
       - If files not found, log debug message and leave as None (backward compatible).

    3. Add `betting_target` field to `ModelInfo` dataclass (if it exists) or return it alongside model loading:
       - After loading `meta.json`, extract `meta.get("betting_target")` and store in `ModelInfo`.
       - If `ModelInfo` is a simple dict or NamedTuple, add the field.

    4. Per D-14, add a method or parameter to validate that loaded model's `betting_target` matches expected value:
       - `validate_betting_target(model_info: ModelInfo, expected: str) -> bool` or integrated into load.

    5. Per D-17, add fail-fast check: if required artifacts are missing from MLflow run (no models logged), raise a clear error instead of silently returning partial models.

    6. Update `tests/test_model_loader.py`:
       - Add test: `load(run_id="abc", models_dir=Path("x"))` raises `ValueError` with "mutually exclusive"
       - Add test: `load()` (neither) raises `ValueError` with "no implicit selection"
       - Add test: `load(run_id="abc")` does NOT check `data/models/` directory (verify with mock that local path is never accessed)
       - Add test: `load(models_dir=Path("data/models"))` loads from local without MLflow
       - Update or remove existing test that relies on `_find_latest_run()` auto-selection (this behavior is removed per D-16)
       - Add test: track_stats restore from local JSON files
       - Add test: betting_target validation

    Run `python -m pytest tests/test_model_loader.py -v` to verify.
  </action>
  <acceptance_criteria>
    - `ModelLoader.load(run_id="x", models_dir=Path("y"))` raises `ValueError` with "mutually exclusive"
    - `ModelLoader.load()` (no args) raises `ValueError` with "no implicit selection"
    - `ModelLoader.load(run_id="x")` never checks `data/models/` directory (verified by mock)
    - `ModelLoader.load(models_dir=Path("y"))` never calls MLflow (verified by mock)
    - `_find_latest_run()` is no longer called from `load()` method
    - Track_stats JSON files are restored into SubmodelSet during loading
    - `ModelInfo` includes `betting_target` field from meta.json
    - Missing MLflow artifacts raise a clear error per D-17
    - All test_model_loader tests pass
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_model_loader.py -v</automated>
  </verify>
  <done>
    ModelLoader priority fixed per D-16: explicit run_id or models_dir required, no implicit fallback, no auto-selection. track_stats restored from JSON artifacts. betting_target validated. Fail-fast on missing artifacts. All tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MLflow model registry | External model store; wrong model could be loaded |
| Local filesystem models | Stale models could silently override MLflow |
| track_stats JSON files | Corrupted stats could produce NaN features |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-06 | Elevation of Privilege | ModelLoader | mitigate | D-16 explicit source selection, no implicit fallback |
| T-51-07 | Tampering | track_stats JSON | mitigate | SHA256 checksum in meta.json + MLflow params (D-15) |
| T-51-08 | Denial of Service | Missing track_stats | mitigate | Fail-fast per D-17, debug log if optional |
| T-51-09 | Tampering | meta.json betting_target | accept | Internal artifact, not user-controlled |
| T-51-SC | Tampering | pip installs | mitigate | No new packages in this plan |

</threat_model>

<verification>
1. `python -m pytest tests/test_model_loader.py -v` -- all model loader tests pass
2. `python -m pytest tests/ -v --timeout=120` -- no regressions
3. `run_train.py --help` shows --betting-target with win|place|wide choices
4. `grep "track_conditions" src/features/feature_engine.py | grep source_paths` -- cache deps updated
5. `grep "betting_target" src/pipelines/training_pipeline.py | grep meta` -- meta.json includes field
6. `grep "track_stats" src/pipelines/training_pipeline.py | grep json` -- track_stats persistence present
</verification>

<success_criteria>
- run_train.py supports --betting-target win|place (wide rejected with error)
- Pre-training Parquet validation checks date range, NaN, and track_conditions availability
- Feature cache key includes track_conditions.parquet and horse_track_aptitude.parquet
- track_stats/track_month_stats saved as JSON in local dir and MLflow, restored by ModelLoader
- ModelLoader requires explicit run_id or models_dir, no implicit fallback
- meta.json includes betting_target field
- All tests pass
</success_criteria>

## Artifacts this phase produces

| Symbol | Type | File | Description |
|--------|------|------|-------------|
| `--betting-target` | CLI flag | scripts/run_train.py | win/place/wide scope control (D-13) |
| `betting_target` | param | TrainingPipelineV5.run() | Scoped training parameter |
| `betting_target` | field | meta.json | Training target recorded in model metadata (D-14) |
| `track_stats_{surface}.json` | artifact | data/models/ + MLflow | Track statistics persistence (D-15) |
| `track_month_stats_{surface}.json` | artifact | data/models/ + MLflow | Monthly track statistics persistence (D-15) |
| `ModelLoader.load(models_dir=...)` | param | src/db/model_loader.py | Explicit local loading (D-16) |
| `ModelInfo.betting_target` | field | src/db/model_loader.py | Betting target from meta.json |
| track_conditions cache dep | source_path | src/features/feature_engine.py | Cache key dependency (TRN-03) |
| horse_track_aptitude cache dep | source_path | src/features/feature_engine.py | Cache key dependency (TRN-03) |

<output>
Create `.planning/phases/51-settlement-integrity-training-pipeline/51-03-SUMMARY.md` when done
</output>
