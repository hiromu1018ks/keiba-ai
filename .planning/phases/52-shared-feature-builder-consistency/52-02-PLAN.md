---
phase: 52-shared-feature-builder-consistency
plan: 02
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - src/backtest/engine.py
  - src/backtest/race_predictor.py
  - src/paper_trading/predictor.py
  - src/pipelines/training_pipeline.py
autonomous: true
requirements: [PLN-01]

must_haves:
  truths:
    - "BacktestEngine.prepare_data() delegates to FeatureBuilder and returns same-shaped output"
    - "BacktestEngine.run() internal feature path delegates to FeatureBuilder"
    - "PaperPredictor.setup() delegates to FeatureBuilder — all 7 gaps resolved"
    - "RacePredictor.predict() no longer contains duplicate track_condition/interaction/relative computation"
    - "TrainingPipeline._train_submodel() delegates feature construction to FeatureBuilder"
    - "No inline feature construction code remains in any of the 4 callers"
  artifacts:
    - path: "src/backtest/engine.py"
      provides: "Modified prepare_data() and run() using FeatureBuilder"
      contains: "FeatureBuilder"
    - path: "src/backtest/race_predictor.py"
      provides: "Simplified predict() without duplicate feature computation"
      contains: "FeatureBuilder"
    - path: "src/paper_trading/predictor.py"
      provides: "Modified setup() using FeatureBuilder"
      contains: "FeatureBuilder"
    - path: "src/pipelines/training_pipeline.py"
      provides: "Modified _train_submodel() using FeatureBuilder"
      contains: "FeatureBuilder"
  key_links:
    - from: "src/backtest/engine.py"
      to: "src/features/feature_builder.py"
      via: "import FeatureBuilder, call build_for_inference()"
      pattern: "from features\\.feature_builder import FeatureBuilder"
    - from: "src/paper_trading/predictor.py"
      to: "src/features/feature_builder.py"
      via: "import FeatureBuilder, call build_for_inference()"
      pattern: "from features\\.feature_builder import FeatureBuilder"
    - from: "src/pipelines/training_pipeline.py"
      to: "src/features/feature_builder.py"
      via: "import FeatureBuilder, call build_for_training()"
      pattern: "from features\\.feature_builder import FeatureBuilder"
---

<objective>
Replace all 4 inline feature construction copies with FeatureBuilder calls.

Purpose: Unify BT/PT/TrainingPipeline feature generation through the FeatureBuilder created in Plan 01. Eliminate the 3-copy divergence in BacktestEngine (prepare_data + run internal) and the 7-module gap in PaperPredictor. Remove RacePredictor duplicate computation per D-10.

Output: Modified engine.py, race_predictor.py, predictor.py, training_pipeline.py — all calling FeatureBuilder.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-shared-feature-builder-consistency/52-CONTEXT.md
@.planning/phases/52-shared-feature-builder-consistency/52-RESEARCH.md
@.planning/phases/52-shared-feature-builder-consistency/52-01-SUMMARY.md
</context>

## Artifacts this phase produces

| Symbol | File | Description |
|--------|------|-------------|
| Modified `BacktestEngine.prepare_data()` | `src/backtest/engine.py` | Thin wrapper delegating to FeatureBuilder |
| Modified `BacktestEngine.run()` internal path | `src/backtest/engine.py` | FeatureBuilder.build_for_inference() replaces inline copy |
| Modified `RacePredictor.predict()` | `src/backtest/race_predictor.py` | Removed track_condition/interaction/relative duplicate code |
| Modified `PaperPredictor.setup()` | `src/paper_trading/predictor.py` | FeatureBuilder.build_for_inference() replaces 6-line setup |
| Modified `TrainingPipeline._train_submodel()` | `src/pipelines/training_pipeline.py` | FeatureBuilder.build_for_training() replaces lines 816-1015 |

<tasks>

<task type="auto">
  <name>Task 1: Replace BacktestEngine feature construction with FeatureBuilder calls</name>
  <files>src/backtest/engine.py, src/backtest/race_predictor.py</files>
  <read_first>
    - src/backtest/engine.py (full file — understand prepare_data() at lines 680-879, run() internal path at lines 1100-1306, and how BacktestPreparedData is used)
    - src/backtest/race_predictor.py (full file — understand predict() at lines 202-350, specifically track_condition at 279-291, interaction at 293-294, relative at 296-299)
    - src/features/feature_builder.py (Plan 01 output — FeatureBuilder API)
    - src/features/feature_manifest.py (Plan 01 output — FeatureState, FeatureBuildResult)
    - src/domain/models.py (SubmodelSet, track_stats/track_month_stats fields)
    - .planning/phases/52-shared-feature-builder-consistency/52-01-SUMMARY.md (Plan 01 results)
  </read_first>
  <action>
    **Part A: BacktestEngine.prepare_data() modification (lines 680-879)**

    Replace the inline feature construction with FeatureBuilder per D-10:

    1. Add import: `from features.feature_builder import FeatureBuilder` and `from features.feature_manifest import FeatureState`
    2. After the FeatureEngine.build_all() call (line 686), replace ALL subsequent feature construction code (lines 694-879) with:
       - Create `FeatureBuilder(store=store)`
       - Call `builder.build_for_inference(race_df, entry_df, pre_post_odds, feature_state=..., odds_ts_df=odds_ts_df)` where feature_state is constructed from the appropriate SubmodelSet — but note: prepare_data() does NOT have models yet. Instead, use `build_for_training()` with `preserve_columns=["kakuteijyuni", "confirmed_odds"]` since prepare_data is used for WF validation where models don't exist yet. Actually, per D-10, prepare_data() should be a thin WF-compatible wrapper. So keep the call pattern as `builder.build_for_training(race_df, entry_df, pre_post_odds, odds_ts_df=odds_ts_df, preserve_columns=["kakuteijyuni", "confirmed_odds"])` — the FeatureBuilder handles all 13 enrichment modules internally.
    3. Delete ALL inline feature module imports and computation code (HorseHistoryFeatures, JockeyContextFeatures, TrainerContextFeatures, JockeyTrainerComboFeatures, SireFeatures, PaceAptitudeFeatures, CourseFeatures, DamPedigreeFeatures, RecordFeatures, MiningFeatures, wide odds pivot, NAR filter). The wide odds pivot and NAR filter remain as BacktestEngine-specific logic (not feature generation). Move them before or after the FeatureBuilder call as needed.
    4. The hist_df_all merge at line 868-879 is now handled inside FeatureBuilder._build() — remove it from prepare_data().
    5. Preserve the `BacktestPreparedData` return structure — assign `feat_df` from `FeatureBuildResult.frame`.

    **Part B: BacktestEngine.run() internal path modification (lines 1100-1306)**

    Same pattern as Part A but in the run() method:

    1. Replace lines 1100-1306 with FeatureBuilder call
    2. For the run() path, models ARE available (`self.models`). Create `FeatureState.from_submodel_set(submodel, version)` for each surface. But since run() processes all surfaces at once, use `build_for_training()` with preserve_columns for the batch feature construction (same as prepare_data), because the run() path still needs kakuteijyuni/confirmed_odds for backtest evaluation. FeatureState is NOT needed for training-mode builds.
    3. Keep wide odds pivot, NAR filter, payout map loading — these are BacktestEngine-specific, not feature generation.
    4. Delete all inline feature module code.
    5. Keep the race grouping code (`build_race_groups`) after FeatureBuilder returns — the feature DataFrame is the input.

    **Part C: RacePredictor.predict() simplification (lines 279-299)**

    Remove the duplicate feature computation that FeatureBuilder now handles per D-01:

    1. Remove `compute_track_condition_features()` call (lines 288-290)
    2. Remove `compute_race_condition_features()` call (line 291)
    3. Remove `compute_interaction_features()` call (line 294)
    4. Remove `compute_relative_features()` call (line 299)
    5. These features are now computed by FeatureBuilder before reaching RacePredictor
    6. Keep the horse_track_aptitude merge (lines 235-251) — this is a runtime merge that happens per-race
    7. Keep the hist_features merge and race_rank computation (lines 254-277) — these remain as per-race operations
    8. Keep odds_to_ability_ratio computation (lines 313-320) — this depends on inference outputs
    9. Keep compute_stage2_relative_features (line 324) — depends on p_ability_win from inference
    10. Keep compute_odds_deviation_features (line 333) — depends on inference outputs
    11. Keep the jockey/trainer/jt merge (lines 337-347) — BUT these should now already be in the DataFrame from FeatureBuilder, so change these from merge to no-op (check if columns already exist, skip merge if so)

    **Important: After this change, the callers (BT/PT) must pass fully-featured DataFrames to RacePredictor that already contain track_condition, interaction, and relative features.**

    **Regression guard:** Add a comment at the deletion point: `# FeatureBuilder handles track_condition, interaction, relative — removed duplicate computation per Phase 52 D-01`
  </action>
  <verify>
    <automated>cd "C:/Users/hirom/develop/keiba-ai" && python -m pytest tests/test_backtest_engine.py -v -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from features.feature_builder import" src/backtest/engine.py` returns >= 1
    - `grep -c "compute_track_condition_features" src/backtest/race_predictor.py` returns 0 (duplicate removed)
    - `grep -c "compute_interaction_features" src/backtest/race_predictor.py` returns 0 or only appears in import (check removal of call)
    - `grep -c "compute_relative_features" src/backtest/race_predictor.py` returns 1 (stage2 only, not base relative)
    - `grep -c "from features.sire_features import" src/backtest/engine.py` returns 0 (moved to FeatureBuilder)
    - `grep -c "HorseHistoryFeatures" src/backtest/engine.py` returns 0 (moved to FeatureBuilder)
    - All existing tests in test_backtest_engine.py pass (excluding pre-existing failures)
  </acceptance_criteria>
  <done>BacktestEngine.prepare_data() and run() internal path both delegate to FeatureBuilder. RacePredictor.predict() no longer contains duplicate track_condition/interaction/relative computation. All existing BT tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Replace PaperPredictor and TrainingPipeline feature construction with FeatureBuilder</name>
  <files>src/paper_trading/predictor.py, src/pipelines/training_pipeline.py</files>
  <read_first>
    - src/paper_trading/predictor.py (full file — setup() at lines 42-126 with 7 gaps)
    - src/pipelines/training_pipeline.py (lines 797-1015 — _train_submodel reference implementation)
    - src/features/feature_builder.py (Plan 01 output — FeatureBuilder API)
    - src/features/feature_manifest.py (Plan 01 output — FeatureState)
    - src/domain/models.py (SubmodelSet, track_stats/track_month_stats)
    - .planning/phases/52-shared-feature-builder-consistency/52-01-SUMMARY.md (Plan 01 results)
  </read_first>
  <action>
    **Part A: PaperPredictor.setup() modification (lines 42-126)**

    Replace the incomplete feature construction (missing Sire/PaceAptitude/Course/DamPedigree/Record/Mining/Interaction) per PLN-01:

    1. Add imports: `from features.feature_builder import FeatureBuilder`, `from features.feature_manifest import FeatureState`
    2. After getting race_df/entry_df/odds_df (lines 69-83), replace lines 89-118 (FeatureEngine, SubModelManager, HorseHistoryFeatures, JockeyContextFeatures, TrainerContextFeatures, JockeyTrainerComboFeatures) with:
       - Create `FeatureBuilder(store=self.store)`
       - Create `FeatureState.from_submodel_set(submodel, version="1.0")` where submodel comes from `self.models.submodels[surface]` for the appropriate surface. PaperPredictor needs the surface from schedule or race_df.
       - Call `result = builder.build_for_inference(race_df, entry_df, odds_df, feature_state=feature_state, odds_ts_df=odds_ts_df)`
       - `feat_df = result.frame`
    3. Delete all inline feature module imports (HorseHistoryFeatures, JockeyContextFeatures, etc.)
    4. Delete the merge loop (lines 111-117) — FeatureBuilder handles all merges internally
    5. Keep the pre-computed features save to parquet (lines 120-124) — this is PT-specific persistence
    6. Keep schedule retrieval (lines 60-65) and race data loading (lines 69-87) — these are data loading, not feature construction

    **Part B: TrainingPipeline._train_submodel() modification (lines 797-1015)**

    Replace the feature construction portion (lines 816-1015) with FeatureBuilder per D-10:

    1. Add imports: `from features.feature_builder import FeatureBuilder`
    2. At the start of _train_submodel, after the surface extraction (line 813), replace lines 816-1015 with:
       - Create `FeatureBuilder(store=self.store)`
       - Since _train_submodel receives `df` which already has base features from the caller, extract the original race_df/entry_df from self._race_df/self._entry_df
       - Actually, _train_submodel receives `df` which is already filtered by surface and contains base features. The FeatureBuilder needs race_df/entry_df for HorseHistoryFeatures. Since self._race_df and self._entry_df are available, pass those.
       - Call `result = builder.build_for_training(self._race_df, self._entry_df, odds_df=..., preserve_columns=["kakuteijyuni", "confirmed_odds"], feature_version="1.0")`
       - BUT: _train_submodel processes one surface at a time. The FeatureBuilder processes all surfaces. Need to handle this carefully:
         Option: Create FeatureBuilder, call _build_base_features and _enrich_features on the surface-filtered df directly. The FeatureBuilder._build() accepts a pre-filtered DataFrame.
       - Better option per D-04: The FeatureBuilder's _build() method operates on the DataFrame it receives. The caller filters by surface before passing to _build(). So:
         ```
         builder = FeatureBuilder(store=self.store)
         result = builder.build_for_training(
             race_df=..., entry_df=..., odds_df=...,
             preserve_columns=["kakuteijyuni", "confirmed_odds"],
             feature_version="1.0"
         )
         df = result.frame[result.frame["surface"] == surface]
         ```
         Wait — this would compute features for ALL surfaces when we only need one. Alternative: pass surface-filtered DataFrames directly to _build().
       - Final decision: FeatureBuilder._build() operates on whatever DataFrames it receives. For _train_submodel, pass the surface-filtered df as the input DataFrame and the full race_df/entry_df for HorseHistoryFeatures (which needs cross-surface history). Modify _build() to accept separate `hist_race_df` and `hist_entry_df` parameters for history computation.
       - Actually, simplest approach: _train_submodel already has self._race_df/self._entry_df. Pass these to FeatureBuilder for history computation. The current `df` (surface-filtered) is what gets enriched.
       - Implementation: Add a `target_df` parameter to _build() that specifies which DataFrame to enrich (defaults to the DataFrame built from race_df/entry_df). When _train_submodel calls it, target_df is the surface-filtered df.

    3. Delete ALL inline feature module code from _train_submodel (lines 816-1015):
       - HorseHistoryFeatures import + compute + merge (816-823)
       - PaceAptitudeFeatures (826-870)
       - CourseFeatures (873-891)
       - SireFeatures (894-930)
       - DamPedigreeFeatures (933-946)
       - RecordFeatures (949-965)
       - TrackConditionFeatures (967-990)
       - InteractionFeatures (992-993)
       - MiningFeatures (995-1009)
       - RelativeFeatures (1011-1015)
       - Object-to-float conversion (1017-1024)
    4. Keep everything after line 1017 (MarketModel, Stage1, TargetEncoder, WinTwoStageModel, etc.) — these are model training, not feature construction
    5. Note: track_stats/track_month_stats are computed during feature construction and need to be captured for SubmodelSet persistence. FeatureBuilder._build() computes them internally. After the FeatureBuilder call, extract track_stats from the FeatureState or compute them from the result DataFrame. Actually, per the current code, _compute_track_stats/_compute_track_month_stats are called on the training df and the results are stored in SubmodelSet. FeatureBuilder should return these as part of FeatureState or a separate mechanism.
       - Solution: FeatureBuilder.build_for_training() also returns track_stats/track_month_stats. Add an optional field to FeatureBuildResult or return them separately. Simplest: have build_for_training() accept a callback or store them on a side channel. Better: compute track_stats from the result DataFrame after build_for_training() returns (they depend on turf_cushion/trackcd which are in the DataFrame).
       - Cleanest solution per D-04: The FeatureState for training is computed from the training data. FeatureBuilder.build_for_training() computes and returns a FeatureState alongside FeatureBuildResult. Add `training_state: FeatureState | None = None` to FeatureBuildResult (not frozen for this field). The _train_submodel uses this to populate SubmodelSet.track_stats.

    **Important:** Do NOT leave any fallback to the old inline code per D-10. All feature construction must go through FeatureBuilder.
  </action>
  <verify>
    <automated>cd "C:/Users/hirom/develop/keiba-ai" && python -m pytest tests/test_paper_predictor.py tests/test_training_pipeline.py -v -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from features.feature_builder import" src/paper_trading/predictor.py` returns >= 1
    - `grep -c "from features.feature_builder import" src/pipelines/training_pipeline.py` returns >= 1
    - `grep -c "from features.sire_features import" src/paper_trading/predictor.py` returns 0 (moved to FeatureBuilder)
    - `grep -c "HorseHistoryFeatures" src/paper_trading/predictor.py` returns 0 (moved to FeatureBuilder)
    - `grep -c "from features.pace_aptitude_features import" src/paper_trading/predictor.py` returns 0
    - `grep -c "from features.course_features import" src/paper_trading/predictor.py` returns 0
    - `grep -c "from features.dam_pedigree_features import" src/pipelines/training_pipeline.py` returns 0 or reduced (moved to FeatureBuilder)
    - PaperPredictor.setup() body is significantly shorter (7 gaps resolved by FeatureBuilder)
    - All existing tests pass (excluding known pre-existing failures)
  </acceptance_criteria>
  <done>PaperPredictor.setup() uses FeatureBuilder.build_for_inference() — all 7 feature gaps resolved. TrainingPipeline._train_submodel() uses FeatureBuilder.build_for_training(). No inline feature construction code remains in either file. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FeatureBuilder -> callers | Callers trust FeatureBuilder output contains correct features in correct order |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-04 | Tampering | BT regression after refactor | mitigate | Existing BT tests provide regression coverage; prepare_data() wrapper maintains interface |
| T-52-05 | Information Disclosure | PT 7-gap features | mitigate | FeatureBuilder ensures PT gets same features as BT/Train — gap eliminated |
| T-52-SC | Tampering | pip installs | accept | No new packages |
</threat_model>

<verification>
1. `python -m pytest tests/test_backtest_engine.py -v` — existing BT tests pass
2. `python -m pytest tests/test_paper_predictor.py tests/test_training_pipeline.py -v` — existing tests pass
3. `python -m ruff check src/backtest/engine.py src/backtest/race_predictor.py src/paper_trading/predictor.py src/pipelines/training_pipeline.py` — lint clean
4. Grep verification: no inline feature module imports remain in engine.py or predictor.py
</verification>

<success_criteria>
- BacktestEngine.prepare_data() delegates to FeatureBuilder
- BacktestEngine.run() internal path delegates to FeatureBuilder
- RacePredictor.predict() has no track_condition/interaction/relative duplicate code
- PaperPredictor.setup() uses FeatureBuilder — all 7 gaps resolved
- TrainingPipeline._train_submodel() uses FeatureBuilder for feature construction
- All existing tests pass
</success_criteria>

<output>
Create `.planning/phases/52-shared-feature-builder-consistency/52-02-SUMMARY.md` when done
</output>
