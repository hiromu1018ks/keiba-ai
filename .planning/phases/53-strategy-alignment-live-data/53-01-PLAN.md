---
phase: 53-strategy-alignment-live-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/run_paper_trading.py
  - src/backtest/race_predictor.py
  - src/features/session_manifest.py
  - src/features/pipeline_consistency.py
  - tests/test_strategy_alignment.py
autonomous: true
requirements: [STR-01, STR-02, STR-03, STR-04, STR-05, STR-06]

must_haves:
  truths:
    - "PT が strategy_manifest を読み込み manifest/PFP を適用し、betting_target と betting_mode を指定して推論を実行できる"
    - "DDController は PT パイプラインで shadow 記録のみで stake に反映しない"
    - "OddsBandFilter は win-target の場合のみ BT 校正済み状態を注入して適用する"
    - "RegimeDetector の detect() 結果は診断ログに記録されるが AGGRESSIVE 固定でベット判断に反映しない"
    - "モデル/manifest/CLI の3者で betting_target が一致しなければ fail-fast する"
  artifacts:
    - path: "scripts/run_paper_trading.py"
      provides: "composition root with --betting-target/--betting-mode/--strategy-manifest args"
      contains: "betting_target"
    - path: "src/backtest/race_predictor.py"
      provides: "OddsBandFilter injection point + DD shadow flag"
    - path: "src/features/session_manifest.py"
      provides: "extended fields for strategy params"
      contains: "betting_target"
    - path: "tests/test_strategy_alignment.py"
      provides: "strategy alignment integration tests"
  key_links:
    - from: "scripts/run_paper_trading.py"
      to: "src/betting/default_strategy.py"
      via: "build_strategy_config_from_params()"
      pattern: "build_strategy_config_from_params"
    - from: "scripts/run_paper_trading.py"
      to: "src/backtest/race_predictor.py"
      via: "RacePredictor construction with stake_calc/dd_ctrl"
      pattern: "RacePredictor\\(.*stake_calculator"
    - from: "scripts/run_paper_trading.py"
      to: "src/betting/odds_band_filter.py"
      via: "OddsBandFilter injection for win-target"
      pattern: "OddsBandFilter"
---

<objective>
PT パイプラインに BT で検証済みの戦略パラメータを注入し、BT/PT 同一設定契約を確立する。

Purpose: PT が BT と同一の戦略パラメータ (manifest/PFP/DD/OddsBandFilter/Regime) で推論を実行できるようにする。新規 StrategyConfigurator クラスは不要 (D-02)。
Output: 変更済み run_paper_trading.py, race_predictor.py, session_manifest.py, pipeline_consistency.py, 新規 test_strategy_alignment.py
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-strategy-alignment-live-data/53-CONTEXT.md
@.planning/phases/53-strategy-alignment-live-data/53-RESEARCH.md
@.planning/phases/52-shared-feature-builder-consistency/52-03-SUMMARY.md

@scripts/run_paper_trading.py
@src/backtest/race_predictor.py
@src/backtest/engine.py
@src/betting/default_strategy.py
@src/betting/drawdown_controller.py
@src/betting/odds_band_filter.py
@src/features/session_manifest.py
@src/features/pipeline_consistency.py
@src/features/data_cutoff_manifest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strategy manifest integration and CLI required args in run_paper_trading.py</name>
  <files>scripts/run_paper_trading.py, src/features/session_manifest.py, src/features/pipeline_consistency.py</files>
  <read_first>
    scripts/run_paper_trading.py — parse_args(), _run_predict(), _run_diagnose(), _run_dry_run() の現在の構造
    src/betting/default_strategy.py — build_strategy_config_from_params() のシグネチャと戻り値
    src/backtest/engine.py lines 360-443 — BacktestEngine.__init__() の manifest→RacePredictor 注入パターン
    src/features/session_manifest.py — SessionManifest dataclass の現在のフィールド定義
    src/features/pipeline_consistency.py — PFPVerifier の現在の freeze/verify インターフェース
    src/betting/drawdown_controller.py — DDConfig, DrawdownController のコンストラクタ引数
    src/betting/stake_calculator.py — StakeCalculator のコンストラクタ引数
  </read_first>
  <acceptance_criteria>
    - parse_args() に --betting-target (required, choices=["win","place"]) と --betting-mode (required, choices=["flat","kelly"]) と --strategy-manifest (optional path) が追加されている
    - Wide は choices=["win","place"] に含まれないため argparse が自動拒否する
    - _run_predict() 内で strategy_manifest_path が存在すれば JSON を読み込み、build_strategy_config_from_params() に渡して strategy_config dict を生成する
    - モデルの betting_target (models.meta の "betting_target") と manifest の "_betting_target" と CLI の args.betting_target の3者が一致しなければ logger.error + sys.exit(1) する
    - strategy_manifest_path・SHA256・適用パラメータが session_manifest に記録される
    - PFPVerifier の betting_target/betting_mode が CLI 引数から設定される (ハードコード "place"/"flat" を置き換え)
  </acceptance_criteria>
  <behavior>
    - Test 1: strategy_manifest_path=None の場合、strategy_config={} で動作 (manifest なしでも実行可能)
    - Test 2: 3者 target 不一致時 (model=win, CLI=place) に sys.exit(1) が発生する
    - Test 3: manifest SHA256 が session_manifest に記録される
    - Test 4: --betting-target wide 指定時に argparse がエラーになる
  </behavior>
  <action>
    D-02, D-03 に基づく戦略注入の実装:

    1. parse_args() に3つの引数を追加: --betting-target (required=True, choices=["win","place"]), --betting-mode (required=True, choices=["flat","kelly"]), --strategy-manifest (type=str, default=None)。Wide は choices に含めない (D-03)。

    2. _run_predict() 内の RacePredictor 構築パターンを BT engine.py L415-442 と同一にする:
       a. strategy_manifest_path が指定されていれば verify_strategy_manifest() で SHA256 検証後、json.loads() して build_strategy_config_from_params() に渡す
       b. betting_mode=="kelly" の場合: StakeCalculator + DDController を構築し RacePredictor に注入。strategy_params から fractional_kelly/target_ev/max_scale を取得 (D-02)
       c. betting_mode=="flat" の場合: RacePredictor(models, betting_target=betting_target) のみ
       d. 現在のハードコード RacePredictor(models) を置き換える

    3. 3者一致検証 (D-03): model_target = models.meta.get("betting_target"), manifest_target = strategy_params.get("_betting_target"), cli_target = args.betting_target。model_target または manifest_target が None でなければ cli_target と一致検証。不一致時は logger.error + sys.exit(1)

    4. D-01: _run_predict() のレースループ内の regime 判定を維持しつつ、detect() 結果を diag_logger.log_race() の regime フィールドに記録するのみ。select_bets() 内部は AGGRESSIVE 固定 (RacePredictor 側で既に実装済み、変更不要)

    5. D-04: PT では DDController による stake 縮小・新規ベット停止を適用しない。DDController は RacePredictor に注入するが、PT ループ内で dd_ctrl.adjust_stake() の結果を上書きするフラグを追加するか、RacePredictor に dd_shadow_only フラグを追加する。具体的には RacePredictor.__init__() に dd_shadow_only: bool = False パラメータを追加し、True の場合 select_bets() 内の dd_ctrl.adjust_stake() をスキップして100円 flat を使用する。DD 状態は diag_logger に記録する

    6. session_manifest.py にフィールドを追加: betting_target: str = "", betting_mode: str = "", strategy_manifest_path: str = "", strategy_manifest_sha256: str = ""。to_dict() にこれらを含める。set_strategy_params() メソッドを追加して4フィールドを一括設定する

    7. PFPVerifier に渡す betting_target/betting_mode を CLI 引数から取得するよう変更。現在のハードコード "place"/"flat" を args.betting_target / args.betting_mode に置き換え

    8. _run_diagnose() と _run_dry_run() にも --betting-target/--betting-mode を反映する。RacePredictor の構築を _run_predict() と同じパターンにする (manifest なし版)

    9. tests/test_strategy_alignment.py を新規作成:
       - test_parse_args_rejects_wide: --betting-target wide が argparse で拒否されること
       - test_strategy_config_from_manifest: manifest JSON 読込 → build_strategy_config_from_params() 呼出
       - test_three_way_target_mismatch: model=win, cli=place で sys.exit(1)
       - test_session_manifest_strategy_fields: set_strategy_params() で4フィールドが記録される
  </action>
  <verify>
    <automated>python -m pytest tests/test_strategy_alignment.py -v</automated>
  </verify>
  <done>
    - parse_args() に --betting-target/--betting-mode/--strategy-manifest が追加され、Wide が拒否される
    - _run_predict() で manifest → strategy_config → RacePredictor 構築が BT パターンと同一
    - 3者一致検証が fail-fast する
    - DDController が shadow 記録のみで stake に反映しない (dd_shadow_only フラグ)
    - PFPVerifier が CLI 引数の target/mode を使用する
    - session_manifest に戦略パラメータが記録される
    - test_strategy_alignment.py の4テストが全て通る
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OddsBandFilter injection for PT win-target (D-08)</name>
  <files>src/backtest/race_predictor.py, scripts/run_paper_trading.py</files>
  <read_first>
    src/backtest/race_predictor.py — select_bets() メソッド全体, __init__() のパラメータ
    src/backtest/engine.py lines 410-413, 487-514, 1184-1188 — OddsBandFilter の作成・キャリブレーション・適用パターン
    src/betting/odds_band_filter.py — OddsBandFilter クラスの public API (calibrate, filter, excluded_bands)
    scripts/run_backtest.py lines 202-227 — BT CLI での manifest 読込パターン
  </read_first>
  <acceptance_criteria>
    - RacePredictor に odds_band_filter: OddsBandFilter | None コンストラクタ引数が追加されている
    - RacePredictor.select_bets() の win 分岐で odds_band_filter.filter(candidates) が呼ばれる
    - run_paper_trading.py _run_predict() で betting_target=="win" の場合のみ OddsBandFilter を構築して RacePredictor に注入する
    - PT では OddsBandFilter の再キャリブレーションを行わず、manifest の roi_threshold からデフォルト構築する
    - Place-target の場合 OddsBandFilter は注入しない (D-08)
    - OddsBandFilter の roi_threshold が session_manifest に記録される
  </acceptance_criteria>
  <behavior>
    - Test 1: RacePredictor(odds_band_filter=filter_instance) でコンストラクタが成功する
    - Test 2: win-target で odds_band_filter が注入されている場合、select_bets() 内で filter() が呼ばれる
    - Test 3: place-target または odds_band_filter=None の場合、filter() は呼ばれない
  </behavior>
  <action>
    D-08 に基づく OddsBandFilter 注入の実装:

    1. src/backtest/race_predictor.py の RacePredictor.__init__() に odds_band_filter: OddsBandFilter | None = None パラメータを追加し、self.odds_band_filter = odds_band_filter で保持する

    2. select_bets() の win 分岐 (L1273以降) に、candidates が取得された後で head(max_bets) する前に OddsBandFilter フィルタを追加:
       if self.odds_band_filter is not None and not candidates.empty:
           candidates = self.odds_band_filter.filter(candidates)
       このパターンは engine.py L1186-1188 と完全に同一

    3. scripts/run_paper_trading.py の _run_predict() で RacePredictor 構築時に:
       a. betting_target=="win" の場合: strategy_params から roi_threshold (デフォルト 1.0) を取得し OddsBandFilter(roi_threshold=roi_threshold) を構築
       b. betting_target=="place" の場合: odds_band_filter=None (D-08: place では適用しない)
       c. RacePredictor のコンストラクタに odds_band_filter を渡す

    4. PT では OddsBandFilter の calibrate() を呼ばない (D-08: 再キャリブレーション禁止)。BT の校正済み状態は除外バンド ROI閾値 として manifest 経由で注入するが、PT での追加キャリブレーションは不要 (BT で除外バンドが確定済み)

    5. test_strategy_alignment.py にテストを追加:
       - test_race_predictor_odds_band_filter_win: OddsBandFilter 注入で select_bets() がフィルタを適用する
       - test_race_predictor_no_odds_band_filter_place: place-target では OddsBandFilter が None で filter が呼ばれない
  </action>
  <verify>
    <automated>python -m pytest tests/test_strategy_alignment.py -v</automated>
  </verify>
  <done>
    - RacePredictor に odds_band_filter コンストラクタ引数が追加されている
    - select_bets() の win 分岐で OddsBandFilter.filter() が呼ばれる
    - PT では win-target のみ OddsBandFilter を注入、place では注入しない
    - PT で再キャリブレーションを行わない
    - テストが全て通る
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| strategy_manifest.json → PT | 外部JSONファイル。改竄検知はPFP SHA256で担保 |
| CLI引数 → RacePredictor | ユーザ入力。argparse choices で検証済み |
| model meta → target validation | モデル成果物。既にSHA256検証済み |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Tampering | strategy_manifest.json | mitigate | PFP SHA256検証で改竄検知 (既存verify_strategy_manifest再利用) |
| T-53-02 | Elevation | 3-way target mismatch | mitigate | fail-fast検証でmodel/manifest/CLI不整合を検出 (D-03) |
| T-53-03 | Information | DD shadow logging | accept | 診断ログはローカルファイルのみ。機密情報なし |
| T-53-SC | Tampering | pip installs | accept | 新規パッケージインストールなし。全て既存依存 |
</threat_model>

<verification>
## Strategy Alignment Verification
1. `python -m pytest tests/test_strategy_alignment.py -v` — 全テスト通過
2. `python -m pytest tests/test_pipeline_consistency.py -v` — 既存テスト回帰なし
3. `ruff check scripts/run_paper_trading.py src/backtest/race_predictor.py src/features/session_manifest.py` — リント通過
</verification>

<success_criteria>
- parse_args() に --betting-target/--betting-mode/--strategy-manifest が追加されている
- Wide 指定時に argparse が自動拒否する
- 3者一致検証が fail-fast する
- RacePredictor が strategy_params に基づいて構築される (BT パターンと同一)
- DDController が shadow 記録のみで stake に反映しない
- OddsBandFilter が win-target のみ注入される
- session_manifest に戦略パラメータが記録される
- tests/test_strategy_alignment.py の全テストが通過
</success_criteria>

<output>
Create `.planning/phases/53-strategy-alignment-live-data/53-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `RacePredictor.__init__()` 新パラメータ: `odds_band_filter: OddsBandFilter | None`, `dd_shadow_only: bool`
- `SessionManifest` 新フィールド: `betting_target`, `betting_mode`, `strategy_manifest_path`, `strategy_manifest_sha256`
- `SessionManifest.set_strategy_params()` メソッド
- `run_paper_trading.py` CLI新引数: `--betting-target`, `--betting-mode`, `--strategy-manifest`
- `tests/test_strategy_alignment.py` 新規テストファイル
