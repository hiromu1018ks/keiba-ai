---
phase: 53-strategy-alignment-live-data
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ingestion/track_condition_fetcher.py
  - tests/test_track_condition_fetcher.py
autonomous: true
requirements: [LIV-01, LIV-02]

must_haves:
  truths:
    - "JRA 公式サイトの HTML から芝クッション値・ダート含水率・芝含水率を取得できる"
    - "HTML 解析が Playwright 非依存の純粋関数で実装され、保存 HTML fixture でテストできる"
    - "HTML 構造変更を hash で検知し、必須要素が見つからない場合は例外を送出する"
    - "取得失敗時に古い値へフォールバックせず例外を送出し、呼び出し元が非ゼロ終了する"
  artifacts:
    - path: "src/ingestion/track_condition_fetcher.py"
      provides: "TrackConditionFetcherProtocol + JRATrackConditionFetcher + parse_track_condition_html()"
      contains: "TrackConditionFetcherProtocol"
    - path: "tests/test_track_condition_fetcher.py"
      provides: "パーサーとフェッチャーのユニットテスト"
  key_links:
    - from: "JRATrackConditionFetcher"
      to: "playwright.sync_api"
      via: "sync_playwright → browser → page.goto → page.content"
      pattern: "sync_playwright"
    - from: "parse_track_condition_html()"
      to: "BeautifulSoup"
      via: "DOM query: #cushion_num strong, #turf_line .gm, #dirt_line .gm"
      pattern: "parse_track_condition_html"
---

<objective>
JRA 公式サイトから芝クッション値・ダート含水率を取得する TrackConditionFetcher を実装する。

Purpose: 当日のトラック条件データを PT パイプラインで利用可能にする。Playwright 取得と純粋関数パーサーを完全分離し (D-05)、HTML 構造変更検知と取得失敗時の予測停止を実現する。
Output: src/ingestion/track_condition_fetcher.py, tests/test_track_condition_fetcher.py
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-strategy-alignment-live-data/53-CONTEXT.md
@.planning/phases/53-strategy-alignment-live-data/53-RESEARCH.md

@src/ingestion/odds_collector.py
@scripts/scrape_everydb2_manual.py
@src/features/track_condition_data.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TrackConditionFetcherProtocol + JRATrackConditionFetcher + pure function parser</name>
  <files>src/ingestion/track_condition_fetcher.py, tests/test_track_condition_fetcher.py</files>
  <read_first>
    src/ingestion/odds_collector.py — OddsFetcherProtocol + OddsCollector の Protocol-based DI パターン (D-05 の参考)
    scripts/scrape_everydb2_manual.py — 既存 Playwright パターン: browser management, page.goto(), DOM query
    src/features/track_condition_data.py — convert_track_conditions() / aggregate_to_race_level() の既存集約ロジック
    53-RESEARCH.md — JRA HTML 構造確認結果: DOM セレクタ (#cushion_num strong, #turf_line .gm, #dirt_line .gm, #moist_list option[selected], #cushion_list option[selected])
  </read_first>
  <acceptance_criteria>
    - TrackConditionFetcherProtocol が runtime_checkable な Protocol で fetch_track_conditions_html(venue_code: str) -> str メソッドを持つ
    - parse_track_condition_html() が純粋関数で Playwright に依存しない
    - parse_track_condition_html() の戻り値が dict[str, Any] で以下のキーを含む: turf_cushion, dirt_moisture_goal, dirt_moisture_4c, turf_moisture_goal, turf_moisture_4c, measured_at_moist, measured_at_cushion, html_hash
    - JRATrackConditionFetcher が Playwright sync_api を使用し HTML を取得する
    - JRATrackConditionFetcher.fetch_track_conditions_html() が Playwright エラー時に例外を送出する (フォールバックなし)
    - parse_track_condition_html() が必須要素 (#cushion_num, #turf_line, #dirt_line) のいずれかが欠落している場合に TrackConditionParseError を送出する
    - 測定時刻・取得時刻・取得元が戻り値のメタデータに含まれる
  </acceptance_criteria>
  <behavior>
    - Test 1: 有効な HTML fixture から正しい turf_cushion/dirt_moisture_goal/dirt_moisture_4c 値を抽出できる
    - Test 2: turf_cushion が None (要素なし) の場合でも残りの値を取得できる (cushion_num は一部開催場のみ)
    - Test 3: #turf_line が欠落している場合に TrackConditionParseError が送出される
    - Test 4: #dirt_line が欠落している場合に TrackConditionParseError が送出される
    - Test 5: _parse_percent("16.2%") が 16.2 を返す
    - Test 6: _parse_percent("") が None を返す
    - Test 7: html_hash が SHA256 ハッシュ文字列である
    - Test 8: 測定時刻が #moist_list option[selected] から抽出される
    - Test 9: Protocol の isinstance() チェックが通る
  </behavior>
  <action>
    D-05 に基づくフェッチャーとパーサーの実装:

    1. src/ingestion/track_condition_fetcher.py を新規作成:

    2. TrackConditionParseError(Exception) を定義。必須DOM要素欠落・HTML空文字・値解析失敗時に送出する

    3. parse_track_condition_html(html: str) -> dict[str, Any] 純粋関数:
       a. html が空文字の場合は TrackConditionParseError("Empty HTML")
       b. html_hash = hashlib.sha256(html.encode()).hexdigest()
       c. BeautifulSoup(html, "html.parser") でパース
       d. #cushion_num strong から turf_cushion (float | None) を抽出。要素なしなら None
       e. #turf_line の .gm と .c4 から turf_moisture_goal と turf_moisture_4c を抽出
       f. #dirt_line の .gm と .c4 から dirt_moisture_goal と dirt_moisture_4c を抽出
       g. #turf_line と #dirt_line が両方欠落の場合は TrackConditionParseError("Missing required elements: #turf_line and #dirt_line")
       h. #moist_list option[selected] から measured_at_moist を抽出 (文字列)
       i. #cushion_list option[selected] から measured_at_cushion を抽出 (文字列)
       j. 戻り値: 上記全キー + html_hash

    4. _parse_percent(text: str) -> float | None ヘルパー: "16.2%" → 16.2。空文字・変換失敗時は None

    5. TrackConditionFetcherProtocol (runtime_checkable Protocol):
       def fetch_track_conditions_html(self, venue_code: str) -> str: ...

    6. JRATrackConditionFetcher クラス (TrackConditionFetcherProtocol を実装):
       a. BASE_URL = "https://www.jra.go.jp/keiba/baba/"
       b. __init__(headless: bool = True, timeout_ms: int = 30000)
       c. fetch_track_conditions_html(venue_code: str) -> str:
          - sync_playwright() でブラウザ起動
          - page.goto(BASE_URL, wait_until="domcontentloaded", timeout=timeout_ms)
          - venue_code に対応するタブ/リンクをクリック (同一ページ内DOM切り替え)
          - page.wait_for_selector("#turf_line", timeout=10000) でコンテンツ待機
          - page.content() で HTML を取得して返す
          - Playwright エラー時は例外をそのまま送出 (フォールバックなし、D-05)
       d. fetch_all_venues(track_date: str) -> dict[str, dict[str, Any]]:
          - JRAサイトの開催場一覧を取得
          - 各開催場ごとに fetch_track_conditions_html() → parse_track_condition_html() を実行
          - 戻り値: {venue_code: parse結果dict}
          - 1場でも失敗した場合は例外を送出 (予測停止)

    7. _detect_html_structure_change(reference_hash: str, current_hash: str) -> bool: ハッシュ比較で構造変更を検知。パーサーが期待するセレクタが存在しない場合も構造変更とみなす

    8. tests/test_track_condition_fetcher.py を新規作成:
       - HTML fixture 文字列をテスト内に定義 (53-RESEARCH.md の確認済み DOM 構造に基づく)
       - test_parse_valid_html: 有効HTML から正しい値を抽出
       - test_parse_cushion_missing: cushion_num 要素なしでも turf_cushion=None で成功
       - test_parse_turf_line_missing: #turf_line 欠落で TrackConditionParseError
       - test_parse_dirt_line_missing: #dirt_line 欠落で TrackConditionParseError
       - test_parse_both_lines_missing: 両方欠落で TrackConditionParseError
       - test_parse_percent: _parse_percent の単体テスト (正常値、空、%なし)
       - test_html_hash_present: 戻り値に html_hash が含まれる
       - test_measured_at_extraction: 測定時刻が正しく抽出される
       - test_protocol_conformance: isinstance(JRATrackConditionFetcher(), TrackConditionFetcherProtocol) == True
       - test_parse_empty_html: 空文字で TrackConditionParseError
  </action>
  <verify>
    <automated>python -m pytest tests/test_track_condition_fetcher.py -v</automated>
  </verify>
  <done>
    - TrackConditionFetcherProtocol が Protocol-based DI で定義されている
    - parse_track_condition_html() が Playwright 非依存の純粋関数で実装されている
    - JRATrackConditionFetcher が Playwright sync_api で HTML を取得する
    - 必須DOM要素欠落時に TrackConditionParseError が送出される
    - 取得失敗時にフォールバックせず例外を送出する
    - html_hash で構造変更検知が可能
    - テストが全て通る (10テスト)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JRA website → PT | 外部HTMLソース。構造変更リスクあり |
| parse_track_condition_html() | 信頼できない入力 (外部HTML)。検証必須 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-04 | Tampering | JRA HTML response | accept | 構造変更検知 (html_hash) + 必須要素チェックで不正値を検出 |
| T-53-05 | Denial | JRA site unavailable | mitigate | 例外送出 → 予測停止 (非ゼロ終了)。フォールバックなし (D-05) |
| T-53-06 | Spoofing | DNS hijack → fake JRA page | accept | HTTPS で接続。PTは紙取引のため金銭的被害なし |
| T-53-SC | Tampering | pip installs | accept | 新規パッケージなし。playwright/bs4 は既存 |
</threat_model>

<verification>
## Live Data Fetcher Verification
1. `python -m pytest tests/test_track_condition_fetcher.py -v` — 全テスト通過
2. `ruff check src/ingestion/track_condition_fetcher.py` — リント通過
3. `python -c "from ingestion.track_condition_fetcher import TrackConditionFetcherProtocol, parse_track_condition_html, JRATrackConditionFetcher"` — インポート確認
</verification>

<success_criteria>
- TrackConditionFetcherProtocol が Protocol-based DI で定義されている
- parse_track_condition_html() が純粋関数で Playwright 非依存
- JRATrackConditionFetcher が Playwright で HTML を取得する
- 必須DOM要素欠落時に TrackConditionParseError を送出
- 取得失敗時にフォールバックせず例外を送出
- html_hash が計算される
- テストが全て通る
</success_criteria>

<output>
Create `.planning/phases/53-strategy-alignment-live-data/53-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `TrackConditionFetcherProtocol` (Protocol) in `src/ingestion/track_condition_fetcher.py`
- `JRATrackConditionFetcher` class in `src/ingestion/track_condition_fetcher.py`
- `parse_track_condition_html()` pure function in `src/ingestion/track_condition_fetcher.py`
- `_parse_percent()` helper in `src/ingestion/track_condition_fetcher.py`
- `TrackConditionParseError` exception in `src/ingestion/track_condition_fetcher.py`
- `tests/test_track_condition_fetcher.py` new test file
