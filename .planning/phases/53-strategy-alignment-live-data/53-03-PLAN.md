---
phase: 53-strategy-alignment-live-data
plan: 03
type: execute
wave: 2
depends_on: [53-01, 53-02]
files_modified:
  - src/features/feature_builder.py
  - src/features/session_manifest.py
  - scripts/run_paper_trading.py
  - src/paper_trading/predictor.py
  - tests/test_live_data_integration.py
autonomous: true
requirements: [LIV-03]

must_haves:
  truths:
    - "JRA から取得したライブ値が FeatureBuilder.build_for_inference() に渡され、履歴Parquetより優先してマージされる"
    - "ライブ生値HTMLがセッション配下にimmutableに保存される"
    - "取得元・測定時刻・取得時刻・raw HTML hash が session_manifest に記録される"
    - "含水率の集約規則が重複期間のJRA値とCSV値の照合で確定される (D-06)。照合不能なら予測を停止する"
  artifacts:
    - path: "src/features/feature_builder.py"
      provides: "live_track_conditions parameter in build_for_inference() and _merge_live_track_conditions()"
      contains: "live_track_conditions"
    - path: "src/features/session_manifest.py"
      provides: "live_data metadata fields"
      contains: "live_data"
    - path: "src/paper_trading/predictor.py"
      provides: "live_track_conditions passthrough in setup()"
      contains: "live_track_conditions"
    - path: "tests/test_live_data_integration.py"
      provides: "live data merge and session manifest extension tests"
  key_links:
    - from: "scripts/run_paper_trading.py"
      to: "src/ingestion/track_condition_fetcher.py"
      via: "JRATrackConditionFetcher → parse → merge"
      pattern: "JRATrackConditionFetcher|fetch_all_venues"
    - from: "src/features/feature_builder.py"
      to: "src/features/track_condition_features.py"
      via: "compute_track_condition_features with live override"
      pattern: "_merge_live_track_conditions"
    - from: "src/paper_trading/predictor.py"
      to: "src/features/feature_builder.py"
      via: "setup() passes live_track_conditions to builder.build_for_inference()"
      pattern: "build_for_inference.*live_track_conditions"
---

<objective>
ライブトラック条件データを FeatureBuilder に統合し、PT パイプラインで当日の馬場状態を特徴量に反映する。

Purpose: JRA から取得した当日トラック条件を FeatureBuilder.build_for_inference() に渡し、履歴 Parquet より優先してマージする (D-07)。含水率の集約規則は重複期間のJRA値とCSV値を照合して確定し、照合不能なら予測を停止する (D-06)。セッション配下に生値を保存し、session_manifest に取得メタデータを記録する。
Output: 変更済み feature_builder.py, session_manifest.py, run_paper_trading.py, predictor.py, 新規 test_live_data_integration.py
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-strategy-alignment-live-data/53-CONTEXT.md
@.planning/phases/53-strategy-alignment-live-data/53-RESEARCH.md
@.planning/phases/53-strategy-alignment-live-data/53-02-SUMMARY.md

@src/features/feature_builder.py
@src/features/track_condition_data.py
@src/features/track_condition_features.py
@src/features/session_manifest.py
@src/paper_trading/predictor.py
@scripts/run_paper_trading.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Live track condition merge in FeatureBuilder + JRA/CSV collation + session integration</name>
  <files>src/features/feature_builder.py, src/features/session_manifest.py, scripts/run_paper_trading.py, src/paper_trading/predictor.py, tests/test_live_data_integration.py</files>
  <read_first>
    src/features/feature_builder.py — build_for_inference() の現在のシグネチャと _enrich_features() 内の TrackConditionFeatures 統合箇所 (lines 341-366)
    src/features/track_condition_data.py — aggregate_to_race_level() の既存集約ロジック
    src/features/track_condition_features.py — compute_track_condition_features() の入力列要件
    src/features/session_manifest.py — SessionManifest の現在のフィールド定義
    src/paper_trading/predictor.py — setup() の現在のシグネチャ (lines 42-131)。FeatureBuilder 呼び出し箇所 (L124-130)
    scripts/run_paper_trading.py — _run_predict() の現在のフロー
    src/ingestion/track_condition_fetcher.py — Plan 02 で作成した fetcher/protocol
  </read_first>
  <acceptance_criteria>
    - FeatureBuilder.build_for_inference() に live_track_conditions: pd.DataFrame | None = None パラメータが追加されている
    - _merge_live_track_conditions(df, live_df) がマージキー race_id でライブ値を優先上書きする
    - ライブ値が NaN の場合、履歴値が保持される (NaN → 履歴値をフォールバック)
    - run_paper_trading.py _run_predict() で JRATrackConditionFetcher を呼び出し、結果を PaperPredictor.setup() 経由で FeatureBuilder に渡す
    - ライブ生値 HTML が data/paper_trading/sessions/{ymd}/live_track_condition_{venue}.html に保存される
    - session_manifest に live_data フィールド (取得元・測定時刻・取得時刻・html_hash) が記録される
    - ライブ取得失敗時に予測を停止し非ゼロ終了する
    - PaperPredictor.setup() に live_track_conditions: pd.DataFrame | None = None パラメータが追加され、builder.build_for_inference() に渡される
    - 含水率集約規則: 重複期間のJRA値とCSV値を照合して goal/4c/mean から最適規則を確定する (D-06)。照合不能なら予測を停止する
  </acceptance_criteria>
  <behavior>
    - Test 1: live_track_conditions が None の場合、_merge が呼ばれず既存動作と同一
    - Test 2: live_track_conditions に値がある場合、履歴の dirt_moisture/turf_cushion が上書きされる
    - Test 3: live 値が NaN の列は履歴値が保持される
    - Test 4: session_manifest.set_live_data() で4メタデータが記録される
    - Test 5: _merge_live_track_conditions の空 DataFrame 入力で例外なし
    - Test 6: collate_moisture_rule でJRA値とCSV値が一致する場合に正しい規則が選択される
    - Test 7: collate_moisture_rule でJRA値とCSV値が照合不能な場合に例外が送出される
  </behavior>
  <action>
    D-06, D-07 に基づくライブデータ統合の実装:

    1. src/features/feature_builder.py の変更:
       a. build_for_inference() に live_track_conditions: pd.DataFrame | None = None パラメータを追加
       b. _build() に live_track_conditions を伝播
       c. _enrich_features() 内の TrackConditionFeatures ステップ (h) の後に、live_track_conditions マージを追加:
          if live_track_conditions is not None and not live_track_conditions.empty:
              df = self._merge_live_track_conditions(df, live_track_conditions)
       d. _merge_live_track_conditions(df, live_df) を追加:
          - マージキー: race_id
          - 上書き対象列: turf_cushion, dirt_moisture (および他の TC 列があれば)
          - ロジック: df と live_df を race_id で left join。live 側の値が NaN でなければ上書き、NaN なら履歴値を保持
          - 上書き後、live_df のみに存在する race_id (新規) もマージに含める

    2. src/features/session_manifest.py の変更:
       a. live_data: dict[str, Any] = field(default_factory=dict) フィールドを追加
       b. set_live_data(source: str, measured_at: str, fetched_at: str, html_hash: str, venue_codes: list[str]) メソッドを追加
       c. to_dict() に live_data を含める

    3. scripts/run_paper_trading.py _run_predict() の変更:
       a. _run_predict() の FeatureBuilder 呼び出し前にライブデータ取得セクションを追加:
          - from ingestion.track_condition_fetcher import JRATrackConditionFetcher, parse_track_condition_html
          - JRATrackConditionFetcher(headless=True).fetch_all_venues(target_date) を呼び出し
          - 取得結果を DataFrame に変換 (venue_code → race_id マッピング)
          - 取得失敗時は例外が送出され、run_paper_trading.py が sys.exit(1) で終了
       b. 生値 HTML をセッション配下に保存:
          - sessions/{ymd}/live_track_condition_{venue}.html に venue ごとの HTML を保存
       c. session_manifest.set_live_data() を呼び出してメタデータを記録:
          - source="JRA", measured_at=パーサーから取得, fetched_at=datetime.now().isoformat(), html_hash=パーサーから取得
       d. ライブ取得結果から dirt_moisture を算出して PaperPredictor.setup() の live_track_conditions パラメータに渡す

    4. PaperPredictor.setup() の変更:
       a. setup() に live_track_conditions: pd.DataFrame | None = None パラメータを追加
       b. builder.build_for_inference() 呼び出し (L124-130) に live_track_conditions を渡す

    5. 含水率集約規則 — JRA/CSV照合による確定 (D-06):
       a. collate_moisture_rule() 関数を新規定義 (feature_builder.py または track_condition_data.py に配置):
          - 入力: JRAライブ値 (dirt_moisture_goal, dirt_moisture_4c) と履歴CSV由来の track_conditions.parquet の対応期間データ
          - 重複期間 (同日にJRA値とCSV値が両方存在するレース) を特定
          - 各集約規則 (goalのみ, 4cのみ, mean) を適用してCSV値と比較し、最も一致度が高い規則を選択
          - 一致判定: 誤差が閾値 (例: 0.5%) 以内なら一致。最も一致数が多い規則を採用
          - 照合結果をロガーに出力し、採用規則を session_manifest.live_data["moisture_rule"] に記録
       b. PT起動時 (run_paper_trading.py _run_predict() ライブ取得セクション内) に collate_moisture_rule() を実行:
          - track_conditions.parquet から当日のCSV由来 dirt_moisture を読み込み
          - JRAライブ取得値と照合
          - 照合に必要な重複データが存在しない場合 (初回実行やCSV対象日なし): logger.warning で警告しつつデフォルト規則 (mean) を使用。ただし session_manifest に "collation_status": "insufficient_data" を記録
          - 重複データが存在するが全規則で一致度が閾値未満の場合: logger.error + 予測停止 (sys.exit(1))
       c. 照合確定後、採用規則に基づいて dirt_moisture を算出:
          - "goal" 規則: dirt_moisture = dirt_moisture_goal
          - "4c" 規則: dirt_moisture = dirt_moisture_4c
          - "mean" 規則: dirt_moisture = (goal + 4c) / 2
          - 片方のみ非NaNの場合はその値を使用

    6. tests/test_live_data_integration.py を新規作成:
       - test_merge_live_overrides_history: ライブ値が履歴を上書きする
       - test_merge_live_nan_preserves_history: ライブNaN は履歴値を保持
       - test_merge_empty_live_df: 空 DataFrame で変更なし
       - test_merge_none_live: None で変更なし
       - test_session_manifest_live_data: set_live_data() でフィールドが記録される
       - test_dirt_moisture_aggregation_goal: goal 規則で正しい値
       - test_dirt_moisture_aggregation_4c: 4c 規則で正しい値
       - test_dirt_moisture_aggregation_mean: mean 規則で正しい値
       - test_collate_moisture_rule_selects_best: 照合で最適規則が選択される
       - test_collate_moisture_rule_insufficient_data: 重複データ不足でデフォルト規則 + 警告
       - test_collate_moisture_rule_mismatch_halts: 全規則不一致で予測停止
       - test_live_failure_stops_prediction: 取得失敗時に sys.exit(1) を期待
       - test_predictor_setup_passes_live_conditions: PaperPredictor.setup() が live_track_conditions を build_for_inference() に渡す
  </action>
  <verify>
    <automated>python -m pytest tests/test_live_data_integration.py -v</automated>
  </verify>
  <done>
    - FeatureBuilder.build_for_inference() に live_track_conditions パラメータが追加されている
    - _merge_live_track_conditions() が race_id マージでライブ値優先上書きする
    - run_paper_trading.py で JRATrackConditionFetcher を呼び出し、結果を PaperPredictor.setup() 経由で FeatureBuilder に渡す
    - PaperPredictor.setup() に live_track_conditions パラメータが追加されている
    - 生値 HTML がセッション配下に保存される
    - session_manifest に取得メタデータが記録される
    - 含水率集約規則が JRA/CSV 照合で確定される (D-06)
    - 照合不能時に予測が停止する (D-06)
    - 取得失敗時に予測が停止する
    - テストが全て通る (13テスト)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JRA HTML → parse → FeatureBuilder | 外部データ→内部特徴量。検証必須 |
| live DataFrame → FeatureBuilder merge | ライブ値が履歴を上書きする。NaN安全性必須 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-07 | Tampering | Live data injection | mitigate | html_hash で検知。NaN フォールバックで不正値の影響限定 |
| T-53-08 | Denial | JRA site down on race day | mitigate | 例外→予測停止 (非ゼロ終了)。不完全予測より予測なしを選択 |
| T-53-09 | Information | Session HTML artifacts | accept | ローカルファイルのみ。公開サーバーへのアップロードなし |
| T-53-10 | Tampering | Moisture rule collation | mitigate | JRA/CSV照合で規則を自動確定。照合不能なら予測停止 (D-06) |
| T-53-SC | Tampering | pip installs | accept | 新規パッケージなし |
</threat_model>

<verification>
## Live Data Integration Verification
1. `python -m pytest tests/test_live_data_integration.py -v` — 全テスト通過
2. `python -m pytest tests/test_strategy_alignment.py -v` — 回帰なし (Plan 01 のテスト)
3. `python -m pytest tests/test_track_condition_fetcher.py -v` — 回帰なし (Plan 02 のテスト)
4. `ruff check src/features/feature_builder.py src/features/session_manifest.py scripts/run_paper_trading.py src/paper_trading/predictor.py` — リント通過
</verification>

<success_criteria>
- FeatureBuilder.build_for_inference() に live_track_conditions パラメータが追加されている
- _merge_live_track_conditions() がライブ値優先で NaN 安全にマージする
- PaperPredictor.setup() に live_track_conditions パラメータが追加されている
- run_paper_trading.py でライブ取得→PaperPredictor→FeatureBuilder 注入が動作する
- 生値 HTML がセッション配下に保存される
- session_manifest に取得メタデータが記録される
- 含水率集約規則が JRA/CSV 照合で確定される (D-06)
- 照合不能時に予測停止する (D-06)
- 取得失敗時に予測停止 (非ゼロ終了) する
- 全テストが通過する
</success_criteria>

<output>
Create `.planning/phases/53-strategy-alignment-live-data/53-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `FeatureBuilder.build_for_inference()` 新パラメータ: `live_track_conditions: pd.DataFrame | None`
- `FeatureBuilder._merge_live_track_conditions()` メソッド
- `collate_moisture_rule()` 関数: JRA/CSV照合による含水率集約規則の確定 (D-06)
- `SessionManifest.live_data` フィールド: `dict[str, Any]`
- `SessionManifest.set_live_data()` メソッド
- `PaperPredictor.setup()` 新パラメータ: `live_track_conditions: pd.DataFrame | None`
- `run_paper_trading.py` ライブデータ取得セクション
- `tests/test_live_data_integration.py` 新規テストファイル
